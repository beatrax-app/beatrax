<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple Computer//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    LaunchAgent: Beatrax Laravel scheduler.

    Runs `php artisan schedule:work` indefinitely — a long-lived
    foreground daemon that wakes once per minute and fires whatever
    routes/console.php has registered (email-scan.incremental hourly,
    email-scan.discovery daily, plus any future entries).

    Placeholders — same substitution shape as the Horizon plist; the
    InstallCommand --launchd path swaps both at install time.

    KeepAlive + ThrottleInterval match the Horizon plist verbatim so
    a misbehaving scheduler can never spin in a tight crash-restart
    loop.
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.beatrax.scheduler</string>

    <key>ProgramArguments</key>
    <array>
        <string>{{ABS_PHP_BINARY}}</string>
        <string>{{ABS_PROJECT_ROOT}}/artisan</string>
        <string>schedule:work</string>
    </array>

    <key>WorkingDirectory</key>
    <string>{{ABS_PROJECT_ROOT}}</string>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <dict>
        <key>Crashed</key>
        <true/>
        <key>SuccessfulExit</key>
        <false/>
    </dict>

    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>StandardOutPath</key>
    <string>{{ABS_PROJECT_ROOT}}/storage/logs/launchd-scheduler.log</string>

    <key>StandardErrorPath</key>
    <string>{{ABS_PROJECT_ROOT}}/storage/logs/launchd-scheduler.err.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/usr/local/bin:/usr/bin:/bin:/opt/homebrew/bin</string>
    </dict>
</dict>
</plist>
