# Tag-triggered installer pipeline. Push a v*.*.* tag for a stable
# draft release; push any tag with a semver prerelease suffix
# (e.g. v1.0.0-beta, v1.0.0-rc.1, v1.0.0-alpha.2) for an immediate
# preview release.
#
# Workflow shape: a quality gate (the same Pint + Larastan + Pest
# matrix that ci.yml runs) fails fast on a broken main before any
# platform job spins up; macOS-14 + Windows-2025 + Ubuntu-24.04 build
# in parallel; the publish job creates a GitHub Release with every
# platform artifact attached.
#
# Tag is the single source of truth for the shipped version string.
# The release workflow strips the leading `v` from the pushed tag
# (so `v0.1.0` becomes `0.1.0`) and exports it as
# NATIVEPHP_APP_VERSION before invoking `php artisan native:build`;
# the bundle reads that value via env('NATIVEPHP_APP_VERSION', ...)
# in config/nativephp.php and surfaces it through the /health probe
# at runtime on real installs.
#
# Every third-party action is pinned to a full 40-char commit SHA;
# tag-only references are a supply-chain risk because mutable tags
# can be repointed at malicious commits without the workflow file
# changing. The trigger is `push: tags: [v*]` only — never
# pull_request_target — so fork PRs cannot reach this workflow.
#
# Auto-update integrity chain:
#   1. macOS builds register the `nativephp_inject_macos_update_settings`
#      prebuild hook so the generated electron-builder config carries
#      `disableDifferentialDownload: true` — full-binary auto-update
#      bypasses OS-signature validation that ad-hoc-signed bundles
#      would otherwise fail.
#   2. The publish job signs the publisher manifests with the Ed25519
#      private key sourced from the `ED25519_PRIVATE_KEY` repo secret;
#      the matching public hex lives in `config/auto_update.php` so
#      installed bundles verify the signature via
#      `sodium_crypto_sign_verify_detached` on every poll. Both channels'
#      sets are signed by that one key and re-verified by the same job:
#      `latest*.yml` for stable, `beta*.yml` for preview. A stable tag
#      writes both, a preview tag writes only the preview set.
#   3. A manifest binds only the one installer its `path:` names, so the
#      publish job also writes a SHA-256 checksum file over every
#      downloaded artefact and signs that with the same key. It is what
#      the `.msi`, the `.deb` and the `.apk` are checked against.
#   4. The verify-published job re-downloads the published manifests and
#      the checksum file, re-runs the Ed25519 verification, and asserts
#      that every asset on the page appears in the checksum file —
#      catches CDN partial-upload races, silently-skipped upload steps,
#      and an artefact nothing vouches for, before a release goes
#      "green" to humans while broken in flight.

name: release

on:
    push:
        tags: ['v*']

# Read at the top so a job that does not publish cannot write. The two that
# do — `publish`, which creates the release, and `move-preview-feed`, which
# retags it — raise it on themselves. Every other job here checks out, builds
# and uploads an artifact, none of which needs write.
permissions:
    contents: read

env:
    TZ: Europe/Amsterdam
    # Wire NativePHP's updater config to the GitHub publisher.
    # `php artisan native:build` reads these via the Updater
    # facade and JSON-encodes them as NATIVEPHP_UPDATER_CONFIG
    # for electron-builder, which makes electron-builder emit
    # the auto-update manifests (`latest.yml`, `latest-mac.yml`,
    # `latest-linux.yml`) alongside the installer artifacts.
    # Those manifests are what the Ed25519 signing step in the
    # publish job signs, and what electron-updater polls at
    # runtime in the installed bundle. The repo is public, so
    # the read-only GITHUB_TOKEN GitHub injects is enough for
    # electron-builder's pre-publish dry-runs; `-p never` keeps
    # it from pushing directly — softprops/action-gh-release
    # owns the actual upload.
    # Owner and repo are read from the GitHub context rather than
    # hardcoded, so an organisation move cannot silently point the
    # updater feed at the previous owner.
    NATIVEPHP_UPDATER_PROVIDER: github
    # Read twice from one name, and the two readers disagreed while it was
    # unset: electron-builder.mjs compares it to the STRING 'true' and so
    # dropped the `publish` block entirely — no `app-update.yml` in the bundle,
    # nothing for electron-updater to poll — while config/nativephp.php
    # defaulted it to true and had the Electron boot hook poll anyway. It is
    # set explicitly here and written into the bundled .env below so neither
    # side is left on a default. The reader's own switch narrows the runtime
    # half at `native:config` time; this only ever says the bundle HAS a feed.
    NATIVEPHP_UPDATER_ENABLED: 'true'
    GITHUB_OWNER: ${{ github.repository_owner }}
    GITHUB_REPO: ${{ github.event.repository.name }}
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    # The origin the app's own Ed25519 pre-check fetches the signed
    # publisher manifest from before any download or install (read at
    # runtime as `config('auto_update.manifest_feed_url')`). Derived from
    # the GitHub context — same reason as the owner/repo above: an
    # organisation move re-points the feed with the repo, never at a
    # hardcoded owner. `releases/latest/download` resolves the newest
    # *published* (non-draft) release, so a drafted stable release surfaces
    # nothing until a human publishes it. Appended to the bundled `.env` in
    # each desktop staging step below.
    AUTO_UPDATE_FEED_URL: https://github.com/${{ github.repository }}/releases/latest/download
    # The preview channel needs an origin of its own, because that alias also
    # skips a *prerelease* — it resolves the newest non-prerelease release, so
    # a release candidate is unreachable through it however the manifest is
    # named, and a reader on preview would be served the newest stable build.
    # A direct tag URL does serve a prerelease, so `move-preview-feed` keeps
    # one rolling `preview` release carrying the verified beta set and this
    # addresses it by name.
    AUTO_UPDATE_PREVIEW_FEED_URL: https://github.com/${{ github.repository }}/releases/download/preview
    PREVIEW_FEED_TAG: preview

jobs:
    spec-gate:
        # release-preflight.yml runs this same check, but it is
        # workflow_dispatch-only while this workflow fires on any `v*` tag
        # push — so a human pushing a tag bypassed the spec entirely and the
        # pipeline would build and draft a release for a version the spec does
        # not consider releasable. Its own job, ahead of the builds,
        # so it costs seconds and fails before anything is compiled.
        name: spec gate
        runs-on: ubuntu-latest
        timeout-minutes: 5
        steps:
            - name: Check out the canonical spec
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
              with:
                  persist-credentials: false
                  repository: beatrax-app/spec
                  ref: main
                  path: .spec-canonical

            - name: The tagged version is releasable in the spec
              env:
                  REF_NAME: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  VERSION="${REF_NAME#v}"
                  printf '%s' "$VERSION" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$' \
                    || { echo "::error::tag must be vX.Y.Z or vX.Y.Z-<prerelease>, got '${REF_NAME}'."; exit 1; }
                  export VERSION
                  python3 - <<'PY'
                  import os, sys, tomllib, pathlib
                  version = os.environ["VERSION"]
                  base, _, pre = version.partition("-")
                  path = pathlib.Path(".spec-canonical/70-operations/versions") / f"{base}.toml"
                  if not path.exists():
                      sys.exit(f"::error::no manifest at 70-operations/versions/{base}.toml — stage the version first.")
                  m = tomllib.loads(path.read_text(encoding="utf-8"))
                  status, repos, goals = m.get("status"), m.get("repos", []), m.get("goals", [])
                  if "beatrax" not in repos:
                      sys.exit(f"::error::{base}.toml does not cut this repo (repos = {repos}).")
                  # A preview tag rides a staged version — the point of a preview is to
                  # exercise goals that are not all satisfied yet. A stable tag needs
                  # every goal satisfied, which is what `releasable` means (staging.md).
                  wanted = ("staged", "releasable") if pre else ("releasable",)
                  if status not in wanted:
                      sys.exit(f"::error::{base}.toml is '{status}'; a {'preview' if pre else 'stable'} tag needs one of {wanted}.")
                  kind = f"preview ({pre})" if pre else "stable"
                  print(f"{base}.toml is '{status}' with {len(goals)} locked goals — {kind} tag accepted.")
                  PY

    gate:
        name: gate (PHP ${{ matrix.php }})
        runs-on: ubuntu-latest
        # The whole suite unsharded is ~12 minutes of Pest on its own -- the
        # three ci.yml shards sum to 11m55s and they already saturate their
        # cores, so running them in one job does not compress. 15 minutes was
        # a budget for a run that had never once reached the end.
        timeout-minutes: 30
        strategy:
            fail-fast: true
            matrix:
                php: ['8.5']
        steps:
            - name: Check out the source
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

            - name: Install poppler-utils (pdftotext)
              run: |
                  set -euo pipefail
                  # apt hangs on a bad mirror often enough to have cost a day of CI: the
                  # job sits here until its own timeout and reports as CANCELLED with no
                  # output, which reads as an externally killed run rather than a stalled
                  # download. Bounded and retried, a stall costs a minute, not the job.
                  for attempt in 1 2 3; do
                      if sudo timeout 120 apt-get update -qq -o Acquire::Retries=3 -o Acquire::http::Timeout=20 \
                          && sudo timeout 180 apt-get install -y --no-install-recommends \
                              -o Acquire::Retries=3 -o Acquire::http::Timeout=20 poppler-utils; then
                          exit 0
                      fi
                      echo "::warning::apt attempt ${attempt} stalled or failed; retrying."
                      sleep 5
                  done
                  echo "::error::poppler-utils could not be installed after three attempts."
                  exit 1

            - name: Set up PHP
              uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240  # v2.37.2
              with:
                  php-version: ${{ matrix.php }}
                  extensions: mbstring, sqlite3, pdo_sqlite, bcmath, gd, intl, xml, dom, fileinfo, tokenizer, json
                  coverage: none
                  tools: composer:v2

            - name: Cache Composer packages
              uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
              with:
                  path: ~/.composer/cache
                  key: composer-${{ runner.os }}-${{ matrix.php }}-${{ hashFiles('**/composer.lock') }}
                  restore-keys: |
                      composer-${{ runner.os }}-${{ matrix.php }}-

            - name: Install Composer dependencies
              run: composer install --prefer-dist --no-interaction --no-progress

            - name: Set up Node
              uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
              with:
                  node-version: '22'
                  cache: npm

            - name: Install npm dependencies
              run: npm ci --ignore-scripts

            - name: Build Vite assets
              run: npm run build

            - name: Prepare environment for Larastan + Pest
              run: |
                  cp .env.example .env
                  php artisan key:generate --ansi
                  mkdir -p database
                  touch database/database.sqlite
                  mkdir -p storage/app/inbox storage/framework/cache storage/framework/sessions storage/framework/views bootstrap/cache
                  chmod -R u+rwX storage bootstrap/cache

            # ci.yml restores both of these and records what they are worth:
            # Pint 175s to 4s, PHPStan 125s to 3s. This job never got them, so
            # on the rc.1 tag it spent 207s on Pint and 38s on PHPStan out of a
            # 15 minute budget. A tag run restores the default branch's cache,
            # which ci.yml writes on every push to main.
            - name: Cache Pint results
              uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
              with:
                  path: .pint.cache
                  key: pint-${{ runner.os }}-${{ hashFiles('pint.json', '**/composer.lock') }}-${{ github.sha }}
                  restore-keys: |
                      pint-${{ runner.os }}-${{ hashFiles('pint.json', '**/composer.lock') }}-
                      pint-${{ runner.os }}-

            - name: Cache PHPStan result cache
              uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
              with:
                  path: .phpstan-cache
                  key: phpstan-${{ runner.os }}-8.5-${{ hashFiles('**/composer.lock', 'phpstan.neon') }}-${{ github.sha }}
                  restore-keys: |
                      phpstan-${{ runner.os }}-8.5-${{ hashFiles('**/composer.lock', 'phpstan.neon') }}-
                      phpstan-${{ runner.os }}-8.5-

            - name: Pint (code-style)
              run: vendor/bin/pint --test --cache-file=.pint.cache

            - name: Larastan level 10 strict
              run: vendor/bin/phpstan analyse --memory-limit=1G --no-progress

            # Serial, this died at test 2,728 of 17,667 with exit 2 and no
            # summary -- a red job carrying no test output at all, which reads
            # as infrastructure flake rather than a gate. Parallel is what
            # ci.yml and coverage.yml already run, and with no --testsuite it
            # takes every suite in one job rather than a shard's share.
            - name: Pest (full suite)
              env:
                  APP_ENV: testing
              run: php artisan test --parallel

    build-macos:
        # macOS Intel (x64) is intentionally skipped from the
        # prebuilt installer pipeline. GitHub deprecated all
        # native macOS-Intel hosted runners earlier this year,
        # so an x64 leg on macos-14 (Apple Silicon) has to run
        # @electron/rebuild under Rosetta 2 — observed wall-clock
        # is 18–25+ min vs ~9 min native arm64, and the silent
        # subprocesses regularly trip the job timeout. Intel Mac
        # users build from source per the README's
        # "Installing on macOS" section. arm64 is the only
        # prebuilt arch shipped from CI.
        name: build (macOS, arm64)
        needs: [gate, spec-gate]
        runs-on: macos-14
        timeout-minutes: 20
        steps:
            - name: Check out the source
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

            - name: Set up PHP
              uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240  # v2.37.2
              with:
                  php-version: '8.5'
                  extensions: mbstring, sqlite3, pdo_sqlite, bcmath, gd, intl, xml, dom, fileinfo, tokenizer, json
                  coverage: none
                  tools: composer:v2

            - name: Install Composer dependencies
              run: composer install --prefer-dist --no-interaction --no-progress

            - name: Install npm dependencies
              run: npm ci --ignore-scripts

            - name: Build Vite assets
              # Blade layouts call `@vite([...])` which resolves URLs via
              # `public/build/manifest.json`. `public/build/` is
              # gitignored, so each per-platform build runner must
              # regenerate it before `native:build` copies the app tree
              # into the bundle — otherwise the shipped installer
              # boots into Laravel's `ViteManifestNotFoundException` on
              # the first request.
              run: npm run build

            # The desktop updater's Ed25519 pre-check reads the feed at runtime
            # and the boot hook reads the enabled flag, and the bundled `.env`
            # is this file — so both have to be written in before native:build
            # copies the app tree.
            - name: Stage the shipped .env
              uses: ./.github/actions/stage-shipped-env
              with:
                  app-version: ${{ github.ref_name }}
                  extra: |
                      AUTO_UPDATE_FEED_URL=${{ env.AUTO_UPDATE_FEED_URL }}
                      AUTO_UPDATE_PREVIEW_FEED_URL=${{ env.AUTO_UPDATE_PREVIEW_FEED_URL }}
                      NATIVEPHP_UPDATER_ENABLED=${{ env.NATIVEPHP_UPDATER_ENABLED }}

            - name: Stage the NativePHP electron scaffold
              # `nativephp/electron/` is gitignored — the desktop build's
              # prebuild hooks (patch-imports, inject-tray) need it on disk
              # first, and the bundler copies the project `.env` staged above
              # into the packaged runtime.
              shell: bash
              run: |
                  set -euo pipefail
                  php artisan key:generate --ansi
                  php artisan native:install --publish --no-interaction --force
                  php scripts/nativephp_supply_pdf_page_size.php

            - name: Build the macOS bundle (arm64)
              env:
                  NATIVEPHP_APP_VERSION: ${{ github.ref_name }}
                  # electron-builder imports CSC_LINK into a temporary keychain
                  # and signs with it; the APPLE_* trio is what notarize.js
                  # submits with. Absent either half the build degrades to an
                  # ad-hoc signature, which the verify step below then rejects.
                  CSC_LINK: ${{ secrets.CSC_LINK }}
                  CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
                  NATIVEPHP_MAC_IDENTITY: ${{ vars.NATIVEPHP_MAC_IDENTITY }}
                  NATIVEPHP_APPLE_ID: ${{ secrets.NATIVEPHP_APPLE_ID }}
                  NATIVEPHP_APPLE_ID_PASS: ${{ secrets.NATIVEPHP_APPLE_ID_PASS }}
                  NATIVEPHP_APPLE_TEAM_ID: ${{ vars.NATIVEPHP_APPLE_TEAM_ID }}
              run: |
                  set -euo pipefail
                  for required in CSC_LINK CSC_KEY_PASSWORD NATIVEPHP_MAC_IDENTITY NATIVEPHP_APPLE_ID NATIVEPHP_APPLE_ID_PASS NATIVEPHP_APPLE_TEAM_ID; do
                      if [ -z "${!required:-}" ]; then
                          echo "::error::${required} is empty — refusing to publish an unsigned macOS build."
                          exit 1
                      fi
                  done
                  # Strip the leading `v` so the bundle's reported version
                  # matches the tag without the prefix (v0.1.0 → 0.1.0).
                  export NATIVEPHP_APP_VERSION="${NATIVEPHP_APP_VERSION#v}"
                  php artisan native:build mac arm64

            # The identity the verify step below checks the bundle against.
            # It is not optional there: an absent one is treated exactly like
            # a wrong one, because a notarised bundle carrying the framework's
            # scaffold id is a correctly signed lie.
            - name: Resolve the bundle identifier the build was given
              id: app-identity
              run: |
                  set -euo pipefail
                  identifier=$(php artisan tinker --execute="echo config('nativephp.app_id');" 2>/dev/null | tail -n 1 | tr -d '[:space:]')
                  if [ -z "${identifier}" ]; then
                      echo "::error::could not resolve config('nativephp.app_id'); refusing to verify a bundle against an unknown identity."
                      exit 1
                  fi
                  echo "identifier=${identifier}" >> "$GITHUB_OUTPUT"

            # A green build proves nothing about the signature here: the
            # Developer ID prebuild hook's non-zero exit is swallowed by
            # NativePHP's runProcess(), electron-builder then falls back to
            # an ad-hoc signature, and notarize.js skips notarisation — all
            # three with a warning and a zero exit code. Interrogate the
            # artifact instead. Kept in lockstep with release-build.yml,
            # which carries the same step in the same position.
            - name: Verify the macOS bundle is genuinely signed
              uses: ./.github/actions/verify-signature
              with:
                  platform: macos
                  artifact-path: nativephp/electron/dist/*.dmg
                  expected-identifier: ${{ steps.app-identity.outputs.identifier }}

            # The bundle, never the exclusion list. The desktop bundler prunes
            # `vendor/bin` and the PHP binary package and nothing else, so a
            # vendored package's TLS test key rode along in every build since
            # that package was installed, and no job ever opened a desktop
            # artefact to say so. Byte-identical in both bundling workflows.
            - name: Read the built bundle for anything it must not carry
              run: |
                  set -euo pipefail
                  APP=$(find nativephp/electron/dist -maxdepth 2 -name '*.app' -print -quit)
                  if [ -z "$APP" ]; then
                      echo "::error::No .app under nativephp/electron/dist — nothing was read, so nothing is proven."
                      exit 1
                  fi
                  php artisan desktop:inspect-bundle "$APP" --no-interaction

            - name: Emit auto-update manifests (latest-mac.yml, beta-mac.yml)
              # electron-builder skips writing the publisher manifest
              # when invoked with `-p never` (NativePHP's default
              # build:* npm scripts). Switching to `-p always` would
              # have electron-builder push installers + manifest to a
              # GitHub Release directly, racing softprops/action-gh-
              # release in the publish job. Cheaper to emit the
              # manifest ourselves: compute the DMG's SHA-512
              # (base64-encoded, as electron-updater expects) plus
              # size, write the minimal UpdateInfo schema, and let
              # the existing upload-artifact + Ed25519 Sign chain
              # carry it through.
              env:
                  TAG_REF: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  cd nativephp/electron/dist
                  INSTALLER=$(find . -maxdepth 1 -name "*.dmg" -print -quit)
                  INSTALLER=${INSTALLER#./}
                  if [ -z "$INSTALLER" ]; then
                      echo "No .dmg found in nativephp/electron/dist/"
                      exit 1
                  fi
                  SHA512_B64=$(openssl dgst -sha512 -binary "$INSTALLER" | base64 | tr -d '\n')
                  SIZE=$(wc -c < "$INSTALLER" | tr -d ' ')
                  VERSION="${TAG_REF#v}"
                  RELEASE_DATE=$(date -u +%Y-%m-%dT%H:%M:%S.000Z)
                  {
                      echo "version: ${VERSION}"
                      echo "files:"
                      echo "  - url: ${INSTALLER}"
                      echo "    sha512: ${SHA512_B64}"
                      echo "    size: ${SIZE}"
                      echo "path: ${INSTALLER}"
                      echo "sha512: ${SHA512_B64}"
                      echo "releaseDate: '${RELEASE_DATE}'"
                  } > manifest.yml

                  # The channel a tag belongs to is its shape: a hyphen makes it a preview.
                  # Both names can be written from the one manifest because a stable release
                  # is also the newest build on the preview channel, while a prerelease must
                  # never appear under the name the stable channel resolves. Before this the
                  # `latest` set was written for every tag shape and the `beta` set for none,
                  # so a bundle on preview asked for a file no release had ever carried.
                  cp manifest.yml beta-mac.yml
                  case "$VERSION" in
                      *-*) echo "preview tag ${VERSION}: publishing beta-mac.yml only" ;;
                      *) cp manifest.yml latest-mac.yml ;;
                  esac
                  rm manifest.yml

                  for emitted in latest-mac.yml beta-mac.yml; do
                      [ -f "$emitted" ] || continue
                      echo "--- ${emitted} ---"
                      cat "$emitted"
                  done

            # No launch-and-probe on this leg, and the reason is measured rather
            # than inherited. `open -a` was the old probe's mistake and this one
            # avoided it: v2.0.0-rc.3 resolved CFBundleExecutable, executed the
            # binary directly, and got past every signing gate. Electron then died
            # before it could serve anything:
            #
            #   Fatal process out of memory: Failed to reserve virtual memory
            #   for CodeRange
            #
            # V8 cannot reserve its code range on a macos-14 runner. That is a
            # property of the runner, not of the bundle, and no flag this step
            # could pass changes it -- Windows and Linux launch the same shipped
            # artefact on their own runners and answer. What covers macOS is
            # installing a release on a real machine and using it, which is a
            # person's job and is recorded as such in the spec.
            - name: Upload macOS artifacts (arm64)
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
              with:
                  name: macos-arm64
                  path: |
                      nativephp/electron/dist/*.dmg
                      nativephp/electron/dist/latest-mac.yml
                      nativephp/electron/dist/beta-mac.yml
                  if-no-files-found: error

    build-windows:
        name: build (Windows)
        needs: [gate, spec-gate]
        runs-on: windows-2025
        timeout-minutes: 30
        steps:
            - name: Check out the source
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

            - name: Set up PHP
              uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240  # v2.37.2
              with:
                  php-version: '8.5'
                  # Windows additions: ext-zip + ext-sodium are needed
                  # by nativephp/desktop and the libsodium-backed
                  # auto-update signing. ext-pcntl + ext-posix are
                  # POSIX-only and cannot be installed on Windows; we
                  # tell composer to ignore those platform requirements
                  # below since the desktop runtime does not invoke the
                  # signal-handled queue worker on this platform.
                  extensions: mbstring, sqlite3, pdo_sqlite, bcmath, gd, intl, xml, dom, fileinfo, tokenizer, json, zip, sodium
                  coverage: none
                  tools: composer:v2

            - name: Install Composer dependencies
              shell: bash
              run: composer install --prefer-dist --no-interaction --no-progress --ignore-platform-req=ext-pcntl --ignore-platform-req=ext-posix

            - name: Install npm dependencies
              run: npm ci --ignore-scripts

            - name: Build Vite assets
              # See the macOS leg above — the manifest must exist on
              # this runner before `native:build` copies the app tree
              # into the bundle, or the shipped installer boots into
              # `ViteManifestNotFoundException`.
              shell: bash
              run: npm run build

            # The desktop updater's Ed25519 pre-check reads the feed at runtime
            # and the boot hook reads the enabled flag, and the bundled `.env`
            # is this file — so both have to be written in before native:build
            # copies the app tree.
            - name: Stage the shipped .env
              uses: ./.github/actions/stage-shipped-env
              with:
                  app-version: ${{ github.ref_name }}
                  extra: |
                      AUTO_UPDATE_FEED_URL=${{ env.AUTO_UPDATE_FEED_URL }}
                      AUTO_UPDATE_PREVIEW_FEED_URL=${{ env.AUTO_UPDATE_PREVIEW_FEED_URL }}
                      NATIVEPHP_UPDATER_ENABLED=${{ env.NATIVEPHP_UPDATER_ENABLED }}

            - name: Stage the NativePHP electron scaffold
              # `nativephp/electron/` is gitignored — the desktop build's
              # prebuild hooks (patch-imports, inject-tray) need it on disk
              # first, and the bundler copies the project `.env` staged above
              # into the packaged runtime.
              shell: bash
              run: |
                  set -euo pipefail
                  php artisan key:generate --ansi
                  php artisan native:install --publish --no-interaction --force
                  php scripts/nativephp_supply_pdf_page_size.php

            - name: Build the Windows bundle
              shell: bash
              env:
                  NATIVEPHP_APP_VERSION: ${{ github.ref_name }}
                  # Azure Trusted Signing. electron-builder.mjs omits
                  # azureSignOptions entirely when any one of the three
                  # NATIVEPHP_AZURE_* values is empty, so a missing secret
                  # ships an UNSIGNED installer from a green build — hence the
                  # guard below rather than trusting the build to complain.
                  NATIVEPHP_AZURE_ENDPOINT: ${{ vars.NATIVEPHP_AZURE_ENDPOINT }}
                  NATIVEPHP_AZURE_CODE_SIGNING_ACCOUNT_NAME: ${{ vars.NATIVEPHP_AZURE_CODE_SIGNING_ACCOUNT_NAME }}
                  NATIVEPHP_AZURE_CERTIFICATE_PROFILE_NAME: ${{ vars.NATIVEPHP_AZURE_CERTIFICATE_PROFILE_NAME }}
                  NATIVEPHP_AZURE_PUBLISHER_NAME: ${{ vars.NATIVEPHP_AZURE_PUBLISHER_NAME }}
                  AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
                  AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
                  AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
              run: |
                  set -euo pipefail
                  for required in NATIVEPHP_AZURE_ENDPOINT NATIVEPHP_AZURE_CODE_SIGNING_ACCOUNT_NAME NATIVEPHP_AZURE_CERTIFICATE_PROFILE_NAME NATIVEPHP_AZURE_PUBLISHER_NAME AZURE_TENANT_ID AZURE_CLIENT_ID AZURE_CLIENT_SECRET; do
                      if [ -z "${!required:-}" ]; then
                          echo "::error::${required} is empty — refusing to publish an unsigned Windows build."
                          exit 1
                      fi
                  done
                  export NATIVEPHP_APP_VERSION="${NATIVEPHP_APP_VERSION#v}"
                  php artisan native:build

            # The guard above proves the six Azure values reached the build;
            # only the artifact proves electron-builder actually signed with
            # them. Kept in lockstep with release-build.yml.
            - name: Verify the Windows installer is genuinely signed
              uses: ./.github/actions/verify-signature
              with:
                  platform: windows
                  artifact-path: nativephp/electron/dist/*.exe

            # The installer above is one file. Microsoft Store certification is
            # per file, and a DLL beside a signed executable can be replaced
            # without the installer's signature saying anything. Reads the
            # package rather than the config: `win.signExts` is what makes
            # electron-builder sign more than .exe, and only the artifact says
            # whether it did.
            - name: Refuse a package carrying a binary nothing signed
              shell: pwsh
              run: |
                  $ErrorActionPreference = 'Stop'
                  $roots = Get-ChildItem -Path 'nativephp/electron/dist' -Directory -Filter '*unpacked*'
                  if ($roots.Count -eq 0) {
                      Write-Output '::error::No *unpacked* directory under nativephp/electron/dist — nothing was read, so nothing is proven.'
                      exit 1
                  }
                  $checked = 0
                  $unsigned = @()
                  foreach ($root in $roots) {
                      Get-ChildItem -Path $root.FullName -Recurse -File -Include *.exe,*.dll,*.node | ForEach-Object {
                          $checked++
                          $status = (Get-AuthenticodeSignature $_.FullName).Status
                          if ($status -ne 'Valid') {
                              $unsigned += ('{0} [{1}]' -f $_.FullName, $status)
                          }
                      }
                  }
                  # A zero here would mean the walk found nothing and passed,
                  # which reads identically to a package that is fully signed.
                  if ($checked -eq 0) {
                      Write-Output '::error::Walked the package and found no PE files at all — the path is wrong, not the package clean.'
                      exit 1
                  }
                  Write-Output "Checked $checked binaries across $($roots.Count) unpacked tree(s)."
                  if ($unsigned.Count -gt 0) {
                      foreach ($file in $unsigned) { Write-Output "::error::unsigned: $file" }
                      Write-Output "::error::$($unsigned.Count) of $checked binaries in the package are not validly signed. A store rejects the package for any one of them."
                      exit 1
                  }

            # The same read as the macOS leg, over the tree NSIS packs. The
            # installer's own bytes are not opened here, so a file added at
            # packaging time would not be seen; everything the application tree
            # carries is. Byte-identical in both bundling workflows.
            - name: Read the built package for anything it must not carry
              shell: bash
              run: |
                  set -euo pipefail
                  ROOT=$(find nativephp/electron/dist -maxdepth 1 -type d -name '*unpacked*' -print -quit)
                  if [ -z "$ROOT" ]; then
                      echo "::error::No *unpacked* directory under nativephp/electron/dist — nothing was read, so nothing is proven."
                      exit 1
                  fi
                  php artisan desktop:inspect-bundle "$ROOT" --no-interaction

            - name: Emit auto-update manifests (latest.yml, beta.yml)
              shell: bash
              env:
                  TAG_REF: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  cd nativephp/electron/dist
                  INSTALLER=$(find . -maxdepth 1 -name "*.exe" -print -quit)
                  INSTALLER=${INSTALLER#./}
                  if [ -z "$INSTALLER" ]; then
                      echo "No .exe found in nativephp/electron/dist/"
                      exit 1
                  fi
                  SHA512_B64=$(openssl dgst -sha512 -binary "$INSTALLER" | base64 | tr -d '\n')
                  SIZE=$(wc -c < "$INSTALLER" | tr -d ' ')
                  VERSION="${TAG_REF#v}"
                  RELEASE_DATE=$(date -u +%Y-%m-%dT%H:%M:%S.000Z)
                  {
                      echo "version: ${VERSION}"
                      echo "files:"
                      echo "  - url: ${INSTALLER}"
                      echo "    sha512: ${SHA512_B64}"
                      echo "    size: ${SIZE}"
                      echo "path: ${INSTALLER}"
                      echo "sha512: ${SHA512_B64}"
                      echo "releaseDate: '${RELEASE_DATE}'"
                  } > manifest.yml

                  # The channel a tag belongs to is its shape: a hyphen makes it a preview.
                  # Both names can be written from the one manifest because a stable release
                  # is also the newest build on the preview channel, while a prerelease must
                  # never appear under the name the stable channel resolves. Before this the
                  # `latest` set was written for every tag shape and the `beta` set for none,
                  # so a bundle on preview asked for a file no release had ever carried.
                  cp manifest.yml beta.yml
                  case "$VERSION" in
                      *-*) echo "preview tag ${VERSION}: publishing beta.yml only" ;;
                      *) cp manifest.yml latest.yml ;;
                  esac
                  rm manifest.yml

                  for emitted in latest.yml beta.yml; do
                      [ -f "$emitted" ] || continue
                      echo "--- ${emitted} ---"
                      cat "$emitted"
                  done

            # The probe removed in 8dd6811d4 installed the NSIS package and then
            # launched what it installed, which is where that commit's timing
            # trouble lived. The unpacked tree holds the same binaries the
            # installer lays down -- the signature walk above reads exactly this
            # tree -- so launching it drops the installer from the path without
            # dropping what is under test.
            - name: Launch the unpacked package and ask it for /health
              shell: bash
              env:
                  REF_NAME: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  ROOT=$(find nativephp/electron/dist -maxdepth 1 -type d -name '*unpacked*' -print -quit)
                  [ -n "$ROOT" ] || { echo "::error::No *unpacked* directory under nativephp/electron/dist to smoke-test."; exit 1; }
                  # Exactly one, rather than the first `find` happens to return: if a
                  # build ever puts a second executable at the root, this says so
                  # instead of silently smoke-testing whichever one came back first.
                  mapfile -t EXES < <(find "$ROOT" -maxdepth 1 -type f -name '*.exe')
                  if [ "${#EXES[@]}" -ne 1 ]; then
                      echo "::error::${ROOT} carries ${#EXES[@]} executables at its root; expected exactly one to launch."
                      printf '::error::  %s\n' "${EXES[@]}"
                      exit 1
                  fi
                  scripts/desktop_smoke.sh "${REF_NAME#v}" "${EXES[0]}"

            - name: Upload Windows artifacts
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
              with:
                  name: windows
                  path: |
                      nativephp/electron/dist/*.exe
                      nativephp/electron/dist/*.msi
                      nativephp/electron/dist/latest.yml
                      nativephp/electron/dist/beta.yml
                  if-no-files-found: error

    build-linux:
        name: build (Linux)
        needs: [gate, spec-gate]
        runs-on: ubuntu-24.04
        timeout-minutes: 30
        steps:
            - name: Check out the source
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

            - name: Set up PHP
              uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240  # v2.37.2
              with:
                  php-version: '8.5'
                  extensions: mbstring, sqlite3, pdo_sqlite, bcmath, gd, intl, xml, dom, fileinfo, tokenizer, json
                  coverage: none
                  tools: composer:v2

            - name: Install Composer dependencies
              run: composer install --prefer-dist --no-interaction --no-progress

            - name: Install npm dependencies
              run: npm ci --ignore-scripts

            - name: Build Vite assets
              # See the macOS leg above — the manifest must exist on
              # this runner before `native:build` copies the app tree
              # into the bundle, or the shipped installer boots into
              # `ViteManifestNotFoundException`.
              run: npm run build

            # The desktop updater's Ed25519 pre-check reads the feed at runtime
            # and the boot hook reads the enabled flag, and the bundled `.env`
            # is this file — so both have to be written in before native:build
            # copies the app tree.
            - name: Stage the shipped .env
              uses: ./.github/actions/stage-shipped-env
              with:
                  app-version: ${{ github.ref_name }}
                  extra: |
                      AUTO_UPDATE_FEED_URL=${{ env.AUTO_UPDATE_FEED_URL }}
                      AUTO_UPDATE_PREVIEW_FEED_URL=${{ env.AUTO_UPDATE_PREVIEW_FEED_URL }}
                      NATIVEPHP_UPDATER_ENABLED=${{ env.NATIVEPHP_UPDATER_ENABLED }}

            - name: Stage the NativePHP electron scaffold
              # `nativephp/electron/` is gitignored — the desktop build's
              # prebuild hooks (patch-imports, inject-tray) need it on disk
              # first, and the bundler copies the project `.env` staged above
              # into the packaged runtime.
              shell: bash
              run: |
                  set -euo pipefail
                  php artisan key:generate --ansi
                  php artisan native:install --publish --no-interaction --force
                  php scripts/nativephp_supply_pdf_page_size.php

            - name: Build the Linux bundle
              env:
                  NATIVEPHP_APP_VERSION: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  export NATIVEPHP_APP_VERSION="${NATIVEPHP_APP_VERSION#v}"
                  php artisan native:build

            # This is the position the macOS and Windows legs hold their
            # signing guard, and Linux deliberately has none. There is no
            # desktop equivalent of Developer ID or Authenticode that a
            # `.deb` or an AppImage carries and a Linux desktop checks on
            # launch, so a signing identity here would be one nothing
            # verifies. What stands in for it is the project's own chain:
            # the Ed25519-signed manifest emitted below covers the AppImage
            # for its architecture, and the Ed25519-signed checksum file the
            # publish job writes covers every artefact. The artifact interrogation the
            # other two legs carry is a separate question and Linux has it,
            # in the step below this one.
            #
            # That leaves one thing worth asserting, because nothing else
            # does: the upload step's `if-no-files-found: error` fires only
            # when the whole path list matches nothing, so a Linux build
            # that stopped producing a `.deb` would publish an AppImage and
            # look green.
            - name: Linux carries no OS-level signature, and this is what stands in for it
              run: |
                  set -euo pipefail
                  cd nativephp/electron/dist
                  missing=()
                  # Per arch, because one name for both is how the x64 image got
                  # overwritten by the arm64 one and shipped under a name that said
                  # nothing about architecture.
                  compgen -G '*-x86_64.AppImage' > /dev/null || missing+=('*-x86_64.AppImage')
                  compgen -G '*-arm64.AppImage' > /dev/null || missing+=('*-arm64.AppImage')
                  compgen -G '*.deb' > /dev/null || missing+=('*.deb')
                  if [ "${#missing[@]}" -gt 0 ]; then
                      echo "::error::the Linux build produced no ${missing[*]}, so that artefact would be missing from the release rather than unsigned."
                      exit 1
                  fi
                  {
                      echo '### Linux integrity'
                      echo
                      echo 'No OS-level code signature, by design — Linux has no equivalent a desktop verifies.'
                      echo 'Each AppImage is covered by the Ed25519-signed manifest for its architecture'
                      echo '(latest-linux.yml on x64, latest-linux-arm64.yml on arm64); every AppImage and'
                      echo '.deb is covered by the Ed25519-signed checksum file.'
                  } >> "$GITHUB_STEP_SUMMARY"

            # The same read as the other two legs. Linux has no OS-level
            # signature to verify, but what a bundle carries is not a signing
            # question, and the AppImage and both `.deb` arches are packed from
            # the unpacked trees this walks. Byte-identical in both workflows.
            - name: Read the built packages for anything they must not carry
              run: |
                  set -euo pipefail
                  mapfile -t ROOTS < <(find nativephp/electron/dist -maxdepth 1 -type d -name '*unpacked*' | sort)
                  if [ "${#ROOTS[@]}" -eq 0 ]; then
                      echo "::error::No *unpacked* directory under nativephp/electron/dist — nothing was read, so nothing is proven."
                      exit 1
                  fi
                  for root in "${ROOTS[@]}"; do
                      php artisan desktop:inspect-bundle "$root" --no-interaction
                  done

            - name: Emit auto-update manifests, one per architecture
              env:
                  TAG_REF: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  cd nativephp/electron/dist
                  VERSION="${TAG_REF#v}"
                  RELEASE_DATE=$(date -u +%Y-%m-%dT%H:%M:%S.000Z)

                  # One manifest per architecture, and each architecture is spelled
                  # two ways. electron-builder renders ${arch} into an AppImage name
                  # the way AppImage names it -- `x86_64`, where the same macro gives
                  # `x64` for a dmg -- while electron-updater resolves
                  # latest-linux.yml on x64 and latest-linux-arm64.yml on everything
                  # else. So each row carries the file spelling and the manifest
                  # suffix, rather than deriving one from the other.
                  for row in 'x86_64:' 'arm64:-arm64'; do
                      FILE_ARCH="${row%%:*}"
                      SUFFIX="${row#*:}"
                      INSTALLER=$(find . -maxdepth 1 -name "*-${FILE_ARCH}.AppImage" -print -quit)
                      INSTALLER=${INSTALLER#./}
                      if [ -z "$INSTALLER" ]; then
                          echo "::error::No *-${FILE_ARCH}.AppImage in nativephp/electron/dist/ — an architecture with no manifest is one no desktop can update."
                          exit 1
                      fi
                      SHA512_B64=$(openssl dgst -sha512 -binary "$INSTALLER" | base64 | tr -d '\n')
                      SIZE=$(wc -c < "$INSTALLER" | tr -d ' ')
                      {
                          echo "version: ${VERSION}"
                          echo "files:"
                          echo "  - url: ${INSTALLER}"
                          echo "    sha512: ${SHA512_B64}"
                          echo "    size: ${SIZE}"
                          echo "path: ${INSTALLER}"
                          echo "sha512: ${SHA512_B64}"
                          echo "releaseDate: '${RELEASE_DATE}'"
                      } > manifest.yml

                      # The channel a tag belongs to is its shape: a hyphen makes it a preview.
                      # Both names can be written from the one manifest because a stable release
                      # is also the newest build on the preview channel, while a prerelease must
                      # never appear under the name the stable channel resolves. Before this the
                      # `latest` set was written for every tag shape and the `beta` set for none,
                      # so a bundle on preview asked for a file no release had ever carried.
                      cp manifest.yml "beta-linux${SUFFIX}.yml"
                      case "$VERSION" in
                          *-*) echo "preview tag ${VERSION}: publishing beta-linux${SUFFIX}.yml only" ;;
                          *) cp manifest.yml "latest-linux${SUFFIX}.yml" ;;
                      esac
                      rm manifest.yml
                  done

                  for emitted in latest-linux.yml beta-linux.yml latest-linux-arm64.yml beta-linux-arm64.yml; do
                      [ -f "$emitted" ] || continue
                      echo "--- ${emitted} ---"
                      cat "$emitted"
                  done

            # The checks above prove the AppImage is SHAPED right. Only
            # launching it proves it runs, which is why this sits before the
            # upload rather than after it.
            # xvfb supplies the display Electron needs, and the AppImage is
            # extracted rather than FUSE-mounted, which a hosted runner cannot do.
            - name: Launch the AppImage and ask it for /health
              env:
                  REF_NAME: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  sudo apt-get update -qq
                  sudo apt-get install -y -qq xvfb
                  # The runner is x86_64, so it is the x64 image that can be launched
                  # here. Naming the architecture is also what makes this test able to
                  # fail the way it did on v2.0.0-rc.2: one name for both arches meant
                  # `find` returned whichever survived, and it was the wrong one.
                  APPIMAGE=$(find nativephp/electron/dist -maxdepth 1 -name '*-x86_64.AppImage' -print -quit)
                  [ -n "$APPIMAGE" ] || { echo "::error::no x86_64 AppImage to smoke-test."; exit 1; }
                  chmod +x "$APPIMAGE"
                  scripts/desktop_smoke.sh "${REF_NAME#v}" \
                      xvfb-run -a "$APPIMAGE" --appimage-extract-and-run --no-sandbox

            - name: Upload Linux artifacts
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
              with:
                  name: linux
                  path: |
                      nativephp/electron/dist/*.AppImage
                      nativephp/electron/dist/*.deb
                      nativephp/electron/dist/latest-linux*.yml
                      nativephp/electron/dist/beta-linux*.yml
                  if-no-files-found: error

    build-android:
        # The public, sideloadable APK. Bifrost builds the store-shaped
        # artifacts from its own credentials; this leg exists so a signed
        # download sits alongside the desktop ones on the Release page.
        name: build (Android)
        needs: [gate, spec-gate]
        runs-on: ubuntu-24.04
        timeout-minutes: 40
        env:
            # The nativephp/mobile-* plugins are a paid, private Composer
            # repository; the mobile root cannot install without these.
            COMPOSER_AUTH: >-
                {"http-basic":{"plugins.nativephp.com":{"username":"${{ secrets.NATIVEPHP_LICENSE_EMAIL }}","password":"${{ secrets.NATIVEPHP_LICENSE_KEY }}"}}}
        steps:
            - name: Check out the source
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

            - name: Set up PHP
              uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240  # v2.37.2
              with:
                  php-version: '8.5'
                  extensions: mbstring, sqlite3, pdo_sqlite, bcmath, gd, intl, xml, dom, fileinfo, tokenizer, json
                  coverage: none
                  tools: composer:v2

            - name: Install npm dependencies
              run: npm ci --ignore-scripts

            - name: Build Vite assets
              # public/build is served on-device, so the bundle needs it before
              # the Laravel tree is copied into the APK.
              run: npm run build

            - name: Stage the shipped .env
              uses: ./.github/actions/stage-shipped-env
              with:
                  working-directory: mobile-app
                  app-version: ${{ github.ref_name }}

            - name: Lay down the rest of the mobile environment
              working-directory: mobile-app
              shell: bash
              run: |
                  set -euo pipefail
                  mkdir -p database storage/framework/cache storage/framework/sessions storage/framework/views bootstrap/cache
                  touch database/database.sqlite
                  chmod -R u+rwX storage bootstrap/cache

            - name: Install Composer dependencies (mobile-app/ root)
              working-directory: mobile-app
              run: composer install --prefer-dist --no-interaction --no-progress

            - name: Mint the mobile application key
              working-directory: mobile-app
              run: php artisan key:generate --ansi

            - name: Restore the release keystore
              env:
                  ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
              run: |
                  set -euo pipefail
                  if [ -z "${ANDROID_KEYSTORE_BASE64:-}" ]; then
                      echo "::error::ANDROID_KEYSTORE_BASE64 is empty — refusing to publish an unsigned Android build."
                      exit 1
                  fi
                  mkdir -p mobile-app/credentials
                  printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 -d > mobile-app/credentials/app-release-key.jks
                  chmod 600 mobile-app/credentials/app-release-key.jks

            - name: Build and sign the APK
              working-directory: mobile-app
              env:
                  NATIVEPHP_APP_VERSION: ${{ github.ref_name }}
                  # Read from the environment rather than passed as --flags:
                  # command-line arguments are visible in the process list.
                  ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
                  ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
                  ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
              run: |
                  set -euo pipefail
                  for required in ANDROID_KEYSTORE_PASSWORD ANDROID_KEY_ALIAS ANDROID_KEY_PASSWORD; do
                      if [ -z "${!required:-}" ]; then
                          echo "::error::${required} is empty — refusing to publish an unsigned Android build."
                          exit 1
                      fi
                  done
                  export ANDROID_KEYSTORE_FILE="$GITHUB_WORKSPACE/mobile-app/credentials/app-release-key.jks"
                  export NATIVEPHP_APP_VERSION="${NATIVEPHP_APP_VERSION#v}"
                  # mobile:package-android, never native:package directly and
                  # never native:run --build=release. native:package returns
                  # success whatever goes wrong — including with no Android
                  # project to build, which is the state a `composer install`
                  # checkout is in. The wrapper generates the project, applies
                  # the shell patches and fails unless an artifact exists.
                  php artisan mobile:package-android --build-type=release --no-interaction

            - name: Verify the APK is signed by the release key
              env:
                  EXPECTED_SHA256: ${{ vars.ANDROID_SIGNING_CERT_SHA256 }}
              run: |
                  set -euo pipefail
                  # Scoped to Gradle's release output rather than the whole
                  # mobile root, so an intermediate or stale APK elsewhere in
                  # the tree cannot stand in for the one just built.
                  apk=$(find mobile-app/nativephp/android/app/build/outputs/apk/release \
                      -name '*.apk' -not -name '*unsigned*' -print -quit 2>/dev/null || true)
                  if [ -z "$apk" ]; then
                      echo "::error::no signed APK was produced."
                      exit 1
                  fi
                  echo "verifying $apk"

                  apksigner=$(find "$ANDROID_HOME/build-tools" -name apksigner -print -quit)
                  if [ -z "$apksigner" ]; then
                      echo "::error::apksigner not found — cannot verify the signature."
                      exit 1
                  fi

                  "$apksigner" verify --verbose "$apk"

                  # apksigner verify passes on a DEBUG-signed APK too, so the
                  # signer identity is the check that matters.
                  actual=$("$apksigner" verify --print-certs "$apk" \
                      | grep -i 'SHA-256 digest:' | head -1 \
                      | sed 's/.*digest: *//' | tr -d ' :' | tr '[:lower:]' '[:upper:]')
                  expected=$(printf '%s' "$EXPECTED_SHA256" | tr -d ' :' | tr '[:lower:]' '[:upper:]')

                  if [ -z "$expected" ]; then
                      echo "::error::ANDROID_SIGNING_CERT_SHA256 is not set — cannot prove which key signed this."
                      exit 1
                  fi
                  if [ "$actual" != "$expected" ]; then
                      echo "::error::APK is signed by an unexpected key."
                      exit 1
                  fi
                  echo "signed by the expected release key"

                  # Copied out of app/build before the store bundle is built: every
                  # native:package run calls prepareAndroidBuild(), whose
                  # cleanGradleCache() removes that directory whole, so the second
                  # build deletes the APK this one just verified.
                  mkdir -p android-artifacts
                  cp "$apk" android-artifacts/
                  echo "APK_PATH=android-artifacts/$(basename "$apk")" >> "$GITHUB_ENV"

            - name: Build and sign the store bundle
              working-directory: mobile-app
              env:
                  NATIVEPHP_APP_VERSION: ${{ github.ref_name }}
                  ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
                  ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
                  ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
              run: |
                  set -euo pipefail
                  export ANDROID_KEYSTORE_FILE="$GITHUB_WORKSPACE/mobile-app/credentials/app-release-key.jks"
                  export NATIVEPHP_APP_VERSION="${NATIVEPHP_APP_VERSION#v}"
                  # Play takes an AAB and refuses an APK, so the sideloadable
                  # artifact above is not a submission — it is the direct
                  # download, and both ship. Same command and the same identity
                  # checks against the shipped artifact, over the project the
                  # APK run already generated and patched.
                  php artisan mobile:package-android --build-type=bundle --no-interaction

            - name: Verify the store bundle is signed by the release key
              env:
                  EXPECTED_SHA256: ${{ vars.ANDROID_SIGNING_CERT_SHA256 }}
              run: |
                  set -euo pipefail
                  aab=mobile-app/nativephp/android/app/build/outputs/bundle/release/app-release.aab
                  if [ ! -s "$aab" ]; then
                      echo "::error::no signed AAB was produced."
                      exit 1
                  fi

                  # apksigner cannot read an AAB: it is a JAR-signed archive,
                  # so the certificate comes out of keytool instead. What
                  # keytool prints is the SHA-256 of the same DER certificate
                  # apksigner digests for the APK, so one recorded value
                  # answers for both artifacts.
                  actual=$(keytool -printcert -jarfile "$aab" \
                      | grep -i 'SHA256:' | head -1 \
                      | sed 's/.*SHA256: *//' | tr -d ' :' | tr '[:lower:]' '[:upper:]')
                  expected=$(printf '%s' "$EXPECTED_SHA256" | tr -d ' :' | tr '[:lower:]' '[:upper:]')

                  if [ -z "$expected" ]; then
                      echo "::error::ANDROID_SIGNING_CERT_SHA256 is not set — cannot prove which key signed this."
                      exit 1
                  fi
                  # An unsigned AAB prints no certificate at all, which an
                  # equality check alone would read as a mismatch rather than
                  # as the worse thing it is.
                  if [ -z "$actual" ]; then
                      echo "::error::the AAB carries no certificate — it is unsigned."
                      exit 1
                  fi
                  if [ "$actual" != "$expected" ]; then
                      echo "::error::AAB is signed by an unexpected key."
                      exit 1
                  fi
                  echo "store bundle signed by the expected release key"

                  # Beside the APK, so the two paths read for what they must not carry
                  # and the two paths uploaded are the same two files.
                  mkdir -p android-artifacts
                  cp "$aab" android-artifacts/
                  echo "AAB_PATH=android-artifacts/$(basename "$aab")" >> "$GITHUB_ENV"

            - name: Read the built artifacts for anything they must not carry
              working-directory: mobile-app
              run: |
                  set -euo pipefail
                  # The artifact, never the exclusion list. gitignore bounds
                  # what reaches git and a cleanup list is a claim about a
                  # build; a shipped iPhone build carried the build machine's
                  # own encrypted sync identities under both.
                  php artisan mobile:inspect-bundle "$GITHUB_WORKSPACE/$APK_PATH" --no-interaction
                  php artisan mobile:inspect-bundle "$GITHUB_WORKSPACE/$AAB_PATH" --no-interaction

            - name: Read the permissions the merged artifact requests
              run: |
                  set -euo pipefail
                  aapt2=$(find "$ANDROID_HOME/build-tools" -name aapt2 -print -quit)
                  if [ -z "$aapt2" ]; then
                      echo "::error::aapt2 not found — cannot read the merged manifest."
                      exit 1
                  fi
                  # The APK's own manifest, after Gradle merged every plugin's
                  # into it. The strip script edits the app manifest and runs
                  # before that merge, so what it can prove and what actually
                  # ships are two different questions.
                  #
                  # xmltree rather than `dump permissions`: only this one prints
                  # protectionLevel, and the level is the whole difference
                  # between a permission the app asks of the world and one it
                  # locks its own receivers with. androidx.core mints one of
                  # the latter, and it is not a line this product can allow by
                  # naming a consumer, because its consumer is the shell.
                  "$aapt2" dump xmltree "$APK_PATH" --file AndroidManifest.xml > "$RUNNER_TEMP/permissions.txt"
                  cat "$RUNNER_TEMP/permissions.txt"

            - name: Refuse a permission this product makes no use for
              working-directory: mobile-app
              run: php artisan mobile:check-permissions "$RUNNER_TEMP/permissions.txt" --no-interaction

            - name: Upload Android artifacts
              uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
              with:
                  name: android
                  # Both shapes: the APK is the direct download and the AAB is
                  # what Play takes. Neither stands in for the other.
                  path: |
                      ${{ env.APK_PATH }}
                      ${{ env.AAB_PATH }}
                  if-no-files-found: error

    smoke-server:
        # The shipped self-host recipe, launched and asked for its health
        # endpoint before anything is uploaded — the shape that once shipped
        # answering 404 to everything. This used to say the four legs above
        # cannot be launched on a hosted runner. That holds for Android, which
        # needs a device; it was wrong about Electron, where xvfb supplies the
        # display — build-linux now launches its AppImage and asks it the same
        # question. macOS and Windows are owed the same step.
        name: smoke (self-host server)
        needs: [gate, spec-gate]
        uses: ./.github/workflows/deploy-smoke.yml

    publish:
        name: publish
        needs: [build-macos, build-windows, build-linux, build-android, smoke-server]
        runs-on: ubuntu-latest
        timeout-minutes: 10
        # The one job that creates the release and edits it afterwards.
        # id-token and attestations belong to the attestation step: Sigstore
        # signs with this job's OIDC identity, so the token has to be able to
        # prove which workflow it is. Both stay here rather than at the top.
        permissions:
            contents: write
            id-token: write
            attestations: write
        env:
            # The workflow-level GITHUB_REPO is NativePHP's, and it is a bare
            # repository name. git-cliff reads that same variable as the
            # fallback for --github-repo and requires OWNER/REPO, so it refused
            # 'beatrax' and took the whole v2.0.0 publish down after every
            # build had passed. Nothing in this job runs native:build, so the
            # name is safe to correct here for whatever else reads it.
            GITHUB_REPO: ${{ github.repository }}
        steps:
            - name: Check out the source
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
              with:
                  # git-cliff walks the tag history to build the release
                  # notes; a shallow clone would leave it with one commit.
                  fetch-depth: 0

            - name: Set up PHP (libsodium for manifest signing)
              uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240  # v2.37.2
              with:
                  php-version: '8.5'
                  extensions: mbstring, sodium
                  coverage: none
                  tools: composer:v2

            - name: Download every platform's artifacts
              uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
              with:
                  path: artifacts

            - name: Write the checksum file covering every published artefact
              id: checksums
              # Each publisher manifest binds exactly the one installer its
              # `path:` field names, so the `.msi`, the `.deb` and the `.apk`
              # reach the release page with nothing a downloader can check them
              # against. This list covers whatever was downloaded, so an
              # artefact added to any build job is covered the day it appears
              # rather than the day somebody remembers this step.
              #
              # Written before the signing pass below, and therefore signed by
              # it: an unsigned list of hashes is only as trustworthy as the
              # page it sits on, which is the page it exists to vouch for.
              env:
                  TAG: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  CHECKSUMS="beatrax-${TAG#v}-checksums.txt"

                  mapfile -t ASSETS < <(find artifacts -type f | sort)
                  if [ "${#ASSETS[@]}" -eq 0 ]; then
                      echo "::error::no artefacts were downloaded, so there is nothing to checksum."
                      exit 1
                  fi

                  # `shasum -c` resolves each line against the working
                  # directory, and a downloader has every asset in one flat
                  # folder. Two artefacts sharing a basename would make one
                  # line silently check the other file.
                  duplicates=$(printf '%s\n' "${ASSETS[@]}" | xargs -n1 basename | sort | uniq -d)
                  if [ -n "$duplicates" ]; then
                      echo "::error::two published artefacts share a basename, which a flat download cannot tell apart: $(printf '%s' "$duplicates" | tr '\n' ' ')"
                      exit 1
                  fi

                  mkdir -p artifacts/checksums
                  for asset in "${ASSETS[@]}"; do
                      printf '%s  %s\n' "$(sha256sum "$asset" | cut -d' ' -f1)" "$(basename "$asset")"
                  done | sort -k2 > "artifacts/checksums/${CHECKSUMS}"

                  echo "--- ${CHECKSUMS} ---"
                  cat "artifacts/checksums/${CHECKSUMS}"

                  # The attestation step reads this, and the name it needs is
                  # built from the tag with its leading `v` removed, which a
                  # workflow expression cannot do.
                  echo "path=artifacts/checksums/${CHECKSUMS}" >> "$GITHUB_OUTPUT"

            # What the Ed25519 chain does not say: it proves a manifest and a
            # checksum file came from this pipeline, not that this pipeline built
            # the binaries they name. Provenance binds each artefact to the
            # workflow, repository and commit that produced it.
            #
            # Driven from the checksum file rather than a path glob, so the
            # subjects are exactly the set that file covers -- one source of
            # truth for "what this release contains", and no glob that might
            # take in a directory or miss an artefact a build job adds later.
            - name: Attest what each build produced
              id: attest
              uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8  # v4.2.2
              with:
                  subject-checksums: ${{ steps.checksums.outputs.path }}

            - name: Carry the attestation onto the release page
              # The attestation API alone is not enough: `gh attestation verify`
              # reads it and a downloader reading the release page does not.
              env:
                  TAG: ${{ github.ref_name }}
                  BUNDLE: ${{ steps.attest.outputs.bundle-path }}
              run: |
                  set -euo pipefail
                  if [ -z "${BUNDLE}" ] || [ ! -f "${BUNDLE}" ]; then
                      echo "::error::the attestation step reported no bundle, so the release would publish without provenance."
                      exit 1
                  fi
                  mkdir -p artifacts/attestations
                  cp "${BUNDLE}" "artifacts/attestations/beatrax-${TAG#v}.intoto.jsonl"
                  echo "attestation: $(wc -c < "${BUNDLE}") bytes"

            - name: Sign the publisher manifests and the checksum file with Ed25519
              env:
                  ED25519_PRIVATE_KEY: ${{ secrets.ED25519_PRIVATE_KEY }}
              run: |
                  set -euo pipefail
                  # Locate every publisher manifest electron-updater
                  # produced, plus the checksum file written above.
                  # `actions/download-artifact` flattens each platform under
                  # `artifacts/<name>/`, so the find pass picks up
                  # `latest.yml` / `latest-mac.yml` / `latest-linux.yml`
                  # regardless of where they landed. Each `.sig` sibling is
                  # written in place next to the file it covers.
                  mapfile -t SIGNABLE < <(find artifacts -type f \( -name 'latest*.yml' -o -name 'beta*.yml' -o -name '*-checksums.txt' \))
                  if [ "${#SIGNABLE[@]}" -eq 0 ]; then
                      echo "No publisher manifests or checksum file found under artifacts/ — nothing to sign."
                      exit 1
                  fi
                  for signable in "${SIGNABLE[@]}"; do
                      echo "Signing ${signable}"
                      # shellcheck disable=SC2016  # inline PHP: $body is a PHP variable, not a shell one
                      php -r '
                          $body = file_get_contents($argv[1]);
                          if ($body === false) { fwrite(STDERR, "could not read " . $argv[1]); exit(1); }
                          $secret = sodium_hex2bin(getenv("ED25519_PRIVATE_KEY"));
                          $sig = sodium_crypto_sign_detached($body, $secret);
                          file_put_contents($argv[1] . ".sig", bin2hex($sig));
                      ' -- "$signable"
                  done

            - name: Generate release notes (git-cliff)
              id: cliff
              uses: orhun/git-cliff-action@f50e11560dce63f7c33227798f90b924471a88b5  # v4.8.0
              with:
                  config: cliff.toml
                  args: --latest --strip header

            # A release body over the API's limit is rejected outright, and
            # a major version spanning hundreds of commits gets there. Trim
            # to the last whole entry that fits and point at the full log.
            - name: Fit the release notes to the API limit
              id: notes
              env:
                  NOTES: ${{ steps.cliff.outputs.content }}
                  TAG: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  limit=124000
                  printf '%s' "$NOTES" > notes.md
                  if [ "$(wc -c < notes.md)" -gt "$limit" ]; then
                      head -c "$limit" notes.md | sed '$d' > notes.trimmed
                      printf '\n\n_Trimmed to fit. The complete log for %s is on the [commit history](%s/commits/%s)._\n' \
                          "$TAG" "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY" "$TAG" >> notes.trimmed
                      mv notes.trimmed notes.md
                      echo "Release notes trimmed to $limit bytes." >> "$GITHUB_STEP_SUMMARY"
                  fi

            - name: Publish the GitHub Release
              uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228  # v3.0.2
              with:
                  tag_name: ${{ github.ref_name }}
                  body_path: notes.md
                  # The repo has "immutable releases" enabled, which
                  # rejects asset uploads after a Release transitions
                  # to published. softprops uploads its asset list
                  # AFTER the Release is created, so a non-draft
                  # create fails immediately. Always create as draft
                  # here, then the next step flips prerelease tags
                  # (anything with a `-` suffix) to a published
                  # prerelease via `gh release edit`. Stable tags
                  # stay as draft so a human can review + publish.
                  draft: true
                  prerelease: ${{ contains(github.ref_name, '-') }}
                  # Every platform artifact, the checksum file, and the
                  # Ed25519 detached signature written beside each of the
                  # things that carry one. Installed bundles fetch the
                  # matching `.sig` alongside the manifest and verify before
                  # any download proceeds; a human verifying by hand reads
                  # the checksum file and its own `.sig`.
                  files: |
                      artifacts/**/*
                      artifacts/**/latest*.yml.sig
                      artifacts/**/beta*.yml.sig

            - name: Flip prerelease draft to published
              if: contains(github.ref_name, '-')
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  # Via env, never inline in the shell: a tag name may contain
                  # $() or backticks (git ref-format permits them), and inline
                  # ${{ }} would let a crafted tag execute in this job.
                  TAG: ${{ github.ref_name }}
                  REPO: ${{ github.repository }}
              run: |
                  set -euo pipefail
                  # Convert the draft Release softprops just created
                  # into a published prerelease. Assets land while the
                  # Release is still a draft (which immutable-releases
                  # allows); publishing here only flips the visibility
                  # bit, no asset upload happens.
                  gh release edit "$TAG" \
                      --repo "$REPO" \
                      --draft=false

    verify-published:
        name: verify published
        needs: publish
        runs-on: ubuntu-latest
        timeout-minutes: 5
        steps:
            - name: Check out the source
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

            - name: Set up PHP (libsodium for manifest verification)
              uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240  # v2.37.2
              with:
                  php-version: '8.5'
                  extensions: mbstring, sodium
                  coverage: none
                  tools: composer:v2

            - name: Download published manifests, checksum file + signatures
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  # Via env, never inline: a crafted tag name must not reach the
                  # shell as executable $()/backtick text.
                  TAG: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  mkdir -p verify
                  gh release download "$TAG" \
                      --dir verify \
                      --pattern 'latest*.yml' \
                      --pattern 'latest*.yml.sig' \
                      --pattern 'beta*.yml' \
                      --pattern 'beta*.yml.sig' \
                      --pattern '*-checksums.txt' \
                      --pattern '*-checksums.txt.sig'

            - name: Re-verify every signed file with sodium_crypto_sign_verify_detached
              run: |
                  set -euo pipefail
                  # Read the publisher public hex from the committed
                  # config file — the source of truth installed bundles
                  # verify against. A simple grep avoids booting the
                  # framework just to read one config value.
                  PUBLIC_HEX=$(grep -oE "'[0-9a-f]{64}'" config/auto_update.php | head -1 | tr -d "'")
                  if [ -z "$PUBLIC_HEX" ]; then
                      echo "Could not extract publisher_public_key_hex from config/auto_update.php"
                      exit 1
                  fi
                  mapfile -t SIGNED < <(find verify -type f -not -name '*.sig' \( -name 'latest*.yml' -o -name 'beta*.yml' -o -name '*-checksums.txt' \))
                  if [ "${#SIGNED[@]}" -eq 0 ]; then
                      echo "Neither a manifest nor a checksum file was downloaded — verification cannot run."
                      exit 1
                  fi
                  for signed in "${SIGNED[@]}"; do
                      sig_file="${signed}.sig"
                      if [ ! -f "$sig_file" ]; then
                          echo "Missing signature for ${signed}"
                          exit 1
                      fi
                      echo "Verifying ${signed}"
                      # shellcheck disable=SC2016  # inline PHP: $body is a PHP variable, not a shell one
                      PUBLIC_HEX="$PUBLIC_HEX" php -r '
                          $body = file_get_contents($argv[1]);
                          $sigHex = trim((string) file_get_contents($argv[1] . ".sig"));
                          $public = sodium_hex2bin(getenv("PUBLIC_HEX"));
                          $sig = sodium_hex2bin($sigHex);
                          exit(sodium_crypto_sign_verify_detached($sig, $body, $public) ? 0 : 1);
                      ' -- "$signed"
                  done
                  echo "Every published manifest and the checksum file verify cleanly against the embedded publisher public key."

            - name: Every published asset appears in the signed checksum file
              # A checksum file that quietly stopped covering one artefact is
              # indistinguishable, on the page, from one that covers them all.
              # The page itself is the only honest input here, so the asset
              # list is read back from the release rather than from the tree
              # the publish job happened to hold.
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  TAG: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  CHECKSUMS=$(find verify -type f -name '*-checksums.txt' -print -quit)
                  if [ -z "$CHECKSUMS" ]; then
                      echo "::error::the release carries no checksum file, so every artefact without a manifest entry has nothing to verify against."
                      exit 1
                  fi

                  mapfile -t COVERED < <(sed -E 's/^[0-9a-f]{64}  //' "$CHECKSUMS")

                  uncovered=()
                  while IFS= read -r asset; do
                      # A detached signature is checked by the key, not by a
                      # hash, and the checksum file cannot list itself.
                      case "$asset" in
                          *.sig | *.intoto.jsonl | *-checksums.txt) continue ;;
                      esac
                      found=""
                      for name in "${COVERED[@]}"; do
                          if [ "$name" = "$asset" ]; then
                              found="yes"
                              break
                          fi
                      done
                      if [ -z "$found" ]; then
                          uncovered+=("$asset")
                      fi
                  done < <(gh release view "$TAG" --json assets --jq '.assets[].name')

                  if [ "${#uncovered[@]}" -gt 0 ]; then
                      echo "::error::published with no entry in ${CHECKSUMS##*/}, so a downloader has nothing to check them against: ${uncovered[*]}"
                      exit 1
                  fi
                  echo "Every published asset is covered by the signed checksum file."

    # `releases/latest/download` resolves the newest NON-prerelease release, so
    # the preview manifests published on a release-candidate page cannot be
    # reached through it however they are named. This keeps one rolling
    # `preview` release carrying the current preview set, which a direct tag
    # URL does serve, and it runs after verify-published so the feed only ever
    # points at bytes that have already been re-verified against the embedded
    # publisher key.
    move-preview-feed:
        name: move the preview feed
        needs: verify-published
        # A hyphen in the tag is what makes it a prerelease, the same test the
        # manifest-emitting steps use. A stable release also writes the preview
        # set on its own page, and the rolling tag is left where it is: a
        # preview reader is not moved backwards onto a stable build.
        if: contains(github.ref_name, '-')
        runs-on: ubuntu-latest
        timeout-minutes: 5
        permissions:
            contents: write
        steps:
            - name: Check out the source
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

            - name: Set up PHP (libsodium for manifest verification)
              uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240  # v2.37.2
              with:
                  php-version: '8.5'
                  extensions: mbstring, sodium
                  coverage: none
                  tools: composer:v2

            - name: Take the verified preview set off the tagged release
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  # Via env, never inline: a crafted tag name must not reach the
                  # shell as executable $()/backtick text.
                  TAG: ${{ github.ref_name }}
              run: |
                  set -euo pipefail
                  mkdir -p preview
                  gh release download "$TAG" \
                      --dir preview \
                      --pattern 'beta*.yml' \
                      --pattern 'beta*.yml.sig'

                  # These are the bytes verify-published already checked. A tag
                  # that carried none would leave the rolling release empty and
                  # the feed answering 404 to every reader on preview.
                  mapfile -t MANIFESTS < <(find preview -type f -name 'beta*.yml')
                  if [ "${#MANIFESTS[@]}" -eq 0 ]; then
                      echo "::error::no preview manifest was published for ${TAG}, so there is nothing to move the feed onto."
                      exit 1
                  fi
                  for manifest in "${MANIFESTS[@]}"; do
                      [ -f "${manifest}.sig" ] || { echo "::error::missing signature for ${manifest}"; exit 1; }
                  done
                  echo "Moving the preview feed onto ${#MANIFESTS[@]} manifest(s) from ${TAG}."

            - name: Move the rolling preview release onto this build
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  TAG: ${{ github.ref_name }}
                  REPO: ${{ github.repository }}
                  FEED_TAG: ${{ env.PREVIEW_FEED_TAG }}
              run: |
                  set -euo pipefail
                  # Deleted and recreated rather than edited: an asset of the
                  # same name cannot be replaced in place, and a release left
                  # holding one build's manifest beside another's is worse than
                  # the seconds this is absent, which read as "offline" and
                  # surface nothing.
                  gh release delete "$FEED_TAG" --repo "$REPO" --yes --cleanup-tag || true
                  gh release create "$FEED_TAG" \
                      --repo "$REPO" \
                      --prerelease \
                      --title "Preview channel feed" \
                      --notes "The current preview manifests, moved here by the release workflow from ${TAG}. Not a release: it carries no installer, only the signed manifests a bundle on the preview channel polls." \
                      preview/*

            - name: Re-verify what the preview feed now serves
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  REPO: ${{ github.repository }}
                  FEED_TAG: ${{ env.PREVIEW_FEED_TAG }}
              run: |
                  set -euo pipefail
                  # The same pass verify-published runs, against what the feed
                  # actually serves rather than against what was uploaded: the
                  # reader fetches this release, so this release is the subject.
                  PUBLIC_HEX=$(grep -oE "'[0-9a-f]{64}'" config/auto_update.php | head -1 | tr -d "'")
                  if [ -z "$PUBLIC_HEX" ]; then
                      echo "Could not extract publisher_public_key_hex from config/auto_update.php"
                      exit 1
                  fi
                  mkdir -p served
                  gh release download "$FEED_TAG" --repo "$REPO" --dir served \
                      --pattern 'beta*.yml' --pattern 'beta*.yml.sig'
                  mapfile -t SERVED < <(find served -type f -name 'beta*.yml')
                  if [ "${#SERVED[@]}" -eq 0 ]; then
                      echo "::error::the preview feed serves no manifest, so a reader on preview polls a 404."
                      exit 1
                  fi
                  for signed in "${SERVED[@]}"; do
                      [ -f "${signed}.sig" ] || { echo "::error::missing signature for ${signed}"; exit 1; }
                      echo "Verifying ${signed}"
                      # shellcheck disable=SC2016  # inline PHP: $body is a PHP variable, not a shell one
                      PUBLIC_HEX="$PUBLIC_HEX" php -r '
                          $body = file_get_contents($argv[1]);
                          $sigHex = trim((string) file_get_contents($argv[1] . ".sig"));
                          $public = sodium_hex2bin(getenv("PUBLIC_HEX"));
                          $sig = sodium_hex2bin($sigHex);
                          exit(sodium_crypto_sign_verify_detached($sig, $body, $public) ? 0 : 1);
                      ' -- "$signed"
                  done
                  echo "The preview feed serves ${#SERVED[@]} manifest(s), every one verifying against the embedded publisher public key."
