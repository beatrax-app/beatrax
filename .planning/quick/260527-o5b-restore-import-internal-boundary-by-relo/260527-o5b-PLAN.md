---
phase: 260527-o5b-restore-import-internal-boundary-by-relo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/Import/tests/Support/PreviewSeedHelper.php
  - Modules/Onboarding/tests/Support/PreviewSeedHelper.php
  - Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php
  - Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php
  - Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php
  - Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php
autonomous: true
requirements: []
must_haves:
  truths:
    - "BoundaryArchTest invariant 'Modules\\Import\\Internal is only used inside Modules\\Import' passes"
    - "All four affected Onboarding feature tests still pass under the new namespace"
    - "No file under Modules/Onboarding/ imports any symbol from Modules\\Import\\Internal"
    - "Pint reports clean on every touched file"
    - "Larastan L10 strict reports clean on every touched file"
  artifacts:
    - path: "Modules/Import/tests/Support/PreviewSeedHelper.php"
      provides: "Relocated test helper, namespace Modules\\Import\\Tests\\Support"
      contains: "namespace Modules\\Import\\Tests\\Support"
  key_links:
    - from: "Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php"
      to: "Modules\\Import\\Tests\\Support\\PreviewSeedHelper"
      via: "use statement"
      pattern: "use Modules\\\\Import\\\\Tests\\\\Support\\\\PreviewSeedHelper"
    - from: "Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php"
      to: "Modules\\Import\\Tests\\Support\\PreviewSeedHelper"
      via: "use statement"
      pattern: "use Modules\\\\Import\\\\Tests\\\\Support\\\\PreviewSeedHelper"
    - from: "Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php"
      to: "Modules\\Import\\Tests\\Support\\PreviewSeedHelper"
      via: "use statement"
      pattern: "use Modules\\\\Import\\\\Tests\\\\Support\\\\PreviewSeedHelper"
    - from: "Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php"
      to: "Modules\\Import\\Tests\\Support\\PreviewSeedHelper"
      via: "use statement"
      pattern: "use Modules\\\\Import\\\\Tests\\\\Support\\\\PreviewSeedHelper"
---

<objective>
Restore the `Modules\Import\Internal is only used inside Modules\Import` BoundaryArchTest invariant by relocating `PreviewSeedHelper` from `Modules/Onboarding/tests/Support/` to `Modules/Import/tests/Support/`. The helper imports `Modules\Import\Internal\Pipeline\PreviewCache`; moving it inside the Import module turns the violating cross-module reach into a legal intra-module call.

Purpose: A test-support file landed in the wrong module's tests/ tree during 16.1.2 Wave 5 (commit 862efbb), making the Import\Internal namespace appear "used outside Modules\Import" in pest-plugin-arch's file walk. This is a structural fix, not an allow-list carve-out — the boundary invariant must stay strict.

Output: One PHP file at its new path, four feature-test files with updated `use` statements, the old file deleted, autoloader refreshed, all four downstream tests green, the boundary arch test green, Pint and Larastan clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@composer.json
@tests/Contracts/BoundaryArchTest.php
@Modules/Onboarding/tests/Support/PreviewSeedHelper.php
@Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php
@Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php
@Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php
@Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php

<interfaces>
<!-- Key contract: composer autoload-dev PSR-4 mappings (composer.json lines 63-83). -->
<!-- Both source and destination namespaces are already mapped — no composer.json edit needed. -->

Existing mappings (autoload-dev):
  "Modules\\Import\\Tests\\":      "Modules/Import/tests/"      (line 72)
  "Modules\\Onboarding\\Tests\\":  "Modules/Onboarding/tests/"  (line 73)

Current (violating) class:
  namespace Modules\Onboarding\Tests\Support;
  final class PreviewSeedHelper { ... }
  imports: use Modules\Import\Internal\Pipeline\PreviewCache;

Target (legal) class:
  namespace Modules\Import\Tests\Support;
  final class PreviewSeedHelper { ... }
  imports: use Modules\Import\Internal\Pipeline\PreviewCache;  // SAME — now intra-module

Four feature tests currently import:
  use Modules\Onboarding\Tests\Support\PreviewSeedHelper;
Must become:
  use Modules\Import\Tests\Support\PreviewSeedHelper;

Exact lines to edit (one line per file):
  Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php          line 15
  Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php  line 21
  Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php    line 20
  Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php     line 19

No other call sites exist (verified via repo-wide grep).

Target directory Modules/Import/tests/Support/ does NOT exist yet — must be created.
</interfaces>

<arch_invariant>
The failing test in tests/Contracts/BoundaryArchTest.php lines 21-23:

  arch('Modules\\Import\\Internal is only used inside Modules\\Import')
      ->expect('Modules\\Import\\Internal')
      ->toOnlyBeUsedIn('Modules\\Import');

pest-plugin-arch walks files by namespace ownership, so a file whose namespace
starts with `Modules\Import\` (regardless of which directory) is treated as
"inside Modules\Import" for the purpose of this rule. Relocating the helper into
the `Modules\Import\Tests\Support` namespace is sufficient — no allow-list edit.
</arch_invariant>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate PreviewSeedHelper into Modules/Import/tests/Support and update its namespace</name>
  <files>
    Modules/Import/tests/Support/PreviewSeedHelper.php (new),
    Modules/Onboarding/tests/Support/PreviewSeedHelper.php (delete)
  </files>
  <action>
Create the new directory `Modules/Import/tests/Support/` (it does not exist yet — Modules/Import/tests/ currently contains Feature/, Unit/, Snapshot/, Pest.php, TestCase.php).

Write `Modules/Import/tests/Support/PreviewSeedHelper.php` with the SAME body as the existing helper at `Modules/Onboarding/tests/Support/PreviewSeedHelper.php`, with exactly two changes:

1. Change the namespace declaration from
     `namespace Modules\Onboarding\Tests\Support;`
   to
     `namespace Modules\Import\Tests\Support;`

2. Leave every other line untouched. In particular:
   - Keep `use Modules\Import\Internal\Pipeline\PreviewCache;` (this is now an intra-module import — legal).
   - Keep `use Modules\Import\Public\Dto\ImportPreviewResult;`
   - Keep `use Modules\Import\Public\Dto\PreviewRowDto;`
   - Keep `use Modules\Ledger\Models\ImportRun;`
   - Keep `use Carbon\CarbonImmutable;`
   - Keep `declare(strict_types=1);`
   - Keep the `app(PreviewCache::class)` call as-is. Test-only support code is exempt from feedback_laravel_di_only.md (which governs production code); this is a static utility called from Pest test bodies that have no DI container. Do NOT refactor to constructor DI.
   - Keep the four-parameter signature (int $userId, string $sourceFormat, int $newRowCount, ?int $accountId = null) and the `$rowAccountId = $accountId ?? 1;` default.
   - Keep the PHPDoc block exactly as-is — it describes current behaviour and contains no history references (per feedback_docs_describe_current_state.md).
   - Keep the `final class PreviewSeedHelper` shape and the single static method `seedRunWithPreview` returning int.

After writing the new file, delete the old file at `Modules/Onboarding/tests/Support/PreviewSeedHelper.php`. After deletion, the `Modules/Onboarding/tests/Support/` directory will be empty — leave the empty directory alone (git does not track empty directories; it will simply disappear from the staged tree).

Do NOT add the new path to any allow-list in BoundaryArchTest. Do NOT edit composer.json — the `Modules\Import\Tests\` PSR-4 mapping at line 72 already covers the new location. Do NOT touch any file outside this task's `<files>` list.
  </action>
  <verify>
    <automated>
test -f Modules/Import/tests/Support/PreviewSeedHelper.php \
  && ! test -f Modules/Onboarding/tests/Support/PreviewSeedHelper.php \
  && grep -q '^namespace Modules\\Import\\Tests\\Support;' Modules/Import/tests/Support/PreviewSeedHelper.php \
  && grep -q 'use Modules\\Import\\Internal\\Pipeline\\PreviewCache;' Modules/Import/tests/Support/PreviewSeedHelper.php \
  && ! grep -rn 'Modules\\Onboarding\\Tests\\Support\\PreviewSeedHelper' Modules/Import Modules/Onboarding/tests/Support 2>/dev/null
    </automated>
  </verify>
  <done>
- `Modules/Import/tests/Support/PreviewSeedHelper.php` exists with `namespace Modules\Import\Tests\Support;` and an unchanged body.
- `Modules/Onboarding/tests/Support/PreviewSeedHelper.php` no longer exists.
- The new file still imports `Modules\Import\Internal\Pipeline\PreviewCache` (now intra-module).
- No edits to composer.json, BoundaryArchTest.php, or any non-listed file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update the four feature tests' use statements, refresh the autoloader, and run the full quality-gate sweep</name>
  <files>
    Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php,
    Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php,
    Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php,
    Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php
  </files>
  <action>
In each of the four feature-test files, replace the single line

  `use Modules\Onboarding\Tests\Support\PreviewSeedHelper;`

with

  `use Modules\Import\Tests\Support\PreviewSeedHelper;`

The unqualified `PreviewSeedHelper::seedRunWithPreview(...)` call sites in the test bodies do not change — only the `use` statement at the top of each file is edited. Confirmed line numbers from a repo-wide grep (use these as a sanity check, not as a hardcoded edit anchor — the `use` line is unique per file):

  Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php          line 15
  Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php  line 21
  Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php    line 20
  Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php     line 19

After the four edits, refresh the PSR-4 classmap and run the gates in this order, stopping on first failure:

  1. `composer dump-autoload`

  2. `vendor/bin/pest --filter BoundaryArch`
     — All BoundaryArchTest assertions pass, including the previously failing
       `Modules\Import\Internal is only used inside Modules\Import`.

  3. `vendor/bin/pest Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php`
     — All four feature suites pass under the new namespace. The fact that the
       seeded `app(PreviewCache::class)` cache survives across the helper call
       and the LiveWire mount is the whole reason the helper exists; no test
       behaviour changes.

  4. `vendor/bin/pint --test Modules/Import/tests/Support/PreviewSeedHelper.php Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php`
     — Pint clean. If any file reports drift, re-run without `--test` to apply,
       then re-verify with `--test`.

  5. `vendor/bin/phpstan analyse --memory-limit=1G Modules/Import/tests/Support/PreviewSeedHelper.php Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php`
     — Larastan L10 strict clean across all five files.

If gate 2 fails (the boundary test is still red): re-grep the repo for any remaining `Modules\Import\Internal` import outside `Modules/Import/` — the helper relocation should be the ONLY violation per the audit, but verify before assuming the fix is incomplete.

If gate 3 fails: read the failure carefully — the helper body is byte-identical except for namespace, so the only plausible failure is a stale autoloader (re-run gate 1) or a missed import in one of the four test files.

Do NOT touch any other file. Per the planning constraint, do NOT modify
`Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` or
`Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php`
even if they have working-tree changes — they are out of scope for this fix.
  </action>
  <verify>
    <automated>
grep -rn 'use Modules\\Onboarding\\Tests\\Support\\PreviewSeedHelper' Modules/ tests/ 2>/dev/null | grep -v '^Binary' | wc -l | tr -d ' ' | grep -q '^0$' \
  && grep -c 'use Modules\\Import\\Tests\\Support\\PreviewSeedHelper;' \
       Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php \
       Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php \
       Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php \
       Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php \
       | awk -F: 'BEGIN{ok=1} {if($2!="1")ok=0} END{exit !ok}' \
  && composer dump-autoload -q \
  && vendor/bin/pest --filter BoundaryArch \
  && vendor/bin/pest Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php \
  && vendor/bin/pint --test Modules/Import/tests/Support/PreviewSeedHelper.php Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php \
  && vendor/bin/phpstan analyse --memory-limit=1G Modules/Import/tests/Support/PreviewSeedHelper.php Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitEverythingTest.php Modules/Onboarding/tests/Feature/FirstImportStepCommitRollbackTest.php Modules/Onboarding/tests/Feature/FirstImportStepStaleIdFilterTest.php
    </automated>
  </verify>
  <done>
- All four feature tests import `Modules\Import\Tests\Support\PreviewSeedHelper` and zero files reference the old `Modules\Onboarding\Tests\Support\PreviewSeedHelper` namespace.
- `composer dump-autoload` exits cleanly.
- `vendor/bin/pest --filter BoundaryArch` is green (the `Modules\Import\Internal is only used inside Modules\Import` invariant is restored).
- All four affected feature-test suites pass.
- Pint `--test` reports clean on the five touched files.
- Larastan L10 strict reports clean on the five touched files.
- No edit to `FirstImportStep.php`, `first-import-step.blade.php`, `composer.json`, or `BoundaryArchTest.php`.
  </done>
</task>

</tasks>

<verification>
Phase-level gates (executed by Task 2):

1. `composer dump-autoload` — PSR-4 classmap refreshed.
2. `vendor/bin/pest --filter BoundaryArch` — every arch invariant green, including the previously-failing Import\Internal rule.
3. `vendor/bin/pest <four feature suites>` — the four previously-passing tests still pass under the new helper namespace.
4. `vendor/bin/pint --test <five files>` — formatting clean.
5. `vendor/bin/phpstan analyse --memory-limit=1G <five files>` — Larastan L10 strict clean.

Boundary verification (sanity grep):

  grep -rn 'Modules\\Import\\Internal' Modules/Onboarding Modules/Auth Modules/Core Modules/Ledger Modules/Ingestion Modules/Categorization Modules/Transfers Modules/Chains Modules/EmailScan Modules/Receipts Modules/Recurring Modules/DriftAlerts Modules/Desktop Modules/DevMode Modules/Community Modules/Forecasting 2>/dev/null

Expected: zero hits. Any hit means another cross-module reach into Import\Internal exists and the boundary fix is incomplete — surface it rather than papering over.
</verification>

<success_criteria>
The boundary fix is complete when:

- `Modules/Import/tests/Support/PreviewSeedHelper.php` exists with namespace `Modules\Import\Tests\Support`.
- `Modules/Onboarding/tests/Support/PreviewSeedHelper.php` no longer exists.
- The four Onboarding feature tests import `Modules\Import\Tests\Support\PreviewSeedHelper` (one line per file).
- `composer.json` is unchanged (both PSR-4 mappings already covered this move).
- `tests/Contracts/BoundaryArchTest.php` is unchanged (no allow-list carve-out).
- `vendor/bin/pest --filter BoundaryArch` is green.
- All four affected feature-test files are green.
- Pint `--test` is clean on the five touched files.
- Larastan L10 strict is clean on the five touched files.
- No other working-tree files (FirstImportStep.php, first-import-step.blade.php) appear in the staged diff.
</success_criteria>

<output>
Create `.planning/quick/260527-o5b-restore-import-internal-boundary-by-relo/260527-o5b-SUMMARY.md` recording:

- The exact six-file diff (1 new, 1 deleted, 4 modified).
- The boundary-test name that flipped from RED to GREEN.
- The five Pest/Pint/Phpstan command exit codes.
- A one-line note: "Structural fix, no allow-list carve-out — Import\Internal stays strict."
</output>
