---
phase: 16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena
plan: 02
type: execute
wave: 2
depends_on: [16.1-01]
files_modified:
  - Modules/Import/Public/Contracts/PaymentTypeHinter.php
  - Modules/Import/Public/Dto/PaymentTypeHint.php
  - Modules/Import/Public/Dto/PreviewRowDto.php
  - Modules/Import/Internal/Pipeline/Stages/PaymentTypeClassifierStage.php
  - Modules/Import/Internal/Pipeline/ImportPipeline.php
  - Modules/Import/Internal/Parsers/Asn/AsnCsvPaymentTypeHinter.php
  - Modules/Import/Internal/Parsers/Asn/AsnCamt053PaymentTypeHinter.php
  - Modules/Import/Internal/Parsers/Asn/AsnMt940PaymentTypeHinter.php
  - Modules/Import/Internal/Parsers/Ics/IcsPdfPaymentTypeHinter.php
  - Modules/Import/Internal/Parsers/Paypal/PaypalCsvPaymentTypeHinter.php
  - Modules/Import/Internal/Parsers/DescriptionKeywordFallbackHinter.php
  - Modules/Import/Providers/ImportServiceProvider.php
  - Modules/Ledger/Public/Dto/CanonicalTransaction.php
  - Modules/Import/tests/Unit/PaymentTypeHinterRegistryTest.php
  - Modules/Import/tests/Feature/PaymentTypeClassifierStageTest.php
  - Modules/Import/tests/Unit/AsnCsvPaymentTypeHinterTest.php
  - Modules/Import/tests/Unit/PaypalCsvPaymentTypeHinterTest.php
  - Modules/Import/tests/Unit/IcsPdfPaymentTypeHinterTest.php
  - Modules/Import/tests/Unit/DescriptionKeywordFallbackHinterTest.php
  - tests/Contracts/BoundaryArchTest.php
autonomous: true
requirements: [PTYPE-02, PTYPE-03]
must_haves:
  truths:
    - "PaymentTypeClassifierStage runs in ImportPipeline::preview() between ClassifyTransactionType and the auto-category/fingerprint stages"
    - "Six per-source hinters exist (ASN CSV, ASN CAMT.053, ASN MT940, ICS PDF, PayPal CSV, plus the description-keyword fallback) and are container-tagged import.payment_type_hinter"
    - "Every CanonicalTransaction emerging from the pipeline carries a non-null PaymentType (defaults to PaymentType::Unknown when no hinter fires)"
    - "PreviewRowDto exposes the resolved paymentType so the import preview UI can render the chip"
    - "A new arch invariant prevents PaymentType string-literal use outside Modules/Import/Public/Enums/PaymentType.php (Plan 01 invariant); a second invariant ensures every Internal/Parsers/**/PaymentTypeHinter.php implements the PaymentTypeHinter contract"
  artifacts:
    - path: "Modules/Import/Public/Contracts/PaymentTypeHinter.php"
      provides: "PaymentTypeHinter contract"
      exports: ["hint"]
    - path: "Modules/Import/Internal/Pipeline/Stages/PaymentTypeClassifierStage.php"
      provides: "Shared classifier stage merging hints from tagged hinters with description-keyword fallback"
      contains: "import.payment_type_hinter"
    - path: "Modules/Import/Providers/ImportServiceProvider.php"
      provides: "container tagging for all 6 hinters + classifier stage singleton"
      contains: "import.payment_type_hinter"
  key_links:
    - from: "Modules/Import/Internal/Pipeline/ImportPipeline.php"
      to: "Modules/Import/Internal/Pipeline/Stages/PaymentTypeClassifierStage.php"
      via: "constructor DI inserted between ClassifyTransactionType and AppliesAutoCategory"
      pattern: "PaymentTypeClassifierStage"
    - from: "Modules/Import/Providers/ImportServiceProvider.php"
      to: "all 6 hinter FQNs"
      via: "tag([$fqn], 'import.payment_type_hinter')"
      pattern: "import\\.payment_type_hinter"
---

<objective>
Ship the shared post-parse classifier stage that derives every transaction's payment_type from the union of source-specific hints + a description-keyword fallback, container-tagged so per-source hinters self-register. Mirrors the existing Receipts MatcherRegistry tagging pattern verbatim.

Purpose: D-05 / D-06 / D-07 / PTYPE-02 / PTYPE-03. The classifier MUST live in one place so a fix for "iDEAL keyword should be online not transfer" lands in one file and benefits every ingestion path. Per-source hinters carry the source-knowledge (CAMT BkTxCd codes, ASN CSV Betaalautomaat keywords, PayPal event_type, ICS PDF transaction-type codes) without polluting the shared classifier.

Output: PaymentTypeHinter contract + PaymentTypeHint DTO + PaymentTypeClassifierStage + 5 per-source hinters + 1 description-keyword fallback hinter, all registered in ImportServiceProvider via `tag()`, slotted into ImportPipeline between ClassifyTransactionType and the auto-category stage. PreviewRowDto extended with a resolved paymentType field. Every hinter has its own unit test; the classifier stage has a feature test exercising fixture hinters; a new arch invariant ensures every parser-tree hinter implements the contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-CONTEXT.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md

@CLAUDE.md

@Modules/Import/Internal/Pipeline/ImportPipeline.php
@Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php
@Modules/Import/Internal/Pipeline/Stages/ClassifyTransactionType.php
@Modules/Import/Public/Dto/PreviewRowDto.php
@Modules/Import/Providers/ImportServiceProvider.php

@Modules/Receipts/Providers/ReceiptsServiceProvider.php
@Modules/Receipts/Public/Contracts/SenderMatcher.php
@Modules/Receipts/Internal/MatcherRegistry.php

@Modules/Ledger/Public/Dto/CanonicalTransaction.php

@Modules/Import/Public/Enums/PaymentType.php

@tests/Contracts/BoundaryArchTest.php

<interfaces>
From Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php (exact shape to mirror — constructor DI, single classify() method, per-user where() pattern):

```
final class FingerprintStage {
    public function __construct(
        private readonly FingerprintComposer $fingerprints,
        private readonly DatabaseManager $db,
        private readonly SourceRefRanker $ranker,
    ) {}
    public function classify(CanonicalTransaction $tx, User $user): FingerprintDisposition { ... }
}
```

From Modules/Receipts/Providers/ReceiptsServiceProvider.php lines 60-119 (container-tagging pattern to mirror — const TAG_FQNS array, foreach class_exists guard + singleton + tag, closure-bound singleton consuming `app->tagged()`):

```
private const MATCHER_FQNS = [ 'Modules\\Receipts\\Internal\\Matchers\\PaypalReceiptMatcher', ... ];

public function register(): void {
    foreach (self::MATCHER_FQNS as $fqn) {
        if (class_exists($fqn)) {
            $this->app->singleton($fqn);
            $this->app->tag([$fqn], 'receipts.matcher');
        }
    }
    $this->app->singleton(MatcherRegistry::class, static function (Container $app): MatcherRegistry { ... });
}
```

From Modules/Import/Public/Enums/PaymentType.php (Plan 01 artifact — the 8-case backed enum with chipLabel/chipClass).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define PaymentTypeHinter contract + PaymentTypeHint DTO + extend PreviewRowDto + extend CanonicalTransaction with paymentType field</name>
  <files>Modules/Import/Public/Contracts/PaymentTypeHinter.php, Modules/Import/Public/Dto/PaymentTypeHint.php, Modules/Import/Public/Dto/PreviewRowDto.php, Modules/Ledger/Public/Dto/CanonicalTransaction.php</files>
  <read_first>
    - Modules/Import/Public/Dto/PreviewRowDto.php FULL (current spatie/laravel-data DTO — extend WITHOUT breaking existing call-sites)
    - Modules/Ledger/Public/Dto/CanonicalTransaction.php FULL (find the constructor signature; add a paymentType field as the last optional parameter so existing callers keep working; provide a `withPaymentType(): self` wither)
    - Modules/Receipts/Public/Contracts/SenderMatcher.php (shape of the existing tagged interface — mirror its docblock + method signature style)
    - Modules/Import/Public/Enums/PaymentType.php (Plan 01 artifact)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md lines 804-829 (PaymentTypeHinter contract + PaymentTypeHint DTO verbatim)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 88-131 (per-file analog rows for Public/Contracts + Public/Dto changes) plus Shared Pattern A (constructor DI)
    - grep -rn "new CanonicalTransaction(" Modules/ (find every call-site so the field addition doesn't break anything)
  </read_first>
  <behavior>
    - `Modules\Import\Public\Contracts\PaymentTypeHinter` is an interface with one method: `hint(\Modules\Ledger\Public\Dto\CanonicalTransaction $tx, string $sourceFormat): ?\Modules\Import\Public\Dto\PaymentTypeHint`. Null return = no signal for this row.
    - `Modules\Import\Public\Dto\PaymentTypeHint` extends `Spatie\LaravelData\Data` with readonly `PaymentType $type`, `int $confidence` (0-100), `string $sourceHint` (logging only).
    - `Modules\Import\Public\Dto\PreviewRowDto` gains 2 new readonly properties: `public readonly ?PaymentType $paymentType = null` AND `public readonly ?string $aliasFriendlyName = null` (the alias field is consumed by Plan 04 but added now so Plan 04 doesn't have to revisit this DTO). Defaults to null so every existing call-site continues to compile.
    - `Modules\Ledger\Public\Dto\CanonicalTransaction` gains a readonly `?PaymentType $paymentType = null` field AND a `withPaymentType(PaymentType $type): self` wither method that returns a new instance with the field set (immutable). Every existing caller of `new CanonicalTransaction(...)` continues to compile because the new field is the last optional argument.
    - Larastan L10 strict + Pint pass on the touched files.
  </behavior>
  <action>
**Contract `Modules/Import/Public/Contracts/PaymentTypeHinter.php`** — mirror the docblock style of `Modules\Receipts\Public\Contracts\SenderMatcher`. The interface MUST live under `Public/Contracts/` (not Internal) because per-source hinters in other modules would import it.

Interface body: namespace `Modules\Import\Public\Contracts`; single method `hint(CanonicalTransaction $tx, string $sourceFormat): ?PaymentTypeHint`. PHPDoc describes that returning null means "this hinter has no signal for the given row"; the classifier ignores nulls and consults the next tagged hinter; when all hinters return null the description-keyword fallback runs.

**DTO `Modules/Import/Public/Dto/PaymentTypeHint.php`** — `final class PaymentTypeHint extends Spatie\LaravelData\Data` with three readonly constructor properties: `PaymentType $type`, `int $confidence` (0-100), `string $sourceHint`. PHPDoc: `$confidence` is 0-100 with the highest-confidence hint winning, ties broken by hinter registration order; `$sourceHint` is a free-form developer-facing string used by the classifier for structured-log lines only — never user-facing.

**Modify `Modules/Import/Public/Dto/PreviewRowDto.php`** — keep every existing readonly property in the constructor in its current position. APPEND two new readonly fields at the end with `null` defaults: `public readonly ?\Modules\Import\Public\Enums\PaymentType $paymentType = null` AND `public readonly ?string $aliasFriendlyName = null`. Update PHPDoc to document the two new properties using the existing style (current-state docs only per rule C-3 — no history). The `aliasFriendlyName` field is consumed by Plan 04's MerchantNameResolver — adding it now avoids a second touch on this DTO.

**Modify `Modules/Ledger/Public/Dto/CanonicalTransaction.php`** — APPEND a readonly `?\Modules\Import\Public\Enums\PaymentType $paymentType = null` field as the LAST constructor parameter (defaults to null so existing call-sites compile). Add a method `public function withPaymentType(\Modules\Import\Public\Enums\PaymentType $type): self` that returns a new CanonicalTransaction instance with `paymentType` replaced and every other field preserved (immutable wither pattern). If CanonicalTransaction has many fields, reconstruct via named args.

**Grep all call-sites for `new CanonicalTransaction(`** — run `grep -rn 'new CanonicalTransaction(' Modules/` and record every match. For every match: either confirm it uses named-arguments syntax OR confirm `paymentType` lands as the trailing optional arg unset, so the call still compiles. If any positional-only call would break, switch it to named-arguments form as part of this task. Re-record the grep output in the task's `<verification>` field at the end.

Tests follow in Task 2 + Task 3 (no new test file in this task — interface and DTO definitions don't need behavior tests, only static-analysis green).
  </action>
  <verify>
    <automated>vendor/bin/pest --filter='PreviewRow|CanonicalTransaction|ImportPipeline' --no-coverage && composer test</automated>
  </verify>
  <done>
    PaymentTypeHinter interface + PaymentTypeHint DTO exist; PreviewRowDto + CanonicalTransaction have the new optional fields without breaking any existing test; `composer test` (full suite) is green, proving every existing `new CanonicalTransaction(` call-site still compiles; the grep audit output is recorded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PaymentTypeClassifierStage + 6 hinters + container-tag in ImportServiceProvider + slot stage into ImportPipeline</name>
  <files>Modules/Import/Internal/Pipeline/Stages/PaymentTypeClassifierStage.php, Modules/Import/Internal/Pipeline/ImportPipeline.php, Modules/Import/Internal/Parsers/Asn/AsnCsvPaymentTypeHinter.php, Modules/Import/Internal/Parsers/Asn/AsnCamt053PaymentTypeHinter.php, Modules/Import/Internal/Parsers/Asn/AsnMt940PaymentTypeHinter.php, Modules/Import/Internal/Parsers/Ics/IcsPdfPaymentTypeHinter.php, Modules/Import/Internal/Parsers/Paypal/PaypalCsvPaymentTypeHinter.php, Modules/Import/Internal/Parsers/DescriptionKeywordFallbackHinter.php, Modules/Import/Providers/ImportServiceProvider.php</files>
  <read_first>
    - Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php FULL (the shape to mirror exactly — constructor DI list, single classify() method, per-user where() pattern, mb_safe string handling, structured-log calls via injected LoggerInterface)
    - Modules/Import/Internal/Pipeline/ImportPipeline.php FULL (find where ClassifyTransactionType runs and where AppliesAutoCategory runs; the new stage slots BETWEEN them per the architecture diagram in 16.1-RESEARCH.md lines 230-249)
    - Modules/Import/Internal/Pipeline/Stages/ClassifyTransactionType.php (existing classifier — read to confirm output shape)
    - Modules/Receipts/Providers/ReceiptsServiceProvider.php FULL (the verbatim shape for container-tag + collector-singleton — lines 60-119)
    - Modules/Receipts/Internal/MatcherRegistry.php (how the registry consumes tagged iterable)
    - Modules/Import/Internal/Parsers/Asn/AsnCsvParser.php OR equivalent (current ASN CSV parser — find the description / source-format fields the hinter keys off)
    - Modules/Import/Internal/Parsers/Paypal/PaypalCsvParser.php OR equivalent
    - Modules/Ingestion/Internal/Adapters/Paypal/ (the existing PayPal event-type list)
    - Modules/Import/Internal/Parsers/Ics/IcsPdfParser.php OR equivalent
    - Modules/Ledger/Public/Dto/CanonicalTransaction.php (Task 1's updated form — read sourceFormat/description fields the hinters key off)
    - Modules/Import/Public/Enums/PaymentType.php (Plan 01 artifact)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md lines 408-440 (Pattern 1 container-tagged collaborators verbatim) plus Architecture Diagram lines 199-261 (where the stage slots)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 361-409 (PaymentTypeClassifierStage analog from FingerprintStage) plus lines 411-451 (hinter analog from PaypalReceiptMatcher) plus Shared Pattern B (container tagging)
  </read_first>
  <behavior>
    - `PaymentTypeClassifierStage::run(CanonicalTransaction $tx, User $user, string $sourceFormat): CanonicalTransaction` iterates every tagged PaymentTypeHinter, collects non-null PaymentTypeHints, picks the highest-confidence hint (tie-breaker: hinter registration order), and returns `$tx->withPaymentType($hint->type)`. If no hinter returns a hint, the fallback hinter's description-keyword scan runs; if even that returns null, the stage returns `$tx->withPaymentType(PaymentType::Unknown)`.
    - ImportPipeline::preview() calls `$tx = $this->paymentTypeClassifier->run($tx, $user, $sourceFormat)` AFTER ClassifyTransactionType and BEFORE AppliesAutoCategory.
    - Each per-source hinter returns null when `$sourceFormat` is not its source (e.g. `AsnCsvPaymentTypeHinter` returns null for `paypal-csv`); returns a `PaymentTypeHint` otherwise.
    - `AsnCsvPaymentTypeHinter` and `AsnMt940PaymentTypeHinter`: description-keyword matching — `Betaalautomaat`/`Geldautomaat` => Pin (confidence 90), `iDEAL`/`OnlineBetaling` => Online (80), `Incasso`/`Direct Debit` => DirectDebit (85), `Overboeking`/`SEPA Credit Transfer` => Transfer (70).
    - `AsnCamt053PaymentTypeHinter`: keys on BkTxCd or transaction-type codes from the CAMT.053 CanonicalTransaction extension (if CanonicalTransaction does NOT expose BkTxCd, fall back to the description keywords above).
    - `PaypalCsvPaymentTypeHinter`: maps PayPal event types — `ExpressCheckoutPayment`/`Payment Sent`/`Subscription Payment` => Online (95), `User Initiated Withdrawal` => Transfer (70), `Refund`/`Refund Sent` => Refund (95), `Fee`/`Service Fee` => Fee (95). Defer ambiguous types to null. Use the canonical PayPal event-type list from `Modules/Ingestion/Internal/Adapters/Paypal/`.
    - `IcsPdfPaymentTypeHinter`: ICS PDF row types map to — `PIN` => Pin, `INTERNET` / `INTERNETBETALING` => Online, `INCASSO` / `AUTOMATISCHE INCASSO` => DirectDebit. The exact ICS PDF codes live in Modules/Import/Internal/Parsers/Ics/.
    - `DescriptionKeywordFallbackHinter`: low-confidence (40) catch-all that runs over EVERY source format; keyword scan covers any of the Dutch/English payment-type signals.
    - ImportServiceProvider::register() adds a `private const HINTER_FQNS` array listing all 6 hinters, loops with `class_exists` guard, `singleton()`, `tag([$fqn], 'import.payment_type_hinter')`. Then binds `PaymentTypeClassifierStage` as a singleton via a closure that resolves the tagged iterable.
    - All new files: constructor DI only (no facades, no `auth()`, no `event()` — inject `Illuminate\Contracts\Events\Dispatcher` where needed); LoggerInterface injection where structured-log calls land.
  </behavior>
  <action>
**Stage `Modules/Import/Internal/Pipeline/Stages/PaymentTypeClassifierStage.php`** — mirror FingerprintStage's shape verbatim. The class is `final`, constructor DI takes `iterable<PaymentTypeHinter> $hinters` plus `LoggerInterface $logger`. Method `run(CanonicalTransaction $tx, User $user, string $sourceFormat): CanonicalTransaction` iterates `$this->hinters`, calls `$hinter->hint($tx, $sourceFormat)`, skips nulls, keeps the highest-confidence hint, falls back to `PaymentType::Unknown` if no hint wins, and returns `$tx->withPaymentType($type)`. PHPDoc describes current behavior only.

**Modify `Modules/Import/Internal/Pipeline/ImportPipeline.php`** — add `private readonly PaymentTypeClassifierStage $paymentTypeClassifier` to the constructor (after `ClassifyTransactionType $classifier` so the order matches the pipeline ordering). In `preview()`, after the existing `ClassifyTransactionType::run(...)` call site (find the line via grep — `$this->classifier->`) and BEFORE the auto-category stage, insert `$tx = $this->paymentTypeClassifier->run($tx, $user, $sourceFormat);`. Sequence: parse → normalize → classifyTransactionType → **paymentTypeClassifier** → autoCategory → fingerprint. Confirm against the architecture diagram in 16.1-RESEARCH.md lines 230-249.

**Hinter `Modules/Import/Internal/Parsers/Asn/AsnCsvPaymentTypeHinter.php`** — `final class AsnCsvPaymentTypeHinter implements PaymentTypeHinter`. Constructor empty (pure-fn). `hint(CanonicalTransaction $tx, string $sourceFormat): ?PaymentTypeHint`:
- if `$sourceFormat !== 'asn-csv'` return null
- mb_strtolower the description into `$desc`
- map keywords to (PaymentType, confidence): `betaalautomaat`/`geldautomaat` → Pin/90, `ideal`/`onlinebetaling` → Online/80, `incasso`/`automatische incasso` → DirectDebit/85, `overboeking`/`sepa credit transfer` → Transfer/70
- return null if no keyword matches; return a PaymentTypeHint with `sourceHint` = the matched keyword string for log trace

**Hinter `Modules/Import/Internal/Parsers/Asn/AsnCamt053PaymentTypeHinter.php`** — same shape, gated on `$sourceFormat === 'asn-camt053'`. If `CanonicalTransaction` exposes a BkTxCd (`bankTransactionCode` or similar field — confirm via the Ledger DTO read in <read_first>), key off it: `PMNT-CCRD-POSD` → Pin, `PMNT-ICDT-ESCT`/`PMNT-RCDT-ESCT` → Transfer, `PMNT-IDDT-ESDD`/`PMNT-RCDT-ESDD` → DirectDebit. Fall back to the same keyword set as AsnCsv if BkTxCd is unrecognized.

**Hinter `Modules/Import/Internal/Parsers/Asn/AsnMt940PaymentTypeHinter.php`** — same shape, gated on `$sourceFormat === 'asn-mt940'`. Use the same keyword set as AsnCsv.

**Hinter `Modules/Import/Internal/Parsers/Ics/IcsPdfPaymentTypeHinter.php`** — gated on `$sourceFormat === 'ics-pdf'`. Read the ICS-PDF parser file under `Modules/Import/Internal/Parsers/Ics/` to find which transaction-type codes the parser surfaces. Map ICS codes (e.g. `PIN`, `INTERNET`, `INCASSO`) to PaymentType. If the codes aren't directly on CanonicalTransaction, the hinter does a description regex scan for those tokens (CAPS keywords are distinctive in ICS PDF text).

**Hinter `Modules/Import/Internal/Parsers/Paypal/PaypalCsvPaymentTypeHinter.php`** — gated on `$sourceFormat === 'paypal-csv'`. Read `Modules/Ingestion/Internal/Adapters/Paypal/*.php` for the existing event-type allow-list. Map: `ExpressCheckoutPayment`/`Payment Sent`/`Subscription Payment`/`Pre-approved Payment`/`Recurring Payment Sent` → Online (95); `User Initiated Withdrawal`/`Bank Deposit to PP Account`/`General Withdrawal` → Transfer (70); `Refund`/`Refund Sent`/`Canceled Payment` → Refund (95); `Fee`/`PayPal Fee`/`Service Fee` → Fee (95). Unknown event types → null.

**Hinter `Modules/Import/Internal/Parsers/DescriptionKeywordFallbackHinter.php`** — runs on EVERY source format (no source gate). Lower confidence (40). Same keyword set as AsnCsv but acts as the universal fallback when no source-specific hinter returns a hint.

**Modify `Modules/Import/Providers/ImportServiceProvider.php` — `register()`:** add a `private const PAYMENT_TYPE_HINTER_FQNS` array listing all six hinter FQCNs (source-specific first, fallback last — registration order drives tie-break). Foreach with `class_exists` guard → `$this->app->singleton($fqn)` → `$this->app->tag([$fqn], 'import.payment_type_hinter')`. Then bind `PaymentTypeClassifierStage` as a singleton via a closure that resolves the tagged iterable from the container, converts it to an array with `iterator_to_array($tagged, preserve_keys: false)`, and instantiates the stage with the array + the container-resolved LoggerInterface. Mirror Receipts lines 60-119 EXACTLY.

All written files: PHPDoc current-state only (rule C-3), no facades / no `auth()` / no global helpers (rule C-1 — inject `Illuminate\Contracts\Events\Dispatcher`, `Illuminate\Support\DateFactory`, `Illuminate\Routing\Redirector`, `Illuminate\Contracts\Routing\ResponseFactory` instead), no `.planning/` references (rule C-2).
  </action>
  <verify>
    <automated>vendor/bin/pest --filter='ImportPipeline|PaymentType' --no-coverage</automated>
  </verify>
  <done>
    PaymentTypeClassifierStage exists with constructor-injected `iterable<PaymentTypeHinter>` + LoggerInterface; ImportPipeline runs it between ClassifyTransactionType and AppliesAutoCategory; all 6 hinters exist and implement the contract; ImportServiceProvider tags every hinter with `import.payment_type_hinter` and binds the stage as a singleton; existing ImportPipeline tests still pass (or are amended with new payment-type assertions in Task 3).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — registry container-tag test + classifier stage test + 4 per-source hinter unit tests + arch invariant for parser-tree hinters (plan close-out gate)</name>
  <files>Modules/Import/tests/Unit/PaymentTypeHinterRegistryTest.php, Modules/Import/tests/Feature/PaymentTypeClassifierStageTest.php, Modules/Import/tests/Unit/AsnCsvPaymentTypeHinterTest.php, Modules/Import/tests/Unit/PaypalCsvPaymentTypeHinterTest.php, Modules/Import/tests/Unit/IcsPdfPaymentTypeHinterTest.php, Modules/Import/tests/Unit/DescriptionKeywordFallbackHinterTest.php, tests/Contracts/BoundaryArchTest.php</files>
  <read_first>
    - Modules/Receipts/tests/Unit/MatcherRegistryTest.php OR equivalent (the existing container-tag-verification test pattern)
    - Modules/Import/tests/Unit/FingerprintStageClassifyTest.php OR similar (pipeline-stage feature test analog)
    - Modules/Import/tests/Unit/SourceRefRankerTest.php (pure-fn unit test shape with datasets)
    - Modules/Ledger/Public/Dto/CanonicalTransaction.php (Task 1's updated form — build fixture transactions in the tests)
    - Modules/Import/Public/Enums/PaymentType.php
    - tests/Contracts/BoundaryArchTest.php (Plan 01's new invariants are now present — add one more invariant for the parser-tree contract enforcement)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 117-130 (test analog cites for PaymentTypeHinterRegistryTest + PaymentTypeClassifierStageTest)
  </read_first>
  <behavior>
    - `vendor/bin/pest Modules/Import/tests/Unit/PaymentTypeHinterRegistryTest.php` asserts that resolving `app('import.payment_type_hinter')` (or equivalent container-tagged resolution) returns exactly 6 instances; every instance implements `PaymentTypeHinter`; the DescriptionKeywordFallbackHinter is the LAST instance in iteration order (registration-order tie-break).
    - `vendor/bin/pest Modules/Import/tests/Feature/PaymentTypeClassifierStageTest.php` asserts: feeding a fixture CanonicalTransaction whose description is "iDEAL Bestelling" through the stage produces a CanonicalTransaction with paymentType=Online; feeding a description "Betaalautomaat Albert Heijn" produces Pin; feeding "Refund Sent" with sourceFormat=paypal-csv produces Refund; feeding gibberish with no matching keyword produces Unknown.
    - 4 per-source unit tests exercise each hinter's allow-list — minimum 3 dataset rows per hinter (PIN keyword, online keyword, direct-debit keyword, foreign-source-format=null-return).
    - A new arch invariant in `tests/Contracts/BoundaryArchTest.php` rejects any class named `*PaymentTypeHinter` under `Modules/Import/Internal/Parsers/` that does NOT implement `Modules\Import\Public\Contracts\PaymentTypeHinter`.
    - Plan close-out gate (final step of this task): `composer test && composer analyse && composer format:check` is green.
  </behavior>
  <action>
**Test `Modules/Import/tests/Unit/PaymentTypeHinterRegistryTest.php`** — Pest unit. Boot the app via the standard module test base. Resolve `app()->tagged('import.payment_type_hinter')` (or whatever the container API call returns the tagged iterable). Convert to array. Assertions: count is 6; every entry `instanceof PaymentTypeHinter`; last entry is `DescriptionKeywordFallbackHinter::class` (tie-break order). Minimum 4 assertions.

**Test `Modules/Import/tests/Feature/PaymentTypeClassifierStageTest.php`** — Pest feature. Build helper `makeTx(string $description, ?string $sourceFormat = 'asn-csv'): CanonicalTransaction` that constructs a minimal CanonicalTransaction with the given description and sourceFormat.

Test cases via `dataset()`:
- `['Betaalautomaat Albert Heijn 1245', 'asn-csv', PaymentType::Pin]`
- `['iDEAL Bestelling Bol.com', 'asn-csv', PaymentType::Online]`
- `['SEPA Incasso Vattenfall', 'asn-csv', PaymentType::DirectDebit]`
- `['Overboeking aan partner', 'asn-csv', PaymentType::Transfer]`
- `['Refund Sent', 'paypal-csv', PaymentType::Refund]`
- `['Service Fee', 'paypal-csv', PaymentType::Fee]`
- `['Random gibberish text', 'asn-csv', PaymentType::Unknown]`

For each row: build the transaction; resolve the classifier stage via `app(PaymentTypeClassifierStage::class)`; call `run($tx, $user, $sourceFormat)`; assert the returned tx's paymentType equals the expected enum case. Create a user via `User::create([...])` once in `beforeEach`. Minimum 7 assertions.

**Test `Modules/Import/tests/Unit/AsnCsvPaymentTypeHinterTest.php`** — direct instantiation of the hinter (no container). Test that `hint(makeTx('iDEAL X', 'asn-csv'), 'asn-csv')` returns a hint with `PaymentType::Online`; `hint(makeTx('X', 'paypal-csv'), 'paypal-csv')` returns null (foreign source format); `hint(makeTx('random', 'asn-csv'), 'asn-csv')` returns null. Minimum 3 assertions.

**Test `Modules/Import/tests/Unit/PaypalCsvPaymentTypeHinterTest.php`** — same shape, exercising the PayPal event-type mappings. Test at least one row per category (Online, Transfer, Refund, Fee, Unknown). Minimum 5 assertions.

**Test `Modules/Import/tests/Unit/IcsPdfPaymentTypeHinterTest.php`** — same shape, exercising PIN / INTERNET / INCASSO ICS PDF codes. Minimum 3 assertions.

**Test `Modules/Import/tests/Unit/DescriptionKeywordFallbackHinterTest.php`** — exercise the universal fallback. Test that it returns a hint regardless of source format (it's the catch-all); test the same keyword set produces lower confidence (40) than the source-specific hinters. Minimum 4 assertions.

**Modify `tests/Contracts/BoundaryArchTest.php`** — append a new arch invariant: `arch('Every *PaymentTypeHinter class under Modules/Import/Internal/Parsers implements the PaymentTypeHinter contract')`. Implementation: `expect('Modules\\Import\\Internal\\Parsers')->classes()->that->haveSuffix('PaymentTypeHinter')->toImplement('Modules\\Import\\Public\\Contracts\\PaymentTypeHinter')`. If the DSL form doesn't support `haveSuffix`, use the wildcard `expect('Modules\\Import\\Internal\\Parsers\\**\\*PaymentTypeHinter')->toImplement(...)`. If neither DSL form works, write a custom Pest test using Composer's class-map to enumerate `*PaymentTypeHinter` classes under the parser tree and assert each implements the contract.

Plan close-out gate — run as the LAST step of this task: `composer test && composer analyse && composer format:check`. Per project rule C-4 fix-every-severity: address every BLOCKER + WARNING + INFO surfaced. Re-run the gate until it exits green.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Import/tests/Unit/PaymentTypeHinterRegistryTest.php Modules/Import/tests/Feature/PaymentTypeClassifierStageTest.php Modules/Import/tests/Unit/AsnCsvPaymentTypeHinterTest.php Modules/Import/tests/Unit/PaypalCsvPaymentTypeHinterTest.php Modules/Import/tests/Unit/IcsPdfPaymentTypeHinterTest.php Modules/Import/tests/Unit/DescriptionKeywordFallbackHinterTest.php tests/Contracts/ --no-coverage && composer test && composer analyse && composer format:check</automated>
  </verify>
  <done>
    All 6 new test files pass with ≥ 26 assertions total; the new arch invariant exists in tests/Contracts/ and is green; the full `composer test` + `composer analyse` + `composer format:check` triple gate is green; no PaymentType string-literal regressions outside the enum file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ImportPipeline ↔ source file (CSV/XML/PDF) | Untrusted source-file content reaches PaymentTypeHinters via CanonicalTransaction.description; classifier never executes the description, only pattern-matches in PHP |
| Hinter ↔ shared SQLite | Hinters are pure-functions — they do NOT touch the DB; only the classifier stage might, and even then only to return an updated CanonicalTransaction (no writes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-02-01 | Tampering | Description-keyword injection — malicious description containing keyword tokens that mis-classify | accept | Mis-classification is bounded — the worst outcome is an incorrect payment_type chip on the preview row; the user manually reviews and can change category/name; no security impact, just UX |
| T-16.1-02-02 | Information Disclosure | Hinter logs description text that may contain PII | mitigate | Hinters use the `$sourceHint` field for log lines (matched keyword token only, e.g. "betaalautomaat"); the full description is NOT logged; LoggerInterface call sites are explicit and bounded |
| T-16.1-02-03 | Tampering | Container-tag missing a hinter so a per-source row falls to fallback only | mitigate | Arch invariant `Every *PaymentTypeHinter implements PaymentTypeHinter` AND the registry test asserts exact count = 6; a forgotten registration in ImportServiceProvider fails the registry test |
| T-16.1-02-04 | Tampering | Hinter returns a PaymentType value the DB triggers reject | mitigate | The PaymentType enum is closed (8 cases); `$tx->withPaymentType()` only accepts enum instances; impossible to set a non-enum value; trigger pair from Plan 01 is defense-in-depth |
| T-16.1-02-05 | Denial of Service | Hinter with a catastrophic regex on attacker-controlled description (ReDoS) | mitigate | All hinter regexes are anchored simple alternations / fixed substrings (`mb_strpos`); no `.*` or nested quantifiers; reviewed during implementation |
</threat_model>

<verification>
- `composer test` (full Pest suite) is green.
- `composer analyse` (Larastan L10 strict) exits 0.
- `composer format:check` exits 0.
- `vendor/bin/pest --filter='PaymentType'` shows all 6 new test files passing with ≥ 26 assertions total.
- `app()->tagged('import.payment_type_hinter')` returns exactly 6 hinters in the documented order.
- An end-to-end import of a fixture ASN CSV file produces preview rows with `paymentType` set to a non-null PaymentType.
</verification>

<success_criteria>
- 6 hinters live under `Modules/Import/Internal/Parsers/` and implement `Modules\Import\Public\Contracts\PaymentTypeHinter`.
- `PaymentTypeClassifierStage` lives at the documented file path and is slotted into ImportPipeline between ClassifyTransactionType and AppliesAutoCategory.
- ImportServiceProvider tags every hinter `import.payment_type_hinter` and binds the stage as a singleton via the verbatim container-tag closure pattern.
- `PreviewRowDto` carries `paymentType` AND `aliasFriendlyName` (the latter is reserved for Plan 04).
- `CanonicalTransaction` carries `paymentType` and exposes `withPaymentType()` wither.
- Arch invariant enforces the parser-tree contract; existing arch suite (including Plan 01's invariants) remains green.
- Larastan L10 strict + Pint clean.
- Zero `.planning/` / D-NN / PLAN.md leakage in production source.
</success_criteria>

<output>
Create `.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-02-SUMMARY.md` when done.
</output>
</content>
</invoke>