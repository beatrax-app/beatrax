---
phase: 16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/Onboarding/module.json
  - Modules/Onboarding/composer.json
  - Modules/Onboarding/Providers/OnboardingServiceProvider.php
  - Modules/Onboarding/Database/Migrations/2026_05_26_000001_create_wizard_progress_table.php
  - Modules/Onboarding/Models/WizardProgress.php
  - Modules/Onboarding/Public/Events/WizardCompleted.php
  - Modules/Onboarding/tests/Pest.php
  - Modules/Onboarding/tests/TestCase.php
  - Modules/Onboarding/tests/Feature/WizardProgressMigrationTest.php
  - Modules/Community/module.json
  - Modules/Community/composer.json
  - Modules/Community/Providers/CommunityServiceProvider.php
  - Modules/Community/Database/Migrations/2026_05_26_000002_create_community_merchant_mappings_table.php
  - Modules/Community/Models/CommunityMerchantMapping.php
  - Modules/Community/tests/Pest.php
  - Modules/Community/tests/TestCase.php
  - Modules/Community/tests/Feature/CommunityCorpusMigrationTest.php
  - Modules/Import/Database/Migrations/2026_05_26_000003_add_payment_type_to_transactions.php
  - Modules/Import/Database/Migrations/2026_05_26_000004_create_merchant_aliases_table.php
  - Modules/Import/Models/MerchantAlias.php
  - Modules/Import/Public/Enums/PaymentType.php
  - Modules/Import/tests/Feature/PaymentTypeColumnMigrationTest.php
  - Modules/Import/tests/Feature/MerchantAliasesMigrationTest.php
  - Modules/Core/Database/Migrations/2026_05_26_000005_add_community_settings_to_users.php
  - Modules/Core/Models/User.php
  - Modules/Core/tests/Feature/UserCommunitySettingsColumnTest.php
  - composer.json
  - phpunit.xml
  - tests/Pest.php
  - tests/Contracts/BoundaryArchTest.php
autonomous: true
requirements: [WIZ-01, WIZ-02, PTYPE-01, CORPUS-01]
must_haves:
  truths:
    - "Onboarding and Community bounded modules exist and boot cleanly; their test bootstraps are wired into composer + phpunit + tests/Pest.php so subsequent plans can write tests from task 1"
    - "The wizard's status field never accepts an unknown value (data integrity invariant enforced at the DB layer)"
    - "Community corpus rows can be shared globally across users (user_id nullable) without a uniqueness collision between global and per-user override rows"
    - "Every transaction carries a payment_type drawn exclusively from the locked 8-case enum; the column defaults to unknown for existing rows after migration"
    - "Per-user merchant aliases cannot collide on (user_id, pattern); the merged_from JSON provenance column round-trips through Eloquent without loss"
    - "PaymentType is a closed 8-case PHP 8.4 backed enum whose chip rendering data (label + CSS modifier) is defined inside the enum itself, not duplicated elsewhere"
    - "The authenticated user has a writable per-user JSON settings store (`users.community_settings`) for module-scoped preferences such as Toggle 1/2/3"
  artifacts:
    - path: "Modules/Onboarding/Database/Migrations/2026_05_26_000001_create_wizard_progress_table.php"
      provides: "wizard_progress table + status trigger pair"
      contains: "wizard_progress_status_check_insert"
    - path: "Modules/Community/Database/Migrations/2026_05_26_000002_create_community_merchant_mappings_table.php"
      provides: "community_merchant_mappings global table"
      contains: "user_id"
    - path: "Modules/Import/Database/Migrations/2026_05_26_000003_add_payment_type_to_transactions.php"
      provides: "payment_type column + trigger pair on transactions"
      contains: "transactions_payment_type_check_insert"
    - path: "Modules/Import/Database/Migrations/2026_05_26_000004_create_merchant_aliases_table.php"
      provides: "merchant_aliases per-user table"
      contains: "merged_from"
    - path: "Modules/Import/Public/Enums/PaymentType.php"
      provides: "PaymentType backed string enum (8 cases)"
      contains: "case Unknown"
    - path: "Modules/Core/Database/Migrations/2026_05_26_000005_add_community_settings_to_users.php"
      provides: "users.community_settings JSON column for per-user module-scoped preferences"
      contains: "community_settings"
  key_links:
    - from: "bootstrap/providers.php OR Modules/*/module.json"
      to: "Modules/Onboarding/Providers/OnboardingServiceProvider, Modules/Community/Providers/CommunityServiceProvider"
      via: "nwidart module.json providers key (auto-discovery)"
      pattern: "OnboardingServiceProvider|CommunityServiceProvider"
    - from: "tests/Pest.php"
      to: "Modules\\Onboarding\\Tests\\TestCase, Modules\\Community\\Tests\\TestCase"
      via: "foreach loop registering TestCase per module"
      pattern: "Modules/Onboarding.*TestCase|Modules/Community.*TestCase"
    - from: "Modules/Core/Models/User.php"
      to: "users.community_settings JSON column"
      via: "$fillable += community_settings; casts() casts it to array"
      pattern: "community_settings"
---

<objective>
Establish the schema and module foundations every later plan in Phase 16.1 builds on. Two brand-new nwidart modules (Modules/Onboarding/, Modules/Community/), five new migrations (wizard_progress, community_merchant_mappings, transactions.payment_type column, merchant_aliases, users.community_settings JSON column), the PaymentType backed enum, the User model extension exposing the new community_settings JSON column, and all the test scaffolding (per-module Pest.php + TestCase, phpunit.xml updates, composer autoload-dev, tests/Pest.php loop) so subsequent plans can write feature tests against these surfaces from task 1.

Purpose: Phase 16.1 introduces 25 REQ-IDs spanning six features. Migrations and module skeletons are the universal dependency — every other plan in waves 2-5 needs at least one of these artifacts to exist. Per the revision feedback, the per-user `users.community_settings` JSON column is also placed here so Plan 05's SharedListSettingsPanel and HelpOthersTriageButton (Toggle 1/2/3) have a concrete column to read and write against — keeping all schema work inside Wave 1 as this plan's <objective> requires.

Output: 5 migrations applied to SQLite (WAL), 2 new modules booting cleanly, 1 PHP 8.4 backed enum, User model exposing community_settings as a cast array, all test bootstraps registered, `php artisan migrate` exits 0, and the full Pest + Larastan L10 strict + Pint gate passes on the new code with zero regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-CONTEXT.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md

@CLAUDE.md
@composer.json
@phpunit.xml
@tests/Pest.php
@tests/Contracts/BoundaryArchTest.php

@Modules/Categorization/module.json
@Modules/Categorization/composer.json
@Modules/Categorization/Providers/CategorizationServiceProvider.php
@Modules/Categorization/tests/Pest.php
@Modules/Categorization/tests/TestCase.php

@Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php
@Modules/Core/Models/User.php
@Modules/Categorization/Models/CategorizationRule.php

<interfaces>
From Modules/Core/Public/Events/UserInstalled.php (payload class signature, consumed by Plan 03 + Plan 05 listeners):

```
final class UserInstalled { public function __construct(public readonly int $userId) {} }
```

From Modules/Categorization/Models/CategorizationRule.php (exact MerchantAlias mirror target — BelongsToUser trait, $table, $fillable, casts() shape):

```
final class CategorizationRule extends Model {
    use BelongsToUser;
    protected $table = 'categorization_rules';
    protected $fillable = [...];
    protected function casts(): array { return [...]; }
}
```

From Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php (trigger pair pattern lines 70-82 plus DatabaseManager helper lines 94-108 — referenced by 3 of the 5 migrations in this plan).

From Modules/Core/Models/User.php (the canonical User model — this plan extends $fillable and casts() with `community_settings` as an array cast).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Onboarding + Community modules + test bootstraps + autoload</name>
  <files>Modules/Onboarding/module.json, Modules/Onboarding/composer.json, Modules/Onboarding/Providers/OnboardingServiceProvider.php, Modules/Onboarding/Public/Events/WizardCompleted.php, Modules/Onboarding/tests/Pest.php, Modules/Onboarding/tests/TestCase.php, Modules/Community/module.json, Modules/Community/composer.json, Modules/Community/Providers/CommunityServiceProvider.php, Modules/Community/tests/Pest.php, Modules/Community/tests/TestCase.php, composer.json, phpunit.xml, tests/Pest.php</files>
  <read_first>
    - Modules/Categorization/module.json (exact shape to mirror — name/alias/description/priority/providers/files)
    - Modules/Categorization/composer.json (autoload PSR-4 shape)
    - Modules/Categorization/Providers/CategorizationServiceProvider.php lines 1-90 (boot() + register() shape; loadMigrationsFrom + loadRoutesFrom + loadViewsFrom + UserInstalled listener registration)
    - Modules/Categorization/tests/Pest.php (5-line bootstrap)
    - Modules/Categorization/tests/TestCase.php (extends Tests\TestCase pattern)
    - tests/Pest.php lines 22-62 (foreach module loop registering TestCase + RefreshDatabase)
    - composer.json lines 50-66 (autoload-dev psr-4 — add Onboarding + Community alphabetically)
    - phpunit.xml (testsuite shape — find Unit + Feature directories enumeration)
    - bootstrap/providers.php if present (verify whether nwidart auto-discovers via module.json or providers are explicitly listed)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md sections titled New Module: Modules/Onboarding/ and New Module: Modules/Community/ plus Shared Pattern A (Constructor DI) plus Shared Pattern C (idempotent listener registration)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md lines 312-407 (Recommended Project Structure)
  </read_first>
  <behavior>
    - Booting the app discovers OnboardingServiceProvider and CommunityServiceProvider (no fatal errors on `php artisan about`).
    - `php artisan module:list` (nwidart command) shows Onboarding + Community as enabled.
    - `vendor/bin/pest Modules/Onboarding/tests/` discovers no tests but exits 0 (empty suite OK).
    - `vendor/bin/pest Modules/Community/tests/` exits 0.
    - `composer dump-autoload` emits no PSR-4 warnings; `Modules\Onboarding\Tests\TestCase::class` and `Modules\Community\Tests\TestCase::class` resolve.
  </behavior>
  <action>
Create both modules as nwidart-style bounded modules mirroring `Modules/Categorization/` shape exactly:

1. `Modules/Onboarding/module.json`: name "Onboarding", alias "onboarding", description "First-run setup wizard: state-machine table, step components, and the resume-on-relaunch UX.", keywords ["onboarding","wizard","setup"], priority 5, providers ["Modules\\Onboarding\\Providers\\OnboardingServiceProvider"], files []. Mirror Categorization's shape exactly; only fields changed are name/alias/description/keywords/providers FQCN.

2. `Modules/Onboarding/composer.json`: copy Categorization's composer.json; replace every `Categorization` token with `Onboarding`; autoload psr-4: `Modules\\Onboarding\\` to `""` (module-local relative).

3. `Modules/Onboarding/Providers/OnboardingServiceProvider.php`: `final class OnboardingServiceProvider extends ServiceProvider`. `register(): void` is empty for this plan (services land in Plan 03). `boot(Dispatcher $events, LivewireManager $livewire): void` calls `$this->loadMigrationsFrom(__DIR__.'/../Database/Migrations')`, `$this->loadRoutesFrom(__DIR__.'/../Routes/web.php')` IF the routes file exists (guard with `file_exists`), `$this->loadViewsFrom(__DIR__.'/../Resources/views', 'onboarding')`. No listener wires yet. PHPDoc describes the module's purpose only (current-state docs only).

4. `Modules/Onboarding/Public/Events/WizardCompleted.php`: `final class WizardCompleted { public function __construct(public readonly int $userId) {} }`. Mirror `Modules\Core\Public\Events\UserInstalled` exactly. PHPDoc describes that this event fires when a user finishes the setup wizard.

5. `Modules/Onboarding/tests/Pest.php`: copy `Modules/Categorization/tests/Pest.php` verbatim (5-line inert bootstrap that defers to the root `tests/Pest.php` loop).

6. `Modules/Onboarding/tests/TestCase.php`: `namespace Modules\Onboarding\Tests; final class TestCase extends \Tests\TestCase {}`. Mirror Categorization's TestCase exactly.

7. Repeat steps 1-6 for `Modules/Community/` with description "Crowd-sourced merchant identification corpus: bundled YAML loader, mystery-merchants destination page, and the suggest-mapping draft-PR flow." and priority 6.

8. Modify `composer.json` autoload-dev psr-4: add `"Modules\\Onboarding\\Tests\\": "Modules/Onboarding/tests/"` and `"Modules\\Community\\Tests\\": "Modules/Community/tests/"` (keep alphabetical sort with siblings).

9. Modify `phpunit.xml`: locate the existing `testsuite` entries (Unit + Feature + Contracts + Snapshot). If they enumerate per-module directories via `directory` entries, add `Modules/Onboarding/tests/Unit` + `Modules/Community/tests/Unit` to the Unit suite and the Feature equivalents to the Feature suite. If they use wildcard discovery, no change needed (verify by reading the file).

10. Modify `tests/Pest.php`: in the `foreach` loop at lines 22-62 add `'Modules/Onboarding' => Modules\Onboarding\Tests\TestCase::class,` and `'Modules/Community' => Modules\Community\Tests\TestCase::class,` in alphabetical position.

11. Verify nwidart auto-discovery: `Modules\\Onboarding\\Providers\\OnboardingServiceProvider` and `Modules\\Community\\Providers\\CommunityServiceProvider` should be auto-registered via each module.json `providers` key (no edit to `config/app.php` or `bootstrap/providers.php` should be necessary). Run `php artisan module:list` to confirm. Per project rule C-2, no `.planning/` references in any file produced here.

After all writes: run `composer dump-autoload` then a smoke `vendor/bin/pest --filter='Onboarding'` (empty suite is acceptable success).
  </action>
  <verify>
    <automated>composer dump-autoload --no-interaction && php artisan about --only=Drivers,Cache,Database && php artisan module:list 2>&1 | grep -E '(Onboarding|Community)' && vendor/bin/pest Modules/Onboarding/tests/ --no-coverage && vendor/bin/pest Modules/Community/tests/ --no-coverage</automated>
  </verify>
  <done>
    Both modules booted cleanly: `composer dump-autoload` exits 0 with no PSR-4 warnings, `php artisan about` runs without errors, `php artisan module:list` lists both Onboarding and Community as enabled, `vendor/bin/pest Modules/Onboarding/tests/` and `vendor/bin/pest Modules/Community/tests/` both exit 0, `Modules\Onboarding\Tests\TestCase` and `Modules\Community\Tests\TestCase` resolve via composer autoload.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: wizard_progress migration + community_merchant_mappings migration + their models + paired migration tests</name>
  <files>Modules/Onboarding/Database/Migrations/2026_05_26_000001_create_wizard_progress_table.php, Modules/Onboarding/Models/WizardProgress.php, Modules/Community/Database/Migrations/2026_05_26_000002_create_community_merchant_mappings_table.php, Modules/Community/Models/CommunityMerchantMapping.php, Modules/Onboarding/tests/Feature/WizardProgressMigrationTest.php, Modules/Community/tests/Feature/CommunityCorpusMigrationTest.php</files>
  <read_first>
    - Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php FULL (trigger pair lines 70-82, DatabaseManager helper lines 94-108, down() shape, nullable user_id pattern for global rows)
    - Modules/Categorization/Database/Migrations/2026_05_17_010003_create_categorization_rules_table.php (per-user table with UNIQUE + index)
    - Modules/Categorization/Models/CategorizationRule.php FULL (BelongsToUser trait, $table, $fillable, casts())
    - Modules/Core/Models/SystemAlert.php (global-row model with nullable user_id — pattern for CommunityMerchantMapping which has NO BelongsToUser)
    - Modules/Core/Public/Concerns/BelongsToUser.php (trait surface)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md lines 493-519 (Pattern 3 SQLite trigger-CHECK pair) plus lines 940-1004 (migration shapes verbatim)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 303-353 (per-file analog cites) plus Shared Pattern D plus Shared Pattern E
    - tests/TestCase.php (RefreshDatabase boot)
  </read_first>
  <behavior>
    - `php artisan migrate` applies both migrations cleanly to a fresh database; re-running `php artisan migrate` is idempotent.
    - Inserting a wizard_progress row with status='BOGUS' raises SQLite RAISE(ABORT) with the substring "Invalid wizard_progress.status value".
    - Inserting community_merchant_mappings with user_id=null succeeds (global rows allowed); duplicate (null, same pattern) fails on UNIQUE; same pattern with two different user_ids both succeed.
  </behavior>
  <action>
**Migration 1 — `wizard_progress`:** `2026_05_26_000001_create_wizard_progress_table.php`. Mirror system_alerts migration's class shape: anonymous `return new class extends Migration`, `up()` calls `$this->schema()->create('wizard_progress', ...)`. Columns: `$table->id()`, `$table->foreignId('user_id')->constrained('users')->cascadeOnDelete()`, `$table->string('step_key', 64)`, `$table->string('status', 16)`, `$table->json('data')->nullable()`, `$table->timestamp('completed_at')->nullable()`, `$table->timestamps()`. Indexes: `$table->unique(['user_id', 'step_key'])`, `$table->index(['user_id', 'status'])`. After create, run paired SQLite triggers (mirror lines 70-82 of system_alerts EXACTLY — same heredoc shape): `wizard_progress_status_check_insert` BEFORE INSERT with `WHEN NEW.status NOT IN ('pending','in_progress','done','skipped')` BEGIN SELECT RAISE(ABORT, 'Invalid wizard_progress.status value'); END and `wizard_progress_status_check_update` BEFORE UPDATE OF status. Use the `db()`/`schema()` helper methods from system_alerts lines 94-108 verbatim. `down()` drops triggers (DROP TRIGGER IF EXISTS) then drops the table.

**Migration 2 — `community_merchant_mappings`:** `2026_05_26_000002_create_community_merchant_mappings_table.php`. Columns: `$table->id()`, `$table->foreignId('user_id')->nullable()->constrained('users')->cascadeOnDelete()` (nullable because corpus rows are GLOBAL — `user_id IS NULL`), `$table->string('pattern')`, `$table->string('generalized_pattern')->nullable()`, `$table->string('name')`, `$table->string('category')->nullable()`, `$table->string('region', 8)->nullable()`, `$table->string('contributor', 64)`, `$table->timestamps()`. Indexes: `$table->unique(['user_id', 'pattern'])` (allows per-user override rows; global rows uniquely keyed on (NULL, pattern)), `$table->index(['generalized_pattern'])`. No triggers. Down: drop table.

**Model `Modules/Onboarding/Models/WizardProgress.php`:** `final class WizardProgress extends Model`. Use `BelongsToUser` trait. `protected $table = 'wizard_progress';`. `$fillable = ['user_id','step_key','status','data','completed_at']`. `casts(): array => ['data' => 'array', 'completed_at' => 'datetime']`. PHPDoc properties for Larastan L10. Per project rule C-3, PHPDoc describes current behavior only.

**Model `Modules/Community/Models/CommunityMerchantMapping.php`:** `final class CommunityMerchantMapping extends Model`. NO `BelongsToUser` trait (global rows). `protected $table = 'community_merchant_mappings';`. `$fillable = ['user_id','pattern','generalized_pattern','name','category','region','contributor']`. PHPDoc properties.

**Tests:**

`Modules/Onboarding/tests/Feature/WizardProgressMigrationTest.php`: `it('creates the wizard_progress table with status trigger pair', function () { ... })`. Assertions: `Schema::hasTable('wizard_progress')` true; `Schema::hasColumns('wizard_progress', ['user_id','step_key','status','data','completed_at'])` true. Create a user via `User::create([...])`, then insert a valid wizard_progress row via DB facade — succeeds. Insert with `status => 'BOGUS'` — `expect(fn () => DB::table(...)->insert([...]))->toThrow(QueryException::class)` AND assert the message contains the substring "Invalid wizard_progress.status value". Minimum 4 assertions. NOTE: tests are allowed to use the `Schema` and `DB` facade per existing test-suite carve-outs in tests/Contracts/BoundaryArchTest.php — production module code is not.

`Modules/Community/tests/Feature/CommunityCorpusMigrationTest.php`: Assert table exists; insert a global row (`user_id => null`) — succeeds; insert two rows with the same `(null, 'PATTERN-X')` — second fails on UNIQUE with `QueryException`; insert two rows with the same pattern but different `user_id` values — both succeed. Minimum 4 assertions.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/Feature/WizardProgressMigrationTest.php Modules/Community/tests/Feature/CommunityCorpusMigrationTest.php --no-coverage</automated>
  </verify>
  <done>
    Both migrations apply cleanly; both migration tests pass with ≥8 assertions total; trigger pair rejects invalid wizard_progress.status values; community_merchant_mappings supports global + per-user override rows with the documented UNIQUE behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: transactions.payment_type column + merchant_aliases + users.community_settings JSON column + PaymentType enum + their models + 3 migration tests</name>
  <files>Modules/Import/Database/Migrations/2026_05_26_000003_add_payment_type_to_transactions.php, Modules/Import/Database/Migrations/2026_05_26_000004_create_merchant_aliases_table.php, Modules/Import/Models/MerchantAlias.php, Modules/Import/Public/Enums/PaymentType.php, Modules/Core/Database/Migrations/2026_05_26_000005_add_community_settings_to_users.php, Modules/Core/Models/User.php, Modules/Import/tests/Feature/PaymentTypeColumnMigrationTest.php, Modules/Import/tests/Feature/MerchantAliasesMigrationTest.php, Modules/Core/tests/Feature/UserCommunitySettingsColumnTest.php</files>
  <read_first>
    - Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php FULL (trigger pair pattern)
    - Modules/Categorization/Database/Migrations/2026_05_17_010003_create_categorization_rules_table.php (per-user table with UNIQUE — model the merchant_aliases shape here)
    - Modules/Categorization/Models/CategorizationRule.php FULL (exact MerchantAlias mirror)
    - Modules/Core/Models/User.php FULL (current $fillable + casts() — this task extends both)
    - Modules/Core/Database/Migrations/2026_05_22_000001_add_theme_to_users.php OR 2026_05_22_000002_add_close_behavior_to_users.php (single-column add-to-users analog for the community_settings JSON column)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md Pitfall 2 lines 627-633 (payment_type re-run safety with hasColumn guard + explicit backfill UPDATE before trigger creation) plus lines 752-798 (PaymentType enum body verbatim)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md (chip glyphs + labels for PaymentType::chipLabel and chipClass)
    - Modules/Ledger/Database/Migrations/*create_transactions_table.php (find which columns transactions has so test inserts can build a minimum-valid row)
    - Modules/Import/tests/TestCase.php (Import module test bootstrap shape)
  </read_first>
  <behavior>
    - `php artisan migrate` applies all 3 migrations cleanly to a fresh database; running migrate twice in a row is idempotent (no SQLSTATE errors on re-run).
    - Inserting a transactions row with payment_type='made_up' raises RAISE(ABORT) with "Invalid transactions.payment_type value".
    - All existing transactions rows after migration carry payment_type='unknown' (backfill via column default + explicit UPDATE statement before trigger creation per Pitfall 2).
    - Inserting two merchant_aliases with the same (user_id, pattern) fails on UNIQUE; merged_from JSON column round-trips through Eloquent cast.
    - PaymentType::from('pin') returns PaymentType::Pin; PaymentType::tryFrom('bogus') returns null; PaymentType::Pin->chipLabel() returns the locked UI-SPEC glyph+word; PaymentType::Pin->chipClass() returns 'pin'; all 8 cases (Pin/Online/Transfer/DirectDebit/Cash/Fee/Refund/Unknown) implemented.
    - `users.community_settings` is a nullable JSON column; the User model exposes it via `$fillable` and `casts()` as an `array`; assigning an array, saving, refreshing, and reading returns the same array.
  </behavior>
  <action>
**Migration 3 — `add_payment_type_to_transactions`:** `2026_05_26_000003_add_payment_type_to_transactions.php`. CRITICAL per Pitfall 2:
  (a) Guard with `if (! $this->schema()->hasColumn('transactions', 'payment_type'))` — wrap the column add.
  (b) `$this->schema()->table('transactions', fn(Blueprint $t) => $t->string('payment_type', 16)->default('unknown')->after('description'))`.
  (c) Add index: `$this->schema()->table('transactions', fn(Blueprint $t) => $t->index(['user_id', 'payment_type']))` (chip-filter hot path on /transactions).
  (d) Explicit backfill BEFORE trigger creation: `$connection->update("UPDATE transactions SET payment_type = 'unknown' WHERE payment_type IS NULL")`.
  (e) THEN create the trigger pair (insert + update OF payment_type), `WHEN NEW.payment_type NOT IN ('pin','online','transfer','direct_debit','cash','fee','refund','unknown')` BEGIN SELECT RAISE(ABORT, 'Invalid transactions.payment_type value'); END. Wrap the whole sequence in a transaction.
Down: drop both triggers, drop index `transactions_user_id_payment_type_index`, drop column.

**Migration 4 — `create_merchant_aliases`:** `2026_05_26_000004_create_merchant_aliases_table.php`. Columns: `$table->id()`, `$table->foreignId('user_id')->constrained('users')->cascadeOnDelete()`, `$table->string('pattern')`, `$table->string('generalized_pattern')`, `$table->string('friendly_name')`, `$table->json('merged_from')->nullable()`, `$table->timestamps()`. Indexes: `$table->unique(['user_id', 'pattern'])`, `$table->index(['user_id', 'generalized_pattern'])`. No triggers. Down: drop table.

**Migration 5 — `add_community_settings_to_users`:** `2026_05_26_000005_add_community_settings_to_users.php`. Mirror the `2026_05_22_000001_add_theme_to_users.php` shape exactly. Guard with `hasColumn` check. `up()` calls `$this->schema()->table('users', fn (Blueprint $t) => $t->json('community_settings')->nullable()->after('close_behavior'));` (after-column choice: pick whichever is the most recent settings-shaped column on users — `close_behavior` per the existing migration list). `down()` drops the column with a `hasColumn` guard. PHPDoc describes the column as the per-user JSON store backing Toggle 1 (Use shared list), Toggle 2 (Offer to contribute), and Toggle 3 (Update on app updates); current-state phrasing only.

**Model `Modules/Import/Models/MerchantAlias.php`:** `final class MerchantAlias extends Model`. Use `BelongsToUser` trait. `protected $table = 'merchant_aliases';`. `$fillable = ['user_id','pattern','generalized_pattern','friendly_name','merged_from']`. Casts: `['merged_from' => 'array']`. PHPDoc properties for Larastan L10. Mirror CategorizationRule.php shape exactly.

**Enum `Modules/Import/Public/Enums/PaymentType.php`:** Copy verbatim from 16.1-RESEARCH.md lines 752-798. All 8 cases: Pin, Online, Transfer, DirectDebit, Cash, Fee, Refund, Unknown. Two methods: `chipLabel(): string` returning glyph + word per UI-SPEC §"Import preview" legend chips; `chipClass(): string` returning the CSS modifier (`pin`/`online`/`transfer`/`dd`/`cash`/`fee`/`refund`/`unknown`). PHPDoc describes the chip-label / chip-class purpose in current-state terms only.

**Modify `Modules/Core/Models/User.php`:** APPEND `'community_settings'` to the `$fillable` list (after `'close_behavior'`); ADD `'community_settings' => 'array'` to the `casts()` return array; ADD `/** @property array<string, mixed>|null $community_settings */` to the class PHPDoc property block. Preserve every other line — no other edits.

**Tests:**

`Modules/Import/tests/Feature/PaymentTypeColumnMigrationTest.php`: Assert `Schema::hasColumn('transactions', 'payment_type')` true; create a user + a minimal valid transactions row WITHOUT specifying payment_type, then assert the inserted row's `payment_type` is `'unknown'` (default); attempt to insert a row with `payment_type => 'made_up'` — assert `QueryException` with "Invalid transactions.payment_type value"; attempt update on existing row setting `payment_type => 'whatever'` — same trigger fires. Minimum 4 assertions.

`Modules/Import/tests/Feature/MerchantAliasesMigrationTest.php`: Assert table + columns exist; insert one valid alias via the MerchantAlias model — succeeds; insert duplicate `(user_id, pattern)` — fails on UNIQUE; the `merged_from` JSON cast round-trips through Eloquent (save with array, refresh, assert array structure). Minimum 4 assertions.

`Modules/Core/tests/Feature/UserCommunitySettingsColumnTest.php`: Assert `Schema::hasColumn('users', 'community_settings')` true. Create a user, set `$user->community_settings = ['useSharedList' => true, 'offerToContribute' => false, 'updateOnAppUpdates' => false]`, `$user->save()`, `$user->refresh()`. Assert `$user->community_settings` is an array with the same three keys and the same values. Assert the column default for a freshly-created user (no assignment) is null. Minimum 4 assertions.

Run `php artisan migrate:fresh` then `vendor/bin/pest --filter='Migration|CommunitySettings'` to validate.
  </action>
  <verify>
    <automated>php artisan migrate:fresh --force --no-interaction && php artisan migrate --no-interaction && vendor/bin/pest Modules/Import/tests/Feature/PaymentTypeColumnMigrationTest.php Modules/Import/tests/Feature/MerchantAliasesMigrationTest.php Modules/Core/tests/Feature/UserCommunitySettingsColumnTest.php --no-coverage</automated>
  </verify>
  <done>
    `php artisan migrate:fresh` succeeds, `php artisan migrate` (second run) is idempotent, all 3 migration tests pass with at least 12 assertions total, PaymentType::from('pin')->chipLabel() returns the UI-SPEC-locked glyph+word, MerchantAlias model's merged_from cast round-trips JSON, trigger pair rejects invalid enum values on transactions.payment_type, `Schema::hasColumn('transactions', 'payment_type')` is true, `users.community_settings` JSON column exists with the User model exposing it as an array cast.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend BoundaryArchTest with 4 new invariants + green Larastan L10 + Pint (plan close-out gate)</name>
  <files>tests/Contracts/BoundaryArchTest.php</files>
  <read_first>
    - tests/Contracts/BoundaryArchTest.php FULL (current invariants — find the existing noLaravelFacadeUsageInModuleCode rule + the allow-list at lines 57-114; locate where new module-bounded toOnlyBeUsedIn invariants are added; find the existing per-module Internal invariant pattern by searching for the substring `Internal'->toOnlyBeUsedIn`)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md lines 1166-1181 (Wave 0 Gaps — 4 new arch invariants enumerated verbatim)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md Shared Pattern A (DI invariants)
    - composer.json scripts section (analyse + format:check + test commands)
    - phpstan.neon or phpstan.neon.dist if present (Larastan L10 strict config; should NOT need modification)
  </read_first>
  <behavior>
    - A new arch invariant rejects any production-source-tree appearance of the 7 enum-value string literals (`pin`,`online`,`transfer`,`direct_debit`,`cash`,`fee`,`refund`) outside `Modules\Import\Public\Enums\PaymentType` (migration files are intentionally excluded — they carry the trigger heredoc).
    - A new arch invariant requires `Modules\Onboarding\Internal\*` to be used only inside `Modules\Onboarding`.
    - A new arch invariant requires `Modules\Community\Internal\*` to be used only inside `Modules\Community`.
    - The existing facade-allow-list arch rule covers `Modules\Onboarding` and `Modules\Community` (no carve-outs).
    - `composer test` exits 0 across the whole suite. `composer analyse` (Larastan L10 strict) exits 0. `composer format:check` (Pint) exits 0.
  </behavior>
  <action>
Modify `tests/Contracts/BoundaryArchTest.php`. After the existing arch invariant suite, append four new arch tests using Pest's `arch()` DSL (mirror existing invariant shapes — search the file for `toOnlyBeUsedIn` and `not->toBeUsed` to find the canonical patterns):

1. `arch('Onboarding Internal namespace stays inside Onboarding module')`: `expect('Modules\Onboarding\Internal')->toOnlyBeUsedIn('Modules\Onboarding')`. Mirror the existing per-module Internal invariant pattern.

2. `arch('Community Internal namespace stays inside Community module')`: same pattern with `Modules\Community\Internal` / `Modules\Community`.

3. `arch('PaymentType string literals stay inside the PaymentType enum')`: First attempt the Pest arch DSL form `expect(['pin','online','transfer','direct_debit','cash','fee','refund'])->not->toBeUsed()` with `->ignoring(['Modules\Import\Public\Enums\PaymentType'])`. If the DSL is identifier-oriented in this version, implement as a custom test: add a sibling file `tests/Contracts/PaymentTypeStringLiteralsTest.php` that runs `shell_exec("grep -rnE \"'(pin|online|transfer|direct_debit|cash|fee|refund)'\" Modules/ --include='*.php' --exclude-dir=tests --exclude-dir=Database 2>/dev/null | grep -v '^[^:]*:\s*\*' | grep -v '^[^:]*:\s*//'")` and asserts the output is empty (no hits outside Database migrations + the enum file + the test tree + comment lines). Strip blank/comment lines from the grep output before the count check; bare `== 0` gates on raw greps are forbidden.

4. `arch('Onboarding + Community modules use no Laravel facades')`: extend the existing facade-allow-list arch rule so its scope covers `Modules\Onboarding\*` and `Modules\Community\*`. The new modules MUST NOT appear in the existing `ignoring()` carve-out list. If the existing invariant already uses a wildcard `Modules\*`, no edit is needed — record that fact in the task summary and skip.

After writing: run the plan close-out quality gate as the final step of this task: `composer test && composer analyse && composer format:check`. Per project rule C-4 fix-every-severity: address every BLOCKER + WARNING + INFO surfaced by Larastan and Pint. If Larastan complains about missing `@phpstan-*` annotations on the new model PHPDoc, add them; if Pint complains about formatting, run `composer format` then re-verify.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/ --no-coverage && composer test && composer analyse && composer format:check</automated>
  </verify>
  <done>
    Four new arch invariants live in tests/Contracts/ and pass. `composer analyse` (Larastan L10 strict) exits 0. `composer format:check` exits 0. `composer test` exits 0 across the whole suite with the new migrations + models + enum + User-extension + tests in place.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner ↔ SQLite | Migrations execute raw SQL (trigger CREATE statements); a malformed heredoc would compromise schema integrity but the heredoc content is hard-coded by the developer, not user-supplied |
| Eloquent model fillable ↔ HTTP request | New models (WizardProgress, MerchantAlias, CommunityMerchantMapping) carry $fillable lists; downstream plans that accept Livewire input must whitelist properties, not bulk-fill from request payloads |
| User.community_settings JSON column ↔ Livewire panel writes | The settings panel writes to a user-scoped JSON column; downstream plans must validate the panel inputs before persisting to the cast array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-01-01 | Tampering | transactions.payment_type column | mitigate | Paired SQLite BEFORE-INSERT/UPDATE triggers enforce enum allow-list at the DB layer; any application bug that writes an unknown value raises RAISE(ABORT) before commit (per Pattern D + Pitfall 2) |
| T-16.1-01-02 | Tampering | wizard_progress.status column | mitigate | Same trigger pair pattern enforces the 4-value status allow-list |
| T-16.1-01-03 | Information Disclosure | merchant_aliases cross-user read | mitigate | BelongsToUser trait + UserScope global scope (Phase 12 contract) auto-filters by current user; downstream plans MUST also use explicit where('user_id', ...) per Pattern E |
| T-16.1-01-04 | Information Disclosure | community_merchant_mappings global rows | accept | Global rows are intentionally readable by every authenticated user (D-13 / D-14); contains no PII (pattern + name + region only); risk is bounded |
| T-16.1-01-05 | Denial of Service | community_merchant_mappings unbounded growth from corpus seeder | mitigate | The seeder (Plan 05) uses updateOrInsert keyed on (user_id, pattern) — idempotent; corpus file is bundled at build time (Plan 05), bounded by file size; the table has user_id+pattern UNIQUE so duplicates can't multiply |
| T-16.1-01-06 | Elevation of Privilege | Migration files using Container::getInstance() (facade-equivalent) outside the documented carve-out | accept | Migration files are explicitly carved out in tests/Contracts/BoundaryArchTest.php (existing precedent); migrations are static developer-authored DDL, not runtime code paths reachable from user input |
| T-16.1-01-07 | Tampering | users.community_settings JSON column written by an unauthorized component | mitigate | Eloquent's array cast + downstream Livewire validation rules; per Shared Pattern E every write filters by current user id; the column has no DB-side enum constraint, so the writing component carries the validation responsibility |
| T-16.1-01-SC | Tampering | composer.json autoload-dev psr-4 additions | accept | No new packages installed; only PSR-4 entries added pointing into Modules/ directories controlled by this repo (no supply-chain surface) |
</threat_model>

<verification>
- `php artisan migrate:fresh && php artisan migrate` exits 0 (idempotent re-run).
- `vendor/bin/pest tests/Contracts/` passes including the 4 new invariants.
- `vendor/bin/pest --filter='Migration|CommunitySettings'` passes with at least 20 assertions across the 5 migration-related tests.
- `composer test` (full Pest suite) is green.
- `composer analyse` (Larastan L10 strict) exits 0.
- `composer format:check` (Pint) exits 0.
- The arch invariant or custom Contracts test for the PaymentType literals returns zero matches outside `Modules/Import/Public/Enums/PaymentType.php` and the documented Database/test carve-outs.
</verification>

<success_criteria>
- All five migrations applied and idempotent on re-run; paired triggers reject invalid enum values for both wizard_progress.status and transactions.payment_type.
- `Modules/Onboarding/` and `Modules/Community/` boot under nwidart with no errors; their tests directories are wired into composer + phpunit + tests/Pest.php.
- `PaymentType` backed enum exists with all 8 cases; `chipLabel()` and `chipClass()` return UI-SPEC-correct values.
- `users.community_settings` JSON column exists; the User model exposes it via $fillable + array cast.
- 4 new arch invariants added to `tests/Contracts/BoundaryArchTest.php` (or a sibling Contracts test); the full Contracts suite is green.
- Larastan L10 strict + Pint clean.
- Zero `.planning/` / PLAN.md / D-NN / RESEARCH.md references in any production source file or PHPDoc.
</success_criteria>

<output>
Create `.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-01-SUMMARY.md` when done.
</output>
</content>
</invoke>