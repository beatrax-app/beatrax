# Phase 16.1: Pattern Map

**Mapped:** 2026-05-25
**Files analyzed:** 73 new / extended
**Analogs found:** 71 / 73 (2 files have NO analog — flagged in §"No Analog Found")

This document is the planner's hand-off. Each file slated for create-or-modify in CONTEXT.md + RESEARCH.md is classified by role + data flow, paired with the closest existing analog in the repo, and accompanied by concrete code excerpts the executor can mirror line-by-line.

Cross-cutting precedents — `Modules/Receipts/Providers/ReceiptsServiceProvider.php` (container tagging), `Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php` (idempotent UserInstalled seed), `Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php` (SQLite trigger-CHECK pair), `Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php` (per-user query + DI shape), `Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php` (Livewire method-DI), `Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php` (Flux modal SFC) — are reused dozens of times below. The §"Shared Patterns" section at the bottom captures them once with full excerpts; per-file rows cite by reference.

---

## File Classification

### New Module: `Modules/Onboarding/` (wizard shell + 6 step components + state table)

| New File | Role | Data Flow | Closest Analog | Match Quality |
|----------|------|-----------|----------------|---------------|
| `Modules/Onboarding/module.json` | module manifest | config | `Modules/Categorization/module.json` | exact |
| `Modules/Onboarding/composer.json` | module composer | config | `Modules/Categorization/composer.json` | exact |
| `Modules/Onboarding/Providers/OnboardingServiceProvider.php` | service provider | wiring | `Modules/Categorization/Providers/CategorizationServiceProvider.php` | exact |
| `Modules/Onboarding/Routes/web.php` | route file | request-response | `Modules/Categorization/Routes/web.php` (also `Modules/DevMode/Routes/web.php` for middleware shape) | exact |
| `Modules/Onboarding/Database/Migrations/YYYY_MM_DD_create_wizard_progress_table.php` | migration | DDL + trigger | `Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php` (trigger-CHECK) + `Modules/Categorization/Database/Migrations/...create_categorization_rules_table.php` (multi-trigger pattern) | exact |
| `Modules/Onboarding/Models/WizardProgress.php` | Eloquent model | CRUD | `Modules/Categorization/Models/CategorizationRule.php` | exact |
| `Modules/Onboarding/Public/Contracts/InitializesWizardProgress.php` | interface | wiring | `Modules/Import/Public/Contracts/RunsImports.php` | exact |
| `Modules/Onboarding/Public/Events/WizardCompleted.php` | event payload | event-driven | `Modules/Core/Public/Events/UserInstalled.php` (referenced) | exact |
| `Modules/Onboarding/Public/Services/WizardProgressQuery.php` | read-only query | request-response | `Modules/Core/Public/Services/SystemAlertQuery.php` (referenced; same per-user shape as `FingerprintStage::classify` user-scoped read) | role-match |
| `Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall.php` | listener | event-driven (UserInstalled) | `Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php` | exact |
| `Modules/Onboarding/Internal/Services/ResumeStepResolver.php` | service | read-only | `Modules/Categorization/Internal/Services/RuleEvaluator.php` (PHP-side decision over query-builder rows) | role-match |
| `Modules/Onboarding/Internal/Services/WizardStepRegistry.php` | service | config | `Modules/Receipts/Internal/MatcherRegistry.php` (sorted iterable holder) | role-match |
| `Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php` | Livewire parent | request-response | `Modules/Import/Internal/Http/Livewire/UploadWizard.php` (multi-step Livewire) + `Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php` (`#[Layout]` + method-DI) | exact |
| `Modules/Onboarding/Internal/Http/Livewire/Steps/WelcomeStep.php` | Livewire step | request-response | `Modules/DevMode/Internal/Http/Livewire/DevOverviewPage.php` (info-only Livewire) | role-match |
| `Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectBankStep.php` | Livewire step | file-upload | `Modules/Import/Internal/Http/Livewire/UploadWizard.php` (WithFileUploads, validate, submit) | exact |
| `Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectCardStep.php` | Livewire step | file-upload | `Modules/Import/Internal/Http/Livewire/UploadWizard.php` (same shape, locked to ICS PDF only) | exact |
| `Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectEmailStep.php` | Livewire step | OAuth-handoff | `Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal.php` (modal is hosted, not duplicated) | exact |
| `Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` | Livewire step | wrapper | `Modules/Import/Internal/Http/Livewire/UploadWizard.php` (`UploadWizard` is mounted inline at 1120px) | exact |
| `Modules/Onboarding/Internal/Http/Livewire/Steps/DoneStep.php` | Livewire step | static | `Modules/DevMode/Internal/Http/Livewire/DevOverviewPage.php` | role-match |
| `Modules/Onboarding/Resources/views/livewire/setup-wizard.blade.php` | Blade view | UI | `Modules/Categorization/Resources/views/livewire/triage-inbox.blade.php` (layout-using page) | role-match |
| `Modules/Onboarding/Resources/views/livewire/steps/*.blade.php` (6 files) | Blade view | UI | UI-SPEC §Wizard chrome + §Welcome/Connector/Done steps (sketches 002D/003C) | NEW — sketch-driven, no in-repo analog of identical geometry |
| `Modules/Onboarding/Resources/views/components/{wiz-card,wiz-dots,wiz-actions,vd-glyph,mini-step,format-chip,drop-zone,privacy-pill}.blade.php` | Blade component | UI primitives | `Modules/DevMode/Resources/views/components/run-card.blade.php`, `status-pill.blade.php`, `tier-chip.blade.php` (atomic Blade components) | role-match |
| `Modules/Onboarding/Resources/views/layouts/app.wizard.blade.php` | layout | UI shell | `resources/views/layouts/app.blade.php` (sibling, no `.side` rail) + `Modules/DevMode/Resources/views/layouts/dev-shell.blade.php` (precedent for module-owned layout) | exact |
| `Modules/Onboarding/tests/Pest.php` | test bootstrap | config | `Modules/Categorization/tests/Pest.php` | exact |
| `Modules/Onboarding/tests/TestCase.php` | test base | config | `Modules/Categorization/tests/TestCase.php` | exact |
| `Modules/Onboarding/tests/Feature/SignupRoutesToSetupTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/RulesPageTest.php` (Livewire + signup pattern) | role-match |
| `Modules/Onboarding/tests/Feature/ResumeWizardTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/RulesPageTest.php` | role-match |
| `Modules/Onboarding/tests/Feature/SkipWizardStepTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/RulesPageTest.php` | role-match |
| `Modules/Onboarding/tests/Feature/EmailStepEmbedsOAuthModalTest.php` | Pest feature | test | `Modules/EmailScan/tests/Feature/...InboxesPage*Test.php` (OAuth modal dispatch assertion) | role-match |
| `Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php` | Pest feature | test | `Modules/Import/tests/Feature/PreviewWizardTest.php` (Livewire markup assertion) | role-match |
| `Modules/Onboarding/tests/Feature/ReRunWizardTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/RulesPageTest.php` | role-match |

### New Module: `Modules/Community/` (corpus + mystery-merchants page + suggest-mapping modal + settings)

| New File | Role | Data Flow | Closest Analog | Match Quality |
|----------|------|-----------|----------------|---------------|
| `Modules/Community/module.json` | module manifest | config | `Modules/Categorization/module.json` | exact |
| `Modules/Community/composer.json` | module composer | config | `Modules/Categorization/composer.json` | exact |
| `Modules/Community/Providers/CommunityServiceProvider.php` | service provider | wiring | `Modules/Categorization/Providers/CategorizationServiceProvider.php` (UserInstalled listener) + `Modules/Receipts/Providers/ReceiptsServiceProvider.php` (Livewire registry) | exact |
| `Modules/Community/Routes/web.php` | route file | request-response | `Modules/Categorization/Routes/web.php` | exact |
| `Modules/Community/Database/Migrations/YYYY_MM_DD_create_community_merchant_mappings_table.php` | migration | DDL | `Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php` (nullable user_id global rows) | exact |
| `Modules/Community/Models/CommunityMerchantMapping.php` | Eloquent model | CRUD | `Modules/Categorization/Models/CategorizationRule.php` (note: global-row model — no `BelongsToUser`) | role-match |
| `Modules/Community/Public/Actions/OpenExternalUrlAction.php` | action | infra-shell | NEW — wraps `Native\Desktop\Contracts\Shell::openExternal`; closest shape is `Modules/Categorization/Public/Actions/CreateCategorizationRule.php` (single-method DI action) | role-match |
| `Modules/Community/Public/Services/CommunityCorpusQuery.php` | read query | request-response | `Modules/Categorization/Public/Services/CategorizationRuleQuery.php` (per-user with system-wide carve-out) | exact |
| `Modules/Community/Public/Events/MysteryMerchantSubmitted.php` | event | event-driven | `Modules/Core/Public/Events/UserInstalled.php` | exact |
| `Modules/Community/Public/Dto/SuggestMappingDto.php` | DTO | DTO | `Modules/Import/Public/Dto/PreviewRowDto.php` (extends `Spatie\LaravelData\Data`) | exact |
| `Modules/Community/Public/Dto/CorpusEntryDto.php` | DTO | DTO | `Modules/Import/Public/Dto/PreviewRowDto.php` | exact |
| `Modules/Community/Internal/Corpus/CorpusLoader.php` | service | file-I/O | `Modules/Receipts/Public/Pipeline/EmlMimeReader.php` (file-read service) + `Modules/Categorization/Database/Seeders/DefaultCategoryTreeSeeder.php` (idempotent bundled data) | role-match |
| `Modules/Community/Internal/Listeners/SeedCommunityCorpus.php` | listener | event-driven (UserInstalled) | `Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php` | exact |
| `Modules/Community/Internal/Services/GitHubCompareUrlBuilder.php` | service | pure-fn | `Modules/Categorization/Internal/Services/RuleEvaluator.php` (pure-PHP service with constructor DI) | role-match |
| `Modules/Community/Internal/Http/Livewire/MysteryMerchantsPage.php` | Livewire page | request-response | `Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php` (page with `#[Layout]` + `#[Url]` filters + cursor pagination) | exact |
| `Modules/Community/Internal/Http/Livewire/SuggestMappingModal.php` | Livewire modal | request-response | `Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php` (Flux modal + `#[On]` open event + dispatch on save) | exact |
| `Modules/Community/Internal/Http/Livewire/SharedListSettingsPanel.php` | Livewire panel | CRUD | `Modules/Core/Internal/Http/Livewire/SettingsPage.php` (settings panel with `.switch` toggle + per-section emit) | exact |
| `Modules/Community/Internal/Http/Livewire/HelpOthersTriageButton.php` | Livewire | UI affordance | `Modules/Categorization/Internal/Http/Livewire/TriageInbox.php` (referenced — this new component nests inside triage row) | role-match |
| `Modules/Community/Resources/views/livewire/mystery-merchants-page.blade.php` | Blade view | UI | `Modules/DevMode/Resources/views/livewire/audit-log-page.blade.php` (paged card list) | role-match |
| `Modules/Community/Resources/views/livewire/suggest-mapping-modal.blade.php` | Blade view | UI | `Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php` (Flux modal with form) | exact |
| `Modules/Community/Resources/views/livewire/shared-list-settings-panel.blade.php` | Blade view | UI | `Modules/Core/Resources/views/livewire/settings-page.blade.php` (settings section + `.switch` toggle) | exact |
| `Modules/Community/Resources/views/components/mystery-card.blade.php` | Blade component | UI | `Modules/DevMode/Resources/views/components/run-card.blade.php` | role-match |
| `Modules/Community/tests/Pest.php` + `TestCase.php` | test bootstrap | config | `Modules/Categorization/tests/Pest.php` + `TestCase.php` | exact |
| `Modules/Community/tests/Feature/CommunityCorpusMigrationTest.php` | Pest feature | test | `Modules/Categorization/tests/Unit/DefaultCategoryTreeSeederTest.php` (migration + seeder shape) | role-match |
| `Modules/Community/tests/Feature/SeedCommunityCorpusTest.php` | Pest feature | test | `Modules/Categorization/tests/Unit/DefaultCategoryTreeSeederTest.php` | exact |
| `Modules/Community/tests/Unit/OpenExternalUrlActionTest.php` | Pest unit | test | NEW — closest is `Modules/DevMode/tests/Unit/RedactSecretsProcessorTest.php` (single-method service with fake collaborators) | role-match |
| `Modules/Community/tests/Feature/SuggestMappingModalSubmitTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/RuleFormModalTest.php` | exact |
| `Modules/Community/tests/Feature/TriageHelpOthersCtaTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/TriagePageTest.php` | exact |
| `resources/corpus/merchant-mappings.yaml` | static asset | data | NEW — no analog. Bundled at build time (see §"No Analog Found" + Phase 18 NativePHP staging hook from `Modules/Desktop/Internal/Native/FirstLaunchBootstrap.php`) | NEW |
| `resources/corpus/built-in-heuristics.yaml` | static asset | data | NEW — same shape as `merchant-mappings.yaml` | NEW |

### Extended Module: `Modules/Import/` (alias model, alias page, payment-type classifier, preview row affordances)

| New / Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---------------------|------|-----------|----------------|---------------|
| `Modules/Import/Database/Migrations/YYYY_MM_DD_add_payment_type_to_transactions.php` | migration | DDL + trigger | `Modules/Core/Database/Migrations/...create_system_alerts_table.php` (trigger-CHECK pair) | exact |
| `Modules/Import/Database/Migrations/YYYY_MM_DD_create_merchant_aliases_table.php` | migration | DDL | `Modules/Categorization/Database/Migrations/...create_categorization_rules_table.php` (per-user table with UNIQUE + index) | exact |
| `Modules/Import/Models/MerchantAlias.php` | Eloquent model | CRUD | `Modules/Categorization/Models/CategorizationRule.php` (BelongsToUser, json cast) | exact |
| `Modules/Import/Public/Contracts/PaymentTypeHinter.php` | interface | contract | `Modules/Receipts/Public/Contracts/SenderMatcher.php` (referenced — same iterable-via-tag shape) | exact |
| `Modules/Import/Public/Enums/PaymentType.php` | enum | type | NEW — no in-repo backed-enum precedent yet. Closest is `Modules/Import/Internal/Pipeline/Stages/ClassifyTransactionType.php` (string constants). Use PHP 8.4 backed enum per RESEARCH.md Code Examples | NEW |
| `Modules/Import/Public/Services/MerchantNameResolver.php` | service | read-only | `Modules/Categorization/Internal/Services/RuleEvaluator.php` (PHP-side string matching after query-builder pull; 5-step precedence walk mirrors candidate-best-score pattern) | exact |
| `Modules/Import/Public/Services/PatternGeneralizer.php` | service | pure-fn | `Modules/Categorization/Internal/Services/RuleEvaluator.php` (mb_-safe PHP string ops) | role-match |
| `Modules/Import/Public/Services/AliasMatchPreviewQuery.php` | read query | request-response | `Modules/Categorization/Internal/Services/RuleEvaluator.php` (explicit user_id + LIMIT-bounded scan + PHP-side match) | exact |
| `Modules/Import/Public/Dto/PaymentTypeHint.php` | DTO | DTO | `Modules/Import/Public/Dto/PreviewRowDto.php` (extends `Spatie\LaravelData\Data`) | exact |
| `Modules/Import/Public/Dto/MerchantAliasDto.php` | DTO | DTO | `Modules/Import/Public/Dto/PreviewRowDto.php` | exact |
| `Modules/Import/Public/Dto/AliasMatchPreviewResultDto.php` | DTO | DTO | `Modules/Import/Public/Dto/PreviewRowDto.php` | exact |
| `Modules/Import/Public/Dto/PreviewRowDto.php` (MODIFY) | DTO extension | DTO | self (add `paymentType` enum + `aliasFriendlyName` properties) | exact (modification) |
| `Modules/Import/Internal/Pipeline/Stages/PaymentTypeClassifierStage.php` | pipeline stage | transform | `Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php` (single-method service in pipeline, DatabaseManager + DI, classify(...) signature, `User` scope) | exact |
| `Modules/Import/Internal/Pipeline/ImportPipeline.php` (MODIFY) | pipeline orchestrator | transform | self (insert `PaymentTypeClassifierStage` after `ClassifyTransactionType`, before `FingerprintStage`) | exact (modification) |
| `Modules/Import/Internal/Parsers/Asn/AsnCsvPaymentTypeHinter.php` | hinter | pure-fn | NEW — closest analog is `Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php` (per-source classifier implementing tagged interface). Co-located beside parser; tag `import.payment_type_hinter`. | role-match |
| `Modules/Import/Internal/Parsers/Asn/AsnCamt053PaymentTypeHinter.php` | hinter | pure-fn | `Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php` | role-match |
| `Modules/Import/Internal/Parsers/Asn/AsnMt940PaymentTypeHinter.php` | hinter | pure-fn | `Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php` | role-match |
| `Modules/Import/Internal/Parsers/Ics/IcsPdfPaymentTypeHinter.php` | hinter | pure-fn | `Modules/Receipts/Internal/Matchers/IcsReceiptMatcher.php` | role-match |
| `Modules/Import/Internal/Parsers/Paypal/PaypalCsvPaymentTypeHinter.php` | hinter | pure-fn | `Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php` | role-match |
| `Modules/Import/Internal/Parsers/DescriptionKeywordFallbackHinter.php` | hinter | pure-fn | `Modules/Categorization/Internal/Services/RuleEvaluator.php` (keyword match against canonical text) | role-match |
| `Modules/Import/Internal/Http/Livewire/AliasesSettingsPage.php` | Livewire page | CRUD + read-preview | `Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php` (filter inputs + cursor pagination + method-DI) + `Modules/Categorization/Internal/Http/Livewire/RulesPage.php` (per-row inline edit + dispatch-to-modal pattern) | exact |
| `Modules/Import/Internal/Http/Livewire/RenameCounterpartyPopover.php` | Livewire popover | request-response | `Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php` (Flux modal SFC, `#[On]` open with prefilled values, save dispatch) | exact |
| `Modules/Import/Internal/Services/LongestCommonPrefix.php` | service | pure-fn | NEW — closest is `Modules/Import/Public/Services/SourceRefRanker.php` (single-purpose pure service). Tested in isolation per RESEARCH.md Pitfall 6. | role-match |
| `Modules/Import/Providers/ImportServiceProvider.php` (MODIFY) | service provider | wiring | self (add per-source hinter tagging + `PaymentTypeClassifierStage` singleton + new Livewire registrations) | exact (modification) |
| `Modules/Import/Resources/views/livewire/upload-wizard.blade.php` (MODIFY) | Blade view | UI | self + UI-SPEC §"Import preview row" (sketch 004D). Currently a simple upload form (87 lines); needs full preview-row Type-chip + click-italic rename + cat-cell three-state markup | modification (significant) |
| `Modules/Import/Resources/views/livewire/preview-wizard.blade.php` (MODIFY) | Blade view | UI | self (add Type column header + chip cell + bulk-confirm legend bar) | exact (modification) |
| `Modules/Import/Resources/views/livewire/aliases-settings-page.blade.php` | Blade view | UI | `Modules/Categorization/Resources/views/livewire/rules-page.blade.php` | exact |
| `Modules/Import/Resources/views/livewire/rename-counterparty-popover.blade.php` | Blade view | UI | `Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php` + UI-SPEC §"Italic-fallback row + rename popover" | exact |
| `Modules/Import/tests/Unit/PatternGeneralizerTest.php` | Pest unit | test | `Modules/Import/tests/Unit/SourceRefRankerTest.php` (pure-fn unit) | exact |
| `Modules/Import/tests/Unit/MerchantNameResolverTest.php` | Pest unit | test | `Modules/Categorization/tests/Unit/RuleEvaluatorSpecificityTest.php` | exact |
| `Modules/Import/tests/Unit/LongestCommonPrefixTest.php` | Pest unit | test | `Modules/Import/tests/Unit/SourceRefRankerTest.php` | exact |
| `Modules/Import/tests/Unit/PaymentTypeHinterRegistryTest.php` | Pest unit | test | NEW — closest is `Modules/Receipts/tests/Unit/Matchers/...` (verifies tagged-collector wiring) | role-match |
| `Modules/Import/tests/Feature/PaymentTypeColumnMigrationTest.php` | Pest feature | migration test | `Modules/Categorization/tests/Unit/DefaultCategoryTreeSeederTest.php` (migration verification pattern) | role-match |
| `Modules/Import/tests/Feature/PaymentTypeClassifierStageTest.php` | Pest feature | test | `Modules/Import/tests/Unit/FingerprintStageClassifyTest.php` | exact |
| `Modules/Import/tests/Feature/AliasesSettingsPageTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/RulesPageTest.php` | exact |
| `Modules/Import/tests/Feature/AliasMatchPreviewQueryTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/RuleEvaluatorTest.php` | role-match |
| `Modules/Import/tests/Feature/BulkMergeAliasesTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/RulesPageTest.php` | role-match |
| `Modules/Import/tests/Feature/RenameCounterpartyPopoverTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/RuleFormModalTest.php` | exact |
| `Modules/Import/tests/Feature/RenamePopoverSeedsRuleTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/RuleFormModalTest.php` + `MerchantMemoryWriterTest.php` | role-match |
| `Modules/Import/tests/Feature/ImportAliasesYamlTest.php` | Pest feature | test | NEW — closest is `Modules/Import/tests/Feature/UploadWizardEmailFileTest.php` (Livewire file-upload feature test) | role-match |
| `Modules/Import/tests/Snapshot/AliasYamlRoundTripTest.php` | Pest snapshot | test | NEW — closest is existing `tests/Snapshot/` directory. Pattern: load YAML → dump → assert byte-equal | role-match |

### Extended Module: `Modules/EmailScan/` (OAuth re-consent detection)

| New / Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---------------------|------|-----------|----------------|---------------|
| `Modules/EmailScan/Internal/OAuth/ReconsentRequiredException.php` | typed exception | sentinel | `Modules/EmailScan/Internal/OAuth/InvalidGrantException.php` | exact |
| `Modules/EmailScan/Internal/Listeners/RaiseReconsentAlertOnTokenFailure.php` | listener | event-driven | `Modules/EmailScan/Internal/Listeners/EmitOAuthReauthRequiredAlert.php` (SAME concern; this listener is the new event-triggered sibling — listens to a token-failure event rather than the file-presence trigger) | exact |
| `Modules/EmailScan/Internal/Http/Livewire/InboxesPage.php` (MODIFY) | Livewire page | request-response | self (add `#[Url(as: 'reconnect')]` query param + `mount()` to auto-open OAuth modal against that inbox) | exact (modification) |
| `Modules/EmailScan/tests/Unit/RaiseReconsentAlertOnTokenFailureTest.php` | Pest unit | test | `Modules/Categorization/tests/Unit/...` (idempotent-listener tests) | role-match |
| `Modules/EmailScan/tests/Feature/InboxesPageReconnectParamTest.php` | Pest feature | test | `Modules/EmailScan/tests/Feature/...InboxesPage*Test.php` (existing OAuth modal-dispatch assertions) | exact |

### Extended Module: `Modules/Core/` (system-alert message kind extension)

| New / Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---------------------|------|-----------|----------------|---------------|
| `Modules/Core/Resources/views/livewire/partials/system-alert-message.blade.php` (MODIFY) | Blade partial | UI | self (add `@case ('oauth_reconsent_required')` branch with `metadata.inbox_id` interpolation + Reconnect button) | exact (modification) |
| `Modules/Core/tests/Feature/SystemAlertsBannerOAuthReconsentTest.php` | Pest feature | test | `Modules/Categorization/tests/Feature/...Banner*Test.php` (existing banner-render assertions) | role-match |

### Extended Module: `Modules/Desktop/` (route exemption for /setup)

| Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---------------|------|-----------|----------------|---------------|
| `Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php` (MODIFY) | middleware | request-response | self (add `'setup'` to `EXEMPT_ROUTE_PREFIXES` array, per RESEARCH.md Pitfall 1) | exact (modification — 1 line) |

### Repo-Level: Config + autoload + phpunit + tests/Pest

| Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---------------|------|-----------|----------------|---------------|
| `composer.json` (MODIFY) | autoload-dev | config | self (add `Modules\\Onboarding\\Tests\\` + `Modules\\Community\\Tests\\` PSR-4 entries) | exact (modification) |
| `phpunit.xml` (MODIFY) | testsuite registration | config | self (add Onboarding + Community Unit/Feature directories to existing `Unit` + `Feature` testsuites) | exact (modification) |
| `tests/Pest.php` (MODIFY) | module test bootstrap | config | self (add `'Modules/Onboarding' => Modules\Onboarding\Tests\TestCase::class` + `'Modules/Community' => ...` to the loop at lines 22–62) | exact (modification) |
| `tests/Contracts/BoundaryArchTest.php` (MODIFY — add invariants) | arch test | test | self (add 4 new arch rules per RESEARCH.md §"Wave 0 Gaps") | exact (modification) |
| `config/community.php` | config | config | NEW — `Modules/Categorization/config/*` not present; closest is no-config-precedent. Holds `github_compare_base` key resolved via env `BEATRAX_GITHUB_COMPARE_BASE`. | role-match |

---

## Pattern Assignments

The per-file blocks below cite line-numbered excerpts from analog files. The executor copies the pattern (shape, DI signature, error handling, logging) — never the literal column names / branches / business logic.

### `Modules/Onboarding/Providers/OnboardingServiceProvider.php` (service provider, wiring)

**Analog:** `Modules/Categorization/Providers/CategorizationServiceProvider.php`

**Imports + class shape** (lines 1–46):
```php
<?php
declare(strict_types=1);
namespace Modules\Categorization\Providers;

use Illuminate\Contracts\Events\Dispatcher;
use Illuminate\Support\ServiceProvider;
use Livewire\LivewireManager;
use Modules\Categorization\Internal\Http\Livewire\TriageInbox;
// ... more Livewire imports
use Modules\Categorization\Internal\Listeners\SeedDefaultCategoryTree;
use Modules\Core\Public\Events\UserInstalled;

/**
 * Wires the Categorization module: [...]
 */
final class CategorizationServiceProvider extends ServiceProvider
```

**Singleton bindings + UserInstalled listener** (lines 48–82):
```php
public function register(): void
{
    $this->app->bind(AssignsCategory::class, AssignCategory::class);
    $this->app->singleton(UncategorizedTriageQuery::class);
    $this->app->singleton(RuleEvaluator::class);
    $this->app->singleton(CreateCategorizationRule::class);
}

public function boot(Dispatcher $events, LivewireManager $livewire): void
{
    $this->loadMigrationsFrom(__DIR__.'/../Database/Migrations');
    $this->loadRoutesFrom(__DIR__.'/../Routes/web.php');
    $this->loadRoutesFrom(__DIR__.'/../Routes/console.php');
    $this->loadViewsFrom(__DIR__.'/../Resources/views', 'categorization');

    $events->listen(UserInstalled::class, SeedDefaultCategoryTree::class);

    $livewire->component('categorization.triage-inbox', TriageInbox::class);
}
```

**Action:** Mirror the structure (constructor-free, `boot()` returns void with `Dispatcher` + `LivewireManager` DI). Register the wizard's parent `SetupWizard` + 6 step components under `onboarding.setup-wizard`, `onboarding.steps.welcome`, etc. Wire `InitializeWizardProgressOnInstall` to `UserInstalled` after `SeedDefaultCategoryTree` (order matters only insofar as both should commit before redirect to `/setup`; they are sibling listeners). View namespace: `onboarding`.

### `Modules/Community/Providers/CommunityServiceProvider.php` (service provider, wiring)

**Analog:** `Modules/Receipts/Providers/ReceiptsServiceProvider.php` (for the `class_exists()`-guarded singleton + tag pattern, even though Community's listener is single not tagged) + `Modules/Categorization/Providers/CategorizationServiceProvider.php` (for the UserInstalled listener registration).

**Action:** Bind `OpenExternalUrlAction`, `CorpusLoader`, `GitHubCompareUrlBuilder`, `CommunityCorpusQuery` as singletons. Bind NoOpShell fallback for `Native\Desktop\Contracts\Shell` only when the desktop binding is not present (RESEARCH.md Pitfall 7). Subscribe `SeedCommunityCorpus` to `UserInstalled`. Register Livewire: `community.mystery-merchants-page`, `community.suggest-mapping-modal`, `community.shared-list-settings-panel`, `community.help-others-triage-button`.

### `Modules/Import/Providers/ImportServiceProvider.php` (MODIFY) — add container-tagged hinters

**Analog:** `Modules/Receipts/Providers/ReceiptsServiceProvider.php` lines 60–119 — **MIRROR EXACTLY**.

**Hinter FQN constant + tagging loop** (lines 60–91 of analog):
```php
private const MATCHER_FQNS = [
    'Modules\\Receipts\\Internal\\Matchers\\PaypalReceiptMatcher',
    'Modules\\Receipts\\Internal\\Matchers\\IcsReceiptMatcher',
    'Modules\\Receipts\\Internal\\Matchers\\GooglePlayReceiptMatcher',
];

public function register(): void
{
    foreach (self::MATCHER_FQNS as $fqn) {
        if (class_exists($fqn)) {
            $this->app->singleton($fqn);
            $this->app->tag([$fqn], 'receipts.matcher');
        }
    }
```

**Container-tagged collector singleton** (lines 103–119 of analog):
```php
$this->app->singleton(
    MatcherRegistry::class,
    static function (Container $app): MatcherRegistry {
        /** @var iterable<SenderMatcher> $tagged */
        $tagged = $app->tagged('receipts.matcher');
        $matchers = [];
        foreach ($tagged as $matcher) {
            $matchers[] = $matcher;
        }
        usort(
            $matchers,
            static fn (SenderMatcher $a, SenderMatcher $b): int => $b->priority() <=> $a->priority(),
        );

        return new MatcherRegistry($matchers);
    },
);
```

**Action:** In `Modules/Import/Providers/ImportServiceProvider.php::register()`, add a `HINTER_FQNS` constant listing the 6 `PaymentTypeHinter` implementations (`AsnCsvPaymentTypeHinter`, `AsnCamt053PaymentTypeHinter`, `AsnMt940PaymentTypeHinter`, `IcsPdfPaymentTypeHinter`, `PaypalCsvPaymentTypeHinter`, `DescriptionKeywordFallbackHinter`). Tag each `'import.payment_type_hinter'`. Bind `PaymentTypeClassifierStage` as a singleton whose closure resolves the tagged iterable. No usort priority needed (classifier merges by confidence + registration order). Also add Livewire registrations for `import.aliases-settings-page` and `import.rename-counterparty-popover`.

### `Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall.php` (listener, event-driven)

**Analog:** `Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php` — **MIRROR EXACTLY** (the cleanest 1-method listener in the repo).

**Full file** (lines 1–24):
```php
<?php
declare(strict_types=1);
namespace Modules\Categorization\Internal\Listeners;

use Modules\Categorization\Database\Seeders\DefaultCategoryTreeSeeder;
use Modules\Core\Public\Events\UserInstalled;

final class SeedDefaultCategoryTree
{
    public function __construct(private readonly DefaultCategoryTreeSeeder $seeder) {}

    public function handle(UserInstalled $event): void
    {
        $this->seeder->run();
    }
}
```

**Action:** Same shape. Constructor-DI a `WizardProgressInitializer` collaborator (or call `DatabaseManager::connection()->table('wizard_progress')->insert(...)` directly inside `handle()` — researcher favored a service for testability). Insert 6 rows: `(welcome, connect-bank, connect-card, connect-email, first-import, done)` with `status='pending'`. Use `firstOrCreate` keyed by `(user_id, step_key)` so a re-fire is a no-op (C-7 idempotency rule).

### `Modules/Community/Internal/Listeners/SeedCommunityCorpus.php` (listener, event-driven)

**Analog:** `Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php` + RESEARCH.md §"Pattern 2" verbatim shape.

**Action:** Constructor DI: `CorpusLoader $loader, DatabaseManager $db, LoggerInterface $logger`. `handle(UserInstalled $event)` loads `resources/corpus/merchant-mappings.yaml` via the loader, then for each `CorpusEntryDto` calls `$db->connection()->table('community_merchant_mappings')->updateOrInsert(['pattern' => $entry->pattern, 'user_id' => null], [...])`. Wrap per-entry in `try/catch (Throwable)` and `$logger->warning('SeedCommunityCorpus: skipped malformed entry.', [...])` — partial-failure tolerance per RESEARCH.md REQ CORPUS-02.

### `Modules/Onboarding/Database/Migrations/...create_wizard_progress_table.php` (migration, DDL + trigger)

**Analog:** `Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php` (trigger-as-CHECK precedent) + `Modules/Categorization/Database/Migrations/...create_categorization_rules_table.php` (multi-trigger version).

**Trigger-pair shape** (system_alerts lines 70–82):
```php
$connection = $this->db()->connection($this->getConnection());

$connection->statement(<<<'SQL'
    CREATE TRIGGER system_alerts_severity_check_insert BEFORE INSERT ON system_alerts FOR EACH ROW
    WHEN NEW.severity NOT IN ('info','warning','critical')
    BEGIN SELECT RAISE(ABORT, 'Invalid system_alerts.severity value'); END
SQL);
$connection->statement(<<<'SQL'
    CREATE TRIGGER system_alerts_severity_check_update BEFORE UPDATE OF severity ON system_alerts FOR EACH ROW
    WHEN NEW.severity NOT IN ('info','warning','critical')
    BEGIN SELECT RAISE(ABORT, 'Invalid system_alerts.severity value'); END
SQL);
```

**DatabaseManager helper pattern** (system_alerts lines 94–108):
```php
private function schema(): Builder
{
    return $this->db()->connection($this->getConnection())->getSchemaBuilder();
}

private function db(): DatabaseManager
{
    if ($this->resolvedDb === null) {
        /** @var DatabaseManager $db */
        $db = Container::getInstance()->make(DatabaseManager::class);
        $this->resolvedDb = $db;
    }
    return $this->resolvedDb;
}
```

**Action:** Use the same helper-method shape (Migration files cannot constructor-DI, so the `Container::getInstance()->make(...)` carve-out is the established pattern). Create `wizard_progress` with `(id, user_id FK cascade, step_key string 64, status string 16, data json nullable, completed_at nullable, timestamps)`, `unique(['user_id','step_key'])`, `index(['user_id','status'])`. Trigger pair guarding `status IN ('pending','in_progress','done','skipped')`. `down()` drops triggers then table.

### `Modules/Import/Database/Migrations/...add_payment_type_to_transactions.php` (migration, DDL + trigger)

**Analog:** `Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php` (trigger-CHECK) + RESEARCH.md §"Pattern 3" verbatim.

**Action:** Use `Schema::hasColumn` guard (Pitfall 2). ALTER TABLE adds `payment_type` string(16) with `->default('unknown')` (backfills automatically). Add `index(['user_id','payment_type'])`. THEN create the trigger pair guarding `payment_type IN ('pin','online','transfer','direct_debit','cash','fee','refund','unknown')`. Run an explicit `UPDATE transactions SET payment_type = 'unknown' WHERE payment_type IS NULL` AFTER column add but BEFORE trigger create (Pitfall 2). `down()` drops triggers + column.

### `Modules/Import/Database/Migrations/...create_merchant_aliases_table.php` (migration, DDL)

**Analog:** `Modules/Categorization/Database/Migrations/...create_categorization_rules_table.php` (per-user table with UNIQUE + index).

**Action:** Per-user table `(id, user_id FK cascade, pattern string, generalized_pattern string, friendly_name string, merged_from json nullable, timestamps)`. `unique(['user_id','pattern'])`, `index(['user_id','generalized_pattern'])`. No triggers (no enum columns).

### `Modules/Community/Database/Migrations/...create_community_merchant_mappings_table.php` (migration, DDL)

**Analog:** `Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php` (global rows via nullable `user_id`).

**Action:** `(id, user_id FK nullable cascade, pattern, generalized_pattern nullable, name, category nullable, region string(8) nullable, contributor string(64), timestamps)`. `unique(['user_id','pattern'])` (allows future per-user overrides while keeping global rows unique by pattern with NULL user_id). `index(['generalized_pattern'])`. No triggers (no constrained enum).

### `Modules/Import/Internal/Pipeline/Stages/PaymentTypeClassifierStage.php` (pipeline stage)

**Analog:** `Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php` — **MIRROR EXACTLY** (shape, DI, single classify(...) method, `User` user-scope).

**Constructor + per-user query** (FingerprintStage lines 71–95):
```php
final class FingerprintStage
{
    public function __construct(
        private readonly FingerprintComposer $fingerprints,
        private readonly DatabaseManager $db,
        private readonly SourceRefRanker $ranker,
    ) {}

    public function classify(CanonicalTransaction $tx, User $user): FingerprintDisposition
    {
        $fingerprint = $this->fingerprints->compose($tx);

        $existing = $this->db->connection()
            ->table('transactions')
            ->where('user_id', $user->id)
            ->where('fingerprint', $fingerprint)
            ->first([/* columns */]);
```

**Action:** Constructor DI: `iterable<PaymentTypeHinter> $hinters` (resolved via tag in `ImportServiceProvider`), `LoggerInterface $logger`. Method `run(CanonicalTransaction $tx, User $user): CanonicalTransaction` — iterate hinters, collect non-null hints, pick highest-confidence (tie-break by hinter registration order), call `$tx->withPaymentType($type)` (add the wither to `CanonicalTransaction` — see Ledger module). Fallback to `PaymentType::Unknown` if no hints. Slot in `ImportPipeline::preview()` right after `$this->classifier->run($normalized, $user)` and before `$this->autoCategory->apply(...)` (RESEARCH.md Architecture Diagram).

### `Modules/Import/Public/Services/MerchantNameResolver.php` (service, read-only)

**Analog:** `Modules/Categorization/Internal/Services/RuleEvaluator.php` (PHP-side string matching after query-builder pull; user-scoped; mb_-safe).

**Per-user pull + PHP-side match** (RuleEvaluator lines 56–100, paraphrased shape):
```php
final class RuleEvaluator
{
    public function __construct(private readonly DatabaseManager $db) {}

    public function evaluate(CanonicalTransaction $tx, User $user): RuleEvaluationOutcome
    {
        $userId = $user->id;
        $connection = $this->db->connection();

        /** @var iterable<stdClass> $rules */
        $rules = $connection
            ->table('categorization_rules')
            ->where('user_id', $userId)
            ->where('active', true)
            ->get();

        foreach ($rules as $rule) {
            // ... mb_strpos / mb_strtolower comparisons in PHP, never SQL LIKE
        }
    }
}
```

**Action:** RESEARCH.md §"Pattern 4" gives the 5-step precedence walk verbatim. Constructor DI `DatabaseManager`. Method `resolve(string $rawDescription, int $userId): ?string`. Walk: (a) `merchant_aliases.pattern` exact by `(user_id, pattern)` — short-circuit if hit; (b) iterate user generalized aliases (PHP-side `mb_strpos`); (c) `community_merchant_mappings.pattern` exact `whereNull('user_id')`; (d) iterate community generalized; (e) `return null`.

### `Modules/Import/Public/Services/AliasMatchPreviewQuery.php` (read query, debounced live test)

**Analog:** `Modules/Categorization/Internal/Services/RuleEvaluator.php` (explicit user_id + bounded SELECT + PHP-side match) + RESEARCH.md §"Test against my transactions" code example verbatim.

**Action:** Constructor DI `DatabaseManager`. Method `preview(string $generalizedPattern, int $userId): AliasMatchPreviewResultDto`. Reject patterns < 3 chars. `select(['id','description','counterparty_name','booked_at','amount_minor'])`, `where('user_id', $userId)`, `orderByDesc('booked_at')->limit(500)` — bounded scan window. Walk in PHP with `mb_strpos(mb_strtolower(...), mb_strtolower($needle))`. Return DTO with total count + first 5 matches. Read-only — never call `update()`.

### `Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php` (Livewire parent component)

**Analog:** `Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php` (method-DI on `render()`, `#[Layout]` attribute) + `Modules/Categorization/Internal/Http/Livewire/RulesPage.php` (sub-component dispatch via `#[On]`).

**Layout attribute + method-DI render** (AuditLogPage lines 28–48):
```php
#[Layout('dev::layouts.dev-shell')]
final class AuditLogPage extends Component
{
    #[Url(as: 'tier', except: '')]
    public string $tierFilter = '';

    public function render(ViewFactory $views, DatabaseManager $db): View
    {
        // ...
        return $views->make('dev::livewire.audit-log-page', [
            'rows' => $rendered,
        ]);
    }
}
```

**Action:** `#[Layout('onboarding::layouts.app-wizard')]` on the SetupWizard parent. Public properties: `int $currentStepIndex`, `array $steps` (from `WizardStepRegistry`). Methods: `mount(ResumeStepResolver $resolver, CurrentUser $currentUser)` resolves the resume step; `next()` / `skip()` / `skipRest()` mutate the `wizard_progress` row via DI'd `DatabaseManager`. `render(ViewFactory $views)` returns the wrapper view that includes the active step's child Livewire component.

### `Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectBankStep.php` (Livewire step, file-upload)

**Analog:** `Modules/Import/Internal/Http/Livewire/UploadWizard.php` (lines 39–217 of `UploadWizard.php` — WithFileUploads + Validate + cascading select + submit() that calls RunsImports).

**Key excerpts (UploadWizard.php):**

Trait + properties (lines 39–77):
```php
final class UploadWizard extends Component
{
    use WithFileUploads;

    public const SUPPORTED_FORMATS = ['asn-csv', 'asn-camt053', /* ... */];

    public ?TemporaryUploadedFile $file = null;
    public ?string $uploadError = null;

    #[Validate('required|in:asn,ics,paypal,email-file')]
    public string $issuer = 'asn';
```

Rules + closure (lines 113–154):
```php
public function rules(): array
{
    $sizeRule = match ($this->sourceFormat) {
        'mbox' => 'max:1048576',
        'eml' => 'max:20480',
        default => 'max:10240',
    };

    return [
        'file' => ['required', 'file', $sizeRule, 'extensions:csv,txt,xml,sta,mt940,940,pdf,eml,mbox'],
        'sourceFormat' => ['required', 'in:...', $this->issuerFormatRule()],
    ];
}
```

Submit (action-DI) (lines 215–272):
```php
public function submit(
    RunsImports $importer,
    CurrentUser $currentUser,
    UrlGenerator $urls,
    LoggerInterface $logger,
): void {
    $this->uploadError = null;
    $this->validate();
    if ($this->file === null) return;
    $user = $currentUser->user();
    try {
        $preview = $importer->runFromUpload($this->file->getRealPath(), $this->sourceFormat, $user, $originalFilename);
    } catch (Throwable $e) {
        $logger->error('UploadWizard: import preview failed.', [/* ... */]);
        $this->uploadError = sprintf('Could not process this file (%s). The full error is in /dev/logs.', $e::class);
        return;
    }
    $this->redirect($urls->route('imports.preview', ['id' => $preview->importRunId]), navigate: false);
}
```

**Action:** Mirror `WithFileUploads`. Lock `$sourceFormat = 'asn-camt053'` (recommended) but allow CSV/MT940 per UI-SPEC format chips. After `RunsImports::runFromUpload` succeeds, advance the wizard step (write `wizard_progress` row to `done` + `completed_at`) and dispatch a Livewire event to the parent SetupWizard. Catch-all error to `$uploadError` via the same `LoggerInterface` shape.

### `Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectEmailStep.php` (Livewire step, OAuth handoff)

**Analog:** `Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal.php` — the modal **is hosted, not duplicated**. Mount via Livewire `<livewire:email-scan.o-auth-client-wizard-modal />` and dispatch `oauth-client-wizard:open` with provider name from the buttons.

**Dispatch pattern** (OAuthClientWizardModal lines 64–91):
```php
#[On('oauth-client-wizard:open')]
public function open(string $provider): void
{
    $this->provider = in_array($provider, ['gmail', 'microsoft'], strict: true) ? $provider : null;
    // ... reset fields
    if ($this->provider !== null) {
        $this->dispatch('modal-show', name: 'oauth-client-wizard-'.$this->provider);
    }
}
```

**Action:** The ConnectEmailStep blade contains two buttons "Authorize with Gmail" / "Authorize with Outlook" that dispatch `$this->dispatch('oauth-client-wizard:open', provider: 'gmail')` from the Livewire layer. The modal is mounted inside the step blade. Wire a `#[On('oauth-client-wizard:saved')]` listener on the step that marks the email step `done` and advances. The step ALSO supports "Skip — set up later in Settings" which marks the step `skipped`.

### `Modules/EmailScan/Internal/Listeners/RaiseReconsentAlertOnTokenFailure.php` (listener)

**Analog:** `Modules/EmailScan/Internal/Listeners/EmitOAuthReauthRequiredAlert.php` — **MIRROR EXACTLY** (the dedup-via-existence-check pattern).

**Dedup pre-check + alert insert** (EmitOAuthReauthRequiredAlert lines 71–87):
```php
// De-dup: an un-acknowledged warning is already on the banner.
$alreadyAlerted = $connection->table('system_alerts')
    ->where('user_id', $userId)
    ->where('kind', self::REAUTH_KIND)
    ->whereNull('acknowledged_at')
    ->exists();
if ($alreadyAlerted) {
    return;
}

SystemAlert::query()->create([
    'user_id' => $userId,
    'kind' => self::REAUTH_KIND,
    'severity' => 'warning',
    'message' => self::MESSAGE,
]);
```

**Action:** Constructor DI `DatabaseManager $db, LoggerInterface $logger`. Triggered from a new typed `ReconsentRequiredException` raised inside `GmailApiClient::refresh()` / `GraphApiClient::refresh()` (or via a `TokenRefreshFailed` event the listener subscribes to). Dedup query uses `json_extract(metadata, '$.inbox_id')` per RESEARCH.md Pitfall 4 (with `LIKE '%"inbox_id":N%'` fallback). Severity `warning`. Metadata `{'inbox_id' => $inboxId, 'provider' => $provider}`. NEVER log the OAuth token (V7 ASVS).

### `Modules/Community/Internal/Http/Livewire/SuggestMappingModal.php` (Livewire modal)

**Analog:** `Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php` (Flux modal + `#[On]` open + dispatch on save) + RESEARCH.md §"Suggest-mapping modal".

**Action:** `#[On('suggest-mapping:open')]` opens with `pattern` prefilled (read-only field 1). Public properties: `string $pattern`, `string $name`, `?string $category`, `string $region = 'NL'`. Submit action constructor-DI's nothing (Livewire forbids); method-DI's `OpenExternalUrlAction $opener, GitHubCompareUrlBuilder $urls`. Build URL via `$urls->build($pattern, $name, $category, $region)`. Call `($opener)($url)`. Dispatch toast on success + `modal-hide`. Flux `<flux:modal name="suggest-mapping" dismissible>` wrapper (NOT triple-gate — non-destructive).

### `Modules/Community/Public/Actions/OpenExternalUrlAction.php` (action, DI wrapper)

**Analog:** RESEARCH.md §"Code Examples" verbatim. Closest in-repo is `Modules/Categorization/Public/Actions/CreateCategorizationRule.php` (single-method `__invoke` DI action shape).

**Action:** Constructor DI `Native\Desktop\Contracts\Shell $shell, LoggerInterface $logger`. `__invoke(string $url): void` — validate `FILTER_VALIDATE_URL` + `str_starts_with('https://')` (defense-in-depth per ASVS V9), call `$shell->openExternal($url)`, log `info` with URL. Throw `InvalidArgumentException` for non-HTTPS. **DI bind via service provider** — never reference `Native\Laravel\Facades\Shell` (Pitfall 7). Arch test invariant: `Native\Desktop\Contracts\Shell::class` may only be imported by this file.

### `Modules/Community/Internal/Http/Livewire/MysteryMerchantsPage.php` (Livewire page)

**Analog:** `Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php` — paginated cards with cursor + `#[Layout]` + method-DI on `render()`.

**Action:** `#[Layout('layouts.app')]` (uses main app layout, not wizard). `render(ViewFactory $views, DatabaseManager $db, CurrentUser $currentUser)` queries `transactions` for the user's unidentified merchant codes (description NOT matching any alias / corpus row). Stats strip: count corpus mappings, count user's unidentified rows, etc. Card click dispatches `suggest-mapping:open` with the row's raw description.

### `Modules/Import/Internal/Http/Livewire/AliasesSettingsPage.php` (Livewire page + read preview)

**Analog:** `Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php` (cursor + filter + method-DI) + `Modules/Categorization/Internal/Http/Livewire/RulesPage.php` (per-row inline edit + dispatch-to-modal + bulk action).

**Bulk action delete pattern** (RulesPage lines 67–86):
```php
public function deleteRule(int $ruleId, CurrentUser $currentUser, DeleteCategorizationRule $delete): void
{
    try {
        ($delete)($currentUser->user(), $ruleId);
    } catch (NotFoundHttpException) {
        $this->confirmingDeleteId = null;
        $this->flashMessage = 'Rule not found (it may have been deleted in another tab).';
        return;
    }
    $this->confirmingDeleteId = null;
    $this->flashMessage = 'Rule deleted.';
}
```

**Action:** List 25/page with cursor. Per-row inline-editable `generalized_pattern` input with `wire:model.live.debounce.400ms="editing.{id}.pattern"` (Pitfall 5). Live "Test against my transactions" preview via `AliasMatchPreviewQuery` (DI'd in the action method). Checkbox-select for bulk-merge: action `mergeSelected(LongestCommonPrefix $lcp, ...)` computes LCP, opens confirm modal. Export action returns `Symfony\Component\Yaml\Yaml::dump($aliases->toArray())` via `Response::streamDownload`. Import action uses `WithFileUploads`, parses with `Yaml::parse(..., Yaml::PARSE_EXCEPTION_ON_INVALID_TYPE)` (ASVS V10), shows preview diff before commit.

### `Modules/Import/Internal/Http/Livewire/RenameCounterpartyPopover.php` (Livewire popover)

**Analog:** `Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php` (`#[On('rule-form:open')]` pattern + prefilled values + save dispatch).

**Action:** `#[On('rename-counterparty:open')]` with `raw` (the raw description), `rowIndex`, optional `currentCategoryId`. Properties: `string $friendly`, `bool $remember = true`, `string $generalized` (computed live from `PatternGeneralizer` via method-DI on an `updated()` hook). Save action method-DI's `MerchantAliasRepository`, `PatternGeneralizer`, optionally `CreateCategorizationRule` (RENAME-03). Dispatch `rename-counterparty:saved` event for the parent UploadWizard/PreviewWizard to refresh.

### `Modules/Import/Models/MerchantAlias.php` (Eloquent model)

**Analog:** `Modules/Categorization/Models/CategorizationRule.php` — **MIRROR EXACTLY** (BelongsToUser trait, fillable, casts, table override).

**Full structure** (CategorizationRule.php lines 40–60):
```php
final class CategorizationRule extends Model
{
    use BelongsToUser;
    /** @var string|null */
    protected $table = 'categorization_rules';
    /** @var list<string> */
    protected $fillable = [
        'user_id', 'field', 'match', 'value', 'category_id', 'hits_count', 'active', 'notes',
    ];
    /** @return array<string, string> */
    protected function casts(): array { /* ... */ }
}
```

**Action:** `final class MerchantAlias extends Model { use BelongsToUser; protected $table = 'merchant_aliases'; protected $fillable = ['user_id','pattern','generalized_pattern','friendly_name','merged_from']; cast 'merged_from' => 'array'; }`. PHPDoc properties for Larastan L10.

### `Modules/Import/Public/Dto/PreviewRowDto.php` (MODIFY — DTO extension)

**Analog:** self (the existing 50-line `final class PreviewRowDto extends Data`).

**Action:** Add two new readonly properties to the constructor: `public readonly ?PaymentType $paymentType = null` and `public readonly ?string $aliasFriendlyName = null`. Keep all existing properties + their position; new properties go at the end with sensible defaults so existing call-sites (`new PreviewRowDto(rowIndex: ...)`) keep compiling. Update PHPDoc.

### `Modules/Import/Public/Enums/PaymentType.php` (PHP 8.4 backed enum)

**Analog:** NEW shape. RESEARCH.md §"Code Examples" gives the full enum verbatim.

**Action:** Copy the enum body from RESEARCH.md lines 752–798 verbatim. Add `from()` + `tryFrom()` (built-in for backed enums). `chipLabel()` + `chipClass()` methods inside. Arch invariant: no PHP literal `'pin'|'online'|...` outside this file (RESEARCH.md §"Wave 0 Gaps").

### `Modules/Import/Public/Services/PatternGeneralizer.php` (service, pure-fn)

**Analog:** RESEARCH.md §"Code Examples" gives the full implementation verbatim (lines 877–938).

**Action:** Copy the algorithm verbatim. Constructor-free OK (no collaborators, pure-fn). Unit test exercises every classifier branch (PIN-tail, terminal-id, SEPA-noise, amount, date-fragment).

### `Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php` (MODIFY)

**Analog:** self (lines 57–61 — `EXEMPT_ROUTE_PREFIXES`).

**Action:** Add `'setup'` to the array. One-line change. Single test: assert `/setup` is not redirected away by the middleware when DB is ready (Pitfall 1).

### `Modules/Core/Resources/views/livewire/partials/system-alert-message.blade.php` (MODIFY)

**Analog:** self (the existing switch-on-kind structure, lines 16–70).

**Action:** Add `@case ('oauth_reconsent_required')` branch before `@default`. Pull `metadata.inbox_id` + `metadata.provider` defensively (mirror the `is_array($alert->metadata)` + `isset(...) && is_string(...)` shape). Render: "Reconnect your Gmail" / "Reconnect your Outlook" with a `<a href="/inboxes?reconnect={{ $inboxId }}" class="...">Reconnect →</a>` link. Match the existing severity-pill styling.

### Blade view — `Modules/Onboarding/Resources/views/livewire/setup-wizard.blade.php` (and step views)

**Analog:** No direct equivalent. UI-SPEC §"Component Inventory" specifies the wizard chrome (`.wiz-page`, `.wiz-card`, `.wiz-dots`, `.wiz-actions`, `.wiz-footer`, `.privacy-pill`) — these are NEW Blade components. Closest CSS-shape precedent for atomic components is `Modules/DevMode/Resources/views/components/run-card.blade.php` (~60 lines, slot + props).

**Action:** Use Blade components (`<x-onboarding::wiz-card>...</x-onboarding::wiz-card>`). Each step's view composes them. The CSS classes (`.wiz-card`, `.mini`, `.format-chip`, `.drop-zone`, `.ptype-chip`) live in `resources/css/app.css` under `@layer components` (UI-SPEC §Theme Wiring). The `.ptype-chip.pin` / `.ptype-chip.cash` two-line color inline (PIN-violet, cash-dark-amber) per UI-SPEC §Color, not in `@theme`.

### Blade view — `Modules/Import/Resources/views/livewire/upload-wizard.blade.php` (MODIFY)

**Analog:** self (existing 87-line file) + UI-SPEC §"Italic-fallback row + rename popover", §"Category cell", §"Import preview" sketch 004D.

**Action:** Significant rewrite. New markup: legend bar above table, Type column header, per-row `.ptype-chip` cell, `.cp-cell` containing either `.cp-name` (normal) or `.desc-fallback` (italic-clickable wired to `$dispatch('rename-counterparty:open', { raw: '...', rowIndex: N })`), `.cat-cell` three-state, `.preview-foot` live counter, mount `<livewire:import.rename-counterparty-popover />` once at the bottom. Reference `Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php` for Flux primitives.

### Pest test files (~25 new files)

**Analog patterns:**
- **Pure-fn unit:** `Modules/Import/tests/Unit/SourceRefRankerTest.php` shape — `it('strips PIN-tail tokens')` + dataset.
- **Livewire feature:** `Modules/Categorization/tests/Feature/RulesPageTest.php` lines 1–80 shape — `beforeEach` creates user + actingAs, `Livewire::test(RulesPage::class)->assertSee(...)->call('method', $arg)->assertDispatched('event')`.
- **Migration:** `Modules/Categorization/tests/Unit/DefaultCategoryTreeSeederTest.php` — assert column exists + trigger fires on invalid value.
- **Snapshot:** existing `tests/Snapshot/` directory + `spatie/pest-plugin-snapshots` — round-trip YAML.

**Action:** Mirror these shapes. Bootstrap each new module's `tests/Pest.php` + `tests/TestCase.php` from Categorization's 14- and 15-line files exactly. Add the two modules to `phpunit.xml` `<testsuite name="Unit">` and `<testsuite name="Feature">` and to `tests/Pest.php` loop lines 22–62.

---

## Shared Patterns

These five patterns are referenced by 10+ files above. Read once, apply everywhere.

### Pattern A — Constructor DI for services, method-DI for Livewire components

**Source:** Project rule C-1 (`MEMORY.md/feedback_laravel_di_only.md`). Enforced by `tests/Contracts/BoundaryArchTest.php` lines 57–114.

**Apply to:** Every new service, listener, action, query (constructor DI). Every Livewire component (method-DI on `render()` + each action method — see comment in `Modules/Core/Internal/Http/Livewire/SettingsPage.php` lines 28–38 stating "Livewire strict-rules ruleset forbids constructor-DI on Component subclasses").

**Excerpt — service constructor DI** (FingerprintStage lines 73–77):
```php
public function __construct(
    private readonly FingerprintComposer $fingerprints,
    private readonly DatabaseManager $db,
    private readonly SourceRefRanker $ranker,
) {}
```

**Excerpt — Livewire method-DI** (SystemAlertsBanner lines 35–51):
```php
public function render(
    CurrentUser $currentUser,
    SystemAlertQuery $query,
    ViewFactory $views,
): View {
    $user = $currentUser->user();
    $alerts = $query->active($user);
    return $views->make('core::livewire.system-alerts-banner', ['alerts' => $alerts]);
}

public function acknowledge(int $alertId, AcknowledgeSystemAlert $action, CurrentUser $currentUser): void
{
    $action($alertId, $currentUser->user());
}
```

### Pattern B — Container-tagged collaborators (per-source plugin registry)

**Source:** `Modules/Receipts/Providers/ReceiptsServiceProvider.php` lines 60–119.

**Apply to:** `PaymentTypeHinter` collection in `ImportServiceProvider::register()`. Future similar shape if Onboarding ever gains pluggable steps.

**Full excerpt** — see "Action" for `Modules/Import/Providers/ImportServiceProvider.php` above. The shape is:
1. `const TAG_FQNS = [/* class names */]` listing tagged implementations.
2. Loop: `class_exists()` guard, `singleton()`, `tag([$fqn], 'tag.name')`.
3. Register the collector singleton with a closure that pulls `$app->tagged('tag.name')` into an array and hands it to the consumer.

### Pattern C — Idempotent install-time listener (`UserInstalled`)

**Source:** `Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php` (full 24-line file) + `Modules/Categorization/Providers/CategorizationServiceProvider.php` line 74 (`$events->listen(UserInstalled::class, SeedDefaultCategoryTree::class)`).

**Apply to:** `InitializeWizardProgressOnInstall` (Onboarding), `SeedCommunityCorpus` (Community).

**Excerpt** (SeedDefaultCategoryTree full file, lines 16–24):
```php
final class SeedDefaultCategoryTree
{
    public function __construct(private readonly DefaultCategoryTreeSeeder $seeder) {}

    public function handle(UserInstalled $event): void
    {
        $this->seeder->run();
    }
}
```

Idempotency comes from the seeder/inserter using `updateOrInsert(['unique_key' => ...], [...])` or `firstOrCreate(...)`. The listener itself is a one-method wrapper — keep it that way.

### Pattern D — SQLite trigger-as-CHECK enum guard pair

**Source:** `Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php` lines 70–82 (single-column) + `Modules/Categorization/Database/Migrations/...create_categorization_rules_table.php` (multi-column).

**Apply to:** `wizard_progress.status`, `transactions.payment_type`.

**Excerpt** — see `system_alerts_severity_check_insert` heredoc above. The full migration class also requires the `DatabaseManager` `Container::getInstance()->make(...)` helper because migration files cannot constructor-DI.

### Pattern E — Per-user query with explicit `where('user_id', …)` (never trust global scope in CLI/queue/test)

**Source:** `Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php` lines 79–95 (the comment at lines 53–58 explicitly documents WHY the explicit filter is mandatory) + `Modules/Categorization/Internal/Services/RuleEvaluator.php` lines 80–84.

**Apply to:** `MerchantNameResolver`, `AliasMatchPreviewQuery`, `WizardProgressQuery`, `CommunityCorpusQuery`, every new read query and listener that runs outside an authenticated HTTP context.

**Excerpt** (FingerprintStage lines 83–95):
```php
$existing = $this->db->connection()
    ->table('transactions')
    ->where('user_id', $user->id)
    ->where('fingerprint', $fingerprint)
    ->first([
        'id', 'source_ref', 'source_format',
        'counterparty_name', 'description', 'currency', 'amount_minor',
    ]);
```

Companion arch invariant lives in `tests/Contracts/BoundaryArchTest.php` — multi-user readiness (C-8) requires every user-scoped read to be explicit.

### Pattern F — Flux modal SFC + `#[On]` open event + dispatch on save

**Source:** `Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php` lines 1–50 + `Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php` + `Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal.php` lines 64–91 (`#[On('oauth-client-wizard:open')]`).

**Apply to:** `RenameCounterpartyPopover`, `SuggestMappingModal`.

**Excerpt** (rule-form-modal.blade.php lines 1–17):
```blade
{{-- Mounted globally in app.blade.php; dispatched via the
     `rule-form:open` Livewire event (optional ruleId param hydrates
     edit mode). Save fires `rule-form:saved` then dispatches
     `modal-hide`. --}}
<div>
    <flux:modal name="rule-form" dismissible>
        <div class="space-y-6">
            <flux:heading size="lg">
                {{ $isEditMode ? 'Edit rule' : 'New rule' }}
            </flux:heading>
            <form wire:submit="save" class="space-y-6">
```

### Pattern G — Livewire feature test (Livewire::test → assertSee → call → assertDispatched)

**Source:** `Modules/Categorization/tests/Feature/RulesPageTest.php` lines 1–90.

**Apply to:** Every new Livewire feature test (~15 files).

**Excerpt** (RulesPageTest.php lines 13–34):
```php
beforeEach(function (): void {
    $this->user = User::create([
        'username' => 'rules-page',
        'password' => 'opensesame',
        'period_start_day' => 1,
    ]);
    $this->actingAs($this->user);
    // ... seed dependent rows
});

it('renders the empty state when the user has no rules', function (): void {
    Livewire::test(RulesPage::class)
        ->assertSee('No rules yet')
        ->assertSee('Create your first rule');
});
```

---

## No Analog Found

Two files have no in-repo precedent. Planner should use RESEARCH.md examples + new shapes:

| File | Role | Data Flow | Reason | Guidance |
|------|------|-----------|--------|----------|
| `resources/corpus/merchant-mappings.yaml` | static YAML data | bundled-data | First YAML data file shipped with the app | Schema fixed by D-13. Researcher recommends seeding ~30-50 well-known Dutch merchants. Path resolved at runtime via `UserDataPathService` / `base_path('resources/corpus/...')`. Ensure Phase 18 `prebuild` NativePHP staging script (see `Modules/Desktop/Internal/Native/FirstLaunchBootstrap.php`) globs `resources/corpus/**`. |
| `resources/corpus/built-in-heuristics.yaml` | static YAML data | bundled-data | Same — first bundled built-in heuristics. | Same schema as community corpus + `contributor: 'built-in'`. Researcher recommends 12 Dutch consumer brands (Albert Heijn, Jumbo, Lidl, Spotify, Netflix, Vattenfall, Ziggo, KPN, NS, Bol.com, Coolblue, Eneco). |

The PHP 8.4 backed enum `Modules/Import/Public/Enums/PaymentType.php` is also strictly NEW (no existing backed-enum in the repo) but RESEARCH.md §"Code Examples" lines 754–798 give the full implementation verbatim — copy-paste with no analog risk.

---

## Metadata

**Analog search scope:**
- `Modules/Auth/`, `Modules/Categorization/`, `Modules/Chains/`, `Modules/Core/`, `Modules/Desktop/`, `Modules/DevMode/`, `Modules/EmailScan/`, `Modules/Import/`, `Modules/Ingestion/`, `Modules/Ledger/`, `Modules/Receipts/`
- `resources/views/layouts/app.blade.php`, `tests/Pest.php`, `tests/Contracts/BoundaryArchTest.php`, `phpunit.xml`, `composer.json`

**Files scanned (full read):**
- `Modules/Receipts/Providers/ReceiptsServiceProvider.php`
- `Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php`
- `Modules/Categorization/Providers/CategorizationServiceProvider.php`
- `Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php`
- `Modules/Import/Providers/ImportServiceProvider.php`
- `Modules/Import/Internal/Pipeline/ImportPipeline.php`
- `Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php`
- `Modules/EmailScan/Internal/Listeners/EmitOAuthReauthRequiredAlert.php`
- `Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php`
- `Modules/Core/Resources/views/livewire/partials/system-alert-message.blade.php`
- `Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php`
- `Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php`
- `Modules/Import/Public/Dto/PreviewRowDto.php`
- `Modules/Import/Public/Contracts/RunsImports.php`
- `Modules/Categorization/Models/CategorizationRule.php`
- `Modules/Categorization/tests/{Pest,TestCase}.php`
- `Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php`
- `Modules/Categorization/Internal/Pipeline/ApplyAutoCategoryStage.php`
- `Modules/Categorization/Internal/Listeners/MerchantMemoryWriter.php`
- `Modules/Categorization/Internal/Services/RuleEvaluator.php` (head)
- `Modules/Categorization/Database/Seeders/DefaultCategoryTreeSeeder.php` (head)
- `Modules/Import/Internal/Http/Livewire/UploadWizard.php`
- `Modules/Import/Resources/views/livewire/upload-wizard.blade.php` (head)
- `Modules/Import/Resources/views/livewire/preview-wizard.blade.php` (row-render section)
- `Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal.php` (head)
- `tests/Pest.php`
- `tests/Contracts/BoundaryArchTest.php` (facade allow-list)
- `phpunit.xml` (testsuite shape)
- `composer.json` (autoload-dev shape)

**Files identified by directory listing (shape inferred):**
- Every `module.json` + `composer.json` across modules
- `Modules/{Module}/Routes/web.php` for every module that has one
- `Modules/Categorization/Internal/Http/Livewire/RulesPage.php` (head + delete pattern)
- `Modules/Core/Internal/Http/Livewire/SettingsPage.php` + view (head)
- `Modules/Core/Resources/views/livewire/app-sidebar.blade.php` (side-item structure)

**Pattern extraction date:** 2026-05-25
