---
phase: 16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena
plan: 03a
type: execute
wave: 2
depends_on: [16.1-01]
files_modified:
  - Modules/Onboarding/Routes/web.php
  - Modules/Onboarding/Public/Services/WizardProgressQuery.php
  - Modules/Onboarding/Internal/Services/WizardStepRegistry.php
  - Modules/Onboarding/Internal/Services/WizardProgressInitializer.php
  - Modules/Onboarding/Internal/Services/ResumeStepResolver.php
  - Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall.php
  - Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/WelcomeStep.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/DoneStep.php
  - Modules/Onboarding/Resources/views/layouts/app-wizard.blade.php
  - Modules/Onboarding/Resources/views/livewire/setup-wizard.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/welcome-step.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/done-step.blade.php
  - Modules/Onboarding/Providers/OnboardingServiceProvider.php
  - Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php
  - Modules/Auth/Public/Actions/SignupAction.php
  - Modules/Onboarding/tests/Feature/SignupRoutesToSetupTest.php
  - Modules/Onboarding/tests/Feature/ResumeWizardTest.php
  - Modules/Onboarding/tests/Feature/ReRunWizardTest.php
  - Modules/Onboarding/tests/Unit/ResumeStepResolverTest.php
  - Modules/Onboarding/tests/Unit/WizardProgressInitializerTest.php
autonomous: true
requirements: [WIZ-01, WIZ-02, WIZ-06]
must_haves:
  truths:
    - "After signup, the next HTTP response redirects to /setup (the wizard root)"
    - "Killing the app mid-wizard and re-launching drops the user back into the same step with a 'Welcome back' banner"
    - "/setup?force=1 resets every wizard_progress row to 'pending' and re-enters from step 1"
    - "/setup is exempt from EnsureDatabaseReady redirects so the wizard doesn't loop back to /welcome"
    - "The wizard parent component, the WelcomeStep, the DoneStep, the body-only app-wizard layout, the supporting state services (WizardStepRegistry, WizardProgressInitializer, ResumeStepResolver, WizardProgressQuery), and the UserInstalled → InitializeWizardProgressOnInstall listener are wired and tested"
    - "Plan 03b's 4 connector step components mount under the parent SetupWizard via Livewire component registration without further parent changes"
  artifacts:
    - path: "Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php"
      provides: "Wizard parent component with resume detection + skip + skip-the-rest + force-reset"
      contains: "WizardProgress"
    - path: "Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall.php"
      provides: "UserInstalled listener seeding 6 wizard_progress rows"
      contains: "UserInstalled"
    - path: "Modules/Onboarding/Routes/web.php"
      provides: "/setup route + /setup?force=1 reset behavior"
      contains: "setup"
    - path: "Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php"
      provides: "EXEMPT_ROUTE_PREFIXES extended with 'setup' (1-line change)"
      contains: "'setup'"
  key_links:
    - from: "Modules/Auth/Public/Actions/SignupAction.php"
      to: "Modules/Core/Public/Events/UserInstalled + Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall"
      via: "event dispatch on signup commit (Phase 16 commit 529fd7a)"
      pattern: "UserInstalled"
    - from: "Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php"
      to: "Modules/Onboarding/Internal/Services/ResumeStepResolver"
      via: "method-DI on mount() method"
      pattern: "ResumeStepResolver"
    - from: "Modules/Auth/Public/Actions/SignupAction.php"
      to: "/setup route redirect"
      via: "UrlGenerator route('setup') redirect on success (replaces existing dashboard redirect)"
      pattern: "route\\('setup'\\)|/setup"
---

<objective>
Ship the wizard's structural backbone: state services + UserInstalled listener + EnsureDatabaseReady middleware exemption + /setup route + signup redirect + SetupWizard parent Livewire + the two static steps (WelcomeStep, DoneStep) + body-only app-wizard layout + 3 acceptance tests covering signup-routes-to-setup, resume, and force-reset. The four connector step components (Bank, Card, Email, FirstImport) land in Plan 03b which depends on this plan.

Purpose: D-01 / D-02 / D-03 / D-04 (structural half). WIZ-01 / WIZ-02 / WIZ-06. The wizard's parent + state machine is the gating dependency for Plan 03b's connector steps + every other Phase 16.1 surface that assumes a user has been through (or skipped) the wizard. Splitting connector steps into Plan 03b keeps each plan within ~50% context.

Output: Full wizard backbone booted; SetupWizard parent renders the WelcomeStep + DoneStep; signup → /setup redirect lands; kill-and-resume drops back into the right step; ?force=1 resets cleanly; all 5 listed tests green; Larastan L10 strict + Pint clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-CONTEXT.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md

@CLAUDE.md
@.claude/skills/sketch-findings-diederik/SKILL.md
@.claude/skills/sketch-findings-diederik/references/onboarding-wizard.md

@Modules/Auth/Public/Actions/SignupAction.php
@Modules/Core/Public/Events/UserInstalled.php
@Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php
@Modules/Categorization/Providers/CategorizationServiceProvider.php

@Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php

@Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php
@Modules/DevMode/Internal/Http/Livewire/DevOverviewPage.php

@Modules/Core/Public/Contracts/CurrentUser.php
@Modules/Core/Public/Services/CurrentUserService.php

@Modules/Onboarding/Models/WizardProgress.php
@Modules/Onboarding/Public/Events/WizardCompleted.php
@Modules/Onboarding/Providers/OnboardingServiceProvider.php
@Modules/Categorization/Routes/web.php

<interfaces>
From Modules/Core/Public/Contracts/CurrentUser.php (CurrentUser contract — every Livewire method that needs the authenticated user method-DIs this):

```
interface CurrentUser { public function user(): \Modules\Core\Models\User; public function userId(): int; }
```

From Modules/Auth/Public/Actions/SignupAction.php (verified at line ~132-138 per RESEARCH.md — dispatches UserInstalled after DB commit).

From Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php (EXEMPT_ROUTE_PREFIXES — 3 entries today, this plan adds 'setup').
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wizard state services + UserInstalled listener + middleware exemption + route + signup redirect</name>
  <files>Modules/Onboarding/Routes/web.php, Modules/Onboarding/Public/Services/WizardProgressQuery.php, Modules/Onboarding/Internal/Services/WizardStepRegistry.php, Modules/Onboarding/Internal/Services/WizardProgressInitializer.php, Modules/Onboarding/Internal/Services/ResumeStepResolver.php, Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall.php, Modules/Onboarding/Providers/OnboardingServiceProvider.php, Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php, Modules/Auth/Public/Actions/SignupAction.php, Modules/Onboarding/tests/Unit/WizardProgressInitializerTest.php, Modules/Onboarding/tests/Unit/ResumeStepResolverTest.php, Modules/Onboarding/tests/Feature/SignupRoutesToSetupTest.php</files>
  <read_first>
    - Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php FULL (24-line listener — exact mirror target per PATTERNS.md Shared Pattern C)
    - Modules/Categorization/Providers/CategorizationServiceProvider.php lines 1-90 (where $events->listen(UserInstalled::class, ...) is registered + view namespace + Livewire registrations)
    - Modules/Auth/Public/Actions/SignupAction.php FULL (verify the existing redirect target + dispatch site; the redirect change is small but load-bearing)
    - Modules/Core/Public/Events/UserInstalled.php (payload shape)
    - Modules/Categorization/Routes/web.php (route registration shape)
    - Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php FULL (the existing EXEMPT_ROUTE_PREFIXES + the redirect logic — 1-line change adds 'setup'; per RESEARCH.md Pitfall 1)
    - Modules/Onboarding/Models/WizardProgress.php (Plan 01 artifact)
    - Modules/Categorization/Database/Seeders/DefaultCategoryTreeSeeder.php (idempotent seeder pattern using updateOrCreate)
    - Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php lines 71-95 (constructor DI + DatabaseManager + explicit where('user_id', ...) — Shared Pattern E)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 271-301 (InitializeWizardProgressOnInstall analog + Pitfall 1 fix narrative)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md Open Question #2 (resume detector branches: in_progress → that step; only pending → first pending; all done|skipped → redirect to /)
    - tests/Pest.php (RefreshDatabase + module TestCase bootstrap)
    - Modules/Categorization/tests/Feature/RulesPageTest.php lines 1-40 (Livewire feature test pattern with actingAs + Livewire::test → assertSee → call → assertDispatched)
  </read_first>
  <behavior>
    - `WizardStepRegistry::steps(): list<string>` returns the canonical 6-step order: `['welcome', 'connect-bank', 'connect-card', 'connect-email', 'first-import', 'done']`.
    - `WizardProgressInitializer::initialize(int $userId): void` writes 6 `wizard_progress` rows via `updateOrInsert` (idempotent — re-fire is a no-op) — all status `pending`. Uses an injected `Illuminate\Support\DateFactory` for the `updated_at` timestamp (NOT the `now()` helper, per project rule C-1).
    - `InitializeWizardProgressOnInstall::handle(UserInstalled $event): void` delegates to the initializer.
    - `ResumeStepResolver::resolve(int $userId): string` returns the step_key of the first `in_progress` row, OR the first `pending` row in registry order, OR the sentinel `''` (empty string) meaning "all done/skipped → redirect to /".
    - `WizardProgressQuery::list(int $userId): array` returns the user's 6 wizard_progress rows keyed by step_key, with status + completed_at — used by SetupWizard's `mount()` to render the progress dots.
    - `Modules/Onboarding/Routes/web.php` registers `Route::get('/setup', SetupWizard::class)->middleware(['web', 'auth'])->name('setup')`. The route NAME `'setup'` is critical.
    - `EnsureDatabaseReady::EXEMPT_ROUTE_PREFIXES` array is extended to include `'setup'`.
    - `SignupAction::__invoke()` continues to dispatch `UserInstalled` after commit (existing behavior). The redirect-to-/setup change happens at the calling Livewire signup component layer (find via `grep -rn "SignupAction" Modules/Auth/`), using a method-injected `Illuminate\Routing\Redirector` (NOT the `redirect()` helper, per rule C-1).
    - Three tests pass: WizardProgressInitializerTest (idempotent re-fire creates exactly 6 rows the first call AND still 6 rows on the second call), ResumeStepResolverTest (3 branches per Open Question #2), SignupRoutesToSetupTest (POST signup → 302 to /setup).
  </behavior>
  <action>
**Service `Modules/Onboarding/Internal/Services/WizardStepRegistry.php`** — `final class WizardStepRegistry`. Method `steps(): array` returns the 6-step list verbatim. Method `isSkippable(string $stepKey): bool` returns true for `connect-bank`, `connect-card`, `connect-email`; false otherwise. Constructor empty. PHPDoc describes the registry contract (current-state only per rule C-3).

**Service `Modules/Onboarding/Internal/Services/WizardProgressInitializer.php`** — `final class WizardProgressInitializer`. Constructor DI: `private readonly WizardStepRegistry $registry, private readonly \Illuminate\Database\DatabaseManager $db, private readonly \Illuminate\Support\DateFactory $dates`. Method `initialize(int $userId): void` runs inside `$this->db->connection()->transaction(...)`, iterates the registry's steps, and calls `$this->db->connection()->table('wizard_progress')->updateOrInsert(['user_id' => $userId, 'step_key' => $step], ['status' => 'pending', 'updated_at' => $this->dates->now()])`. Idempotent via the UNIQUE(user_id, step_key) constraint + updateOrInsert. PHPDoc describes the upsert behavior.

**Service `Modules/Onboarding/Internal/Services/ResumeStepResolver.php`** — `final class ResumeStepResolver`. Constructor DI: `private readonly WizardStepRegistry $registry, private readonly \Illuminate\Database\DatabaseManager $db`. Method `resolve(int $userId): string`: query wizard_progress for the user; if any row has status=`in_progress` return that step_key; else iterate the registry's step order and return the first step whose row status=`pending`; else return `''` (sentinel — all done/skipped).

**Service `Modules/Onboarding/Public/Services/WizardProgressQuery.php`** — `final class WizardProgressQuery`. Constructor DI: `private readonly \Illuminate\Database\DatabaseManager $db, private readonly WizardStepRegistry $registry`. Method `list(int $userId): array` returns `array<string, array{status: string, completed_at: ?string}>` keyed by step_key, in registry order (so the progress-dots render is deterministic). Always returns 6 entries — missing rows render as `['status' => 'pending', 'completed_at' => null]` (defensive).

**Listener `Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall.php`** — mirror `SeedDefaultCategoryTree` exactly: constructor DI of WizardProgressInitializer; `handle(UserInstalled $event): void` delegates `$this->initializer->initialize($event->userId)`.

**Modify `Modules/Onboarding/Providers/OnboardingServiceProvider.php`** — `register()` adds singleton bindings for `WizardStepRegistry`, `WizardProgressInitializer`, `ResumeStepResolver`, `WizardProgressQuery`. `boot(Dispatcher $events)` adds `$events->listen(\Modules\Core\Public\Events\UserInstalled::class, InitializeWizardProgressOnInstall::class)`. Routes: `$this->loadRoutesFrom(__DIR__.'/../Routes/web.php')`. View namespace: already 'onboarding' (set in Plan 01 task 1).

**Route `Modules/Onboarding/Routes/web.php`** — declare strict_types, use the `Route` facade, register `Route::middleware(['web', 'auth'])->group(...)` with `Route::get('/setup', SetupWizard::class)->name('setup');`. SetupWizard is added in Task 2 — the route definition lands here now so the next task's component has a registered name to bind against.

**Modify `Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php`** — 1-line change. Append `'setup'` to the `EXEMPT_ROUTE_PREFIXES` array. Per RESEARCH.md Pitfall 1 — without this the user signs up, hits /, gets redirected back to /welcome, falls back to /signup → infinite loop. Add a docblock line above the array entry: "Setup wizard surface gates its own first-launch UX (the user is already past welcome)."

**Modify `Modules/Auth/Public/Actions/SignupAction.php`** — read the file FIRST. If the action itself does the redirect (returns a RedirectResponse) change the redirect target to `$this->redirector->route('setup')` using a method-injected or constructor-injected `Illuminate\Routing\Redirector`. If the redirect lives in the calling Livewire signup component (more likely per Laravel/Livewire patterns), modify that component's submit() method instead — find it via `grep -rn "SignupAction" Modules/Auth/`. The change is small: replace the dashboard route name with `'setup'`. Per rule C-1: NEVER call the global `redirect()` helper — inject `Illuminate\Routing\Redirector` (constructor DI on services, method DI on Livewire actions).

**Test `Modules/Onboarding/tests/Unit/WizardProgressInitializerTest.php`** — Pest unit, RefreshDatabase. Create a user; resolve `WizardProgressInitializer` via `app()`; call `initialize($user->id)` → assert `DB::table('wizard_progress')->where('user_id', $user->id)->count() === 6` AND every row has status='pending'; call `initialize($user->id)` AGAIN → assert still 6 rows (idempotent). Minimum 4 assertions.

**Test `Modules/Onboarding/tests/Unit/ResumeStepResolverTest.php`** — Pest unit, RefreshDatabase. Create user + initialize. Per Open Question #2 three branches:
1. Set one row to status='in_progress' → `resolve($user->id)` returns that step_key.
2. Set all rows to status='pending' → returns 'welcome' (first in registry).
3. Set every row to 'done' or 'skipped' → returns '' (sentinel).
Minimum 3 assertions across the 3 branches.

**Test `Modules/Onboarding/tests/Feature/SignupRoutesToSetupTest.php`** — Pest feature, RefreshDatabase. Render the signup Livewire component (find via `Modules/Auth/Internal/Http/Livewire/`), fill in valid credentials, submit. Assert: `assertRedirect('/setup')` OR `assertRedirectToRoute('setup')`. Also assert post-signup: the user exists, 6 wizard_progress rows exist for that user. Minimum 3 assertions.

Per project rule C-1: every service constructor-DIs collaborators (no facades, no `now()` / `redirect()` / `event()` / `auth()` / `abort()` / `response()` helpers — use `Illuminate\Support\DateFactory`, `Illuminate\Routing\Redirector`, `Illuminate\Contracts\Events\Dispatcher`, the project's `CurrentUser` contract, and concrete exceptions / `Illuminate\Contracts\Routing\ResponseFactory` instead). Per C-2: no GSD leakage. Per C-3: PHPDoc current-state only. Per C-4: fix every Larastan/Pint severity surfaced.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/Unit/WizardProgressInitializerTest.php Modules/Onboarding/tests/Unit/ResumeStepResolverTest.php Modules/Onboarding/tests/Feature/SignupRoutesToSetupTest.php --no-coverage</automated>
  </verify>
  <done>
    Three new tests pass with ≥10 assertions total; UserInstalled listener wires correctly (signup creates exactly 6 wizard_progress rows); /setup route exists and is exempt from EnsureDatabaseReady; post-signup HTTP redirect lands on /setup.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SetupWizard parent + WelcomeStep + DoneStep + app-wizard layout + setup-wizard blade + the 2 static-step blades</name>
  <files>Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php, Modules/Onboarding/Internal/Http/Livewire/Steps/WelcomeStep.php, Modules/Onboarding/Internal/Http/Livewire/Steps/DoneStep.php, Modules/Onboarding/Resources/views/layouts/app-wizard.blade.php, Modules/Onboarding/Resources/views/livewire/setup-wizard.blade.php, Modules/Onboarding/Resources/views/livewire/steps/welcome-step.blade.php, Modules/Onboarding/Resources/views/livewire/steps/done-step.blade.php, Modules/Onboarding/Providers/OnboardingServiceProvider.php</files>
  <read_first>
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md FULL (Wizard chrome lines 180-194, Welcome step lines 195-210, Final step lines 257-267, Component Inventory lines 437-466, Color lines 113-165, Spacing lines 37-74, Density rules lines 53-65)
    - .claude/skills/sketch-findings-diederik/references/onboarding-wizard.md FULL (sketches 002 + 003 visual lock)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 425-510 (SetupWizard analog cites)
    - Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php lines 28-60 (#[Layout] attribute + method-DI on render() — exact pattern for SetupWizard's parent)
    - Modules/DevMode/Internal/Http/Livewire/DevOverviewPage.php (info-only Livewire — pattern for WelcomeStep + DoneStep)
    - Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php (existing banner pattern for the "Welcome back" notice)
    - resources/views/layouts/app.blade.php (sidebar-bearing layout; app-wizard.blade.php is a SIBLING — body-only, no sidebar)
    - Modules/Core/Public/Contracts/CurrentUser.php
  </read_first>
  <behavior>
    - Navigating to `/setup` (with an authenticated user) renders the wizard using the `onboarding::layouts.app-wizard` layout (body-only, no sidebar).
    - `SetupWizard::mount(ResumeStepResolver, CurrentUser, WizardProgressQuery, WizardProgressInitializer, DatabaseManager, Redirector, Request, DateFactory)` reads the resume step; if the resolver returns `''` (all done/skipped) the component returns a `Redirector::to('/')` response. When `?force=1`, every wizard_progress row for the user is reset to 'pending' + completed_at=null via an explicit `where('user_id', $user->id)` update using the method-injected DatabaseManager + DateFactory.
    - SetupWizard tracks `string $currentStepKey` as the active step; renders the active step's child Livewire component inline via blade `@switch($currentStepKey)`.
    - SetupWizard exposes 3 listener methods (all method-DI): `next(DatabaseManager, CurrentUser, WizardStepRegistry, DateFactory)` marks current step `done` + advances; `skip(DatabaseManager, CurrentUser, WizardStepRegistry, DateFactory)` marks current step `skipped` + advances (only valid on skippable steps from the registry); `skipRest(DatabaseManager, CurrentUser, Redirector, DateFactory)` marks every non-done row `skipped` + redirects to `/`.
    - SetupWizard's `mount()` accepts `Request $request` and reads `$request->boolean('force')` via that injection (NOT the `request()` helper, per rule C-1).
    - SetupWizard renders the "Welcome back — let's pick up where you left off" banner conditionally on `currentStepKey !== 'welcome'` AND the page was just mounted.
    - WelcomeStep: static content from UI-SPEC §Wizard welcome step + 3 glyph rows; primary CTA "Continue →" dispatches `wizard.step.completed` to the parent.
    - DoneStep: static UI-SPEC §Wizard final step content; "Open dashboard →" CTA fires `WizardCompleted($userId)` event via a method-injected `Illuminate\Contracts\Events\Dispatcher` and returns a `Redirector::to('/')` response.
  </behavior>
  <action>
**Parent `Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php`** — mirror AuditLogPage + RESEARCH.md/PATTERNS.md guidance. The class has `#[Layout('onboarding::layouts.app-wizard')]`, public properties `string $currentStepKey = 'welcome'`, `array $progress = []`, `bool $isResuming = false`, `bool $allComplete = false`.

`mount()` method-DIs every collaborator: `ResumeStepResolver $resolver`, `WizardProgressQuery $query`, `WizardProgressInitializer $initializer`, `\Modules\Core\Public\Contracts\CurrentUser $currentUser`, `\Illuminate\Database\DatabaseManager $db`, `\Illuminate\Routing\Redirector $redirector`, `\Illuminate\Http\Request $request`, `\Illuminate\Support\DateFactory $dates`. The method body:
- Reads the user via `$currentUser->user()`.
- Calls `$initializer->initialize($user->id)` (idempotent safety net — if the listener missed, seed now).
- If `$request->boolean('force')`: runs an UPDATE on wizard_progress filtered by `where('user_id', $user->id)` setting `status=pending, completed_at=null, updated_at=$dates->now()`.
- Reads `$this->progress = $query->list($user->id)`.
- Calls `$resumeKey = $resolver->resolve($user->id)`.
- If empty resume sentinel, returns `$redirector->to('/')`.
- Sets `$this->isResuming = $resumeKey !== 'welcome' && ! $request->boolean('force');`
- Sets `$this->currentStepKey = $resumeKey;` and returns null.

The class also has:
- `goToStep(string $stepKey)`: validates stepKey is in the registry AND every prior step is done|skipped before assigning `$this->currentStepKey`.
- `#[\Livewire\Attributes\On('wizard.step.completed')] public function next(DatabaseManager, CurrentUser, WizardStepRegistry, DateFactory): void` — UPDATE current row to status=done + completed_at=$dates->now(), then advance via registry.
- `#[On('wizard.step.skipped')] public function skip(...)` — same but status=skipped; guard with `WizardStepRegistry::isSkippable($this->currentStepKey)` (no-op on non-skippable steps).
- `skipRest(DatabaseManager, CurrentUser, Redirector, DateFactory): \Illuminate\Http\RedirectResponse` — UPDATE every non-done row for the user to skipped + completed_at=$dates->now(), then return `$redirector->to('/')`.
- `render(\Illuminate\Contracts\View\Factory $views): View` returns `$views->make('onboarding::livewire.setup-wizard')`.

Per rule C-1: Livewire components cannot constructor-DI — every action method method-DIs its collaborators. Per rule C-2: no `.planning/` references. Per rule C-3: PHPDoc current-state only. Forbidden: `redirect()`, `now()`, `event()`, `auth()`, `request()`, `abort()`, `response()`. Allowed: Livewire's own `$this->dispatch()` (Livewire framework method, not a global helper).

**Step components — 2 of them in this plan.** Each is `final class XxxStep extends Component`. Each step's `render(ViewFactory $views): View` returns `$views->make('onboarding::livewire.steps.xxx-step')`.

`WelcomeStep.php`: static. Single action `continue(): void` calls `$this->dispatch('wizard.step.completed')`.

`DoneStep.php`: static. Single action `finish(\Illuminate\Contracts\Events\Dispatcher $events, \Modules\Core\Public\Contracts\CurrentUser $currentUser, \Illuminate\Routing\Redirector $redirector): \Illuminate\Http\RedirectResponse` calls `$events->dispatch(new WizardCompleted($currentUser->user()->id))` and returns `$redirector->to('/')`.

**Layout `Modules/Onboarding/Resources/views/layouts/app-wizard.blade.php`** — body-only, no sidebar. Wraps `@yield('content')` (or `{{ $slot }}`) inside `<div class="wiz-page">` per UI-SPEC §"Wizard shell" component inventory. Includes the standard `<head>` (csrf token, viewport, app.css link, Livewire scripts, Alpine via Livewire bundle). The `wiz-top` brand row, `wiz-dots` progress, and `wiz-footer` privacy pill live inside this layout so every step inherits the chrome.

**Blade `Modules/Onboarding/Resources/views/livewire/setup-wizard.blade.php`** — outer `<div>` (Livewire single root). Inside: progress dots, the "Welcome back" banner gated on `$isResuming`, then a `@switch($currentStepKey)` mounting the active step's Livewire component (`<livewire:onboarding.steps.welcome-step :key="welcome" />`, `<livewire:onboarding.steps.done-step :key="done" />`, plus 4 placeholder Livewire-component tags for the connector steps that Plan 03b registers). Render the connector steps' Livewire tags conditionally — when Plan 03b lands, the components exist; before that the @switch cases that match them are still emitted but render an inert placeholder via `@livewireIf(...)` style guard.

**Step blade views** — render UI-SPEC copy verbatim:
- `welcome-step.blade.php`: H1 + lede + 3 vd-glyph rows + primary CTA "Continue →" bound to `wire:click="continue"` per UI-SPEC §"Wizard welcome step". Uses the Blade components landing in Plan 03b (wiz-card, wiz-actions, vd-glyph) — emit the tags inline as raw HTML/Tailwind for this plan so the welcome step renders correctly even before Plan 03b ships the Blade components. Plan 03b replaces the inline markup with `<x-onboarding::*>` tags when it ships.
- `done-step.blade.php`: UI-SPEC §Wizard final step content + "Open dashboard →" CTA `wire:click="finish"`.

**Modify `Modules/Onboarding/Providers/OnboardingServiceProvider.php`** — `boot(LivewireManager $livewire)` adds Livewire component registrations for the 3 components landing in this plan:
- `$livewire->component('onboarding.setup-wizard', SetupWizard::class);`
- `$livewire->component('onboarding.steps.welcome-step', WelcomeStep::class);`
- `$livewire->component('onboarding.steps.done-step', DoneStep::class);`

Plan 03b appends the 4 connector-step registrations.

Per rule C-1: all DI through constructors (services) or method-DI (Livewire). Per rule C-2/C-3: no GSD leakage, no history docs. Per rule C-4: fix all severities.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/Unit --no-coverage && php artisan view:clear</automated>
  </verify>
  <done>
    SetupWizard parent + WelcomeStep + DoneStep components exist + render their UI-SPEC copy; the `app-wizard` layout exists; the 3 Livewire components are registered in the service provider; the wizard renders cleanly for a user resumed on the welcome step.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WIZ-02 + WIZ-06 acceptance tests (Resume + ReRun) — plan close-out gate</name>
  <files>Modules/Onboarding/tests/Feature/ResumeWizardTest.php, Modules/Onboarding/tests/Feature/ReRunWizardTest.php</files>
  <read_first>
    - Modules/Categorization/tests/Feature/RulesPageTest.php lines 1-90 (Pest Livewire test pattern with beforeEach + actingAs + Livewire::test → assertSee → call → assertDispatched)
    - Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php (Task 2 output — read to confirm public properties and dispatched events for assertions)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md Shared Pattern G (Livewire feature test shape)
    - tests/Pest.php (RefreshDatabase + module TestCase bootstrap)
  </read_first>
  <behavior>
    - ResumeWizardTest: a user with wizard_progress rows where `connect-card` is `in_progress` mounts SetupWizard → `currentStepKey === 'connect-card'` AND `isResuming === true` AND the rendered output contains "Welcome back" (or the actual UI-SPEC copy line).
    - ReRunWizardTest: a user with all 6 wizard_progress rows = 'done' loads `/setup` → SetupWizard's mount() returns a redirect to /. Then loads `/setup?force=1` → every row is reset to 'pending' AND `currentStepKey === 'welcome'`. The "Settings → First-run tour" link target route is `route('setup', ['force' => 1])` — verify the route accepts the query param.
    - Plan close-out gate (final step of this task): `composer test && composer analyse && composer format:check` is green.
  </behavior>
  <action>
**Test `Modules/Onboarding/tests/Feature/ResumeWizardTest.php`** — Pest feature. `beforeEach`: create user, actingAs, initialize wizard, manually set `wizard_progress` rows so `welcome`=`done`, `connect-bank`=`done`, `connect-card`=`in_progress`, rest `pending`. Render `Livewire::test(SetupWizard::class)`. Assert: `->assertSet('currentStepKey', 'connect-card')`, `->assertSet('isResuming', true)`, `->assertSee('Welcome back')` (or the exact UI-SPEC copy used by the banner — read setup-wizard.blade.php from Task 2 to find the exact string). Minimum 3 assertions.

**Test `Modules/Onboarding/tests/Feature/ReRunWizardTest.php`** — Pest feature. `beforeEach`: create user, initialize wizard, manually set every wizard_progress row to status='done'.
Case 1: GET `/setup` (no force) → expect a 302 redirect to `/`. (Test via the HTTP test client `$this->get('/setup')->assertRedirect('/')`.)
Case 2: GET `/setup?force=1` → expect a 200 with the wizard rendered AND every wizard_progress row reset to 'pending'. Test: `$this->get('/setup?force=1')->assertOk()->assertSee('Let\'s get diederik to know your money')` (the welcome H1); then assert `DB::table('wizard_progress')->where('user_id', $user->id)->where('status', '!=', 'pending')->count() === 0`.
Minimum 4 assertions across the 2 cases.

Plan close-out — final step of this task: run `composer test && composer analyse && composer format:check`. Per rule C-4 fix every BLOCKER + WARNING + INFO surfaced.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/ --no-coverage && composer test && composer analyse && composer format:check</automated>
  </verify>
  <done>
    Resume + ReRun acceptance tests pass with ≥ 7 assertions total; `vendor/bin/pest Modules/Onboarding/tests/` is fully green; Larastan + Pint clean; the wizard's backbone is end-to-end usable (sign up → land on /setup → walk through welcome → finish via /setup?force=1 reset); Plan 03b's connector steps slot in cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request ↔ /setup route | Authenticated user request; the route is auth-gated; cross-user access blocked by per-user wizard_progress reads |
| Wizard step user input ↔ wizard_progress writes | step_key + status writes are bounded to the registry's 6 keys + 4 statuses; trigger pair from Plan 01 enforces status allow-list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-03a-01 | Tampering | Cross-user wizard_progress access via Livewire property tampering | mitigate | SetupWizard's progress property is read via WizardProgressQuery which always filters by current user (Shared Pattern E); mount() rebuilds the array on every render so a client tamper is overwritten |
| T-16.1-03a-02 | Spoofing | Direct URL navigation skipping ahead (e.g. /setup with someone else's state) | mitigate | The route is auth-gated; wizard_progress rows are per-user via FK to users; the resolver returns ''-sentinel for users with no rows so a non-initialized user lands on / |
| T-16.1-03a-03 | Tampering | currentStepKey property tampering — client sets it to 'done' to skip ahead | mitigate | The goToStep() method validates the step_key is in the registry AND requires every PRIOR step to be done|skipped; tampering to a future step without prior progress fails closed |
| T-16.1-03a-04 | Information Disclosure | Wizard mount leaks user data via the rendered banner | accept | The banner copy is static ("Welcome back — let's pick up where you left off"); no PII rendered |
| T-16.1-03a-05 | Denial of Service | Repeated /setup?force=1 resets are abusive | accept | force=1 writes to the user's own 6 rows only — bounded; no amplification |
| T-16.1-03a-CSRF | Tampering | Cross-site request forgery on the Livewire POST endpoints | mitigate | Livewire's POST endpoint is web-middleware-protected and ships CSRF tokens out of the box |
</threat_model>

<verification>
- `vendor/bin/pest Modules/Onboarding/tests/` is fully green with ≥ 17 assertions across 5 test files.
- `composer analyse` + `composer format:check` are green.
- Manual smoke: open a clean SQLite db, sign up a user, observe redirect to /setup, observe welcome step renders; kill PHP, mark `connect-card` row in_progress, restart, navigate to /setup, observe "Welcome back" banner; navigate to /setup?force=1, observe wizard reset.
- `EnsureDatabaseReady::EXEMPT_ROUTE_PREFIXES` contains `'setup'`; `grep -n "'setup'" Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php` shows the entry.
- Every UI-SPEC §"Wizard chrome" / §"Wizard welcome step" / §"Wizard final step" copy string is present in the rendered blades.
</verification>

<success_criteria>
- WIZ-01 + WIZ-02 + WIZ-06 all green per their tests.
- /setup route + signup redirect + EnsureDatabaseReady exemption all wired.
- SetupWizard parent + WelcomeStep + DoneStep + app-wizard layout exist and render UI-SPEC copy.
- `wizard_progress` rows are 100% per-user-scoped (BelongsToUser global scope active on the model + explicit where clauses in services).
- Calm-slate UI-SPEC contract respected — no new theme tokens introduced.
- Larastan L10 strict + Pint clean.
- Zero `.planning/` / D-NN / PLAN.md leakage in production source.
- Zero global helpers (`redirect()`, `now()`, `event()`, `auth()`, `request()`, `abort()`, `response()`) introduced; every collaborator is constructor-DI'd (services) or method-DI'd (Livewire).
</success_criteria>

<output>
Create `.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-03a-SUMMARY.md` when done.
</output>
</content>
</invoke>