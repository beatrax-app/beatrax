---
phase: 16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena
plan: 05
type: execute
wave: 5
depends_on: [16.1-01, 16.1-02, 16.1-03b, 16.1-04]
files_modified:
  - Modules/Community/Routes/web.php
  - Modules/Community/Internal/Corpus/CorpusLoader.php
  - Modules/Community/Internal/Listeners/SeedCommunityCorpus.php
  - Modules/Community/Internal/Services/GitHubCompareUrlBuilder.php
  - Modules/Community/Internal/Shell/NoOpShell.php
  - Modules/Community/Public/Actions/OpenExternalUrlAction.php
  - Modules/Community/Public/Services/CommunityCorpusQuery.php
  - Modules/Community/Public/Dto/SuggestMappingDto.php
  - Modules/Community/Public/Dto/CorpusEntryDto.php
  - Modules/Community/Public/Events/MysteryMerchantSubmitted.php
  - Modules/Community/Internal/Http/Livewire/MysteryMerchantsPage.php
  - Modules/Community/Internal/Http/Livewire/SuggestMappingModal.php
  - Modules/Community/Internal/Http/Livewire/SharedListSettingsPanel.php
  - Modules/Community/Internal/Http/Livewire/HelpOthersTriageButton.php
  - Modules/Community/Resources/views/livewire/mystery-merchants-page.blade.php
  - Modules/Community/Resources/views/livewire/suggest-mapping-modal.blade.php
  - Modules/Community/Resources/views/livewire/shared-list-settings-panel.blade.php
  - Modules/Community/Resources/views/livewire/help-others-triage-button.blade.php
  - Modules/Community/Resources/views/components/mystery-card.blade.php
  - Modules/Community/Providers/CommunityServiceProvider.php
  - Modules/Import/Public/Services/MerchantNameResolver.php
  - Modules/Categorization/Internal/Http/Livewire/TriageInbox.php
  - Modules/Categorization/Resources/views/livewire/triage-inbox.blade.php
  - resources/corpus/merchant-mappings.yaml
  - resources/corpus/built-in-heuristics.yaml
  - config/community.php
  - resources/css/app.css
  - tests/Contracts/BoundaryArchTest.php
  - Modules/Community/tests/Feature/SeedCommunityCorpusTest.php
  - Modules/Community/tests/Feature/CorpusUnknownCategoryWarningTest.php
  - Modules/Community/tests/Unit/OpenExternalUrlActionTest.php
  - Modules/Community/tests/Feature/SuggestMappingModalSubmitTest.php
  - Modules/Community/tests/Feature/MysteryMerchantsPageTest.php
  - Modules/Community/tests/Feature/SharedListSettingsPanelTest.php
  - Modules/Community/tests/Feature/TriageHelpOthersCtaTest.php
  - Modules/Import/tests/Unit/MerchantNameResolverCommunityTest.php
autonomous: true
requirements: [CORPUS-02, CORPUS-03, CORPUS-04, CORPUS-05]
must_haves:
  truths:
    - "Bundled resources/corpus/merchant-mappings.yaml + resources/corpus/built-in-heuristics.yaml exist with the documented schema; the loader parses both via symfony/yaml"
    - "SeedCommunityCorpus listener fires on UserInstalled, upserts each entry into community_merchant_mappings keyed by (user_id=null, pattern), tolerates malformed individual entries (logs + skips)"
    - "MerchantNameResolver's community fallback (steps c+d) now returns community names when no user alias matches; precedence is user-exact → user-generalized → community-exact → community-generalized → null"
    - "OpenExternalUrlAction wraps Native\\Desktop\\Contracts\\Shell::openExternal via constructor DI, validates URL is https:// before invocation, falls back to a NoOpShell binding when not under NativePHP runtime"
    - "SuggestMappingModal opens with a prefilled pattern; submit builds a GitHub Compare URL via GitHubCompareUrlBuilder + opens it via OpenExternalUrlAction; subsequent toast confirms"
    - "MysteryMerchantsPage renders at /community/mystery-merchants with stats strip + card grid; cards open the SuggestMappingModal; payment-type chips on cards reflect the PaymentType enum from Plan 02"
    - "Triage row CTA Help others identify this is rendered only when Settings Toggle 2 (Offer to contribute) is enabled (default on)"
    - "Settings panel exposes 3 toggles per UI-SPEC; Toggle 3 (Update on app updates) ships disabled-no-op with an inline note that is version-agnostic (no invented release number)"
    - "Toggle 1/2/3 state persists in users.community_settings JSON column (added by Plan 01); HelpOthersTriageButton + SharedListSettingsPanel both read the same column"
  artifacts:
    - path: "resources/corpus/merchant-mappings.yaml"
      provides: "Bundled corpus seed file (30-50 well-known Dutch merchants)"
      min_lines: 30
    - path: "resources/corpus/built-in-heuristics.yaml"
      provides: "12 dominant Dutch consumer brand heuristics tagged contributor=built-in"
      min_lines: 12
    - path: "Modules/Community/Internal/Listeners/SeedCommunityCorpus.php"
      provides: "Idempotent UserInstalled listener seeding the corpus"
      contains: "updateOrInsert"
    - path: "Modules/Community/Public/Actions/OpenExternalUrlAction.php"
      provides: "DI-wrapped system-browser launch; https-only allow-list"
      contains: "openExternal"
    - path: "config/community.php"
      provides: "github_compare_base config key resolved from BEATRAX_GITHUB_COMPARE_BASE env"
      contains: "github_compare_base"
  key_links:
    - from: "Modules/Community/Providers/CommunityServiceProvider.php"
      to: "Modules/Community/Internal/Listeners/SeedCommunityCorpus"
      via: "events->listen(UserInstalled::class, SeedCommunityCorpus::class)"
      pattern: "SeedCommunityCorpus"
    - from: "Modules/Community/Internal/Http/Livewire/SuggestMappingModal.php"
      to: "Modules/Community/Public/Actions/OpenExternalUrlAction"
      via: "method-DI on save() action; URL built by GitHubCompareUrlBuilder then handed to OpenExternalUrlAction::__invoke"
      pattern: "OpenExternalUrlAction"
    - from: "Modules/Import/Public/Services/MerchantNameResolver.php"
      to: "community_merchant_mappings table"
      via: "explicit per-table query in steps c+d of the precedence walk (mirrors Shared Pattern E)"
      pattern: "community_merchant_mappings"
    - from: "Modules/Community/Internal/Http/Livewire/SharedListSettingsPanel.php"
      to: "users.community_settings"
      via: "Eloquent array cast on User model (Plan 01 added); read + write through method-injected CurrentUser + DatabaseManager"
      pattern: "community_settings"
---

<objective>
Bring the crowd-sourced merchant identification corpus online: bundle a YAML file at build time, seed it into `community_merchant_mappings` on `UserInstalled`, extend `MerchantNameResolver` so the community-mapping tail fills in the corpus-name when a user has no local alias, ship the `/community/mystery-merchants` browse destination, the shared `SuggestMappingModal` (Triage row + browse page + Settings → Shared list all open the same modal), the `OpenExternalUrlAction` that hands the GitHub Compare URL to the system browser via `Native\Desktop\Contracts\Shell::openExternal`, and the Settings → Shared merchant list panel with 3 toggles (per UI-SPEC §"Settings → Shared merchant list").

Depends on Plan 02 because Mystery-card payment-type chips consume the PaymentType enum + classifier output from Plan 02. Depends on Plan 03b because the Triage row CTA is wired alongside the existing Triage UX which the wizard's FirstImportStep flow eventually drives users into.

Purpose: D-12 through D-17. The corpus is the single biggest UX leverage in Phase 16.1 — when one user identifies `BCK*SHELL PIETER NIEUW`, every diederik user benefits the next time they import a Shell PIN transaction. The local-only privacy posture is preserved: no live network fetch, just a bundled YAML refreshed at app-update time (a future release).

Output: Two YAML asset files; CorpusLoader + SeedCommunityCorpus listener; OpenExternalUrlAction with HTTPS allow-list + NoOpShell fallback; GitHubCompareUrlBuilder building the Compare URL from a SuggestMappingDto; MerchantNameResolver extended with the community fallback; MysteryMerchantsPage + SuggestMappingModal + SharedListSettingsPanel + HelpOthersTriageButton Livewire components + their blades; Triage row extended with the dashed CTA gated on Settings Toggle 2; one config file (`config/community.php`) carrying `github_compare_base`; new arch invariant restricting `Native\Desktop\Contracts\Shell` imports; CORPUS-02/03/04/05 tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-CONTEXT.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md

@CLAUDE.md
@.claude/skills/sketch-findings-diederik/SKILL.md
@.claude/skills/sketch-findings-diederik/references/community-merchant-identification.md

@Modules/Community/Models/CommunityMerchantMapping.php
@Modules/Community/Providers/CommunityServiceProvider.php
@Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php

@Modules/Import/Public/Services/MerchantNameResolver.php
@Modules/Import/Public/Services/PatternGeneralizer.php
@Modules/Import/Models/MerchantAlias.php
@Modules/Import/Public/Enums/PaymentType.php

@Modules/Categorization/Internal/Http/Livewire/TriageInbox.php
@Modules/Categorization/Resources/views/livewire/triage-inbox.blade.php
@Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php
@Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php
@Modules/Core/Internal/Http/Livewire/SettingsPage.php
@Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php

@vendor/nativephp/desktop/src/Contracts/Shell.php
@vendor/nativephp/desktop/src/Fakes/ShellFake.php

@Modules/Core/Models/User.php
@Modules/Core/Public/Contracts/CurrentUser.php

<interfaces>
From vendor/nativephp/desktop/src/Contracts/Shell.php (verified at line 5):
```
namespace Native\Desktop\Contracts;
interface Shell { public function openExternal(string $url): void; }
```

From vendor/nativephp/desktop/src/Fakes/ShellFake.php (test-fake exists).

From Modules/Import/Public/Services/MerchantNameResolver.php (Plan 04 artifact — steps a+b implemented, steps c+d return null; this plan fills in c+d via constructor extension).

From Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php (24-line listener — exact mirror target for SeedCommunityCorpus).

From Modules/Core/Models/User.php (Plan 01 extended — `community_settings` is an array-cast nullable column; this plan reads + writes that column directly via Eloquent, NOT via a separate UserSettings model).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: YAML asset files + CorpusLoader + SeedCommunityCorpus listener + config + MerchantNameResolver community-tail + corpus query service + unknown-category warning test</name>
  <files>resources/corpus/merchant-mappings.yaml, resources/corpus/built-in-heuristics.yaml, config/community.php, Modules/Community/Internal/Corpus/CorpusLoader.php, Modules/Community/Public/Dto/CorpusEntryDto.php, Modules/Community/Public/Services/CommunityCorpusQuery.php, Modules/Community/Internal/Listeners/SeedCommunityCorpus.php, Modules/Community/Providers/CommunityServiceProvider.php, Modules/Import/Public/Services/MerchantNameResolver.php, Modules/Community/tests/Feature/SeedCommunityCorpusTest.php, Modules/Community/tests/Feature/CorpusUnknownCategoryWarningTest.php, Modules/Import/tests/Unit/MerchantNameResolverCommunityTest.php</files>
  <read_first>
    - Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php FULL (mirror target for the listener)
    - Modules/Categorization/Database/Seeders/DefaultCategoryTreeSeeder.php (idempotent seed pattern using updateOrCreate; mirror for the corpus loader's upsert)
    - Modules/Categorization/Database/Seeders/DefaultCategoryTreeSeeder.php (find the category names the DefaultCategoryTree seeds — corpus category strings must match these names verbatim, OR the loader logs an unknown-category warning per W-9)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md lines 441-489 (Pattern 2 idempotent listener verbatim) plus lines 522-570 plus Open Question #3 plus Claude's Discretion built-in heuristics list
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 297-301 (SeedCommunityCorpus action narrative) plus Shared Pattern C
    - Modules/Import/Public/Services/MerchantNameResolver.php (Plan 04 — read to find the exact spot of the community-extension seam this task fills)
    - Modules/Community/Models/CommunityMerchantMapping.php (Plan 01 artifact)
    - Modules/Import/Public/Services/PatternGeneralizer.php (Plan 04 — corpus loader uses the same generalizer)
    - composer.json (confirm symfony/yaml is transitively available)
    - bootstrap/providers.php if present
    - Modules/Desktop/Internal/Native/FirstLaunchBootstrap.php (verify resources/corpus/** is shipped in NativePHP bundles)
    - Modules/Core/Public/Events/UserInstalled.php
  </read_first>
  <behavior>
    - `resources/corpus/merchant-mappings.yaml` is a bundled YAML file with ≥30 entries (well-known Dutch merchants). Schema: `entries: list<{pattern, name, category?, region?, contributor}>`. Contributor `"diederik-bot"`.
    - `resources/corpus/built-in-heuristics.yaml` is a sibling file with exactly the 12 Dutch consumer-brand heuristics: Albert Heijn, Jumbo, Lidl, Spotify, Netflix, Vattenfall, Ziggo, KPN, NS, Bol.com, Coolblue, Eneco. Contributor `"built-in"`.
    - `config/community.php` returns `['github_compare_base' => env('BEATRAX_GITHUB_COMPARE_BASE', 'https://github.com/nightworksio/beatrax/compare/main'), 'corpus' => ['bundled_path' => 'resources/corpus/merchant-mappings.yaml', 'heuristics_path' => 'resources/corpus/built-in-heuristics.yaml']]`.
    - `CorpusLoader::loadBundled(): list<CorpusEntryDto>` reads both YAML files via `\Symfony\Component\Yaml\Yaml::parseFile($path, \Symfony\Component\Yaml\Yaml::PARSE_EXCEPTION_ON_INVALID_TYPE)`, validates each entry has at least `pattern` + `name` + `contributor`, generalizes any missing `generalized_pattern` via PatternGeneralizer. If an entry's `category` string does NOT match a row in the `categories` table at seed time, the loader writes the string verbatim AND emits a `warning` log via the injected logger. The import preview's auto-suggest chip is responsible for graceful degradation when the category cannot be resolved.
    - `CorpusEntryDto` (spatie/laravel-data): readonly `pattern`, `generalizedPattern`, `name`, `category`, `region`, `contributor`.
    - `SeedCommunityCorpus` listens to `UserInstalled`. Constructor DI: `CorpusLoader, DatabaseManager, LoggerInterface, DateFactory`. `handle(UserInstalled $event)` loads the corpus, then per entry: `try { $db->connection()->table('community_merchant_mappings')->updateOrInsert(['pattern' => $entry->pattern, 'user_id' => null], [..., 'updated_at' => $dates->now()]); } catch (Throwable $e) { $logger->warning('Skipped malformed corpus entry.', [...]); }`.
    - `CommunityCorpusQuery::lookupExact(string $rawDescription): ?string` returns the name from `community_merchant_mappings` where `pattern = $raw AND user_id IS NULL`. `lookupGeneralized(string $rawDescription): ?string` iterates community generalized patterns and PHP-mb_strpos matches. `mappingsCount(): int` returns the corpus size. `contributorsCount(): int` returns DISTINCT contributor count.
    - `MerchantNameResolver` is EXTENDED — steps (c) and (d) of the precedence walk now call CommunityCorpusQuery::lookupExact / lookupGeneralized instead of returning null. Constructor gains `private readonly CommunityCorpusQuery $corpus`.
    - `CorpusUnknownCategoryWarningTest` asserts that loading a fixture YAML containing an entry whose category does not match any seeded category emits a `warning` log line via a fake LoggerInterface.
    - `SeedCommunityCorpusTest` verifies idempotency + partial-failure tolerance + upsert update path.
    - `MerchantNameResolverCommunityTest` verifies the 4-step walk including the 2 community branches.
  </behavior>
  <action>
**Asset `resources/corpus/merchant-mappings.yaml`** — top-level key `entries:`; list of mapping rows. Each row carries `pattern`, `name`, optional `category` + `region`, `contributor: diederik-bot`. Ship ≥30 entries covering common Dutch banks' codes for major merchants.

**Asset `resources/corpus/built-in-heuristics.yaml`** — exactly 12 entries for the dominant Dutch consumer brands listed above. Schema identical; contributor `"built-in"`.

**Config `config/community.php`** — returns the config array described in <behavior>. Pull `BEATRAX_GITHUB_COMPARE_BASE` from env via `env(...)`. Per project rule C-2, no `.planning/` references.

**DTO `Modules/Community/Public/Dto/CorpusEntryDto.php`** — spatie/laravel-data subclass.

**Service `Modules/Community/Internal/Corpus/CorpusLoader.php`** — Constructor DI: `PatternGeneralizer $generalizer, LoggerInterface $logger, \Illuminate\Contracts\Config\Repository $config, \Illuminate\Database\DatabaseManager $db`. Method `loadBundled(): array`:

- Read the configured YAML path via `$path = base_path($this->config->get('community.corpus.bundled_path'))`. NOTE on `base_path`: if `tests/Contracts/BoundaryArchTest.php` carves out `base_path` for module config-bound asset reads, this is fine; otherwise inject a path resolver. Per the Phase 13 PKG-01 ban, `base_path` for READ-only project-rooted assets is generally acceptable — confirm by reading the arch test.
- Guard the file's existence; log a warning + return [] if missing.
- Parse via `\Symfony\Component\Yaml\Yaml::parseFile($path, \Symfony\Component\Yaml\Yaml::PARSE_EXCEPTION_ON_INVALID_TYPE)`.
- For each entry: validate required fields; if missing → log warning + skip.
- Resolve the category string. Load the `categories` table once into a Set of valid category names (`$validCategories = $this->db->connection()->table('categories')->pluck('name')->all()`). If the entry's `category` is non-null AND NOT in `$validCategories`, log `$this->logger->warning('Corpus entry references unknown category.', ['pattern' => $entry->pattern, 'category' => $entry->category])`. Still include the entry in the returned list with the unresolved category string preserved verbatim — graceful degradation is the consumer's responsibility.
- Generalize missing `generalized_pattern` via `$this->generalizer->generalize($pattern)`.
- Return the list.

**Service `Modules/Community/Public/Services/CommunityCorpusQuery.php`** — Constructor DI: `DatabaseManager $db`. Methods as documented. Per Shared Pattern E mirror RuleEvaluator's PHP-side match (mb_strpos in PHP, never SQL LIKE).

**Listener `Modules/Community/Internal/Listeners/SeedCommunityCorpus.php`** — mirror SeedDefaultCategoryTree. Constructor DI: `CorpusLoader $loader, DatabaseManager $db, LoggerInterface $logger, \Illuminate\Support\DateFactory $dates`. `handle(UserInstalled $event): void`:
- `$entries = $this->loader->loadBundled()`
- `$now = $this->dates->now()` (NOT the `now()` helper, per rule C-1)
- foreach entry: try updateOrInsert via `$this->db->connection()->table(...)` keyed on (pattern, user_id=null) with the entry's fields + `updated_at => $now`; catch Throwable → log warning.

**Modify `Modules/Community/Providers/CommunityServiceProvider.php`** — `register()` adds singletons: CorpusLoader, CommunityCorpusQuery, OpenExternalUrlAction (Task 2), GitHubCompareUrlBuilder (Task 2). Bind `\Native\Desktop\Contracts\Shell` to a `NoOpShell` implementation IF the binding doesn't already exist. Use `if (! $this->app->bound(\Native\Desktop\Contracts\Shell::class)) { $this->app->singleton(\Native\Desktop\Contracts\Shell::class, NoOpShell::class); }`. NoOpShell lives at `Modules/Community/Internal/Shell/NoOpShell.php` (created in Task 2 alongside the action). `boot(Dispatcher $events)` adds `$events->listen(\Modules\Core\Public\Events\UserInstalled::class, SeedCommunityCorpus::class);`.

**Modify `Modules/Import/Public/Services/MerchantNameResolver.php`** — extend the constructor to add `private readonly \Modules\Community\Public\Services\CommunityCorpusQuery $corpus`. Replace the previously-null community seam from Plan 04 with:
- Step (c): `$communityExact = $this->corpus->lookupExact($rawDescription); if ($communityExact !== null) return $communityExact;`
- Step (d): `$communityGen = $this->corpus->lookupGeneralized($rawDescription); if ($communityGen !== null) return $communityGen;`
- Step (e): return null.

This crosses a module boundary (Import depending on Community Public service) — symmetric to other inter-module Public dependencies.

**Test `Modules/Community/tests/Feature/SeedCommunityCorpusTest.php`** — Pest feature, RefreshDatabase. `beforeEach`: write a small fixture YAML to a temp path (3 valid entries + 1 corrupt entry), bind `community.corpus.bundled_path` to the fixture path via `config(['community.corpus.bundled_path' => $tempPath])`. Per rule C-1, prefer injecting `\Illuminate\Contracts\Config\Repository` and calling `$config->set(...)` instead of the `config()` helper if Larastan complains. Test cases:
1. Create a user + dispatch `UserInstalled` via a method-injected `\Illuminate\Contracts\Events\Dispatcher` (`$events->dispatch(new UserInstalled($user->id))`) — NOT the `event()` global helper per rule C-1 → assert `community_merchant_mappings` has exactly 3 rows.
2. Re-dispatch UserInstalled for the SAME user → assert still 3 rows (idempotent upsert).
3. Modify the fixture YAML to change one entry's `name` → re-dispatch → assert the existing row's `name` is updated to the new value.
4. Set the heuristics path to a non-existent file → re-dispatch → corpus still has 3 rows from merchant-mappings.yaml, no exception thrown.
Minimum 5 assertions.

**Test `Modules/Community/tests/Feature/CorpusUnknownCategoryWarningTest.php`** — Pest feature, RefreshDatabase. `beforeEach`: seed a couple of valid categories via the existing factory; write a fixture YAML with one entry referencing a category name NOT in the categories table; bind the config path; bind a fake LoggerInterface (use `Log::spy()` if the project carves out `Log` facade for tests, otherwise bind a custom test-double `LoggerInterface` impl into the container). Cases:
1. Load via `app(CorpusLoader::class)->loadBundled()` → assert the returned array still contains the entry (graceful degradation) AND assert the fake logger received a `warning` level call with the substring "Corpus entry references unknown category" AND a context array including the offending pattern + category.
2. Same fixture with the same entry but the category set to a known category name → no warning logged.
Minimum 3 assertions.

**Test `Modules/Import/tests/Unit/MerchantNameResolverCommunityTest.php`** — Pest unit, RefreshDatabase. `beforeEach`: create user. Branches:
1. No aliases, no community rows → resolve returns null.
2. Insert community row `(user_id=null, pattern='SHELL*PIETER*', name='Shell — Pieter Nieuwlandstraat')` → `resolve('SHELL PIETER X', $user->id)` → returns 'Shell — Pieter Nieuwlandstraat' (community-exact branch).
3. Insert community generalized row `(generalized_pattern='shell pieter')` → `resolve('SHELL PIETER X', $user->id)` → returns the community name (community-generalized branch).
4. Insert BOTH a user-exact alias AND a community row matching the same raw → user-exact wins (precedence D-11 + D-15).
Minimum 4 assertions.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Community/tests/Feature/SeedCommunityCorpusTest.php Modules/Community/tests/Feature/CorpusUnknownCategoryWarningTest.php Modules/Import/tests/Unit/MerchantNameResolverCommunityTest.php --no-coverage</automated>
  </verify>
  <done>
    Two YAML asset files exist with the documented schema; CorpusLoader parses them via symfony/yaml with PARSE_EXCEPTION_ON_INVALID_TYPE; unknown-category entries log a warning and still load (graceful degradation); SeedCommunityCorpus listener is wired to UserInstalled and is idempotent; MerchantNameResolver's community fallback is implemented; the 3 tests pass with ≥12 assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OpenExternalUrlAction + NoOpShell + GitHubCompareUrlBuilder + SuggestMappingModal + suggest-modal blade + arch invariant + unit + feature tests</name>
  <files>Modules/Community/Public/Actions/OpenExternalUrlAction.php, Modules/Community/Internal/Shell/NoOpShell.php, Modules/Community/Internal/Services/GitHubCompareUrlBuilder.php, Modules/Community/Public/Dto/SuggestMappingDto.php, Modules/Community/Internal/Http/Livewire/SuggestMappingModal.php, Modules/Community/Public/Events/MysteryMerchantSubmitted.php, Modules/Community/Resources/views/livewire/suggest-mapping-modal.blade.php, Modules/Community/Providers/CommunityServiceProvider.php, tests/Contracts/BoundaryArchTest.php, Modules/Community/tests/Unit/OpenExternalUrlActionTest.php, Modules/Community/tests/Feature/SuggestMappingModalSubmitTest.php</files>
  <read_first>
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md lines 834-872 (OpenExternalUrlAction verbatim implementation) + Pitfall 7 lines 676-684 (NativePHP Shell facade vs DI contract)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md lines 378-402 (Suggest-mapping modal copy verbatim + post-submit states)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 559-567 (SuggestMappingModal analog) + Shared Pattern F (Flux modal SFC)
    - vendor/nativephp/desktop/src/Contracts/Shell.php (verified — `openExternal(string): void`)
    - vendor/nativephp/desktop/src/Fakes/ShellFake.php (test-fake binding shape)
    - Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php FULL
    - Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php (Flux modal blade)
    - tests/Contracts/BoundaryArchTest.php (existing arch invariants — this task adds a new invariant)
    - Modules/Categorization/tests/Feature/RuleFormModalTest.php (Flux modal feature test shape)
    - W-7 PRE-SURVEY: run `grep -rn 'Native\\\\Desktop\\\\Contracts\\\\Shell' Modules/ vendor/ 2>/dev/null` and record existing consumers BEFORE writing the arch invariant. If pre-existing legitimate consumers exist outside OpenExternalUrlAction + NoOpShell, include them in the allow-list explicitly.
  </read_first>
  <behavior>
    - `OpenExternalUrlAction::__invoke(string $url): void` validates the URL: `filter_var($url, FILTER_VALIDATE_URL) !== false` AND `str_starts_with($url, 'https://')` AND the host matches an allow-list (`github.com` for this phase). Throws `\InvalidArgumentException` otherwise. Then calls `$this->shell->openExternal($url)` and logs `info` with the URL.
    - `NoOpShell` is a tiny `final class NoOpShell implements \Native\Desktop\Contracts\Shell` with constructor-DI'd LoggerInterface; `openExternal(string $url): void` logs the URL at `info` level and does nothing else.
    - `GitHubCompareUrlBuilder::build(SuggestMappingDto $dto): string` returns `{base}/compare/main...{branch}?expand=1&body={url-encoded-yaml-entry}` with deterministic branch slug (`suggest-{hash(pattern)}`) and URL-encoded YAML body.
    - `SuggestMappingDto` (spatie/laravel-data): readonly `pattern`, `name`, `?category`, `region = 'NL'`.
    - `SuggestMappingModal`: prefilled pattern via `#[On('suggest-mapping:open')]`; `submit()` method-DIs OpenExternalUrlAction, GitHubCompareUrlBuilder, `\Illuminate\Contracts\Events\Dispatcher`, CurrentUser; validates; builds URL; calls action; dispatches `MysteryMerchantSubmitted` event via the injected Dispatcher (NOT the `event()` global helper); dispatches toast + modal-hide; on OpenExternalUrlAction throw, sets inline error and keeps modal open.
    - YAML-preview pane inside the modal: per I-5 fix, move the YAML template to a `data-template` attribute on the `<pre>` element and reference it with `$el.dataset.template` (or a thin Alpine x-data object holding a single string) — cleaner escape model than inline backtick interpolation. Reactive updates via `wire:model.live` properties + `x-effect` keeping the rendered `<pre>` in sync with the four properties. This stays in the Browser tier per UI-SPEC.
    - A new arch invariant: `Native\Desktop\Contracts\Shell` may ONLY be imported by `Modules\Community\Public\Actions\OpenExternalUrlAction` AND `Modules\Community\Internal\Shell\NoOpShell` (plus any legitimate pre-existing consumers surfaced by the W-7 grep survey).
  </behavior>
  <action>
**Action `Modules/Community/Public/Actions/OpenExternalUrlAction.php`** — `final class` with constructor DI of `\Native\Desktop\Contracts\Shell $shell` + `LoggerInterface $logger`. `__invoke(string $url): void`:
- Validate `filter_var($url, FILTER_VALIDATE_URL) !== false` AND `str_starts_with($url, 'https://')` — throw `\InvalidArgumentException` otherwise with a descriptive message.
- Parse host via `parse_url($url, PHP_URL_HOST)` and require it to be in `self::ALLOWED_HOSTS = ['github.com']`. Throw `\InvalidArgumentException('OpenExternalUrlAction: host not allow-listed, got: '.$host)` if not.
- Call `$this->shell->openExternal($url)`.
- `$this->logger->info('OpenExternalUrlAction: launched system browser.', ['url' => $url])`.

**Class `Modules/Community/Internal/Shell/NoOpShell.php`** — tiny implementation. `final class NoOpShell implements \Native\Desktop\Contracts\Shell`. Constructor DI: `LoggerInterface $logger`. `openExternal(string $url): void` → `$this->logger->info('NoOpShell: would launch URL', ['url' => $url]);`. PHPDoc describes the dev-mode fallback.

**Service `Modules/Community/Internal/Services/GitHubCompareUrlBuilder.php`** — Constructor DI: `\Illuminate\Contracts\Config\Repository $config`. Method `build(SuggestMappingDto $dto): string` computes base + branch slug + URL-encoded YAML body. Private `quoteYaml(string): string` wraps in double-quotes + escapes embedded quotes. PHPDoc describes the deterministic-branch-slug + URL-encode behavior.

**DTO `Modules/Community/Public/Dto/SuggestMappingDto.php`** — spatie/laravel-data subclass with readonly fields.

**Event `Modules/Community/Public/Events/MysteryMerchantSubmitted.php`** — `final class MysteryMerchantSubmitted { public function __construct(public readonly int $userId, public readonly string $pattern) {} }`.

**Livewire `Modules/Community/Internal/Http/Livewire/SuggestMappingModal.php`** — public properties + methods per <behavior>. Errors caught from OpenExternalUrlAction set `$this->submitError = $e->getMessage()` and render inline. The `submit()` method method-DIs `\Illuminate\Contracts\Events\Dispatcher $events` and uses `$events->dispatch(new MysteryMerchantSubmitted(...))` — NEVER the `event()` global helper. PHPDoc current-state per rule C-3.

**Blade `Modules/Community/Resources/views/livewire/suggest-mapping-modal.blade.php`** — render UI-SPEC §"Suggest-mapping modal" verbatim. Outer `<div>` with `<flux:modal name="suggest-mapping" dismissible>` inside. Title, subtitle, prefilled mono pattern field (read-only), editable name input, optional category dropdown, region dropdown, YAML preview pane.

For the YAML preview pane, per I-5:
- Store the YAML template format in a `data-template` attribute on the `<pre>` element OR equivalent simple Alpine binding that does not require multi-level backslash/quote escaping.
- Use a single Alpine `x-data` object holding the four reactive properties (`pattern`, `name`, `category`, `region`) bound via `$wire.entangle('...')`.
- Use `x-effect` or a single `x-text` that calls a small function (defined in the same x-data object) which returns the YAML string assembled from the four properties. Keeps escape complexity inside one JS function body, not a multi-quote inline expression.
- Stays in the Browser tier — no server roundtrip per keystroke.

Footer: ghost Cancel + primary "Submit as draft PR". If `$submitError`, render it inline above the footer.

**Modify `Modules/Community/Providers/CommunityServiceProvider.php`** — `boot(LivewireManager $livewire)` registers `community.suggest-mapping-modal`; `register()` already added OpenExternalUrlAction + GitHubCompareUrlBuilder singletons in Task 1's edit (verify; if not, add them here).

**Modify `tests/Contracts/BoundaryArchTest.php`** — add a new arch invariant: `arch('Native\\Desktop\\Contracts\\Shell is only used inside Modules\\Community\\Public\\Actions\\OpenExternalUrlAction or its NoOpShell fallback')`. Allow-list MUST be the actual surveyed list from <read_first> W-7 grep — if any pre-existing legitimate consumers exist (the survey may surface zero), include them. Implementation: `expect('Native\\Desktop\\Contracts\\Shell')->toOnlyBeUsedIn([...allow-list...])`. If the arch DSL form requires a single string, write a custom grep-based test (grep over Modules/ for `Native\\Desktop\\Contracts\\Shell` and assert the only files mentioning it are the allow-listed paths). Per RESEARCH.md Wave 0 Gaps.

**Test `Modules/Community/tests/Unit/OpenExternalUrlActionTest.php`** — Pest unit. Bind a test fake into the container before each test (use ShellFake or a custom test-double impl). Cases:
1. Valid `https://github.com/...` URL → `$action($url)` succeeds; the fake recorded the URL.
2. `http://github.com/...` URL → throws `InvalidArgumentException`.
3. `https://evil.com/...` URL → throws `InvalidArgumentException` (host not in allow-list).
4. `javascript:alert('XSS')` URL → throws.
Minimum 4 assertions.

**Test `Modules/Community/tests/Feature/SuggestMappingModalSubmitTest.php`** — Pest feature, RefreshDatabase. `beforeEach`: bind a ShellFake into the container; bind a Dispatcher spy. Cases:
1. `Livewire::test(SuggestMappingModal::class)->dispatch('suggest-mapping:open', rawDescription: 'SHELL*PIETER*')->assertSet('pattern', 'SHELL*PIETER*')->set('name', 'Shell Pieter')->call('submit')->assertDispatched('toast.show')->assertDispatched('modal-hide')`. Assert ShellFake recorded a URL starting with `https://github.com/`; assert the Dispatcher spy received a `MysteryMerchantSubmitted` event.
2. Submit with empty name → validation error; ShellFake recorded zero calls.
3. Override the config `community.github_compare_base` to `'https://evil.com/compare/main'` → submit fails with `submitError` containing 'host not allow-listed'.
Minimum 6 assertions.

Per rule C-1: zero global helpers (`event()`, `now()`, `redirect()`, `request()`, `auth()`, `abort()`, `response()`) in any new code. Replace each with method-injected `Illuminate\Contracts\Events\Dispatcher`, `Illuminate\Support\DateFactory`, `Illuminate\Routing\Redirector`, the project's `CurrentUser` contract, and concrete exceptions / `Illuminate\Contracts\Routing\ResponseFactory`.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Community/tests/Unit/OpenExternalUrlActionTest.php Modules/Community/tests/Feature/SuggestMappingModalSubmitTest.php tests/Contracts/ --no-coverage</automated>
  </verify>
  <done>
    OpenExternalUrlAction validates https + github.com allow-list + delegates to Shell contract; NoOpShell + binding work in Herd dev mode; GitHubCompareUrlBuilder produces deterministic-slug URLs with url-encoded YAML body; SuggestMappingModal opens with prefilled pattern + submit launches the system browser via the action + dispatches event via the injected Dispatcher + hides modal; new arch invariant restricts Shell-contract import using a surveyed allow-list; all 3 new tests pass with ≥13 assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3a: MysteryMerchantsPage + suggest-modal mount + mystery-card component + MysteryMerchantsPageTest</name>
  <files>Modules/Community/Routes/web.php, Modules/Community/Internal/Http/Livewire/MysteryMerchantsPage.php, Modules/Community/Resources/views/livewire/mystery-merchants-page.blade.php, Modules/Community/Resources/views/components/mystery-card.blade.php, Modules/Community/Providers/CommunityServiceProvider.php, resources/css/app.css, Modules/Community/tests/Feature/MysteryMerchantsPageTest.php</files>
  <read_first>
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md lines 331-354 (mystery-merchants destination + stats strip + cards)
    - .claude/skills/sketch-findings-diederik/references/community-merchant-identification.md (sketches 005A/B)
    - Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php (paginated cards + #[Layout] + method-DI pattern)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 569-577 (MysteryMerchantsPage analog)
    - Modules/Import/Public/Enums/PaymentType.php (Plan 01) — payment-type chip glyph + class
  </read_first>
  <behavior>
    - `/community/mystery-merchants` route exists, auth-gated, rendered by `MysteryMerchantsPage` Livewire component.
    - MysteryMerchantsPage queries the user's transactions for descriptions that DO NOT resolve via MerchantNameResolver; displays them as paginated cards. Stats strip shows 4 KPIs: mystery codes in user data, mappings in shared list (from CommunityCorpusQuery::mappingsCount), of-imports-auto-named %, user's-contributions live count.
    - Each card has primary "Suggest a name →" CTA that dispatches `suggest-mapping:open` with the row's raw description.
    - Each card carries a payment-type chip via the existing `.ptype-chip.{class}` styling driven by the PaymentType enum from Plan 02.
    - The page mounts SuggestMappingModal once.
  </behavior>
  <action>
**Route `Modules/Community/Routes/web.php`** — `Route::middleware(['web', 'auth'])->group(function () { Route::get('/community/mystery-merchants', \Modules\Community\Internal\Http\Livewire\MysteryMerchantsPage::class)->name('community.mystery-merchants'); });`.

**Livewire `Modules/Community/Internal/Http/Livewire/MysteryMerchantsPage.php`** — `#[Layout('layouts.app')]`. Public properties: `int $page = 1; int $perPage = 12;`. Method `render(ViewFactory $views, DatabaseManager $db, CurrentUser $currentUser, CommunityCorpusQuery $corpus, MerchantNameResolver $resolver): View`:
- Query transactions for the user, grouped by description (or normalized form), filtering out those resolvable by MerchantNameResolver. Top N descriptions by count → render as cards.
- Stats: mystery code count (computed above), `$corpus->mappingsCount()`, percentage-auto-named, user-contribution count (ship 0 since the bot is deferred).
- Returns view with `'rows' => $cards, 'stats' => [...]`.

Per rule C-1: every collaborator method-DI'd; zero global helpers.

**Blade `Modules/Community/Resources/views/livewire/mystery-merchants-page.blade.php`** — H1 "Mystery merchants", lede, stats strip (4 cards with tabular-nums values), card grid mounting `<x-community::mystery-card />` per row; "About the shared list" footer section. Mount `<livewire:community.suggest-mapping-modal />` once at the bottom.

**Blade component `Modules/Community/Resources/views/components/mystery-card.blade.php`** — props: `$row` (the mystery code + count + last-seen + payment-type). Renders the card layout per UI-SPEC §"/community/mystery-merchants destination": grid template `1.4fr 1fr 1fr auto`, mono code chip, "Likely:" line, last-seen + frequency, payment-type chip rendered via `<span class="ptype-chip {{ $row->paymentType?->chipClass() }}">{{ $row->paymentType?->chipLabel() }}</span>`, primary "Suggest a name →" CTA `wire:click="$dispatch('suggest-mapping:open', {rawDescription: '{{ $row->description }}'})"`, secondary "Skip — show in 30 days" CTA (logs only; snooze storage is out-of-scope).

**Modify `Modules/Community/Providers/CommunityServiceProvider.php`** — register Livewire `community.mystery-merchants-page`.

**Modify `resources/css/app.css` — @layer components**:
- `.mystery-card { display: grid; grid-template-columns: 1.4fr 1fr 1fr auto; gap: 16px; padding: 16px; background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-md); }`
- `.mystery-card .code { font-family: var(--font-mono); background: var(--color-surface-2); padding: 3px 8px; border-radius: var(--radius-sm); font-size: var(--text-sm); }`

**Test `Modules/Community/tests/Feature/MysteryMerchantsPageTest.php`** — Pest feature, RefreshDatabase. Create user, actingAs, seed corpus, seed transactions with at least one description NOT in the corpus AND not in user aliases. Cases:
1. GET `/community/mystery-merchants` → 200 + assert page H1 copy + assert at least one mystery card rendered with the unidentified description.
2. The page route requires auth — assert `$this->get(...)` without actingAs returns redirect-to-login or 302.
3. Click "Suggest a name →" on a card → assert Livewire `assertDispatched('suggest-mapping:open')` with the card's raw description.
Minimum 4 assertions.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Community/tests/Feature/MysteryMerchantsPageTest.php --no-coverage</automated>
  </verify>
  <done>
    /community/mystery-merchants route renders the destination page with stats + cards driven by user data; payment-type chips on cards reflect the Plan 02 enum; suggest-mapping modal mount works; MysteryMerchantsPageTest passes with ≥4 assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3b: SharedListSettingsPanel + HelpOthersTriageButton + Triage row integration in Modules/Categorization/ + SharedListSettingsPanelTest + TriageHelpOthersCtaTest (plan close-out gate)</name>
  <files>Modules/Community/Internal/Http/Livewire/SharedListSettingsPanel.php, Modules/Community/Internal/Http/Livewire/HelpOthersTriageButton.php, Modules/Community/Resources/views/livewire/shared-list-settings-panel.blade.php, Modules/Community/Resources/views/livewire/help-others-triage-button.blade.php, Modules/Community/Providers/CommunityServiceProvider.php, Modules/Categorization/Internal/Http/Livewire/TriageInbox.php, Modules/Categorization/Resources/views/livewire/triage-inbox.blade.php, resources/css/app.css, Modules/Community/tests/Feature/SharedListSettingsPanelTest.php, Modules/Community/tests/Feature/TriageHelpOthersCtaTest.php</files>
  <read_first>
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md lines 319-329 (Triage row + community CTA copy) + lines 356-376 (Settings → Shared merchant list)
    - .claude/skills/sketch-findings-diederik/references/community-merchant-identification.md (sketch 005C)
    - Modules/Categorization/Internal/Http/Livewire/TriageInbox.php FULL (current triage row Livewire)
    - Modules/Categorization/Resources/views/livewire/triage-inbox.blade.php (current row template — append `.row-cta` group)
    - Modules/Core/Internal/Http/Livewire/SettingsPage.php (settings panel registration pattern)
    - Modules/Core/Resources/views/livewire/settings-page.blade.php
    - Modules/Core/Models/User.php (Plan 01 extended — `community_settings` is an array-cast nullable column; this task reads + writes via Eloquent `$user->community_settings` directly)
    - .planning/ROADMAP.md (Phase 18: Auto-Update Plumbing — verify whether a specific version number is documented; if not, Toggle 3 copy is version-agnostic)
    - Modules/Categorization/tests/Feature/TriagePageTest.php (Triage row Livewire feature test pattern)
  </read_first>
  <behavior>
    - `SharedListSettingsPanel` renders the UI-SPEC §"Settings → Shared merchant list" layout: 280px+1fr two-column grid, meta-side "About the shared list" + stats, body-side 3 toggle rows.
    - Toggle 1 = "Use the shared merchant list" (default true). Toggle 2 = "Offer to contribute" (default true). Toggle 3 = "Update on app updates" (default false, ships disabled-no-op).
    - Toggle changes write to `users.community_settings` JSON column. Reads/writes use `$user->community_settings` directly (the Eloquent array cast from Plan 01); no separate UserSettings model.
    - Toggle 3's inline note is VERSION-AGNOSTIC per B-5: "Activates with a future app update — see Settings → About for the current version." NO invented version number. ROADMAP.md confirms Phase 18 ships auto-update plumbing but no specific release version is committed at planning time.
    - `HelpOthersTriageButton` renders a dashed-border CTA `❋ Help others identify this` that dispatches `suggest-mapping:open` with the triage row's raw description. Component is gated on `Settings → Offer to contribute` toggle — when off, the component returns an empty view (server-side absent, NOT CSS-hidden, per UI-SPEC §"Empty / loading / error states").
    - Triage row Livewire (`TriageInbox.php` + blade) extended with a `.row-cta` group hosting both the existing "Rename & categorise" pill-btn AND the new `<livewire:community.help-others-triage-button :raw="$row->raw_description" />` mount.
    - SuggestMappingModal is mounted once at the layout level so all three surfaces (Triage / browse page / Settings → "Browse mystery merchants" CTA) dispatch to it.
    - SharedListSettingsPanelTest: panel renders 3 toggles; toggling Toggle 1 writes to `users.community_settings` with the new value; Toggle 3 toggle attempt is a no-op (column not mutated for that key); inline note is rendered and contains NO version number string (assert via regex no-match against `/\b\d+\.\d+/`).
    - TriageHelpOthersCtaTest: toggle ON (default) → the CTA renders inside the triage row; toggle OFF → the CTA does NOT render.
    - Plan close-out gate (final step of this task): `composer test && composer analyse && composer format:check` is green.
  </behavior>
  <action>
**Livewire `Modules/Community/Internal/Http/Livewire/SharedListSettingsPanel.php`** — public properties: `bool $useSharedList = true; bool $offerToContribute = true; bool $updateOnAppUpdates = false;`. `mount(CurrentUser $currentUser)` reads `$user->community_settings ?? []` and assigns each toggle from that array (defaults preserved when missing). Update method per toggle (e.g. `toggleUseSharedList(CurrentUser $currentUser)`) reads the existing JSON, flips the relevant key, assigns the array back to `$user->community_settings`, calls `$user->save()`. `toggleUpdateOnAppUpdates` is a no-op (writes nothing — Toggle 3 is disabled in this phase per UI-SPEC §"Toggle 3 sub").

`render(ViewFactory $views, CommunityCorpusQuery $corpus): View` returns the blade with `stats` (mappings count + contributors count) passed in.

Per rule C-1: every collaborator method-DI'd. Per C-2/C-3: no GSD leakage, no temporal language in PHPDoc / comments.

**Livewire `Modules/Community/Internal/Http/Livewire/HelpOthersTriageButton.php`** — public properties: `string $raw = ''; bool $visible = true;`. `mount(CurrentUser $currentUser, string $raw)` assigns `$this->raw = $raw`. Reads `$currentUser->user()->community_settings['offerToContribute'] ?? true` and assigns `$this->visible = $value`. `clicked(): void` → `$this->dispatch('suggest-mapping:open', rawDescription: $this->raw)`. Render returns an empty view when `! $visible`. The component should re-render when the underlying setting changes — Livewire's default re-render on parent state change covers this in the simple case; if not, expose a `#[On('shared-list-settings:saved')]` listener that re-reads the setting.

**Blade `Modules/Community/Resources/views/livewire/shared-list-settings-panel.blade.php`** — two-column 280px + 1fr grid; meta-side renders the "About the shared list" headline + paragraph + "View the file on GitHub →" link (uses the config base from `config('community.github_compare_base')` — via method-injected `\Illuminate\Contracts\Config\Repository` if the helper is forbidden); body-side has 3 toggle rows. Toggle 3 ships disabled with `disabled` attribute + inline note "Activates with a future app update — see Settings → About for the current version." (VERSION-AGNOSTIC per B-5; no invented release number).

**Blade `Modules/Community/Resources/views/livewire/help-others-triage-button.blade.php`** — returns `<button type="button" wire:click="clicked" class="help-others-link">❋ Help others identify this</button>` ONLY when `$visible`. Otherwise empty `<div></div>`.

**Modify `Modules/Categorization/Internal/Http/Livewire/TriageInbox.php`** — read the file; pass the row's `raw_description` to the new HelpOthersTriageButton in the per-row blade markup.

**Modify `Modules/Categorization/Resources/views/livewire/triage-inbox.blade.php`** — per row, render `<div class="row-cta">` wrapping the existing "Rename & categorise" pill-btn alongside the new `<livewire:community.help-others-triage-button :raw="$row->raw_description" :key="'help-'.$row->id" />`.

**Modify `resources/css/app.css` — @layer components**:
- `.row-cta { display: flex; gap: 8px; opacity: 0; transition: opacity 150ms var(--ease-smooth, ease); }`
- `.triage-row:hover .row-cta { opacity: 1; }` (use the actual row class — verify via triage-inbox.blade.php)
- `.help-others-link { padding: 4px 8px; border: 1px dashed var(--color-border-strong); background: transparent; color: var(--color-text-muted); border-radius: var(--radius-sm); font-size: var(--text-xs); cursor: pointer; }`
- `.help-others-link:hover { border-color: var(--color-border); color: var(--color-text); }`
- `.settings-section { display: grid; grid-template-columns: 280px 1fr; gap: 24px; padding: 22px; border-bottom: 1px solid var(--color-border); }`
- `.settings-section .meta-side { background: var(--color-bg-subtle); padding: 22px; border-radius: var(--radius-md); }`
- `.settings-section .body-side { padding: 22px; }`
- `.toggle-row { display: flex; align-items: center; justify-content: space-between; padding: 12px 0; border-bottom: 1px solid var(--color-border); }`
- `.toggle-row:last-of-type { border-bottom: 0; }`

**Modify `Modules/Community/Providers/CommunityServiceProvider.php`** — register Livewire `community.shared-list-settings-panel` + `community.help-others-triage-button`. Hook the SharedListSettingsPanel into the Core SettingsPage's panel rendering (find the registration mechanism inside SettingsPage.php).

**Test `Modules/Community/tests/Feature/SharedListSettingsPanelTest.php`** — Pest feature, RefreshDatabase. `beforeEach`: create user, actingAs. Cases:
1. Render the panel → assert 3 toggle rows render; Toggle 3 has the `disabled` attribute; Toggle 3's inline note text contains the substring "Activates with a future app update" AND does NOT match the regex `/\b\d+\.\d+/` (no version number).
2. `Livewire::test(SharedListSettingsPanel::class)->call('toggleUseSharedList')` → re-read `$user->fresh()->community_settings` → assert the `useSharedList` key flipped to false (started default true).
3. `Livewire::test(SharedListSettingsPanel::class)->call('toggleUpdateOnAppUpdates')` → re-read `$user->fresh()->community_settings` → assert the `updateOnAppUpdates` key is NOT set (or is still false) — confirming the no-op behavior.
Minimum 5 assertions.

**Test `Modules/Community/tests/Feature/TriageHelpOthersCtaTest.php`** — Pest feature, RefreshDatabase. Cases:
1. User with `offerToContribute=true` (default) viewing the triage page → assert the rendered triage row HTML contains `❋ Help others identify this` for the mystery rows.
2. Set the user's `community_settings` to `['offerToContribute' => false]`, save, re-render the triage page → assert the HTML does NOT contain the help-others CTA copy.
Minimum 2 assertions.

Plan close-out — final step of this task: run `composer test && composer analyse && composer format:check`. Per rule C-4 fix every BLOCKER + WARNING + INFO surfaced.

Audit: run `grep -rnE "[^\\$\\>]\\b(redirect|now|abort|response|event|auth|request)\\(" Modules/Community/` against new code; expect zero matches. Replace any with method-injected `Illuminate\Routing\Redirector`, `Illuminate\Support\DateFactory`, `Illuminate\Contracts\Events\Dispatcher`, `Illuminate\Contracts\Routing\ResponseFactory`, the project's `CurrentUser` contract, or concrete exception classes.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Community/tests/ Modules/Import/tests/Unit/MerchantNameResolverCommunityTest.php Modules/Categorization/tests/Feature/TriagePageTest.php --no-coverage && composer test && composer analyse && composer format:check</automated>
  </verify>
  <done>
    SharedListSettingsPanel exposes 3 toggles per UI-SPEC (Toggle 3 disabled with version-agnostic inline note); SharedListSettingsPanel persists Toggle 1/2 state in users.community_settings via Eloquent array cast; HelpOthersTriageButton is server-side absent when Toggle 2 is off; Triage row integrates the new CTA without breaking existing tests; SharedListSettingsPanelTest + TriageHelpOthersCtaTest pass with ≥7 assertions total; Larastan + Pint green; full `composer test` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled corpus YAML ↔ PHP runtime | YAML is a static file in the project tree; loaded via symfony/yaml with PARSE_EXCEPTION_ON_INVALID_TYPE to prevent PHP-object instantiation tag exploits |
| SuggestMappingModal input ↔ GitHub Compare URL | User-supplied name/category/region are URL-encoded before insertion into the Compare URL body |
| OpenExternalUrlAction ↔ system browser | Defense-in-depth: https-only + github.com host allow-list before the Shell contract invocation |
| MerchantNameResolver community lookup ↔ shared table | CommunityCorpusQuery reads `community_merchant_mappings` with explicit `whereNull('user_id')` filter |
| SharedListSettingsPanel ↔ users.community_settings | Eloquent array cast; per-user write filtered by `$currentUser->user()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-05-01 | Tampering | Malicious YAML in resources/corpus/* instantiating arbitrary PHP objects | mitigate | symfony/yaml `PARSE_EXCEPTION_ON_INVALID_TYPE` flag rejects object/php-tag YAML; per ASVS V10 |
| T-16.1-05-02 | Tampering | open-redirect via SuggestMappingModal | mitigate | OpenExternalUrlAction enforces https + host allow-list (github.com) BEFORE delegating to Shell |
| T-16.1-05-03 | Information Disclosure | Cross-user leak via community_merchant_mappings | accept | This plan only writes global rows; per-user overrides are future scope |
| T-16.1-05-04 | Tampering | Crafted raw description in suggest-mapping:open event (XSS) | mitigate | Livewire's default Blade escaping covers the read-only mono field; the URL builder url-encodes |
| T-16.1-05-05 | DoS | Giant YAML corpus file causing OOM at seed time | mitigate | Bundled file is project-controlled (audited at PR time); the seeder is per-entry try/catch |
| T-16.1-05-06 | Tampering | Triage row CTA appearing despite Toggle 2 being off (defense-in-depth bypass) | mitigate | HelpOthersTriageButton checks the setting on every render; the CTA is server-side absent (returns empty view), not CSS-hidden |
| T-16.1-05-07 | Elevation of Privilege | UserInstalled handler running with elevated context | accept | The listener writes only to community_merchant_mappings (global) using updateOrInsert; no escalation surface |
| T-16.1-05-08 | Tampering | users.community_settings written by a different user via Livewire tampering | mitigate | SharedListSettingsPanel resolves the target user via `$currentUser->user()` (method-DI); writes use Eloquent on that exact model instance — never via mass-assign from request input |
| T-16.1-05-CSRF | Tampering | CSRF on SharedListSettingsPanel toggle change | mitigate | Livewire's web-middleware CSRF protection covers all toggle-change POSTs |
</threat_model>

<verification>
- `vendor/bin/pest Modules/Community/tests/` is fully green.
- `vendor/bin/pest Modules/Import/tests/Unit/MerchantNameResolverCommunityTest.php` passes.
- The full `composer test` suite is green.
- `composer analyse` + `composer format:check` clean.
- New arch invariant: `Native\Desktop\Contracts\Shell` only imported by OpenExternalUrlAction + NoOpShell (or surveyed allow-list).
- Toggle 3 inline note is version-agnostic (no `\d+\.\d+` match in the rendered HTML).
- Zero global-helper usages in new code (`grep -rnE "[^\\$\\>]\\b(redirect|now|abort|response|event|auth|request)\\(" Modules/Community/` returns no hits).
</verification>

<success_criteria>
- CORPUS-02 + CORPUS-03 + CORPUS-04 + CORPUS-05 green per their tests.
- Bundled YAML files exist with ≥30 corpus entries + 12 built-in heuristics.
- SeedCommunityCorpus is idempotent + partial-failure-tolerant; the loader logs unknown-category warnings; uses symfony/yaml with strict-type parsing.
- MerchantNameResolver's community fallback is wired.
- OpenExternalUrlAction + NoOpShell + arch invariant restricting Shell-contract imports all green.
- SuggestMappingModal opens with prefilled pattern, builds a deterministic-slug GitHub Compare URL, launches it, dispatches MysteryMerchantSubmitted via injected Dispatcher.
- /community/mystery-merchants page renders with the UI-SPEC layout + payment-type chips from Plan 02's enum.
- Settings panel exposes 3 toggles; Toggle 3 ships disabled with version-agnostic inline note; Toggle 1/2 persist in `users.community_settings`.
- HelpOthersTriageButton is server-side absent when Toggle 2 is off.
- Larastan L10 strict + Pint clean.
- Zero `.planning/` / D-NN / PLAN.md leakage in production source.
- Zero global helpers introduced.
</success_criteria>

<output>
Create `.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-05-SUMMARY.md` when done.
</output>
</content>
</invoke>