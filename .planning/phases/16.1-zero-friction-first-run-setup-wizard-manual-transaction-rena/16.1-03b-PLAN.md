---
phase: 16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena
plan: 03b
type: execute
wave: 3
depends_on: [16.1-01, 16.1-03a]
files_modified:
  - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectBankStep.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectCardStep.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectEmailStep.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php
  - Modules/Onboarding/Resources/views/livewire/steps/connect-bank-step.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/connect-card-step.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/connect-email-step.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php
  - Modules/Onboarding/Resources/views/components/wiz-card.blade.php
  - Modules/Onboarding/Resources/views/components/wiz-dots.blade.php
  - Modules/Onboarding/Resources/views/components/wiz-actions.blade.php
  - Modules/Onboarding/Resources/views/components/vd-glyph.blade.php
  - Modules/Onboarding/Resources/views/components/mini-step.blade.php
  - Modules/Onboarding/Resources/views/components/format-chip.blade.php
  - Modules/Onboarding/Resources/views/components/drop-zone.blade.php
  - Modules/Onboarding/Resources/views/components/privacy-pill.blade.php
  - Modules/Onboarding/Providers/OnboardingServiceProvider.php
  - resources/css/app.css
  - Modules/Onboarding/tests/Feature/SkipWizardStepTest.php
  - Modules/Onboarding/tests/Feature/EmailStepEmbedsOAuthModalTest.php
  - Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php
autonomous: true
requirements: [WIZ-03, WIZ-04, WIZ-05]
must_haves:
  truths:
    - "Every skippable step (bank, card, email) has a Skip button that marks the wizard_progress row 'skipped' and advances; a 'Skip the rest' affordance marks every remaining row 'skipped' and routes to /"
    - "The email step embeds the existing OAuthClientWizardModal (no duplicated OAuth dance); successful authorization marks the email row 'done' and advances"
    - "The first-import step relaxes the card width to ~1120px while other steps stay at 620px"
    - "8 atomic Blade components (wiz-card, wiz-dots, wiz-actions, vd-glyph, mini-step, format-chip, drop-zone, privacy-pill) compose every wizard step's chrome and replace any inline placeholder markup from Plan 03a's welcome-step / done-step blades"
    - "ConnectBankStep / ConnectCardStep reuse the existing Modules\\Import\\Public\\Contracts\\RunsImports — no new upload pipeline"
    - "FirstImportStep mounts the existing Modules\\Import upload-wizard Livewire component inline at 1120px — no duplicate UI"
  artifacts:
    - path: "Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectEmailStep.php"
      provides: "Email connector step embedding OAuthClientWizardModal via dispatch"
      contains: "oauth-client-wizard:open"
    - path: "Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php"
      provides: "First-import wrapper mounting UploadWizard inline at 1120px"
      contains: "upload-wizard"
    - path: "resources/css/app.css"
      provides: "Wizard CSS components inside @layer components using existing tokens"
      contains: ".wiz-card"
  key_links:
    - from: "Modules/Onboarding/Resources/views/livewire/steps/connect-email-step.blade.php"
      to: "Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal"
      via: "<livewire:email-scan.o-auth-client-wizard-modal /> mount + $this->dispatch('oauth-client-wizard:open', provider: ...)"
      pattern: "o-auth-client-wizard-modal"
    - from: "Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php"
      to: "Modules/Import/Internal/Http/Livewire/UploadWizard"
      via: "<livewire:import.upload-wizard /> inside a wiz-card--wide container"
      pattern: "upload-wizard"
---

<objective>
Ship the four connector step components (ConnectBankStep, ConnectCardStep, ConnectEmailStep, FirstImportStep) + their UI-SPEC-locked blade views + 8 atomic Blade chrome components + the wizard CSS (under `@layer components` in `resources/css/app.css`) + the 3 acceptance tests covering WIZ-03 (skip), WIZ-04 (email modal embed), and WIZ-05 (first-import width relaxation). Slots cleanly under Plan 03a's SetupWizard parent without changes to it (Plan 03a already emits the @switch cases for these step keys).

Purpose: D-01 / D-02 / D-03 / D-04 (UI half). WIZ-03 / WIZ-04 / WIZ-05. The connector steps are the visible content of the wizard — without them, a non-technical user can sign up and land on /setup but only sees the welcome + done shells.

Output: 4 connector step Livewire components + 4 step blade views + 8 atomic Blade components + the wizard CSS classes in `app.css` under `@layer components` (no new theme tokens) + 3 acceptance tests + Larastan L10 strict + Pint clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-CONTEXT.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md

@CLAUDE.md
@.claude/skills/sketch-findings-diederik/SKILL.md
@.claude/skills/sketch-findings-diederik/references/onboarding-wizard.md

@Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php
@Modules/Onboarding/Internal/Http/Livewire/Steps/WelcomeStep.php
@Modules/Onboarding/Internal/Http/Livewire/Steps/DoneStep.php
@Modules/Onboarding/Resources/views/livewire/setup-wizard.blade.php
@Modules/Onboarding/Providers/OnboardingServiceProvider.php

@Modules/Import/Internal/Http/Livewire/UploadWizard.php
@Modules/Import/Public/Contracts/RunsImports.php
@Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal.php
@Modules/EmailScan/Internal/Http/Livewire/InboxesPage.php

@Modules/Core/Public/Contracts/CurrentUser.php
@Modules/Core/Public/Services/CurrentUserService.php

@Modules/DevMode/Resources/views/components/run-card.blade.php
@Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php

@resources/css/app.css

<interfaces>
From Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal.php (the modal hosted by ConnectEmailStep, not duplicated). Read the file head to confirm the dispatch event name; the modal listens via `#[On('oauth-client-wizard:open')]` and fires `oauth-client-wizard:saved` (or similar) on success.

From Modules/Import/Internal/Http/Livewire/UploadWizard.php (the upload-wizard mounted inline by FirstImportStep — find the existing event the wizard fires on "preview ready" success).

From Modules/Import/Public/Contracts/RunsImports.php (the existing pipeline contract — ConnectBankStep / ConnectCardStep submit() calls it; no new upload surface introduced).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 8 atomic Blade components + the wizard CSS @layer components additions</name>
  <files>Modules/Onboarding/Resources/views/components/wiz-card.blade.php, Modules/Onboarding/Resources/views/components/wiz-dots.blade.php, Modules/Onboarding/Resources/views/components/wiz-actions.blade.php, Modules/Onboarding/Resources/views/components/vd-glyph.blade.php, Modules/Onboarding/Resources/views/components/mini-step.blade.php, Modules/Onboarding/Resources/views/components/format-chip.blade.php, Modules/Onboarding/Resources/views/components/drop-zone.blade.php, Modules/Onboarding/Resources/views/components/privacy-pill.blade.php, resources/css/app.css</files>
  <read_first>
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md FULL (Component Inventory lines 437-466, Color lines 113-165, Spacing lines 37-74, Density rules lines 53-65)
    - .claude/skills/sketch-findings-diederik/references/onboarding-wizard.md FULL (sketches 002 + 003 visual lock)
    - Modules/DevMode/Resources/views/components/run-card.blade.php (atomic Blade component shape with slot + props)
    - Modules/DevMode/Resources/views/components/status-pill.blade.php
    - Modules/DevMode/Resources/views/components/tier-chip.blade.php
    - resources/css/app.css FULL — find @theme block, @layer components block; the new component classes land in @layer components using EXISTING tokens (no new theme tokens per UI-SPEC §Spacing and §Color)
  </read_first>
  <behavior>
    - 8 Blade components live under `Modules/Onboarding/Resources/views/components/`. Each is a small Blade Component (props + slot) per UI-SPEC §"Component Inventory".
    - `resources/css/app.css`'s `@layer components` gains every new class needed by the components + the wizard layout from Plan 03a.
    - No new `@theme` tokens are added. Every new CSS class references existing CSS variables.
  </behavior>
  <action>
**Blade components — 8 files** under `Modules/Onboarding/Resources/views/components/`:

- `wiz-card.blade.php`: optional prop `$wide` (bool, defaults false). Renders `<div @class(['wiz-card', 'wiz-card--wide' => $wide])>{{ $slot }}</div>`.
- `wiz-dots.blade.php`: props `array $progress` + `string $current`. Renders 6 dots/rects per UI-SPEC §"Wizard chrome" (8px dot for upcoming/done [emerald], 24px rounded rect for current).
- `wiz-actions.blade.php`: right-aligned flex of slot children.
- `vd-glyph.blade.php`: props `string $glyph`, `string $title`, `string $description`, `?bool $optional = null`. Renders 44×44 tile + title + description rows.
- `mini-step.blade.php`: tile for connector-step mini-step grid.
- `format-chip.blade.php`: chip with optional "[recommended]" badge.
- `drop-zone.blade.php`: dashed border + glyph + lede + sub-link + a `<input type="file" wire:model="file">` slot.
- `privacy-pill.blade.php`: "● Your data stays on this computer".

**Modify `resources/css/app.css` — `@layer components`** — ADD (do not modify existing rules):
- `.wiz-page { min-height: calc(100vh - 48px); display: flex; flex-direction: column; background: var(--color-bg-subtle); }`
- `.wiz-top { display: flex; align-items: center; justify-content: space-between; padding: 16px 24px; }`
- `.wiz-body { flex: 1; display: grid; place-items: center; padding: 24px; }`
- `.wiz-card { max-width: 620px; width: 100%; background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-xl); padding: 44px 48px 32px; box-shadow: var(--shadow-md); }`
- `.wiz-card--wide { max-width: 1120px; padding: 32px; }` (the first-import-step exception)
- `.wiz-dots { display: flex; gap: 8px; align-items: center; }`
- `.wiz-dots .dot { width: 8px; height: 8px; border-radius: 999px; background: var(--color-border); }`
- `.wiz-dots .dot.done { background: var(--color-emerald, #059669); }`
- `.wiz-dots .dot.now { width: 24px; height: 8px; border-radius: 4px; background: var(--color-text); }`
- `.wiz-actions { display: flex; justify-content: flex-end; gap: 8px; margin-top: 24px; }`
- `.wiz-footer { display: flex; align-items: center; justify-content: space-between; padding: 16px 24px; color: var(--color-text-muted); font-size: var(--text-xs); }`
- `.privacy-pill { display: inline-flex; align-items: center; gap: 4px; }`
- `.privacy-pill::before { content: ''; width: 6px; height: 6px; border-radius: 999px; background: var(--color-emerald, #059669); display: inline-block; }`
- `.vd-rows { display: flex; flex-direction: column; gap: 16px; margin-top: 24px; }`
- `.vd-glyph { display: flex; align-items: center; gap: 12px; }`
- `.vd-glyph .tile { width: 44px; height: 44px; display: grid; place-items: center; background: var(--color-bg-subtle); border-radius: var(--radius-md); font-size: 26px; }`
- `.mini-steps { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; }`
- `.mini { padding: 14px 12px; border-radius: var(--radius-md); display: flex; flex-direction: column; gap: 4px; }`
- `.mini.done { background: var(--color-emerald-bg, rgba(16,185,129,0.10)); }`
- `.mini.now { background: var(--color-surface-2); color: var(--color-text); }`
- `.format-chips { display: flex; align-items: center; gap: 8px; }`
- `.format-chip { padding: 4px 10px; border-radius: 999px; background: var(--color-surface-2); border: 1px solid var(--color-border); font-size: var(--text-xs); }`
- `.format-chip.recommended { background: var(--color-emerald-bg); color: var(--color-emerald, #059669); }`
- `.drop-zone { padding: 28px 20px; border: 1px dashed var(--color-border-strong); border-radius: var(--radius-md); display: flex; flex-direction: column; align-items: center; gap: 8px; }`
- `.drop-zone:hover { background: var(--color-bg-subtle); }`

Per UI-SPEC §"Design System" token-source: NONE of these new classes introduces a new theme token; all reference existing CSS variables from `@theme`. If `--color-emerald` or `--color-emerald-bg` is not exported, use literal hex per UI-SPEC §"Color" sketch-locked exceptions.
  </action>
  <verify>
    <automated>php artisan view:clear && vendor/bin/pest Modules/Onboarding/tests/ --no-coverage</automated>
  </verify>
  <done>
    8 Blade components exist under Resources/views/components/; resources/css/app.css has the new component classes inside `@layer components` using existing tokens; Plan 03a's welcome-step + done-step blades can swap inline markup for `<x-onboarding::*>` tags without rendering changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4 connector step Livewire components + step blade views + service-provider Livewire registrations</name>
  <files>Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectBankStep.php, Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectCardStep.php, Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectEmailStep.php, Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php, Modules/Onboarding/Resources/views/livewire/steps/connect-bank-step.blade.php, Modules/Onboarding/Resources/views/livewire/steps/connect-card-step.blade.php, Modules/Onboarding/Resources/views/livewire/steps/connect-email-step.blade.php, Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php, Modules/Onboarding/Providers/OnboardingServiceProvider.php</files>
  <read_first>
    - Modules/Import/Internal/Http/Livewire/UploadWizard.php lines 39-272 (WithFileUploads + Validate + submit($importer, $currentUser, ...) — pattern for ConnectBankStep + ConnectCardStep; FirstImportStep mounts UploadWizard inline)
    - Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal.php FULL (find the event names it dispatches/listens to; verify the registered Livewire component name via `grep -rn "->component(" Modules/EmailScan/Providers/`)
    - Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php (Flux modal SFC pattern — referenced by ConnectEmailStep which dispatches modal-show)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md (ASN connector lines 211-225, ICS connector lines 226-242, Email step lines 243-256, Component Inventory lines 437-466)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 425-510 (connector-step analog cites)
    - Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php (Plan 03a output — read to confirm the `wizard.step.completed` + `wizard.step.skipped` event names the step components dispatch)
    - Modules/Onboarding/Providers/OnboardingServiceProvider.php (Plan 03a registered 3 Livewire components; this task appends 4 more)
    - Modules/Core/Public/Contracts/CurrentUser.php
  </read_first>
  <behavior>
    - Each connector step is `final class XxxStep extends Component`. Each step's `render(ViewFactory $views): View` returns `$views->make('onboarding::livewire.steps.xxx-step')`.
    - ConnectBankStep: `use WithFileUploads`. Properties `?TemporaryUploadedFile $file = null`, `?string $uploadError = null`, `string $selectedFormat = 'asn-camt053'`. `rules()` returns the file rule (mirror UploadWizard's exact shape — extensions:csv,xml,sta,mt940,940 + max size). `submit(RunsImports $importer, CurrentUser $currentUser, LoggerInterface $logger)` mirrors UploadWizard's submit shape but on success dispatches `wizard.step.completed` (NOT a redirect — the wizard advances in-place). Optional `skip(): void` dispatches `wizard.step.skipped`.
    - ConnectCardStep: same shape as ConnectBankStep but locked to `extensions:pdf` + `max:10240` (per project MEMORY rule `project_ics_portal_pdf_only`); only one format chip "PDF [only format]" muted per UI-SPEC.
    - ConnectEmailStep: NO WithFileUploads. Properties: `?string $authStartedFor = null`. Methods: `authorize(string $provider): void` validates provider is 'gmail' or 'microsoft' → `$this->authStartedFor = $provider` → `$this->dispatch('oauth-client-wizard:open', provider: $provider)`. `#[On('oauth-client-wizard:saved')] public function onAuthSaved(): void` dispatches `wizard.step.completed`. `skip(): void` dispatches `wizard.step.skipped`. Blade mounts `<livewire:email-scan.o-auth-client-wizard-modal />` (verify the exact registered name via grep over EmailScanServiceProvider).
    - FirstImportStep: minimal Livewire shell. Blade mounts `<livewire:import.upload-wizard />` inside a 1120px-wide container (use `<x-onboarding::wiz-card wide>` from Task 1). The UploadWizard's existing event for "preview submitted successfully" is captured via `#[On(...)]` and dispatches `wizard.step.completed`. If UploadWizard doesn't currently fire a structured event on success (it redirects to imports.preview), this task adds a small adapter — DO NOT modify UploadWizard's submit() (it's used in many places); listen to UploadWizard's existing dispatched events or use a Livewire global dispatch sentinel.
    - The chip/popover/pill geometry exceptions from UI-SPEC §"Spacing density rules" apply only to chip/popover/pill components — never to wizard card / layout containers.
  </behavior>
  <action>
**Step components — 4 of them.**

`ConnectBankStep.php`: properties + methods per <behavior>. Method DI on `submit(RunsImports $importer, CurrentUser $currentUser, LoggerInterface $logger): void` — never call global helpers. On validation failure, leave `$this->uploadError = $error` and the modal stays open. On success, dispatch `wizard.step.completed`.

`ConnectCardStep.php`: same shape as ConnectBankStep but locked to PDF-only.

`ConnectEmailStep.php`: per <behavior>. The dispatch payload for `oauth-client-wizard:open` includes the provider string; verify Plan 06's modal accepts the additional optional `?int $inboxId = null` argument — for the wizard flow, `inboxId` is omitted (this is a fresh-connect, not a re-consent).

`FirstImportStep.php`: per <behavior>. Method `onUploadPreviewReady(): void` (with the actual event name from UploadWizard) dispatches `wizard.step.completed`.

**Step blade views — 4 files.** Each composes the wizard primitives via `<x-onboarding::wiz-card>...</x-onboarding::wiz-card>`. Render UI-SPEC copy verbatim (the §Copywriting Contract is the source of truth — every label and CTA matches).

Example `connect-bank-step.blade.php` skeleton:
- `<x-onboarding::wiz-card>` outer
- `<p class="wiz-eyebrow">Step 1 of 4</p>`
- H1 + lede per UI-SPEC §"ASN connector"
- `<x-onboarding::mini-steps>` group
- `<x-onboarding::format-chips>` row
- `<x-onboarding::drop-zone>` with `wire:model="file"`
- `<x-onboarding::wiz-actions>` with Skip + Continue CTAs

`connect-card-step.blade.php` analogously for ICS PDF (only one format chip).

`connect-email-step.blade.php`:
- Two buttons: `<flux:button wire:click="authorize('gmail')">Authorize with Gmail</flux:button>` + same for `microsoft`.
- One mounted modal: `<livewire:email-scan.o-auth-client-wizard-modal />`.
- Skip CTA: `<flux:button variant="ghost" wire:click="skip">Skip — set up later in Settings</flux:button>`.

`first-import-step.blade.php`:
- `<x-onboarding::wiz-card wide>` (the 1120px exception)
- Inside the card: `<livewire:import.upload-wizard />`.
- Below the card: `<x-onboarding::wiz-actions>` with a primary CTA that progresses on the upload event (or wires via the `#[On]` listener — the user does not click).

**Modify `Modules/Onboarding/Providers/OnboardingServiceProvider.php`** — append the 4 connector Livewire components to the `boot(LivewireManager $livewire)` registration list:
- `$livewire->component('onboarding.steps.connect-bank-step', ConnectBankStep::class);`
- `$livewire->component('onboarding.steps.connect-card-step', ConnectCardStep::class);`
- `$livewire->component('onboarding.steps.connect-email-step', ConnectEmailStep::class);`
- `$livewire->component('onboarding.steps.first-import-step', FirstImportStep::class);`

Per project rule C-1: every Livewire action method method-DIs its collaborators. Forbidden global helpers: `redirect()`, `now()`, `event()`, `auth()`, `request()`, `abort()`, `response()` — inject `Illuminate\Routing\Redirector`, `Illuminate\Support\DateFactory`, `Illuminate\Contracts\Events\Dispatcher`, the project's `CurrentUser` contract, and concrete exceptions / `Illuminate\Contracts\Routing\ResponseFactory` instead. Livewire's own `$this->dispatch()` and `$this->validate()` are framework methods, not global helpers — keep them. Per rule C-2/C-3: no GSD leakage, no history docs. Per rule C-4: fix all severities.

ALSO: revisit Plan 03a's `welcome-step.blade.php` + `done-step.blade.php` and replace any inline placeholder markup (Tailwind divs imitating wiz-card / vd-glyph from Plan 03a's emergency-fallback rendering) with `<x-onboarding::*>` Blade-component tags now that the components exist.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/ --no-coverage && php artisan view:clear</automated>
  </verify>
  <done>
    4 connector step components exist and are registered as Livewire components; the 4 step blade views compose the Plan 03b atomic Blade components correctly; Plan 03a's welcome-step + done-step blades now use the same atomic components; existing Onboarding tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WIZ-03 + WIZ-04 + WIZ-05 acceptance tests (plan close-out gate)</name>
  <files>Modules/Onboarding/tests/Feature/SkipWizardStepTest.php, Modules/Onboarding/tests/Feature/EmailStepEmbedsOAuthModalTest.php, Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php</files>
  <read_first>
    - Modules/Categorization/tests/Feature/RulesPageTest.php lines 1-90 (Pest Livewire test pattern)
    - Modules/EmailScan/tests/Feature/InboxesPageTest.php OR similar (existing OAuth modal-dispatch assertion pattern)
    - Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php (Plan 03a output)
    - Modules/Onboarding/Internal/Http/Livewire/Steps/*.php (verify the event names used)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md Shared Pattern G (Livewire feature test shape)
    - tests/Pest.php (RefreshDatabase + module TestCase bootstrap)
  </read_first>
  <behavior>
    - SkipWizardStepTest: a user on `connect-bank` calls the step's `skip()` → wizard advances to `connect-card`; the `connect-bank` wizard_progress row has status='skipped'. Also test `skipRest()` from `connect-bank` marks the 3 connector steps + first-import step as skipped + redirects to /. Also test skip is rejected on non-skippable steps (welcome, done).
    - EmailStepEmbedsOAuthModalTest: mounting the ConnectEmailStep + calling `authorize('gmail')` dispatches `oauth-client-wizard:open` with `provider: 'gmail'`; receiving the `oauth-client-wizard:saved` event dispatches `wizard.step.completed`.
    - FirstImportStepWidthTest: rendering FirstImportStep asserts the HTML contains the wiz-card--wide class (the 1120px exception); rendering ConnectBankStep asserts it does NOT contain wiz-card--wide.
    - Plan close-out gate (final step of this task): `composer test && composer analyse && composer format:check` is green.
  </behavior>
  <action>
**Test `Modules/Onboarding/tests/Feature/SkipWizardStepTest.php`** — Pest feature. Three cases:
1. User on `connect-bank`. `Livewire::test(SetupWizard::class)->set('currentStepKey', 'connect-bank')->call('skip')`. Assert: currentStepKey advanced to `'connect-card'`; `DB::table('wizard_progress')->where('user_id', $user->id)->where('step_key', 'connect-bank')->value('status') === 'skipped'`.
2. User on `connect-bank` calls `skipRest`. Assert: redirect to /; every wizard_progress row for the user NOT in {welcome, connect-bank} that was previously 'pending' is now 'skipped' (welcome stays whatever it was, connect-bank stays whatever it was).
3. User on `welcome` calls `skip()`. Assert: nothing happens (skip is no-op on non-skippable steps); welcome stays the current step.
Minimum 5 assertions across the 3 cases.

**Test `Modules/Onboarding/tests/Feature/EmailStepEmbedsOAuthModalTest.php`** — Pest feature. `Livewire::test(ConnectEmailStep::class)`. Assert the rendered HTML contains `<livewire:email-scan.o-auth-client-wizard-modal` (the modal is mounted). Call `->call('authorize', 'gmail')`. Assert `->assertDispatched('oauth-client-wizard:open', provider: 'gmail')`. Then simulate the modal saving: `->dispatch('oauth-client-wizard:saved')` AND assert `->assertDispatched('wizard.step.completed')`. Minimum 3 assertions.

**Test `Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php`** — Pest feature. Render `Livewire::test(FirstImportStep::class)` and assert the HTML body contains the CSS class `wiz-card--wide`. Render `Livewire::test(ConnectBankStep::class)` and assert it does NOT contain `wiz-card--wide`. Minimum 2 assertions.

Plan close-out — final step of this task: run `composer test && composer analyse && composer format:check`. Per rule C-4 fix every BLOCKER + WARNING + INFO surfaced.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/ --no-coverage && composer test && composer analyse && composer format:check</automated>
  </verify>
  <done>
    WIZ-03 + WIZ-04 + WIZ-05 acceptance tests pass with ≥ 10 assertions total; `vendor/bin/pest Modules/Onboarding/tests/` is fully green; Larastan + Pint clean; the wizard is end-to-end usable (sign up → land on /setup → walk through all 6 steps → finish → land on dashboard); the "Settings → First-run tour" link target route `route('setup', ['force' => 1])` works as Plan 03a documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConnectBankStep / ConnectCardStep file upload | Untrusted file content reaches the existing UploadWizard pipeline; this plan adds NO new file-upload surface — reuses Modules/Import/Public/Contracts/RunsImports |
| ConnectEmailStep OAuth handoff | The step ONLY dispatches the open event; the existing OAuthClientWizardModal handles OAuth itself — no new OAuth surface introduced here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-03b-01 | Tampering | File-upload abuse on ConnectBankStep / ConnectCardStep | mitigate | Inherits UploadWizard's existing file-validation chain (extensions allow-list + max-size + ephemeral temp path) via `RunsImports` — no new attack surface |
| T-16.1-03b-02 | Spoofing | ConnectEmailStep authorize() called with unknown provider | mitigate | The method validates `$provider` against the closed set ['gmail', 'microsoft'] before dispatching the modal-open event |
| T-16.1-03b-03 | Tampering | FirstImportStep mounts UploadWizard inline — risk of conflicting Livewire state | accept | Livewire isolates components by ID; the nested upload-wizard maintains its own state independent of FirstImportStep |
| T-16.1-03b-04 | Information Disclosure | ConnectEmailStep dispatches OAuth provider name to the client | accept | Provider name is public ('gmail'/'microsoft'), no PII; the OAuth dance itself runs inside the modal which has its own security review |
| T-16.1-03b-CSRF | Tampering | Cross-site forgery on connector-step wire:click POSTs | mitigate | Livewire's standard CSRF protection covers all wire:click POSTs |
</threat_model>

<verification>
- `vendor/bin/pest Modules/Onboarding/tests/` is fully green with ≥ 10 additional assertions from this plan (≥ 27 total across the 8 test files spanning Plans 03a + 03b).
- `composer analyse` + `composer format:check` are green.
- Manual smoke: walk through all 6 wizard steps after Plan 03a's backbone landed; verify the email step opens the existing OAuthClientWizardModal; verify FirstImportStep is visibly wider than the other steps.
- Every UI-SPEC §"Wizard connector step" copy string is present in the rendered blades.
</verification>

<success_criteria>
- WIZ-03 + WIZ-04 + WIZ-05 all green per their tests.
- 4 connector step components exist and are registered as Livewire components.
- 8 atomic Blade components live under Resources/views/components/.
- `resources/css/app.css` has the new component classes inside `@layer components` using existing tokens — no new theme tokens introduced.
- Onboarding module hosts the existing OAuthClientWizardModal (email step) and existing UploadWizard (first-import step) — zero duplication.
- Calm-slate UI-SPEC contract respected.
- Larastan L10 strict + Pint clean.
- Zero `.planning/` / D-NN / PLAN.md leakage in production source.
- Zero global helpers (`redirect()`, `now()`, `event()`, `auth()`, `request()`, `abort()`, `response()`) introduced; every collaborator is constructor-DI'd (services) or method-DI'd (Livewire).
</success_criteria>

<output>
Create `.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-03b-SUMMARY.md` when done.
</output>
</content>
</invoke>