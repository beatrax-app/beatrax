---
phase: 16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena
plan: 03
type: execute
wave: 2
depends_on: [16.1-01]
files_modified:
  - Modules/Onboarding/Routes/web.php
  - Modules/Onboarding/Public/Services/WizardProgressQuery.php
  - Modules/Onboarding/Internal/Services/WizardStepRegistry.php
  - Modules/Onboarding/Internal/Services/WizardProgressInitializer.php
  - Modules/Onboarding/Internal/Services/ResumeStepResolver.php
  - Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall.php
  - Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/WelcomeStep.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectBankStep.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectCardStep.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectEmailStep.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/DoneStep.php
  - Modules/Onboarding/Resources/views/layouts/app-wizard.blade.php
  - Modules/Onboarding/Resources/views/livewire/setup-wizard.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/welcome-step.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/connect-bank-step.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/connect-card-step.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/connect-email-step.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php
  - Modules/Onboarding/Resources/views/livewire/steps/done-step.blade.php
  - Modules/Onboarding/Resources/views/components/wiz-card.blade.php
  - Modules/Onboarding/Resources/views/components/wiz-dots.blade.php
  - Modules/Onboarding/Resources/views/components/wiz-actions.blade.php
  - Modules/Onboarding/Resources/views/components/vd-glyph.blade.php
  - Modules/Onboarding/Resources/views/components/mini-step.blade.php
  - Modules/Onboarding/Resources/views/components/format-chip.blade.php
  - Modules/Onboarding/Resources/views/components/drop-zone.blade.php
  - Modules/Onboarding/Resources/views/components/privacy-pill.blade.php
  - Modules/Onboarding/Providers/OnboardingServiceProvider.php
  - Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php
  - Modules/Auth/Public/Actions/SignupAction.php
  - resources/css/app.css
  - Modules/Onboarding/tests/Feature/SignupRoutesToSetupTest.php
  - Modules/Onboarding/tests/Feature/ResumeWizardTest.php
  - Modules/Onboarding/tests/Feature/SkipWizardStepTest.php
  - Modules/Onboarding/tests/Feature/EmailStepEmbedsOAuthModalTest.php
  - Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php
  - Modules/Onboarding/tests/Feature/ReRunWizardTest.php
  - Modules/Onboarding/tests/Unit/ResumeStepResolverTest.php
  - Modules/Onboarding/tests/Unit/WizardProgressInitializerTest.php
autonomous: true
requirements: [WIZ-01, WIZ-02, WIZ-03, WIZ-04, WIZ-05, WIZ-06]
must_haves:
  truths:
    - "After signup, the next HTTP response redirects to /setup (the wizard root)"
    - "Killing the app mid-wizard and re-launching drops the user back into the same step with a 'Welcome back' banner"
    - "Every skippable step (bank, card, email) has a Skip button that marks the wizard_progress row 'skipped' and advances; a 'Skip the rest' affordance marks every remaining row 'skipped' and routes to /"
    - "The email step embeds the existing OAuthClientWizardModal (no duplicated OAuth dance); successful authorization marks the email row 'done' and advances"
    - "The first-import step relaxes the card width to ~1120px while other steps stay at 620px"
    - "/setup?force=1 resets every wizard_progress row to 'pending' and re-enters from step 1"
    - "/setup is exempt from EnsureDatabaseReady redirects so the wizard doesn't loop back to /welcome"
  artifacts:
    - path: "Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php"
      provides: "Wizard parent component with resume detection + skip + skip-the-rest + force-reset"
      contains: "WizardProgress"
    - path: "Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall.php"
      provides: "UserInstalled listener seeding 6 wizard_progress rows"
      contains: "UserInstalled"
    - path: "Modules/Onboarding/Routes/web.php"
      provides: "/setup route + /setup?force=1 reset behavior"
      contains: "setup"
    - path: "Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php"
      provides: "EXEMPT_ROUTE_PREFIXES extended with 'setup' (1-line change)"
      contains: "'setup'"
  key_links:
    - from: "Modules/Auth/Public/Actions/SignupAction.php"
      to: "Modules/Core/Public/Events/UserInstalled + Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall"
      via: "event dispatch on signup commit (Phase 16 commit 529fd7a)"
      pattern: "UserInstalled"
    - from: "Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php"
      to: "Modules/Onboarding/Internal/Services/ResumeStepResolver"
      via: "constructor-DI on mount() method (Livewire method-DI)"
      pattern: "ResumeStepResolver"
    - from: "Modules/Auth/Public/Actions/SignupAction.php"
      to: "/setup route redirect"
      via: "UrlGenerator route('setup') redirect on success (replaces existing dashboard redirect)"
      pattern: "route\\('setup'\\)|/setup"
---

<objective>
Build the new Onboarding module's wizard surface end-to-end: `/setup` route, parent `SetupWizard` Livewire component, six step components (welcome / connect-bank / connect-card / connect-email / first-import / done), resume detection, "Welcome back" banner, "Skip the rest" affordance, force-reset semantics, the `InitializeWizardProgressOnInstall` listener wired to `UserInstalled`, the `EnsureDatabaseReady` middleware exemption, and the `app-wizard` body-only layout. Hosts the existing `OAuthClientWizardModal` for the email step and the existing `UploadWizard` for the first-import step — no duplicate OAuth dance, no duplicate upload UI.

Purpose: D-01 / D-02 / D-03 / D-04. The wizard is the user-visible delta for non-technical users. Every later plan's preview / Triage / Settings surface assumes a user has gone through the wizard (or skipped it) and lives in the main app; without a complete wizard the rest of Phase 16.1 has no entry point.

Output: Full wizard module booted, every WIZ-01 to WIZ-06 acceptance test passing, UI-SPEC §"Wizard chrome" / §"Wizard welcome step" / §"Wizard connector step" copy rendered verbatim, calm-slate visual contract respected via the existing token vocabulary, zero new color tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-CONTEXT.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md
@.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md

@CLAUDE.md
@.claude/skills/sketch-findings-diederik/SKILL.md
@.claude/skills/sketch-findings-diederik/references/onboarding-wizard.md

@Modules/Auth/Public/Actions/SignupAction.php
@Modules/Core/Public/Events/UserInstalled.php
@Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php
@Modules/Categorization/Providers/CategorizationServiceProvider.php

@Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php

@Modules/Import/Internal/Http/Livewire/UploadWizard.php
@Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal.php
@Modules/EmailScan/Internal/Http/Livewire/InboxesPage.php
@Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php
@Modules/DevMode/Internal/Http/Livewire/DevOverviewPage.php

@Modules/Core/Public/Contracts/CurrentUser.php
@Modules/Core/Public/Services/CurrentUserService.php

@Modules/Onboarding/Models/WizardProgress.php
@Modules/Onboarding/Public/Events/WizardCompleted.php
@Modules/Onboarding/Providers/OnboardingServiceProvider.php
@Modules/Categorization/Routes/web.php

@resources/css/app.css

<interfaces>
From Modules/Core/Public/Contracts/CurrentUser.php (CurrentUser contract — every Livewire method that needs the authenticated user method-DIs this):

```
interface CurrentUser { public function user(): \Modules\Core\Models\User; public function userId(): int; }
```

From Modules/Auth/Public/Actions/SignupAction.php (verified at line ~132-138 per RESEARCH.md — dispatches UserInstalled after DB commit):

```
final class SignupAction {
    public function __invoke(array $payload): User {
        // ... create user inside transaction
        // after commit:
        $this->events->dispatch(new UserInstalled($user->id));
        return $user;
    }
}
```

From Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal.php (the modal hosted by the email step, not duplicated):

```
// dispatch open via: $this->dispatch('oauth-client-wizard:open', provider: 'gmail');
// modal listens via #[On('oauth-client-wizard:open')]
// success fires: 'oauth-client-wizard:saved' (or similar — verify by reading the file head)
```

From Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php (EXEMPT_ROUTE_PREFIXES — 3 entries today, this plan adds 'setup'):

```
private const EXEMPT_ROUTE_PREFIXES = ['desktop.setup', 'desktop.welcome', 'signup'];
// after this plan: add 'setup'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wizard state services + UserInstalled listener + middleware exemption + route + signup redirect</name>
  <files>Modules/Onboarding/Routes/web.php, Modules/Onboarding/Public/Services/WizardProgressQuery.php, Modules/Onboarding/Internal/Services/WizardStepRegistry.php, Modules/Onboarding/Internal/Services/WizardProgressInitializer.php, Modules/Onboarding/Internal/Services/ResumeStepResolver.php, Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall.php, Modules/Onboarding/Providers/OnboardingServiceProvider.php, Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php, Modules/Auth/Public/Actions/SignupAction.php, Modules/Onboarding/tests/Unit/WizardProgressInitializerTest.php, Modules/Onboarding/tests/Unit/ResumeStepResolverTest.php, Modules/Onboarding/tests/Feature/SignupRoutesToSetupTest.php</files>
  <read_first>
    - Modules/Categorization/Internal/Listeners/SeedDefaultCategoryTree.php FULL (24-line listener — exact mirror target per PATTERNS.md Shared Pattern C)
    - Modules/Categorization/Providers/CategorizationServiceProvider.php lines 1-90 (where $events->listen(UserInstalled::class, ...) is registered + view namespace + Livewire registrations)
    - Modules/Auth/Public/Actions/SignupAction.php FULL (verify the existing redirect target + dispatch site; the redirect change is small but load-bearing)
    - Modules/Core/Public/Events/UserInstalled.php (payload shape)
    - Modules/Categorization/Routes/web.php (route registration shape — find `Route::middleware([...])->group(...)` pattern)
    - Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php FULL (the existing EXEMPT_ROUTE_PREFIXES + the redirect logic — 1-line change adds 'setup'; per RESEARCH.md Pitfall 1)
    - Modules/Onboarding/Models/WizardProgress.php (Plan 01 artifact)
    - Modules/Categorization/Database/Seeders/DefaultCategoryTreeSeeder.php (idempotent seeder pattern — firstOrCreate / updateOrCreate keyed by unique cols)
    - Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php lines 71-95 (constructor DI + DatabaseManager + explicit where('user_id', ...) — Shared Pattern E)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 271-301 (InitializeWizardProgressOnInstall analog + Pitfall 1 fix narrative)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-RESEARCH.md Open Question #2 (resume detector branches: in_progress → that step; only pending → first pending; all done|skipped → redirect to /)
    - tests/Pest.php (RefreshDatabase + module TestCase bootstrap)
    - Modules/Categorization/tests/Feature/RulesPageTest.php lines 1-40 (Livewire feature test pattern with actingAs + Livewire::test → assertSee → call → assertDispatched)
  </read_first>
  <behavior>
    - `WizardStepRegistry::steps(): list<string>` returns the canonical 6-step order: `['welcome', 'connect-bank', 'connect-card', 'connect-email', 'first-import', 'done']`.
    - `WizardProgressInitializer::initialize(int $userId): void` writes 6 `wizard_progress` rows via `firstOrCreate` (idempotent — re-fire is a no-op) — all status `pending`.
    - `InitializeWizardProgressOnInstall::handle(UserInstalled $event): void` delegates to the initializer.
    - `ResumeStepResolver::resolve(int $userId): string` returns the step_key of the first `in_progress` row, OR the first `pending` row in registry order, OR the sentinel `''` (empty string) meaning "all done/skipped → redirect to /".
    - `WizardProgressQuery::list(int $userId): array` returns the user's 6 wizard_progress rows keyed by step_key, with status + completed_at — used by SetupWizard's `mount()` to render the progress dots.
    - `Modules/Onboarding/Routes/web.php` registers `Route::get('/setup', SetupWizard::class)->middleware(['web', 'auth'])->name('setup')`. The route NAME `'setup'` is critical — EnsureDatabaseReady's exempt list uses prefix `'setup'` so any setup-namespaced route inherits the exemption.
    - `EnsureDatabaseReady::EXEMPT_ROUTE_PREFIXES` array is extended to `['desktop.setup', 'desktop.welcome', 'signup', 'setup']` — a literal 1-line edit per RESEARCH.md Pitfall 1.
    - `SignupAction::__invoke()` continues to dispatch `UserInstalled` after commit (existing behavior) AND now ALSO returns a result that the calling Livewire signup component uses to redirect to `route('setup')` instead of `/` — the redirect change happens at the Livewire-caller layer, NOT inside the Action itself. Verify by reading the existing signup component to see where the redirect lives; the change is at that caller's `submit()` method, not inside SignupAction.
    - All three new tests pass: WizardProgressInitializerTest (idempotent re-fire creates exactly 6 rows the first call AND still 6 rows on the second call), ResumeStepResolverTest (3 branches per Open Question #2), SignupRoutesToSetupTest (POST signup → 302 to /setup).
  </behavior>
  <action>
**Service `Modules/Onboarding/Internal/Services/WizardStepRegistry.php`** — `final class WizardStepRegistry { public function steps(): array { return ['welcome', 'connect-bank', 'connect-card', 'connect-email', 'first-import', 'done']; } public function isSkippable(string $stepKey): bool { return in_array($stepKey, ['connect-bank', 'connect-card', 'connect-email'], true); } }`. Constructor empty. PHPDoc describes the registry contract (current-state only per rule C-3).

**Service `Modules/Onboarding/Internal/Services/WizardProgressInitializer.php`** — `final class WizardProgressInitializer`. Constructor DI: `private readonly WizardStepRegistry $registry, private readonly \Illuminate\Database\DatabaseManager $db`. Method `initialize(int $userId): void` runs inside `$this->db->connection()->transaction(function () use ($userId) { foreach ($this->registry->steps() as $step) { $this->db->connection()->table('wizard_progress')->updateOrInsert(['user_id' => $userId, 'step_key' => $step], ['status' => 'pending', 'updated_at' => now()]); } })`. Idempotent via the UNIQUE(user_id, step_key) constraint + updateOrInsert. PHPDoc describes the upsert behavior.

**Service `Modules/Onboarding/Internal/Services/ResumeStepResolver.php`** — `final class ResumeStepResolver`. Constructor DI: `private readonly WizardStepRegistry $registry, private readonly \Illuminate\Database\DatabaseManager $db`. Method `resolve(int $userId): string`: query wizard_progress for the user; if any row has status=`in_progress` return that step_key; else iterate the registry's step order and return the first step whose row status=`pending`; else return `''` (sentinel — all done/skipped).

**Service `Modules/Onboarding/Public/Services/WizardProgressQuery.php`** — `final class WizardProgressQuery`. Constructor DI: `private readonly \Illuminate\Database\DatabaseManager $db, private readonly WizardStepRegistry $registry`. Method `list(int $userId): array` returns `array<string, array{status: string, completed_at: ?string}>` keyed by step_key, in registry order (so the progress-dots render is deterministic). Always returns 6 entries — missing rows render as `['status' => 'pending', 'completed_at' => null]` (defensive).

**Listener `Modules/Onboarding/Internal/Listeners/InitializeWizardProgressOnInstall.php`** — mirror `SeedDefaultCategoryTree` exactly:
```
final class InitializeWizardProgressOnInstall {
    public function __construct(private readonly WizardProgressInitializer $initializer) {}
    public function handle(\Modules\Core\Public\Events\UserInstalled $event): void {
        $this->initializer->initialize($event->userId);
    }
}
```

**Modify `Modules/Onboarding/Providers/OnboardingServiceProvider.php`** — `register()` adds singleton bindings for `WizardStepRegistry`, `WizardProgressInitializer`, `ResumeStepResolver`, `WizardProgressQuery`. `boot(Dispatcher $events)` adds `$events->listen(\Modules\Core\Public\Events\UserInstalled::class, InitializeWizardProgressOnInstall::class)`. Routes: `$this->loadRoutesFrom(__DIR__.'/../Routes/web.php')`. View namespace: already 'onboarding' (set in Plan 01 task 1).

**Route `Modules/Onboarding/Routes/web.php`** — `<?php declare(strict_types=1); use Illuminate\Support\Facades\Route; use Modules\Onboarding\Internal\Http\Livewire\SetupWizard; Route::middleware(['web', 'auth'])->group(function () { Route::get('/setup', SetupWizard::class)->name('setup'); });`. Tasks 2 & 3 add the SetupWizard component. CRITICAL: the route NAME is exactly `'setup'` so EnsureDatabaseReady's prefix match catches it.

**Modify `Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php`** — 1-line change. Append `'setup'` to the `EXEMPT_ROUTE_PREFIXES` array. Per RESEARCH.md Pitfall 1 — without this the user signs up, hits /, gets redirected back to /welcome, falls back to /signup → infinite loop. Add a docblock line above the array entry: "Setup wizard surface gates its own first-launch UX (the user is already past welcome)."

**Modify `Modules/Auth/Public/Actions/SignupAction.php`** — read the file FIRST. If the action itself does the redirect (returns a RedirectResponse) change the redirect target to `route('setup')` via the injected UrlGenerator. If the redirect lives in the calling Livewire signup component (more likely per Laravel/Livewire patterns), modify that component's submit() method instead — find it via `grep -rn "SignupAction" Modules/Auth/`. The change is small: replace the dashboard route name with `'setup'`. ALSO inject a feature/runtime guard so that future automated tests still redirect to /setup correctly — DO NOT introduce a config flag; the route is unconditional (every fresh signup goes through the wizard).

**Test `Modules/Onboarding/tests/Unit/WizardProgressInitializerTest.php`** — Pest unit, RefreshDatabase. Create a user; resolve `WizardProgressInitializer` via `app()`; call `initialize($user->id)` → assert `DB::table('wizard_progress')->where('user_id', $user->id)->count() === 6` AND every row has status='pending'; call `initialize($user->id)` AGAIN → assert still 6 rows (idempotent). Minimum 4 assertions.

**Test `Modules/Onboarding/tests/Unit/ResumeStepResolverTest.php`** — Pest unit, RefreshDatabase. Create user + initialize. Per Open Question #2 three branches:
1. Set one row to status='in_progress' → `resolve($user->id)` returns that step_key.
2. Set all rows to status='pending' → returns 'welcome' (first in registry).
3. Set every row to 'done' or 'skipped' → returns '' (sentinel).
Minimum 3 assertions across the 3 branches.

**Test `Modules/Onboarding/tests/Feature/SignupRoutesToSetupTest.php`** — Pest feature, RefreshDatabase. Render the signup Livewire component (find via `Modules/Auth/Internal/Http/Livewire/`), fill in valid credentials, submit. Assert: `assertRedirect('/setup')` OR `assertRedirectToRoute('setup')`. Also assert post-signup: the user exists, 6 wizard_progress rows exist for that user. Minimum 3 assertions.

Per project rule C-1: every service constructor-DIs collaborators (no facades). Per C-2: no GSD leakage. Per C-3: PHPDoc current-state only. Per C-4: fix every Larastan/Pint severity surfaced.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/Unit/WizardProgressInitializerTest.php Modules/Onboarding/tests/Unit/ResumeStepResolverTest.php Modules/Onboarding/tests/Feature/SignupRoutesToSetupTest.php --no-coverage; composer analyse; composer format:check</automated>
  </verify>
  <done>
    Three new tests pass with ≥10 assertions total; UserInstalled listener wires correctly (signup creates exactly 6 wizard_progress rows); /setup route exists and is exempt from EnsureDatabaseReady; post-signup HTTP redirect lands on /setup; Larastan + Pint green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SetupWizard parent Livewire component + 6 step Livewire components + skip / skip-the-rest / force-reset + UI-SPEC blade views + CSS components</name>
  <files>Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php, Modules/Onboarding/Internal/Http/Livewire/Steps/WelcomeStep.php, Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectBankStep.php, Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectCardStep.php, Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectEmailStep.php, Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php, Modules/Onboarding/Internal/Http/Livewire/Steps/DoneStep.php, Modules/Onboarding/Resources/views/layouts/app-wizard.blade.php, Modules/Onboarding/Resources/views/livewire/setup-wizard.blade.php, Modules/Onboarding/Resources/views/livewire/steps/welcome-step.blade.php, Modules/Onboarding/Resources/views/livewire/steps/connect-bank-step.blade.php, Modules/Onboarding/Resources/views/livewire/steps/connect-card-step.blade.php, Modules/Onboarding/Resources/views/livewire/steps/connect-email-step.blade.php, Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php, Modules/Onboarding/Resources/views/livewire/steps/done-step.blade.php, Modules/Onboarding/Resources/views/components/wiz-card.blade.php, Modules/Onboarding/Resources/views/components/wiz-dots.blade.php, Modules/Onboarding/Resources/views/components/wiz-actions.blade.php, Modules/Onboarding/Resources/views/components/vd-glyph.blade.php, Modules/Onboarding/Resources/views/components/mini-step.blade.php, Modules/Onboarding/Resources/views/components/format-chip.blade.php, Modules/Onboarding/Resources/views/components/drop-zone.blade.php, Modules/Onboarding/Resources/views/components/privacy-pill.blade.php, Modules/Onboarding/Providers/OnboardingServiceProvider.php, resources/css/app.css</files>
  <read_first>
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-UI-SPEC.md FULL (Wizard chrome lines 180-194, Welcome step lines 195-210, ASN connector lines 211-225, ICS connector lines 226-242, Email step lines 243-256, Final step lines 257-267, Component Inventory lines 437-466, Color lines 113-165, Spacing lines 37-74, Density rules lines 53-65)
    - .claude/skills/sketch-findings-diederik/references/onboarding-wizard.md FULL (sketches 002 + 003 visual lock)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md lines 425-510 (SetupWizard + ConnectBankStep + ConnectEmailStep analog cites)
    - Modules/DevMode/Internal/Http/Livewire/AuditLogPage.php lines 28-60 (#[Layout] attribute + method-DI on render() — exact pattern for SetupWizard's parent)
    - Modules/DevMode/Internal/Http/Livewire/DevOverviewPage.php (info-only Livewire — pattern for WelcomeStep + DoneStep)
    - Modules/Import/Internal/Http/Livewire/UploadWizard.php lines 39-272 (WithFileUploads + Validate + submit($importer, $currentUser, ...) — pattern for ConnectBankStep + ConnectCardStep; FirstImportStep mounts UploadWizard inline)
    - Modules/EmailScan/Internal/Http/Livewire/OAuthClientWizardModal.php FULL (the modal mounted by ConnectEmailStep — find the event names it dispatches/listens to)
    - Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php (existing banner pattern for the "Welcome back" notice)
    - resources/css/app.css FULL — find @theme block, @layer components block; the new component classes (.wiz-page, .wiz-card, .wiz-dots, .wiz-actions, .wiz-footer, .privacy-pill, .vd-glyph, .mini, .format-chip, .drop-zone) land in @layer components using EXISTING tokens (no new theme tokens per UI-SPEC §Spacing and §Color)
    - Modules/DevMode/Resources/views/components/run-card.blade.php (atomic Blade component shape with slot + props)
    - resources/views/layouts/app.blade.php (sidebar-bearing layout; app-wizard.blade.php is a SIBLING — body-only, no sidebar)
    - Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php (Flux modal SFC pattern — referenced by ConnectEmailStep which dispatches modal-show)
    - Modules/Core/Public/Contracts/CurrentUser.php
  </read_first>
  <behavior>
    - Navigating to `/setup` (with an authenticated user) renders the wizard using the `onboarding::layouts.app-wizard` layout (body-only, no sidebar).
    - `SetupWizard::mount(ResumeStepResolver, CurrentUser)` reads the resume step; if the resolver returns `''` (all done/skipped) the component redirects to `/` immediately.
    - SetupWizard tracks `string $currentStepKey` as the active step; renders the active step's child Livewire component inline (`<livewire:onboarding.steps.welcome-step :key="welcome-step" />` switch via blade `@switch($currentStepKey)`).
    - SetupWizard exposes 3 methods (all method-DI): `next(DatabaseManager $db)` marks current step `done` + advances; `skip(DatabaseManager $db)` marks current step `skipped` + advances (only valid on skippable steps from the registry); `skipRest(DatabaseManager $db, UrlGenerator $urls)` marks every NON-done step `skipped` + redirects to `/`.
    - SetupWizard has `mount(?bool $force = null)` — when the request carries `?force=1`, the mount runs an "reset all rows to pending" UPDATE, removes any `completed_at`, then re-enters at the welcome step. Per UI-SPEC §"Settings → First-run tour": `force=1` is how the re-run-from-Settings link works.
    - SetupWizard renders the "Welcome back — let's pick up where you left off" banner conditionally on `currentStepKey !== 'welcome'` AND the page was just mounted (i.e. the user is resuming). The banner has a quiet "Skip the rest" dismiss button bound to `skipRest()`.
    - Each step component:
      - WelcomeStep: static content from UI-SPEC §Wizard welcome step + 3 glyph rows; primary CTA "Continue →" calls parent's `next()` via Livewire dispatch.
      - ConnectBankStep / ConnectCardStep: WithFileUploads + Validate + submit() that calls `RunsImports::runFromUpload(...)` (DI'd) — on success advances the wizard step; on failure shows the UI-SPEC error copy.
      - ConnectEmailStep: hosts `<livewire:email-scan.o-auth-client-wizard-modal />` once; renders 2 buttons ("Authorize with Gmail" / "Authorize with Outlook") that dispatch `oauth-client-wizard:open` with the provider; listens via `#[On('oauth-client-wizard:saved')]` (or the actual event name from OAuthClientWizardModal — read the file to find) and marks the email step `done` + advances. Also exposes a "Skip — set up later in Settings" secondary that marks the step `skipped`.
      - FirstImportStep: mounts the existing `<livewire:import.upload-wizard />` inline at 1120px container width (single exception to the 620px rule per UI-SPEC §"Density rules"). The wizard advances when UploadWizard fires its existing "preview ready" event (find the event name in UploadWizard.php).
      - DoneStep: static UI-SPEC §Wizard final step content; "Open dashboard →" CTA redirects to `/` AND fires `WizardCompleted($userId)` event via DI'd Dispatcher.
    - The chip/popover/pill geometry exceptions from UI-SPEC §"Spacing density rules" apply only to chip/popover/pill components — never to wizard card / layout containers.
  </behavior>
  <action>
**Parent `Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php`** — mirror AuditLogPage + RESEARCH.md/PATTERNS.md guidance:

```
#[Layout('onboarding::layouts.app-wizard')]
final class SetupWizard extends Component {
    public string $currentStepKey = 'welcome';
    /** @var array<string, array{status: string, completed_at: ?string}> */
    public array $progress = [];
    public bool $isResuming = false;
    public bool $allComplete = false;

    public function mount(
        ResumeStepResolver $resolver,
        WizardProgressQuery $query,
        WizardProgressInitializer $initializer,
        \Modules\Core\Public\Contracts\CurrentUser $currentUser,
        \Illuminate\Database\DatabaseManager $db,
        \Illuminate\Contracts\Routing\UrlGenerator $urls,
        \Illuminate\Http\Request $request,
    ): mixed {
        $user = $currentUser->user();
        // Idempotent safety net — if the listener missed (test runs, edge cases), seed now
        $initializer->initialize($user->id);

        if ($request->boolean('force')) {
            $db->connection()->table('wizard_progress')
                ->where('user_id', $user->id)
                ->update(['status' => 'pending', 'completed_at' => null, 'updated_at' => now()]);
        }

        $this->progress = $query->list($user->id);
        $resumeKey = $resolver->resolve($user->id);
        if ($resumeKey === '') {
            return redirect()->to('/');
        }
        $this->isResuming = $resumeKey !== 'welcome' && ! $request->boolean('force');
        $this->currentStepKey = $resumeKey;
        return null;
    }

    public function goToStep(string $stepKey): void { /* validate stepKey is in registry; set $this->currentStepKey */ }

    #[\Livewire\Attributes\On('wizard.step.completed')]
    public function next(\Illuminate\Database\DatabaseManager $db, \Modules\Core\Public\Contracts\CurrentUser $currentUser, WizardStepRegistry $registry): void { /* mark current step done, advance to next */ }

    #[\Livewire\Attributes\On('wizard.step.skipped')]
    public function skip(\Illuminate\Database\DatabaseManager $db, \Modules\Core\Public\Contracts\CurrentUser $currentUser, WizardStepRegistry $registry): void { /* mark current step skipped, advance */ }

    public function skipRest(\Illuminate\Database\DatabaseManager $db, \Modules\Core\Public\Contracts\CurrentUser $currentUser, \Illuminate\Contracts\Routing\UrlGenerator $urls): \Illuminate\Http\RedirectResponse { /* mark every non-done row as skipped, redirect to / */ }

    public function render(\Illuminate\Contracts\View\Factory $views): View {
        return $views->make('onboarding::livewire.setup-wizard');
    }
}
```

Per project rule C-1: Livewire forbids constructor DI on Component subclasses — every public action method DIs its collaborators. Per rule C-2: no `.planning/` references.

**Step components — 6 of them.** Each is `final class XxxStep extends Component`. Each step's `render(ViewFactory $views): View` returns `$views->make('onboarding::livewire.steps.xxx-step')`. Step components dispatch `wizard.step.completed` (or `wizard.step.skipped`) to the parent — the parent's `#[On]` listeners handle advancement.

`WelcomeStep.php`: static. Single action `continue()` dispatches `wizard.step.completed`.

`ConnectBankStep.php`: `use WithFileUploads`. Properties: `?TemporaryUploadedFile $file = null`, `?string $uploadError = null`, `string $selectedFormat = 'asn-camt053'`. `rules()` returns the file rule (mirror UploadWizard's exact shape — extensions:csv,xml,sta,mt940,940 + max size). `submit(RunsImports $importer, CurrentUser $currentUser, LoggerInterface $logger)` mirrors UploadWizard's submit shape but on success dispatches `wizard.step.completed` (NOT a redirect — the wizard advances in-place).

`ConnectCardStep.php`: same shape as ConnectBankStep but locked to `extensions:pdf` + `max:10240` (per MEMORY `project_ics_portal_pdf_only`); only one format chip "PDF [only format]" muted per UI-SPEC.

`ConnectEmailStep.php`: NO WithFileUploads. Properties: `?string $authStartedFor = null`. Methods: `authorize(string $provider): void` → validate provider is 'gmail' or 'microsoft' → `$this->authStartedFor = $provider` → `$this->dispatch('oauth-client-wizard:open', provider: $provider)`. `#[On('oauth-client-wizard:saved')] public function onAuthSaved(): void` → `$this->dispatch('wizard.step.completed')`. `skip(): void` → `$this->dispatch('wizard.step.skipped')`. Blade mounts `<livewire:email-scan.o-auth-client-wizard-modal />` (or whatever the registered Livewire component name is — find by `grep -rn "->component(" Modules/EmailScan/Providers/` for the registration FQCN).

`FirstImportStep.php`: minimal Livewire shell. Blade mounts `<livewire:import.upload-wizard />` inside a 1120px-wide container (set via the wiz-card layout exception). The UploadWizard's existing event for "preview submitted successfully" is captured via `#[On(...)]` and dispatches `wizard.step.completed`. If UploadWizard doesn't currently fire a structured event on success (it redirects to imports.preview), this task adds a small adapter — DO NOT modify UploadWizard's submit() (it's used in many places); instead listen to UploadWizard's existing dispatched events or use a Livewire global dispatch sentinel.

`DoneStep.php`: static. Single action `finish(Dispatcher $events, CurrentUser $currentUser, UrlGenerator $urls): RedirectResponse` → `$events->dispatch(new WizardCompleted($currentUser->user()->id))` → `return redirect()->to('/')`.

**Layout `Modules/Onboarding/Resources/views/layouts/app-wizard.blade.php`** — body-only, no sidebar. Wraps `@yield('content')` (or `{{ $slot }}`) inside `<div class="wiz-page">` per UI-SPEC §"Wizard shell" component inventory. Includes the standard `<head>` (csrf token, viewport, app.css link, Livewire scripts, Alpine via Livewire bundle). The `wiz-top` brand row, `wiz-dots` progress, and `wiz-footer` privacy pill live inside this layout (NOT inside each step) so every step inherits the chrome.

**Blade `Modules/Onboarding/Resources/views/livewire/setup-wizard.blade.php`** — outer `<div>` (Livewire single root). Inside: progress dots (component `<x-onboarding::wiz-dots :progress="$progress" :current="$currentStepKey" />`), the "Welcome back" banner gated on `$isResuming`, then a `@switch($currentStepKey)` mounting the active step's Livewire component (`<livewire:onboarding.steps.welcome-step :key="welcome" />` etc — each step has a stable Livewire key so wire:transition works).

**Step blade views** (6 files) — each composes the wizard primitives via `<x-onboarding::wiz-card>...</x-onboarding::wiz-card>`. Render UI-SPEC copy verbatim (the §Copywriting Contract is the source of truth — every label and CTA matches). Example for welcome-step.blade.php:
```
<x-onboarding::wiz-card>
    <p class="wiz-eyebrow">Welcome</p>
    <h1 class="wiz-headline">Let's get diederik to know your money.</h1>
    <p class="wiz-lede">About 5 minutes. Nothing leaves this computer — everything you connect stays in a file on your machine.</p>
    <div class="vd-rows">
        <x-onboarding::vd-glyph glyph="🏦" title="Your bank (ASN)" description="Drop a statement file. We'll show you exactly where to find it." />
        <x-onboarding::vd-glyph glyph="💳" title="Your credit card (ICS)" description="Drop the monthly PDF statement from Mijn ICS." />
        <x-onboarding::vd-glyph glyph="✉️" title="Receipts from email" description="Connect Gmail or Outlook to capture purchase confirmations automatically." optional="true" />
    </div>
    <x-onboarding::wiz-actions>
        <flux:button wire:click="continue" variant="primary">Continue →</flux:button>
    </x-onboarding::wiz-actions>
</x-onboarding::wiz-card>
```

**Blade components** (8 files) under `Modules/Onboarding/Resources/views/components/` — each is a small Blade Component (props + slot). `wiz-card.blade.php`: `<div class="wiz-card">{{ $slot }}</div>`. `wiz-dots`: takes `$progress` + `$current`, renders 6 dots/rect per UI-SPEC §"Wizard chrome" (8px dot for upcoming/done [emerald], 24px rounded rect for current). `wiz-actions`: right-aligned flex of slot children. `vd-glyph`: 44×44 tile + title + description rows. `mini-step`: tile for connector-step mini-step grid. `format-chip`: chip with optional "[recommended]" badge. `drop-zone`: dashed border + glyph + lead + sublink + Livewire file-input wiring. `privacy-pill`: "● Your data stays on this computer".

**Modify `resources/css/app.css` — `@layer components`** — ADD (do not modify existing rules):
- `.wiz-page { min-height: calc(100vh - 48px); display: flex; flex-direction: column; background: var(--color-bg-subtle); }`
- `.wiz-top { display: flex; align-items: center; justify-content: space-between; padding: 16px 24px; }`
- `.wiz-body { flex: 1; display: grid; place-items: center; padding: 24px; }`
- `.wiz-card { max-width: 620px; width: 100%; background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-xl); padding: 44px 48px 32px; box-shadow: var(--shadow-md); }`
- `.wiz-card--wide { max-width: 1120px; padding: 32px; }` (the first-import-step exception)
- `.wiz-dots { display: flex; gap: 8px; align-items: center; }`
- `.wiz-dots .dot { width: 8px; height: 8px; border-radius: 999px; background: var(--color-border); }`
- `.wiz-dots .dot.done { background: var(--color-emerald, #059669); }`
- `.wiz-dots .dot.now { width: 24px; height: 8px; border-radius: 4px; background: var(--color-text); }`
- `.wiz-actions { display: flex; justify-content: flex-end; gap: 8px; margin-top: 24px; }`
- `.wiz-footer { display: flex; align-items: center; justify-content: space-between; padding: 16px 24px; color: var(--color-text-muted); font-size: var(--text-xs); }`
- `.privacy-pill { display: inline-flex; align-items: center; gap: 4px; }`
- `.privacy-pill::before { content: ''; width: 6px; height: 6px; border-radius: 999px; background: var(--color-emerald, #059669); display: inline-block; }`
- `.vd-rows { display: flex; flex-direction: column; gap: 16px; margin-top: 24px; }`
- `.vd-glyph { display: flex; align-items: center; gap: 12px; }`
- `.vd-glyph .tile { width: 44px; height: 44px; display: grid; place-items: center; background: var(--color-bg-subtle); border-radius: var(--radius-md); font-size: 26px; }`
- `.mini-steps { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; }`
- `.mini { padding: 14px 12px; border-radius: var(--radius-md); display: flex; flex-direction: column; gap: 4px; }`
- `.mini.done { background: var(--color-emerald-bg, rgba(16,185,129,0.10)); }`
- `.mini.now { background: var(--color-surface-2); color: var(--color-text); }`
- `.format-chips { display: flex; align-items: center; gap: 8px; }`
- `.format-chip { padding: 4px 10px; border-radius: 999px; background: var(--color-surface-2); border: 1px solid var(--color-border); font-size: var(--text-xs); }`
- `.format-chip.recommended { background: var(--color-emerald-bg); color: var(--color-emerald, #059669); }`
- `.drop-zone { padding: 28px 20px; border: 1px dashed var(--color-border-strong); border-radius: var(--radius-md); display: flex; flex-direction: column; align-items: center; gap: 8px; }`
- `.drop-zone:hover { background: var(--color-bg-subtle); }`

Per UI-SPEC §"Design System" token-source: NONE of these new classes introduces a new theme token; all reference existing CSS variables from `@theme`. If `--color-emerald` or `--color-emerald-bg` is not exported, use literal hex per UI-SPEC §"Color" sketch-locked exceptions (the inlined values are explicitly permitted).

**Modify `Modules/Onboarding/Providers/OnboardingServiceProvider.php`** — `boot(LivewireManager $livewire)` adds Livewire component registrations:
- `$livewire->component('onboarding.setup-wizard', SetupWizard::class);`
- `$livewire->component('onboarding.steps.welcome-step', WelcomeStep::class);`
- `$livewire->component('onboarding.steps.connect-bank-step', ConnectBankStep::class);`
- `$livewire->component('onboarding.steps.connect-card-step', ConnectCardStep::class);`
- `$livewire->component('onboarding.steps.connect-email-step', ConnectEmailStep::class);`
- `$livewire->component('onboarding.steps.first-import-step', FirstImportStep::class);`
- `$livewire->component('onboarding.steps.done-step', DoneStep::class);`

Per rule C-1: all DI through constructors (services) or method-DI (Livewire). Per rule C-2/C-3: no GSD leakage, no history docs. Per rule C-4: fix all severities.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/Unit --no-coverage; vendor/bin/pest Modules/Onboarding/tests/Feature/SignupRoutesToSetupTest.php --no-coverage; composer analyse; composer format:check; php artisan view:clear</automated>
  </verify>
  <done>
    SetupWizard parent + 6 step components exist + render their UI-SPEC copy; the `app-wizard` layout exists; 8 Blade components live under Resources/views/components/; resources/css/app.css has the new component classes inside `@layer components` using existing tokens; all Livewire components registered in the service provider; Larastan + Pint green; existing tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WIZ-02 through WIZ-06 acceptance tests + verify all wizard behaviors end-to-end</name>
  <files>Modules/Onboarding/tests/Feature/ResumeWizardTest.php, Modules/Onboarding/tests/Feature/SkipWizardStepTest.php, Modules/Onboarding/tests/Feature/EmailStepEmbedsOAuthModalTest.php, Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php, Modules/Onboarding/tests/Feature/ReRunWizardTest.php</files>
  <read_first>
    - Modules/Categorization/tests/Feature/RulesPageTest.php lines 1-90 (Pest Livewire test pattern with beforeEach + actingAs + Livewire::test → assertSee → call → assertDispatched)
    - Modules/EmailScan/tests/Feature/InboxesPageTest.php OR similar (existing OAuth modal-dispatch assertion pattern)
    - Modules/Onboarding/Internal/Http/Livewire/SetupWizard.php (Task 2 output — read to confirm public properties and dispatched events for assertions)
    - Modules/Onboarding/Internal/Http/Livewire/Steps/*.php (verify the event names used)
    - .planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-PATTERNS.md Shared Pattern G (Livewire feature test shape)
    - tests/Pest.php (RefreshDatabase + module TestCase bootstrap)
  </read_first>
  <behavior>
    - ResumeWizardTest: a user with wizard_progress rows where `connect-card` is `in_progress` mounts SetupWizard → `currentStepKey === 'connect-card'` AND `isResuming === true` AND the rendered output contains "Welcome back" (or the actual UI-SPEC copy line).
    - SkipWizardStepTest: a user on `connect-bank` calls the step's `skip()` → wizard advances to `connect-card`; the `connect-bank` wizard_progress row has status='skipped'. Also test `skipRest()` from `connect-bank` marks the 3 connector steps + first-import step as skipped + redirects to /. Also test skip is rejected on non-skippable steps (welcome, done).
    - EmailStepEmbedsOAuthModalTest: mounting the ConnectEmailStep + calling `authorize('gmail')` dispatches `oauth-client-wizard:open` with `provider: 'gmail'`; receiving the `oauth-client-wizard:saved` event marks the email row `done` + dispatches `wizard.step.completed`.
    - FirstImportStepWidthTest: rendering FirstImportStep asserts the HTML contains the wiz-card--wide class (the 1120px exception); rendering ConnectBankStep asserts it does NOT contain wiz-card--wide.
    - ReRunWizardTest: a user with all 6 wizard_progress rows = 'done' loads `/setup` → SetupWizard's mount() returns a redirect to /. Then loads `/setup?force=1` → every row is reset to 'pending' AND `currentStepKey === 'welcome'`. The "Settings → First-run tour" link target route is `route('setup', ['force' => 1])` — verify the route accepts the query param.
  </behavior>
  <action>
**Test `Modules/Onboarding/tests/Feature/ResumeWizardTest.php`** — Pest feature. `beforeEach`: create user, actingAs, initialize wizard, manually set `wizard_progress` rows so `welcome`=`done`, `connect-bank`=`done`, `connect-card`=`in_progress`, rest `pending`. Render `Livewire::test(SetupWizard::class)`. Assert: `->assertSet('currentStepKey', 'connect-card')`, `->assertSet('isResuming', true)`, `->assertSee('Welcome back')` (or the exact UI-SPEC copy used by the banner — read setup-wizard.blade.php from Task 2 to find the exact string). Minimum 3 assertions.

**Test `Modules/Onboarding/tests/Feature/SkipWizardStepTest.php`** — Pest feature. Three cases:
1. User on `connect-bank`. `Livewire::test(SetupWizard::class)->set('currentStepKey', 'connect-bank')->call('skip')`. Assert: currentStepKey advanced to `'connect-card'`; `DB::table('wizard_progress')->where('user_id', $user->id)->where('step_key', 'connect-bank')->value('status') === 'skipped'`.
2. User on `connect-bank` calls `skipRest`. Assert: redirect to /; every wizard_progress row for the user NOT in {welcome, connect-bank} that was previously 'pending' is now 'skipped' (welcome stays whatever it was, connect-bank stays whatever it was).
3. User on `welcome` calls `skip()`. Assert: nothing happens (skip is no-op on non-skippable steps); welcome stays the current step. (If the implementation throws instead of no-op, assert the exception type — confirm by reading the SetupWizard impl.)
Minimum 5 assertions across the 3 cases.

**Test `Modules/Onboarding/tests/Feature/EmailStepEmbedsOAuthModalTest.php`** — Pest feature. `Livewire::test(ConnectEmailStep::class)`. Assert the rendered HTML contains `<livewire:email-scan.o-auth-client-wizard-modal` (the modal is mounted). Call `->call('authorize', 'gmail')`. Assert `->assertDispatched('oauth-client-wizard:open', provider: 'gmail')`. Then simulate the modal saving: `->dispatch('oauth-client-wizard:saved')` AND assert `->assertDispatched('wizard.step.completed')`. Minimum 3 assertions.

**Test `Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php`** — Pest feature. Render `Livewire::test(FirstImportStep::class)` and assert the HTML body contains the CSS class `wiz-card--wide` (or the exact class name introduced in Task 2). Render `Livewire::test(ConnectBankStep::class)` and assert it does NOT contain `wiz-card--wide`. Minimum 2 assertions.

**Test `Modules/Onboarding/tests/Feature/ReRunWizardTest.php`** — Pest feature. `beforeEach`: create user, initialize wizard, manually set every wizard_progress row to status='done'.
Case 1: GET `/setup` (no force) → expect a 302 redirect to `/`. (Test via the HTTP test client `$this->get('/setup')->assertRedirect('/')`.)
Case 2: GET `/setup?force=1` → expect a 200 with the wizard rendered AND every wizard_progress row reset to 'pending'. Test: `$this->get('/setup?force=1')->assertOk()->assertSee('Let\'s get diederik to know your money')` (the welcome H1); then assert `DB::table('wizard_progress')->where('user_id', $user->id)->where('status', '!=', 'pending')->count() === 0`.
Minimum 4 assertions across the 2 cases.

After writing: run the full module test suite + Larastan + Pint. Per rule C-4: any failure / warning gets fixed.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/ --no-coverage; composer analyse; composer format:check</automated>
  </verify>
  <done>
    All 6 WIZ-* acceptance tests pass with ≥ 21 assertions total; `vendor/bin/pest Modules/Onboarding/tests/` is fully green; Larastan + Pint clean; the wizard is end-to-end usable (sign up → land on /setup → walk through steps → finish → land on dashboard; mid-flow exit + relaunch resumes; Settings re-run via ?force=1 works).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request ↔ /setup route | Authenticated user request; the route is auth-gated; cross-user access blocked by per-user wizard_progress reads |
| Wizard step user input ↔ wizard_progress writes | step_key + status writes are bounded to the registry's 6 keys + 4 statuses; trigger pair from Plan 01 enforces status allow-list |
| ConnectBankStep / ConnectCardStep file upload | Untrusted file content reaches the existing UploadWizard pipeline; this plan adds NO new file-upload surface — reuses Modules/Import/Public/Contracts/RunsImports |
| ConnectEmailStep OAuth handoff | The step ONLY dispatches the open event; the existing OAuthClientWizardModal handles OAuth itself — no new OAuth surface introduced here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1-03-01 | Tampering | Cross-user wizard_progress access via Livewire property tampering | mitigate | SetupWizard's progress property is read via WizardProgressQuery which always filters by current user (Shared Pattern E); mount() rebuilds the array on every render so a client tamper is overwritten |
| T-16.1-03-02 | Spoofing | Direct URL navigation skipping ahead (e.g. /setup with someone else's state) | mitigate | The route is auth-gated; wizard_progress rows are per-user via FK to users; the resolver returns ''-sentinel for users with no rows so a non-initialized user lands on / |
| T-16.1-03-03 | Tampering | currentStepKey property tampering — client sets it to 'done' to skip ahead | mitigate | The goToStep() method validates the step_key is in the registry AND requires every PRIOR step to be done|skipped; tampering to a future step without prior progress fails closed |
| T-16.1-03-04 | Information Disclosure | Wizard mount leaks user data via the rendered banner | accept | The banner copy is static ("Welcome back — let's pick up where you left off"); no PII rendered |
| T-16.1-03-05 | Denial of Service | Repeated /setup?force=1 resets are abusive | accept | force=1 writes to the user's own 6 rows only — bounded; no amplification |
| T-16.1-03-06 | Elevation of Privilege | Skip-the-rest skipping required onboarding gate | accept | There is no required gate after the wizard; skipping is by design (D-02 / D-04); the user reaches a working dashboard with no data and can return via Settings |
| T-16.1-03-CSRF | Tampering | Cross-site request forgery on the Livewire POST endpoints | mitigate | Livewire's POST endpoint is web-middleware-protected and ships CSRF tokens out of the box; the existing global middleware stack covers /setup |
</threat_model>

<verification>
- `vendor/bin/pest Modules/Onboarding/tests/` is fully green with ≥ 31 assertions across 8 test files (3 from Task 1, 5 from Task 3).
- `composer analyse` + `composer format:check` are green.
- Manual smoke: open a clean SQLite db, sign up a user, observe redirect to /setup, walk through 6 steps; kill PHP, restart, navigate to /setup, observe "Welcome back" banner; click "Skip the rest", observe redirect to /; navigate to /setup?force=1, observe wizard reset.
- `EnsureDatabaseReady::EXEMPT_ROUTE_PREFIXES` contains `'setup'`; `grep -n "'setup'" Modules/Desktop/Internal/Http/Middleware/EnsureDatabaseReady.php` shows the entry.
- Every UI-SPEC §"Wizard chrome" / §"Wizard welcome step" / §"Wizard connector step" copy string is present in the rendered blades (manual or grep verification).
</verification>

<success_criteria>
- 6 WIZ-* requirements (WIZ-01 through WIZ-06) all green per their tests.
- Onboarding module hosts the existing OAuthClientWizardModal (email step) and existing UploadWizard (first-import step) — zero duplication.
- The "Settings → First-run tour" link target route is documented as `route('setup', ['force' => 1])` (a follow-up plan or the existing Settings page can wire the link).
- `wizard_progress` rows are 100% per-user-scoped (BelongsToUser global scope active on the model + explicit where clauses in services).
- Calm-slate UI-SPEC contract respected — no new theme tokens introduced in `app.css`'s `@theme` block; only `@layer components` extensions using existing variables.
- Larastan L10 strict + Pint clean (rule C-4 fix-every-severity).
- Zero `.planning/` / D-NN / PLAN.md leakage in production source (rule C-2).
</success_criteria>

<output>
Create `.planning/phases/16.1-zero-friction-first-run-setup-wizard-manual-transaction-rena/16.1-03-SUMMARY.md` when done.
</output>
