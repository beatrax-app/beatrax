---
phase: 07-tax-deductible-tagging-per-year-export
plan: 06
type: execute
wave: 5
depends_on: [07-05]
files_modified:
  - Modules/Tax/Resources/views/components/tax-badge.blade.php
  - Modules/Tax/Resources/views/components/tax-tag-popover.blade.php
  - Modules/Tax/Internal/Http/Livewire/Concerns/HandlesTaxTagging.php
  - Modules/Ledger/Internal/Http/Livewire/TransactionsList.php
  - Modules/Ledger/Resources/views/livewire/transactions-list.blade.php
  - Modules/Ledger/Internal/Http/Livewire/TransactionDetail.php
  - Modules/Ledger/Resources/views/livewire/transaction-detail.blade.php
  - Modules/Counterparties/Internal/Http/Livewire/CounterpartyProfile.php
  - Modules/Counterparties/Resources/views/livewire/counterparty-profile.blade.php
  - Modules/CashBook/Internal/Http/Livewire/CashBookPage.php
  - Modules/CashBook/Resources/views/livewire/cash-book-page.blade.php
  - Modules/Tax/tests/Feature/TaxBadgeSurfacesTest.php
autonomous: false
requirements: [TAX-01]

must_haves:
  truths:
    - "An untagged transaction row reveals a calm Tag action (hover on desktop, always-visible on touch) on all four surfaces"
    - "Tagging is one tap; a category picker popover (desktop) / bottom sheet (phone) then offers the optional deduction category"
    - "A tagged row shows the emerald tax badge with the category short name on all four surfaces"
    - "Tagging the same counterparty's row offers Also-tag-N-more when ≥2 untagged remain in the tax year"
    - "Tagged state is batch-loaded per page (no N+1)"
  artifacts:
    - path: "Modules/Tax/Resources/views/components/tax-badge.blade.php"
      provides: "Shared x-tax::tax-badge rendered on four surfaces"
      contains: "tax-badge"
    - path: "Modules/Tax/Internal/Http/Livewire/Concerns/HandlesTaxTagging.php"
      provides: "Reusable Livewire trait: tag/untag/category/batch actions + #[On] listeners"
      contains: "HandlesTaxTagging"
  key_links:
    - from: "Modules/Tax/Internal/Http/Livewire/Concerns/HandlesTaxTagging.php"
      to: "TagTransaction + UntagTransaction + TaxTagQuery"
      via: "delegate from each surface component"
      pattern: "TagTransaction"
    - from: "Modules/Ledger/Resources/views/livewire/transactions-list.blade.php"
      to: "x-tax::tax-badge"
      via: "row partial"
      pattern: "x-tax::tax-badge"
---

<objective>
Ship the shared `x-tax::tax-badge` and category picker (popover on desktop,
bottom sheet on phone), the one-tap tag flow with optional category + note, the
batch-tag suggestion banner, and integrate them on all FOUR transaction-row
surfaces (transactions list, transaction detail, counterparty profile
transactions tab, cash book). A reusable `HandlesTaxTagging` trait gives each
surface the tag/untag/category/batch actions without duplication.

Purpose: TAX-01 made "always at hand" — tagging works wherever a transaction
row renders (D-19/D-20), with the calm badge as the at-a-glance indicator.
Output: Tagging + badge live on four surfaces, batch-loaded, phone-parity.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-CONTEXT.md
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-RESEARCH.md
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-PATTERNS.md
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-UI-SPEC.md

<interfaces>
<!-- Consumes Plans 01/02 (actions, queries, DTOs) and Plan 05 (.tax-badge CSS). -->
TagTransaction::execute(userId, transactionId, ?categoryId, ?note, ?yearOverride): void
UntagTransaction::execute(userId, transactionId): void
TaxTagQuery::forTransactionIds(userId, array $ids): array<int,TaxTagData>   — batch badge state
TaxTagQuery::untaggedCountForCounterparty(userId, txId, taxYear): BatchTagSuggestion
TaxCategoryWriter::listForUser(userId): array   — picker category list
TaxCategoryWriter::add(userId, name, ?short, ?hint): int   — inline "new category…"

Badge events (dispatched by x-tax::tax-badge, caught by each surface via #[On]):
  'tax-tag'      { id }   → tag + open picker
  'tax-edit-tag' { id }   → open picker for an already-tagged row
CSS classes (.tax-badge, .tax-badge--untagged, .tax-badge--amber) ship in Plan 05.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared badge + picker + HandlesTaxTagging trait</name>
  <files>Modules/Tax/Resources/views/components/tax-badge.blade.php, Modules/Tax/Resources/views/components/tax-tag-popover.blade.php, Modules/Tax/Internal/Http/Livewire/Concerns/HandlesTaxTagging.php</files>
  <read_first>
    - Modules/Community/Resources/views/components/mystery-card.blade.php (anonymous Blade component @props + @php extraction)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-PATTERNS.md (tax-badge dispatch-based wire:click excerpt lines 533–581; shared-pattern trait guidance)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-RESEARCH.md (Open Question 1 — use $dispatch + #[On] listeners, not direct nested-component method calls; Pitfall 7 batch double-count)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-UI-SPEC.md (Section 5 badge states; Section 6 picker popover/bottom-sheet; Section 7 batch banner; Section 15 interaction; Section 16 a11y)
    - .claude/skills/sketch-findings-beatrax/SKILL.md — invoke Skill("sketch-findings-beatrax")
  </read_first>
  <action>
Create `Modules/Tax/Resources/views/components/tax-badge.blade.php` — anonymous
component with `@props(['transaction','showAlways'=>false])`. Extract
`$tagged`, `$categoryShortName`, `$txId` from the `$transaction` row array
(keys `taxTagged`, `taxCategoryShortName`, `id`). Tagged → emerald `.tax-badge`
pill (category short name or "Tax"), `wire:click="$dispatch('tax-edit-tag',{id})"`.
Untagged → `.tax-badge--untagged` ghost button labelled "Tag",
`wire:click="$dispatch('tax-tag',{id})"`, with the `.row-cta` hover-reveal
unless `$showAlways` (touch) forces always-visible (≥44px). Add the exact
aria-labels from UI-SPEC Section 16.

Create `Modules/Tax/Resources/views/components/tax-tag-popover.blade.php` — the
shared picker UI per UI-SPEC Section 6: a note `<textarea>`, the scrollable
category list ("No category" first, check-mark on selected), the inline
"New category…" quick-add, and the year-assignment row when the booked year
differs from the tax year. Desktop = anchored popover; phone = bottom sheet via
the Phase 4 bottom-sheet wrapper (Alpine `x-data` for open/dismiss-on-outside;
`role="dialog"` `aria-modal`). Reduced-motion respected.

Create `Modules\Tax\Internal\Http\Livewire\Concerns\HandlesTaxTagging` (a
trait, not a component). It provides, for any host Livewire component:
  - public state: `?int $taxPickerTxId`, `bool $batchSuggestionDismissed`,
    `?array $batchSuggestion`, picker form fields (note, selectedCategoryId,
    yearOverride).
  - `#[On('tax-tag')] tagTransaction(int $id, TagTransaction $tag, CurrentUser
    $u, TaxTagQuery $q, Clock $c)`: one-tap tag (optimistic), open the picker,
    and compute the batch suggestion via `untaggedCountForCounterparty` (only
    surface when count ≥ 2 — D-03 threshold).
  - `#[On('tax-edit-tag')] editTaxTag(int $id, ...)`: open the picker for an
    already-tagged row, prefilled.
  - `saveTaxCategory(...)`, `addInlineCategory(...)` (delegates to
    TaxCategoryWriter::add), `untag(UntagTransaction, ...)`, `applyBatchTag(...)`
    (applies the same category+note to all matching untagged rows, then sets
    `batchSuggestionDismissed=true` to avoid Pitfall-7 re-appearance),
    `dismissBatch()`.
  - a `taxTagStateFor(array $transactionIds, TaxTagQuery, CurrentUser): array`
    helper that batch-loads tagged state for a page of rows (Pitfall 1: ONE
    query per render, never per row), to be merged into each surface's row
    arrays as `taxTagged` + `taxCategoryShortName`.
All collaborators arrive as method parameters (no constructor DI). Use
$dispatch/#[On] wiring (Open Question 1) so the badge works inside deeply
nested row partials.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse Modules/Tax/Internal/Http/Livewire/Concerns 2>&1 | tail -5; ./vendor/bin/pint --test Modules/Tax/Internal/Http/Livewire/Concerns 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `tax-badge.blade.php` dispatches `tax-tag` / `tax-edit-tag` events (grep assertion) and never calls a surface method directly.
    - `HandlesTaxTagging` declares `#[On('tax-tag')]` and `#[On('tax-edit-tag')]` listeners (source assertion).
    - `taxTagStateFor` issues one query for a batch of ids (asserted in Task 2's surface test via query-count).
    - `./vendor/bin/phpstan analyse Modules/Tax` clean; Pint clean.
  </acceptance_criteria>
  <done>The shared badge, picker, and reusable tagging trait exist with event-based wiring and batch-loaded state.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate badge + tagging on all four surfaces</name>
  <files>Modules/Ledger/Internal/Http/Livewire/TransactionsList.php, Modules/Ledger/Resources/views/livewire/transactions-list.blade.php, Modules/Ledger/Internal/Http/Livewire/TransactionDetail.php, Modules/Ledger/Resources/views/livewire/transaction-detail.blade.php, Modules/Counterparties/Internal/Http/Livewire/CounterpartyProfile.php, Modules/Counterparties/Resources/views/livewire/counterparty-profile.blade.php, Modules/CashBook/Internal/Http/Livewire/CashBookPage.php, Modules/CashBook/Resources/views/livewire/cash-book-page.blade.php, Modules/Tax/tests/Feature/TaxBadgeSurfacesTest.php</files>
  <read_first>
    - Modules/Ledger/Internal/Http/Livewire/TransactionsList.php (row-array projection ~line 84; render() injection params; infinite-scroll accumulation)
    - Modules/Ledger/Internal/Http/Livewire/TransactionDetail.php (single-transaction render ~line 224; existing reclassify actions = the D-01 detail-toggle home)
    - Modules/Counterparties/Internal/Http/Livewire/CounterpartyProfile.php (transactions tab render ~line 70)
    - Modules/CashBook/Internal/Http/Livewire/CashBookPage.php (entries projection ~line 102)
    - Modules/Tax/Internal/Http/Livewire/Concerns/HandlesTaxTagging.php (Task 1 trait being applied)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-RESEARCH.md (Pitfall 1 eager/batch load; Pitfall 6 — do NOT regress CrossUserIsolation)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-UI-SPEC.md (Section 5 badge placement per surface; Section 17 phone parity)
  </read_first>
  <action>
Apply the `HandlesTaxTagging` trait to each of the four Livewire components:
`TransactionsList`, `TransactionDetail`, `CounterpartyProfile`, `CashBookPage`.
In each component's `render()`, after building the row/entry collection, collect
the visible transaction ids and call `$this->taxTagStateFor($ids, $query,
$currentUser)` ONCE, merging `taxTagged` + `taxCategoryShortName` into each row
array (Pitfall 1 — one query, no N+1). For TransactionsList respect the
infinite-scroll accumulation (compute state for the currently-rendered window).

In each view, render `<x-tax::tax-badge :transaction="$row" :showAlways="$isTouch" />`
at the placement UI-SPEC Section 5 specifies (after the category chip / before
the amount on desktop tables; line-3 pill on phone cards; detail page next to
the existing reclassify actions — D-01). Render `<x-tax::tax-tag-popover />`
once per component (it reads the trait's `$taxPickerTxId` state) plus the
`.batch-tag-banner` (UI-SPEC Section 7) bound to the trait's `$batchSuggestion`.
Use the `$isTouch` coarse-pointer flag the surfaces already use for Phase 4
always-visible actions.

Create `TaxBadgeSurfacesTest` (feature): for each surface, assert (a) an
untagged row exposes the Tag action, (b) the `tax-tag` event tags the
transaction and the row re-renders as the emerald badge, (c) tagged state is
batch-loaded with a single query for a multi-row page (DB::enableQueryLog
assertion — Pitfall 1), (d) tagging a counterparty with ≥2 untagged siblings
surfaces the batch suggestion, applying it tags all and does not re-surface
(Pitfall 7), (e) the four surfaces still pass cross-user isolation (a user never
sees another user's badge — Pitfall 6).
  </action>
  <verify>
    <automated>php artisan test --filter=TaxBadgeSurfacesTest 2>&1 | tail -15; php artisan test --filter=CrossUserIsolationTest 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Each of the four views references `x-tax::tax-badge` (grep assertion across the four blade files).
    - Each of the four components `use HandlesTaxTagging` (grep assertion).
    - TaxBadgeSurfacesTest green: tag/untag round-trip per surface, single-query batch load, batch suggestion fires + applies + does not re-surface, cross-user badge isolation.
    - CrossUserIsolationTest stays green (no regression — Pitfall 6).
    - `./vendor/bin/phpstan analyse Modules/Tax Modules/Ledger Modules/Counterparties Modules/CashBook && ./vendor/bin/pint --test` (touched files) clean.
  </acceptance_criteria>
  <done>The tax badge + one-tap tagging + category picker + batch suggestion work on all four transaction-row surfaces, batch-loaded and cross-user safe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: four-surface tagging + picker + batch UAT</name>
  <action>Pause for the human to UAT one-tap tagging, the category picker/bottom-sheet, the batch-tag banner, and badge rendering across all four surfaces and at phone width before the plan completes.</action>
  <what-built>The shared tax badge + one-tap tagging + category picker (popover/bottom-sheet) + batch-tag suggestion, integrated on the transactions list, transaction detail, counterparty profile transactions tab, and cash book.</what-built>
  <how-to-verify>
    1. Transactions list (http://localhost:8000/transactions): hover an untagged row → the calm "Tag" ghost action appears; click it → row flips to the emerald badge and a category popover opens; pick a category → badge label updates. Add a note → it persists (check on /tax).
    2. Tag a second transaction from a counterparty that has ≥2 more untagged this year → the "Also tag N more from X?" blue banner appears; click "Tag all" → siblings tag and the banner does not re-appear.
    3. Transaction detail page: confirm the tax toggle sits next to the existing reclassify actions and tags/untags correctly.
    4. Counterparty profile → Transactions tab: tag a charity's donation row right there; confirm the badge shows.
    5. Cash book: confirm a manual entry row can be tagged.
    6. Phone width: confirm the Tag action is always visible (≥44px), the picker is a bottom sheet, and the badge reads calmly on card rows.
    7. Untag a row → instant removal with the undo toast; confirm /tax reflects the change.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → tag/untag/batch actions | transaction ids + category ids arrive from the client on four surfaces |
| four-surface row projections | each surface must not leak another user's tagged state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-18 | Elevation of privilege | tag/untag from any surface (forged id) | mitigate | HandlesTaxTagging delegates to TagTransaction/UntagTransaction which enforce user-scope + 404 (Plan 01) |
| T-07-19 | Information disclosure | badge state leaks across users | mitigate | taxTagStateFor is user-scoped via TaxTagQuery (Plan 02); CrossUserIsolation re-asserted (Task 2) |
| T-07-20 | Information disclosure | batch suggestion exposes another user's counterparty tx | mitigate | untaggedCountForCounterparty is user-scoped (Plan 02); tested cross-user (Task 2) |
</threat_model>

<verification>
- `php artisan test --filter=Tax && php artisan test --filter=CrossUserIsolationTest` green.
- `./vendor/bin/phpstan analyse Modules/Tax Modules/Ledger Modules/Counterparties Modules/CashBook && ./vendor/bin/pint --test`.
- Human-verify checkpoint approved (four-surface tagging + picker + batch + phone).
</verification>

<success_criteria>
- The tax badge + one-tap tagging + optional category + note work on all four surfaces.
- Batch-tag suggestion fires at ≥2 untagged siblings, applies once, no re-surface.
- Tagged state batch-loaded (no N+1); cross-user isolation preserved; phone parity.
</success_criteria>

<output>
Create `.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-06-SUMMARY.md` when done.
</output>
