---
phase: 07-tax-deductible-tagging-per-year-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/Tax/module.json
  - Modules/Tax/Providers/TaxServiceProvider.php
  - Modules/Tax/Routes/web.php
  - Modules/Tax/Database/Migrations/2026_06_12_000001_create_tax_deduction_categories_table.php
  - Modules/Tax/Database/Migrations/2026_06_12_000002_create_tax_transaction_tags_table.php
  - Modules/Tax/Database/Migrations/2026_06_12_000003_add_tax_country_code_to_users_table.php
  - Modules/Tax/Public/Dto/TaxTagData.php
  - Modules/Tax/Public/Dto/TaxYearData.php
  - Modules/Tax/Public/Dto/BatchTagSuggestion.php
  - Modules/Tax/Public/Dto/TaxYearSummary.php
  - Modules/Tax/Public/Events/TransactionTagged.php
  - Modules/Tax/Public/Actions/TagTransaction.php
  - Modules/Tax/Public/Actions/UntagTransaction.php
  - Modules/Tax/tests/Pest.php
  - Modules/Tax/tests/TestCase.php
  - Modules/Tax/tests/Arch/TaxBoundaryTest.php
  - Modules/Tax/tests/Feature/TaxTagActionTest.php
  - Modules/Tax/tests/Feature/TaxPageTest.php
  - Modules/Tax/tests/Feature/TaxExportCsvTest.php
  - Modules/Tax/tests/Feature/TaxExportPdfTest.php
  - Modules/Tax/tests/Feature/TaxSettingsTest.php
  - Modules/Tax/tests/Unit/TaxCorpusLoaderTest.php
  - Modules/Tax/tests/Unit/TaxYearQueryTest.php
  - tests/Pest.php
  - phpunit.xml
  - Modules/Auth/tests/Feature/CrossUserIsolationTest.php
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03]

must_haves:
  truths:
    - "Tax module is registered and boots without error"
    - "D-04, D-10: tax_deduction_categories and tax_transaction_tags tables exist after migration — the tag schema carries an optional free-text note and a tax_year_override column, and any transaction type (income too) is taggable"
    - "All Tax service singletons are bound in the provider (lazily) so later plans never edit register()"
    - "A failing (RED) test suite under Modules/Tax/tests exists for every TAX requirement"
    - "CrossUserIsolationTest enumerates tax.index without failing its route-coverage guard"
  artifacts:
    - path: "Modules/Tax/Providers/TaxServiceProvider.php"
      provides: "Module provider — ALL service singleton binds, migration/route/view loading"
      contains: "loadMigrationsFrom"
    - path: "Modules/Tax/Database/Migrations/2026_06_12_000001_create_tax_deduction_categories_table.php"
      provides: "tax_deduction_categories schema (user_id, name, short_name, hint, corpus_key, country_code, status, sort_order)"
      contains: "tax_deduction_categories"
    - path: "Modules/Tax/Database/Migrations/2026_06_12_000002_create_tax_transaction_tags_table.php"
      provides: "tax_transaction_tags schema (user_id, transaction_id, deduction_category_id, tax_year_override, note)"
      contains: "tax_transaction_tags"
    - path: "Modules/Tax/Public/Dto/TaxTagData.php"
      provides: "Per-tag DTO contract consumed by badge surfaces and TaxPage"
      contains: "extends Data"
    - path: "Modules/Tax/tests/Feature/TaxTagActionTest.php"
      provides: "RED feature tests for TAX-01 tag/untag/cross-user"
      contains: "NotFoundHttpException"
  key_links:
    - from: "Modules/Tax/Providers/TaxServiceProvider.php"
      to: "Modules/Tax/Database/Migrations"
      via: "loadMigrationsFrom"
      pattern: "loadMigrationsFrom"
    - from: "tests/Pest.php"
      to: "Modules/Tax/tests/TestCase.php"
      via: "module test-case mapping loop"
      pattern: "Modules.Tax.*TestCase"
---

<objective>
Stand up the `Tax` module skeleton, the two-table schema, the user tax-country
column, the immutable DTO contracts, the cross-surface tagging events, the
tag/untag public actions, and the complete Wave 0 RED test harness. This plan
also pre-binds EVERY Tax service singleton in the provider so no downstream plan
ever edits `register()` — that keeps later waves free of provider file
conflicts. This plan ships first and alone.

Purpose: Interface-first foundation. Downstream plans (services, UI, badge,
export) must not explore the codebase to discover the schema or DTO shapes —
they receive them here.
Output: A registered, boot-clean `Modules/Tax` module; migrated tables;
DTOs/events/actions; all service binds; and a failing test suite that defines
done for the whole phase.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-CONTEXT.md
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-RESEARCH.md
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES. Downstream plans consume these verbatim. -->

DTO: Modules\Tax\Public\Dto\TaxTagData (extends Spatie\LaravelData\Data, final, readonly):
  - int $transactionId
  - ?int $deductionCategoryId
  - ?string $deductionCategoryShortName
  - ?string $note
  - ?int $taxYearOverride

DTO: Modules\Tax\Public\Dto\TaxYearData (extends Data, final, readonly):
  - int $year
  - int $deductionsTotalMinor      (sum of expense-type tagged settled_amount_minor)
  - int $incomeTotalMinor          (sum of income-type tagged settled_amount_minor)
  - int $itemCount
  - array $categories              (list of category group arrays: id, name, shortName, subtotalMinor, rows[])
  // each row is an associative array with keys:
  //   transactionId, bookedAt, accountName, counterpartyName, counterpartyIban,
  //   description, note, settledAmountMinor, settledCurrency, amountMinor,
  //   currency, transactionType, categoryId, categoryName, categoryShortName,
  //   taxYearOverride, sourceFormat, importRunId, fingerprint

DTO: Modules\Tax\Public\Dto\TaxYearSummary (extends Data, final, readonly):
  - int $year
  - int $totalMinor                (deductions + income combined, for sidebar/dashboard)
  - int $count

DTO: Modules\Tax\Public\Dto\BatchTagSuggestion (extends Data, final, readonly):
  - int $counterpartyId
  - string $counterpartyName
  - int $untaggedCount             (other untagged tx from same counterparty in tax year)

Event: Modules\Tax\Public\Events\TransactionTagged (final, readonly):
  - int $userId
  - int $transactionId
  - ?int $deductionCategoryId

Provider singleton binds (ALL bound here, lazily — classes are created in later plans):
  TagTransaction, UntagTransaction (this plan) ·
  TaxYearQuery, TaxTagQuery (Plan 02) · TaxCsvExporter, TaxPdfRenderer (Plan 03) ·
  TaxCorpusLoader, TaxCategoryWriter (Plan 04)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module scaffold + provider (all binds) + routes + two migrations + user tax-country column</name>
  <files>Modules/Tax/module.json, Modules/Tax/Providers/TaxServiceProvider.php, Modules/Tax/Routes/web.php, Modules/Tax/Database/Migrations/2026_06_12_000001_create_tax_deduction_categories_table.php, Modules/Tax/Database/Migrations/2026_06_12_000002_create_tax_transaction_tags_table.php, Modules/Tax/Database/Migrations/2026_06_12_000003_add_tax_country_code_to_users_table.php</files>
  <read_first>
    - Modules/Goals/module.json (module.json shape)
    - Modules/Goals/Providers/GoalsServiceProvider.php (provider register/boot, Livewire registration)
    - Modules/Community/Providers/CommunityServiceProvider.php (loadViewsFrom 'tax' alias + component auto-discovery pattern)
    - Modules/Goals/Routes/web.php (route group middleware + name pattern)
    - Modules/Goals/Database/Migrations/2026_06_08_000001_create_goals_table.php (the DatabaseManager-helper migration pattern — copy verbatim structure)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-PATTERNS.md (migration + provider full code excerpts)
  </read_first>
  <behavior>
    - Test: migrating creates `tax_deduction_categories` with columns user_id (nullable FK), name, short_name, hint, corpus_key, country_code, status (default 'active'), sort_order (default 0), timestamps; unique(user_id,name); index(user_id,status).
    - Test: migrating creates `tax_transaction_tags` with user_id (nullable FK), transaction_id (FK cascadeOnDelete), deduction_category_id (nullable FK nullOnDelete), tax_year_override (unsignedSmallInteger nullable), note (text nullable), timestamps; unique(user_id,transaction_id); index(user_id,deduction_category_id).
    - Test: migrating adds nullable `tax_country_code` (string 2) to `users`.
    - Test: the Tax module provider boots without error (app boots green) even though most bound service classes do not exist yet (binds are lazy).
  </behavior>
  <action>
Create `Modules/Tax/module.json` with name "Tax", alias "tax", priority 10,
providers `["Modules\\Tax\\Providers\\TaxServiceProvider"]`, keywords
["tax","deductions","tagging","export"], files [].

Create `Modules/Tax/Providers/TaxServiceProvider.php` (final class extends
ServiceProvider). In `register()` bind ALL Tax service classes as singletons,
listed in the `<interfaces>` "Provider singleton binds" block — including the
ones created in Plans 02–04. `$this->app->singleton(Class::class)` stores the
binding lazily; the class only needs to exist when first RESOLVED (which happens
in the plan that ships it), so binding non-existent-yet classes here is safe and
does NOT break boot. This deliberately makes Plan 01 the sole editor of
`register()`, so Plans 02–04 never touch the provider's bind list (no
cross-wave provider conflict). In `boot(LivewireManager $livewire)`:
`loadMigrationsFrom` the Migrations dir, conditionally `loadRoutesFrom`
Routes/web.php, `loadViewsFrom($viewsPath, 'tax')`. Do NOT register Livewire
components in this plan (the components do not exist yet) — Plans 04 and 05 add
their own `$livewire->component(...)` lines in `boot()`. Add a TODO comment in
`boot()` marking where those component registrations will be appended.

Create `Modules/Tax/Routes/web.php` mirroring Goals: middleware ['web','auth']
group. For THIS plan register the route as a closure stub returning a 501
response named `tax.index` (the real `TaxPage::class` swap happens in Plan 05,
same pattern STATE.md records for goals.index). This lets Wave 0 add the
CrossUserIsolation probe now without the page class existing yet.

Create the two table migrations and the users-column migration using the
`private ?DatabaseManager $resolvedDb` + `db()`/`schema()` helper pattern from
the Goals migration (PATTERNS.md lines 49–104) — never the `Schema::` facade.
Column definitions exactly as in the `<behavior>` block above and RESEARCH.md
Pattern 1. The users-column migration adds `tax_country_code` (string length 2,
nullable) via `$this->schema()->table('users', ...)`.
  </action>
  <verify>
    <automated>php artisan migrate --pretend 2>&1 | grep -q tax_transaction_tags && php artisan migrate && php artisan about 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate` runs clean; `tax_deduction_categories`, `tax_transaction_tags` tables and `users.tax_country_code` column exist (verify via `php artisan tinker --execute="echo Schema::hasTable('tax_transaction_tags');"` returning 1).
    - `Modules/Tax/module.json` parses as valid JSON and lists `Modules\Tax\Providers\TaxServiceProvider`.
    - App boots green (`php artisan about` exits 0) — lazy binds to not-yet-existing classes do not break boot.
    - `TaxServiceProvider::register()` binds all eight service classes named in `<interfaces>` (source assertion).
    - `tax.index` route is registered (`php artisan route:list --name=tax.index` shows one row).
  </acceptance_criteria>
  <done>Both tax tables + users column migrate; module boots; all service singletons pre-bound; tax.index route exists as a 501 stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DTOs + events + TagTransaction / UntagTransaction public actions</name>
  <files>Modules/Tax/Public/Dto/TaxTagData.php, Modules/Tax/Public/Dto/TaxYearData.php, Modules/Tax/Public/Dto/TaxYearSummary.php, Modules/Tax/Public/Dto/BatchTagSuggestion.php, Modules/Tax/Public/Events/TransactionTagged.php, Modules/Tax/Public/Actions/TagTransaction.php, Modules/Tax/Public/Actions/UntagTransaction.php</files>
  <read_first>
    - Modules/Categorization/Public/Actions/AssignCategory.php (ownership-check + event-dispatch pattern, 404-not-403)
    - Modules/EmailScan/Public/Dto/InboxHealthLine.php (Spatie Data DTO shape — final, readonly, constructor-only)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-PATTERNS.md (TagTransaction excerpt lines 128–183; DTO excerpt lines 185–212)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-RESEARCH.md (TagTransaction code example + Pitfall 2 cross-user leakage)
  </read_first>
  <behavior>
    - Test: TagTransaction::execute() with the owner's transaction id inserts/updates a single tax_transaction_tags row (idempotent re-tag updates, never duplicates — unique(user_id,transaction_id)).
    - Test: TagTransaction::execute() with another user's transaction id throws NotFoundHttpException (404-not-403, V4 access control).
    - Test: TagTransaction validates deduction_category_id belongs to the same user (or is null); a category id owned by another user throws NotFoundHttpException (V5 input validation).
    - Test: TagTransaction rejects a tax_year_override outside now±10 years (throws \InvalidArgumentException) and accepts null.
    - Test: TagTransaction dispatches TransactionTagged after a successful write.
    - Test: UntagTransaction::execute() deletes the owner's tag row and is a silent no-op for a non-existent/cross-user tag (no exception, nothing deleted for the other user).
  </behavior>
  <action>
Create the four DTOs exactly matching the `<interfaces>` block — all `final`,
`extends Spatie\LaravelData\Data`, `readonly` constructor-promoted properties,
no methods beyond the constructor.

Create `Modules\Tax\Public\Events\TransactionTagged` as a `final` class with a
`readonly` promoted constructor (userId, transactionId, deductionCategoryId).

Create `Modules\Tax\Public\Actions\TagTransaction` (final, constructor injects
`Illuminate\Database\DatabaseManager $db` and
`Illuminate\Contracts\Events\Dispatcher $events`). Signature:
`execute(int $userId, int $transactionId, ?int $deductionCategoryId, ?string $note, ?int $taxYearOverride): void`.
Order of operations: (1) verify the transaction belongs to `$userId` via
`$db->connection()->table('transactions')->where('id',$transactionId)->where('user_id',$userId)->exists()`,
else throw `Symfony\Component\HttpKernel\Exception\NotFoundHttpException`.
(2) If `$deductionCategoryId !== null`, verify it belongs to `$userId` in
`tax_deduction_categories` (same exists()+throw pattern). (3) If
`$taxYearOverride !== null`, reject when outside `now()->year - 10` ..
`now()->year + 10` with `\InvalidArgumentException`. (4) `updateOrInsert` on
key `['user_id'=>$userId,'transaction_id'=>$transactionId]` setting
deduction_category_id, note, tax_year_override, updated_at=now(),
created_at=now(). (5) Dispatch `TransactionTagged`. Use raw DatabaseManager
queries only — never Eloquent statics (PHPStan level 10 staticMethod.dynamicCall).

Create `Modules\Tax\Public\Actions\UntagTransaction` (final, injects
DatabaseManager). Signature `execute(int $userId, int $transactionId): void`:
delete from tax_transaction_tags where user_id AND transaction_id match
(cross-user/missing = no rows deleted, no exception — fire-and-forget, matching
the lifecycle-no-op convention from GoalWriter recorded in STATE.md).

Both actions are already bound as singletons in TaxServiceProvider (Task 1) — no
provider edit needed here.
  </action>
  <verify>
    <automated>php artisan test --filter=TaxTagActionTest 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `TaxTagActionTest` passes: tag, idempotent re-tag, untag, cross-user tag → NotFoundHttpException, cross-user category → NotFoundHttpException, out-of-range tax_year_override → InvalidArgumentException, TransactionTagged dispatched.
    - `./vendor/bin/phpstan analyse Modules/Tax/Public` reports no errors (no `mixed`, no static model calls).
    - Each DTO field name and type matches the `<interfaces>` block exactly (source assertion).
  </acceptance_criteria>
  <done>Tag/untag actions enforce ownership, validate inputs, dispatch the tagged event; DTO contracts exist for downstream consumers.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test harness — RED suite, bootstrap wiring, CrossUserIsolation probe</name>
  <files>Modules/Tax/tests/Pest.php, Modules/Tax/tests/TestCase.php, Modules/Tax/tests/Arch/TaxBoundaryTest.php, Modules/Tax/tests/Feature/TaxTagActionTest.php, Modules/Tax/tests/Feature/TaxPageTest.php, Modules/Tax/tests/Feature/TaxExportCsvTest.php, Modules/Tax/tests/Feature/TaxExportPdfTest.php, Modules/Tax/tests/Feature/TaxSettingsTest.php, Modules/Tax/tests/Unit/TaxCorpusLoaderTest.php, Modules/Tax/tests/Unit/TaxYearQueryTest.php, tests/Pest.php, phpunit.xml, Modules/Auth/tests/Feature/CrossUserIsolationTest.php</files>
  <read_first>
    - Modules/Goals/tests/TestCase.php and Modules/Goals/tests/Pest.php (module test bootstrap shape)
    - Modules/Recurring/tests/Feature/RejectRecurringSeriesTest.php (feature test structure + fixture-user helper)
    - Modules/Counterparties/tests/Arch/CounterpartiesBoundaryTest.php (arch test — copy verbatim with Tax namespace)
    - tests/Pest.php (the module → TestCase mapping loop; add a Modules/Tax entry)
    - phpunit.xml (testsuite directory registration)
    - Modules/Auth/tests/Feature/CrossUserIsolationTest.php (covered-route constant block ~line 141, allow-list block ~line 36, the regression-guard test ~line 377)
  </read_first>
  <behavior>
    - The Tax test suite is discoverable by `php artisan test --filter=Tax` and runs (RED is acceptable for unimplemented services this plan does not build).
    - TaxTagActionTest is GREEN (Task 2 implements it); all other Tax feature/unit tests assert the not-yet-built behavior and are skipped-with-reason tied to the implementing plan (not errored).
    - CrossUserIsolationTest's route-coverage regression guard passes WITH `tax.index` present (allow-list entry added).
  </behavior>
  <action>
Create `Modules/Tax/tests/TestCase.php` (namespace `Modules\Tax\Tests`, extends
`Tests\TestCase`) and `Modules/Tax/tests/Pest.php` (extend TestCase + use
RefreshDatabase in Feature; extend TestCase in Unit) following the Goals/Receipts
module bootstrap pattern.

Add `'Modules/Tax' => Modules\Tax\Tests\TestCase::class` to the mapping loop in
root `tests/Pest.php`. Register `Modules/Tax/tests/Feature` and
`Modules/Tax/tests/Unit` (and Arch) directories in `phpunit.xml` testsuites
following the existing module entries.

Create `Modules/Tax/tests/Arch/TaxBoundaryTest.php` — copy the Counterparties
boundary arch test, asserting `Modules\Tax\Internal` is only used in
`Modules\Tax`.

Create the RED feature/unit test files. `TaxTagActionTest.php` carries the full
Task-2 behavior assertions (it goes GREEN this plan). The remaining files
(`TaxPageTest`, `TaxExportCsvTest`, `TaxExportPdfTest`, `TaxSettingsTest`,
`TaxCorpusLoaderTest`, `TaxYearQueryTest`) describe the expected behavior of
services/pages built in Plans 02–05. For each MISSING-target test, write the
assertion as the real expectation (not a stub) and mark it with a
`->skip('built in Plan 0X')` annotation that names the implementing plan, so the
suite is informative but not falsely red on merge gates. The map:
  - TaxYearQueryTest → Plan 02 (grouped query + COALESCE year override)
  - TaxExportCsvTest, TaxExportPdfTest → Plan 03 (exporters)
  - TaxCorpusLoaderTest, TaxSettingsTest → Plan 04 (corpus + settings)
  - TaxPageTest → Plan 05 (/tax page render + year switching)

Add the `tax.index` cross-user probe to `CrossUserIsolationTest`: since this
plan's `tax.index` is still a 501 closure stub (no cross-user data yet), add it
to the auth-plumbing/allow-list block with a comment "Phase 07 — real TaxPage +
data-scoped probe lands in Plan 05; stub returns 501". Plan 05 will REPLACE the
allow-list entry with a real two-user data-isolation probe.
  </action>
  <verify>
    <automated>php artisan test --filter=Tax 2>&1 | tail -15 && php artisan test --filter=CrossUserIsolationTest 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Tax` discovers and runs the Tax suite (TaxTagActionTest + TaxBoundaryTest green; others skipped-with-reason, not errored).
    - `CrossUserIsolationTest` "covers or allow-lists every auth-gated GET route" passes with `tax.index` present.
    - `tests/Pest.php` contains a `Modules/Tax` → `Modules\Tax\Tests\TestCase` mapping (source assertion).
    - `phpunit.xml` references `Modules/Tax/tests` directories (source assertion).
  </acceptance_criteria>
  <done>Tax test infrastructure exists, the RED suite encodes every downstream requirement, and the route-coverage guard stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Livewire action | Untrusted `transactionId` / `deductionCategoryId` arrive from the client and must be ownership-checked |
| migration / schema | Tables must be multi-user-scoped (nullable user_id) for the future sync op-log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of privilege | TagTransaction (forged transaction_id) | mitigate | `where('user_id',$userId)->exists()` before write; `NotFoundHttpException` on miss (Task 2) |
| T-07-02 | Elevation of privilege | TagTransaction (forged deduction_category_id) | mitigate | Validate category ownership before write; 404 on cross-user category (Task 2) |
| T-07-03 | Tampering | tax_year_override input | mitigate | Range-check now±10 years; InvalidArgumentException out of range (Task 2) |
| T-07-04 | Information disclosure | tax.index new GET route | mitigate | CrossUserIsolation allow-list entry now; real data-scoped probe in Plan 05 (Task 3) |
</threat_model>

<verification>
- `php artisan migrate:fresh && php artisan test --filter=Tax` — schema migrates and Tax suite runs.
- `php artisan test --filter=CrossUserIsolationTest` — route guard green.
- `./vendor/bin/phpstan analyse Modules/Tax && ./vendor/bin/pint --test Modules/Tax` — level 10 + format clean.
</verification>

<success_criteria>
- Tax module registered, boots clean, two tables + users column migrate, all service singletons pre-bound.
- TagTransaction/UntagTransaction enforce ownership and input validation.
- DTO + event contracts exist exactly as specified for downstream plans.
- RED test suite covers all of TAX-01..03; CrossUserIsolation guard green.
</success_criteria>

<output>
Create `.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-01-SUMMARY.md` when done.
</output>
