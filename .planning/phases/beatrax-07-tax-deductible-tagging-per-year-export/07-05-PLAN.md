---
phase: 07-tax-deductible-tagging-per-year-export
plan: 05
type: execute
wave: 4
depends_on: [07-02, 07-03, 07-04]
files_modified:
  - Modules/Tax/Internal/Http/Livewire/TaxPage.php
  - Modules/Tax/Internal/Http/Livewire/TaxSummaryCard.php
  - Modules/Tax/Resources/views/livewire/tax-page.blade.php
  - Modules/Tax/Resources/views/livewire/tax-summary-card.blade.php
  - Modules/Tax/Routes/web.php
  - Modules/Tax/Providers/TaxServiceProvider.php
  - resources/css/app.css
  - Modules/Core/Resources/views/livewire/app-sidebar.blade.php
  - Modules/Core/Resources/views/livewire/dashboard.blade.php
  - Modules/DevMode/Providers/DevModeServiceProvider.php
  - Modules/Auth/tests/Feature/CrossUserIsolationTest.php
  - Modules/Tax/tests/Feature/TaxPageTest.php
autonomous: false
requirements: [TAX-02, TAX-03]

must_haves:
  truths:
    - "D-09, D-11, D-12, D-21: Visiting /tax shows tagged transactions for a year, grouped by deduction category as collapsible sections with subtotals and grand totals in settled EUR (dedicated /tax cockpit; phone parity via card-per-row collapsible sections)"
    - "D-22: The year switcher changes the displayed year; the default year follows the seasonal rule (Jan–Apr → previous/aangifte year, May–Dec → current year)"
    - "D-16: Export CSV and Export PDF buttons download beatrax-tax-{year}.csv / .pdf (two stateless streamDownload buttons, regenerate any time)"
    - "D-18: The dashboard shows a small year-total tax summary card linking to /tax"
    - "D-17: The sidebar and ⌘K palette expose a Tax entry"
    - "D-23: A user cannot see another user's tagged transactions on /tax; first-visit and empty-year guided empty states read as a filter, not lost data"
  artifacts:
    - path: "Modules/Tax/Internal/Http/Livewire/TaxPage.php"
      provides: "/tax cockpit: year switcher, grouped sections, export actions"
      contains: "exportCsv"
    - path: "Modules/Tax/Resources/views/livewire/tax-page.blade.php"
      provides: "Cockpit view: totals strip, category sections, empty states"
      min_lines: 60
    - path: "Modules/Tax/Internal/Http/Livewire/TaxSummaryCard.php"
      provides: "Dashboard tax summary card"
      contains: "summaryForUser"
  key_links:
    - from: "Modules/Tax/Internal/Http/Livewire/TaxPage.php"
      to: "Modules/Tax/Internal/Services/TaxYearQuery.php"
      via: "render() forUser(year)"
      pattern: "TaxYearQuery"
    - from: "Modules/Tax/Internal/Http/Livewire/TaxPage.php"
      to: "TaxCsvExporter + TaxPdfRenderer"
      via: "streamDownload export actions"
      pattern: "streamDownload"
    - from: "Modules/Core/Resources/views/livewire/app-sidebar.blade.php"
      to: "tax.index"
      via: "sidebar nav link"
      pattern: "route\\('tax.index'\\)"
---

<objective>
Build the `/tax` year cockpit (TAX-02, D-09): the year switcher with seasonal default,
the grouped category sections with subtotals + grand totals, the guided empty
states, and the two stream-download export buttons (TAX-03). Add the dashboard
summary card (D-18), the sidebar + ⌘K palette entry (D-17), the cockpit CSS primitives, and
the real cross-user isolation probe. Swap the `tax.index` route stub for the
live page.

Purpose: The "tax season cockpit" — one place to review and export a year's
tagged set. This is where TAX-02 and TAX-03 become visible to the user.
Output: A working /tax page with exports, dashboard card, and navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-CONTEXT.md
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-RESEARCH.md
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-PATTERNS.md
@.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-UI-SPEC.md

<interfaces>
<!-- Consumes Plans 02/03/04 services. -->
TaxYearQuery::forUser(userId, year): TaxYearData  — grouped categories + totals
TaxYearQuery::availableYears(userId): array<int>  — year switcher options
TaxTagQuery::summaryForUser(userId, year): TaxYearSummary — dashboard card
TaxCsvExporter::export(user, year): string        — CSV body
TaxPdfRenderer::render(user, year): string        — PDF bytes
TaxCategoryWriter not needed here (Settings owns CRUD)

Seasonal default (D-22): month <= 4 ? year-1 : year
Filenames: beatrax-tax-{year}.csv / beatrax-tax-{year}.pdf
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TaxPage cockpit — year switcher, grouped sections, exports, empty states + CSS</name>
  <files>Modules/Tax/Internal/Http/Livewire/TaxPage.php, Modules/Tax/Resources/views/livewire/tax-page.blade.php, Modules/Tax/Routes/web.php, Modules/Tax/Providers/TaxServiceProvider.php, resources/css/app.css, Modules/Tax/tests/Feature/TaxPageTest.php</files>
  <read_first>
    - Modules/Goals/Internal/Http/Livewire/GoalsPage.php (class page render() + $view->extends pattern)
    - Modules/DriftAlerts/Internal/Http/Livewire/DriftPage.php (#[Url(as:'year')] switcher pattern)
    - Modules/Auth/Internal/Http/Livewire/RecoveryCodesDisplay.php (streamDownload action pattern)
    - Modules/Import/Internal/Http/Livewire/AliasesSettingsPage.php (second streamDownload analog)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-PATTERNS.md (TaxPage excerpts lines 377–452: #[Url], seasonal mount, streamDownload, render)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-UI-SPEC.md (Section 8 page structure; Section 9 empty states; Section 13 copy; Section 14 new CSS classes; Section 17 responsive)
    - .claude/skills/sketch-findings-beatrax/SKILL.md (calm-slate primitives) — invoke Skill("sketch-findings-beatrax")
    - resources/css/app.css (existing @layer components, .card, .pill-btn-*, .row-cta, tokens)
  </read_first>
  <action>
Create `Modules\Tax\Internal\Http\Livewire\TaxPage` (final extends Component,
NO constructor DI). `#[Url(as:'year', except:0)] public int $year = 0;`.
`mount(CurrentUser, Clock)` applies the seasonal default (D-22) when year is 0:
`month <= 4 ? year-1 : year`. `render(CurrentUser, TaxYearQuery, ViewFactory)`
calls `forUser($user->id, $this->year)` and `availableYears(...)`, passes the
TaxYearData + year list to `tax::livewire.tax-page`, and `$view->extends(
'layouts.app', ['title' => "Tax {$this->year} · beatrax"])` (guard the
unauthenticated branch as GoalsPage does). Add `exportCsv(ResponseFactory,
TaxCsvExporter, CurrentUser)` and `exportPdf(ResponseFactory, TaxPdfRenderer,
CurrentUser)` returning `StreamedResponse` via `streamDownload` with filenames
`beatrax-tax-{year}.csv` / `.pdf` and the correct content types.

Create `Modules/Tax/Resources/views/livewire/tax-page.blade.php` per UI-SPEC
Section 8: page header (H1 "Tax {year}", year segmented switcher with
`wire:model.live`, two export buttons with in-flight spinner + aria-busy — D-16 stream-download, filenames beatrax-tax-{year}.csv/.pdf),
year-totals strip (deductions / income / item count grand totals, D-11/D-12), then one collapsible
`.tax-section` per category with header (name + count chip + subtotal +
chevron) and a transaction table (columns per UI-SPEC Section 8: date, account,
counterparty link, note, settled EUR, original, year-override amber chip,
actions), "no category" group last. Phone: card-per-row (Section 17). Empty
states (Section 9): country-not-set → redirect to the Settings-driven country
picker path or show the first-visit card; empty-year → calm note + count of
tagged items in other years (use availableYears). Copy strings exactly from
UI-SPEC Section 13.

Add the new CSS classes from UI-SPEC Section 14 (`.tax-badge`,
`.tax-badge--untagged`, `.tax-badge--amber`, `.tax-section`,
`.tax-section-header`, `.tax-totals-strip`, `.batch-tag-banner`,
`.tax-export-btn`) to `resources/css/app.css` `@layer components`, resolving
only from existing `@theme` tokens (no new variables). NOTE: `.tax-badge*`
classes are defined here so Plan 06 (badge surfaces) can reuse them without
touching app.css again.

Swap the `tax.index` route from the 501 closure stub to
`Route::get('/tax', TaxPage::class)->name('tax.index')`. Register the Livewire
component in TaxServiceProvider::boot()
(`$livewire->component('tax.tax-page', TaxPage::class)`).

Implement the RED `TaxPageTest`: page renders grouped sections for a tagged
year; year switcher changes results; seasonal default (mock Clock to Feb → prev
year; to Aug → curr year); exportCsv/exportPdf return a StreamedResponse with
the right filename/content-type; empty-year renders the calm note. Remove the
Plan-01 skip annotation.
  </action>
  <verify>
    <automated>php artisan test --filter=TaxPageTest 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `/tax` route resolves to `TaxPage::class` (`php artisan route:list --name=tax.index` shows the class, not a closure).
    - TaxPageTest green: grouped render, year switch, seasonal default (Feb→prev, Aug→curr), export StreamedResponse filenames `beatrax-tax-{year}.csv`/`.pdf`, empty-year note.
    - app.css contains all 8 new classes from UI-SPEC Section 14 (grep assertion); no new `@theme` variables added (grep diff).
    - `npm run build` (or the project asset build) compiles app.css without error.
    - `./vendor/bin/phpstan analyse Modules/Tax/Internal/Http/Livewire/TaxPage.php && ./vendor/bin/pint --test Modules/Tax resources/css/app.css` clean.
  </acceptance_criteria>
  <done>/tax renders the grouped cockpit with year switching, seasonal default, exports, and empty states; cockpit + badge CSS shipped.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard summary card + sidebar entry + ⌘K palette entry + real cross-user probe</name>
  <files>Modules/Tax/Internal/Http/Livewire/TaxSummaryCard.php, Modules/Tax/Resources/views/livewire/tax-summary-card.blade.php, Modules/Tax/Providers/TaxServiceProvider.php, Modules/Core/Resources/views/livewire/dashboard.blade.php, Modules/Core/Resources/views/livewire/app-sidebar.blade.php, Modules/DevMode/Providers/DevModeServiceProvider.php, Modules/Auth/tests/Feature/CrossUserIsolationTest.php</files>
  <read_first>
    - Modules/Goals/Internal/Http/Livewire/GoalsSummaryCard.php (summary-card component pattern — copy structure)
    - Modules/Goals/Resources/views/livewire/goals-summary-card.blade.php (card markup analog)
    - Modules/Core/Resources/views/livewire/dashboard.blade.php (the @livewire('goals.summary-card') slot ~line 161)
    - Modules/Core/Resources/views/livewire/app-sidebar.blade.php (counterparties nav entry + side-badge muted pattern ~lines 108–124)
    - Modules/DevMode/Providers/DevModeServiceProvider.php (palette page-entry array ~lines 360–370 — add a tax entry)
    - Modules/Auth/tests/Feature/CrossUserIsolationTest.php (covered-route constant + probe-case pattern; move tax.index from allow-list to a real probe)
    - .planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-UI-SPEC.md (Section 10 dashboard card; Section 11 sidebar entry; Section 13 copy)
  </read_first>
  <action>
Create `Modules\Tax\Internal\Http\Livewire\TaxSummaryCard` (final extends
Component, no constructor DI) following GoalsSummaryCard exactly:
`render(CurrentUser, TaxTagQuery, ViewFactory, Clock)` computes the seasonal
year, calls `summaryForUser($user->id, $year)`, passes total + count + year to
`tax::livewire.tax-summary-card`. Renders inline (no `$view->extends`).

Create `Modules/Tax/Resources/views/livewire/tax-summary-card.blade.php` per
UI-SPEC Section 10: `.card` primitive, heading "Tax {year}", tagged total
(EUR, tabular, --text-xl), "{N} items tagged" sub-line, whole card is an anchor
to `route('tax.index')`, calm empty state when count is 0. Copy from Section 13.

Register the component in TaxServiceProvider::boot()
(`$livewire->component('tax.summary-card', TaxSummaryCard::class)`). Add
`@livewire('tax.summary-card')` to `dashboard.blade.php` in the records/insight
cluster near `@livewire('goals.summary-card')`.

Add the Tax sidebar entry to `app-sidebar.blade.php` near Counterparties
(UI-SPEC Section 11): `<a href="{{ route('tax.index') }}" class="side-item
{{ $isActive('/tax') }}">` with the receipt glyph and a `.side-badge muted`
showing `$navCounts['tax_tagged']` when > 0.

Add a ⌘K palette page entry to the page-entries array in
`DevModeServiceProvider.php` (~line 363, alongside calendar.index):
`['id'=>'tax.index','label'=>'Tax','hint'=>'Deductible records and per-year
export','icon'=>'⊞','route'=>'tax.index','keywords'=>['deduction','aangifte',
'export','records']]`.

Update `CrossUserIsolationTest`: REMOVE the Plan-01 allow-list entry for
tax.index and ADD a real two-user probe — tag a transaction for user A, assert
user B gets 200 on their own `/tax` but B's page never contains A's tagged
transaction/counterparty. Keep the route-coverage regression guard green.
  </action>
  <verify>
    <automated>php artisan test --filter=CrossUserIsolationTest 2>&1 | tail -10; php artisan test --filter=Tax 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard renders `@livewire('tax.summary-card')` (grep + a Livewire render test asserting the card shows the year total).
    - Sidebar contains `route('tax.index')` with the tax_tagged side-badge (grep assertion).
    - The palette page-entries array contains a `tax.index` entry (grep assertion).
    - CrossUserIsolationTest has a real tax.index data-isolation probe (not an allow-list entry) and the full suite is green.
    - `./vendor/bin/phpstan analyse Modules/Tax && ./vendor/bin/pint --test Modules/Tax Modules/Core/Resources/views Modules/DevMode/Providers/DevModeServiceProvider.php` clean.
  </acceptance_criteria>
  <done>Tax is reachable from the dashboard card, sidebar, and ⌘K palette; cross-user isolation on /tax is proven by a real probe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: /tax cockpit + exports + PDF layout UAT</name>
  <action>Pause for the human to UAT the /tax cockpit, CSV/PDF exports (including the manual PDF layout check), dashboard card, and sidebar/palette entries before the plan completes.</action>
  <what-built>The /tax cockpit (year switcher, grouped category sections with subtotals + grand totals, guided empty states), CSV + PDF exports, the dashboard tax summary card, and the sidebar + ⌘K palette entries.</what-built>
  <how-to-verify>
    1. Tag a few transactions across two deduction categories (and one with no category, one income) for the current year. (Use the transactions list / detail; if Plan 06 badge surfaces are not yet merged, seed tags via tinker or the TagTransaction action.)
    2. Visit http://localhost:8000/tax — confirm: grouped sections per category with correct subtotals; year-totals strip shows deductions / income / item count; "no category" group last.
    3. Use the year switcher — confirm results change and a deep link `?year=2025` works. Confirm the seasonal default (this month is June 2026 → opens current year 2026).
    4. Click "Export CSV" — confirm a `beatrax-tax-2026.csv` downloads with all D-15 audit columns.
    5. Click "Export PDF" — confirm a `beatrax-tax-2026.pdf` downloads; OPEN it and check the summary page + per-category tables render cleanly, headers repeat across pages, rows do not clip (the manual-only PDF layout verification from 07-VALIDATION.md).
    6. Open an empty year — confirm the calm note + "{N} tagged items in other years" reads as a filter, not lost data.
    7. Check the dashboard tax card and the sidebar "Tax" entry + ⌘K palette "Tax" entry all link to /tax.
    8. Resize to phone width — confirm card-per-row rows, stacked export buttons, collapsed sections.
  </how-to-verify>
  <resume-signal>Type "approved" or describe layout/behavior issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /tax (?year=) | year param + export requests are client-controlled |
| export download | streamed bytes must contain only the requesting user's data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Information disclosure | /tax cross-user data via URL/year | mitigate | TaxYearQuery user-scoped (Plan 02); real CrossUserIsolation probe added (Task 2) |
| T-07-16 | Information disclosure | export streams another user's rows | mitigate | exportCsv/exportPdf pass `CurrentUser->user()` to user-scoped services only (Task 1) |
| T-07-17 | Spoofing | unauthenticated /tax access | mitigate | route in `['web','auth']` group; unauthenticated render branch returns empty view (Task 1) |
</threat_model>

<verification>
- `php artisan test --filter=Tax && php artisan test --filter=CrossUserIsolationTest` green.
- `npm run build` compiles app.css.
- `./vendor/bin/phpstan analyse Modules/Tax && ./vendor/bin/pint --test Modules/Tax`.
- Human-verify checkpoint approved (cockpit + exports + PDF layout + nav).
</verification>

<success_criteria>
- /tax renders grouped, override-aware, settled-EUR year data with seasonal default + year switcher.
- CSV + PDF export the year's set with correct filenames.
- Dashboard card, sidebar, and ⌘K palette expose Tax; cross-user isolation proven.
</success_criteria>

<output>
Create `.planning/phases/beatrax-07-tax-deductible-tagging-per-year-export/07-05-SUMMARY.md` when done.
</output>
