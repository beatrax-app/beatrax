---
phase: 03-ics-cards-multi-currency-display
plan: 05
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-04
files_modified:
  - Modules/Ledger/Public/Dto/TransactionRowDto.php
  - Modules/Ledger/Public/Services/TransactionListQuery.php
  - Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php
  - Modules/Ledger/Public/Dto/PerCurrencyTile.php
  - Modules/Ledger/Public/ValueObjects/Money.php
  - Modules/Ledger/Internal/Http/Livewire/TransactionsList.php
  - Modules/Ledger/Resources/views/livewire/transactions-list.blade.php
  - Modules/Core/Internal/Http/Livewire/Dashboard.php
  - Modules/Core/Resources/views/livewire/dashboard.blade.php
  - Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php
  - Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php
  - Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php
autonomous: true
requirements:
  - MC-02
  - UI-06

must_haves:
  truths:
    - "Visiting /transactions with the URL ?currency=eur shows every row's amount in EUR (settled-EUR pair) on a single line"
    - "Visiting /transactions with no ?currency param falls back to the user's default_currency_view; when default is 'original', foreign-currency rows render two lines (native primary, settled-EUR secondary muted)"
    - "Visiting /transactions?currency=original (explicit) shows the dual-line stack regardless of user default"
    - "The Flux segmented control switches between EUR / Original visually; URL updates via #[Url]"
    - "Dashboard /home in original mode renders one tile-row per currency present in the period; an EUR-only month collapses to a single tile-row identical to EUR-only mode"
    - "Transaction detail page renders 'Effective rate' as '€0.929 / USD' (EUR-per-unit-native, 3 decimals) with 'Includes any ICS markup.' helper text below — only when fx_rate_used is non-null"
  artifacts:
    - path: "Modules/Ledger/Public/Dto/PerCurrencyTile.php"
      provides: "Per-currency dashboard tile DTO (currency, inflow Money, outflow Money, net Money)"
      contains: "PerCurrencyTile"
    - path: "Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php"
      provides: "New forByCurrency() method returning list<PerCurrencyTile> grouped by settled_currency"
      contains: "forByCurrency"
    - path: "Modules/Ledger/Public/Dto/TransactionRowDto.php"
      provides: "Extended with nullable Money $secondaryAmount for dual-line render"
      contains: "secondaryAmount"
    - path: "Modules/Ledger/Internal/Http/Livewire/TransactionsList.php"
      provides: "#[Url] currency property + mount() fallback to user default + render() projection branching"
      contains: "#[Url"
    - path: "Modules/Ledger/Resources/views/livewire/transactions-list.blade.php"
      provides: "Flux segmented control + dual-line amount stack in original mode"
      contains: "flux:radio.group"
  key_links:
    - from: "Modules/Ledger/Internal/Http/Livewire/TransactionsList.php"
      to: "users.default_currency_view"
      via: "$currentUser->user()->default_currency_view fallback in mount()"
      pattern: "default_currency_view"
    - from: "Modules/Ledger/Resources/views/livewire/transactions-list.blade.php"
      to: "Modules/Ledger/Internal/Http/Livewire/TransactionsList.php"
      via: "wire:model.live='currency' bound to #[Url(as: 'currency')]"
      pattern: "wire:model\\.live=\"currency\""
    - from: "Modules/Core/Internal/Http/Livewire/Dashboard.php"
      to: "Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php"
      via: "$glance->forByCurrency($user, $period) when user default is 'original'"
      pattern: "forByCurrency"
---

<objective>
Wire the multi-currency display end-to-end: per-page Flux segmented toggle on /transactions (Livewire 4 #[Url]) defaulting to the user's `default_currency_view` preference, dual-line render of FX rows in original mode (D-47), GROUP-BY-currency dashboard tiles in original mode (D-46), and an FX-rate row on the transaction detail page (D-48). This is the final Wave delivering Success Criterion 2 (toggle) and finishing UI-06 (per-row original-currency surface).

Purpose: Plan 03-02 made FX data land in the DB; plan 03-04 stored the user preference; this plan exposes the toggle UX that lets the user actually see the multi-currency information. After this plan, the phase's three ROADMAP success criteria are fully discharged.

Output: TransactionsList component with `#[Url(as: 'currency')]` + user-preference fallback, Blade view with Flux segmented control + dual-line stack, ThisPeriodAtAGlanceQuery::forByCurrency, PerCurrencyTile DTO, Dashboard branching, Money::format() locale-aware extension, transaction detail FX-rate render. All seventeen scaffolds across the three feature test files (TransactionsListCurrencyToggleTest, DashboardCurrencyModeTest, TransactionDetailFxRateTest) GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
@.planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
@.planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
@.planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
@.planning/phases/03-ics-cards-multi-currency-display/03-02-SUMMARY.md
@.planning/phases/03-ics-cards-multi-currency-display/03-04-SUMMARY.md

# Files this plan modifies — current state read once each:
@Modules/Ledger/Public/Dto/TransactionRowDto.php
@Modules/Ledger/Public/Services/TransactionListQuery.php
@Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php
@Modules/Ledger/Public/ValueObjects/Money.php
@Modules/Ledger/Internal/Http/Livewire/TransactionsList.php
@Modules/Ledger/Resources/views/livewire/transactions-list.blade.php
@Modules/Core/Internal/Http/Livewire/Dashboard.php
@Modules/Core/Resources/views/livewire/dashboard.blade.php
@Modules/Ledger/Public/Dto/DashboardSummary.php
@Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php
@Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php
@Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php

<interfaces>
Locked from 03-UI-SPEC.md "Currency-view segmented control (/transactions)":
- Component: flux:radio.group variant="segmented" with two flux:radio children
- Option 1: value="eur", label="EUR only"
- Option 2: value="original", label="Original currency"
- URL param: ?currency=eur OR ?currency=original (no param = user default)
- aria-label on the group: "Currency view"

Locked dual-currency row presentation (UI-SPEC "Currency display"):
- Primary line: native (e.g. `$12.99 USD`) — text-sm text-slate-900 14px Body
- Secondary line: settled (`€12.07 EUR`) — text-xs text-slate-500 12px Micro, mt-1 (4px xs gap)
- EUR-native row: only primary line, single-line height matches Phase 1 verbatim
- Tabular-nums on every money cell + secondary line
- Negative: leading minus before symbol — `-$12.99 USD`, never parentheses, color stays text-slate-900

Locked per-currency KPI rows (UI-SPEC "Per-currency KPI tile rows"):
- Each currency caption: 12px text-slate-500 (e.g. literal text `EUR` then `USD`)
- Three tile cards per row (In / Out / Net) — same `rounded-lg border border-slate-200 bg-white p-6` chrome as Phase 1
- Rows separate with gap-2xl (48px) — Tailwind `gap-12` if 2xl is not a configured token
- Order: alphabetical by ISO code (UI-SPEC determinism)
- Omit zero-activity currencies — a currency with all-zero in/out/net for the period does not render

Locked FX rate detail (UI-SPEC "FX rate on the transaction detail page"):
- Format: `€{rate} / {native_currency_code}` — e.g. `€0.929 / USD`
- Precision: 3 decimal places via number_format($rate, 3, '.', '')
- Orientation: EUR-per-unit-native (locked); rate value is settled_amount / amount → already in this orientation
- Label: "Effective rate" (text-sm text-slate-500 12px caption-ish, but UI-SPEC §"Typography" says Body 14px regular for the label)
- Helper below: "Includes any ICS markup." (text-slate-500 text-xs)
- Rendered ONLY when fx_rate_used IS NOT NULL

Locked Money formatter strategy (UI-SPEC "Currency display"):
- EUR → Money::format('nl_NL') → `€ 12,07` (Dutch locale)
- non-EUR → Money::format('en_US') → `$12.99` (US locale, symbol-prefix style)
- Both lines append ISO-4217 suffix after a non-breaking space: `$12.99 USD`

TransactionListQuery.recent() / fullHistory() signature (existing — verbatim):
  recent(User $user, int $daysBack = 90, ?int $cursorId = null, ?string $cursorPostedAt = null, ?string $currency = null): TransactionListPage
  fullHistory(User $user, ?int $cursorId = null, ?string $cursorPostedAt = null, ?string $currency = null): TransactionListPage

When $currency is 'EUR' the query returns rows projected with settled_amount_minor + settled_currency='EUR'.
When $currency is null the query returns rows projected with native amount_minor + currency.
The toggle's wire-value mapping:
- $this->currency = 'eur'      → query $currency = 'EUR'   → settled-EUR projection (one line per row)
- $this->currency = 'original' → query $currency = null    → native projection (dual-line stack on FX rows)
- $this->currency = ''         → fallback to user default_currency_view ('eur_only'→'eur' OR 'original'→'original')

The secondary line in original mode (FX row) requires the SETTLED pair alongside the native pair. Three approaches:
1. Extend TransactionRowDto with ?Money $secondaryAmount and project it in TransactionListQuery when $currency is null.
2. Run a second query per page for the settled pair.
3. Fetch both pairs in one query and let the Blade choose which to render.

PICK: approach 1 (extend the DTO). Cleanest; one query; one Larastan-typed surface. Detail in Task 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TransactionRowDto + TransactionListQuery to surface the SETTLED pair as a nullable secondaryAmount in original mode</name>
  <files>
    Modules/Ledger/Public/Dto/TransactionRowDto.php
    Modules/Ledger/Public/Services/TransactionListQuery.php
  </files>
  <read_first>
    Modules/Ledger/Public/Dto/TransactionRowDto.php
    Modules/Ledger/Public/Services/TransactionListQuery.php
    Modules/Ledger/Public/ValueObjects/Money.php
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
  </read_first>
  <behavior>
    - When `recent()`/`fullHistory()` is called with `$currency = 'EUR'`: every row's `amount` carries the settled-EUR Money, `secondaryAmount` is null (one-line render).
    - When `recent()`/`fullHistory()` is called with `$currency = null` (original mode): every row's `amount` carries the NATIVE Money (amount_minor / currency); for FX rows where settled_currency != currency, `secondaryAmount` carries the settled-EUR Money. For EUR-native rows, `secondaryAmount` is null.
    - DTO additions are additive only — existing Phase 1/2 call sites work without modification (the new property defaults to null).
    - Query continues to use `DatabaseManager::table()` builder (no Eloquent in this query — Larastan-strict `staticMethod.dynamicCall` invariant).
  </behavior>
  <action>
    1. Modify `Modules/Ledger/Public/Dto/TransactionRowDto.php`: append a new readonly nullable property AFTER `public readonly Money $amount`:

       public readonly ?Money $secondaryAmount = null,

       Update the class docblock with one sentence: "The optional `secondaryAmount` carries the settled-EUR amount when this row is rendered in original-currency mode AND the native currency differs from EUR — drives the two-line stack on /transactions per D-47."

    2. Modify `Modules/Ledger/Public/Services/TransactionListQuery.php`:

       a. In the SELECT clause (look for the existing `select` calls — around lines 95-110 per the existing query shape), when `$currency` is null, ALSO project `transactions.settled_amount_minor` and `transactions.settled_currency` (aliased as `secondary_minor` and `secondary_currency`). When `$currency` is 'EUR', do NOT project the secondary pair (it's not needed; the single line shows settled-EUR which is already projected via `display_minor`/`display_currency`).

       b. In the row-to-DTO mapping (look for the existing `new TransactionRowDto(...)` construction), add the secondaryAmount argument:

          secondaryAmount: ($currency === null && (string) $r->secondary_currency !== '' && (int) $r->secondary_minor !== (int) $r->display_minor)
              ? Money::ofMinor((int) $r->secondary_minor, (string) $r->secondary_currency)
              : null,

          The "!== display_minor" check filters out the EUR-native case where settled equals native — secondaryAmount stays null so the Blade renders a single line.

       c. Confirm: when `$currency === 'EUR'`, the SELECT does NOT emit secondary_* columns and the mapping passes `secondaryAmount: null` unconditionally.

       d. Add a docblock note above the projection block explaining the D-47 contract verbatim.

    3. Add a Pest unit test to either an existing TransactionListQuery test or create `Modules/Ledger/tests/Unit/Services/TransactionListQuerySecondaryAmountTest.php`:
       - it('returns secondaryAmount when row is FX and currency filter is null'): seed one FX transaction (currency='USD', amount_minor=1299, settled_currency='EUR', settled_amount_minor=1207); call recent() with currency=null; assert row's amount is `Money::ofMinor(1299, 'USD')` AND secondaryAmount is `Money::ofMinor(1207, 'EUR')`.
       - it('returns null secondaryAmount for EUR-native row when currency filter is null'): seed EUR-native; assert secondaryAmount is null.
       - it('returns null secondaryAmount when currency filter is EUR even for FX row'): seed FX; call recent(currency='EUR'); assert secondaryAmount is null AND amount is the settled-EUR Money.

       Use the existing test setup pattern from `Modules/Ledger/tests/Unit/Services/` (e.g. TransactionListQueryTest if it exists; otherwise mirror PeriodQueryTest or ThisPeriodAtAGlanceQueryTest).

       Tag with `pest()->group('phase-3')`.
  </action>
  <verify>
    <automated>vendor/bin/pest --filter='TransactionListQuery\|secondaryAmount' --stop-on-failure && vendor/bin/phpstan analyse Modules/Ledger/Public/Dto/TransactionRowDto.php Modules/Ledger/Public/Services/TransactionListQuery.php --memory-limit=1G && vendor/bin/pint --test Modules/Ledger/Public/Dto/TransactionRowDto.php Modules/Ledger/Public/Services/TransactionListQuery.php && grep -q 'secondaryAmount' Modules/Ledger/Public/Dto/TransactionRowDto.php && grep -q 'secondary_minor\\|secondaryAmount' Modules/Ledger/Public/Services/TransactionListQuery.php</automated>
  </verify>
  <acceptance_criteria>
    - TransactionRowDto has nullable `?Money $secondaryAmount = null` appended: `grep -c 'readonly ?Money \\$secondaryAmount' Modules/Ledger/Public/Dto/TransactionRowDto.php` returns 1
    - TransactionListQuery projects secondary pair conditionally: `grep -c 'secondary_minor\\|secondary_currency' Modules/Ledger/Public/Services/TransactionListQuery.php` returns at least 2
    - Three new test cases GREEN: `vendor/bin/pest --filter='secondaryAmount\\|TransactionListQuery' --stop-on-failure` exits zero
    - Larastan + Pint clean
    - Phase 1/2 regression: full TransactionListQuery test file stays GREEN; all callers of TransactionListQuery (dashboard recent panel, /transactions list — currently rendering before the toggle binding lands in Task 4) work without modification because secondaryAmount defaults to null
  </acceptance_criteria>
  <done>DTO + query carry the secondary pair on demand; FX rows have both legs accessible from one Pest assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThisPeriodAtAGlanceQuery::forByCurrency() + PerCurrencyTile DTO + DashboardCurrencyModeTest GREEN</name>
  <files>
    Modules/Ledger/Public/Dto/PerCurrencyTile.php
    Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php
    Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php
  </files>
  <read_first>
    Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php
    Modules/Ledger/Public/Dto/DashboardSummary.php
    Modules/Ledger/Public/Dto/TransactionRowDto.php
    Modules/Ledger/Public/ValueObjects/Money.php
    Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
  </read_first>
  <behavior>
    - `forByCurrency(User $user, Period $period)` returns `list<PerCurrencyTile>`, one per distinct settled_currency present in the period that has at least one non-zero net or in or out amount.
    - An EUR-only period returns exactly one tile-row (PerCurrencyTile with currency='EUR').
    - A mixed EUR + USD period returns two tile-rows in alphabetical ISO order: EUR first, USD second.
    - A period with EUR + USD + GBP returns three rows alphabetical.
    - The existing `for($user, $period)` method stays untouched — the EUR-only path is preserved verbatim.
    - PerCurrencyTile is a Spatie Data DTO with readonly properties: currency (string), inflow (Money), outflow (Money), net (Money).
  </behavior>
  <action>
    1. Create `Modules/Ledger/Public/Dto/PerCurrencyTile.php` per PATTERNS.md lines 771-797:

       <?php
       declare(strict_types=1);
       namespace Modules\Ledger\Public\Dto;

       use Modules\Ledger\Public\ValueObjects\Money;
       use Spatie\LaravelData\Data;

       /**
        * One per-currency totals row for the dashboard's original-currency
        * view (D-46). Built by ThisPeriodAtAGlanceQuery::forByCurrency() and
        * consumed by the dashboard Blade. EUR-only months collapse to a
        * single row, so a EUR-only user's view stays visually identical to
        * the EUR-only mode.
        */
       final class PerCurrencyTile extends Data
       {
           public function __construct(
               public readonly string $currency,
               public readonly Money $inflow,
               public readonly Money $outflow,
               public readonly Money $net,
           ) {}
       }

    2. Extend `Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php` with a `forByCurrency()` method ADDED ALONGSIDE the existing `for()` method (do NOT modify `for()`). Implementation per PATTERNS.md lines 729-761:

       /**
        * Original-currency mode (D-46). Returns one tile-row per distinct
        * `settled_currency` present in the period with non-zero activity.
        * EUR-only periods collapse to a single tile-row that renders
        * identically to the EUR-only mode output.
        *
        * @return list<PerCurrencyTile>
        */
       public function forByCurrency(User $user, Period $period): array
       {
           $connection = $this->db->connection();
           $rows = $connection
               ->table('transactions')
               ->where('user_id', $user->id)
               ->where('posted_at', '>=', $period->start->toDateString())
               ->where('posted_at', '<',  $period->endExclusive->toDateString())
               ->groupBy('settled_currency')
               ->havingRaw(
                   '(COALESCE(SUM(CASE WHEN settled_amount_minor > 0 THEN settled_amount_minor ELSE 0 END), 0) <> 0)
                    OR (COALESCE(SUM(CASE WHEN settled_amount_minor < 0 THEN -settled_amount_minor ELSE 0 END), 0) <> 0)'
               )
               ->selectRaw(
                   'settled_currency,
                    COALESCE(SUM(CASE WHEN settled_amount_minor > 0 THEN settled_amount_minor ELSE 0 END), 0) AS inflow_minor,
                    COALESCE(SUM(CASE WHEN settled_amount_minor < 0 THEN -settled_amount_minor ELSE 0 END), 0) AS outflow_minor,
                    COALESCE(SUM(settled_amount_minor), 0) AS net_minor'
               )
               ->orderBy('settled_currency')
               ->get();

           return $rows->map(fn (stdClass $r) => new PerCurrencyTile(
               currency: (string) $r->settled_currency,
               inflow: Money::ofMinor(self::toInt($r->inflow_minor), (string) $r->settled_currency),
               outflow: Money::ofMinor(self::toInt($r->outflow_minor), (string) $r->settled_currency),
               net: Money::ofMinor(self::toInt($r->net_minor), (string) $r->settled_currency),
           ))->all();
       }

       Use the existing private `toInt()` helper (already in the file). The `HAVING (in != 0) OR (out != 0)` clause implements the UI-SPEC "Omit zero-activity currencies" requirement. The `orderBy('settled_currency')` implements the alphabetical-determinism requirement.

    3. Make `Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php` (scaffolded in 03-01 with five failing placeholders) GREEN. Replace each failing scaffold with a real assertion:

       - it('returns one tile-row per currency in original mode for a mixed EUR/USD month'): seed one user + ICS account + 2 EUR transactions (one positive, one negative) + 1 USD transaction (negative). Build a Period covering the month. Call forByCurrency() and assert exactly 2 PerCurrencyTile entries; assert the EUR entry's net is the sum of the two EUR rows; assert the USD entry's net is the negative USD amount; assert the order is [EUR, USD].
       - it('collapses an EUR-only month to one tile-row in original mode'): seed EUR-only; assert forByCurrency() returns exactly 1 PerCurrencyTile with currency='EUR'.
       - it('omits zero-activity currencies from the original-mode tile rows'): seed only EUR rows in the period AND seed a USD row OUTSIDE the period (e.g. previous month). Call forByCurrency() for the current period; assert exactly 1 PerCurrencyTile (USD does not appear because no in-period activity).
       - it('returns one summary in eur_only mode (settled-EUR sum)'): assert the existing `for($user, $period)` method's behavior is unchanged — it returns a DashboardSummary, not an array. This is the regression guard for the EUR-only path.
       - it('orders the original-mode tile rows by settled_currency alphabetically'): seed EUR + USD + GBP. Assert the returned array's currency sequence is exactly ['EUR', 'GBP', 'USD'] (alphabetical).

       Mirror the seed-setup pattern from `Modules/Ledger/tests/Unit/Services/ThisPeriodAtAGlanceQueryTest.php` if it exists (use `grep -rn 'ThisPeriodAtAGlanceQuery' Modules/Ledger/tests/` to locate); otherwise use the User::create + Transaction::create direct-Eloquent pattern from Modules/Categorization/tests/Feature/TriagePageTest.php for the seeding, since Modules/Ledger/Models/Transaction is available cross-module.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php --stop-on-failure && grep -c 'toBe(false' Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php | grep -q '^0$' && vendor/bin/phpstan analyse Modules/Ledger/Public/Dto/PerCurrencyTile.php Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php --memory-limit=1G && vendor/bin/pint --test Modules/Ledger/Public/Dto/PerCurrencyTile.php Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php && grep -q 'forByCurrency' Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php && grep -q 'PerCurrencyTile' Modules/Ledger/Public/Dto/PerCurrencyTile.php</automated>
  </verify>
  <acceptance_criteria>
    - PerCurrencyTile DTO exists and extends Spatie\LaravelData\Data
    - forByCurrency() method added without touching for(): `git diff Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php | grep -c '^- ' | grep -q '^0$'` (no removed lines except possibly imports)
    - Five DashboardCurrencyModeTest scaffolds GREEN: `grep -c 'toBe(false' Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php` returns 0
    - Larastan level 10 strict clean
    - Phase 1/2 dashboard regression GREEN: `vendor/bin/pest --filter='Dashboard\\|ThisPeriodAtAGlance' --stop-on-failure` exits zero (the existing for() method is untouched, so its tests stay GREEN)
  </acceptance_criteria>
  <done>Dashboard query supports both modes; PerCurrencyTile typed; five new Pest cases GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Money formatter — locale-aware EUR + ISO-symbol non-EUR with ISO suffix</name>
  <files>
    Modules/Ledger/Public/ValueObjects/Money.php
  </files>
  <read_first>
    Modules/Ledger/Public/ValueObjects/Money.php
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
    .planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
  </read_first>
  <behavior>
    - `Money::ofMinor(1299, 'USD')->format()` returns `$12.99 USD` (en_US locale + space + ISO suffix).
    - `Money::ofMinor(1207, 'EUR')->format()` returns `€ 12,07 EUR` (nl_NL locale + space + ISO suffix). The Dutch locale's space-after-symbol style is the default; UI-SPEC accepts either `€ 12,07` or `€12,07` so the executor follows whatever `MoneyLocaleFormatter` emits on the runtime intl extension.
    - Negative: `Money::ofMinor(-1299, 'USD')->format()` returns `-$12.99 USD` (leading minus before symbol).
    - The suffix uses a regular space (ASCII 0x20), not a non-breaking space — UI-SPEC §"Currency display" mentions both styles; the Pest assertions are byte-for-byte and the executor picks one (recommended: regular space for simpler testing).
    - `format()` is a NEW method or extends the existing format method on the Money VO — Phase 1 may have a `format()` taking a locale parameter (`format('nl_NL')`). If so, KEEP that signature for backward-compat AND add an overload / parameterless variant that selects the locale based on the currency code.
    - The ISO 3-char suffix is always present.
  </behavior>
  <action>
    1. Read `Modules/Ledger/Public/ValueObjects/Money.php` fully. Identify the current `format(string $locale = ?)` signature.

    2. Add (or update) a `format(?string $locale = null): string` method that:
       a. If `$locale === null`, picks `'nl_NL'` for EUR and `'en_US'` for every other currency. This is the new default behavior introduced by Phase 3.
       b. Otherwise, uses the explicit `$locale` argument (preserves existing Phase 1 call sites like `$money->format('nl_NL')`).
       c. Calls `Brick\Money\Formatter\MoneyLocaleFormatter` (or whatever the existing implementation uses) with the chosen locale.
       d. Appends ` <ISO_CODE>` (space + 3-char ISO) after the formatted body.

       Implementation sketch:

       public function format(?string $locale = null): string
       {
           $resolved = $locale ?? ($this->currency() === 'EUR' ? 'nl_NL' : 'en_US');
           $body = $this->inner->formatTo($resolved);
           return $body . ' ' . $this->currency();
       }

       (Adjust `$this->inner` / `$this->currency()` to match the existing field/method names in the file. If `formatTo` raises on absent intl, fall back to a hand-rolled `ISOCurrencyProvider->getCurrency($code)->getSymbol() . number_format(...)` — wrap that in a try/catch keyed off `Throwable` from `extension_loaded('intl')`.)

    3. Phase 1/2 callers pass `format('nl_NL')` explicitly — those keep working unchanged. The new `format()` (no-arg) callsites land in plan 03-05's Blade view (Task 4/5).

    4. Add Pest tests at `Modules/Ledger/tests/Unit/ValueObjects/MoneyFormatTest.php` (or extend existing money tests):
       - it('formats EUR with nl_NL locale and EUR suffix by default'): `Money::ofMinor(1207, 'EUR')->format()` → contains `€` AND ends with ` EUR` AND contains the digits `12,07` (Dutch decimal).
       - it('formats USD with en_US locale and USD suffix by default'): `Money::ofMinor(1299, 'USD')->format()` → contains `$` AND ends with ` USD` AND contains `12.99`.
       - it('formats negative USD with leading minus'): `Money::ofMinor(-1299, 'USD')->format()` → starts with `-` (or contains `-$` — UI-SPEC locks "leading minus before symbol", so the BIT-FOR-BIT assertion may need to be `-$12.99 USD`; verify against the actual formatter output and lock the expected string in the test).
       - it('honours an explicit locale argument for backward compat'): `Money::ofMinor(1207, 'EUR')->format('en_US')` → renders in en_US style (different from nl_NL output).
  </action>
  <verify>
    <automated>vendor/bin/pest --filter='MoneyFormat\\|Money formatter' --stop-on-failure && vendor/bin/phpstan analyse Modules/Ledger/Public/ValueObjects/Money.php --memory-limit=1G && vendor/bin/pint --test Modules/Ledger/Public/ValueObjects/Money.php && php -m | grep -q 'intl' && grep -q 'function format' Modules/Ledger/Public/ValueObjects/Money.php</automated>
  </verify>
  <acceptance_criteria>
    - Money::format() with no argument selects nl_NL for EUR and en_US otherwise: documented in the docblock + verified by the two unit tests
    - Existing format('nl_NL') callsites still work — Phase 1/2 dashboard tests pass
    - All four new MoneyFormatTest cases GREEN
    - ext-intl is enabled on the runtime: `php -m | grep -q '^intl$'` (this is the project's standing assumption; if absent, the executor MUST flag this in the SUMMARY and the fallback hand-rolled formatter is implemented per RESEARCH.md Pattern 4 caveat)
    - Larastan + Pint clean
  </acceptance_criteria>
  <done>Money VO yields locale-aware + ISO-suffixed strings; four formatter tests GREEN; Phase 1/2 callers unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: TransactionsList #[Url] currency property + mount() user-preference fallback + Blade Flux segmented + dual-line stack — TransactionsListCurrencyToggleTest GREEN</name>
  <files>
    Modules/Ledger/Internal/Http/Livewire/TransactionsList.php
    Modules/Ledger/Resources/views/livewire/transactions-list.blade.php
    Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php
  </files>
  <read_first>
    Modules/Ledger/Internal/Http/Livewire/TransactionsList.php
    Modules/Ledger/Resources/views/livewire/transactions-list.blade.php
    Modules/Ledger/Public/Services/TransactionListQuery.php
    Modules/Ledger/Public/Dto/TransactionRowDto.php
    Modules/Core/Models/User.php
    Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
  </read_first>
  <behavior>
    - The component has a `#[Url(as: 'currency', except: '')] public string $currency = '';` property.
    - On mount, when `$currency === ''`, the component reads the user's `default_currency_view` ('eur_only' → sets $currency='eur'; 'original' → sets $currency='original'). When the URL already has `?currency=`, the URL value wins.
    - `render()` maps the wire-property to the query's `?string $currency` argument:
      - `$currency === 'eur'` → query gets `'EUR'` → settled-EUR single-line projection
      - `$currency === 'original'` → query gets `null` → native projection with secondaryAmount on FX rows
      - `$currency === ''` (only at mount before fallthrough runs — defensive) → query gets `null`
    - The Blade adds a `flux:radio.group variant="segmented"` next to the existing full-history toggle button; options `'eur'` (label "EUR only") and `'original'` (label "Original currency").
    - In original mode (`$currency === 'original'`), FX rows render the two-line stack: native primary (text-sm text-slate-900), settled secondary (mt-1 text-xs text-slate-500). EUR-native rows render one line.
    - In EUR mode (`$currency === 'eur'`), every row renders one line (settled-EUR).
    - URL stays clean when on the default — `except: ''` drops `?currency=` from the URL when the property is the empty string.
  </behavior>
  <action>
    1. Modify `Modules/Ledger/Internal/Http/Livewire/TransactionsList.php`:

       a. Add `use Livewire\Attributes\Url;` to the imports.
       b. Add the new property between `$cursorPostedAt` and the methods:

          /**
           * Currency view mode. Empty string '' = use the user's
           * default_currency_view preference. 'eur' = project the settled-EUR
           * pair (one line). 'original' = project the native pair with a
           * settled-EUR secondary line for FX rows (D-44 + D-47).
           */
          #[Url(as: 'currency', except: '')]
          public string $currency = '';

       c. Add a `mount()` method (or extend the existing one if it exists — the current file may not have a mount). The method uses method-level DI:

          public function mount(CurrentUser $currentUser): void
          {
              if ($this->currency === '') {
                  $pref = $currentUser->user()->default_currency_view;
                  $this->currency = $pref === 'eur_only' ? 'eur' : 'original';
              }
          }

          This implements the D-44 default fallthrough — `mount()` reads the user preference exactly once when the URL has no override.

       d. Modify `render()` to map the wire-property to the query's `$currency` argument and pass `$currency` through to the Blade:

          $queryCurrency = $this->currency === 'eur' ? 'EUR' : null;

          $page = $this->fullHistory
              ? $listQuery->fullHistory($user, cursorId: $this->cursorId, cursorPostedAt: $this->cursorPostedAt, currency: $queryCurrency)
              : $listQuery->recent($user, daysBack: 90, cursorId: $this->cursorId, cursorPostedAt: $this->cursorPostedAt, currency: $queryCurrency);

          return $views->make('ledger::livewire.transactions-list', [
              'page' => $page,
              'fullHistory' => $this->fullHistory,
              'currency' => $this->currency,
          ]);

       e. Add `use Modules\Core\Public\Contracts\CurrentUser;` if not already present.

       f. Add `pest()->group('phase-3')` at the top of the Pest test file (Task 4 step 4 below) if not inherited from Plan 03-01's registration.

    2. Modify `Modules/Ledger/Resources/views/livewire/transactions-list.blade.php`:

       a. Locate the page header block (currently contains the "Show full history" toggle button). Add the Flux segmented control BEFORE the existing toggle button:

          <flux:radio.group wire:model.live="currency" variant="segmented" aria-label="Currency view">
              <flux:radio value="eur"      label="EUR only" />
              <flux:radio value="original" label="Original currency" />
          </flux:radio.group>

          Wrap the segmented control + the existing button in a single right-aligned flex container with `flex items-center gap-2` — matches the existing header layout.

       b. Locate the amount cell in the row table (currently a single `{{ $fmt($row->amount) }}` or equivalent). Replace it with a conditional dual-line stack:

          <td class="px-4 py-2 text-right text-slate-900" style="font-variant-numeric: tabular-nums;">
              <span class="block text-sm text-slate-900">{{ $row->amount->format() }}</span>
              @if ($currency === 'original' && $row->secondaryAmount !== null)
                  <span class="mt-1 block text-xs text-slate-500">{{ $row->secondaryAmount->format() }}</span>
              @endif
          </td>

          Replace any pre-existing `$fmt` closure call with `$row->amount->format()` — Task 3's locale-aware default makes the closure unnecessary. If a Blade-level `$fmt` closure exists in the file for date formatting or other purposes, keep it; only swap the money-rendering call.

       c. Verify the `$currency` variable arrives in the Blade context — it does, via `render()`'s view-data array in step 1d.

    3. Make `Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php` (seven scaffolds from plan 03-01) GREEN. Required assertions per scaffold:

       - it('defaults the currency property to the user default_currency_view preference when no URL override is present'):
         - Seed a User with default_currency_view='original' + actingAs.
         - Livewire::test(TransactionsList::class) — assert $currency === 'original' AFTER mount.

       - it('overrides the user preference when ?currency=eur is present in the URL'):
         - Seed a User with default_currency_view='original'.
         - Livewire::withQueryParams(['currency' => 'eur'])->test(TransactionsList::class) — assert $currency === 'eur'.

       - it('overrides the user preference when ?currency=original is present in the URL'):
         - Seed a User with default_currency_view='eur_only'.
         - Livewire::withQueryParams(['currency' => 'original'])->test(TransactionsList::class) — assert $currency === 'original'.

       - it('renders two lines for a foreign-currency row in original mode'):
         - Seed an ICS account + one USD transaction (amount_minor=1299, currency='USD', settled_amount_minor=1207, settled_currency='EUR').
         - Set $currency='original' on the Livewire component, render, assert the rendered HTML contains both the native USD-formatted body AND the settled EUR-formatted body.

       - it('renders one line for a foreign-currency row in eur mode'):
         - Same fixture. Set $currency='eur'. Assert HTML contains the EUR body AND does NOT contain the USD body (the row is projected as settled-EUR).

       - it('renders one line for an EUR-native row in original mode'):
         - Seed an EUR-native transaction. Set $currency='original'. Assert the secondaryAmount is null in the page DTO row AND the HTML does NOT contain a second line for that row.

       - it('keeps the URL clean when the toggle is on the default value'):
         - The Livewire #[Url(except: '')] semantic: when $currency='', no ?currency= param is in the URL. Assert via the Livewire test API's URL inspection (or by checking the component's currentUrl()).
         - Note: the mount() fallback sets $currency to 'eur' or 'original' immediately — so $currency=='' is rarely the runtime state. The "clean URL" assertion verifies that when the user has default_currency_view='eur_only' AND visits /transactions with no query, the URL stays /transactions (no ?currency=eur appended). This is the `except: ''` semantic applied to the empty-string side.

       Use `Livewire\Livewire::test(...)`, `Livewire\Livewire::withQueryParams([...])->test(...)`, and `->assertSeeHtml(...)` / `->assertSet(...)` per the existing Livewire test patterns. Use `Modules\Ledger\Models\Transaction::create([...])` for direct seeding (Phase 1/2 pattern).

    4. After driving the scaffolds GREEN, run the full phase-3 + transactions regression: `vendor/bin/pest --group=phase-3 Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php --stop-on-failure`.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php --stop-on-failure && grep -c 'toBe(false' Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php | grep -q '^0$' && vendor/bin/phpstan analyse Modules/Ledger/Internal/Http/Livewire/TransactionsList.php --memory-limit=1G && vendor/bin/pint --test Modules/Ledger/Internal/Http/Livewire/TransactionsList.php Modules/Ledger/Resources/views/livewire/transactions-list.blade.php Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php && grep -q "#\\[Url(as: 'currency'" Modules/Ledger/Internal/Http/Livewire/TransactionsList.php && grep -q 'flux:radio.group' Modules/Ledger/Resources/views/livewire/transactions-list.blade.php && grep -q 'variant="segmented"' Modules/Ledger/Resources/views/livewire/transactions-list.blade.php && grep -q 'secondaryAmount' Modules/Ledger/Resources/views/livewire/transactions-list.blade.php && ! grep -E 'auth\\(\\)|Auth::user' Modules/Ledger/Internal/Http/Livewire/TransactionsList.php</automated>
  </verify>
  <acceptance_criteria>
    - Component has #[Url(as: 'currency', except: '')] on a string property: grep returns a match
    - mount() injects CurrentUser and applies the user-preference fallback: grep returns a match for `default_currency_view`
    - render() maps `'eur'` → `'EUR'` and other values → `null` for the query: grep returns a match
    - Blade has flux:radio.group with variant="segmented" and aria-label="Currency view": grep all three match
    - Blade has the conditional secondary-line render keyed off `$currency === 'original'` and `$row->secondaryAmount !== null`: grep returns a match
    - Seven scaffolds GREEN: `grep -c 'toBe(false' Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php` returns 0
    - Larastan + Pint clean
    - No facade/helper slippage in TransactionsList.php
    - Phase 1/2 transactions-list regression: `vendor/bin/pest --filter='TransactionsList\\|TransactionList'` exits zero (the existing test file — if it exists — stays GREEN; the toggle is additive)
    - Full phase-3 suite (plans 01-05): `vendor/bin/pest --group=phase-3 --stop-on-failure` exits zero
  </acceptance_criteria>
  <done>Per-page currency toggle live with URL sync + user-preference fallback; dual-line stack renders on FX rows in original mode; seven scaffolds GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Dashboard branching — original mode renders per-currency tile rows; EUR-only mode unchanged</name>
  <files>
    Modules/Core/Internal/Http/Livewire/Dashboard.php
    Modules/Core/Resources/views/livewire/dashboard.blade.php
  </files>
  <read_first>
    Modules/Core/Internal/Http/Livewire/Dashboard.php
    Modules/Core/Resources/views/livewire/dashboard.blade.php
    Modules/Ledger/Public/Services/ThisPeriodAtAGlanceQuery.php
    Modules/Ledger/Public/Dto/PerCurrencyTile.php
    Modules/Core/Models/User.php
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
  </read_first>
  <behavior>
    - Dashboard.render() reads $currentUser->user()->default_currency_view. When 'eur_only', it computes `$summary = $glance->for(...)` (current Phase 1/2 path, untouched) and passes `'tiles' => null` to the view.
    - When 'original', it ALSO computes `$tiles = $glance->forByCurrency(...)` and passes `'tiles' => $tiles`. It STILL computes `$summary` because the existing dashboard rendering uses `$summary` for the top-categories panel and the recent-transactions panel — those are EUR-only paths that stay live in original mode (the per-currency split applies only to the KPI tiles).
    - The Blade view branches on `$tiles === null`: if null, render the existing three-tile single row (Phase 1 verbatim); if non-null, render one labeled three-tile row per PerCurrencyTile, separated by a 2xl gap.
    - Each per-currency row has a 12px slate-500 caption above the three tiles (e.g. literal text `EUR`, then `USD`).
    - The per-currency row of tiles reuses the existing card chrome (rounded-lg border-slate-200 bg-white p-6) verbatim.
  </behavior>
  <action>
    1. Modify `Modules/Core/Internal/Http/Livewire/Dashboard.php` render() method:

       public function render(
           CurrentUser $currentUser,
           PeriodQuery $periods,
           ThisPeriodAtAGlanceQuery $glance,
           ViewFactory $views,
       ): View {
           $user = $currentUser->user();
           $period = $this->resolvePeriod($periods);

           $summary = $glance->for($user, $period);

           $tiles = null;
           if ($user->default_currency_view === 'original') {
               $tiles = $glance->forByCurrency($user, $period);
           }

           return $views->make('core::livewire.dashboard', [
               'summary' => $summary,
               'tiles' => $tiles,
           ]);
       }

       Do not touch any other Dashboard.php method (previousPeriod / nextPeriod / today / resolvePeriod stay verbatim).

    2. Modify `Modules/Core/Resources/views/livewire/dashboard.blade.php` to add the conditional per-currency rendering:

       a. Locate the existing KPI tile section (likely a `<section>` containing three `<div>` cards for In / Out / Net using `$summary->inflow`, `$summary->outflow`, `$summary->net`).
       b. Wrap the entire section in a conditional that branches on `$tiles`:

          @if ($tiles === null)
              {{-- EUR-only view: existing single row of three tiles --}}
              <section class="grid grid-cols-1 gap-md md:grid-cols-3" aria-label="This period totals">
                  <!-- existing In / Out / Net card markup VERBATIM -->
              </section>
          @else
              {{-- Original-currency view (D-46): one row of three tiles per currency --}}
              <div class="space-y-12">     {{-- gap-2xl between currency rows --}}
                  @foreach ($tiles as $tile)
                      <section aria-label="This period totals — {{ $tile->currency }}" class="space-y-2">
                          <h2 class="text-xs uppercase tracking-wide text-slate-500">{{ $tile->currency }}</h2>
                          <div class="grid grid-cols-1 gap-md md:grid-cols-3">
                              <div class="rounded-lg border border-slate-200 bg-white p-6">
                                  <p class="text-xs uppercase tracking-wide text-slate-500">In</p>
                                  <p class="mt-2 text-3xl font-semibold text-slate-900" style="font-variant-numeric: tabular-nums;">{{ $tile->inflow->format() }}</p>
                              </div>
                              <div class="rounded-lg border border-slate-200 bg-white p-6">
                                  <p class="text-xs uppercase tracking-wide text-slate-500">Out</p>
                                  <p class="mt-2 text-3xl font-semibold text-slate-900" style="font-variant-numeric: tabular-nums;">{{ $tile->outflow->format() }}</p>
                              </div>
                              <div class="rounded-lg border border-slate-200 bg-white p-6">
                                  <p class="text-xs uppercase tracking-wide text-slate-500">Net</p>
                                  <p class="mt-2 text-3xl font-semibold text-slate-900" style="font-variant-numeric: tabular-nums;">{{ $tile->net->format() }}</p>
                              </div>
                          </div>
                      </section>
                  @endforeach
              </div>
          @endif

          Reuse the existing card chrome exactly — do NOT introduce new colors, padding, or borders. Only the wrapping + caption is new.

    3. The top-categories panel and recent-transactions panel below the KPI tiles use `$summary` regardless of mode — leave those Blade sections untouched. UI-SPEC §"Dashboard" focus-point note confirms only the KPI-tile section needs branching.

    4. Add a Pest feature test at `Modules/Core/tests/Feature/DashboardOriginalModeRenderTest.php` (or extend an existing dashboard feature test):

       - it('renders three tiles labeled by currency caption in original mode'):
         - Seed User with default_currency_view='original'; seed one EUR transaction; visit /; assert HTML contains the literal `>EUR<` caption text AND the In/Out/Net labels.
       - it('renders the original-mode tile rows in alphabetical order'):
         - Seed User original; seed EUR + USD + GBP transactions; visit /; assert the rendered HTML has the `>EUR<` caption appearing before `>GBP<` which appears before `>USD<` (use string-position assertions: `expect(strpos($html, '>EUR<'))->toBeLessThan(strpos($html, '>GBP<'))->toBeLessThan(strpos($html, '>USD<'))`).
       - it('renders the single Phase 1 layout when default_currency_view is eur_only'):
         - Seed eur_only; visit /; assert HTML does NOT contain a `>USD<` caption AND assert it contains exactly ONE In tile (single-row layout).
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Core/tests/Feature/DashboardOriginalModeRenderTest.php --stop-on-failure && vendor/bin/phpstan analyse Modules/Core/Internal/Http/Livewire/Dashboard.php --memory-limit=1G && vendor/bin/pint --test Modules/Core/Internal/Http/Livewire/Dashboard.php Modules/Core/Resources/views/livewire/dashboard.blade.php && grep -q 'forByCurrency' Modules/Core/Internal/Http/Livewire/Dashboard.php && grep -q '@if (\$tiles' Modules/Core/Resources/views/livewire/dashboard.blade.php</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.render() conditionally calls forByCurrency: grep returns a match
    - Dashboard Blade has the @if($tiles === null) / @else branch: grep returns a match
    - Three new dashboard render tests GREEN
    - Existing Phase 1/2 dashboard tests stay GREEN: `vendor/bin/pest --filter='Dashboard' --stop-on-failure` exits zero
    - Larastan + Pint clean
    - No new colors / new chrome introduced — `git diff Modules/Core/Resources/views/livewire/dashboard.blade.php` shows additive lines and no Tailwind class outside the existing palette
  </acceptance_criteria>
  <done>Dashboard branches cleanly between EUR-only and original modes; D-46 collapse-to-single-row-for-EUR-only behavior intact; three new tests GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Transaction detail FX-rate row (D-48) — TransactionDetailFxRateTest GREEN</name>
  <files>
    Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php
  </files>
  <read_first>
    Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php
    Modules/Ledger/Internal/Http/Livewire
    Modules/Ledger/Resources/views/livewire
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
    Modules/Ledger/Models/Transaction.php
  </read_first>
  <behavior>
    - When the user opens a transaction's detail page (e.g. /transactions/{id} or whatever route Phase 1/2 introduced), if fx_rate_used is non-null, the page renders an "Effective rate" row showing `€{rate} / {native_currency_code}` with 3 decimal places and the helper text "Includes any ICS markup." below.
    - When fx_rate_used is NULL, no such row renders.
    - The orientation is EUR-per-unit-native — the rate value stored in fx_rate_used is already in this orientation (settled_amount / amount = EUR / native).
    - The rendering MUST be tested whether or not the detail page itself exists in Phase 1/2: if the detail page does NOT exist yet, this task creates the minimum Livewire SFC + route + Blade to host the FX-rate row. PATTERNS.md "No Analog Found" explicitly flags this as the one Phase 3 surface that might land here in full.
  </behavior>
  <action>
    1. **Discover detail-page state.** Search the codebase for an existing transaction-detail surface:
       - `grep -rn "transactions/{id}\\|transactions\\.show\\|TransactionDetail" Modules/Ledger Modules/Core 2>/dev/null` — locate any existing route, component, or Blade.
       - If a detail page exists, this task extends it with one new `<dl>` row. If not, this task creates a minimal one.

    2. **If the detail page does NOT exist:**

       a. Create `Modules/Ledger/Internal/Http/Livewire/TransactionDetail.php` as a final Livewire Component:
          - Method-DI only (no constructor); CurrentUser injected on mount/render.
          - Public property `public int $transactionId;` populated by route-model binding via `mount(int $transactionId, CurrentUser $currentUser)`.
          - `render(...)` loads the Transaction by id scoped to the current user via Eloquent (or DatabaseManager builder per phpstan-strict invariant), 404s if not found, renders `ledger::livewire.transaction-detail` with the row data.
          - The view receives: `$transaction` (the Eloquent model OR a typed DTO with id, currency, amount_minor, settled_currency, settled_amount_minor, fx_rate_used, posted_at, counterparty_name, description).

       b. Create `Modules/Ledger/Resources/views/livewire/transaction-detail.blade.php` with the calm-aesthetic shell from dashboard + a `<dl>` block displaying the row's main metadata. Add the conditional FX-rate row:

          @if ($transaction->fx_rate_used !== null)
              <div class="space-y-1" data-testid="fx-rate-row">
                  <dt class="text-sm text-slate-500">Effective rate</dt>
                  <dd class="text-sm text-slate-900" style="font-variant-numeric: tabular-nums;">
                      €{{ number_format((float) $transaction->fx_rate_used, 3, '.', '') }} / {{ $transaction->currency }}
                  </dd>
                  <p class="text-xs text-slate-500">Includes any ICS markup.</p>
              </div>
          @endif

          Note: `(float)` cast on fx_rate_used is needed for number_format, but the underlying value is the decimal(18,8) string from BigDecimal — the float cast is for display-only and never re-enters the money pipeline. Add a `// phpcs:ignore` / `// @phpstan-ignore-line` comment ONLY if the NoFloatMoneyArchTest flags this — but the arch test should NOT match `(float)` in Blade files; verify with `vendor/bin/pest --filter='NoFloatMoney' --stop-on-failure` after Task 6 commits.

       c. Register the route in `Modules/Ledger/Routes/web.php` (create if not present): `Route::get('/transactions/{transactionId}', TransactionDetail::class)->name('transactions.show');` under the existing auth middleware group.

    3. **If the detail page DOES exist:** locate the existing Blade view and add the conditional FX-rate `<dl>` row block (step 2b's snippet) at an appropriate position (likely beneath the amount row). Do NOT modify other parts of the existing detail view.

    4. Make `Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php` (four scaffolds from plan 03-01) GREEN:

       - it('renders the Effective rate line when fx_rate_used is populated'):
         - Seed a User + ICS account + Transaction with currency='USD', amount_minor=1299, settled_amount_minor=1207, settled_currency='EUR', fx_rate_used='0.92917629'.
         - GET /transactions/{id}; assertOk; assertSee('Effective rate', false); assertSee('USD', false).

       - it('omits the Effective rate line when fx_rate_used is NULL'):
         - Seed an EUR-native Transaction with fx_rate_used=null.
         - GET /transactions/{id}; assertOk; assertDontSee('Effective rate', false).

       - it('formats the rate as EUR-per-unit-native with 3 decimal places'):
         - Same FX seed as test 1.
         - Assert the rendered HTML contains the exact substring `€0.929 / USD` (3 decimals from the rate 0.92917629 truncated by number_format). Use `assertSee('€0.929 / USD', false)`.

       - it('renders the Includes any ICS markup helper text below the rate'):
         - Same FX seed.
         - assertSee('Includes any ICS markup.', false).

       Each test uses the standard Phase 1/2 actingAs($user) + GET pattern. If the detail page is a Livewire SFC, use Livewire::test(TransactionDetail::class, ['transactionId' => $tx->id])->assertSee(...).
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php --stop-on-failure && grep -c 'toBe(false' Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php | grep -q '^0$' && vendor/bin/phpstan analyse Modules/Ledger --memory-limit=1G && vendor/bin/pint --test Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php && vendor/bin/pest --filter='NoFloatMoney' --stop-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - All four TransactionDetailFxRateTest scaffolds GREEN: `grep -c 'toBe(false' Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php` returns 0
    - The detail page renders (whether it existed before or was created in step 2): GET /transactions/{id} returns 200 for an authenticated user
    - When fx_rate_used IS NOT NULL, the rendered HTML contains the locked strings: `€0.929 / USD` AND `Effective rate` AND `Includes any ICS markup.`
    - When fx_rate_used IS NULL, the HTML does NOT contain `Effective rate`
    - NoFloatMoneyArchTest stays GREEN — the Blade-level `(float)` cast on fx_rate_used does NOT trip the arch test (the test scope is Modules/Ingestion + Modules/Import + Modules/Ledger PHP files, not Blade)
    - Larastan + Pint clean
    - Full phase-3 suite GREEN: `vendor/bin/pest --group=phase-3 --stop-on-failure` exits zero
    - Phase 1/2 regression intact: `vendor/bin/pest --filter='Transaction\\|Dashboard\\|Auth' --stop-on-failure` exits zero
  </acceptance_criteria>
  <done>FX-rate detail row renders with the locked format (`€{0.929} / USD`) + helper text only when fx_rate_used is non-null; four scaffolds GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ?currency= URL param → Livewire property | `#[Url(as: 'currency')]` accepts arbitrary strings; the component must defensively map only known values |
| Livewire property → TransactionListQuery $currency arg | The query's signature accepts ?string $currency; an unknown value silently filters to "no match" (empty list) |
| User detail page → cross-user transaction read | GET /transactions/{id} must scope by user_id; never expose another user's row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | currency wire-property | mitigate | render() explicitly maps only `'eur'` → `'EUR'`, EVERY other value → null. A `?currency=garbage` URL silently falls into the "original" path (native projection). No invalid value reaches the SQL layer. |
| T-03-05-02 | Information Disclosure | /transactions/{id} cross-user leak | mitigate | TransactionDetail component (or existing detail page) scopes by `$currentUser->user()->id` via `where('user_id', $user->id)` or Eloquent `firstOrFail` with the same scope (Phase 1/2 pattern). The new test `it('omits the Effective rate line when fx_rate_used is NULL')` indirectly verifies the scoping by depending on actingAs(). Add an explicit cross-user test only if the existing detail page does not have one already — verified during the codebase discovery step in Task 6 step 1. |
| T-03-05-03 | Tampering | fx_rate_used display value | accept | The (float) cast in Blade for number_format is display-only; the canonical store stays as decimal(18,8) string. The float cast is bounded to 18-digit precision plus the formatter's 3-decimal cap, which never re-enters the money pipeline. |
| T-03-05-04 | Information Disclosure | secondaryAmount on /transactions in eur mode | mitigate | TransactionListQuery only projects secondary_* columns when $currency is null (original mode). In eur mode the SELECT does not include the secondary pair, so the API surface accidentally exposing the other-currency leg is impossible. |
| T-03-05-05 | DoS | forByCurrency() on a year-long period | accept | The GROUP BY settled_currency aggregates over indexed columns (transactions has indexes on user_id + posted_at + settled_currency per Phase 1/2). For a single-user app with statement-sized history, the query completes well under 50ms on SQLite. No mitigation required in Phase 3. |
| T-03-05-06 | Spoofing | Flux segmented control CSRF | mitigate | Livewire-native CSRF on every wire:model.live update. Phase 1 invariant — no new mitigation needed. |
</threat_model>

<verification>
- `vendor/bin/pest --group=phase-3 --stop-on-failure` exits zero (all 35+ phase-3 scaffolds GREEN)
- `vendor/bin/pest --memory-limit=1G` (full suite) exits zero — no regression anywhere
- `vendor/bin/phpstan analyse --memory-limit=1G` exits zero on the entire codebase (Larastan level 10 strict)
- `vendor/bin/pint --test` exits zero on every modified file
- Manual smoke check: visit /transactions on a fresh checkout with the ICS fixture imported, observe the segmented toggle, switch between EUR / Original, confirm the dual-line stack on the USD row in original mode
- Manual smoke check: visit / with a User whose default_currency_view='original', observe per-currency tile rows
- Manual smoke check: visit /transactions/{id} of the USD transaction, observe the Effective rate row with `€0.929 / USD`
- All architecture invariants GREEN: BoundaryArchTest, NoFloatMoneyArchTest, MoneyColumnsArchTest, UserIdColumnArchTest, NoExtImapTest, ModulePrioritiesArchTest
</verification>

<success_criteria>
1. /transactions has a working Flux segmented toggle (EUR only / Original currency) bound to a URL param.
2. The toggle falls back to the user's default_currency_view preference when no URL override is present (D-44 Settings round-trip closed).
3. Foreign-currency rows show two lines in original mode (D-47); EUR-native rows show one line; toggle to EUR shows every row on one line projected as settled-EUR.
4. Dashboard shows one labeled tile-row per currency in original mode (D-46); EUR-only months collapse to a single row.
5. Transaction detail page surfaces fx_rate_used as `€0.929 / USD` with helper text `Includes any ICS markup.` (D-48) only when non-null.
6. ROADMAP Phase 3 success criteria 1, 2, 3 all TRUE.
7. No Phase 1/2 regression — every prior test stays GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ics-cards-multi-currency-display/03-05-SUMMARY.md` documenting:
- Whether the transaction detail page existed before Task 6 or was created in this plan.
- The exact format of `Money::format()` output for EUR + USD on the runtime intl extension (snapshot the precise strings — e.g. `€ 12,07 EUR` vs `€12,07 EUR` — and lock them in the SUMMARY so any future drift is flagged).
- Whether `ext-intl` was present on the runtime; if absent, the hand-rolled fallback formatter implementation choice.
- Any UI-SPEC copy that was paraphrased rather than rendered verbatim (should be zero — UI-SPEC explicitly locks every string in this plan).
- The final phase-3 group test count and runtime.
</output>
