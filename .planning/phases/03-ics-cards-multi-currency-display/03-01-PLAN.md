---
phase: 03-ics-cards-multi-currency-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - composer.lock
  - .gitignore
  - README.md
  - tests/Pest.php
  - scripts/anonymize_ics_text.php
  - Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt
  - Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md
  - Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf
  - Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.md
  - Modules/Ingestion/tests/Unit/Adapters/Ics/IcsPdfAdapterTest.php
  - Modules/Ingestion/tests/Unit/Adapters/Ics/PdfTextExtractorTest.php
  - Modules/Ingestion/tests/Unit/Adapters/Ics/IcsAmountParserTest.php
  - Modules/Ingestion/tests/Unit/Adapters/Ics/IcsDateParserTest.php
  - Modules/Import/tests/Feature/IcsPdfImportTest.php
  - Modules/Core/tests/Feature/SettingsPageTest.php
  - Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php
  - Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php
  - Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php
  - tests/Feature/AnonymisedFixtureSweepTest.php
autonomous: false
requirements:
  - ING-04
  - LED-03
  - MC-02
  - UI-06

must_haves:
  truths:
    - "spatie/pdf-to-text is installed via composer and resolves to a stable version (D-31a)"
    - "README documents the brew install poppler prerequisite plus the pdftotext -v check (D-31a)"
    - ".gitignore lists local/ so raw PDF statements never enter the repo (D-32a)"
    - "Anonymisation is OUR job — Wave 0 extracts the raw PDF to text via pdftotext -layout, runs an in-repo redaction pass, and commits the redacted .txt fixture; the raw .pdf stays under local/ics/ (D-32a)"
    - "The committed redacted fixture contains zero 12-digit-or-longer runs, zero IBAN-shaped strings other than a deterministic placeholder, and the literal KAARTHOUDER placeholder for the cardholder name (D-32a, D-37, D-56)"
    - "A companion fixture-record markdown documents extraction command, anchor tokens, per-page noise, FX-row visual shape, source_ref availability, markup separability, statement-summary tokens, Dutch date formats, Dutch amount formats, and masked-card metadata schema (D-34, D-35, D-40, D-49, D-51, D-52, D-53, D-56)"
    - "Phase-3 Pest group convention is documented in tests/Pest.php so vendor/bin/pest --group=phase-3 runs every Phase 3 test"
    - "Failing test scaffolds for every Phase 3 requirement (ING-04, LED-03, MC-02, UI-06) are committed in a Red state so Waves 2..N drive them green"
    - "An anonymisation-sweep Pest test guards the committed .txt fixture against accidental re-import of an un-redacted statement"
    - "A tiny synthetic anonymised ics-sample-tiny.pdf is committed so the Wave 2 idempotency-contract test can exercise the real upload path through pdftotext without depending on the human-provided raw export"
  artifacts:
    - path: "composer.json"
      provides: "Declares spatie/pdf-to-text as a runtime dependency (D-31a)"
      contains: "spatie/pdf-to-text"
    - path: ".gitignore"
      provides: "Carves out local/ so the raw PDF stays out of the repo (D-32a)"
      contains: "/local/"
    - path: "README.md"
      provides: "Local development setup line pointing at brew install poppler plus pdftotext -v check (D-31a)"
      contains: "brew install poppler"
    - path: "scripts/anonymize_ics_text.php"
      provides: "Re-runnable PHP CLI script that consumes an extracted .txt and writes a redacted .txt; documents the redaction protocol (D-32a)"
    - path: "Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt"
      provides: "Anonymised pdftotext -layout output of a real ICS PDF statement — anchor tokens, FX-row shape, Dutch tokens preserved verbatim, all PII redacted"
    - path: "Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md"
      provides: "Empirical fixture record (extraction command, anchor tokens, per-page noise, FX shape, source_ref availability, statement-summary tokens, Dutch formats, masked-card extras schema)"
      contains: "Anchor tokens"
    - path: "Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf"
      provides: "Tiny (≤ 10 KB) anonymised PDF for the Wave 2 idempotency-contract test to exercise the real pdftotext binary path"
    - path: "tests/Pest.php"
      provides: "phase-3 group convention documented alongside the existing phase-2 docblock"
      contains: "phase-3"
    - path: "Modules/Ingestion/tests/Unit/Adapters/Ics/IcsPdfAdapterTest.php"
      provides: "Failing scaffold — adapter unit + snapshot tests (driven green by 03-02)"
    - path: "Modules/Ingestion/tests/Unit/Adapters/Ics/PdfTextExtractorTest.php"
      provides: "Failing scaffold — exec() boundary tests with the spatie wrapper mocked (driven green by 03-02)"
    - path: "Modules/Ingestion/tests/Unit/Adapters/Ics/IcsAmountParserTest.php"
      provides: "Failing scaffold — nl_NL amount parsing (driven green by 03-02)"
    - path: "Modules/Ingestion/tests/Unit/Adapters/Ics/IcsDateParserTest.php"
      provides: "Failing scaffold — nl_NL date parsing (driven green by 03-02)"
    - path: "Modules/Import/tests/Feature/IcsPdfImportTest.php"
      provides: "Failing scaffold — end-to-end ING-04 wire-level test (driven green across 03-02 + 03-03)"
    - path: "Modules/Core/tests/Feature/SettingsPageTest.php"
      provides: "Failing scaffold — D-45 Settings round-trip (driven green by 03-04)"
    - path: "Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php"
      provides: "Failing scaffold — D-44 / D-47 toggle UX (driven green by 03-05)"
    - path: "Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php"
      provides: "Failing scaffold — D-46 GROUP-BY-currency mode (driven green by 03-06)"
    - path: "Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php"
      provides: "Failing scaffold — D-48 effective-rate surface (driven green by 03-07)"
    - path: "tests/Feature/AnonymisedFixtureSweepTest.php"
      provides: "Repo-wide sweep that fails if the committed ICS fixture ever re-acquires PAN-shaped or IBAN-shaped strings"
  key_links:
    - from: "Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md"
      to: "Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt"
      via: "Fixture record documents the empirical extraction that produced the .txt"
      pattern: "Anchor tokens"
    - from: "tests/Feature/AnonymisedFixtureSweepTest.php"
      to: "Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt"
      via: "Sweep test reads the .txt and asserts no 12+ digit runs and no IBAN-shaped strings remain"
      pattern: "ics-sample-1.txt"
    - from: "README.md"
      to: "poppler.freedesktop.org"
      via: "Setup link to the official poppler project page (security-conscious framing)"
      pattern: "poppler.freedesktop.org"
---

<objective>
Wave 0 enablement for the rebuilt (PDF-pivot) Phase 3: install spatie/pdf-to-text, document the brew install poppler prerequisite, carve out local/ in .gitignore, receive the raw ICS PDF from the user, run the extract-then-redact-text protocol (pdftotext -layout → anonymise the .txt → commit the redacted .txt), commit a companion fixture-record markdown documenting every empirical detail Waves 2..N need, commit a tiny synthetic anonymised .pdf for the wire-level contract test, document the phase-3 Pest group convention, and land a Red-by-design failing test scaffold for every Phase 3 requirement (ING-04, LED-03, MC-02, UI-06).

Purpose: Phase 1 plan 03 and Phase 2 plan 01 both proved the value of front-loading the real fixture and group/scaffolds before any adapter code. The PDF pivot (D-31a, 2026-05-15) makes that doubly important here — without the empirical anchor tokens, Dutch tokens, and per-page noise patterns from a real statement, 03-02's PdfTextExtractor + IcsPdfExtractionMap + IcsPdfAdapter would be guessing. Anonymisation is OUR responsibility per D-32a; the user supplies the raw PDF, the planner-supplied protocol extracts and redacts it. The committed artefact is plain UTF-8 text — trivially diffable, no PDF-binary redaction problems.

autonomous: false — this plan contains a `checkpoint:human-action` gate to receive the raw ICS PDF (cannot be automated; the user must download from Mijn ICS and hand the file over) and a `checkpoint:human-verify` gate so the user confirms the redacted fixture preserves the data shape they expect before commit.

Output: composer dependency installed; README + .gitignore updated; raw PDF intake protocol executed; redacted .txt fixture + fixture-record markdown + tiny synthetic .pdf committed; phase-3 Pest group convention established; failing test scaffolds across nine test files committed in a Red state; anonymisation-sweep guard test committed and Green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
@.planning/phases/03-ics-cards-multi-currency-display/03-DISCUSSION-LOG.md
@.planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
@.planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
@.planning/phases/03-ics-cards-multi-currency-display/03-VALIDATION.md
@.planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md

# Phase 1/2 fixture-record patterns to mirror verbatim:
@tests/fixtures/asn-sample-1.md
@tests/fixtures/asn-mt940-sample-1.md

# Phase-2 group-registration precedent (per-test ->group chain — see tests/Pest.php "Phase-2 group convention" docblock):
@tests/Pest.php

# Idempotency dataset shape (extended in 03-02, but Wave 0 commits the tiny synthetic .pdf the new dataset row will reference):
@tests/Contracts/IdempotencyContractTest.php

# Composer manifest before mutation:
@composer.json
@.gitignore
@README.md

<interfaces>
<!-- Existing contracts the executor needs. Extracted from codebase. -->
<!-- Use these directly — no codebase exploration needed. -->

From Modules/Ingestion/Public/Contracts/SourceAdapter.php (current):
```php
interface SourceAdapter
{
    public function format(): string;
    public function parse(string $localPath, AccountResolver $accounts): \Generator;
    public function statementMetadata(): ?\Modules\Ledger\Public\Dto\StatementSummaryData;
}
```
Note: `statementMetadata()` already exists on the contract (added in Phase 2 plan 02-03). `IcsPdfAdapter` (built in 03-02) populates it from the PDF header block per D-51. No interface change required.

From Modules/Ingestion/Public/Dto/SourceTransactionDto.php (current, BEFORE D-42 extension which lands in 03-02):
```php
final class SourceTransactionDto extends \Spatie\LaravelData\Data
{
    public function __construct(
        public readonly \Carbon\CarbonImmutable $bookedAt,
        public readonly \Carbon\CarbonImmutable $postedAt,
        public readonly \Carbon\CarbonImmutable $valueDate,
        public readonly string $ownIban,
        public readonly ?string $counterpartyIban,
        public readonly ?string $counterpartyName,
        public readonly string $currency,
        public readonly int $amountMinor,
        public readonly ?string $sourceRef,
        public readonly ?string $description,
        public readonly array $rawPayload,
        public readonly int $sourceRowIndex,
    ) {}
}
```
The three new nullable fields (`settledAmountMinor`, `settledCurrency`, `fxRateUsed`) are appended by 03-02. Scaffolds in this plan reference them inside Red-by-design expectations; the PHP file does not need to compile-time-pass them yet (Pest compiles each test on demand, the assertions fail fast).

From tests/Pest.php (current — see the "Phase-2 group convention" docblock near the bottom):
> The phase-2 group is NOT registered in tests/Pest.php; it is created implicitly by each `it(...)->group('phase-2')` call. The focused dev loop is `vendor/bin/pest --group=phase-2 --bail`.

Phase 3 inherits the same convention. Each Phase 3 scaffold's `it(...)` chain ends with `->group('phase-3')`. The acceptance grep simply checks that the literal `'phase-3'` token appears in tests/Pest.php (as a documentation update) AND at least once across the new scaffold files (per-test chain). A registration block in tests/Pest.php is optional but the documentation block IS required so the next contributor finds the convention.

Failing-scaffold convention (Phase 2 precedent): every scaffold test MUST contain at least one runnable `it(...)` with an explicit failure (e.g. `expect(true)->toBe(false, 'scaffold — implemented in plan 03-NN');`). `->skip()` is NOT acceptable — VALIDATION.md's Dimension 8 (Nyquist sampling on requirements) treats skipped tests as gaps. Use the literal failure pattern uniformly across all scaffold files.

From .gitignore (current — the existing carve-outs for `/database/*.sqlite` and `/storage/imports` set the precedent. The Task 1 patch adds `/local/` in the same shape.)

From composer.json (current): `brick/math`, `brick/money`, `league/csv`, `genkgo/camt`, `nesbot/carbon`, `webklex/laravel-imap`, `spatie/laravel-data` are already declared runtime deps. `spatie/pdf-to-text` is NOT yet declared — Task 1 adds it. The latest stable line is 1.x (let composer's solver pick); a constraint like `^1.0` is appropriate.

From README.md (current): "Prerequisites" section lists PHP / Composer / SQLite / Node / macOS-Herd. The new local-development-setup bullet extends that section with the brew install poppler prerequisite and the `pdftotext -v` sanity check. Do NOT introduce a separate top-level heading — fold the note into the existing "Prerequisites" list.

Locked tokens / shapes the executor must use verbatim (sourced from CONTEXT.md D-31a / D-32a / D-49–D-56):
- pdftotext flags: `-layout -enc UTF-8 -eol unix -nopgbrk`
- Cardholder placeholder: `KAARTHOUDER` (literal Dutch token, all caps; Wave 2 looks for this exact string when populating `statement_summaries.extras.cardholderName`)
- Card-number replacement: `****-****-****-XXXX` where the last four are randomised digits (NOT the original last-four — see D-37 + the v2-multi-card landmine in 03-CONTEXT.md `<deferred>`; the real last-four is captured at parse-time from the raw PDF and written to `statement_summaries.extras.cardLast4`, never preserved in the committed fixture)
- IBAN placeholder: `NL00BANK0000000000` (matches Phase 2's anonymised-IBAN check-digit convention)
- Statement-summary anchor tokens (D-51): `Periode`, `Beginsaldo`, `Eindsaldo`, `Totaal nieuw saldo`, `Totaal betaald`
- Page-noise patterns (D-53): page footer `/^Pagina \d+ van \d+$/m`, masked-card watermark, cardholder banner line
- Dutch month abbreviations (D-52): `jan feb mrt apr mei jun jul aug sep okt nov dec`
- Discriminator field for rawPayload (D-49): `format = 'ics-pdf'`
- Synthetic ICS account iban (D-36 / D-38, established by 03-02 + 03-03): `sprintf('ICS-CARD-%d', $userId)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install spatie/pdf-to-text, document brew install poppler, carve out local/ in .gitignore</name>
  <files>
    composer.json
    composer.lock
    .gitignore
    README.md
  </files>
  <read_first>
    composer.json
    composer.lock
    .gitignore
    README.md
    .planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
  </read_first>
  <action>
    1. **Install the composer dependency.** Run `composer require spatie/pdf-to-text:^1.0` from the project root. Let composer's solver pick the resolved version; commit both composer.json and composer.lock. The package is a thin wrapper around the poppler `pdftotext` binary (per D-31a); the runtime exec() boundary is wrapped in 03-02's `PdfTextExtractor` service, so this task just installs the package — it writes no PHP code.

    2. **Carve out the local-only input directory in .gitignore.** Append a new section to .gitignore, after the existing `/storage/imports` carve-out, in the same shape:
       ```
       # Raw ICS Cards PDF statements — never commit financial data (D-32a).
       # The extracted-and-redacted .txt fixture under
       # Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt IS committed; the
       # raw .pdf lives here on the user's machine only.
       /local/
       ```
       Use a leading-slash anchor (`/local/`) matching the project's existing `/database/`, `/storage/imports`, `/vendor` conventions, so only the top-level `local/` is gitignored.

       Create the directory on disk: `mkdir -p local/ics` (this lets the user drop a PDF there immediately — Task 3 reads from `local/ics/`). The directory itself is not committed (no `.gitkeep`) because .gitignore rejects it.

    3. **Document brew install poppler in README.md.** Locate the existing `## Prerequisites` section (lines 12-18 of README.md per the current state). Add ONE new bullet at the end of the list:
       ```
       - Poppler (`pdftotext` binary) for ICS PDF statement parsing — `brew install poppler` on macOS; verify with `pdftotext -v`. See https://poppler.freedesktop.org/ for source builds on other platforms.
       ```
       The trailing link to poppler.freedesktop.org is intentional — security-conscious framing (do NOT just say "run `brew install poppler`" without pointing at the upstream project). The user can verify the formula on their own machine before installing.

    4. **Document the runtime check in the Install section.** Add a one-line sanity check to the "Install" section right after `composer install` and before `php artisan diederik:install`:
       ```
       pdftotext -v          # confirms poppler is on PATH (required for ICS PDF imports — see Prerequisites)
       ```
       This is a documentation-only line; the install script does not run it programmatically. The Wave 2 `PdfTextExtractor` service will surface a clean error if poppler is missing at runtime.
  </action>
  <verify>
    <automated>composer show spatie/pdf-to-text >/dev/null && grep -qE '^/local/?$' .gitignore && grep -q 'brew install poppler' README.md && grep -q 'poppler.freedesktop.org' README.md && grep -q 'pdftotext -v' README.md && test -d local/ics</automated>
  </verify>
  <acceptance_criteria>
    - `composer show spatie/pdf-to-text` exits zero with a non-empty version line
    - `composer.json` declares `spatie/pdf-to-text` under `require`: `grep -q '"spatie/pdf-to-text"' composer.json`
    - `composer.lock` is updated and committed together with composer.json
    - `.gitignore` carves out `local/`: `grep -qE '^/local/?$' .gitignore`
    - `local/ics/` directory exists on disk: `test -d local/ics`
    - README documents the poppler prerequisite verbatim: `grep -q 'brew install poppler' README.md` AND `grep -q 'poppler.freedesktop.org' README.md` AND `grep -q 'pdftotext -v' README.md`
    - No source code added in this task (composer + docs only): the cumulative diff under `Modules/`, `tests/`, `scripts/` is empty for this task
  </acceptance_criteria>
  <done>spatie/pdf-to-text installed; README documents the brew install poppler prerequisite; .gitignore carves out local/ to keep raw PDFs out of the repo; local/ics/ directory exists on disk ready for the user's raw export.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Receive raw ICS PDF statement from user</name>
  <read_first>
    .planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
    .planning/phases/03-ics-cards-multi-currency-display/03-DISCUSSION-LOG.md
    tests/fixtures/asn-sample-1.md
    tests/fixtures/asn-mt940-sample-1.md
  </read_first>
  <what-built>
    Task 1 installed spatie/pdf-to-text and carved out local/ in .gitignore. Tasks 3-7 (automated) extract the raw PDF to text, redact PII, commit the redacted .txt fixture + fixture-record markdown + tiny synthetic .pdf, document the phase-3 group convention, write failing test scaffolds, and add the anonymisation-sweep guard test.

    This checkpoint exists because Claude cannot download a statement from the Mijn ICS consumer portal — the user must log in and export it. Per D-32a anonymisation is OUR job once the file is in hand, not the user's. The raw PDF never enters the git tree.
  </what-built>
  <how-to-verify>
    1. Log in to the Mijn ICS consumer portal (https://www.icscards.nl/).
    2. Download a recent monthly statement as PDF. **Important**: the chosen statement MUST include at least one foreign-currency charge (e.g. a USD purchase, a GBP purchase). If recent statements have been EUR-only, pick an older statement — Phase 3's whole multi-currency pipeline depends on the fixture exercising the FX-row code path.
    3. Save the file at `local/ics/raw-ics-statement.pdf` in the project root (Task 1 created the `local/ics/` directory; .gitignore keeps it untracked).
    4. Tell the executor: "raw PDF is at local/ics/raw-ics-statement.pdf, contains FX rows: yes/no".

    The raw file MUST NOT be committed, pasted into the chat as text, or copied to any tracked location. Per D-32a it is processed in-place under `local/ics/` and the redaction pipeline only commits the extracted-and-anonymised .txt.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms a raw ICS PDF is on local disk at `local/ics/raw-ics-statement.pdf`
    - Human confirms the chosen statement contains at least one non-EUR currency row, OR explicitly notes "no FX rows available — synthesise one foreign-currency row during anonymisation"
    - The raw file is NOT in the git tree: `git check-ignore local/ics/raw-ics-statement.pdf` exits zero (the path is recognised as gitignored) — the executor verifies this BEFORE moving on
  </acceptance_criteria>
  <resume-signal>
    Reply with one of:
    - `approved` (the raw PDF is at local/ics/raw-ics-statement.pdf AND it contains at least one FX row),
    - `approved synthesise-fx` (the raw PDF is at local/ics/raw-ics-statement.pdf, has no FX rows; add ONE synthetic USD row at anonymisation time per the locked example shape `$12.99 USD → €12.07 EUR` from UI-SPEC).
  </resume-signal>
  <done>Human has supplied the raw PDF at local/ics/raw-ics-statement.pdf and confirmed FX-row availability (or requested synthetic-FX-row injection).</done>
</task>

<task type="auto">
  <name>Task 3: Extract-then-redact-text protocol — produce ics-sample-1.txt + anonymize_ics_text.php + fixture-record markdown</name>
  <files>
    scripts/anonymize_ics_text.php
    Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt
    Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md
  </files>
  <read_first>
    tests/fixtures/asn-sample-1.md
    tests/fixtures/asn-mt940-sample-1.md
    .planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
    .planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
  </read_first>
  <action>
    1. **Run pdftotext -layout against the raw PDF.** Execute:
       ```bash
       pdftotext -layout -enc UTF-8 -eol unix -nopgbrk local/ics/raw-ics-statement.pdf local/ics/raw-extracted.txt
       ```
       The exact flag set is locked by D-31a + D-53. `-layout` preserves the statement's tabular column structure via whitespace (load-bearing for downstream regex-anchor parsing). `-nopgbrk` strips form-feed characters so the per-page noise regexes (D-53) can match line-anchored patterns cleanly. Do NOT print the contents of `raw-extracted.txt` to stdout — it contains real PAN, real cardholder name, real merchant data.

    2. **Create `scripts/anonymize_ics_text.php`.** A small re-runnable PHP CLI script (keeps the toolchain single-language; the Phase 1 anonymisation script lived under `/tmp/` per `tests/fixtures/asn-mt940-sample-1.md` — this is the first long-lived in-repo anonymisation script). Required shape:
       ```php
       #!/usr/bin/env php
       <?php
       declare(strict_types=1);
       /**
        * Reads an extracted ICS PDF text dump (produced via
        * `pdftotext -layout -enc UTF-8 -eol unix -nopgbrk`) and writes a
        * redacted version suitable for committing as a test fixture.
        *
        * Redaction protocol (D-32a + D-37 + D-56):
        *   - Card-number runs (≥ 12 contiguous digits, optionally interrupted
        *     by spaces or hyphens) → `****-****-****-XXXX` where XXXX is
        *     randomised digits (NOT the original last-four — landmine guard
        *     per D-37).
        *   - Cardholder-name line(s) detected by the statement banner
        *     pattern → literal `KAARTHOUDER` (all caps, Dutch).
        *   - IBAN-shaped tokens (NL / DE / BE / etc. + 2 check digits +
        *     ≥ 10 alnum) → `NL00BANK0000000000` (deterministic placeholder;
        *     mirrors Phase 2's anonymised-IBAN check-digit convention).
        *   - Email addresses → `kaarthouder@example.invalid`.
        *   - Phone numbers (NL-style runs) → `+31 6 0000 0000`.
        *   - Dates, amounts, currency codes, merchant strings → preserved
        *     VERBATIM (load-bearing for the parser's empirical tests).
        *
        * Usage:
        *   php scripts/anonymize_ics_text.php <input.txt> <output.txt>
        *
        * Re-running on a fresh extraction is safe (idempotent — every
        * replacement is regex-driven on input shape, not state).
        */
       ```
       Implementation guidance:
       - Read all of `$argv[1]` into a string (`file_get_contents`).
       - Apply each redaction as an explicit `preg_replace_callback` so the script is auditable line-by-line. Order matters: redact card numbers BEFORE IBANs (some 12+ digit runs would otherwise match the IBAN pattern's tail).
       - Write the result to `$argv[2]` (`file_put_contents`).
       - Exit 0 on success, non-zero on missing argv or unreadable input. Print a one-line summary to stderr: "Redacted N card-numbers, M IBANs, K names — wrote <output>".
       - Use only PHP core functions (no composer deps) so the script runs from a fresh clone without `composer install` having completed.

       Make the script executable: `chmod +x scripts/anonymize_ics_text.php`.

    3. **Run the anonymisation.** Execute:
       ```bash
       php scripts/anonymize_ics_text.php local/ics/raw-extracted.txt local/ics/redacted.txt
       ```
       Confirm the redacted file exists. **If** the human's resume signal was `approved synthesise-fx`, append ONE synthetic FX-row block to `local/ics/redacted.txt` BEFORE committing it. The synthetic block must match the empirical FX-row shape (D-35) — read the redacted text first to see what shape pdftotext produced, then mimic that shape exactly with values `12,99` USD → `12,07` EUR (UI-SPEC example) and a plausible merchant string like `SYNTHETIC USD MERCHANT`. Document the synthesis in the fixture-record markdown's "Anonymisation protocol" section.

    4. **Sanity-grep the redacted output BEFORE committing.** Run each of these checks against `local/ics/redacted.txt`; ALL must pass:
       - No 12+ digit runs: `grep -cE '[0-9]{12,}' local/ics/redacted.txt` returns `0`.
       - No IBAN-shaped tokens other than the deterministic `NL00BANK0000000000` placeholder: `grep -oE '\b[A-Z]{2}[0-9]{2}[A-Z0-9]{10,}\b' local/ics/redacted.txt | grep -v '^NL00BANK0000000000$' | wc -l` returns `0`.
       - Card-number placeholder present: `grep -c '\*\*\*\*-\*\*\*\*-\*\*\*\*-' local/ics/redacted.txt` returns ≥ 1.
       - Cardholder placeholder present: `grep -c 'KAARTHOUDER' local/ics/redacted.txt` returns ≥ 1.
       - Statement-summary anchors present (D-51): each of `Periode`, `Beginsaldo`, `Eindsaldo`, `Totaal nieuw saldo`, `Totaal betaald` `grep -q` returns 0 (matches). If any anchor is MISSING from the empirical statement, document the absence in the fixture-record markdown's "Statement summary tokens" section — Wave 2 will branch accordingly.

       If ANY sanity-grep fails, FIX the script's redaction logic and re-run step 3. Do NOT commit a fixture that fails any sanity check.

    5. **Copy the redacted file into the repo:**
       ```bash
       cp local/ics/redacted.txt Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt
       ```
       Then delete the intermediate files: `rm local/ics/raw-extracted.txt local/ics/redacted.txt`. The raw PDF at `local/ics/raw-ics-statement.pdf` STAYS — the user may want to re-extract later if the script's redaction rules evolve. (.gitignore keeps it out of commits.)

    6. **Write `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md`.** Mirror `tests/fixtures/asn-mt940-sample-1.md` headings (the closer analog — both fixtures document a parser-targeted text format, both detail the empirical shape Wave 2 codes against). Required sections, headings verbatim:

       - `# ICS PDF — empirical fixture record`
       - One-paragraph intro stating the file is the `pdftotext -layout` extraction of a real Mijn ICS consumer-portal monthly statement, anonymised per the D-32a extract-then-redact-text protocol; the raw .pdf stays under `local/ics/` (gitignored).
       - `## Extraction command`: the exact `pdftotext -layout -enc UTF-8 -eol unix -nopgbrk <input> <output>` invocation used.
       - `## Anonymisation protocol`: list which fields are redacted (card numbers, cardholder name, IBANs, emails, phone numbers) and which are preserved verbatim (dates, amounts, currency codes, merchant strings). If a synthetic FX row was appended, note it here with the exact values used.
       - `## Statement layout`: page count, lines per page, column ordering of the transactions table, whether per-page footers / banners repeat (D-53).
       - `## Anchor tokens`: the exact Dutch tokens (verbatim from the redacted .txt) that mark the start and end of the transactions table — these become constants on `IcsPdfExtractionMap` in 03-02. Quote the surrounding context (preceding line + following line) so the parser's regex can be tightened.
       - `## Per-page noise patterns`: the exact regexes (or substring tokens) for repeating per-page footers, masked-card watermarks, cardholder banners. Format as `/^Pagina \d+ van \d+$/m`-style ready-to-paste regexes.
       - `## FX-row visual shape (D-35)`: state which of the three D-35 shapes is present: (a) single line with both legs, (b) two-line block (merchant + native amount, then conversion rate + settled amount), (c) footnote-style with reference markers. Quote ONE redacted example block verbatim.
       - `## source_ref availability (D-34)`: state whether the per-transaction text exposes a stable identifier token (authorisation code, slip number, transaction reference) suitable for `source_ref`. If yes, name the token and its line-relative position. If no, state "source_ref will be NULL; v3 fingerprint is the only dedup anchor".
       - `## Markup separability (D-40)`: state whether the ICS markup is itemised as a separate text line / footnote, or whether it is rolled implicitly into the displayed rate. If separable, note where; if not, note D-40's "rolled into settled" disposition.
       - `## Statement summary tokens (D-51)`: list the line-anchored positions of `Periode`, `Beginsaldo`, `Eindsaldo`, `Totaal nieuw saldo`, `Totaal betaald` (or note any that are absent from this statement). For each present token, quote the line verbatim AND record the line offset from the top of the redacted .txt. Wave 2 turns these into named-capture regexes.
       - `## Dutch date formats (D-52)`: list the date format(s) present (`d-m-Y`, `j M Y`, etc.) with one verbatim example each. Include the Dutch month abbreviations used (the canonical set is `jan feb mrt apr mei jun jul aug sep okt nov dec`).
       - `## Dutch amount formats (D-52)`: state the decimal separator (`,`) and thousands separator (`.` or whitespace), and whether the currency symbol (`€`) is space-separated, prefix, or suffix. One verbatim example.
       - `## Masked-card metadata schema (D-56)`: the keys + value shape `IcsPdfAdapter` will write into `statement_summaries.extras` — exact JSON shape `{"issuer": "Mastercard", "cardLast4": "XXXX", "cardholderName": "STRIPPED"}` (the `XXXX` here is literally `XXXX` — per D-37 we do NOT preserve real last-four digits in the fixture; the real last-four is captured at parse-time from the raw PDF, never from the committed fixture).

       The markdown is human-readable — its job is to let Wave 2's planner copy-paste regex shapes and constants without re-inspecting the redacted .txt themselves.

    7. **Surface "Major Deviations" if any.** If during extraction or anonymisation any CONTEXT.md decision was contradicted by the empirical evidence (e.g. D-49 assumes per-transaction extracted-text blocks are contiguous in the .txt but the real layout splits the merchant onto a different page), add a `## Major Deviations from CONTEXT.md` section at the end of the markdown listing them. The orchestrator audits this section and may emit a CONTEXT.md addendum or revise downstream plans.
  </action>
  <verify>
    <automated>test -f Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt && test -f Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md && test -x scripts/anonymize_ics_text.php && [ "$(grep -cE '[0-9]{12,}' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt)" = "0" ] && grep -q '\*\*\*\*-\*\*\*\*-\*\*\*\*-' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt && grep -q 'KAARTHOUDER' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt && grep -q 'Extraction command' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md && grep -q 'Anchor tokens' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md && grep -q 'Statement summary tokens' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md && grep -q 'FX-row visual shape' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md && grep -q 'Dutch date formats' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md && grep -q 'Masked-card metadata schema' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/anonymize_ics_text.php` exists, is executable, has a docstring documenting the redaction protocol
    - `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt` exists and is plain UTF-8 text (verify with `file Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt` reporting UTF-8 / ASCII)
    - No 12+ contiguous digits anywhere in the committed fixture: `grep -cE '[0-9]{12,}' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt` returns `0`
    - No real IBAN-shaped tokens in the committed fixture (only the deterministic `NL00BANK0000000000` placeholder, if any): `grep -oE '\b[A-Z]{2}[0-9]{2}[A-Z0-9]{10,}\b' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt | grep -v '^NL00BANK0000000000$' | wc -l` returns `0`
    - Card-number redaction placeholder present: `grep -c '\*\*\*\*-\*\*\*\*-\*\*\*\*-' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt` returns ≥ 1
    - Cardholder placeholder present: `grep -c 'KAARTHOUDER' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt` returns ≥ 1
    - Fixture record has every required section heading: `Extraction command`, `Anonymisation protocol`, `Statement layout`, `Anchor tokens`, `Per-page noise patterns`, `FX-row visual shape (D-35)`, `source_ref availability (D-34)`, `Markup separability (D-40)`, `Statement summary tokens (D-51)`, `Dutch date formats (D-52)`, `Dutch amount formats (D-52)`, `Masked-card metadata schema (D-56)` — verified by grep (the verify line covers the load-bearing ones; the rest are confirmed in the manual checkpoint at Task 4)
    - The intermediate files under `local/ics/raw-extracted.txt` and `local/ics/redacted.txt` are deleted after copy: `ls local/ics/raw-extracted.txt 2>/dev/null` returns "No such file"
    - `local/ics/raw-ics-statement.pdf` STILL exists (user may re-extract later): `test -f local/ics/raw-ics-statement.pdf` returns true
  </acceptance_criteria>
  <done>The redacted-text fixture is committed; the anonymisation script that produced it is committed alongside; the fixture-record markdown documents every empirical detail Wave 2..N need; raw PDF stays in local/ics/ (gitignored).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — committed fixture preserves the data shape and contains zero PII</name>
  <what-built>
    Tasks 1-3 installed spatie/pdf-to-text, carved out local/ics/, extracted-and-redacted the raw PDF to text, and committed `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt` + `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md` + `scripts/anonymize_ics_text.php`.
  </what-built>
  <how-to-verify>
    1. Open `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt` in your editor. Skim end-to-end and confirm:
       - Card numbers (16-digit runs) are gone everywhere — replaced with `****-****-****-XXXX` placeholders.
       - Your real cardholder name is gone — replaced with the literal `KAARTHOUDER`.
       - Real IBANs (if any appeared) are gone — replaced with `NL00BANK0000000000`.
       - Dates, amounts, currencies, merchant strings are preserved verbatim (the parser must exercise the real data shape).
       - If you requested `synthesise-fx`, the synthetic USD row (e.g. `SYNTHETIC USD MERCHANT $12,99 USD ... €12,07 EUR`) is present.
    2. Open `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md`. Confirm every section is filled in (no `TODO`, no `<placeholder>` markers).
    3. If you spot ANY surviving PII or a missing fixture-record section, reply `redact-again` and the executor re-runs Task 3 with a refined script.
    4. If the fixture is clean and the record is complete, reply `approved`.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the redacted fixture contains zero PII visually
    - Human confirms the fixture record is complete (no TODO / placeholder markers left)
  </acceptance_criteria>
  <resume-signal>
    Reply with `approved` (proceed to Tasks 5-7) OR `redact-again` (the executor refines `scripts/anonymize_ics_text.php` and re-runs Task 3).
  </resume-signal>
  <done>Human has verified the redacted fixture preserves the data shape Wave 2 needs and contains zero PII; the fixture-record markdown is complete.</done>
</task>

<task type="auto">
  <name>Task 5: Commit a tiny synthetic ics-sample-tiny.pdf so the Wave 2 idempotency-contract test can exercise the real pdftotext binary path</name>
  <files>
    Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf
    Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.md
  </files>
  <read_first>
    Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt
    Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md
    tests/Contracts/IdempotencyContractTest.php
    .planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
  </read_first>
  <action>
    Rationale: the `IdempotencyContractTest` dataset row that 03-02 adds for `'ics-pdf'` exercises the wire-level re-import path through `RunImport`, which hashes the uploaded file (Tier-1 SHA dedup) and then invokes the `IcsPdfAdapter::parse()` generator. Re-using `ics-sample-1.txt` here is structurally wrong (it's not a PDF — the `PdfTextExtractor` would shell out to `pdftotext` on a non-PDF and fail). The cleanest shape is a tiny checked-in `.pdf` that, when run through `pdftotext -layout`, yields at least one parseable transaction line.

    1. **Generate the tiny PDF using `cupsfilter` (available on every macOS Herd machine without additional installs).** Run:
       ```bash
       cat > /tmp/tiny-ics.txt <<'EOF'
       Mijn ICS — synthetisch testbestand
       KAARTHOUDER
       ****-****-****-XXXX

       Periode  : 01-04-2026 t/m 30-04-2026
       Beginsaldo: EUR 0,00
       Eindsaldo : EUR 1,00
       Totaal nieuw saldo: EUR 1,00
       Totaal betaald   : EUR 0,00

       12-04-2026  SYNTHETIC ICS TINY                    EUR 1,00
       EOF
       cupsfilter -i text/plain -m application/pdf /tmp/tiny-ics.txt > Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf 2>/dev/null
       rm /tmp/tiny-ics.txt
       ```
       The input text deliberately includes the locked anonymisation placeholders (`KAARTHOUDER`, `****-****-****-XXXX`) AND the locked statement-summary anchor tokens (`Periode`, `Beginsaldo`, `Eindsaldo`, `Totaal nieuw saldo`, `Totaal betaald`) AND ONE EUR-native transaction row. This lets the Wave 2 contract test assert:
       (a) exactly one `SourceTransactionDto` is yielded,
       (b) `statementMetadata()` is populated with the synthetic Periode / Beginsaldo / Eindsaldo,
       (c) `statement_summaries.extras.cardholderName` is `STRIPPED` (per D-56 — adapter strips at the boundary regardless of fixture content).

    2. **Verify file size ≤ 10 KB.** `wc -c Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf` returns a value ≤ 10240. If `cupsfilter` produces something larger, the executor regenerates with a shorter input text (drop the blank lines and the Dutch-month example block).

    3. **Verify the PDF parses cleanly.** Run:
       ```bash
       pdftotext -layout -enc UTF-8 -eol unix -nopgbrk Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf - 2>/dev/null | grep -q SYNTHETIC
       ```
       If this fails, regenerate. The `SYNTHETIC` literal MUST survive the round-trip — it's the Wave 2 test's anchor for "yes, one row was parsed".

    4. **Sweep the tiny PDF for residual PII** (paranoia — the input was synthetic, but the sweep test in Task 7 will guard this too):
       ```bash
       pdftotext -layout -enc UTF-8 -eol unix -nopgbrk Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf - 2>/dev/null | grep -cE '[0-9]{12,}'
       ```
       Must return `0`.

    5. **Write a tiny companion markdown `Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.md`** (≤ 25 lines). Content:
       - One paragraph stating "Synthetic anonymised PDF generated via `cupsfilter` from a known-good UTF-8 text source. NOT a real ICS export. Used by `tests/Contracts/IdempotencyContractTest.php` (extended in plan 03-02) to exercise the wire-level re-import path through the real `pdftotext` binary."
       - The exact `cupsfilter` invocation used (so the file is reproducible by any future contributor).
       - A note: "If your dev machine does not have `cupsfilter` (rare; ships with macOS), regenerate via any PDF library that can render plain UTF-8 text; the only requirement is that `pdftotext -layout` on the output yields at least one line containing the literal `SYNTHETIC`."

    6. **Fallback path** (only if `cupsfilter` is unavailable on the executor's host — rare but possible): the executor writes a tiny generator using `groff -T pdf` if available, or hand-crafts a minimal "Hello World" PDF byte string (~400 bytes; the format is well-documented). Document the chosen path in `ics-sample-tiny.md` so the SUMMARY can reflect it.
  </action>
  <verify>
    <automated>test -f Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf && test -f Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.md && [ "$(wc -c < Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf)" -le 10240 ] && head -c 5 Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf | grep -q '%PDF-' && pdftotext -layout -enc UTF-8 -eol unix -nopgbrk Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf - 2>/dev/null | grep -qi SYNTHETIC && [ "$(pdftotext -layout -enc UTF-8 -eol unix -nopgbrk Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf - 2>/dev/null | grep -cE '[0-9]{12,}')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf` exists, is ≤ 10 KB, starts with the `%PDF-` magic bytes
    - `pdftotext` on the file extracts text containing the literal `SYNTHETIC` (load-bearing for the Wave 2 contract test to assert it parsed the row)
    - The extracted text contains zero 12+ digit runs and no IBAN-shaped tokens (paranoia sweep)
    - Companion `Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.md` exists and documents how the PDF was generated so the fixture is reproducible
    - If a fallback generator script was needed, it lives under `scripts/` and is documented in `ics-sample-tiny.md`
  </acceptance_criteria>
  <done>A tiny (≤ 10 KB), anonymised, deterministically-parseable .pdf is committed under the ICS fixture directory; Wave 2's `IdempotencyContractTest` dataset row will point at it.</done>
</task>

<task type="auto">
  <name>Task 6: Document phase-3 Pest convention + commit nine failing test scaffolds across the Phase 3 surface</name>
  <files>
    tests/Pest.php
    Modules/Ingestion/tests/Unit/Adapters/Ics/IcsPdfAdapterTest.php
    Modules/Ingestion/tests/Unit/Adapters/Ics/PdfTextExtractorTest.php
    Modules/Ingestion/tests/Unit/Adapters/Ics/IcsAmountParserTest.php
    Modules/Ingestion/tests/Unit/Adapters/Ics/IcsDateParserTest.php
    Modules/Import/tests/Feature/IcsPdfImportTest.php
    Modules/Core/tests/Feature/SettingsPageTest.php
    Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php
    Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php
    Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php
  </files>
  <read_first>
    tests/Pest.php
    Modules/Ingestion/tests/Unit/AsnCsvAdapterTest.php
    Modules/Import/tests/Feature/AsnCsvImportTest.php
    Modules/Core/tests/Feature/InstallCommandTest.php
    Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md
    .planning/phases/03-ics-cards-multi-currency-display/03-VALIDATION.md
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
    .planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
  </read_first>
  <action>
    1. **Update `tests/Pest.php` with the phase-3 group documentation.** Locate the existing "Phase-2 group convention" docblock at the bottom of `tests/Pest.php`. Add an analogous Phase-3 docblock immediately below it:
       ```
       /*
        * Phase-3 group convention.
        *
        * Same as Phase 2: no global registration here — the `phase-3` group
        * is created implicitly by tests chaining ->group('phase-3') on each
        * it(...) call (Ingestion ICS PDF adapter, Import wire-level, Core
        * Settings page, Ledger toggle/dashboard/detail tests).
        *
        * The focused dev loop is:
        *     vendor/bin/pest --group=phase-3 --bail
        */
       ```
       This is documentation-only — the literal `phase-3` token landing in `tests/Pest.php` is what the acceptance grep checks.

    2. **Commit nine failing test scaffolds.** Every scaffold:
       - Uses `declare(strict_types=1);` at the top.
       - Has a Pest test file shape matching the analog (`Modules/Ingestion/tests/Unit/AsnCsvAdapterTest.php` for unit tests; `Modules/Import/tests/Feature/AsnCsvImportTest.php` for feature tests).
       - Tags every `it(...)` with `->group('phase-3');` so the focused dev loop picks it up.
       - Fails with `expect(true)->toBe(false, 'scaffold — implemented in plan 03-NN');` — NEVER `->skip()`, NEVER an unimplemented `markIncomplete()`. VALIDATION.md's Dimension 8 treats skipped tests as gaps.
       - Targets a behaviour traceable to a CONTEXT.md decision or REQUIREMENTS.md ID.

       **File A — `Modules/Ingestion/tests/Unit/Adapters/Ics/IcsPdfAdapterTest.php`** (10 cases):
       - `it('reports its stable format identifier as ics-pdf')` — D-31a
       - `it('parses the redacted .txt fixture into the expected SourceTransactionDto stream')` — driven green by 03-02 (the adapter unit test injects a `PdfTextExtractor` test-double that returns the committed .txt contents directly)
       - `it('preserves the per-transaction extracted-text block in rawPayload under the ics-pdf format discriminator')` — D-49
       - `it('emits monotonically increasing sourceRowIndex starting at zero')`
       - `it('rejects a file that fails the magic-byte sniff before reading any transaction')` — D-54
       - `it('yields native + settled pair for a foreign-currency row')` — D-35 resolved + LED-03
       - `it('yields settledAmountMinor = null for an EUR-native row')` — D-42 honesty contract
       - `it('discards card-number text from the per-transaction block before writing rawPayload')` — D-37 landmine guard
       - `it('exposes statement-summary tokens via statementMetadata() after parse() completes')` — D-51 stateful-adapter shape
       - `it('registers under the ics-pdf key in the SourceAdapterRegistry')` — driven green by 03-02 step 9 (registry wiring)
       Beforeach sets up a stub `AccountResolver` mirroring `AsnCsvAdapterTest`.

       **File B — `Modules/Ingestion/tests/Unit/Adapters/Ics/PdfTextExtractorTest.php`** (4 cases):
       - `it('returns the extracted text via the spatie/pdf-to-text wrapper')` — D-31a
       - `it('throws PdfExtractionFailed when the binary is missing or non-zero exits')` — D-31a + Larastan strictness
       - `it('applies the locked flag set: -layout -enc UTF-8 -eol unix -nopgbrk')` — D-31a + D-53 anchor stability
       - `it('escapes the path argument before exec to defend against shell injection')` — security_constraint in planning context
       The spatie wrapper is constructor-DI'd; tests mock its `text()` method via a fake test double.

       **File C — `Modules/Ingestion/tests/Unit/Adapters/Ics/IcsAmountParserTest.php`** (5 cases):
       - `it('parses a positive EUR amount with comma decimal: € 22,75 → 2275')` — D-52
       - `it('parses a negative EUR amount written with a minus prefix: -€ 22,75 → -2275')` — D-52
       - `it('parses a USD amount with the ISO symbol: $ 12,99 → 1299')` — D-52
       - `it('rejects a malformed amount string by throwing InvalidAmountException')` — D-52 + Larastan strictness
       - `it('does not mutate global locale state')` — D-52 (no `setLocale()` rule)

       **File D — `Modules/Ingestion/tests/Unit/Adapters/Ics/IcsDateParserTest.php`** (5 cases):
       - `it('parses the dd-mm-yyyy Dutch numeric format')` — D-52
       - `it('parses the j M Y abbreviated-month format with Dutch abbreviations (mei, mrt)')` — D-52
       - `it('rejects an invalid date string by throwing InvalidAmountException')` — D-52
       - `it('returns a CarbonImmutable at startOfDay (00:00:00) to match the fingerprint composer')` — D-43 (FingerprintComposer v3 day-precision invariant)
       - `it('does not mutate global locale state')` — D-52

       **File E — `Modules/Import/tests/Feature/IcsPdfImportTest.php`** (9 cases — covers ING-04 + LED-03 wire-level + D-55 + D-49 + D-37):
       - `it('imports every parsed row from the redacted .txt fixture on the first run via the test-double extractor')` — ING-04 SC#1
       - `it('returns zero new rows when re-importing the same SHA-256 tiny PDF')` — D-55 Tier 1
       - `it('returns zero new rows when re-importing a different SHA but identical content')` — D-55 Tier 2 (mutate one byte of the tiny PDF and re-import; row-level fingerprint catches it)
       - `it('persists settled_amount_minor and settled_currency for an EUR-native row')` — LED-03 mirror branch
       - `it('persists native + settled + fx_rate_used for a foreign-currency row')` — LED-03 + D-39 derivation
       - `it('persists rawPayload.format = ics-pdf and a non-empty extractedText per row')` — D-49
       - `it('never persists card-number text into transactions.raw_payload')` — D-37 + security constraint
       - `it('prompts the user to name the ICS Account on the first ICS upload')` — D-36 / D-38 (driven green by 03-03)
       - `it('skips the name-your-account step on subsequent ICS uploads')` — D-38 skip path (driven green by 03-03)
       The two "name your account" cases stay Red after 03-02 — 03-03 owns them.

       **File F — `Modules/Core/tests/Feature/SettingsPageTest.php`** (6 cases — MC-02 storage half + D-45):
       - `it('renders the Settings page with the user current preferences pre-filled')` — D-45
       - `it('persists default_currency_view when changed via the toggle')` — MC-02 round-trip
       - `it('persists period_start_day when changed')` — D-45 co-discharge of Phase 1 D-19
       - `it('rejects period_start_day outside 1..28')` — validation
       - `it('rejects default_currency_view outside {eur_only, original}')` — validation
       - `it('round-trips default_currency_view = original into the TransactionsList default mode')` — Pitfall 5 four-boundary path

       **File G — `Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php`** (7 cases — MC-02 list half + UI-06):
       - `it('defaults the currency property to the user default_currency_view preference when no URL override is present')` — D-44
       - `it('overrides the user preference when ?currency=eur is present in the URL')` — D-44 URL precedence
       - `it('overrides the user preference when ?currency=original is present in the URL')`
       - `it('renders two lines for a foreign-currency row in original mode')` — D-47 (UI-06)
       - `it('renders one line for a foreign-currency row in eur mode')` — D-47
       - `it('renders one line for an EUR-native row in original mode')` — D-47 collapse
       - `it('keeps the URL clean when the toggle is on the default value')` — D-44 except-empty semantics

       **File H — `Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php`** (5 cases — MC-02 dashboard half + D-46):
       - `it('returns one tile-row per currency in original mode for a mixed EUR/USD month')` — D-46
       - `it('collapses an EUR-only month to one tile-row in original mode')` — D-46 specifics collapse rule
       - `it('omits zero-activity currencies from the original-mode tile rows')` — UI-SPEC Per-currency KPI tile rows
       - `it('returns one summary in eur_only mode (settled-EUR sum)')` — preserves the Phase 1 path
       - `it('orders the original-mode tile rows by settled_currency alphabetically')` — UI-SPEC determinism

       **File I — `Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php`** (4 cases — UI-06 + D-48):
       - `it('renders the Effective rate line when fx_rate_used is populated')` — D-48
       - `it('omits the Effective rate line when fx_rate_used is NULL')` — D-48 null branch
       - `it('formats the rate as EUR-per-unit-native with 3 decimal places (€0.929 / USD)')` — UI-SPEC FX rate detail format
       - `it('renders the Includes any ICS markup helper text below the rate')` — UI-SPEC helper text contract

       Total `it(...)` invocations across the nine files: 10 + 4 + 5 + 5 + 9 + 6 + 7 + 5 + 4 = **55**. Acceptance requires ≥ 30; we are comfortably over.

    3. Each scaffold file ends with a single trailing newline and passes `vendor/bin/pint --test`. Run `vendor/bin/pint` on the new files at the end of this task.

    4. **No facade / helper slippage in any scaffold.** The CLAUDE.md project rule forbids `auth()`, `Auth::user()`, `\Illuminate\Support\Facades\Auth`, any global helper. Tests use Laravel's `actingAs($user)` (Pest's built-in helper inherited from `Tests\TestCase` is acceptable) but NEVER `auth()->user()` or facade-style retrieval. When seeding test data, use `Modules\Core\Models\User::factory()->create()` style.
  </action>
  <verify>
    <automated>grep -q 'phase-3' tests/Pest.php && test -f Modules/Ingestion/tests/Unit/Adapters/Ics/IcsPdfAdapterTest.php && test -f Modules/Ingestion/tests/Unit/Adapters/Ics/PdfTextExtractorTest.php && test -f Modules/Ingestion/tests/Unit/Adapters/Ics/IcsAmountParserTest.php && test -f Modules/Ingestion/tests/Unit/Adapters/Ics/IcsDateParserTest.php && test -f Modules/Import/tests/Feature/IcsPdfImportTest.php && test -f Modules/Core/tests/Feature/SettingsPageTest.php && test -f Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php && test -f Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php && test -f Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php && grep -v '^#' Modules/Ingestion/tests/Unit/Adapters/Ics/IcsPdfAdapterTest.php Modules/Ingestion/tests/Unit/Adapters/Ics/PdfTextExtractorTest.php Modules/Ingestion/tests/Unit/Adapters/Ics/IcsAmountParserTest.php Modules/Ingestion/tests/Unit/Adapters/Ics/IcsDateParserTest.php Modules/Import/tests/Feature/IcsPdfImportTest.php Modules/Core/tests/Feature/SettingsPageTest.php Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php | grep -c '^it(' | awk '{exit !($1>=30)}' && [ "$(grep -rc '\->skip(' Modules/Ingestion/tests/Unit/Adapters/Ics/IcsPdfAdapterTest.php Modules/Ingestion/tests/Unit/Adapters/Ics/PdfTextExtractorTest.php Modules/Ingestion/tests/Unit/Adapters/Ics/IcsAmountParserTest.php Modules/Ingestion/tests/Unit/Adapters/Ics/IcsDateParserTest.php Modules/Import/tests/Feature/IcsPdfImportTest.php Modules/Core/tests/Feature/SettingsPageTest.php Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php | awk -F: '{s+=$2} END{print s}')" = "0" ] && ! vendor/bin/pest --group=phase-3 2>&1 | grep -qE '^OK \(' && vendor/bin/pint --test Modules/Ingestion/tests/Unit/Adapters/Ics Modules/Import/tests/Feature/IcsPdfImportTest.php Modules/Core/tests/Feature/SettingsPageTest.php Modules/Ledger/tests/Feature</automated>
  </verify>
  <acceptance_criteria>
    - All nine scaffold files exist at the listed paths
    - Total `it(...)` invocation count across the nine files is at least 30 (actual target ~55): the awk gate in the verify line returns success
    - Zero `->skip()` calls across the nine files: the grep gate returns `0`
    - `vendor/bin/pest --group=phase-3` runs the group AND exits non-zero (Red — at least one scaffold fails as designed): the negated grep gate returns success
    - Pint clean on all nine files: `vendor/bin/pint --test` returns zero on the listed paths
    - `tests/Pest.php` documents the phase-3 convention: `grep -q 'phase-3' tests/Pest.php`
    - Every `it(...)` in the nine files chains `->group('phase-3')` (single OR double quotes accepted): `grep -rE "->group\\(['\"]phase-3['\"]\\)" <nine files> | wc -l` returns ≥ 30
    - No facade / helper slippage in any scaffold (CLAUDE.md project rule): `! grep -rE 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' Modules/Ingestion/tests/Unit/Adapters/Ics Modules/Import/tests/Feature/IcsPdfImportTest.php Modules/Core/tests/Feature/SettingsPageTest.php Modules/Ledger/tests/Feature >/dev/null`
    - No `.planning/` / D-XX references inside any scaffold's PHP source (CLAUDE.md GSD-agnostic rule): `! grep -rE '\.planning/|D-[0-9]{2,}' Modules/Ingestion/tests/Unit/Adapters/Ics Modules/Import/tests/Feature/IcsPdfImportTest.php Modules/Core/tests/Feature/SettingsPageTest.php Modules/Ledger/tests/Feature >/dev/null` — the scaffold's per-test failure messages refer to plan numbers (e.g. `'scaffold — implemented in plan 03-02'`), which is acceptable; D-XX decision IDs are not
  </acceptance_criteria>
  <done>Nine failing test scaffolds committed across Ingestion / Import / Core / Ledger; phase-3 group convention documented in tests/Pest.php; `vendor/bin/pest --group=phase-3` runs Red as designed; Waves 2..N have a complete behavioural target to drive Green.</done>
</task>

<task type="auto">
  <name>Task 7: Anonymisation-sweep guard test — protects the committed fixture against future PII regressions</name>
  <files>
    tests/Feature/AnonymisedFixtureSweepTest.php
  </files>
  <read_first>
    Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt
    Modules/Ingestion/tests/fixtures/ics/ics-sample-tiny.pdf
    tests/Contracts/IdempotencyContractTest.php
    tests/Feature/LoopbackOnlyTest.php
  </read_first>
  <action>
    Write `tests/Feature/AnonymisedFixtureSweepTest.php` — a Green-by-design Pest feature test that scans the committed ICS fixtures for PII-shaped strings and fails the build the moment any re-appears. This is the load-bearing repo-wide defence against an inattentive contributor pasting a raw export into the fixture directory in the future.

    Required `it(...)` cases (each `->group('phase-3')`):

    1. **`it('the redacted ICS text fixture contains zero 12-digit-or-longer runs')`** — reads `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt`, asserts `preg_match_all('/[0-9]{12,}/', $contents, $m) === 0`.

    2. **`it('the redacted ICS text fixture contains zero IBAN-shaped tokens other than the deterministic placeholder')`** — reads the file, captures every match of `/\b[A-Z]{2}[0-9]{2}[A-Z0-9]{10,}\b/`, asserts every match equals the literal `NL00BANK0000000000` (the deterministic placeholder used by `scripts/anonymize_ics_text.php`).

    3. **`it('the redacted ICS text fixture contains the KAARTHOUDER placeholder')`** — asserts `str_contains($contents, 'KAARTHOUDER')`. This catches a regression where the script's name-redaction rule mistakenly strips the placeholder instead of producing it.

    4. **`it('the redacted ICS text fixture contains a card-number placeholder')`** — asserts `preg_match('/\*\*\*\*-\*\*\*\*-\*\*\*\*-/', $contents) === 1`. Symmetrical to case 3.

    5. **`it('the tiny synthetic ICS PDF, after pdftotext extraction, contains zero PII-shaped strings')`** — uses `Symfony\Component\Process\Process` (Laravel transitive dep) to exec `pdftotext -layout -enc UTF-8 -eol unix -nopgbrk <tiny.pdf> -`, captures stdout, asserts no 12+ digit runs and no IBAN-shaped tokens other than the placeholder. This is the integration-style guard; tag it `->group('phase-3')->group('integration')` so CI hosts without poppler can `--exclude-group=integration`.

    Lives at the project root `tests/Feature/AnonymisedFixtureSweepTest.php` (not under a module — it sweeps across multiple fixture directories). The existing `tests/Feature/LoopbackOnlyTest.php` is the analog test-file shape.

    All five cases use plain `file_get_contents()` + `preg_match` (case 5 additionally invokes `Process` for the extraction); no facade calls; the test itself does the work directly so constructor-DI does not apply. The test ships GREEN — it never goes through a Red phase.
  </action>
  <verify>
    <automated>test -f tests/Feature/AnonymisedFixtureSweepTest.php && vendor/bin/pest tests/Feature/AnonymisedFixtureSweepTest.php --exclude-group=integration --stop-on-failure && grep -q 'AnonymisedFixtureSweep' tests/Feature/AnonymisedFixtureSweepTest.php && grep -q 'phase-3' tests/Feature/AnonymisedFixtureSweepTest.php && vendor/bin/phpstan analyse tests/Feature/AnonymisedFixtureSweepTest.php --memory-limit=1G --no-progress && vendor/bin/pint --test tests/Feature/AnonymisedFixtureSweepTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Feature/AnonymisedFixtureSweepTest.php` exists
    - Five `it(...)` cases present, each tagged `->group('phase-3')`; case 5 additionally tagged `->group('integration')`
    - All four non-integration cases pass: `vendor/bin/pest tests/Feature/AnonymisedFixtureSweepTest.php --exclude-group=integration --stop-on-failure` exits zero
    - The integration case passes when poppler is on PATH: `vendor/bin/pest tests/Feature/AnonymisedFixtureSweepTest.php --group=integration --stop-on-failure` exits zero on the executor's macOS Herd machine (the executor verifies this before claiming done; CI hosts may legitimately exclude the group)
    - Larastan level 10 strict clean: `vendor/bin/phpstan analyse tests/Feature/AnonymisedFixtureSweepTest.php --memory-limit=1G` exits zero
    - Pint clean: `vendor/bin/pint --test tests/Feature/AnonymisedFixtureSweepTest.php` exits zero
    - No facade / helper slippage: `! grep -E 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' tests/Feature/AnonymisedFixtureSweepTest.php >/dev/null`
  </acceptance_criteria>
  <done>The repo-wide anonymisation-sweep guard test is committed; the committed ICS fixtures are protected against accidental PII regressions on every future build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filesystem → repo | Raw ICS PDF crosses from `local/ics/` into the build through the `pdftotext` extraction + the anonymisation script; only the redacted output enters git |
| Repo → public source control | Committed fixture is read by every contributor / CI; must contain no real PAN, no real cardholder name, no real IBAN, no email / phone PII linkable to the user |
| External binary (`pdftotext`) → PHP runtime | poppler is invoked via `composer require spatie/pdf-to-text` plus a documented `brew install poppler` step; supply-chain risk on the binary is the user's responsibility (poppler.freedesktop.org link in README sets that framing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.txt` | mitigate | The anonymisation script redacts every 12+ digit run, IBAN-shape, email, phone-number, and cardholder-banner. The acceptance criterion `grep -cE '[0-9]{12,}' <fixture>` MUST return `0` before commit, AND the Task 7 sweep test fails the build on any future regression. |
| T-03-01-02 | Information Disclosure | Raw user file at `local/ics/raw-ics-statement.pdf` | accept | `.gitignore` keeps the file out of commits via the `/local/` carve-out. The file lives on the user's machine only; the user can `rm local/ics/raw-ics-statement.pdf` once the redacted fixture is committed to remove the original from disk (Task 3 leaves it in place for re-extraction; the user decides when to delete). |
| T-03-01-03 | Tampering | `scripts/anonymize_ics_text.php` | mitigate | The script is committed alongside the fixture so re-runs are auditable; the fixture record's "Anonymisation protocol" section documents the exact rules. If a future contributor refines the script, they can re-run it on a fresh export and diff the output. |
| T-03-01-04 | Information Disclosure | Synthetic FX row (if added per resume-signal `synthesise-fx`) | accept | The synthetic row uses UI-SPEC example values (`$12.99 → €12.07`) which are not derived from any real transaction; no PII risk. |
| T-03-01-05 | Tampering | `pdftotext` binary path discovery via `spatie/pdf-to-text` | mitigate | Wave 2's `PdfTextExtractor` injects the binary path via constructor / config (default `/opt/homebrew/bin/pdftotext` for Apple-silicon Herd). The Wave 0 README documents `pdftotext -v` as the sanity check. The binary is installed from the official Homebrew formula → upstream poppler.freedesktop.org per README — supply-chain risk is bounded to the package manager. |
| T-03-01-06 | Spoofing | A future contributor commits a raw PDF by mistake | mitigate | `.gitignore` carves out `/local/`. The user must explicitly `git add` a path outside `/local/` to commit it, AND the Task 7 sweep test fails the build if a PII-shaped string lands in any committed fixture. Belt + braces. |
| T-03-01-07 | Information Disclosure | Tiny synthetic `ics-sample-tiny.pdf` | mitigate | Generated from a synthetic UTF-8 source containing only placeholders + one EUR-native row; Task 5 step 4 paranoia-greps the extracted text for 12+ digit runs and IBAN shapes; Task 7's case 5 (integration) does the same on every future build. |
</threat_model>

<verification>
- spatie/pdf-to-text installed; README documents the brew install poppler prerequisite + the `pdftotext -v` sanity check
- `.gitignore` carves out `/local/` so the raw PDF stays out of the repo
- Anonymised redacted-text fixture is committed; passes the no-12+-digits + no-IBAN-shapes + KAARTHOUDER-placeholder-present + card-placeholder-present sanity sweep
- Fixture-record markdown documents every empirical detail Wave 2 needs (anchor tokens, per-page noise, FX shape, source_ref, markup, statement-summary tokens, Dutch formats, masked-card schema)
- Tiny synthetic `ics-sample-tiny.pdf` committed and parses via `pdftotext` to a text containing the literal `SYNTHETIC`
- `tests/Pest.php` documents the phase-3 group convention
- Nine failing test scaffolds across Ingestion / Import / Core / Ledger; total `it(...)` count ≥ 30 (~55); zero `->skip()` calls
- `vendor/bin/pest --group=phase-3` runs Red as designed (the scaffolds explicitly fail; subsequent plans drive them Green)
- `tests/Feature/AnonymisedFixtureSweepTest.php` is Green; protects the committed fixture against future PII regressions
- `vendor/bin/pint --test` exits zero on every new / modified file
- `vendor/bin/phpstan analyse` (Larastan level 10 strict) exits zero on every new file
- No facade / helper slippage in any new PHP source (CLAUDE.md project rule)
- No `.planning/` or D-XX references in any committed PHP source (CLAUDE.md GSD-agnostic rule)
</verification>

<success_criteria>
1. spatie/pdf-to-text installed and resolvable; README + .gitignore reflect the new local/ + brew install poppler workflow.
2. Anonymised redacted-text fixture + fixture-record markdown + anonymisation script + tiny synthetic .pdf + tiny .md committed.
3. Anonymisation-sweep guard test Green; nine failing test scaffolds Red; phase-3 group convention documented.
4. Raw PDF stays out of the repo: `git check-ignore local/ics/raw-ics-statement.pdf` exits zero.
5. No PII surviving in any committed artefact (verified by Task 7's sweep test on every future build).
</success_criteria>

<output>
After completion, create `.planning/phases/03-ics-cards-multi-currency-display/03-01-SUMMARY.md` documenting:
- The exact `pdftotext -layout` flag set and command used; whether macOS Herd shipped poppler out-of-the-box or required `brew install poppler`.
- The empirical anchor tokens identified for the transactions-table region (D-53) — quoted verbatim so plan 03-02 can drop them into `IcsPdfExtractionMap` constants.
- The D-34 / D-35 / D-40 / D-51 / D-52 / D-53 / D-56 dispositions (which option each one resolved to empirically; if a CONTEXT.md decision was contradicted, surface it under "Major Deviations" — the orchestrator may emit a follow-up CONTEXT.md addendum or downstream plan revision).
- Whether the synthetic FX row was added or the export already contained one; the merchant string and amount values used.
- The chosen PDF-generation path for the tiny synthetic .pdf (cupsfilter, groff, or hand-crafted) and the exact invocation.
- Total `it(...)` invocation count across the nine scaffolds; whether any scaffold needed a deviation from the planned case list (e.g. an extra case that emerged during scaffolding).
- Whether the anonymisation-sweep test's `integration` case ran Green on the executor's machine (poppler-on-PATH confirmation).
</output>
