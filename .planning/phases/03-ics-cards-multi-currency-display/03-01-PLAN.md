---
phase: 03-ics-cards-multi-currency-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv
  - Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md
  - Modules/Ingestion/tests/fixtures/ics/anonymize_ics.py
  - Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php
  - Modules/Import/tests/Feature/IcsCsvImportTest.php
  - Modules/Core/tests/Feature/SettingsPageTest.php
  - Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php
  - Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php
  - Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php
  - tests/Pest.php
autonomous: false
requirements:
  - ING-04
  - LED-03
  - MC-02
  - UI-06

must_haves:
  truths:
    - "Anonymised ICS CSV fixture exists with at least one foreign-currency row (e.g. USD); anonymisation is OUR job, not the user's (D-32)"
    - "Fixture record documents column map, FX-row shape (D-35), source_ref availability (D-34), markup separability (D-40)"
    - "Phase-3 Pest group is registered so `pest --group=phase-3` runs phase tests"
    - "Failing test scaffolds exist for every Phase 3 requirement so subsequent waves drive them green"
  artifacts:
    - path: "Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv"
      provides: "Anonymised real ICS CSV export — dates / amounts / currencies / merchants preserved"
    - path: "Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md"
      provides: "Empirical fixture record (column map, encoding, delimiter, FX shape, anonymisation protocol)"
      contains: "Column map"
    - path: "Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php"
      provides: "Failing scaffold for IcsCsvAdapter unit + snapshot tests"
    - path: "Modules/Import/tests/Feature/IcsCsvImportTest.php"
      provides: "Failing scaffold for ING-04 end-to-end import test"
    - path: "Modules/Core/tests/Feature/SettingsPageTest.php"
      provides: "Failing scaffold for D-45 Settings round-trip"
    - path: "Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php"
      provides: "Failing scaffold for D-44 + D-47 toggle UX"
    - path: "Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php"
      provides: "Failing scaffold for D-46 GROUP-BY-currency mode"
    - path: "Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php"
      provides: "Failing scaffold for D-48 effective-rate surface"
  key_links:
    - from: "tests/Pest.php"
      to: "phase-3 group"
      via: "->group('phase-3')"
      pattern: "phase-3"
    - from: "tests/Contracts/IdempotencyContractTest.php"
      to: "Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv"
      via: "dataset addition in Plan 02"
      pattern: "ics-sample-1.csv"
---

<objective>
Wave 0 enablement for Phase 3: receive the raw ICS Cards CSV export from the user, anonymise it in-repo (D-32), commit the redacted fixture + a fixture-record markdown that documents the empirical column layout / FX-row shape (D-35) / source_ref availability (D-34) / markup separability (D-40), register the `phase-3` Pest group, and land failing test scaffolds for every Phase 3 requirement so subsequent plans can drive them GREEN.

Purpose: Phase 2's plan 02-01 proved the value of front-loading fixture + group registration before any adapter code. Without this, plan 03-02 would have to assume column indices and re-baseline tests later when the real shape surfaces. The anonymisation is OUR responsibility, not the user's (per D-32) — strip card numbers, cardholder names, any cross-referenceable identifiers; preserve dates, amounts, currencies, and merchant strings verbatim.

Output: Anonymised fixture + fixture record + phase-3 group registration + failing test scaffolds. The fixture record's Column Map section locks in the constants that 03-02 will copy into `IcsCsvHeaderProfile` and `IcsCsvColumnMap`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
@.planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
@.planning/phases/03-ics-cards-multi-currency-display/03-VALIDATION.md
@.planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
@.planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md

# Phase 1/2 fixture pattern to mirror verbatim:
@tests/fixtures/asn-sample-1.md

# Phase 2 group-registration precedent:
@tests/Pest.php

# Idempotency dataset shape (extended in 03-02, scaffolded here):
@tests/Contracts/IdempotencyContractTest.php

<interfaces>
<!-- Existing contracts the executor needs. Extracted from codebase. -->
<!-- Use these directly — no codebase exploration needed. -->

From Modules/Ingestion/Public/Dto/SourceTransactionDto.php (current, BEFORE D-42 extension):
```php
final class SourceTransactionDto extends Data
{
    public function __construct(
        public readonly CarbonImmutable $bookedAt,
        public readonly CarbonImmutable $postedAt,
        public readonly CarbonImmutable $valueDate,
        public readonly string $ownIban,
        public readonly ?string $counterpartyIban,
        public readonly ?string $counterpartyName,
        public readonly string $currency,
        public readonly int $amountMinor,
        public readonly ?string $sourceRef,
        public readonly ?string $description,
        public readonly array $rawPayload,
        public readonly int $sourceRowIndex,
    ) {}
}
```

From Modules/Ledger/Models/Account.php:
```php
final class Account extends Model {
    protected $fillable = ['user_id', 'name', 'slug', 'kind', 'iban', 'default_currency'];
    // existing `kind` values: 'bank', 'paypal', ...; ICS adds 'ics_card' in plan 03-02
}
```

Failing-scaffold convention (Phase 2 precedent): every scaffold test file MUST contain at least one runnable `it(...)` with an explicit failure (e.g. `expect(true)->toBe(false, 'scaffold — replace in plan 03-02');`) so the suite is RED until the implementing plan delivers the behaviour. Skipped tests are NOT acceptable — they hide gaps.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Receive raw ICS CSV export from user</name>
  <read_first>
    .planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
    .planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
    tests/fixtures/asn-sample-1.md
  </read_first>
  <what-built>Nothing yet — this is the human-action gate. Subsequent automated tasks anonymise and commit the file.</what-built>
  <how-to-verify>
    1. Download a recent ICS Cards CSV export from the icscards.nl portal. **Important:** the export MUST include at least one foreign-currency charge (e.g. a USD purchase, GBP charge, etc.) — pick a statement period containing one if recent statements are EUR-only (per Pitfall 2 in 03-RESEARCH.md). If no recent statement has FX rows, request an older statement from the portal.
    2. Place the file at `~/Downloads/ics-raw.csv` (or any local path) — do NOT commit it.
    3. Tell the executor the absolute path to the raw file.

    The executor must NOT receive the raw file via a commit, paste, or any tracked location. Per D-32 the raw file is processed in-place and discarded; only the anonymised fixture lands in git.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms raw ICS CSV is on local disk at a known absolute path
    - Human confirms file contains at least one non-EUR currency row (or explicitly notes "no FX rows available, synthetic FX row will be added during anonymisation")
    - Raw file is NOT in the git tree (verify with `git status -- $(realpath ~/Downloads/ics-raw.csv)` returning untracked or path outside repo)
  </acceptance_criteria>
  <resume-signal>Reply with the absolute path to the raw ICS CSV and "approved", e.g. `/Users/wesselverheij/Downloads/ics-raw.csv approved`. If the raw export has no FX rows, append "synthesise FX" and the executor will add one synthetic USD row at anonymisation time preserving a plausible USD → EUR rate.</resume-signal>
  <done>Human has provided absolute path + approval signal; raw file is reachable from the working tree but not committed.</done>
</task>

<task type="auto">
  <name>Task 2: Anonymise raw ICS CSV, commit redacted fixture + fixture record + anonymisation script, register phase-3 Pest group</name>
  <read_first>
    tests/fixtures/asn-sample-1.md
    tests/fixtures/asn-sample-1.csv
    tests/Pest.php
    Modules/Ingestion/Internal/Adapters/Asn/AsnCsvHeaderProfile.php
    Modules/Ingestion/Internal/Adapters/Asn/AsnCsvColumnMap.php
    .planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
    .planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
  </read_first>
  <action>
    1. **Inspect the raw file** at the path the user provided. Use `file -bI <path>` to confirm encoding (UTF-8 / windows-1252 / us-ascii) and `head -3 <path>` to capture the literal header row. Do NOT print full data rows to stdout — the file may contain real PAN or merchant data. Use a temp scratch file outside the repo for intermediate work.

    2. **Create `Modules/Ingestion/tests/fixtures/ics/anonymize_ics.py`** — a small, re-runnable Python 3 script that:
       - Reads the raw CSV path from `argv[1]`
       - Writes the anonymised CSV to `argv[2]` (caller chooses output path)
       - **Strips PII verbatim:** any column containing a card number (any digit run of length ≥ 12 anywhere in the row) is replaced with `XXXXXXXXXXXX####` where `####` is the last 4 digits if present, else `0000`. Cardholder-name column (if present in the empirical header) is replaced with the literal `CARDHOLDER`. Any 16-digit-or-more digit run inside the description column is scrubbed to `[redacted]`.
       - **Preserves verbatim:** all date columns, all amount columns, all currency columns, all merchant-name columns (the merchant string is the user-visible signal; do not modify), auth-code columns, transaction-type / category columns.
       - Writes the same delimiter, encoding, and column count as the input.
       - Mirrors the structure and comment style of any prior anonymisation script if one exists in `tests/fixtures/` (Phase 1's `anonymize_asn.py` — referenced in `tests/fixtures/asn-sample-1.md` §"Anonymization protocol"). If no prior script exists in the tree, write a fresh one with a `--help` flag and a doctring describing the protocol.
       - **If the human appended "synthesise FX" to the resume signal** (no FX row in the raw file), AFTER anonymisation append exactly ONE synthetic row to the output. The synthetic row uses: a plausible date matching the period, a synthetic merchant string like `"SYNTHETIC USD MERCHANT"`, original-currency `USD`, original-amount `12.99`, settled-EUR `12.07` (these mirror UI-SPEC §"Currency display" example values verbatim — `0.929 EUR per 1 USD`), in whatever column positions the empirical layout dictates. Document the synthetic row in the fixture record's "Anonymisation protocol" section.

    3. **Run the script:** `python3 Modules/Ingestion/tests/fixtures/ics/anonymize_ics.py <raw-path> Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv`. Confirm the output file exists and has the same row count as the input (± 1 if FX row was synthesised).

    4. **Sanity-grep the output** for any residual PII before committing:
       - `grep -Ec '[0-9]{12,}' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv` → MUST output `0` (no 12+ digit run remains anywhere).
       - `grep -ic '<cardholder-real-first-name>' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv` for the user's real first name if you know it from `git config user.name` → MUST output `0`.

    5. **Write `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md`** mirroring `tests/fixtures/asn-sample-1.md` headings exactly:
       - `# ICS CSV — empirical fixture record`
       - `## Confirmed format` — table with rows: Delimiter, Encoding (source), Encoding (committed fixture), BOM, Header row, Column count, Date format, Decimal separator, Amount sign style, Multi-line cells. Fill from the empirical inspection in step 1.
       - `## Column map` — table `| Index | Header (NL/EN) | Field | Notes |` with one row per empirical column. The "Field" column maps to canonical names: `posted date`, `merchant name`, `original amount`, `original currency`, `settled amount (EUR)`, `auth code`, `card number (discarded per D-37)`, etc.
       - `## D-34 source_ref availability` — one paragraph stating EITHER "auth-code column at index N is a stable per-transaction reference suitable for `source_ref`" OR "no stable per-transaction reference is exposed; `source_ref` will be NULL for ICS rows and the v3 fingerprint is the only dedup anchor".
       - `## D-35 FX-row shape` — one paragraph stating EITHER "shape (a): one CSV row carries both original-currency and settled-EUR columns" OR "shape (b): each FX charge appears as two adjacent CSV rows (merchant line + FX-conversion line) sharing column X". Include a literal redacted example of one FX row (or pair).
       - `## D-40 markup separability` — one paragraph stating whether the CSV exposes the ICS markup as a separately addressable cell or whether the markup is folded into the settled-EUR amount with no row-level split.
       - `## Anonymisation protocol` — describe the script's rules, list which columns were redacted vs preserved verbatim, and (if applicable) note the synthetic FX row added per the "synthesise FX" branch.

    6. **Register the `phase-3` Pest group** in `tests/Pest.php`. Mirror the Phase 2 pattern verbatim. Find the existing `phase-2` group registration (use `grep -n "phase-2" tests/Pest.php` to locate the exact lines) and add an identically-shaped `->group('phase-3')` registration covering test paths under `Modules/Ingestion/tests/Unit/Adapters/Ics/`, `Modules/Import/tests/Feature/IcsCsvImportTest.php`, `Modules/Core/tests/Feature/SettingsPageTest.php`, `Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php`, `Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php`, `Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php`.

    7. **Delete the raw export.** After step 3 succeeds, `rm <raw-path>` (the path the human supplied). The raw file must not survive into a subsequent session. The fixture record's "Anonymisation protocol" section notes this hygiene step.
  </action>
  <verify>
    <automated>test -f Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv && test -f Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md && test -f Modules/Ingestion/tests/fixtures/ics/anonymize_ics.py && [ "$(grep -Ec '[0-9]{12,}' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv)" = "0" ] && grep -q 'phase-3' tests/Pest.php && grep -q 'D-34' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md && grep -q 'D-35' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md && grep -q 'D-40' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md && grep -q 'Column map' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md</automated>
  </verify>
  <acceptance_criteria>
    - `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv` exists
    - `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md` exists and contains the headings `## Confirmed format`, `## Column map`, `## D-34 source_ref availability`, `## D-35 FX-row shape`, `## D-40 markup separability`, `## Anonymisation protocol`
    - `Modules/Ingestion/tests/fixtures/ics/anonymize_ics.py` exists, has a doctring, accepts `argv[1]` (input) and `argv[2]` (output)
    - `grep -Ec '[0-9]{12,}' Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv` outputs `0` (no 12+ digit run survived anonymisation)
    - `tests/Pest.php` contains a `phase-3` group registration covering the six scaffold test paths listed in the action; running `vendor/bin/pest --group=phase-3 --list-tests 2>&1 | grep -c "ICS\|Settings\|Currency\|Fx"` returns a count > 0 (group is wired)
    - The raw user export path is no longer reachable on disk (executor's responsibility per step 7)
  </acceptance_criteria>
  <done>Anonymised fixture, fixture record, anonymisation script, and phase-3 group registration are committed. Raw user file deleted from disk.</done>
</task>

<task type="auto">
  <name>Task 3: Write failing test scaffolds for every Phase 3 requirement</name>
  <read_first>
    Modules/Ingestion/tests/Unit/AsnCsvAdapterTest.php
    Modules/Import/tests/Feature/AsnCsvImportTest.php
    Modules/Core/tests/Feature/InstallCommandTest.php
    Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md
    tests/Pest.php
    .planning/phases/03-ics-cards-multi-currency-display/03-VALIDATION.md
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
  </read_first>
  <action>
    Create each test file below as a runnable failing scaffold. Every file MUST be parseable PHP (so `vendor/bin/pest --group=phase-3 --list-tests` succeeds) and every top-level `it(...)` MUST fail when executed (so Wave 2-4 plans drive them GREEN). Use the literal failure pattern `expect(true)->toBe(false, '<reason> — implemented in plan 03-NN');` — do NOT use `->skip()` because skipped tests hide gaps from the Nyquist sampling contract in `03-VALIDATION.md`.

    Every file uses `pest()->group('phase-3');` at the top so the registration from Task 2 picks it up automatically (Pest's group inheritance from file-level group calls).

    **File 1: `Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php`**
    Mirror the scaffolding from `Modules/Ingestion/tests/Unit/AsnCsvAdapterTest.php` (the same `beforeEach` AccountResolver stub pattern). Required failing scaffolds (one `it(...)` per behaviour, each failing with "implemented in plan 03-02"):
    - `it('reports its stable format identifier')`
    - `it('parses the real fixture into SourceTransactionDtos')`
    - `it('preserves the full source row in rawPayload for audit')`
    - `it('emits monotonically increasing sourceRowIndex starting at zero')`
    - `it('rejects a file that fails the header sniffer before reading any data row')`
    - `it('yields native + settled pair for a foreign-currency row')` — guards Pitfall 1 (RESEARCH.md §"Common Pitfalls")
    - `it('yields settledAmountMinor=null for an EUR-native row')` — D-42 honesty contract (adapter reports what the source provided)
    - `it('discards the card number column at the adapter boundary per D-37')` — landmine guard
    - `it('registers under the ics-csv key in the SourceAdapterRegistry')`

    **File 2: `Modules/Import/tests/Feature/IcsCsvImportTest.php`**
    Mirror `Modules/Import/tests/Feature/AsnCsvImportTest.php`. Required failing scaffolds:
    - `it('imports every parsed row from the gold fixture on the first run')` — ING-04 Success Criterion 1
    - `it('returns zero new rows when re-importing the same file')` — ING-06 reuse for ICS
    - `it('persists settled_amount_minor and settled_currency for an EUR-native row')` — LED-03 mirror branch
    - `it('persists native + settled + fx_rate_used for a foreign-currency row')` — LED-03 + D-39 derivation
    - `it('prompts the user to name the ICS Account on the first ICS upload')` — D-36 / D-38 (driven GREEN in plan 03-03)
    - `it('skips the name-your-account step on subsequent ICS uploads')` — D-38 skip path (driven GREEN in plan 03-03)

    **File 3: `Modules/Core/tests/Feature/SettingsPageTest.php`**
    Use the `Modules\Core\Models\User` row + `actingAs` pattern from `Modules/Core/tests/Feature/InstallCommandTest.php`. Required failing scaffolds:
    - `it('renders the Settings page with the user current preferences pre-filled')` — D-45
    - `it('persists default_currency_view when changed')` — MC-02 round-trip step
    - `it('persists period_start_day when changed')` — D-45 co-discharge of Phase 1 D-19
    - `it('rejects period_start_day outside 1..28')` — validation
    - `it('rejects default_currency_view outside {eur_only, original}')` — validation
    - `it('round-trips default_currency_view = original into TransactionsList default mode')` — Pitfall 5 four-boundary path (covers Settings → DB → query → URL fallback)

    **File 4: `Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php`**
    Required failing scaffolds:
    - `it('defaults the currency property to the user default_currency_view preference when no URL override is present')` — D-44
    - `it('overrides the user preference when ?currency=eur is present in the URL')` — D-44 URL precedence
    - `it('overrides the user preference when ?currency=original is present in the URL')`
    - `it('renders two lines for a foreign-currency row in original mode')` — D-47 (UI-06)
    - `it('renders one line for a foreign-currency row in eur mode')` — D-47
    - `it('renders one line for an EUR-native row in original mode')` — D-47 collapse
    - `it('keeps the URL clean when the toggle is on the default value')` — D-44 `except: ''` semantics

    **File 5: `Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php`**
    Required failing scaffolds:
    - `it('returns one tile-row per currency in original mode for a mixed EUR/USD month')` — D-46
    - `it('collapses an EUR-only month to one tile-row in original mode')` — D-46 "specifics" collapse rule
    - `it('omits zero-activity currencies from the original-mode tile rows')` — UI-SPEC §"Per-currency KPI tile rows"
    - `it('returns one summary in eur_only mode (settled-EUR sum)')` — preserves the Phase 1 path
    - `it('orders the original-mode tile rows by settled_currency alphabetically')` — UI-SPEC determinism

    **File 6: `Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php`**
    Required failing scaffolds:
    - `it('renders the Effective rate line when fx_rate_used is populated')` — D-48
    - `it('omits the Effective rate line when fx_rate_used is NULL')` — D-48 null branch
    - `it('formats the rate as EUR-per-unit-native with 3 decimal places')` — UI-SPEC §"FX rate on the transaction detail page" locked format `€0.929 / USD`
    - `it('renders the Includes any ICS markup helper text below the rate')` — UI-SPEC helper text contract

    **All six files** end with a single trailing newline and pass `vendor/bin/pint --test`.
  </action>
  <verify>
    <automated>test -f Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php && test -f Modules/Import/tests/Feature/IcsCsvImportTest.php && test -f Modules/Core/tests/Feature/SettingsPageTest.php && test -f Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php && test -f Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php && test -f Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php && grep -c "^it(" Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php Modules/Import/tests/Feature/IcsCsvImportTest.php Modules/Core/tests/Feature/SettingsPageTest.php Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php | awk -F: '{s+=$2} END{exit !(s>=30)}' && ! vendor/bin/pest --group=phase-3 --stop-on-failure 2>&1 | grep -q 'OK ('</automated>
  </verify>
  <acceptance_criteria>
    - All six scaffold files exist at the paths listed above
    - The total count of top-level `it(` invocations across the six files is at least 30: `grep -c "^it(" <six files> | awk -F: '{s+=$2} END{exit !(s>=30)}'` exits zero (the counts above sum to 35)
    - Running `vendor/bin/pest --group=phase-3 --stop-on-failure` exits non-zero (suite is RED — at least one scaffold fails as designed)
    - No `->skip()` calls in any of the six new files: `grep -c '->skip(' Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php Modules/Import/tests/Feature/IcsCsvImportTest.php Modules/Core/tests/Feature/SettingsPageTest.php Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php` returns `0` on each
    - `vendor/bin/pint --test Modules/Ingestion/tests/Unit/Adapters/Ics Modules/Import/tests/Feature/IcsCsvImportTest.php Modules/Core/tests/Feature/SettingsPageTest.php Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php Modules/Ledger/tests/Feature/DashboardCurrencyModeTest.php Modules/Ledger/tests/Feature/TransactionDetailFxRateTest.php` exits zero
  </acceptance_criteria>
  <done>Six failing test scaffolds committed; `pest --group=phase-3` is RED; subsequent plans have a complete behavioural target to drive GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filesystem → repo | Raw ICS CSV crosses from `~/Downloads/` into the build through the anonymisation script; only the redacted output enters git |
| Repo → public source control | Committed fixture is read by every contributor / CI; must contain no real PAN, no real cardholder name, no real reference data linkable to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | `Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv` | mitigate | The anonymisation script redacts any 12+ digit run (catches full PAN + IBAN-like tokens) and replaces cardholder column with `CARDHOLDER`. The acceptance criterion `grep -Ec '[0-9]{12,}' <fixture>` MUST output `0` before commit. |
| T-03-01-02 | Information Disclosure | Raw user file at `~/Downloads/ics-raw.csv` | mitigate | Step 7 of Task 2 deletes the raw file from disk after anonymisation succeeds. The path is never logged or copied into the repo. |
| T-03-01-03 | Tampering | `anonymize_ics.py` | mitigate | The script is committed alongside the fixture so re-runs are auditable; the fixture record's "Anonymisation protocol" section documents the exact rules. If a future contributor refines the script, they can re-run it on a fresh export and diff the output. |
| T-03-01-04 | Information Disclosure | Synthetic FX row (if added) | accept | The synthetic row uses UI-SPEC example values (`$12.99 → €12.07`) which are not derived from any real transaction; no PII risk. |
</threat_model>

<verification>
- Anonymised fixture exists and passes `grep -Ec '[0-9]{12,}'` returning `0`
- Fixture record documents D-34, D-35, D-40 disposition empirically
- Phase-3 Pest group runs the six scaffold files
- `vendor/bin/pest --group=phase-3 --stop-on-failure` is RED (suite contains the failing scaffolds that subsequent plans will drive GREEN)
- `vendor/bin/pint --test` passes on all new files
- Raw user file deleted from disk after anonymisation
</verification>

<success_criteria>
1. Wave 0 deliverable: anonymised fixture + fixture record + anonymisation script committed; raw file deleted.
2. Phase-3 group registered; `pest --group=phase-3 --list-tests` lists ≥ 30 test cases.
3. Six scaffold files committed; suite is RED with failures pointing to plans 03-02 through 03-07.
4. No PII surviving the anonymisation pass (verified by grep).
</success_criteria>

<output>
After completion, create `.planning/phases/03-ics-cards-multi-currency-display/03-01-SUMMARY.md` documenting:
- The empirical column count, encoding, delimiter, date format, and decimal style of the ICS CSV (extracted from the fixture record for quick reference by plan 03-02).
- The D-34 / D-35 / D-40 dispositions (which option each one resolved to).
- Whether the synthetic FX row was added or the export already contained one.
- Any unexpected column / shape discovery that should feed into a CONTEXT.md addendum (per the "Risks Phase 3 Specifically Owns" note in 03-CONTEXT.md).
</output>
