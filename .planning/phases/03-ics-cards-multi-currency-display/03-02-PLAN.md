---
phase: 03-ics-cards-multi-currency-display
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - Modules/Ingestion/Public/Dto/SourceTransactionDto.php
  - Modules/Ingestion/Public/Services/HeaderSniffer.php
  - Modules/Ingestion/Providers/IngestionServiceProvider.php
  - Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php
  - Modules/Ingestion/Internal/Adapters/Ics/IcsCsvHeaderProfile.php
  - Modules/Ingestion/Internal/Adapters/Ics/IcsCsvColumnMap.php
  - Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php
  - Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php
  - Modules/Ingestion/tests/Unit/Adapters/Ics/__snapshots__/parses-the-real-fixture-into-source-transaction-dtos.snap
  - Modules/Import/tests/Feature/IcsCsvImportTest.php
  - Modules/Import/tests/Unit/Pipeline/Stages/NormalizeStageTest.php
  - tests/Contracts/IdempotencyContractTest.php
autonomous: true
requirements:
  - ING-04
  - LED-03

must_haves:
  truths:
    - "User uploads an ICS CSV (already validated by the wizard validator extended in plan 03-03) and its rows land in the transactions table when the ICS account exists"
    - "A foreign-currency ICS row lands with native currency + amount AND settled-EUR currency + amount AND derived fx_rate_used"
    - "An EUR-native ICS row lands with settled = native and fx_rate_used = NULL (D-41)"
    - "CSV is the only supported ICS format; no phpoffice/phpspreadsheet dependency is introduced (D-31)"
    - "Card-number column from the source CSV is dropped at the adapter boundary and NOT preserved in rawPayload (D-37)"
    - "Re-importing the same ICS CSV produces zero new rows (idempotency)"
    - "IdempotencyContractTest covers the ics-csv format via its dataset"
  artifacts:
    - path: "Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php"
      provides: "ICS CSV → SourceTransactionDto generator (lazy, league/csv, BOM-safe)"
      min_lines: 100
    - path: "Modules/Ingestion/Internal/Adapters/Ics/IcsCsvHeaderProfile.php"
      provides: "FORMAT='ics-csv', DELIMITER, SOURCE_ENCODING, EXPECTED_COLUMN_COUNT, HEADER_SIGNATURE, DATE_FORMAT constants (sourced from 03-01-SUMMARY.md)"
    - path: "Modules/Ingestion/Internal/Adapters/Ics/IcsCsvColumnMap.php"
      provides: "Zero-based column index constants for ICS CSV (POSTED_DATE, ORIGINAL_AMOUNT, ORIGINAL_CURRENCY, SETTLED_AMOUNT, MERCHANT, AUTH_CODE, CARD_NUMBER)"
    - path: "Modules/Ingestion/Public/Dto/SourceTransactionDto.php"
      provides: "Extended with nullable settledAmountMinor, settledCurrency, fxRateUsed (D-42)"
      contains: "settledAmountMinor"
    - path: "Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php"
      provides: "D-42 substitution branch (settled = native when source omits) + D-39 fx_rate_used derivation via BigDecimal"
      contains: "BigDecimal"
  key_links:
    - from: "Modules/Ingestion/Providers/IngestionServiceProvider.php"
      to: "Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php"
      via: "SourceAdapterRegistry map entry 'ics-csv' => IcsCsvAdapter::class"
      pattern: "ics-csv.*IcsCsvAdapter"
    - from: "Modules/Ingestion/Public/Services/HeaderSniffer.php"
      to: "Modules/Ingestion/Internal/Adapters/Ics/IcsCsvHeaderProfile.php"
      via: "match arm dispatching IcsCsvHeaderProfile::FORMAT to sniffIcsCsv"
      pattern: "IcsCsvHeaderProfile::FORMAT"
    - from: "Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php"
      to: "Brick\\Math\\BigDecimal"
      via: "fx_rate_used = settled_minor / native_minor at scale 8"
      pattern: "BigDecimal::of"
---

<objective>
Wire-level ICS CSV ingestion: adapter parses the gold fixture → NormalizeStage canonicalises native + settled pair + derives fx_rate_used → RecordTransactions inserts with v3-fingerprint idempotency → re-import is a no-op. The IdempotencyContractTest dataset gains an `ics-csv` row. This plan does NOT touch the wizard UI surface — plan 03-03 owns the two-step issuer-format picker AND the PreviewWizard "Name your ICS Account" step. By keeping this plan strictly wire-level, the file count stays under the warning threshold and Larastan-strict review focuses on the parser/normaliser correctness.

Purpose: Discharge ING-04 (User can upload an ICS Cards CSV and have its transactions imported) and the LED-03 schema population (settled pair + fx_rate_used now actively populated for non-EUR rows for the first time in project history). The schema columns have existed since Phase 1 plan 01-03; this plan finally exercises them with real non-EUR data.

Output: Working ICS CSV parsing path: extended SourceTransactionDto, IcsCsvAdapter + HeaderProfile + ColumnMap + registry wiring + HeaderSniffer dispatch, NormalizeStage substitution + FX-rate derivation, IdempotencyContractTest dataset row, IcsCsvAdapterTest snapshot, and the four import-feature scaffolds that can be made GREEN without the wizard UX (account-naming-related scaffolds remain RED until plan 03-03 ships).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
@.planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
@.planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
@.planning/phases/03-ics-cards-multi-currency-display/03-01-SUMMARY.md

# Closest analogs the executor must mirror (read once, extract pattern in single pass):
@Modules/Ingestion/Internal/Adapters/Asn/AsnCsvAdapter.php
@Modules/Ingestion/Internal/Adapters/Asn/AsnCsvHeaderProfile.php
@Modules/Ingestion/Internal/Adapters/Asn/AsnCsvColumnMap.php
@Modules/Ingestion/Public/Dto/SourceTransactionDto.php
@Modules/Ingestion/Public/Services/HeaderSniffer.php
@Modules/Ingestion/Providers/IngestionServiceProvider.php
@Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php
@Modules/Ingestion/tests/Unit/AsnCsvAdapterTest.php
@Modules/Import/tests/Feature/AsnCsvImportTest.php
@Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv
@Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md
@tests/Contracts/IdempotencyContractTest.php

<interfaces>
<!-- Contracts the executor needs verbatim -->

From Modules/Ingestion/Public/Contracts/SourceAdapter.php (existing):
```php
interface SourceAdapter {
    public function format(): string;
    public function statementMetadata(): ?StatementSummaryData;
    public function parse(string $localPath, AccountResolver $accounts): Generator;
}
```

From Modules/Ingestion/Public/Contracts/AccountResolver.php (existing):
```php
interface AccountResolver {
    public function resolve(string $iban): AccountResolution;
}
```
ICS rows have no IBAN per the source — the adapter passes a synthetic per-user ICS-account identifier (e.g. `ICS-CARD-<userId>`) to `AccountResolver::resolve()`. The resolver returns `KnownAccount` when an `accounts` row with `kind='ics_card'` exists for the user; `UnknownAccount` otherwise (this triggers the naming step in PreviewWizard, wired in plan 03-03).

From Modules/Ledger/Models/Account.php (existing — current `kind` values: 'bank', 'paypal'):
```php
protected $fillable = ['user_id', 'name', 'slug', 'kind', 'iban', 'default_currency'];
```
Phase 3 adds a third value `'ics_card'`. `iban` is non-nullable on the existing schema — for ICS accounts the synthetic identifier (e.g. `ICS-CARD-<userId>`) is used as the iban-column value so the existing UNIQUE(iban) constraint still works. The synthetic shape avoids a schema migration; the account row itself is created in plan 03-03's PreviewWizard step.

D-42 extension target shape (apply to SourceTransactionDto):
```php
public function __construct(
    // ... existing fields verbatim ...
    public readonly int $sourceRowIndex,
    // NEW D-42 — appended with = null defaults so Phase 1/2 adapter call sites stay binary-compatible:
    public readonly ?int $settledAmountMinor = null,
    public readonly ?string $settledCurrency = null,
    public readonly ?string $fxRateUsed = null,
) {}
```

D-39 FX rate derivation formula (apply in NormalizeStage):
```
fx_rate_used = settled_amount_minor / amount_minor       (scale 8, HALF_UP rounding)
```
e.g. USD 12.99 settled to EUR 12.07 → fx_rate_used = "0.92917629" (=1207/1299 to 8 places). Stored as the string form of BigDecimal so `decimal(18,8)` precision is exact.

UI-SPEC §"Currency display" example (locked verbatim — use as the synthetic FX assertion data):
```
$12.99 USD  → settled €12.07 EUR  → fx_rate_used ≈ "0.92917629"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SourceTransactionDto (D-42) + NormalizeStage substitution + FX rate derivation (D-39)</name>
  <files>
    Modules/Ingestion/Public/Dto/SourceTransactionDto.php
    Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php
    Modules/Import/tests/Unit/Pipeline/Stages/NormalizeStageTest.php
  </files>
  <read_first>
    Modules/Ingestion/Public/Dto/SourceTransactionDto.php
    Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php
    Modules/Import/tests/Unit/Pipeline/Stages/NormalizeStageTest.php
    .planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
  </read_first>
  <behavior>
    - When a `SourceTransactionDto` has `settledAmountMinor = null, settledCurrency = null, fxRateUsed = null` (every Phase 1/2 ASN row), `NormalizeStage` MUST set `CanonicalTransaction.settledAmountMinor = source.amountMinor`, `settledCurrency = source.currency`, `fxRateUsed = null`. The existing `NormalizeStageTest::'mirrors native amount/currency to settled amount/currency by default'` test stays GREEN unchanged.
    - When a `SourceTransactionDto` has `settledAmountMinor = 1207, settledCurrency = 'EUR', amountMinor = 1299, currency = 'USD'`, NormalizeStage MUST set `CanonicalTransaction.settledAmountMinor = 1207, settledCurrency = 'EUR', fxRateUsed = '0.92917629'` (the BigDecimal(1207).dividedBy(BigDecimal(1299), 8, HALF_UP) result as a string).
    - When `settledCurrency === currency` (e.g. an EUR-native ICS row that DID supply the settled pair explicitly), NormalizeStage MUST set `fxRateUsed = null` — no FX took place, no rate to derive.
    - When `amountMinor === 0`, NormalizeStage MUST set `fxRateUsed = null` — divide-by-zero guard.
    - `SourceTransactionDto` MUST remain Spatie/laravel-data compatible; existing Phase 1/2 adapter call sites MUST NOT be modified (the three new fields default to `null`).
  </behavior>
  <action>
    1. **Extend `Modules/Ingestion/Public/Dto/SourceTransactionDto.php`:** Append three new readonly nullable properties to the constructor, AFTER `public readonly int $sourceRowIndex`, in this order with these exact names and defaults (per D-42 and PATTERNS.md):
       ```php
       public readonly ?int $settledAmountMinor = null,
       public readonly ?string $settledCurrency = null,
       public readonly ?string $fxRateUsed = null,
       ```
       Update the class docblock to mention the new fields: "`settledAmountMinor` / `settledCurrency` carry the foreign-currency settled-EUR pair when the source row provides it (ICS Cards, future PayPal). `fxRateUsed` is always `null` at the adapter boundary — `NormalizeStage` derives it from the native + settled pair."

    2. **Modify `Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php`:** Locate the section that currently sets `settledAmountMinor: $source->amountMinor`, `settledCurrency: $source->currency`, `fxRateUsed: null` on the `CanonicalTransaction` constructor call (per PATTERNS.md lines 380-388 these are the three lines that currently hard-mirror native→settled). Replace those three CanonicalTransaction-constructor argument lines with a block BEFORE the `return new CanonicalTransaction(...)` that computes:
       ```php
       use Brick\Math\BigDecimal;       // add to imports
       use Brick\Math\RoundingMode;      // add to imports

       // D-42 substitution: settled = native when the source omitted them
       // (Phase 1/2 ASN adapters; EUR-native rows in any adapter).
       $settledMinor = $source->settledAmountMinor ?? $source->amountMinor;
       $settledCurrency = $source->settledCurrency ?? $source->currency;

       // D-39 derived effective rate. Use BigDecimal to preserve decimal(18,8)
       // precision; float is forbidden by NoFloatMoneyArchTest.
       $fxRateUsed = null;
       if ($source->settledAmountMinor !== null
           && $source->settledCurrency !== null
           && $source->amountMinor !== 0
           && $source->settledCurrency !== $source->currency
       ) {
           $fxRateUsed = (string) BigDecimal::of((string) $source->settledAmountMinor)
               ->dividedBy(
                   BigDecimal::of((string) $source->amountMinor),
                   8,
                   RoundingMode::HALF_UP,
               );
       }
       ```
       Then in the `return new CanonicalTransaction(...)` call, replace the three settled/rate arguments with `settledAmountMinor: $settledMinor`, `settledCurrency: $settledCurrency`, `fxRateUsed: $fxRateUsed`. Leave every other CanonicalTransaction constructor argument untouched.

    3. **Extend `Modules/Import/tests/Unit/Pipeline/Stages/NormalizeStageTest.php`:** Add new `it(...)` cases adjacent to the existing 'mirrors native amount/currency to settled amount/currency by default' test:
       - `it('derives fx_rate_used when source supplies a different settled currency')` — construct a `SourceTransactionDto` with `amountMinor=1299, currency='USD', settledAmountMinor=1207, settledCurrency='EUR'`; call NormalizeStage; assert the resulting CanonicalTransaction has `amountMinor=1299, currency='USD', settledAmountMinor=1207, settledCurrency='EUR', fxRateUsed='0.92917629'` (8 decimal places, period separator, no trailing whitespace). Use the BigDecimal-derived exact string — do not approximate.
       - `it('leaves fx_rate_used NULL when source-supplied settled currency equals native currency')` — `amountMinor=1299, currency='EUR', settledAmountMinor=1299, settledCurrency='EUR'` → `fxRateUsed=null`.
       - `it('leaves fx_rate_used NULL when amountMinor is zero')` — divide-by-zero guard with `amountMinor=0, currency='USD', settledAmountMinor=0, settledCurrency='EUR'` → `fxRateUsed=null`.
       - `it('mirrors settled = native and leaves fx_rate_used NULL when source omits the settled pair')` — `settledAmountMinor=null, settledCurrency=null, fxRateUsed=null` (the Phase 1/2 ASN call shape) → CanonicalTransaction has `settledAmountMinor=amountMinor`, `settledCurrency=currency`, `fxRateUsed=null`. This is the regression guard for Phases 1/2.

       Tag the new tests with `pest()->group('phase-3');` if the file does not already group-tag from Phase 2. Existing assertions stay untouched.
  </action>
  <verify>
    <automated>vendor/bin/pest --filter='NormalizeStage' --stop-on-failure && vendor/bin/phpstan analyse Modules/Ingestion/Public/Dto/SourceTransactionDto.php Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php --memory-limit=1G && vendor/bin/pint --test Modules/Ingestion/Public/Dto/SourceTransactionDto.php Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php Modules/Import/tests/Unit/Pipeline/Stages/NormalizeStageTest.php && grep -q 'BigDecimal' Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php && grep -q 'settledAmountMinor' Modules/Ingestion/Public/Dto/SourceTransactionDto.php && ! grep -E 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `Modules/Ingestion/Public/Dto/SourceTransactionDto.php` contains all three new fields named exactly `settledAmountMinor`, `settledCurrency`, `fxRateUsed` (grep returns exact name matches)
    - `Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php` imports `Brick\Math\BigDecimal` and `Brick\Math\RoundingMode` and contains a `->dividedBy(...8, RoundingMode::HALF_UP)` call
    - `vendor/bin/pest --filter='NormalizeStage' --stop-on-failure` exits zero (all four new assertions GREEN plus the existing regression)
    - The `tests/Contracts/NoFloatMoneyArchTest.php` test stays GREEN — `vendor/bin/pest --filter='NoFloatMoney' --stop-on-failure` exits zero (no `(float)` casts introduced)
    - `vendor/bin/phpstan analyse Modules/Ingestion/Public/Dto/SourceTransactionDto.php Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php --memory-limit=1G` exits zero (Larastan level 10 strict clean)
    - `vendor/bin/pint --test` exits zero on all three modified files
    - No facade / helper slippage in NormalizeStage: `! grep -E 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php >/dev/null`
    - Phase 1/2 ASN adapter call sites in `Modules/Ingestion/Internal/Adapters/Asn/*Adapter.php` are NOT modified: `git diff --stat Modules/Ingestion/Internal/Adapters/Asn/ | grep -c 'AsnCsvAdapter\|AsnCamt053Adapter\|AsnMt940Adapter'` returns `0`
  </acceptance_criteria>
  <done>SourceTransactionDto carries the D-42 nullable settled pair + rate; NormalizeStage substitutes/derives per D-42 + D-39; four new NormalizeStage assertions GREEN; Phase 1/2 ASN regression GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build IcsCsvAdapter + HeaderProfile + ColumnMap, wire through HeaderSniffer + registry, drive all IcsCsvAdapterTest scaffolds GREEN</name>
  <files>
    Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php
    Modules/Ingestion/Internal/Adapters/Ics/IcsCsvHeaderProfile.php
    Modules/Ingestion/Internal/Adapters/Ics/IcsCsvColumnMap.php
    Modules/Ingestion/Public/Services/HeaderSniffer.php
    Modules/Ingestion/Providers/IngestionServiceProvider.php
    Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php
    Modules/Ingestion/tests/Unit/Adapters/Ics/__snapshots__/parses-the-real-fixture-into-source-transaction-dtos.snap
  </files>
  <read_first>
    Modules/Ingestion/Internal/Adapters/Asn/AsnCsvAdapter.php
    Modules/Ingestion/Internal/Adapters/Asn/AsnCsvHeaderProfile.php
    Modules/Ingestion/Internal/Adapters/Asn/AsnCsvColumnMap.php
    Modules/Ingestion/Public/Services/HeaderSniffer.php
    Modules/Ingestion/Providers/IngestionServiceProvider.php
    Modules/Ingestion/tests/Unit/AsnCsvAdapterTest.php
    Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv
    Modules/Ingestion/tests/fixtures/ics/ics-sample-1.md
    .planning/phases/03-ics-cards-multi-currency-display/03-01-SUMMARY.md
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
  </read_first>
  <behavior>
    - `IcsCsvAdapter::format()` returns the string `'ics-csv'`.
    - `IcsCsvAdapter::parse()` is a `Generator` that yields one `SourceTransactionDto` per logical transaction in the fixture, BOM-safe, encoding-converted via `League\Csv\CharsetConverter`.
    - For an EUR-native row: yielded DTO has `currency = 'EUR'`, `amountMinor = <settled cents>`, `settledAmountMinor = null`, `settledCurrency = null`, `fxRateUsed = null` (per D-42 honesty contract).
    - For a foreign-currency row: yielded DTO has `currency = '<original code>'`, `amountMinor = <original cents>`, `settledAmountMinor = <EUR cents>`, `settledCurrency = 'EUR'`, `fxRateUsed = null` (always null at adapter boundary; NormalizeStage derives).
    - `rawPayload` preserves every column from the source row EXCEPT the card-number cell, which is replaced with the literal `'<discarded per D-37>'` before being written to `rawPayload`. (Verified by Pitfall guard.)
    - `sourceRowIndex` is zero-based, monotonically increasing, one per logical transaction yielded (NOT one per source CSV line if D-35 shape (b) merges two lines).
    - `HeaderSniffer::sniff($path, 'ics-csv')` dispatches to a new `sniffIcsCsv()` method that rejects non-CSV extensions, rejects empty files, rejects column-count mismatch, and rejects header-signature mismatch with a user-facing error string.
    - `SourceAdapterRegistry` resolves `'ics-csv'` → `IcsCsvAdapter` instance via the service provider.
  </behavior>
  <action>
    1. **Read `03-01-SUMMARY.md`** to obtain the empirical column count, encoding, delimiter, date format, decimal separator, the column index of each canonical field, the D-34 source_ref disposition (auth-code column or NULL), the D-35 FX-row shape (a or b), and the D-40 markup separability. Use these values verbatim in steps 2-3 — do NOT re-inspect the fixture.

    2. **Create `Modules/Ingestion/Internal/Adapters/Ics/IcsCsvHeaderProfile.php`** mirroring `AsnCsvHeaderProfile.php` shape exactly:
       ```php
       <?php
       declare(strict_types=1);
       namespace Modules\Ingestion\Internal\Adapters\Ics;

       final class IcsCsvHeaderProfile
       {
           public const FORMAT = 'ics-csv';
           public const DELIMITER = /* from 03-01-SUMMARY */;
           public const HAS_HEADER = true;
           public const SOURCE_ENCODING = /* from 03-01-SUMMARY */;
           public const EXPECTED_COLUMN_COUNT = /* from 03-01-SUMMARY */;
           /** @var list<string> */
           public const HEADER_SIGNATURE = [/* first two header cells verbatim from 03-01-SUMMARY */];
           public const DATE_FORMAT = /* from 03-01-SUMMARY, e.g. 'd-m-Y' */;
       }
       ```

    3. **Create `Modules/Ingestion/Internal/Adapters/Ics/IcsCsvColumnMap.php`** with `public const` integer constants for every column index named per the canonical field. Required constants (use the empirical indices from 03-01-SUMMARY's column map):
       - `POSTED_DATE`
       - `MERCHANT`
       - `ORIGINAL_AMOUNT` and `ORIGINAL_CURRENCY` (the foreign-currency leg per D-35; if the empirical layout has no separate native columns and only the EUR leg is present, name the EUR column `SETTLED_AMOUNT` and document that ICS only emits native+settled when an FX charge occurred)
       - `SETTLED_AMOUNT` (the EUR leg; if the layout uses a single amount column for EUR-native rows AND that same column for the EUR settlement leg of an FX row, the adapter inspects ORIGINAL_CURRENCY to decide whether the row is FX or not)
       - `AUTH_CODE` if D-34 resolved to "available", or omit + document NULL source_ref
       - `CARD_NUMBER` (read then discard per D-37)
       Mirror the right-of-constant comment style from `AsnCsvColumnMap.php`.

    4. **Create `Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php`** implementing `SourceAdapter`. Mirror `AsnCsvAdapter.php` structurally:
       - Constructor: `public function __construct(private readonly HeaderSniffer $sniffer) {}` (no amount parser unless the empirical decimal style demands one — if ICS uses comma-decimal like ASN's MT940, reuse `AsnAmountParser` via constructor DI; PATTERNS.md line 188-189 documents this fallback).
       - `format(): string` returns `IcsCsvHeaderProfile::FORMAT`.
       - `statementMetadata(): ?StatementSummaryData` returns `null` for Phase 3 (revisit only if 03-01-SUMMARY reports statement-level metadata in the CSV).
       - `parse(string $localPath, AccountResolver $accounts): Generator` follows the AsnCsvAdapter pattern:
         a. Call `$this->sniffer->sniff($localPath, IcsCsvHeaderProfile::FORMAT)`.
         b. Build `League\Csv\Reader::from($localPath, 'r')` with the profile's delimiter, escape `''`, header offset 0, `CharsetConverter::addTo($reader, IcsCsvHeaderProfile::SOURCE_ENCODING, 'UTF-8')`.
         c. Iterate `$reader->getRecords()` with a zero-based `$index`. For each record:
            - Run `normaliseRow($record)` (mirror the AsnCsvAdapter private helper verbatim).
            - Compute synthetic `ownIban = sprintf('ICS-CARD-%d', $userId)` where `$userId` is read via the AccountResolver shape — the resolver is given the synthetic identifier and resolves to the user's ICS account row. (If the project's existing AccountResolver implementation requires an IBAN-shaped string, prefix with `'ICS'` and pad to a 16-char-or-longer synthetic — the v3 fingerprint uses `account_id` not `iban`, so the exact synthetic shape only matters for the `accounts.iban` UNIQUE constraint.)
            - Parse `postedAt` via `CarbonImmutable::createFromFormat('!'.IcsCsvHeaderProfile::DATE_FORMAT, $row[IcsCsvColumnMap::POSTED_DATE])`; reuse the same value for `bookedAt` AND `valueDate` unless 03-01-SUMMARY reports separate columns (mirror AsnCsvAdapter's fallback).
            - Parse the amount cells. If `$row[IcsCsvColumnMap::ORIGINAL_CURRENCY] !== '' && $row[IcsCsvColumnMap::ORIGINAL_CURRENCY] !== 'EUR'` (the row is FX): set `$hasFxRow = true`, `$nativeCurrency = $row[IcsCsvColumnMap::ORIGINAL_CURRENCY]`, `$nativeMinor = <parse original amount to integer cents>`, `$settledMinor = <parse settled EUR amount to integer cents>`. Otherwise (EUR-native): `$hasFxRow = false`, `$nativeCurrency = 'EUR'`, `$nativeMinor = <parse settled amount to integer cents>`, `$settledMinor = null`.
            - Validate the currency code via `Brick\Money\ISOCurrencyProvider::getInstance()->getCurrency($code)`; on `UnknownCurrencyException` throw `InvalidAmountException` so the ImportPipeline's per-row error path catches it (Pitfall 6 in 03-RESEARCH.md).
            - Compute `$counterpartyName = $this->nullIfEmpty($row[IcsCsvColumnMap::MERCHANT])`.
            - Compute `$sourceRef`: if D-34 resolved to "auth code available", `$this->nullIfEmpty($row[IcsCsvColumnMap::AUTH_CODE])`; else `null`.
            - Build `$rawPayload = $row` and REPLACE the CARD_NUMBER cell value with the literal string `'<discarded per D-37>'` before passing it to the DTO (Pitfall guard for the v2 multi-card landmine in 03-CONTEXT.md `<deferred>`).
            - Call `$accounts->resolve($ownIban)`.
            - `yield new SourceTransactionDto(bookedAt: $postedAt, postedAt: $postedAt, valueDate: $postedAt, ownIban: $ownIban, counterpartyIban: null, counterpartyName: $counterpartyName, currency: $nativeCurrency, amountMinor: $nativeMinor, sourceRef: $sourceRef, description: null, rawPayload: $rawPayload, sourceRowIndex: $index, settledAmountMinor: $hasFxRow ? $settledMinor : null, settledCurrency: $hasFxRow ? 'EUR' : null, fxRateUsed: null);`
            - Increment `$index`.
       - Wrap every per-row parsing call in a `try { ... } catch (Throwable $e) { throw new InvalidAmountException(sprintf('Row %d: %s', $index, $e->getMessage()), 0, $e); }` mirroring AsnCsvAdapter's contract — the ImportPipeline converts these to ERROR `PreviewRowDto`.

       **Shape-(b) handling:** If 03-01-SUMMARY reports D-35 shape (b) — two CSV rows per FX charge — implement the 1-row look-ahead buffer per RESEARCH.md "Pattern 2" lines 286-326. The yielded DTO's `sourceRowIndex` is the MERCHANT row's index, NOT the FX-conversion row's. The FX-conversion row's cells are stored under `rawPayload['fxConversion']` for later markup-split work (per D-40 deferred recoverability).

    5. **Modify `Modules/Ingestion/Public/Services/HeaderSniffer.php`:** Extend the `match ($declaredFormat)` arm (PATTERNS.md lines 296-306) to include `IcsCsvHeaderProfile::FORMAT => $this->sniffIcsCsv($localPath, $head),`. Implement `sniffIcsCsv()` as a private method mirroring `sniffAsnCsv()` (PATTERNS.md lines 309-350). User-facing error strings are locked by UI-SPEC §"Two-step wizard picker labels" → "Pre-parse MIME error (ICS path)" = `"That file doesn't look like an ICS export. Drop in the CSV you downloaded from the ICS portal."` and "Pre-parse header error (ICS path)" = `"This CSV doesn't match the expected ICS column layout. If ICS changed their export format, file an issue."` Use these literal strings.

    6. **Modify `Modules/Ingestion/Providers/IngestionServiceProvider.php`:** Append `'ics-csv' => $app->make(IcsCsvAdapter::class),` to the `SourceAdapterRegistry` map (PATTERNS.md lines 361-369).

    7. **Make `Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php` (scaffolded in 03-01) GREEN.** Replace the failing `expect(true)->toBe(false, ...)` placeholders with real assertions mirroring `Modules/Ingestion/tests/Unit/AsnCsvAdapterTest.php` (the analog file). For the FX-row assertion (`it('yields native + settled pair for a foreign-currency row')`), find the row in the fixture identified as the foreign-currency row (per 03-01-SUMMARY) and assert: `currency`, `amountMinor`, `settledAmountMinor`, `settledCurrency` to the empirical values, AND `fxRateUsed === null` (adapter honesty contract — derivation happens in NormalizeStage, not here). For the EUR-native row assertion, assert `settledAmountMinor === null` AND `settledCurrency === null`. For the card-number-discard assertion (`it('discards the card number column at the adapter boundary per D-37')`), assert the yielded DTO's `rawPayload[<CARD_NUMBER index>] === '<discarded per D-37>'`.

    8. **Create the snapshot file** by running `vendor/bin/pest Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php --filter='matches the snapshot' --update-snapshots` after step 7 lands. Commit the resulting `.snap` file under `Modules/Ingestion/tests/Unit/Adapters/Ics/__snapshots__/`.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php --stop-on-failure && vendor/bin/phpstan analyse Modules/Ingestion/Internal/Adapters/Ics Modules/Ingestion/Public/Services/HeaderSniffer.php Modules/Ingestion/Providers/IngestionServiceProvider.php --memory-limit=1G && vendor/bin/pint --test Modules/Ingestion/Internal/Adapters/Ics Modules/Ingestion/Public/Services/HeaderSniffer.php && grep -q "'ics-csv'" Modules/Ingestion/Providers/IngestionServiceProvider.php && grep -q 'IcsCsvHeaderProfile::FORMAT' Modules/Ingestion/Public/Services/HeaderSniffer.php && grep -q '<discarded per D-37>' Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php && test -d Modules/Ingestion/tests/Unit/Adapters/Ics/__snapshots__ && ! grep -E 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - All `it(...)` cases in `Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php` pass (no more `->toBe(false, ...)` placeholders): `grep -c 'toBe(false' Modules/Ingestion/tests/Unit/Adapters/Ics/IcsCsvAdapterTest.php` returns `0`
    - `IcsCsvAdapter` class exists at the path and implements `Modules\Ingestion\Public\Contracts\SourceAdapter`: `grep -q 'implements SourceAdapter' Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php`
    - `IcsCsvAdapter::format()` returns the literal `'ics-csv'`: `grep -q "return IcsCsvHeaderProfile::FORMAT" Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php` AND `grep -q "const FORMAT = 'ics-csv'" Modules/Ingestion/Internal/Adapters/Ics/IcsCsvHeaderProfile.php`
    - Card-number scrubbing: `grep -c '<discarded per D-37>' Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php` returns ≥ 1
    - Registry wiring: `grep -c "'ics-csv'.*=>.*\\\$app->make(IcsCsvAdapter::class)" Modules/Ingestion/Providers/IngestionServiceProvider.php` returns 1
    - HeaderSniffer dispatch: `grep -c 'IcsCsvHeaderProfile::FORMAT => \$this->sniffIcsCsv' Modules/Ingestion/Public/Services/HeaderSniffer.php` returns 1
    - Two locked user-facing error strings present verbatim in `Modules/Ingestion/Public/Services/HeaderSniffer.php`: `grep -q "That file doesn't look like an ICS export" Modules/Ingestion/Public/Services/HeaderSniffer.php` AND `grep -q "doesn't match the expected ICS column layout" Modules/Ingestion/Public/Services/HeaderSniffer.php`
    - Snapshot file committed: `test -f Modules/Ingestion/tests/Unit/Adapters/Ics/__snapshots__/parses-the-real-fixture-into-source-transaction-dtos.snap` (or whatever Pest names it for the snapshot test — adjust if the snapshot test name differs)
    - No facade/helper slippage in the new adapter file: `! grep -E 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' Modules/Ingestion/Internal/Adapters/Ics/IcsCsvAdapter.php >/dev/null`
    - `vendor/bin/phpstan analyse Modules/Ingestion/Internal/Adapters/Ics --memory-limit=1G` exits zero (Larastan level 10 strict clean — no `(float)` casts, no `auth()` calls, no facade usage)
    - `vendor/bin/pint --test` exits zero on all new/modified files
  </acceptance_criteria>
  <done>IcsCsvAdapter parses the gold fixture into the expected DTO stream, snapshotted; HeaderSniffer + registry know `ics-csv`; nine IcsCsvAdapterTest scaffolds GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend IdempotencyContractTest with an ics-csv dataset row + drive the wire-level IcsCsvImportTest scaffolds GREEN</name>
  <files>
    tests/Contracts/IdempotencyContractTest.php
    Modules/Import/tests/Feature/IcsCsvImportTest.php
  </files>
  <read_first>
    tests/Contracts/IdempotencyContractTest.php
    Modules/Import/tests/Feature/AsnCsvImportTest.php
    Modules/Ledger/Models/Account.php
    Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv
    .planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
  </read_first>
  <behavior>
    - With an ICS Account pre-seeded for the test user, calling the import on the gold fixture inserts every parsed row.
    - Re-running the import on the same fixture inserts zero new rows (v3 fingerprint idempotency).
    - Foreign-currency rows persist with both legs + derived `fx_rate_used`; EUR-native rows persist with `settled = native` and `fx_rate_used IS NULL`.
    - `IdempotencyContractTest` extends its dataset with an `'ics-csv'` row using the same-file fallback pattern from the existing `asn-camt053` / `asn-mt940` entries.
    - The `prompts the user to name the ICS Account on the first ICS upload` and `skips the name-your-account step on subsequent ICS uploads` scaffolds remain RED in this plan — they are driven GREEN in plan 03-03 alongside the PreviewWizard naming step. This is deliberate: plan 03-02 stays strictly wire-level.
  </behavior>
  <action>
    1. **Extend `tests/Contracts/IdempotencyContractTest.php` dataset** with an `'ics-csv'` entry mirroring the same-file fallback shape used by `asn-camt053` / `asn-mt940` (lines 20-31 of the file). The new entry:
       ```php
       'ics-csv' => [
           'adapterFormat' => 'ics-csv',
           'fixture' => __DIR__.'/../../Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv',
           'overlapBase' => __DIR__.'/../../Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv',
           'overlapNext' => __DIR__.'/../../Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv',
       ],
       ```
       Comment-explain (next to the entry) that the ICS fixture corpus does not yet ship a real overlap-period pair, so the same-file fallback is the strongest claim today (matching the comment on lines 14-19 of the file for CAMT/MT940).

       **Test-setup adjustment for ICS:** The existing `seedFixtureUserAndAccount()` helper seeds an ASN-shaped account. The `it('produces zero new rows when the same file is imported twice', ...)` test parameterises over the dataset; for the `'ics-csv'` row to import successfully it needs a pre-existing `Account` of `kind='ics_card'`. Two acceptable paths:
       - Path A (preferred): seed an ICS account in `seedFixtureUserAndAccount()` alongside the ASN one (using the synthetic iban `ICS-CARD-<user.id>`). Both seed rows are cheap; the test does not pollute production.
       - Path B: extend the dataset row with an optional `seedKind` field and conditionally seed the ICS-card account only for the `'ics-csv'` dataset row.
       Pick Path A for symmetry; document the choice in the SUMMARY.

    2. **Make the four wire-level scaffolds in `Modules/Import/tests/Feature/IcsCsvImportTest.php` GREEN.** Leave the two wizard-naming scaffolds (`prompts the user to name the ICS Account on the first ICS upload`, `skips the name-your-account step on subsequent ICS uploads`) RED — they are owned by plan 03-03.

       Replace the failing placeholders for the following scaffolds with real assertions mirroring `Modules/Import/tests/Feature/AsnCsvImportTest.php`:
       - `it('imports every parsed row from the gold fixture on the first run')`: pre-seed an ICS account for the user (so the account exists); call `$this->importer->runAndConfirm($icsFixturePath, 'ics-csv', $this->fixtureUser)`; assert `$result->inserted > 0` and `Transaction::count() === $result->inserted`.
       - `it('returns zero new rows when re-importing the same file')`: run the import twice; assert second run has `inserted=0, duplicates=$firstResult->inserted`.
       - `it('persists settled_amount_minor and settled_currency for an EUR-native row')`: find a known EUR-native row in the fixture (per 03-01-SUMMARY) and assert its persisted Transaction has `currency='EUR', amount_minor=<expected>, settled_amount_minor=<same>, settled_currency='EUR', fx_rate_used IS NULL`.
       - `it('persists native + settled + fx_rate_used for a foreign-currency row')`: find the known FX row in the fixture and assert its Transaction has `currency='<native code>', amount_minor=<native>, settled_amount_minor=<EUR>, settled_currency='EUR', fx_rate_used='<derived 8-decimal string>'`. Use the exact BigDecimal result — the assertion is byte-for-byte comparison on the rate string.

       All four scaffolds use the existing `seedFixtureUserAndAccount()` + `actingAs($this->fixtureUser)` + `$this->importer = $this->app->make(RunsImports::class)` setup from the analog. Remove `pest()->group('phase-3');` only if the file already inherits via the registration in 03-01.

    3. **Leave the wizard-naming scaffolds RED.** Verify the file still contains `expect(true)->toBe(false` on exactly the two scaffolds owned by plan 03-03:
       ```bash
       grep -A 1 -E "prompts the user to name the ICS Account|skips the name-your-account step" Modules/Import/tests/Feature/IcsCsvImportTest.php
       ```
       MUST still show the failing-placeholder pattern. This keeps the suite RED for the wizard half until plan 03-03 lands.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Import/tests/Feature/IcsCsvImportTest.php --filter='imports every parsed row|returns zero new rows when re-importing|persists settled_amount_minor and settled_currency|persists native + settled' --stop-on-failure && vendor/bin/pest tests/Contracts/IdempotencyContractTest.php --stop-on-failure && vendor/bin/phpstan analyse tests/Contracts/IdempotencyContractTest.php Modules/Import/tests/Feature/IcsCsvImportTest.php --memory-limit=1G && vendor/bin/pint --test tests/Contracts/IdempotencyContractTest.php Modules/Import/tests/Feature/IcsCsvImportTest.php && grep -q "'ics-csv'" tests/Contracts/IdempotencyContractTest.php && [ "$(grep -c 'toBe(false' Modules/Import/tests/Feature/IcsCsvImportTest.php)" = "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - `IdempotencyContractTest` dataset has the `'ics-csv'` entry: `grep -c "'ics-csv' =>" tests/Contracts/IdempotencyContractTest.php` returns 1
    - All four wire-level `IcsCsvImportTest` scaffolds GREEN: running the filter command in `<verify>` exits zero
    - Exactly two failing placeholders remain (the two wizard-naming scaffolds, owned by plan 03-03): `grep -c 'toBe(false' Modules/Import/tests/Feature/IcsCsvImportTest.php` returns `2`
    - IdempotencyContractTest's parametric `produces zero new rows when the same file is imported twice` row for `'ics-csv'` exits zero
    - Phase-3 wire-level slice stays GREEN: `vendor/bin/pest --group=phase-3 --filter='IcsCsv|Idempotency|NormalizeStage' --filter='-prompts the user to name|-skips the name-your-account step' --stop-on-failure` exits zero (or equivalent path that excludes the two RED wizard scaffolds)
    - Larastan + Pint clean on all touched files
    - All Phase 1/2 regression tests stay GREEN: `vendor/bin/pest --filter='AsnCsv|AsnCamt053|AsnMt940|UploadWizard' --stop-on-failure` exits zero (this plan does NOT modify any wizard code)
  </acceptance_criteria>
  <done>ICS wire-level ingestion is end-to-end functional in code: adapter parses → NormalizeStage canonicalises → transactions persist with both legs → re-import is a no-op. IdempotencyContractTest extended. Four IcsCsvImportTest scaffolds GREEN; two wizard scaffolds stay RED for plan 03-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-uploaded file → adapter | ICS CSV crosses from the HTTP upload into the parsing pipeline; untrusted column count, encoding, currency codes |
| Adapter → NormalizeStage | SourceTransactionDto crosses; the new D-42 fields are nullable and must be honestly null when the source omits them |
| NormalizeStage → DB | CanonicalTransaction values land in `transactions.fx_rate_used` (decimal(18,8)); precision must be exact, never float-coerced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | NormalizeStage FX rate derivation | mitigate | Use `Brick\Math\BigDecimal::dividedBy(scale: 8, RoundingMode::HALF_UP)` exclusively — never PHP float `/`. The existing `tests/Contracts/NoFloatMoneyArchTest.php` greps for `(float)` casts on money paths; new code adds no float cast (verified by Task 1 acceptance). |
| T-03-02-02 | Tampering | Card-number cell in `rawPayload` | mitigate | Adapter replaces the CARD_NUMBER column with the literal `'<discarded per D-37>'` before assembling `rawPayload`. Verified by `it('discards the card number column at the adapter boundary per D-37')` in Task 2. The v2-multi-card landmine (CONTEXT.md `<deferred>`) is contained: even if a real PAN survived the fixture-anonymisation pass, the adapter scrubs it at the canonical boundary. |
| T-03-02-03 | Information Disclosure | Currency code field on `SourceTransactionDto` | mitigate | Adapter calls `Brick\Money\ISOCurrencyProvider::getInstance()->getCurrency($code)` before yielding; on `UnknownCurrencyException` the row throws `InvalidAmountException` which the ImportPipeline converts to an ERROR `PreviewRowDto` — the row is surfaced to the user as a clear failure, not silently accepted. |
| T-03-02-04 | DoS | Multi-million-row ICS CSV upload | accept | ICS CSVs are statement-sized (hundreds of rows, one month at a time per CONTEXT.md). The `max:10240` Livewire upload-size rule from Phase 1 caps the file at 10 MB. Streaming via `league/csv` keeps the heap flat regardless. No new mitigation needed in Phase 3. |
</threat_model>

<verification>
- `vendor/bin/pest --group=phase-3 --filter='IcsCsv|Idempotency|NormalizeStage' --stop-on-failure` exits zero for the wire-level scaffolds (the two wizard scaffolds remain RED until plan 03-03)
- `vendor/bin/phpstan analyse Modules/Ingestion Modules/Import --memory-limit=1G` exits zero (Larastan level 10 strict)
- `vendor/bin/pint --test` exits zero on every modified file
- `vendor/bin/pest --filter='NoFloatMoney|BoundaryArch|UserIdColumnArch'` stays GREEN (no regression on the architecture invariants)
- `vendor/bin/pest --filter='AsnCsv|AsnCamt053|AsnMt940'` stays GREEN (Phase 1/2 ingestion paths untouched)
</verification>

<success_criteria>
1. Adapter parses the gold fixture into the expected DTO stream (snapshotted); NormalizeStage derives `fx_rate_used` from the native+settled pair with BigDecimal precision.
2. Foreign-currency rows persist with `currency='<native>', amount_minor=<native>, settled_currency='EUR', settled_amount_minor=<EUR>, fx_rate_used='<8-decimal string>'` — both legs preserved per LED-03.
3. EUR-native rows persist with `settled_amount_minor=amount_minor, settled_currency=currency, fx_rate_used IS NULL` (the substitution branch).
4. Re-importing the same ICS file produces zero new rows (`IdempotencyContractTest` GREEN for the new dataset row).
5. No regression on Phase 1/2: AsnCsv / AsnCamt053 / AsnMt940 paths and their tests stay GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ics-cards-multi-currency-display/03-02-SUMMARY.md` documenting:
- Whether `IcsCsvAdapter` needed an `IcsAmountParser` constructor dep or whether `AsnAmountParser` was reusable (or whether the league/csv default parsing covered the empirical decimal style).
- The D-35 shape implementation choice (single-row shape (a) vs look-ahead buffer shape (b)) and how the `sourceRowIndex` invariant was preserved.
- The synthetic iban shape used for ICS accounts (`ICS-CARD-<userId>` or alternative) and any UNIQUE-constraint considerations.
- Path A vs Path B chosen for the `IdempotencyContractTest` seeding (per Task 3 step 1).
- The two RED scaffolds (`prompts the user to name the ICS Account on the first ICS upload`, `skips the name-your-account step on subsequent ICS uploads`) explicitly listed as plan 03-03's responsibility.
</output>
