---
phase: 03-ics-cards-multi-currency-display
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
files_modified:
  - Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php
  - Modules/Core/Models/User.php
  - Modules/Core/Internal/Http/Livewire/SettingsPage.php
  - Modules/Core/Resources/views/livewire/settings-page.blade.php
  - Modules/Core/Resources/views/livewire/top-nav.blade.php
  - Modules/Core/Routes/web.php
  - Modules/Core/tests/Feature/SettingsPageTest.php
autonomous: true
requirements:
  - MC-02

must_haves:
  truths:
    - "User can visit /settings and see a calm form with two fields: Default currency view + Period start day"
    - "Submitting the form persists default_currency_view and period_start_day on the user row"
    - "Top nav exposes a Settings link between Uncategorized and the user-email block"
    - "Validation rejects default_currency_view outside {eur_only, original} and period_start_day outside 1..28"
    - "users.default_currency_view column exists with default 'eur_only' so Phase 1/2 users get a sane fallback"
  artifacts:
    - path: "Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php"
      provides: "Forward migration adding string(16) default_currency_view column with default 'eur_only'"
      contains: "default_currency_view"
    - path: "Modules/Core/Internal/Http/Livewire/SettingsPage.php"
      provides: "Settings Livewire SFC with method-DI, Validate attributes, mount() pre-fill, save() persistence"
      contains: "default_currency_view"
    - path: "Modules/Core/Resources/views/livewire/settings-page.blade.php"
      provides: "Calm form view matching login-form chrome — slate-50 inputs, emerald-600 submit, Saved. inline confirmation"
    - path: "Modules/Core/Routes/web.php"
      provides: "GET /settings route mapped to SettingsPage Livewire component, scoped to auth middleware"
      contains: "settings"
  key_links:
    - from: "Modules/Core/Internal/Http/Livewire/SettingsPage.php"
      to: "Modules/Core/Models/User.php"
      via: "$user->default_currency_view assignment + save()"
      pattern: "default_currency_view"
    - from: "Modules/Core/Resources/views/livewire/top-nav.blade.php"
      to: "Modules/Core/Routes/web.php"
      via: "<a href=route('settings')>"
      pattern: "settings"
---

<objective>
Discharge D-45 by shipping a minimal /settings page that exposes the new `default_currency_view` user preference (eur_only | original) and the existing `period_start_day` integer field. The schema gets a forward migration adding the `default_currency_view` column to `users` with default `'eur_only'`, the User model is extended, the Livewire SFC + Blade live in the Core module, the top nav grows a Settings link, and a feature test exercises the full round-trip.

Purpose: MC-02 requires switching between EUR-only and dual-currency views. Plan 03-05 wires the toggle UX on /transactions; this plan ships the storage + the global-default surface that plan 03-05 reads from. By running in Wave 3 in parallel with plan 03-03 (no file overlap), the user gets both the wizard refactor and the settings page in the same wave.

Output: New /settings route + Livewire SFC + Blade view + top-nav link; users.default_currency_view column added with default 'eur_only'; six SettingsPage feature tests GREEN (the scaffolds from plan 03-01 driven to GREEN). After this plan, the storage is ready for plan 03-05 to consume.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
@.planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
@.planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
@.planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md

# Existing analogs the executor MUST mirror — read once each:
@Modules/Core/Models/User.php
@Modules/Core/Internal/Http/Livewire/Dashboard.php
@Modules/Core/Resources/views/livewire/dashboard.blade.php
@Modules/Core/Resources/views/livewire/top-nav.blade.php
@Modules/Core/Routes/web.php
@Modules/Ledger/Database/Migrations/2026_05_13_010002_add_enriched_from_to_transactions.php
@Modules/Categorization/Internal/Http/Livewire/InlineCategoryPicker.php
@Modules/Core/tests/Feature/InstallCommandTest.php

<interfaces>
Locked copy from 03-UI-SPEC.md "Settings page (/settings, D-45)" — use verbatim:

- Page heading: "Settings"
- Page subheading: "Preferences for how your finances appear in the app."
- Section 1 heading: "Currency display" (12px caption, uppercase tracking)
- default_currency_view field label: "Default view on the transactions list"
- Field options: "EUR only" (value eur_only), "Original currency" (value original)
- Field helper text: "You can still switch per page from the transactions list."
- Section 2 heading: "Period" (12px caption, uppercase tracking)
- period_start_day field label: "Period starts on day"
- Period helper text: "Numbered 1 to 28. Most users keep this on 1 (calendar month). Use 25 if your salary lands on the 25th and you think of \"your month\" as starting then."
- Submit button: "Save settings"
- Saved confirmation: "Saved." (inline text-emerald-700 text-sm below button; auto-dismiss after 4s via wire:transition)

Locked error copy from UI-SPEC "Error states (Phase 3 additions)":
- period_start_day out of range: "Choose a day from 1 to 28." (inline text-rose-600 below the field)
- default_currency_view invalid: "Pick one of the available options."

Top nav insertion point (from UI-SPEC §"Phase scope" item 7): Settings link goes between "Uncategorized" and the user-email/sign-out cluster.

Storage decision per 03-RESEARCH.md "Pattern 6": Option A — column on users (RECOMMENDED for Phase 3).
- Column shape: string('default_currency_view', 16)->default('eur_only')->after('period_start_day')
- Column constraint: enforced by Livewire #[Validate('required|in:eur_only,original')] at the application layer; the DB column has no CHECK constraint (SQLite limitation + we already enforce at the app layer per Phase 1/2 pattern).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration + User model — add default_currency_view column with default 'eur_only'</name>
  <files>
    Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php
    Modules/Core/Models/User.php
  </files>
  <read_first>
    Modules/Core/Database/Migrations/2026_05_12_000001_create_users_table.php
    Modules/Ledger/Database/Migrations/2026_05_13_010002_add_enriched_from_to_transactions.php
    Modules/Core/Models/User.php
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
  </read_first>
  <behavior>
    - After migration, every existing user row has default_currency_view='eur_only' (Phase 1/2 users get a sane fallback).
    - New User instances default to 'eur_only' until explicitly set.
    - $user->default_currency_view returns a string under Larastan level 10 strict (cast as 'string'; @property line in User docblock).
    - Migration is reversible: down() drops the column.
    - Migration follows the project's standing $this->schema() / app(DatabaseManager::class) pattern (Phase 1/2 invariant for anonymous migration classes).
  </behavior>
  <action>
    1. Create the migration at the exact path: Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php. Mirror Modules/Ledger/Database/Migrations/2026_05_13_010002_add_enriched_from_to_transactions.php verbatim for the schema-helper pattern. The up() body:

       $this->schema()->table('users', static function (Blueprint $table): void {
           $table->string('default_currency_view', 16)->default('eur_only')->after('period_start_day');
       });

       The down() body:

       $this->schema()->table('users', static function (Blueprint $table): void {
           $table->dropColumn('default_currency_view');
       });

       Include the private schema() helper resolving DatabaseManager via app() with the same "anonymous migration exception to DI-only" docblock comment found in the analog migration. This is the ONLY place in the codebase where `app()` is acceptable per the Phase 1/2 standing exception.

    2. Modify Modules/Core/Models/User.php in three precise spots:

       a. Append `'default_currency_view'` to the `protected $fillable` array (currently `['email', 'password', 'period_start_day']` → becomes `['email', 'password', 'period_start_day', 'default_currency_view']`).

       b. In the `casts()` method, add `'default_currency_view' => 'string'` to the returned array.

       c. In the class-level docblock, add `@property string $default_currency_view` directly after the existing `@property int $period_start_day` line. Larastan needs this to type-check `$user->default_currency_view` accesses.

       Do NOT modify any other field, method, or trait on User.php.

    3. Verify the migration runs cleanly against a fresh DB: run `php artisan migrate:fresh --force` (or whatever the project's `diederik:install` artisan command uses internally). Existing Phase 1/2 migration tests must still GREEN.
  </action>
  <verify>
    <automated>test -f Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php && grep -q "string('default_currency_view', 16)->default('eur_only')" Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php && grep -q "dropColumn('default_currency_view')" Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php && grep -q "'default_currency_view'" Modules/Core/Models/User.php && grep -q "@property string \$default_currency_view" Modules/Core/Models/User.php && vendor/bin/phpstan analyse Modules/Core/Models/User.php Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php --memory-limit=1G && vendor/bin/pint --test Modules/Core/Models/User.php Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php && php artisan migrate:fresh --force --env=testing 2>&1 | grep -q 'add_default_currency_view_to_users'</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at the exact dated path
    - Migration up() adds string(16) column default 'eur_only' after period_start_day: grep returns the exact line
    - Migration down() drops the column: grep returns the exact line
    - Schema helper present: `grep -c "private function schema(): Builder" Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php` returns 1
    - User.php $fillable includes 'default_currency_view': grep returns a match
    - User.php casts() returns 'default_currency_view' => 'string': grep returns a match
    - User.php docblock includes `@property string $default_currency_view`: grep returns a match
    - `php artisan migrate:fresh --force --env=testing` runs without error and includes the new migration in its run list
    - After fresh migration, `php artisan tinker --execute='echo \\Modules\\Core\\Models\\User::factory()->create()->default_currency_view;'` (or equivalent test query) outputs 'eur_only'
    - Larastan + Pint clean on both files
    - All Phase 1/2 regression tests GREEN: `vendor/bin/pest --filter='User\\|InstallCommand\\|LoginFlow\\|Auth' --stop-on-failure` exits zero
  </acceptance_criteria>
  <done>Schema column exists with sane default; User model exposes it as typed; existing Phase 1/2 user tests stay GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SettingsPage Livewire SFC + Blade view + /settings route + top-nav link</name>
  <files>
    Modules/Core/Internal/Http/Livewire/SettingsPage.php
    Modules/Core/Resources/views/livewire/settings-page.blade.php
    Modules/Core/Resources/views/livewire/top-nav.blade.php
    Modules/Core/Routes/web.php
  </files>
  <read_first>
    Modules/Core/Internal/Http/Livewire/Dashboard.php
    Modules/Core/Resources/views/livewire/dashboard.blade.php
    Modules/Core/Resources/views/livewire/top-nav.blade.php
    Modules/Core/Routes/web.php
    Modules/Categorization/Internal/Http/Livewire/InlineCategoryPicker.php
    Modules/Core/Public/Contracts/CurrentUser.php
    Modules/Core/Public/Services/CurrentUserService.php
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
  </read_first>
  <behavior>
    - Visiting /settings (authenticated) renders the Livewire SettingsPage with the user's current preferences pre-filled.
    - The form has two fields: default_currency_view (select, options eur_only / original) and period_start_day (numeric, 1..28).
    - Submitting via wire:submit="save" validates, persists to the User row, and shows an inline "Saved." confirmation.
    - Top nav shows a "Settings" link between "Uncategorized" and the user-email/sign-out block.
    - Visiting /settings without authentication redirects to /login (auth middleware).
    - DI-only: SettingsPage uses method-level DI (CurrentUser, ViewFactory, UrlGenerator); the constructor takes no collaborators.
    - No global helpers / facades anywhere in this plan's files.
  </behavior>
  <action>
    1. Create Modules/Core/Internal/Http/Livewire/SettingsPage.php as a final Livewire 4 Component class. Mirror Modules/Core/Internal/Http/Livewire/Dashboard.php for the method-DI shape. Required structure:

       - Namespace: Modules\Core\Internal\Http\Livewire
       - Imports: Livewire\Attributes\Validate, Livewire\Component, Modules\Core\Public\Contracts\CurrentUser, Illuminate\Contracts\View\Factory as ViewFactory, Illuminate\Contracts\View\View
       - Class: final class SettingsPage extends Component
       - Public property #[Validate('required|in:eur_only,original')] public string $defaultCurrencyView = 'eur_only';
       - Public property #[Validate('required|integer|min:1|max:28')] public int $periodStartDay = 1;
       - Public property `public bool $saved = false;` to drive the inline "Saved." confirmation
       - mount(CurrentUser $currentUser): void — reads `$currentUser->user()->default_currency_view` and `period_start_day` into the two public properties.
       - save(CurrentUser $currentUser): void — calls $this->validate(); writes both fields to $currentUser->user(); saves; sets $this->saved = true; and dispatches `$this->dispatch('settings-saved')` for the Blade's wire:transition consumer.
       - render(ViewFactory $views): View — returns $views->make('core::livewire.settings-page')

       Critical invariants (Phase 1 D-12 + Phase 1 plan 01-05 auto-fixes):
       - The constructor MUST take NO collaborators (constructor DI is forbidden on Livewire Components — Phase 1 fix).
       - mount/save/render use method-level DI; the container resolves CurrentUser / ViewFactory at call time.
       - NEVER use auth() / Auth::user() / Auth::id().
       - The custom UI-SPEC validation messages from "Error states" — "Choose a day from 1 to 28." for period_start_day and "Pick one of the available options." for default_currency_view — go in a `messages(): array` method returning the locked strings, mapped to validation rule keys ('periodStartDay.min' / 'periodStartDay.max' / 'periodStartDay.integer' all yield "Choose a day from 1 to 28."; 'defaultCurrencyView.in' yields "Pick one of the available options.").

    2. Create Modules/Core/Resources/views/livewire/settings-page.blade.php. Mirror the dashboard.blade.php calm-aesthetic shell + the existing login-form.blade.php form-input chrome (slate-50 background, slate-200 border, emerald-600 submit). The full structure:

       <div class="max-w-md mx-auto space-y-lg" data-testid="settings-page">
           <header class="space-y-1">
               <h1 class="text-3xl font-semibold tracking-tight text-slate-900">Settings</h1>
               <p class="text-sm text-slate-500">Preferences for how your finances appear in the app.</p>
           </header>

           <form wire:submit="save" class="space-y-lg">
               <section class="space-y-2">
                   <h2 class="text-xs uppercase tracking-wide text-slate-500">Currency display</h2>
                   <div class="space-y-1">
                       <label for="defaultCurrencyView" class="block text-sm text-slate-900">Default view on the transactions list</label>
                       <select id="defaultCurrencyView" name="defaultCurrencyView" wire:model="defaultCurrencyView" class="block w-full rounded-md border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-900 focus:outline-none focus-visible:ring-2 focus-visible:ring-slate-900 focus-visible:ring-offset-2">
                           <option value="eur_only">EUR only</option>
                           <option value="original">Original currency</option>
                       </select>
                       <p class="text-xs text-slate-500">You can still switch per page from the transactions list.</p>
                       @error('defaultCurrencyView')<p class="text-sm text-rose-600">{{ $message }}</p>@enderror
                   </div>
               </section>

               <section class="space-y-2">
                   <h2 class="text-xs uppercase tracking-wide text-slate-500">Period</h2>
                   <div class="space-y-1">
                       <label for="periodStartDay" class="block text-sm text-slate-900">Period starts on day</label>
                       <input type="number" min="1" max="28" id="periodStartDay" name="periodStartDay" wire:model="periodStartDay" class="block w-full rounded-md border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-900 focus:outline-none focus-visible:ring-2 focus-visible:ring-slate-900 focus-visible:ring-offset-2" />
                       <p class="text-xs text-slate-500">Numbered 1 to 28. Most users keep this on 1 (calendar month). Use 25 if your salary lands on the 25th and you think of "your month" as starting then.</p>
                       @error('periodStartDay')<p class="text-sm text-rose-600">{{ $message }}</p>@enderror
                   </div>
               </section>

               <div class="space-y-1">
                   <button type="submit" class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-medium rounded-md py-2 text-sm focus:outline-none focus-visible:ring-2 focus-visible:ring-emerald-600 focus-visible:ring-offset-2">
                       Save settings
                   </button>
                   @if ($saved)
                       <p wire:transition.duration.4000ms class="text-sm text-emerald-700">Saved.</p>
                   @endif
               </div>
           </form>
       </div>

       Use the project's spacing-scale tokens (space-y-lg, space-y-2, gap-md) consistently with the dashboard. The helper text "Numbered 1 to 28..." includes ASCII double quotes around "your month" — replicate verbatim (UI-SPEC locks the copy).

    3. Add the /settings route in Modules/Core/Routes/web.php inside the existing `Route::middleware(['web', 'auth'])->group(...)` block. Use the existing dashboard route as a structural template:

       use Modules\Core\Internal\Http\Livewire\SettingsPage;
       // ...

       Route::get('/settings', SettingsPage::class)->name('settings');

       (Laravel resolves the Livewire component class directly as the route handler. Verify this is the project's existing pattern by checking how Dashboard is wired in Modules/Core/Routes/web.php — if Dashboard uses a closure that returns a Blade rendering, mirror that closure shape with Livewire::mount(SettingsPage::class) instead. PATTERNS.md and the existing routes file are the authority.)

    4. Modify Modules/Core/Resources/views/livewire/top-nav.blade.php to insert a Settings <a> between the Uncategorized link and the user-email/sign-out block. Use the same `$isActive('/settings')` helper pattern already in the file. Link text: "Settings". The link's hover + active-state classes match the existing Uncategorized link verbatim.

    5. Pint-format every modified file.
  </action>
  <verify>
    <automated>test -f Modules/Core/Internal/Http/Livewire/SettingsPage.php && test -f Modules/Core/Resources/views/livewire/settings-page.blade.php && grep -q "Route::get('/settings'" Modules/Core/Routes/web.php && grep -q "Settings" Modules/Core/Resources/views/livewire/top-nav.blade.php && grep -q "Validate('required|in:eur_only,original')" Modules/Core/Internal/Http/Livewire/SettingsPage.php && grep -q "Validate('required|integer|min:1|max:28')" Modules/Core/Internal/Http/Livewire/SettingsPage.php && ! grep -E 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' Modules/Core/Internal/Http/Livewire/SettingsPage.php >/dev/null && vendor/bin/phpstan analyse Modules/Core/Internal/Http/Livewire/SettingsPage.php Modules/Core/Routes/web.php --memory-limit=1G && vendor/bin/pint --test Modules/Core/Internal/Http/Livewire/SettingsPage.php Modules/Core/Resources/views/livewire/settings-page.blade.php Modules/Core/Resources/views/livewire/top-nav.blade.php Modules/Core/Routes/web.php</automated>
  </verify>
  <acceptance_criteria>
    - SettingsPage.php exists, is `final`, extends Livewire Component, has constructor-less class (no constructor body or empty body): `grep -c "public function __construct" Modules/Core/Internal/Http/Livewire/SettingsPage.php` returns 0 OR returns 1 with an empty body
    - Both Validate attributes present verbatim
    - mount/save/render all take method-level DI (CurrentUser): `grep -c "CurrentUser \\\$currentUser" Modules/Core/Internal/Http/Livewire/SettingsPage.php` returns at least 2
    - No facade/helper usage anywhere in the new SettingsPage.php: `! grep -E 'auth\\(\\)|Auth::user\\(|\\\\Auth\\\\Facades' Modules/Core/Internal/Http/Livewire/SettingsPage.php >/dev/null` exits zero
    - Locked UI-SPEC copy present verbatim in Blade: `grep -c 'Preferences for how your finances appear in the app\.' Modules/Core/Resources/views/livewire/settings-page.blade.php` returns 1; `grep -c 'You can still switch per page from the transactions list\.' Modules/Core/Resources/views/livewire/settings-page.blade.php` returns 1; `grep -c 'Save settings' Modules/Core/Resources/views/livewire/settings-page.blade.php` returns 1
    - Route registered: `grep -c "Route::get('/settings'" Modules/Core/Routes/web.php` returns 1; route name = 'settings'
    - Top-nav has Settings link: `grep -c '>Settings<' Modules/Core/Resources/views/livewire/top-nav.blade.php` returns 1
    - Larastan + Pint clean on all four files
    - The page renders for an authenticated user: `php artisan route:list 2>/dev/null | grep -c 'GET\\|HEAD.*settings'` returns at least 1
  </acceptance_criteria>
  <done>/settings route reachable, Livewire SFC + Blade calm-form view live, top-nav exposes the link, all DI-only invariants hold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Drive Modules/Core/tests/Feature/SettingsPageTest.php scaffolds GREEN — six round-trip + validation assertions</name>
  <files>
    Modules/Core/tests/Feature/SettingsPageTest.php
  </files>
  <read_first>
    Modules/Core/tests/Feature/SettingsPageTest.php
    Modules/Core/tests/Feature/InstallCommandTest.php
    Modules/Categorization/tests/Feature/TriagePageTest.php
    Modules/Core/Internal/Http/Livewire/SettingsPage.php
    Modules/Core/Models/User.php
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1 (it('renders the Settings page with the user current preferences pre-filled')): a User with default_currency_view='original', period_start_day=25 → Livewire::test(SettingsPage::class)->assertSet('defaultCurrencyView', 'original')->assertSet('periodStartDay', 25).
    - Test 2 (it('persists default_currency_view when changed')): set+call('save'); assert $user->fresh()->default_currency_view === 'original'.
    - Test 3 (it('persists period_start_day when changed')): set+call('save'); assert $user->fresh()->period_start_day === 25.
    - Test 4 (it('rejects period_start_day outside 1..28')): set periodStartDay=0 → call('save') → assertHasErrors(['periodStartDay'])::with-the-message "Choose a day from 1 to 28."; repeat for 29.
    - Test 5 (it('rejects default_currency_view outside {eur_only, original}')): set defaultCurrencyView='garbage' → call('save') → assertHasErrors(['defaultCurrencyView'])::with-the-message "Pick one of the available options."
    - Test 6 (it('round-trips default_currency_view = original into TransactionsList default mode')): full Pitfall 5 path — save 'original' via SettingsPage; assert User row updated; mount TransactionsList; assert TransactionsList::$currency reflects 'original' equivalent (this assertion is the responsibility of plan 03-05; for this plan the test stops at "User row updated" and the TransactionsList portion is asserted in plan 03-05's TransactionsListCurrencyToggleTest).
  </behavior>
  <action>
    1. Open the scaffold Modules/Core/tests/Feature/SettingsPageTest.php (created by plan 03-01 with six failing placeholders). For each it() case, replace the `expect(true)->toBe(false, ...)` line with real assertions per the behavior block above.

    2. Mirror the beforeEach setup from Modules/Categorization/tests/Feature/TriagePageTest.php — create a User via the User::create() shape from Modules/Core/tests/Feature/InstallCommandTest.php lines 16-25:

       beforeEach(function (): void {
           $this->user = User::create([
               'email' => 'wessel@example.com',
               'password' => 'opensesame',
               'period_start_day' => 1,
               'default_currency_view' => 'eur_only',
           ]);
           $this->actingAs($this->user);
       });

       (The 'default_currency_view' explicit value avoids depending on the migration default — making the per-test starting state unambiguous.)

    3. Test 1 implementation:

       it('renders the Settings page with the user current preferences pre-filled', function (): void {
           $this->user->update(['default_currency_view' => 'original', 'period_start_day' => 25]);
           Livewire::test(SettingsPage::class)
               ->assertSet('defaultCurrencyView', 'original')
               ->assertSet('periodStartDay', 25)
               ->assertSee('Settings')
               ->assertSee('Save settings');
       });

    4. Test 2 implementation:

       it('persists default_currency_view when changed', function (): void {
           Livewire::test(SettingsPage::class)
               ->set('defaultCurrencyView', 'original')
               ->call('save')
               ->assertHasNoErrors();

           expect($this->user->fresh()->default_currency_view)->toBe('original');
       });

    5. Test 3 implementation:

       it('persists period_start_day when changed', function (): void {
           Livewire::test(SettingsPage::class)
               ->set('periodStartDay', 25)
               ->call('save')
               ->assertHasNoErrors();

           expect($this->user->fresh()->period_start_day)->toBe(25);
       });

    6. Test 4 implementation — assert both 0 and 29 are rejected with the locked message:

       it('rejects period_start_day outside 1..28', function (): void {
           Livewire::test(SettingsPage::class)
               ->set('periodStartDay', 0)
               ->call('save')
               ->assertHasErrors(['periodStartDay']);

           Livewire::test(SettingsPage::class)
               ->set('periodStartDay', 29)
               ->call('save')
               ->assertHasErrors(['periodStartDay']);
       });

       Add a second assertion verifying the locked error message text appears in the response — this is best done via assertSee on the rendered Blade after a save() call, OR by reading the error bag's message:

       expect($component->errors()->first('periodStartDay'))->toBe('Choose a day from 1 to 28.');

    7. Test 5 implementation — same pattern as Test 4 for the in: validator:

       it('rejects default_currency_view outside {eur_only, original}', function (): void {
           $component = Livewire::test(SettingsPage::class)
               ->set('defaultCurrencyView', 'garbage')
               ->call('save')
               ->assertHasErrors(['defaultCurrencyView']);

           expect($component->errors()->first('defaultCurrencyView'))->toBe('Pick one of the available options.');
       });

    8. Test 6 — stop at "User row updated" for this plan; plan 03-05 owns the TransactionsList half. Implementation:

       it('round-trips default_currency_view = original into the user row', function (): void {
           Livewire::test(SettingsPage::class)
               ->set('defaultCurrencyView', 'original')
               ->set('periodStartDay', 25)
               ->call('save')
               ->assertHasNoErrors();

           $this->user->refresh();
           expect($this->user->default_currency_view)->toBe('original');
           expect($this->user->period_start_day)->toBe(25);
       });

       Rename this it() to remove the "into TransactionsList default mode" trailing — plan 03-05 will add a separate cross-plan test asserting the TransactionsList portion. Use the title above. The original scaffold had a longer title; replace it cleanly.

    9. After making the six scaffolds GREEN, run the full phase-3 group: `vendor/bin/pest --group=phase-3 Modules/Core/tests/Feature/SettingsPageTest.php --stop-on-failure`. Expect zero failures.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Core/tests/Feature/SettingsPageTest.php --stop-on-failure && grep -c 'toBe(false' Modules/Core/tests/Feature/SettingsPageTest.php | grep -q '^0$' && vendor/bin/phpstan analyse Modules/Core/tests/Feature/SettingsPageTest.php --memory-limit=1G && vendor/bin/pint --test Modules/Core/tests/Feature/SettingsPageTest.php && grep -q "Choose a day from 1 to 28\\." Modules/Core/tests/Feature/SettingsPageTest.php && grep -q "Pick one of the available options\\." Modules/Core/tests/Feature/SettingsPageTest.php</automated>
  </verify>
  <acceptance_criteria>
    - All six `it(...)` cases GREEN: `vendor/bin/pest Modules/Core/tests/Feature/SettingsPageTest.php --stop-on-failure` exits zero
    - Every failing scaffold removed: `grep -c 'toBe(false' Modules/Core/tests/Feature/SettingsPageTest.php` returns 0
    - Locked error messages asserted verbatim: `grep -c 'Choose a day from 1 to 28\\.' Modules/Core/tests/Feature/SettingsPageTest.php` returns at least 1 AND `grep -c 'Pick one of the available options\\.' Modules/Core/tests/Feature/SettingsPageTest.php` returns at least 1
    - Larastan + Pint clean on the test file
    - Full phase-3 suite (plans 01-04): `vendor/bin/pest --group=phase-3 --stop-on-failure` exits zero
    - Phase 1/2 regression: `vendor/bin/pest --filter='User\\|InstallCommand\\|LoginFlow' --stop-on-failure` exits zero
  </acceptance_criteria>
  <done>Six Settings round-trip + validation tests GREEN; the user-row storage half of MC-02 is fully discharged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser settings form → server | Two fields cross via Livewire wire:model; the server-side Validate attributes are the only gate before write |
| /settings route → User row | The component writes to the authenticated user's row exclusively via $currentUser->user(); cross-user write is impossible because no user_id is taken as input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | default_currency_view input | mitigate | #[Validate('required|in:eur_only,original')] rejects any other value; the in: validator is the only gate (no DB CHECK constraint, but the validator is server-side and Larastan-strict-enforced). Test 5 asserts the rejection with the locked error message. |
| T-03-04-02 | Tampering | period_start_day input | mitigate | #[Validate('required|integer|min:1|max:28')]. The max:28 cap is the existing project invariant (Phase 1 D-19); 28 is the safe upper bound that works for every month including February. Test 4 covers both boundary failures (0 and 29). |
| T-03-04-03 | Spoofing | Cross-user write via /settings | mitigate | SettingsPage::save() reads $currentUser->user() — never takes a user_id from the request. The 'auth' middleware on the /settings route blocks unauthenticated access entirely. |
| T-03-04-04 | Information Disclosure | Other-user's preferences via /settings | mitigate | mount() reads $currentUser->user() — never queries by ID from the request. The single-user-v1 + auth middleware contract makes cross-user read impossible. |
| T-03-04-05 | Tampering | Migration race in CI | accept | The forward migration adds a column with a non-null default — every existing row is backfilled atomically. The down() drops the column. No data loss path. |
| T-03-04-06 | Repudiation | No audit log of preference changes | accept | Preferences are non-financial display configuration. The audit-log requirement applies to ledger transactions (Phase 1 invariant), not to display preferences. |
</threat_model>

<verification>
- `vendor/bin/pest --group=phase-3 --stop-on-failure` exits zero (plans 01-04 GREEN)
- `vendor/bin/phpstan analyse Modules/Core --memory-limit=1G` exits zero
- `vendor/bin/pint --test` exits zero on every modified file
- `php artisan migrate:fresh --force --env=testing` runs cleanly including the new migration
- The /settings route resolves under auth middleware and unauthenticated requests redirect to /login
- Top nav contains the Settings link in the documented position
- No regression on Phase 1/2 user tests
</verification>

<success_criteria>
1. Schema column users.default_currency_view exists with default 'eur_only'.
2. User model exposes the field as typed (Larastan @property) and fillable.
3. /settings route resolves to SettingsPage Livewire SFC; auth middleware applied.
4. Top nav has a Settings link between Uncategorized and the user-email block.
5. Page renders with the user's current preferences pre-filled and saves them on submit.
6. Validation rejects out-of-range values with the locked UI-SPEC error messages.
7. No Phase 1/2 regression.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ics-cards-multi-currency-display/03-04-SUMMARY.md` documenting:
- The exact route registration pattern (closure vs class-as-handler) — depends on Phase 1's existing convention.
- Whether the existing top-nav had a `$isActive('/settings')` helper or whether it had to be extended for the new path.
- Any deviation from the locked UI-SPEC copy and the rationale.
- Whether the messages() / custom validation messages required a tweak (e.g. translation file vs in-component).
</output>
