---
phase: 03-ics-cards-multi-currency-display
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - Modules/Import/Internal/Http/Livewire/UploadWizard.php
  - Modules/Import/Resources/views/livewire/upload-wizard.blade.php
  - Modules/Import/Internal/Http/Livewire/PreviewWizard.php
  - Modules/Import/tests/Feature/UploadWizardTest.php
  - Modules/Import/tests/Feature/IcsCsvImportTest.php
autonomous: true
requirements:
  - ING-04

must_haves:
  truths:
    - "Wizard upload page surfaces TWO cascading selects: Source (ASN / ICS) and Format (filtered by source) — two-step grouped picker per D-33"
    - "Choosing ICS reveals only the CSV option in the Format dropdown"
    - "Choosing ASN reveals CSV / CAMT.053 (XML) / MT940 in the Format dropdown"
    - "The submit validator still rejects any sourceFormat outside the four-leaf allow-list"
    - "Existing Phase 1/2 UploadWizardTest assertions stay GREEN"
    - "Page subheading reads Drop in an ASN or ICS export. (UI-SPEC verbatim)"
    - "On the first ICS upload the PreviewWizard surfaces the Name your ICS card account. prompt (D-36 / D-38); subsequent uploads skip the prompt"
  artifacts:
    - path: "Modules/Import/Internal/Http/Livewire/UploadWizard.php"
      provides: "Two-step issuer-format cascade — issuer property, availableFormats() method, updatedIssuer() reset hook"
      contains: "availableFormats"
    - path: "Modules/Import/Resources/views/livewire/upload-wizard.blade.php"
      provides: "Two cascading select elements bound to issuer + sourceFormat"
      contains: "wire:model.live"
    - path: "Modules/Import/Internal/Http/Livewire/PreviewWizard.php"
      provides: "ICS-Account naming branch generalised from the IBAN-naming step (D-36 / D-38)"
      contains: "Name your ICS card account"
  key_links:
    - from: "Modules/Import/Resources/views/livewire/upload-wizard.blade.php"
      to: "Modules/Import/Internal/Http/Livewire/UploadWizard.php"
      via: "wire:model.live binding on the issuer select triggers availableFormats() re-render"
      pattern: "wire:model.live=\"issuer\""
    - from: "Modules/Import/Internal/Http/Livewire/PreviewWizard.php"
      to: "Modules/Ledger/Models/Account.php"
      via: "Account row insert with kind='ics_card', synthetic iban='ICS-CARD-<userId>', user-supplied name"
      pattern: "ics_card"
---

<objective>
Two related UX surfaces that finish ING-04 discoverability + onboarding: (1) refactor the wizard's flat sourceFormat dropdown into a two-step cascading picker (issuer → format) per D-33; (2) generalise the existing PreviewWizard IBAN-naming step so it ALSO triggers on the first ICS upload (no `accounts` row with `kind='ics_card'` for the current user) — D-36 / D-38. ASN keeps its three formats; ICS exposes only CSV. PayPal and Google Play groups in later phases will extend the issuer list without touching the format-dropdown logic again.

Purpose: D-33 explicitly carves the cascade refactor as a UI-only change with a snapshot-rebaseline carve-out (Pitfall 3 in 03-RESEARCH.md). D-36/D-38 carves the ICS-Account naming generalisation as a wizard-only UX extension. Both belong on the same UI-affordance surface (the wizard) and are kept off plan 03-02 so the wire-level adapter plan stays single-concern. The two scaffolds in `IcsCsvImportTest` that 03-02 deliberately left RED (`prompts the user to name the ICS Account on the first ICS upload`, `skips the name-your-account step on subsequent ICS uploads`) are driven GREEN here.

Output: UploadWizard refactored with two-select cascade; Blade view restructured; PreviewWizard ICS-Account naming branch wired with UI-SPEC-locked copy; UploadWizardTest extended with cascade assertions; the two IcsCsvImportTest scaffolds driven GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
@.planning/phases/03-ics-cards-multi-currency-display/03-RESEARCH.md
@.planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
@.planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
@.planning/phases/03-ics-cards-multi-currency-display/03-02-SUMMARY.md

# Files this plan modifies — read for current state before touching:
@Modules/Import/Internal/Http/Livewire/UploadWizard.php
@Modules/Import/Resources/views/livewire/upload-wizard.blade.php
@Modules/Import/Internal/Http/Livewire/PreviewWizard.php
@Modules/Import/tests/Feature/UploadWizardTest.php
@Modules/Import/tests/Feature/IcsCsvImportTest.php
@Modules/Ledger/Models/Account.php

<interfaces>
Locked copy from 03-UI-SPEC.md, "Two-step wizard picker labels" — use verbatim:

- Page heading: "Upload statement" (unchanged from Phase 1)
- Page subheading: "Drop in an ASN or ICS export." (replaces Phase 1's "Drop in an ASN CSV, CAMT.053 (XML), or MT940 export.")
- Issuer select label: "Source"
- Issuer options: "ASN" (value asn), "ICS" (value ics)
- Format select label: "Format"
- Format options when ASN: "CSV" (asn-csv), "CAMT.053 (XML)" (asn-camt053), "MT940" (asn-mt940)
- Format options when ICS: "CSV" (ics-csv)

Locked copy from 03-UI-SPEC.md, "Name-your-ICS-Account step (preview wizard, D-36 / D-38)" — use verbatim:

- Prompt heading: "Name your ICS card account."
- Helper paragraph: "This is the first time you've imported ICS data. Give this card a name so it shows up consistently across the app."
- Input label: "Account name"
- Input placeholder: "e.g. ICS card"
- Save button: "Save name" (reuses the IBAN-naming button copy)

Locked behavior from 03-UI-SPEC.md "Two-step wizard picker (D-33)":
- Issuer select drives the Format select options via Livewire wire:model.live
- Changing issuer resets sourceFormat to the first valid leaf for that issuer
- Submit validation allow-list: "in:asn-csv,asn-camt053,asn-mt940,ics-csv"
- aria-live="polite" on the format select wrapper for screen readers
- Hand-rolled <select> cascade (NOT flux:select) per UI-SPEC "Component Inventory" lock

Locked behavior from 03-UI-SPEC.md "Name-your-ICS-Account inline step (D-38)":
- Trigger: ICS upload AND no `accounts` row with `kind='ics_card'` for the current user
- Validation: name required, max 80 chars (mirrors the existing IBAN-naming validation)
- Persistence: insert one accounts row with `user_id = $currentUser->user()->id`, `kind = 'ics_card'`, `name = $input`, `slug = Str::slug($input)`, `iban = sprintf('ICS-CARD-%d', $userId)` (synthetic — matches plan 03-02 Task 2 step 4c), `default_currency = 'EUR'`
- Skip path: once an ICS Account exists, the wizard goes straight to the rows preview
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor UploadWizard.php with issuer property + availableFormats() cascade + reset hook</name>
  <files>
    Modules/Import/Internal/Http/Livewire/UploadWizard.php
  </files>
  <read_first>
    Modules/Import/Internal/Http/Livewire/UploadWizard.php
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
  </read_first>
  <behavior>
    - Setting issuer to 'asn' makes availableFormats() return three entries (asn-csv, asn-camt053, asn-mt940) with UI-SPEC-locked labels.
    - Setting issuer to 'ics' makes availableFormats() return one entry (ics-csv).
    - When issuer changes, sourceFormat resets to the first leaf of the new issuer (switching from ics to asn resets sourceFormat from 'ics-csv' to 'asn-csv').
    - Default issuer on mount is 'asn'; default sourceFormat is 'asn-csv'. Preserves Phase 1/2 most-common-path default.
    - Submit-time validator allow-list stays exactly "in:asn-csv,asn-camt053,asn-mt940,ics-csv" (defensive against direct property tampering).
    - DI-only: no auth() / Auth::user() / global helpers; method-level DI on every Livewire action.
  </behavior>
  <action>
    1. Append 'ics-csv' to SUPPORTED_FORMATS if it isn't already in the array (the legacy plan 03-02 ICS validator extension is moved here in this revised structure — the UploadWizard becomes the single owner of all wizard-level surface changes).

    2. Add a new public string property $issuer = 'asn' with a docblock describing the cascade semantics and a Livewire #[Validate('required|in:asn,ics')] attribute. Add the import: use Livewire\Attributes\Validate; — already present if the file uses #[Validate] elsewhere; verify with grep first.

    3. Add a public method availableFormats() returning list<array{value: string, label: string}>. Implementation (use this exact match expression — labels are LOCKED from UI-SPEC):

       return match ($this->issuer) {
           'asn' => [
               ['value' => 'asn-csv',     'label' => 'CSV'],
               ['value' => 'asn-camt053', 'label' => 'CAMT.053 (XML)'],
               ['value' => 'asn-mt940',   'label' => 'MT940'],
           ],
           'ics' => [
               ['value' => 'ics-csv', 'label' => 'CSV'],
           ],
           default => [],
       };

       The PHPDoc return type MUST be `@return list<array{value: string, label: string}>` for Larastan level 10 strict.

    4. Add a Livewire updated hook for the issuer property that resets sourceFormat to the first leaf of the new issuer:

       public function updatedIssuer(): void
       {
           $first = $this->availableFormats()[0] ?? null;
           if ($first !== null) {
               $this->sourceFormat = $first['value'];
           }
       }

       This guards the case where the user is on Format=ics-csv then changes Source to ASN — without the reset, sourceFormat would be 'ics-csv' which is not in the visible ASN options.

    5. Verify the existing rules() / inline 'in:...' allow-list reads exactly "in:asn-csv,asn-camt053,asn-mt940,ics-csv". If rules() is the validation surface, add `'issuer' => ['required', 'in:asn,ics']` to it as a defensive second layer alongside the #[Validate] attribute. If the validator does not yet include 'ics-csv' (because plan 03-02 was revised to remove its wizard touch), extend it now as part of this task.

    6. Do NOT modify submit() / sanitiseFilename() / mount() / render(). The cascade is purely additive: the leaf sourceFormat value remains the wire-format consumed by HeaderSniffer, SourceAdapterRegistry, and IdempotencyContractTest.

    7. Do NOT touch the Blade view in this task — Task 2 owns that.
  </action>
  <verify>
    <automated>vendor/bin/phpstan analyse Modules/Import/Internal/Http/Livewire/UploadWizard.php --memory-limit=1G && vendor/bin/pint --test Modules/Import/Internal/Http/Livewire/UploadWizard.php && grep -q 'public string \$issuer' Modules/Import/Internal/Http/Livewire/UploadWizard.php && grep -q 'public function availableFormats' Modules/Import/Internal/Http/Livewire/UploadWizard.php && grep -q 'public function updatedIssuer' Modules/Import/Internal/Http/Livewire/UploadWizard.php && grep -q "in:asn-csv,asn-camt053,asn-mt940,ics-csv" Modules/Import/Internal/Http/Livewire/UploadWizard.php && ! grep -E 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' Modules/Import/Internal/Http/Livewire/UploadWizard.php >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - File contains `public string $issuer = 'asn'` (or equivalent typed default): grep returns a match
    - File contains `public function availableFormats(): array` returning the match expression: grep returns a match
    - File contains `public function updatedIssuer(): void` resetting sourceFormat: grep returns a match
    - File contains the Validate attribute on issuer: `grep -c "Validate('required|in:asn,ics')" Modules/Import/Internal/Http/Livewire/UploadWizard.php` returns 1
    - Allow-list present and includes ics-csv: `grep -c "in:asn-csv,asn-camt053,asn-mt940,ics-csv" Modules/Import/Internal/Http/Livewire/UploadWizard.php` returns at least 1
    - All four locked format labels appear verbatim: `grep -c "'CAMT.053 (XML)'" Modules/Import/Internal/Http/Livewire/UploadWizard.php` returns 1 AND `grep -c "'MT940'" Modules/Import/Internal/Http/Livewire/UploadWizard.php` returns 1
    - Larastan level 10 strict clean: `vendor/bin/phpstan analyse Modules/Import/Internal/Http/Livewire/UploadWizard.php --memory-limit=1G` exits zero
    - Pint clean: `vendor/bin/pint --test Modules/Import/Internal/Http/Livewire/UploadWizard.php` exits zero
    - No auth/facade slippage: `! grep -E 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' Modules/Import/Internal/Http/Livewire/UploadWizard.php >/dev/null`
  </acceptance_criteria>
  <done>UploadWizard.php has the two-step picker cascade in PHP; existing Phase 1/2 paths untouched; tests still callable via leaf-value injection.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure upload-wizard.blade.php into two cascading selects and update subheading</name>
  <files>
    Modules/Import/Resources/views/livewire/upload-wizard.blade.php
  </files>
  <read_first>
    Modules/Import/Resources/views/livewire/upload-wizard.blade.php
    Modules/Import/Internal/Http/Livewire/UploadWizard.php
    Modules/Categorization/Resources/views/livewire/inline-category-picker.blade.php
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
    .planning/phases/03-ics-cards-multi-currency-display/03-PATTERNS.md
  </read_first>
  <behavior>
    - The page subheading reads exactly: "Drop in an ASN or ICS export." (replaces Phase 1's text).
    - Two stacked select elements appear in this order: Source (issuer), then Format (sourceFormat). File input + submit button follow unchanged.
    - The Source select uses `wire:model.live="issuer"` so changing the source triggers a Livewire re-render of the Format options.
    - The Format select is wrapped in an element carrying `aria-live="polite"` so screen readers announce option changes when issuer flips.
    - When issuer="asn", three <option> elements render in the Format select; when issuer="ics", one renders. The visible <option> set is driven by `$this->availableFormats()` from the PHP component, not by JavaScript or static markup.
    - Existing focus-ring + slate-50 / slate-200 / emerald-600 chrome (from Phase 1 login + Phase 1/2 wizard) is reused verbatim.
    - No third-party UI component (no flux:select) is introduced — UI-SPEC §"Component Inventory" locks the wizard's cascade to hand-rolled <select>.
  </behavior>
  <action>
    1. Update the existing page subheading text. Locate the current subheading paragraph "Drop in an ASN CSV, CAMT.053 (XML), or MT940 export." and replace it verbatim with "Drop in an ASN or ICS export." Do NOT change the heading wrapper, classes, or HTML structure — only the literal text inside the paragraph.

    2. Locate the current single Format <select> block. Replace it with two stacked <select> blocks:

       Source select (NEW, BEFORE the Format select):
       - Wrapper: <div class="space-y-1">
       - Label: <label for="issuer" class="block text-sm text-slate-900">Source</label>
       - Select: <select id="issuer" name="issuer" wire:model.live="issuer" class="block w-full rounded-md border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-900 focus:outline-none focus-visible:ring-2 focus-visible:ring-slate-900 focus-visible:ring-offset-2">
         - <option value="asn">ASN</option>
         - <option value="ics">ICS</option>
       - Validation error block: @error('issuer')<p class="text-sm text-rose-600">{{ $message }}</p>@enderror

       Format select (REPLACES the existing one):
       - Wrapper: <div class="space-y-1" aria-live="polite">    (aria-live on the wrapper, NOT the <select> — screen readers announce options when the wrapper's child set changes)
       - Label: <label for="sourceFormat" class="block text-sm text-slate-900">Format</label>
       - Select: <select id="sourceFormat" name="sourceFormat" wire:model="sourceFormat" class="block w-full rounded-md border border-slate-200 bg-slate-50 px-3 py-2 text-sm text-slate-900 focus:outline-none focus-visible:ring-2 focus-visible:ring-slate-900 focus-visible:ring-offset-2">
         - @foreach ($this->availableFormats() as $fmt)
           - <option value="{{ $fmt['value'] }}">{{ $fmt['label'] }}</option>
           - @endforeach
       - Validation error block: @error('sourceFormat')<p class="text-sm text-rose-600">{{ $message }}</p>@enderror

       Use the existing chrome classes verbatim (slate-50 / slate-200 / emerald-600 palette per 03-UI-SPEC.md §"Color"). The cascade pattern wire:model.live mirrors `Modules/Categorization/Resources/views/livewire/inline-category-picker.blade.php` line 3 — verify that file before composing the markup.

    3. Leave the file input + submit button + every other block (header, error summaries, helper text, csrf, layout shell) UNTOUCHED. Only the subheading paragraph + the Format-select block are mutated by this task.

    4. Do NOT introduce a flux:select or any flux:* component. UI-SPEC explicitly locks this to hand-rolled <select> for blast-radius containment.
  </action>
  <verify>
    <automated>grep -q 'Drop in an ASN or ICS export\.' Modules/Import/Resources/views/livewire/upload-wizard.blade.php && grep -q 'wire:model\.live="issuer"' Modules/Import/Resources/views/livewire/upload-wizard.blade.php && grep -q 'aria-live="polite"' Modules/Import/Resources/views/livewire/upload-wizard.blade.php && grep -q 'this->availableFormats' Modules/Import/Resources/views/livewire/upload-wizard.blade.php && ! grep -q 'flux:select' Modules/Import/Resources/views/livewire/upload-wizard.blade.php && ! grep -q 'Drop in an ASN CSV, CAMT' Modules/Import/Resources/views/livewire/upload-wizard.blade.php</automated>
  </verify>
  <acceptance_criteria>
    - Subheading replaced: `grep -c 'Drop in an ASN or ICS export\.' Modules/Import/Resources/views/livewire/upload-wizard.blade.php` returns at least 1; old subheading absent: `grep -c 'Drop in an ASN CSV, CAMT' Modules/Import/Resources/views/livewire/upload-wizard.blade.php` returns 0
    - Source <select> bound to issuer with .live modifier: `grep -c 'wire:model\.live="issuer"' Modules/Import/Resources/views/livewire/upload-wizard.blade.php` returns 1
    - aria-live region around Format select: `grep -c 'aria-live="polite"' Modules/Import/Resources/views/livewire/upload-wizard.blade.php` returns at least 1
    - Format options driven by component method, not static markup: `grep -c 'availableFormats' Modules/Import/Resources/views/livewire/upload-wizard.blade.php` returns at least 1
    - No flux:select introduced: `grep -c 'flux:select' Modules/Import/Resources/views/livewire/upload-wizard.blade.php` returns 0
    - Both Source labels visible verbatim: `grep -c '>Source<' Modules/Import/Resources/views/livewire/upload-wizard.blade.php` returns 1 AND `grep -c '>Format<' Modules/Import/Resources/views/livewire/upload-wizard.blade.php` returns 1
    - Both issuer options present: `grep -c '<option value="asn">ASN<' Modules/Import/Resources/views/livewire/upload-wizard.blade.php` returns 1 AND `grep -c '<option value="ics">ICS<' Modules/Import/Resources/views/livewire/upload-wizard.blade.php` returns 1
  </acceptance_criteria>
  <done>Blade view restructured into the two-step picker matching UI-SPEC; old single-dropdown gone; existing focus/chrome reused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend UploadWizardTest with cascade behavior + ICS pick + reset assertions; verify ASN regression</name>
  <files>
    Modules/Import/tests/Feature/UploadWizardTest.php
  </files>
  <read_first>
    Modules/Import/tests/Feature/UploadWizardTest.php
    Modules/Import/Internal/Http/Livewire/UploadWizard.php
    Modules/Import/Resources/views/livewire/upload-wizard.blade.php
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
  </read_first>
  <behavior>
    - A new test asserts the wizard's default state has issuer='asn' and sourceFormat='asn-csv'.
    - A new test asserts setting issuer='ics' causes availableFormats() to return exactly one entry: {value: 'ics-csv', label: 'CSV'}.
    - A new test asserts setting issuer='ics' (via Livewire set) then submitting with file + sourceFormat='ics-csv' passes validation and reaches the same downstream wizard step as ASN (preview redirect or PreviewWizard mount).
    - A new test asserts switching issuer from 'ics' to 'asn' resets sourceFormat to 'asn-csv'.
    - A new test asserts the rendered Blade includes both Source select (with issuer wire-binding) and Format select (with at least one option) on the initial GET.
    - Every existing UploadWizardTest assertion stays GREEN (the Livewire-test API uses ->set('sourceFormat', ...) directly, bypassing the cascade).
  </behavior>
  <action>
    1. Read the existing UploadWizardTest fully. Identify the existing setup (likely a beforeEach with actingAs($user) and storage faking) and reuse it for new tests.

    2. Append the following Pest tests AFTER the existing tests. Use the Livewire test API (`use Livewire\Livewire;`) consistent with the existing file's style.

       Test A: it('opens the wizard with ASN as the default issuer and asn-csv as the default format')
       - Livewire::test(UploadWizard::class)
       - ->assertSet('issuer', 'asn')
       - ->assertSet('sourceFormat', 'asn-csv');

       Test B: it('returns only the CSV format when the issuer is ICS')
       - $component = Livewire::test(UploadWizard::class)->set('issuer', 'ics');
       - $available = $component->instance()->availableFormats();
       - expect($available)->toBe([['value' => 'ics-csv', 'label' => 'CSV']]);

       Test C: it('returns the three ASN formats when the issuer is ASN')
       - $component = Livewire::test(UploadWizard::class)->set('issuer', 'asn');
       - $available = $component->instance()->availableFormats();
       - expect($available)->toBe([
           ['value' => 'asn-csv',     'label' => 'CSV'],
           ['value' => 'asn-camt053', 'label' => 'CAMT.053 (XML)'],
           ['value' => 'asn-mt940',   'label' => 'MT940'],
         ]);

       Test D: it('resets sourceFormat to the first leaf when the issuer changes')
       - Livewire::test(UploadWizard::class)
       - ->set('issuer', 'ics')
       - ->assertSet('sourceFormat', 'ics-csv')
       - ->set('issuer', 'asn')
       - ->assertSet('sourceFormat', 'asn-csv');

       Test E: it('lets the user pick ICS issuer and ics-csv format and submit')
       - Use the existing test's storage-fake + UploadedFile pattern. Build a fake CSV upload file (any non-empty .csv blob is fine — HeaderSniffer is mocked out at this layer, OR the test uploads the actual ICS fixture file via UploadedFile::fake() pointing at Modules/Ingestion/tests/fixtures/ics/ics-sample-1.csv).
       - Livewire::test(UploadWizard::class)
       - ->set('issuer', 'ics')
       - ->set('sourceFormat', 'ics-csv')
       - ->set('file', $fakeUpload)
       - ->call('submit')
       - ->assertHasNoErrors();
       - If the existing AsnCsv test's submit() asserts a redirect or a session value, match that assertion shape here.

       Test F: it('renders the two-step picker on the upload page')
       - $this->get(route('imports.new'))
       - ->assertOk()
       - ->assertSee('Source', false)
       - ->assertSee('Format', false)
       - ->assertSee('Drop in an ASN or ICS export.', false)
       - ->assertSee('wire:model.live="issuer"', false);

    3. Do NOT remove any existing test. The legacy "ASN happy path" tests stay verbatim — they use ->set('sourceFormat', 'asn-csv') and bypass the cascade.

    4. Pint-format the file: vendor/bin/pint Modules/Import/tests/Feature/UploadWizardTest.php — apply the project style.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Import/tests/Feature/UploadWizardTest.php --stop-on-failure && vendor/bin/phpstan analyse Modules/Import/tests/Feature/UploadWizardTest.php --memory-limit=1G && vendor/bin/pint --test Modules/Import/tests/Feature/UploadWizardTest.php && grep -q "opens the wizard with ASN as the default" Modules/Import/tests/Feature/UploadWizardTest.php && grep -q "lets the user pick ICS issuer" Modules/Import/tests/Feature/UploadWizardTest.php && grep -q "resets sourceFormat to the first leaf" Modules/Import/tests/Feature/UploadWizardTest.php</automated>
  </verify>
  <acceptance_criteria>
    - All six new `it(...)` cases present (A through F) — grep -c "^it\(" Modules/Import/tests/Feature/UploadWizardTest.php returns at least the pre-existing count plus 6
    - All six new tests GREEN: `vendor/bin/pest Modules/Import/tests/Feature/UploadWizardTest.php --stop-on-failure` exits zero
    - Every existing UploadWizardTest test still GREEN (regression): the full file passes in one run (covered by the same command above)
    - Larastan + Pint clean
    - Phase 1/2 ASN regression suite stays GREEN: `vendor/bin/pest --filter='AsnCsv|AsnCamt053|AsnMt940|UploadWizard' --stop-on-failure` exits zero
  </acceptance_criteria>
  <done>Six new wizard-cascade tests GREEN; ASN regression GREEN; Phase 3 ING-04 wizard discoverability discharged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: PreviewWizard ICS-Account naming branch (D-36 / D-38) + drive the two RED IcsCsvImportTest scaffolds GREEN</name>
  <files>
    Modules/Import/Internal/Http/Livewire/PreviewWizard.php
    Modules/Import/tests/Feature/IcsCsvImportTest.php
  </files>
  <read_first>
    Modules/Import/Internal/Http/Livewire/PreviewWizard.php
    Modules/Import/tests/Feature/IcsCsvImportTest.php
    Modules/Import/tests/Feature/AsnCsvImportTest.php
    Modules/Ledger/Models/Account.php
    Modules/Core/Public/Contracts/CurrentUser.php
    Modules/Core/Public/Services/CurrentUserService.php
    .planning/phases/03-ics-cards-multi-currency-display/03-UI-SPEC.md
    .planning/phases/03-ics-cards-multi-currency-display/03-CONTEXT.md
  </read_first>
  <behavior>
    - On the FIRST ICS upload (no `Account` row with `kind = 'ics_card'` exists for the current user), `PreviewWizard` renders the ICS-Account naming prompt INSTEAD OF the rows table. Locked copy from UI-SPEC §"Name-your-ICS-Account step":
      - Heading: `Name your ICS card account.`
      - Helper paragraph: `This is the first time you've imported ICS data. Give this card a name so it shows up consistently across the app.`
      - Input label: `Account name`
      - Input placeholder: `e.g. ICS card`
      - Save button: `Save name`
    - Saving the form inserts ONE `accounts` row with: `user_id = $currentUser->user()->id`, `kind = 'ics_card'`, `name = <input>` (max 80 chars), `slug = Str::slug($input)`, `iban = sprintf('ICS-CARD-%d', $userId)` (synthetic — matches the synthetic id chosen in plan 03-02 Task 2 step 4c), `default_currency = 'EUR'`.
    - Subsequent ICS uploads (with an `accounts` row of `kind='ics_card'` already present for the user) SKIP the naming step entirely and go straight to the rows preview.
    - DI-only: PreviewWizard uses method-level DI on every Livewire action (`CurrentUser` + `DatabaseManager` or direct Eloquent Account model). Forbidden: `auth()`, `Auth::user()`, `\Illuminate\Support\Facades\Auth`, any global helper.
    - Validation: name is `required|string|max:80`; the synthetic iban is server-built, never user input.
  </behavior>
  <action>
    1. **Read the existing IBAN-naming branch** in `Modules/Import/Internal/Http/Livewire/PreviewWizard.php`. The wizard currently surfaces a naming prompt when `AccountResolver` returns `UnknownAccount` for an ASN IBAN. Map out:
       - The trigger predicate (likely `$this->needsAccountName` or similar)
       - The Blade partial / inline block that renders the IBAN-naming form
       - The save action (`saveAccountName`, `createAccount`, or similar)

    2. **Generalise the trigger.** Add a condition path so the naming prompt ALSO fires when:
       - `sourceFormat === 'ics-csv'` (the wizard was launched on an ICS upload), AND
       - no `accounts` row with `kind = 'ics_card'` exists for `$currentUser->user()->id`.

       Implement as a derived property or method, e.g.:
       ```php
       public function needsIcsAccountName(CurrentUser $currentUser): bool
       {
           if ($this->sourceFormat !== 'ics-csv') {
               return false;
           }
           return ! Account::query()
               ->where('user_id', $currentUser->user()->id)
               ->where('kind', 'ics_card')
               ->exists();
       }
       ```
       (Or whatever DI shape the existing IBAN trigger uses — keep the structural pattern symmetric.)

    3. **Render the naming prompt with the locked copy.** Reuse the existing IBAN-naming Blade partial / inline form, replacing ONLY the heading + helper copy with the UI-SPEC §"Name-your-ICS-Account step" locked strings verbatim:
       - Heading: `Name your ICS card account.`
       - Helper: `This is the first time you've imported ICS data. Give this card a name so it shows up consistently across the app.`
       - Input label: `Account name`
       - Input placeholder: `e.g. ICS card`
       - Save button: `Save name`

       If the IBAN-naming form is in a partial, branch the partial on the trigger reason; if it is inline, add an `@if/@elseif` around the heading/helper block. The input + save button can stay shared.

    4. **Persist the row on save.** Implement (or extend) the save action so that when the trigger fired for `ics-csv`, it inserts an `accounts` row with the exact shape in `<behavior>`:
       ```php
       public function saveIcsAccountName(CurrentUser $currentUser): void
       {
           $this->validate([
               'newAccountName' => ['required', 'string', 'max:80'],
           ]);

           $userId = $currentUser->user()->id;
           Account::create([
               'user_id' => $userId,
               'kind' => 'ics_card',
               'name' => $this->newAccountName,
               'slug' => Str::slug($this->newAccountName),
               'iban' => sprintf('ICS-CARD-%d', $userId),
               'default_currency' => 'EUR',
           ]);

           // Continue into the rows preview now that the account exists.
           // Re-resolve any UnknownAccount entries that were blocked.
           $this->dispatch('account-created');
       }
       ```
       Inject collaborators via method-level DI ONLY. Reuse the existing IBAN-name Livewire `#[Validate]` attribute pattern. The `slug` and `name` fields are validated; the synthetic iban is server-built (not user input).

    5. **Subsequent uploads skip path.** Verify by inspection: when `needsIcsAccountName()` returns `false`, the wizard renders the rows table immediately (no naming form). If the existing IBAN-name conditional already guards the rows table render, the new ICS branch automatically inherits the skip path.

    6. **Make the two RED scaffolds in `Modules/Import/tests/Feature/IcsCsvImportTest.php` GREEN.** Replace the two remaining `expect(true)->toBe(false, ...)` placeholders:

       - `it('prompts the user to name the ICS Account on the first ICS upload')`: with NO `Account` row of `kind='ics_card'` for the user, Livewire-test the PreviewWizard flow on an ICS upload; assert the rendered Blade contains the locked heading `"Name your ICS card account."` AND the rows table is NOT rendered (assertDontSee on the row table marker).
       - `it('skips the name-your-account step on subsequent ICS uploads')`: pre-seed an ICS account; Livewire-test the PreviewWizard; assert the heading `"Name your ICS card account."` is NOT rendered AND the rows table IS rendered.

       Both scaffolds use the existing `seedFixtureUserAndAccount()` + `actingAs($this->fixtureUser)` + `$this->importer = $this->app->make(RunsImports::class)` setup from the analog. After this task, `grep -c 'toBe(false' Modules/Import/tests/Feature/IcsCsvImportTest.php` returns `0`.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Import/tests/Feature/IcsCsvImportTest.php --stop-on-failure && vendor/bin/phpstan analyse Modules/Import/Internal/Http/Livewire/PreviewWizard.php --memory-limit=1G && vendor/bin/pint --test Modules/Import/Internal/Http/Livewire/PreviewWizard.php Modules/Import/tests/Feature/IcsCsvImportTest.php && grep -q "Name your ICS card account" Modules/Import/Internal/Http/Livewire/PreviewWizard.php && grep -q "first time you've imported ICS data" Modules/Import/Internal/Http/Livewire/PreviewWizard.php && ! grep -E 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' Modules/Import/Internal/Http/Livewire/PreviewWizard.php >/dev/null && [ "$(grep -c 'toBe(false' Modules/Import/tests/Feature/IcsCsvImportTest.php)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - PreviewWizard contains the locked ICS-Account naming heading: `grep -c 'Name your ICS card account' Modules/Import/Internal/Http/Livewire/PreviewWizard.php` returns ≥ 1
    - PreviewWizard contains the locked helper paragraph: `grep -c "first time you've imported ICS data" Modules/Import/Internal/Http/Livewire/PreviewWizard.php` returns ≥ 1
    - PreviewWizard uses DI-only (no facade/helper slippage): `! grep -E 'auth\(\)|Auth::user\(|\\\\Auth\\\\Facades' Modules/Import/Internal/Http/Livewire/PreviewWizard.php >/dev/null`
    - The two previously-RED scaffolds are now GREEN: `grep -c 'toBe(false' Modules/Import/tests/Feature/IcsCsvImportTest.php` returns `0`
    - All six `IcsCsvImportTest` scaffolds GREEN end-to-end: `vendor/bin/pest Modules/Import/tests/Feature/IcsCsvImportTest.php --stop-on-failure` exits zero
    - Phase 1/2 IBAN-naming regression stays GREEN: `vendor/bin/pest --filter='PreviewWizard|AsnCsvImport' --stop-on-failure` exits zero
    - Larastan + Pint clean on every modified file
  </acceptance_criteria>
  <done>PreviewWizard recognises ICS as a naming-trigger source; the locked copy from UI-SPEC renders verbatim; both IcsCsvImportTest wizard scaffolds GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser issuer/sourceFormat input → server | Both fields land via Livewire wire:model bindings; the server-side Validate attribute + rules() allow-list enforce the allowed values |
| Browser ICS-account-name input → accounts table | Free-text user input persisted to `accounts.name` and `accounts.slug` |
| Authenticated user → accounts INSERT | The new ICS account row is keyed off `$currentUser->user()->id`; never accepts a user_id from the form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | issuer + sourceFormat properties | mitigate | Both fields carry server-side validators: issuer in:asn,ics; sourceFormat in:asn-csv,asn-camt053,asn-mt940,ics-csv. A direct DOM tweak to an unknown issuer or format value is rejected before submit reaches HeaderSniffer. |
| T-03-03-02 | Information Disclosure | Cross-issuer leak | accept | The cascade is purely UI-affordance: every server-side path (HeaderSniffer, SourceAdapterRegistry, ImportPipeline) consumes the leaf sourceFormat only. Submitting a deliberately-mismatched issuer (e.g. issuer=ics, sourceFormat=asn-csv via DOM tweak) is harmless — sourceFormat is the only field the pipeline reads. |
| T-03-03-03 | Spoofing | CSRF on the wizard form | mitigate | Livewire-native CSRF protection covers wire:submit / wire:click by default (Phase 1 pattern). No new mitigation needed in Phase 3. |
| T-03-03-04 | Tampering | `accounts.name` input from PreviewWizard | mitigate | Livewire `#[Validate('required|string|max:80')]` (or equivalent rules) on the name field; the synthetic `iban='ICS-CARD-<userId>'` is server-built, never user-supplied, so the `accounts.iban` UNIQUE constraint cannot be hijacked. |
| T-03-03-05 | Spoofing | Cross-user account write via PreviewWizard | mitigate | DI-injected `CurrentUser` (never `auth()`); the new ICS Account row is keyed off `$currentUser->user()->id`. The existing `BoundaryArchTest` + `UserIdColumnArchTest` regression-test the multi-user-ready scoping. |
</threat_model>

<verification>
- `vendor/bin/pest --group=phase-3 --stop-on-failure` exits zero
- `vendor/bin/pest Modules/Import/tests/Feature/UploadWizardTest.php --stop-on-failure` exits zero (regression + new)
- `vendor/bin/pest Modules/Import/tests/Feature/IcsCsvImportTest.php --stop-on-failure` exits zero (all six scaffolds now GREEN)
- `vendor/bin/phpstan analyse Modules/Import --memory-limit=1G` exits zero
- `vendor/bin/pint --test` exits zero on all modified files
- ASN Phase 1/2 regression suite stays GREEN
- The Blade view contains both Source and Format selects with the locked label text
- No flux:select introduced (UI-SPEC component-inventory lock)
- PreviewWizard contains the locked ICS-Account naming heading + helper paragraph verbatim
</verification>

<success_criteria>
1. Visiting `/imports/new` shows the two-step picker: Source select (ASN / ICS) on top, Format select below, file input, submit button.
2. Choosing Source=ICS hides ASN's three format options and reveals only "CSV".
3. Choosing Source=ASN restores the three format options (CSV / CAMT.053 (XML) / MT940).
4. Submitting an ICS CSV via the cascade reaches the same downstream pipeline as Phase 1/2's ASN paths.
5. The subheading reads "Drop in an ASN or ICS export." verbatim.
6. The first ICS upload surfaces the locked "Name your ICS card account." prompt; subsequent uploads skip it.
7. No Phase 1/2 regression: every prior wizard test stays GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ics-cards-multi-currency-display/03-03-SUMMARY.md` documenting:
- Whether the existing wizard had Phase 1 D-19 default values that needed manual reconciliation with `issuer='asn'`/`sourceFormat='asn-csv'`.
- Any structural anomaly in the existing UploadWizardTest that required adapting Test A-F's setup.
- Whether the Blade view's existing chrome (focus-ring, palette) was modified beyond the locked subheading + select-block swap (it should NOT have been).
- Snapshot files (if any existed for the wizard view) that were re-baselined.
- The PreviewWizard structural choice: did the ICS-naming branch reuse the existing IBAN-naming Blade partial, or did it require a separate partial? Document the exact trigger predicate (`needsIcsAccountName` or alternative) and its DI signature.
- Whether `Str::slug()` was DI-injected or used as a global helper (project rule: no global helpers — if `Str::slug` was unavoidable, justify in the SUMMARY).
</output>
