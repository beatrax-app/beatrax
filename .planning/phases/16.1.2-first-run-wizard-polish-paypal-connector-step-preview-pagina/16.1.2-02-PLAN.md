---
phase: 16.1.2
plan: 02
title: "PayPal connector wizard step + FirstImportStep stash read"
slug: connect-paypal-step
type: execute
wave: 2
depends_on:
  - 16.1.2-01
files_modified:
  - Modules/Onboarding/Internal/Services/WizardStepRegistry.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php
  - Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php
  - Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php
  - Modules/Onboarding/tests/Feature/ConnectPaypalStepStashesRunIdTest.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "The wizard step order is welcome → connect-bank → connect-paypal → connect-card → connect-email → first-import → done"
    - "A user can drop a PayPal Activity Download CSV on the connect-paypal step and the step previews the upload + stashes paypal_import_run_id (single int) in wizard_progress.data"
    - "On a successful first preview, the user has a PayPal account row (iban='PAYPAL', kind='paypal', EUR-settled) created via EnsurePaypalAccountAction (idempotent on resubmit)"
    - "When the user uploads a PayPal Balance Reconciliation Report by mistake, UnsupportedPaypalCsvShapeException bubbles into the step's inline error band (same shape ConnectCardStep uses for per-file failures)"
    - "FirstImportStep::resolveStashedImportRunIds reads paypal_import_run_id alongside bank + card ids and orders preview sections bank → paypal → card → email"
    - "The connect-paypal step is skippable; when skipped, no paypal_import_run_id is stashed and FirstImportStep's PayPal section simply does not render"
  artifacts:
    - path: "Modules/Onboarding/Internal/Services/WizardStepRegistry.php"
      provides: "Step order + skippable list with 'connect-paypal' inserted between 'connect-bank' and 'connect-card'"
      contains: "connect-paypal"
    - path: "Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php"
      provides: "Livewire step component: single-file PayPal Activity CSV upload + stash + auto-create-account"
      contains: "class ConnectPaypalStep"
    - path: "Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php"
      provides: "Blade view assembling wiz-card / mini-step / format-chip / drop-zone / privacy-pill / wiz-actions atoms for the PayPal step"
      contains: "wire:model=\"activityCsv\""
    - path: "Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php"
      provides: "resolveStashedImportRunIds extended to read paypal_import_run_id + step_key whereIn list extended + ORDER BY case extended"
      contains: "paypal_import_run_id"
    - path: "Modules/Onboarding/tests/Feature/ConnectPaypalStepStashesRunIdTest.php"
      provides: "Pest feature coverage: stash key written + Account auto-created"
      contains: "ConnectPaypalStep"
  key_links:
    - from: "Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php"
      to: "Modules/Import/Public/Actions/EnsurePaypalAccountAction.php"
      via: "method-DI on submit()"
      pattern: "EnsurePaypalAccountAction"
    - from: "Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php"
      to: "Modules/Import/Public/Contracts/RunsImports"
      via: "method-DI on submit() + runFromUpload('paypal-csv', ...)"
      pattern: "RunsImports"
    - from: "Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php"
      to: "wizard_progress.data['paypal_import_run_id']"
      via: "resolveStashedImportRunIds JSON decode"
      pattern: "paypal_import_run_id"
---

<objective>
Ship the missing PayPal connector wizard step (D-01 through D-08). Insert `'connect-paypal'` into `WizardStepRegistry::STEPS` between `'connect-bank'` and `'connect-card'` and into `SKIPPABLE`. Create `ConnectPaypalStep` Livewire component as the fourth instance of the connector-step skeleton (parallels ConnectBankStep's single-file branch) + its Blade view reusing the shipped atomic primitives verbatim. Wire `EnsurePaypalAccountAction` (Plan 01) for idempotent synthetic-PayPal-account auto-create on first preview. Extend `FirstImportStep::resolveStashedImportRunIds` (the line-347 method) to read `paypal_import_run_id` alongside the existing bank + card ids, and update the ORDER BY case so preview sections render bank → paypal → card → email in the wizard's flow order. Ship one Pest feature test (`ConnectPaypalStepStashesRunIdTest`) covering the stash-key write + the Account auto-create.

Purpose: Make the wizard's funding-source coverage match the chain-resolution mental model from Phase 5 — bank funds PayPal funds card. Without this step, a new user whose primary spending lives on PayPal can complete the wizard with PayPal silently invisible, breaking the "show me what I actually owe" core value.

Output: Six modified/new files (registry, step, blade, FirstImportStep, test, deferred-items if any).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-CONTEXT.md
@.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-PATTERNS.md
@.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-01-SUMMARY.md
@CLAUDE.md
@.claude/skills/sketch-findings-diederik/SKILL.md
@.claude/skills/sketch-findings-diederik/references/onboarding-wizard.md
@Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectBankStep.php
@Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectCardStep.php
@Modules/Onboarding/Resources/views/livewire/steps/connect-card-step.blade.php
@Modules/Onboarding/Resources/views/livewire/steps/connect-bank-step.blade.php
@Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php
@Modules/Onboarding/Internal/Services/WizardStepRegistry.php
@Modules/Onboarding/tests/Feature/ConnectCardStepStashesAllRunIdsTest.php
@Modules/Import/Public/Actions/EnsurePaypalAccountAction.php

<interfaces>
<!-- Locked literals + contracts the executor consumes -->
Source format key (PayPal):       'paypal-csv'  (PaypalCsvPaymentTypeHinter, PaypalCsvStartingBalanceDetector, ClassifyTransactionType.php:96)
Synthetic IBAN literal:           'PAYPAL'      (EnsurePaypalAccountAction::PAYPAL_OWN_IBAN)
Step key (wizard_progress):       'connect-paypal'
Stash key (wizard_progress.data): 'paypal_import_run_id'  (single int, NOT array — D-03)
Step glyph (mini-step row):       💸           (already used by sketch 002D welcome PayPal tile)
Format chip label:                'PayPal CSV' (single chip; PayPal exports one format)
RunsImports method:               runFromUpload(string $tmpPath, string $format, User $user, string $originalFilename): ImportPreviewResult
WizardProgress upsert pattern:    ConnectBankStep.php:282-294 (stash + dispatch('wizard.step.completed'))
Inline error band field:          $uploadError (one-shot string, reset on next submit)
</interfaces>

<existing_atoms>
<!-- All atoms are already shipped in Phase 16.1 Plan 03b — do NOT create new ones -->
<x-onboarding::wiz-card>           Resources/views/components/wiz-card.blade.php
<x-onboarding::mini-step>          Resources/views/components/mini-step.blade.php
<x-onboarding::format-chip>        Resources/views/components/format-chip.blade.php
<x-onboarding::drop-zone>          Resources/views/components/drop-zone.blade.php (single-file via no `multiple`; `wire-model` prop)
<x-onboarding::privacy-pill>       Resources/views/components/privacy-pill.blade.php
<x-onboarding::wiz-actions>        Resources/views/components/wiz-actions.blade.php (Skip + Continue buttons)
<x-onboarding::vd-glyph>           Resources/views/components/vd-glyph.blade.php (generic glyph prop — accepts any emoji string)
</existing_atoms>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert 'connect-paypal' into WizardStepRegistry::STEPS + SKIPPABLE (D-01)</name>
  <files>Modules/Onboarding/Internal/Services/WizardStepRegistry.php</files>
  <read_first>
    - Modules/Onboarding/Internal/Services/WizardStepRegistry.php (full file — 49 LOC; both const arrays at lines ~10-25)
    - Modules/Onboarding/Internal/Services/WizardProgressInitializer.php (lines 48-62 — confirm the seeding loop walks the registry STEPS const without other hardcoded lists; PATTERNS.md notes this is the only downstream effect)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-CONTEXT.md (D-01, D-07)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-PATTERNS.md (section "WizardStepRegistry.php — edit-in-place")
    - CLAUDE.md (docs-describe-current-state)
  </read_first>
  <action>
    Edit `Modules/Onboarding/Internal/Services/WizardStepRegistry.php`. In the `private const STEPS` array, insert the literal string `'connect-paypal'` between `'connect-bank'` and `'connect-card'`. In the `private const SKIPPABLE` array, insert `'connect-paypal'` between `'connect-bank'` and `'connect-card'`. Per D-01 and D-07.

    Per CLAUDE.md docs-describe-current-state: do NOT add a comment like "// PayPal step added in Phase 16.1.2". The const declarations are self-documenting.

    Do not change anything else. Do not add new methods. Do not touch other constants. Per CLAUDE.md fix-all-severities — if Pint or Larastan flags anything in this file, fix it in this task (no carry-forward).
  </action>
  <verify>
    <automated>grep -c "'connect-paypal'" Modules/Onboarding/Internal/Services/WizardStepRegistry.php</automated>
  </verify>
  <acceptance_criteria>
    - 'connect-paypal' appears exactly twice (STEPS + SKIPPABLE): `grep -c "'connect-paypal'" Modules/Onboarding/Internal/Services/WizardStepRegistry.php` equals 2
    - Order preserved (between bank and card): `grep -A 8 "private const STEPS" Modules/Onboarding/Internal/Services/WizardStepRegistry.php | grep -B 1 "'connect-card'" | head -1 | grep -q "'connect-paypal'"` exits 0
    - Total step count is now 7 (welcome + 4 connectors + first-import + done): `grep -c "    '" Modules/Onboarding/Internal/Services/WizardStepRegistry.php` is at least 11 (7 STEPS + 4 SKIPPABLE; both arrays)
    - No GSD-leakage added: `grep -E "\\.planning|PLAN\\.md|RESEARCH\\.md|D-[0-9]+" Modules/Onboarding/Internal/Services/WizardStepRegistry.php` returns no matches
    - Pint clean: `vendor/bin/pint --test Modules/Onboarding/Internal/Services/WizardStepRegistry.php` exits 0
    - Larastan L10 strict clean: `vendor/bin/phpstan analyse --no-progress --memory-limit=2G Modules/Onboarding/Internal/Services/WizardStepRegistry.php` exits 0
    - Existing wizard tests still pass (regression check on the step list — initializer + resume-wizard tests walk this list): `vendor/bin/pest Modules/Onboarding/tests/Feature/ResumeWizardTest.php Modules/Onboarding/tests/Feature/SkipWizardStepTest.php Modules/Onboarding/tests/Feature/WizardProgressMigrationTest.php --no-coverage` exits 0
  </acceptance_criteria>
  <done>
    The wizard's single source of truth lists `connect-paypal` in position 3 (welcome=1, connect-bank=2, connect-paypal=3, connect-card=4, connect-email=5, first-import=6, done=7) and as skippable. WizardProgressInitializer will start seeding a `connect-paypal` row per new user on the next signup.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ConnectPaypalStep Livewire component + Blade view + Pest stash test (D-02..D-05, D-07, D-08)</name>
  <files>Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php, Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php, Modules/Onboarding/tests/Feature/ConnectPaypalStepStashesRunIdTest.php</files>
  <read_first>
    - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectBankStep.php (primary analog — single-file upload branch + stash single int + sanitiseFilename helper + Livewire DI rule PHPDoc lines 49-52)
    - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectCardStep.php (secondary analog — inline error band shape lines 62-72 + skip() + render() lines 336-365)
    - Modules/Onboarding/Resources/views/livewire/steps/connect-card-step.blade.php (Blade skeleton — mini-step row + format-chip + drop-zone + wiz-actions; the inline error band block at lines 62-72)
    - Modules/Onboarding/Resources/views/livewire/steps/connect-bank-step.blade.php (single-file ready pill at lines 133-137 — to copy verbatim for the PayPal step)
    - Modules/Onboarding/tests/Feature/ConnectCardStepStashesAllRunIdsTest.php (exact test scaffold; reuse setUp, Pest helpers)
    - Modules/Import/Public/Actions/EnsurePaypalAccountAction.php (newly shipped in Plan 01 — method-DI shape on submit())
    - Modules/Import/Public/Contracts/RunsImports.php (the runFromUpload contract; locked signature)
    - Modules/Onboarding/Models/WizardProgress.php (Eloquent model — confirm $data is `array` casted)
    - Modules/Import/tests/Feature/PaypalCsvImportTest.php (line 27 — fixture path: `base_path('Modules/Ingestion/tests/fixtures/paypal/paypal-sample-1.csv')`)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-CONTEXT.md (D-02, D-03, D-04, D-05, D-07, D-08)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-PATTERNS.md (sections "ConnectPaypalStep.php" + "connect-paypal-step.blade.php" + "ConnectPaypalStepStashesRunIdTest.php")
    - .claude/skills/sketch-findings-diederik/SKILL.md (auto-loads via project skill routing; design contract)
    - .claude/skills/sketch-findings-diederik/references/onboarding-wizard.md (connector-step contract from sketch 003 — four-tile mini-step row + format chips + persistent drop zone)
    - CLAUDE.md (Laravel DI-only — method-DI on Livewire action methods; fix-all-severities)
  </read_first>
  <behavior>
    - Dropping a valid PayPal Activity Download CSV → submit() returns without setting $uploadError; ImportRun row exists with source_format='paypal-csv', user_id=current user; wizard_progress row for step_key='connect-paypal' has data['paypal_import_run_id'] === (int) the run id; 'wizard.step.completed' event dispatched
    - On the first successful submit by a user with no existing PayPal account: accounts table grows by exactly one row with user_id=current user, iban='PAYPAL', kind='paypal', default_currency='EUR'; the action returns true and the re-preview pass fires from the stored path
    - On a subsequent successful submit by a user who already has a PayPal account: accounts table stays at one PayPal row (idempotent); the re-preview branch is skipped because the action returns false
    - Dropping a PayPal Balance Reconciliation Report (or anything that raises UnsupportedPaypalCsvShapeException inside the parser pipeline): the Throwable catch sets $uploadError to "Could not read this file. The full error is in /dev/logs." and the wizard_progress row is NOT updated (no paypal_import_run_id stash); 'wizard.step.completed' is NOT dispatched
    - skip() dispatches 'wizard.step.skipped'; no DB writes happen; wizard_progress row for 'connect-paypal' remains with data['paypal_import_run_id'] absent
    - Validation: empty submit (no file) → validate() throws + error message "Drop your PayPal Activity Download CSV into the box first."; oversized file (>10240 KB) → "That file is too large…"; wrong extension → "That file doesn't look like a PayPal CSV…"
  </behavior>
  <action>
    Create `Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php`. Follow PATTERNS.md verbatim for the imports, class shape, rules(), messages(), submit(), skip(), render(), sanitiseFilename(). Concrete contract:

    Class: `final class ConnectPaypalStep extends \Livewire\Component` with `use WithFileUploads;`. Public properties: `public string $selectedFormat = 'paypal-csv';` (locked leaf format key — matches PaypalCsvPaymentTypeHinter + PaypalCsvStartingBalanceDetector + ClassifyTransactionType:96); `public ?\Livewire\Features\SupportFileUploads\TemporaryUploadedFile $activityCsv = null;` (D-02 — single file, not array); `public ?string $uploadError = null;` (one-shot inline error band — same shape as ConnectBankStep::$uploadError).

    `rules()` returns `['activityCsv' => ['required', 'file', 'max:10240', 'extensions:csv,txt']]`. `messages()` returns the three friendly strings from the behavior block above.

    `submit()` signature — method-DI on every collaborator (Livewire forbids constructor-DI on Component subclasses; see ConnectBankStep.php:49-52 PHPDoc): `public function submit(\Modules\Import\Public\Contracts\RunsImports $importer, \Modules\Import\Public\Actions\EnsurePaypalAccountAction $ensurePaypalAccount, \Modules\Core\Public\Contracts\CurrentUser $currentUser, \Psr\Log\LoggerInterface $logger, \Illuminate\Contracts\Foundation\Application $app, \Illuminate\Database\DatabaseManager $db): void`.

    Submit body — verbatim shape from PATTERNS.md "Submit pattern" block:
    1. `$this->uploadError = null;` reset
    2. `$this->validate();` (rules above)
    3. `if ($this->activityCsv === null) return;` (defensive null guard)
    4. `$user = $currentUser->user();`
    5. `$tmp = $this->activityCsv->getRealPath();`
    6. `$originalFilename = $this->sanitiseFilename($this->activityCsv->getClientOriginalName());`
    7. `try { $result = $importer->runFromUpload($tmp, $this->selectedFormat, $user, $originalFilename); } catch (Throwable $e) { ... }` — on catch, log via $logger->error with the diagnostic shape from PATTERNS.md (source_format, filename, exception_class, exception_message, exception_trace via SafeTrace::cap($e, $app->basePath())); set `$this->uploadError = 'Could not read this file. The full error is in /dev/logs.';`; return. UnsupportedPaypalCsvShapeException (shipped in 16.1.1-04) bubbles into this catch — same friendly toast surface (D-08).
    8. `$created = ($ensurePaypalAccount)($user);` — uses defaults (no overrides; wizard auto-create uses name='PayPal', slug='paypal-paypal' per D-04 + Plan 01)
    9. `if ($created) { ...re-preview from stored path... }` — fetch ImportRun by id + user_id; if file still exists, call `$importer->runFromUpload($run->raw_file_path, $this->selectedFormat, $user, basename($run->raw_file_path));` inside a `try { ... } catch (Throwable $e) { $logger->warning(...); }` — the warning shape mirrors the error log (PATTERNS.md "ConnectBankStep::251-279" re-preview pattern; PayPal omits the 5th formatHint param because PayPal CSV is unambiguous).
    10. Stash the run id: `$progress = WizardProgress::query()->where('user_id', $user->id)->where('step_key', 'connect-paypal')->first(); if ($progress !== null) { $data = $progress->data ?? []; $data['paypal_import_run_id'] = $result->importRunId; $progress->update(['data' => $data]); }` — D-03 single int, not array.
    11. `$this->dispatch('wizard.step.completed');`

    `skip()` — verbatim from ConnectBankStep: `public function skip(): void { $this->dispatch('wizard.step.skipped'); }` (D-07).

    `render(ViewFactory $views)` — `return $views->make('onboarding::livewire.steps.connect-paypal-step');`.

    `sanitiseFilename(string $original)` — verbatim copy from PATTERNS.md (regex strip + 'upload' fallback + '.csv' suffix).

    PHPDoc on the class: one short paragraph in present tense — "Wizard step 3: collects a single PayPal Activity Download CSV, previews it through RunsImports, idempotently creates the synthetic PAYPAL accounts row, and stashes the resulting import run id under wizard_progress.data['paypal_import_run_id']." No history, no PLAN/D-NN references, no GSD-leakage per CLAUDE.md.

    --- Blade view: `Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php`. Mirror `connect-card-step.blade.php` skeleton with these swaps:
    - Eyebrow + step number: `<p class="wiz-eyebrow">💸 Step 3 — Your PayPal account</p>` (PayPal moved to step 3 per D-01)
    - H1: `<h1 class="wiz-h1">Connect your PayPal account</h1>`
    - Lede: short calm-Notion line — "Drop your PayPal Activity Download CSV. Balance Reconciliation Reports won't work — we need Activity." (matches the friendly-error copy from messages())
    - mini-step row reflecting the PayPal portal export path (Log in → Activity → Download → CSV) — use four `<x-onboarding::mini-step>` instances with the existing glyph prop contract (vd-glyph is accepted on the prop per PATTERNS.md "vd-glyph" section — no atom edit needed); each mini-step gets a glyph + word per the sketch 003 contract
    - Format chip: single `<x-onboarding::format-chip label="PayPal CSV" badge="only format" />` (mirrors ICS PDF single-chip pattern per CONTEXT.md D-15 + sketch 003 winner)
    - Drop zone: `<x-onboarding::drop-zone wire-model="activityCsv" accept=".csv" />` (single-file — no `multiple` attribute on the underlying input)
    - Ready pill (from connect-bank-step.blade.php:133-137): `@if ($activityCsv !== null) <p class="wiz-file-ready">{{ $activityCsv->getClientOriginalName() }} · ✓ ready</p> @endif`
    - Inline error band (from connect-card-step.blade.php:62-72): `@error('activityCsv') <p class="wiz-error" role="alert">{{ $message }}</p> @enderror @if ($uploadError !== null) <p class="wiz-error" role="alert">{{ $uploadError }}</p> @endif`
    - Actions slot (from connect-card-step.blade.php:74-90): `<x-onboarding::wiz-actions :show-skip="true" submit-label="Continue" />` (or whatever the locked atom signature is — read the atom file if unsure)
    - All cell rendering uses `{{ }}` Blade auto-escape — NEVER `{!! !!}` (CSV-injection mitigation per threat_model T-16.1.2.02-01)

    Per CONTEXT.md D-15: this step's visual contract is the existing sketch 003 winner (four-tile mini-step row + format chips + persistent drop zone). No new sketch. No new atoms. The skill auto-loads for cross-screen consistency reference.

    --- Pest test: `Modules/Onboarding/tests/Feature/ConnectPaypalStepStashesRunIdTest.php`. Copy the scaffold verbatim from `ConnectCardStepStashesAllRunIdsTest.php`:
    - Top of file: `<?php declare(strict_types=1); use ...; uses(RefreshDatabase::class);`
    - beforeEach: create a User via `\Modules\Core\Models\User::query()->create(['username' => 'paypal-step', 'password' => 'fixture-password-12chars', 'period_start_day' => 1]);` then `$this->actingAs($this->user);` then `\Modules\Onboarding\Internal\Services\WizardProgressInitializer` via `$this->app->make(...)->initialize($this->user->id);`
    - Two `it(...)` cases per PATTERNS.md "Test 4":
      1. `it('stashes paypal_import_run_id in wizard_progress.data after a successful submit', function (): void { ... })` — load the fixture via `\Illuminate\Http\UploadedFile::fake()->createWithContent('activity.csv', file_get_contents(base_path('Modules/Ingestion/tests/fixtures/paypal/paypal-sample-1.csv')))`; `\Livewire\Livewire::test(\Modules\Onboarding\Internal\Http\Livewire\Steps\ConnectPaypalStep::class)->set('activityCsv', $csv)->call('submit')->assertDispatched('wizard.step.completed');` — then fetch the wizard_progress row and assert `data['paypal_import_run_id']` is an int.
      2. `it('auto-creates the PayPal account via EnsurePaypalAccountAction on first preview', function (): void { ... })` — assert account count is 0 before; submit; assert account count is 1 after with iban='PAYPAL'.

    The test uses `app()` only inside the test fixture (helpers) — acceptable per PATTERNS.md "Helper uses app() directly only inside the test fixture" note. Production code never uses helpers.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/Feature/ConnectPaypalStepStashesRunIdTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist: `test -f Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php -a -f Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php -a -f Modules/Onboarding/tests/Feature/ConnectPaypalStepStashesRunIdTest.php` returns 0
    - Class declared: `grep -c "final class ConnectPaypalStep" Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php` equals 1
    - Single-file (not array) property: `grep -c "public ?TemporaryUploadedFile \\$activityCsv" Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php` equals 1
    - Locked format key: `grep -c "public string \\$selectedFormat = 'paypal-csv'" Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php` equals 1
    - Method-DI on submit (no constructor DI): `grep -c "EnsurePaypalAccountAction \\$ensurePaypalAccount" Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php` equals 1
    - No facades / helpers in the step: `grep -E "Illuminate\\\\Support\\\\Facades|^use DB;|app\\(|auth\\(|request\\(\\)" Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php` returns no matches
    - Stash key + step key literals: `grep -c "'paypal_import_run_id'" Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php` equals 1 AND `grep -c "'connect-paypal'" Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php` is at least 1
    - Blade view uses wire:model on the activity CSV: `grep -c "wire:model=\"activityCsv\"\\|wire-model=\"activityCsv\"" Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php` is at least 1
    - Blade uses Blade-escape only (no unescape): `grep -c "{!!" Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php` equals 0
    - Format chip is the locked single-chip variant: `grep -c "format-chip" Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php` is at least 1
    - Pest tests pass: `vendor/bin/pest Modules/Onboarding/tests/Feature/ConnectPaypalStepStashesRunIdTest.php --no-coverage` exits 0
    - Larastan L10 strict clean on the step: `vendor/bin/phpstan analyse --no-progress --memory-limit=2G Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php` exits 0
    - Pint clean on all three files: `vendor/bin/pint --test Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php Modules/Onboarding/tests/Feature/ConnectPaypalStepStashesRunIdTest.php` exits 0
    - No GSD-leakage in any of the new files: `grep -rE "\\.planning|PLAN\\.md|RESEARCH\\.md|D-[0-9]+" Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php Modules/Onboarding/tests/Feature/ConnectPaypalStepStashesRunIdTest.php` returns no matches
  </acceptance_criteria>
  <done>
    A user can navigate to /setup, advance through Welcome → Connect Bank → Connect PayPal, drop a real PayPal Activity Download CSV, see the preview, and have `paypal_import_run_id` stashed under wizard_progress.data['paypal_import_run_id'] with a PayPal account auto-created. Pest test proves both branches (stash + auto-create) green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend FirstImportStep::resolveStashedImportRunIds to read paypal_import_run_id (D-06)</name>
  <files>Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php</files>
  <read_first>
    - Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php (lines 347-394 — `resolveStashedImportRunIds`; the whereIn list at line 352, ORDER BY at line 353, JSON decode block at lines 367-380)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-CONTEXT.md (D-03, D-06)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-PATTERNS.md (section "FirstImportStep.php — resolveStashedImportRunIds extension (D-06)" — the three load-bearing fragments to edit)
    - CLAUDE.md
  </read_first>
  <behavior>
    - resolveStashedImportRunIds returns the union of bank_import_run_id (single int), paypal_import_run_id (single int), card_import_run_ids (array<int>), and email_import_run_id (if any) — same shape as before, just with PayPal added to the union
    - The returned list is ordered bank-source ids first, then PayPal source id, then card source ids, then email source ids (mirrors the wizard's flow order — affects rendered preview section order in FirstImportStep)
    - When wizard_progress.data['paypal_import_run_id'] is absent or null, the method behaves identically to before (no PayPal id in the union)
    - When wizard_progress.data['paypal_import_run_id'] is a non-int (defensive guard) or ≤0, the method skips it (matches the bank-id guard at the same level)
  </behavior>
  <action>
    Edit `Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php::resolveStashedImportRunIds()` per the three PATTERNS.md fragments:

    1. Extend the `whereIn('step_key', [...])` list (line 352) to add `'connect-paypal'`: `->whereIn('step_key', ['connect-bank', 'connect-paypal', 'connect-card', 'connect-email'])`.

    2. Extend the ORDER BY case (line 353) to assign PayPal precedence 2 (between bank=1 and card=3): `->orderByRaw("CASE step_key WHEN 'connect-bank' THEN 1 WHEN 'connect-paypal' THEN 2 WHEN 'connect-card' THEN 3 ELSE 4 END")`. ICS card moves from 2→3; the trailing ELSE moves from 3→4 (covers connect-email). This keeps the PayPal section rendered between bank and card in the consolidated preview.

    3. Add the JSON decode read (after the bank_import_run_id block, before card_import_run_ids — PATTERNS.md exact insertion point):

       ```
       $paypalId = $decoded['paypal_import_run_id'] ?? null;
       if (is_int($paypalId) && $paypalId > 0) {
           $ids[] = $paypalId;
       }
       ```

       Insert this fragment between the existing bank-id `if (is_int($bankId) && $bankId > 0) { $ids[] = $bankId; }` block (line ~371) and the existing card-id `$cardIds = $decoded['card_import_run_ids'] ?? null;` block (line ~373). The insertion order matters: bank → paypal → card mirrors the ORDER BY precedence above and the rendered section order downstream.

    Do not change the method signature. Return type stays `list<int>`. No DB schema change. No PreviewWizard touch.

    Per CLAUDE.md docs-describe-current-state: do NOT add a PHPDoc note like "// PayPal added in 16.1.2". The structural shape of the method (3 stash sources + ORDER BY) is self-evident.

    Per CLAUDE.md fix-all-severities: if Pint or Larastan flags anything in the touched method, fix it now.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - The whereIn list contains 'connect-paypal': `grep -c "'connect-paypal'" Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` is at least 1
    - The ORDER BY case mentions all four step keys: `grep -c "WHEN 'connect-paypal' THEN 2" Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` equals 1 AND `grep -c "WHEN 'connect-card' THEN 3" Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` equals 1
    - JSON read for paypal_import_run_id present with guard: `grep -c "'paypal_import_run_id'" Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` is at least 1 AND `grep -c "is_int(\\$paypalId)" Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` equals 1
    - Method signature unchanged: `grep -c "private function resolveStashedImportRunIds" Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` equals 1
    - Existing FirstImportStep tests still pass (ConsolidatedPreviewLoadTest + FirstImportStepWidthTest + StartingBalanceConfirmCardsTest): `vendor/bin/pest Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php Modules/Onboarding/tests/Feature/StartingBalanceConfirmCardsTest.php --no-coverage` exits 0
    - Larastan L10 strict clean on FirstImportStep: `vendor/bin/phpstan analyse --no-progress --memory-limit=2G Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` exits 0
    - Pint clean on FirstImportStep: `vendor/bin/pint --test Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` exits 0
    - No GSD-leakage added: `grep -E "\\.planning|PLAN\\.md|RESEARCH\\.md|D-[0-9]+" Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` returns no matches
  </acceptance_criteria>
  <done>
    FirstImportStep now consumes the stashed PayPal run id alongside bank + card + email. End-to-end walk: drop a PayPal Activity CSV at /setup step 3 → continue → reach the FirstImportStep page → see a PayPal preview section rendered between the bank section and the card section. No other FirstImportStep behavior changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → ConnectPaypalStep::submit | TemporaryUploadedFile + validate() rules + extensions guard + 10 MB cap before the importer sees the file |
| ConnectPaypalStep → RunsImports | $tmpPath + format + $user reach the parser pipeline; unknown CSV shapes raise UnsupportedPaypalCsvShapeException which the catch surfaces friendly-error-only (no stack trace leaks to UI) |
| browser → Blade-rendered preview cells | All cells (counterparty, description, amount, dates) render via `{{ }}` Blade auto-escape — no `{!! !!}` anywhere |
| caller → EnsurePaypalAccountAction | Plan 01 already mitigates cross-user account leak (user_id scoped into exists() + INSERT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.2.02-01 | Tampering (CSV injection) | connect-paypal-step.blade.php + downstream preview rendering | mitigate | All cells use `{{ }}` Blade auto-escape; grep gate `{!!` count == 0 enforced in acceptance criteria |
| T-16.1.2.02-02 | Denial of Service (oversized upload) | ConnectPaypalStep::rules() | mitigate | `max:10240` (10 MB) cap on activityCsv; Laravel + Livewire upload framework already rejects larger payloads before the controller body runs |
| T-16.1.2.02-03 | Information Disclosure (stack trace leak) | ConnectPaypalStep::submit catch | mitigate | catch sets generic `$uploadError = 'Could not read this file. The full error is in /dev/logs.'`; full trace goes to LoggerInterface (with SafeTrace::cap), never to the user-visible band |
| T-16.1.2.02-04 | Tampering (cross-user stash read) | FirstImportStep::resolveStashedImportRunIds | mitigate | The wizard_progress query is already user-scoped (existing code, unchanged); the new paypal_import_run_id read inherits the same user-scope guard |
| T-16.1.2.02-05 | Spoofing (unauthenticated wizard step access) | ConnectPaypalStep | accept | Step mounts inside the SetupWizard parent which is already authenticated-only (Modules/Onboarding routing); the new step inherits that access boundary |
| T-16.1.2.02-06 | Tampering (TemporaryUploadedFile race) | ConnectPaypalStep::submit re-preview branch | mitigate | re-preview reads from `$run->raw_file_path` (the stored path) via `file_exists()` guard; if the temp file disappeared between calls, the branch silently skips the re-preview |
</threat_model>

<verification>
End-to-end manual smoke after the plan lands (NOT a checkpoint — just a developer sanity check):

```bash
# Reset the dev DB to a fresh user
php artisan migrate:fresh --seed
# Run the wizard manually:
# 1. /signup → username/password
# 2. Advance through Welcome
# 3. Connect Bank (skip)
# 4. Connect PayPal — drop Modules/Ingestion/tests/fixtures/paypal/paypal-sample-1.csv
# 5. Confirm wizard_progress.data['paypal_import_run_id'] is set:
php artisan tinker --execute="dd(\Modules\Onboarding\Models\WizardProgress::where('step_key','connect-paypal')->first()?->data);"
# 6. Continue → Connect Card (skip) → Connect Email (skip) → First Import
# 7. Confirm the FirstImportStep page renders a PayPal preview section between bank (absent) and card (absent)
```

Automated regression suite:

```bash
vendor/bin/pest Modules/Onboarding/tests/Feature/ --no-coverage
vendor/bin/pest Modules/Import/tests/Feature/ --no-coverage
vendor/bin/phpstan analyse --no-progress --memory-limit=2G Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php Modules/Onboarding/Internal/Services/WizardStepRegistry.php Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php
vendor/bin/pint --test Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectPaypalStep.php Modules/Onboarding/Internal/Services/WizardStepRegistry.php Modules/Onboarding/Resources/views/livewire/steps/connect-paypal-step.blade.php Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php Modules/Onboarding/tests/Feature/ConnectPaypalStepStashesRunIdTest.php
```
</verification>

<success_criteria>
- WizardStepRegistry has `connect-paypal` in position 3 in STEPS and present in SKIPPABLE
- ConnectPaypalStep Livewire component shipped + Blade view shipped, using only already-shipped atoms
- Pest `ConnectPaypalStepStashesRunIdTest` covers both branches (stash + auto-create) — green
- FirstImportStep::resolveStashedImportRunIds reads `paypal_import_run_id` and orders sections bank → paypal → card → email
- Existing wizard tests + existing FirstImportStep tests all stay green
- Larastan L10 strict + Pint clean on every touched file
- No GSD-leakage in any new or modified file
- A real PayPal Activity CSV walk-through reaches FirstImportStep with the PayPal section rendered between bank and card
</success_criteria>

<output>
Create `.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-02-SUMMARY.md` with: files created/modified, the manual smoke walk result (steps run + observation), and a one-line note that FirstImportStep is now ready for Plan 03 (pagination state) and Plan 05 (sketch winner layout).
</output>
