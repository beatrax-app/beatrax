---
phase: 16.1.2
plan: 01
title: "EnsurePaypalAccountAction extraction + PreviewWizard swap"
slug: ensure-paypal-account-action
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/Import/Public/Actions/EnsurePaypalAccountAction.php
  - Modules/Import/Internal/Http/Livewire/PreviewWizard.php
  - Modules/Import/tests/Feature/EnsurePaypalAccountActionTest.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "An EnsurePaypalAccountAction class exists at Modules/Import/Public/Actions/EnsurePaypalAccountAction.php and is invokable with a User"
    - "Calling the action when no PayPal account exists for the user INSERTs one with iban='PAYPAL', kind='paypal', default_currency='EUR' and returns true"
    - "Calling the action when a PayPal account already exists for the user does NOT INSERT a second row and returns false"
    - "PreviewWizard::savePaypalAccountName() delegates synthetic-account creation to EnsurePaypalAccountAction (no inline Account::query()->create([...kind: 'paypal'...]) block remains in PreviewWizard)"
    - "PreviewWizard's user-typed name + slug overrides are honored by the action when supplied"
  artifacts:
    - path: "Modules/Import/Public/Actions/EnsurePaypalAccountAction.php"
      provides: "Synthetic PayPal account auto-create action (constructor-DI, single DatabaseManager collaborator, idempotent)"
      contains: "class EnsurePaypalAccountAction"
    - path: "Modules/Import/Internal/Http/Livewire/PreviewWizard.php"
      provides: "PreviewWizard with the inline synthetic-PayPal-account create replaced by an EnsurePaypalAccountAction invocation"
      contains: "EnsurePaypalAccountAction"
    - path: "Modules/Import/tests/Feature/EnsurePaypalAccountActionTest.php"
      provides: "Pest feature coverage for idempotent-create + name-override behavior"
      contains: "EnsurePaypalAccountAction"
  key_links:
    - from: "Modules/Import/Internal/Http/Livewire/PreviewWizard.php"
      to: "Modules/Import/Public/Actions/EnsurePaypalAccountAction.php"
      via: "method-DI on savePaypalAccountName(...)"
      pattern: "EnsurePaypalAccountAction"
---

<objective>
Extract the inline synthetic-PayPal-account creation in `PreviewWizard::savePaypalAccountName()` (PreviewWizard.php:347-366) into a new constructor-DI'd action class at `Modules/Import/Public/Actions/EnsurePaypalAccountAction.php` (D-05). The action is idempotent — it returns `true` when an INSERT happened and `false` when the account already existed — and accepts optional `$nameOverride` / `$slugBodyOverride` so PreviewWizard can keep its user-typed name flow while the upcoming `ConnectPaypalStep` (Plan 02) can call the action with no override and get the default `'PayPal'` / `'paypal-paypal'` shape.

Purpose: Centralise the synthetic-IBAN + kind + EUR-currency literals so two callers (PreviewWizard standalone flow, ConnectPaypalStep wizard flow) cannot drift, and so the wizard step has a clean DI seam without re-implementing the create block. Per memory `feedback_laravel_di_only`: constructor-DI only, no facades, no global helpers.

Output: New action class + refactored PreviewWizard + Pest feature test proving idempotent-create + name-override behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-CONTEXT.md
@.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-PATTERNS.md
@CLAUDE.md
@Modules/Import/Internal/Http/Livewire/PreviewWizard.php
@Modules/Import/Public/Actions/DiscardImport.php
@Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectCardStep.php

<interfaces>
<!-- Locked literals for the action body -->
PreviewWizard::PAYPAL_OWN_IBAN = 'PAYPAL'        (PreviewWizard.php:67)
account kind                   = 'paypal'        (PreviewWizard.php:355)
default currency               = 'EUR'           (PreviewWizard.php:357)
default name (no override)     = 'PayPal'        (Plan-02 wizard auto-create)
default slug (no override)     = 'paypal-paypal' (Plan-02 wizard auto-create)
PHPStan-clean exists-check     = DB::connection()->table('accounts')->where(...)->exists()
                                 (NOT Account::query()->exists() — trips staticMethod.dynamicCall)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create EnsurePaypalAccountAction with idempotent INSERT + Pest coverage</name>
  <files>Modules/Import/Public/Actions/EnsurePaypalAccountAction.php, Modules/Import/tests/Feature/EnsurePaypalAccountActionTest.php</files>
  <read_first>
    - Modules/Import/Public/Actions/DiscardImport.php (shape analog for the action class — final readonly + single collaborator constructor-DI)
    - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectCardStep.php (lines 181-206 — body analog: raw Query Builder exists() + Account::query()->create() idempotent pattern, AND the `// Raw Query Builder used instead of Account::query()->exists()` PHPStan-strict-rules comment that must be preserved verbatim in the new action)
    - Modules/Import/Internal/Http/Livewire/PreviewWizard.php (lines 324-366 — current inline create block being replaced; line 67 PAYPAL_OWN_IBAN const)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-CONTEXT.md (decisions D-04, D-05; planner-discretion note on name override shape)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-PATTERNS.md (section "Modules/Import/Public/Actions/EnsurePaypalAccountAction.php" — suggested target shape)
    - CLAUDE.md (Laravel DI-only rule; docs-describe-current-state rule; fix-all-severities)
  </read_first>
  <behavior>
    - When invoked with a User who has no existing accounts row with iban='PAYPAL' for their user_id → INSERTs one and returns true
    - When invoked with a User who already has an accounts row with iban='PAYPAL' for their user_id → does NOT INSERT and returns false (no second row created; assertCount on accounts stays at 1)
    - Defaults: name='PayPal', slug='paypal-paypal', kind='paypal', default_currency='EUR' when no overrides are supplied
    - When called with $nameOverride='PayPal NL', $slugBodyOverride='paypal-nl' → INSERTed row has name='PayPal NL' and slug='paypal-nl-paypal' (the action appends `-paypal` to slugBodyOverride to mirror PreviewWizard's existing slug shape at line 356)
    - Two users each invoking the action get exactly one PayPal accounts row per user_id (no cross-user leak — the user_id scope on the exists() query is honored)
  </behavior>
  <action>
    Create `Modules/Import/Public/Actions/EnsurePaypalAccountAction.php` as a `final readonly` class implementing decision D-05 from CONTEXT.md.

    Class signature: `final readonly class EnsurePaypalAccountAction` in namespace `Modules\Import\Public\Actions`. Single constructor-DI dependency: `private DatabaseManager $db`. No facades. No global helpers (per CLAUDE.md Laravel DI-only rule).

    Public constant: `public const string PAYPAL_OWN_IBAN = 'PAYPAL';` — mirrors `Modules\Import\Internal\Http\Livewire\PreviewWizard::PAYPAL_OWN_IBAN` literal.

    Method signature: `public function __invoke(\Modules\Core\Models\User $user, ?string $nameOverride = null, ?string $slugBodyOverride = null): bool` returning true iff an INSERT happened, false when the row already existed (per D-05 + PATTERNS.md suggested target shape).

    Body — mirror the ConnectCardStep.php:181-206 pattern verbatim:
    1. Raw Query Builder exists check: `$this->db->connection()->table('accounts')->where('user_id', $user->id)->where('iban', self::PAYPAL_OWN_IBAN)->exists()`. Preserve the `// Raw Query Builder used instead of Account::query()->exists() to satisfy PHPStan strict-rules staticMethod.dynamicCall — same pattern as TransactionDetail and UpdateTransactionCategory.` inline comment (literal text from ConnectCardStep.php:189-191) so future readers know the pattern is load-bearing.
    2. If exists → `return false;` (idempotent skip).
    3. Otherwise: `\Modules\Ledger\Models\Account::query()->create([...])` with `user_id`, name (override or 'PayPal'), slug (override.'-paypal' if override truthy, else 'paypal-paypal'), kind='paypal', iban=self::PAYPAL_OWN_IBAN, default_currency='EUR'.
    4. Return true.

    PHPDoc on the class: state what the action does in present tense (current state per CLAUDE.md). No mention of `.planning/`, no PLAN/D-NN reference, no "I changed this because" history. One short paragraph: "Ensures the user has a synthetic PayPal accounts row keyed on iban 'PAYPAL', kind 'paypal', EUR-settled. Returns true when an INSERT happened, false when the account already existed."

    Create the Pest feature test at `Modules/Import/tests/Feature/EnsurePaypalAccountActionTest.php`. Use `uses(RefreshDatabase::class);` at the top. Bind the action via `$this->app->make(EnsurePaypalAccountAction::class)` (constructor-DI resolves DatabaseManager from the container — no facades). Four `it(...)` cases per the behavior block above (creates-on-first-call, idempotent-second-call, honors-name-override, no-cross-user-leak). Use `\Modules\Core\Models\User::query()->create([...])` to seed two distinct users. Assert via `\Modules\Ledger\Models\Account::query()->where('user_id', $user->id)->where('iban', 'PAYPAL')->count()` — never reach for facades.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Import/tests/Feature/EnsurePaypalAccountActionTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f Modules/Import/Public/Actions/EnsurePaypalAccountAction.php` returns 0
    - Class declaration: `grep -c "final readonly class EnsurePaypalAccountAction" Modules/Import/Public/Actions/EnsurePaypalAccountAction.php` equals 1
    - Public constant present: `grep -c "public const string PAYPAL_OWN_IBAN = 'PAYPAL'" Modules/Import/Public/Actions/EnsurePaypalAccountAction.php` equals 1
    - DI-only (no facades / helpers): `grep -E "use Illuminate\\\\Support\\\\Facades|^use DB;|app\\(|auth\\(|request\\(\\)" Modules/Import/Public/Actions/EnsurePaypalAccountAction.php` returns no matches (exit 1)
    - PHPStan strict-rules comment preserved: `grep -c "Raw Query Builder used instead of Account::query()->exists()" Modules/Import/Public/Actions/EnsurePaypalAccountAction.php` equals 1
    - Pest tests pass: `vendor/bin/pest Modules/Import/tests/Feature/EnsurePaypalAccountActionTest.php --no-coverage` exits 0
    - Larastan L10 strict clean on the new action: `vendor/bin/phpstan analyse --no-progress --memory-limit=2G Modules/Import/Public/Actions/EnsurePaypalAccountAction.php` exits 0
    - Pint clean on the new file: `vendor/bin/pint --test Modules/Import/Public/Actions/EnsurePaypalAccountAction.php` exits 0
    - Pint clean on the new test: `vendor/bin/pint --test Modules/Import/tests/Feature/EnsurePaypalAccountActionTest.php` exits 0
    - No GSD-leakage in the action source: `grep -E "\\.planning|PLAN\\.md|RESEARCH\\.md|D-[0-9]+" Modules/Import/Public/Actions/EnsurePaypalAccountAction.php` returns no matches
  </acceptance_criteria>
  <done>
    EnsurePaypalAccountAction exists, is invokable, idempotent, and covered by four passing Pest tests. Larastan L10 strict + Pint pass on the new files. No facades or helpers anywhere in the action body. The action is ready to be wired into PreviewWizard (next task) and ConnectPaypalStep (Plan 02).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap PreviewWizard::savePaypalAccountName() to delegate to EnsurePaypalAccountAction</name>
  <files>Modules/Import/Internal/Http/Livewire/PreviewWizard.php</files>
  <read_first>
    - Modules/Import/Internal/Http/Livewire/PreviewWizard.php (lines 324-366 — current inline create block being replaced; line 67 PAYPAL_OWN_IBAN const — keep or drop depending on whether other PreviewWizard methods reference it)
    - Modules/Import/Public/Actions/EnsurePaypalAccountAction.php (newly created in Task 1 — confirm signature)
    - Modules/Onboarding/Internal/Http/Livewire/Steps/ConnectBankStep.php (lines 49-52 — Livewire DI rule reminder PHPDoc; lines 169-175 — method-DI signature pattern; PreviewWizard is also a Livewire Component subclass so the same rule applies)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-PATTERNS.md (section "Modules/Import/Internal/Http/Livewire/PreviewWizard.php" — two extract shapes, recommendation #1)
    - CLAUDE.md (Laravel DI-only rule for Livewire method-DI on action methods)
  </read_first>
  <behavior>
    - savePaypalAccountName() no longer contains an `Account::query()->create([...kind: 'paypal'...])` block — synthetic-account creation flows through EnsurePaypalAccountAction
    - The user-typed `$this->paypalAccountName` and the derived `$slugBody` from AccountNamer::validateName() are passed as `$nameOverride` / `$slugBodyOverride` to the action so the resulting Account row has the user's chosen name (parity with the pre-refactor behavior)
    - The subsequent `runFromUpload(...)` re-preview call (PreviewWizard.php:340-349) is preserved verbatim
    - Existing PreviewWizard Pest tests that exercise savePaypalAccountName still pass (no behavior change observable from the outside)
    - The InvalidAccountNameException early-return path stays intact (the action is never reached when validation fails)
  </behavior>
  <action>
    Modify `Modules/Import/Internal/Http/Livewire/PreviewWizard.php::savePaypalAccountName()` to delegate the synthetic-account create to `\Modules\Import\Public\Actions\EnsurePaypalAccountAction` per D-05 and PATTERNS.md recommendation #1.

    Add method-DI on `savePaypalAccountName()` — append `\Modules\Import\Public\Actions\EnsurePaypalAccountAction $ensurePaypal` to the existing signature `public function savePaypalAccountName(RunsImports $importer, CurrentUser $currentUser)`. Livewire `Component` subclasses cannot constructor-DI; method-DI on action methods is the locked pattern per ConnectBankStep.php:49-52 PHPDoc and CLAUDE.md.

    Replace the inline `Account::query()->create([...])` block (PreviewWizard.php:355-362) with a single line: `($ensurePaypal)($user, nameOverride: $trimmed, slugBodyOverride: $slugBody);`. The action's return value is unused here (PreviewWizard always wants the row to exist after this call; whether it was just created or already existed doesn't change the re-preview flow that follows).

    Keep the AccountNamer::validateName() guard (PreviewWizard.php:331-337) intact — when validation throws, the early return prevents the action call. The `$user = $currentUser->user();` line stays.

    Keep the subsequent `runFromUpload(...)` re-preview block (PreviewWizard.php:340-349) verbatim — it reads the just-created Account row via the stored `$this->importRunId` lookup.

    Remove the `use Modules\Ledger\Models\Account;` import ONLY IF no other PreviewWizard method still references `Account::query()` (search the file first — `saveIcsAccountName()` at line 259 and `saveBankAccountName()` likely still call `Account::query()->create(...)` for ICS / bank, so the import almost certainly stays). If the import stays, leave it untouched.

    Do not edit any other PreviewWizard method. Do not touch the `PAYPAL_OWN_IBAN = 'PAYPAL'` constant at line 67 — leave it as a redundant-but-harmless literal so any other internal PreviewWizard code that still references it keeps compiling.

    No PHPDoc edits narrating "extracted to EnsurePaypalAccountAction because…" — per CLAUDE.md docs-describe-current-state rule, PHPDocs reflect what the code does now, not history.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Import/tests/Feature/ --filter=PreviewWizard --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - savePaypalAccountName signature includes the action: `grep -c "EnsurePaypalAccountAction \\$ensurePaypal" Modules/Import/Internal/Http/Livewire/PreviewWizard.php` is at least 1
    - Inline create removed: `grep -c "'kind' => 'paypal'" Modules/Import/Internal/Http/Livewire/PreviewWizard.php` equals 0 (the only `kind: paypal` literal in this file moves to the action)
    - Action invocation present: `grep -c "(\\$ensurePaypal)(\\$user" Modules/Import/Internal/Http/Livewire/PreviewWizard.php` equals 1
    - Other Paypal-name-flow surfaces intact: `grep -c "AccountNamer::validateName" Modules/Import/Internal/Http/Livewire/PreviewWizard.php` is at least 1 (validation guard kept)
    - Existing PreviewWizard tests still pass: `vendor/bin/pest Modules/Import/tests/Feature/ --filter=PreviewWizard --no-coverage` exits 0
    - Larastan L10 strict clean on PreviewWizard: `vendor/bin/phpstan analyse --no-progress --memory-limit=2G Modules/Import/Internal/Http/Livewire/PreviewWizard.php` exits 0
    - Pint clean on PreviewWizard: `vendor/bin/pint --test Modules/Import/Internal/Http/Livewire/PreviewWizard.php` exits 0
    - No new GSD-leakage: `grep -E "\\.planning|PLAN\\.md|RESEARCH\\.md|D-[0-9]+" Modules/Import/Internal/Http/Livewire/PreviewWizard.php` returns no matches
  </acceptance_criteria>
  <done>
    PreviewWizard's PayPal-account branch now calls EnsurePaypalAccountAction with the user-typed name/slug overrides. Existing PreviewWizard tests still pass — no observable behavior change. The action is the single source of truth for synthetic-PayPal-account literals, ready for Plan 02 to wire it into the new wizard step.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → action | The action runs whatever User object the caller supplies; cross-user account leak is only prevented because the user_id scope is part of the exists() query AND the INSERT row |
| Livewire request → PreviewWizard | The user-typed `$this->paypalAccountName` reaches the action as `$nameOverride`; PHPStan-checked AccountNamer::validateName() throws before that on hostile input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.2.01-01 | Tampering | EnsurePaypalAccountAction | mitigate | user_id is scoped into BOTH the exists() check AND the INSERT row (mandatory in the action body) — Pest test `no-cross-user-leak` asserts two users get separate rows |
| T-16.1.2.01-02 | Information Disclosure | EnsurePaypalAccountAction | mitigate | action takes a User parameter, never derives the user from facade/global state — caller cannot accidentally create a row "for the wrong user" via session bleed |
| T-16.1.2.01-03 | Elevation of Privilege | PreviewWizard::savePaypalAccountName | accept | PreviewWizard is already authenticated-only (mounted inside the existing Onboarding/Import flow); the action delegation does not relax the existing access boundary |
| T-16.1.2.01-04 | Tampering | $nameOverride / $slugBodyOverride | mitigate | upstream AccountNamer::validateName() throws InvalidAccountNameException before the action call — only validated values reach the action body |
</threat_model>

<verification>
Run the full module test suite for Import to confirm no regression in adjacent PreviewWizard methods:

```bash
vendor/bin/pest Modules/Import/tests/Feature/ --no-coverage
vendor/bin/pest Modules/Import/tests/Feature/EnsurePaypalAccountActionTest.php --no-coverage
vendor/bin/phpstan analyse --no-progress --memory-limit=2G Modules/Import/Public/Actions/EnsurePaypalAccountAction.php Modules/Import/Internal/Http/Livewire/PreviewWizard.php
vendor/bin/pint --test Modules/Import/Public/Actions/EnsurePaypalAccountAction.php Modules/Import/Internal/Http/Livewire/PreviewWizard.php Modules/Import/tests/Feature/EnsurePaypalAccountActionTest.php
```
</verification>

<success_criteria>
- New `EnsurePaypalAccountAction` class shipped at `Modules/Import/Public/Actions/EnsurePaypalAccountAction.php`, idempotent, constructor-DI'd, PHPStan L10 strict clean
- Four passing Pest cases in `EnsurePaypalAccountActionTest.php` (creates-on-first-call, idempotent-second-call, honors-name-override, no-cross-user-leak)
- `PreviewWizard::savePaypalAccountName()` no longer contains the inline `Account::query()->create([...kind: 'paypal'...])` block — delegated to the action via method-DI
- All previously-passing PreviewWizard tests still pass — no observable behavior change
- Pint clean + Larastan L10 strict clean on both touched production files + the new test
</success_criteria>

<output>
Create `.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-01-SUMMARY.md` when done with: list of files created/modified, the count of new tests, and a one-line note that EnsurePaypalAccountAction is now ready for Plan 02's ConnectPaypalStep to wire via method-DI on submit().
</output>
