---
phase: 16.1.2
plan: 04
title: "FirstImportStep visual polish + body-block layout (sketch 006 winner B)"
slug: first-import-step-visual-polish
type: execute
wave: 4
depends_on:
  - 16.1.2-03
files_modified:
  - Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php
  - resources/css/app.css
autonomous: false
requirements: []

must_haves:
  truths:
    - "The FirstImportStep page renders each per-source preview inside a framed sub-card (border + radius + var(--color-bg-subtle) fill); the sub-cards read as carved out of the page wash"
    - "Empty-status sub-cards swap their fill from var(--color-bg-subtle) to var(--color-surface-2) (quiet, one-token difference)"
    - "After the last preview sub-card, a framed starting-balance block holds the balance cards in a 3-up CSS grid (grid-template-columns: repeat(3, 1fr); gap: 16px) — only when $detectedCount > 0"
    - "The 3-up balance grid collapses to 2-up at max-width 1024px and 1-up at max-width 720px"
    - "The .wiz-card--wide CSS class is unchanged (1120px max-width); the FirstImportStep page keeps using this locked class (NOT .wiz-card.wide)"
    - "The commit footer sits below the balance block, separated by a 1px top border, with the counter on the left + commit-btn-primary on the right"
    - "Every CSS rule uses only existing slate-50/100/200/700/900/950 + emerald/amber/rose/blue theme tokens — no new tokens added per D-17"
    - "D-14: A-3 (starting-balance section tidy) and A-4 (preview body-block layout) are delivered together as the implementation of the single sketch 006 winner B contract — both surfaces ship in one Blade+CSS pass, never independently"
    - "D-16: The sketch 006 winner B variant is the binding CSS contract for both starting-balance polish and preview body-block layout; the two surfaces are merged into one Blade+CSS plan because the winner uses a single composition (framed sub-card per source + framed starting-balance block sharing the same border/radius/fill primitives)"
  artifacts:
    - path: "Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php"
      provides: "Sketch 006 winner B layout: wiz-eyebrow/h1/lede unframed; per-source <section class=\"source-subcard\"> wrappers; framed .balance-section-subcard for balances; .commit-footer below"
      contains: "source-subcard"
    - path: "resources/css/app.css"
      provides: ".source-subcard, .source-subcard.empty, .balance-section-subcard, .starting-balance-stack, .starting-balance-lede, .commit-footer, .commit-counter, .commit-btn-primary rules"
      contains: ".source-subcard"
  key_links:
    - from: "first-import-step.blade.php"
      to: "resources/css/app.css .source-subcard rule"
      via: "<section class=\"source-subcard {{ $section->status === 'empty' ? 'empty' : '' }}\">"
      pattern: "source-subcard"
    - from: "first-import-step.blade.php"
      to: "resources/css/app.css .balance-section-subcard rule"
      via: "<section class=\"balance-section-subcard\">"
      pattern: "balance-section-subcard"
---

<objective>
Implement sketch 006 winner B for the FirstImportStep page per the sketch-findings-diederik skill's `first-import-review-step.md` reference. The change is presentation-only (CSS rules + Blade chrome) — the `<x-onboarding::consolidated-preview-section>` internals, the `<livewire:onboarding.starting-balance-card>` atom, the @php precomputation block at first-import-step.blade.php:62-95, and the Plan 03 "Load more (N remaining)" button all stay untouched. The visible delta: each preview section is wrapped in a `<section class="source-subcard">` that reads as carved out of the page wash; the starting-balance block is wrapped in a `<section class="balance-section-subcard">` with a 3-up CSS grid for the cards; the commit footer becomes a flex row with counter + primary CTA; empty-status sections quietly swap fill from `--color-bg-subtle` to `--color-surface-2`.

This plan merges A-3 (starting-balance section polish) and A-4 (body-block layout) into one CSS+Blade plan, as CONTEXT.md D-16 explicitly invites — sketch 006 winner B is a single composition contract spanning both surfaces and the two plans serialize on the same files (app.css + first-import-step.blade.php) anyway.

Purpose: UAT feedback after 16.1.1 flagged the live FirstImportStep as too "undifferentiated" — preview rows ran flush against the wizard card with no visual grouping between sources, and the full-width starting-balance stack made simpler "detected" cards look stranded next to the denser "conflict" state. Sketch 006 variant B resolves both with a single sub-card-per-source frame + 3-up balance grid contract using only existing theme tokens (D-17).

Output: One modified Blade view + one modified CSS file. No behavior change. No new theme tokens. No new atoms. The .wiz-card--wide locked class stays (verified by FirstImportStepWidthTest).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-CONTEXT.md
@.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-PATTERNS.md
@.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-03-SUMMARY.md
@CLAUDE.md
@.claude/skills/sketch-findings-diederik/SKILL.md
@.claude/skills/sketch-findings-diederik/references/first-import-review-step.md
@Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php
@Modules/Onboarding/Resources/views/livewire/starting-balance-card.blade.php
@Modules/Onboarding/Resources/views/components/consolidated-preview-section.blade.php
@resources/css/app.css

<interfaces>
<!-- Locked classes / tokens this plan consumes -->
Locked wide-card class (DO NOT RENAME): .wiz-card--wide (resources/css/app.css:664)
                                        — sketch uses .wiz-card.wide; live app uses .wiz-card--wide;
                                          per PATTERNS.md "Modifier class naming" + FirstImportStepWidthTest,
                                          we keep --wide. Any sketch CSS targeting .wiz-card.wide MUST be
                                          rewritten to .wiz-card--wide before paste.

Tokens to reuse (already declared in sources/themes/default.css / app.css):
  Surfaces: --color-surface, --color-bg-subtle, --color-surface-2
  Borders:  --color-border, --color-border-strong
  Text:     --color-text, --color-text-muted, --color-text-faint, --color-primary-text
  Brand:    --color-primary
  Radius:   --radius-lg, --radius-md, --radius-sm
  Type:     --text-xs, --text-sm, --text-md
  Motion:   --tx-quick

Class names introduced (no new tokens):
  .source-subcard, .source-subcard.empty
  .balance-section-subcard
  .starting-balance-stack, .starting-balance-lede
  .commit-footer, .commit-counter, .commit-btn-primary
</interfaces>

<existing_precomputation>
<!-- Already lives at first-import-step.blade.php:62-95 — keep verbatim, only the layout chrome changes -->
$hasAnyReadySection, $sourceCount, $balancesByAccount, $detectedCount,
$commitButtonLabel, $commitDisabled, $preview->dedupedTotalCount,
$preview->alreadyImportedCount
</existing_precomputation>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append sketch 006 CSS rules to resources/css/app.css</name>
  <files>resources/css/app.css</files>
  <read_first>
    - resources/css/app.css (lines 582-700 — wiz-page, wiz-card, .wiz-card--wide at line 664; lines 1316-1390 .balance-card atom; lines 1392-1521 .preview-section primitives; locate the existing .preview-section-load-more block added by Plan 03 — append the new sketch rules below it so all preview-side primitives stay grouped)
    - .claude/skills/sketch-findings-diederik/SKILL.md (auto-loads — calm-slate aesthetic contract)
    - .claude/skills/sketch-findings-diederik/references/first-import-review-step.md (lines 130-233 — the literal CSS to paste; lines 188-207 — responsive collapse breakpoints; lines 211-233 — commit footer)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-CONTEXT.md (D-14, D-16, D-17)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-PATTERNS.md (section "resources/css/app.css — A-3 + A-4 CSS plan (sketch 006 winner B)" — explicit "use .wiz-card--wide not .wiz-card.wide" caveat)
    - CLAUDE.md (calm-slate Linear/Notion contract; no GSD-leakage in CSS comments)
  </read_first>
  <action>
    Append the following CSS rules to `resources/css/app.css`, placed immediately after the `.preview-section-load-more` block that Plan 03 added (so all preview-side primitives stay grouped). Pull the rule text verbatim from `.claude/skills/sketch-findings-diederik/references/first-import-review-step.md` lines 130-233, with TWO mandatory transformations:

    1. Any sketch selector targeting `.wiz-card.wide` MUST be rewritten to `.wiz-card--wide` (the locked live-app class — see PATTERNS.md "Modifier class naming" note and FirstImportStepWidthTest).
    2. Any sketch selector that references a token NOT already declared in `app.css` MUST be replaced by an existing token. The contract is **zero new tokens** per CONTEXT.md D-17. Allowed tokens listed in the `<interfaces>` block above.

    Concrete rules to add (transformed):

    ```css
    /* Sub-card per source (sketch 006 variant B) */
    .source-subcard {
        border: 1px solid var(--color-border);
        border-radius: var(--radius-lg);
        background: var(--color-bg-subtle);
        padding: 22px 24px;
        transition: var(--tx-quick);
    }

    .source-subcard + .source-subcard {
        margin-top: 18px;
    }

    /* Empty-status fill swap — quiet one-token difference */
    .source-subcard.empty {
        background: var(--color-surface-2);
    }

    /* Inside the sub-card, table-row hover swaps to surface so the
       row stands out against the sub-card fill */
    .source-subcard .preview-section-table tbody tr:hover {
        background: var(--color-surface);
    }

    /* Starting-balance block (sketch 006 variant B) */
    .balance-section-subcard {
        margin-top: 28px;
        border: 1px solid var(--color-border);
        border-radius: var(--radius-lg);
        background: var(--color-bg-subtle);
        padding: 22px 24px;
    }

    .starting-balance-stack {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 16px;
        margin-top: 16px;
    }

    .starting-balance-lede {
        margin: 0;
        color: var(--color-text-muted);
        font-size: var(--text-sm);
        line-height: 1.5;
    }

    /* Responsive collapse: 3-up balance grid → 2-up → 1-up */
    @media (max-width: 1024px) {
        .starting-balance-stack {
            grid-template-columns: repeat(2, 1fr);
        }
    }

    @media (max-width: 720px) {
        .starting-balance-stack {
            grid-template-columns: 1fr;
        }
        .wiz-card--wide {
            padding: 28px 24px 24px;
        }
    }

    /* Commit footer */
    .commit-footer {
        margin-top: 28px;
        padding-top: 20px;
        border-top: 1px solid var(--color-border);
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 16px;
        flex-wrap: wrap;
    }

    .commit-counter {
        margin: 0;
        font-size: var(--text-sm);
        color: var(--color-text-muted);
    }

    .commit-counter strong {
        color: var(--color-text);
        font-weight: 600;
    }

    .commit-btn-primary {
        padding: 11px 22px;
        border-radius: var(--radius-md);
        background: var(--color-primary);
        color: var(--color-primary-text);
        border: 1px solid var(--color-primary);
        font-size: var(--text-md);
        font-weight: 500;
        cursor: pointer;
        transition: var(--tx-quick);
    }

    .commit-btn-primary:hover {
        opacity: 0.94;
    }

    .commit-btn-primary[disabled] {
        opacity: 0.4;
        cursor: not-allowed;
    }
    ```

    Use real `@media` queries (not Tailwind utilities) — the wizard chrome is the calm-slate semantic-CSS layer (sketch-findings-diederik skill). Do NOT add Tailwind utility classes. Do NOT introduce `@theme` blocks or new `--color-*`, `--radius-*`, `--text-*`, `--tx-*` variables. Do NOT touch any unrelated CSS block.

    Per CLAUDE.md docs-describe-current-state: CSS comments describe what the rule does ("Sub-card per source"), NOT history ("added in Phase 16.1.2"). No `.planning/` / D-NN references in comments.
  </action>
  <verify>
    <automated>grep -c "^\\.source-subcard\\b" resources/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    - All seven new top-level selectors present: `grep -cE "^\\.source-subcard\\b|^\\.balance-section-subcard\\b|^\\.starting-balance-stack\\b|^\\.starting-balance-lede\\b|^\\.commit-footer\\b|^\\.commit-counter\\b|^\\.commit-btn-primary\\b" resources/css/app.css` is at least 7
    - Empty-status fill swap rule present: `grep -c "^\\.source-subcard.empty\\b" resources/css/app.css` is at least 1
    - Locked wide-card class preserved (NOT renamed): `grep -c "\\.wiz-card--wide" resources/css/app.css` is at least 2 (existing block at line 664 + new responsive override) AND `grep -c "^\\.wiz-card\\.wide\\b" resources/css/app.css` equals 0 (no accidental sketch-form selector)
    - Responsive breakpoints added at 1024px + 720px: `grep -c "@media (max-width: 1024px)" resources/css/app.css` is at least 1 AND `grep -c "@media (max-width: 720px)" resources/css/app.css` is at least 1
    - Existing .preview-section-load-more rule (from Plan 03) still present: `grep -c "^\\.preview-section-load-more\\b" resources/css/app.css` is at least 1
    - No new theme tokens DECLARATIONS introduced. A token declaration is a `+` diff line whose first non-whitespace content is `--<namespace>-...` followed by a `:` (distinguishes a `--color-x: value;` declaration from a `var(--color-x)` reference inside a value): `git diff resources/css/app.css | grep -cE '^\\+\\s+--(color|radius|text|tx|space)-[a-zA-Z0-9_-]+:'` equals 0
    - Pint clean (no formatter for CSS; rely on file write being syntactically valid; sanity check via Tailwind/Vite build): `grep -v '^#' resources/css/app.css | grep -c '{ *}'` returns no empty blocks (orphaned selectors)
    - No GSD-leakage in CSS comments: `grep -E "\\.planning|PLAN\\.md|RESEARCH\\.md|D-[0-9]+" resources/css/app.css` returns no matches
    - File still parses as CSS (syntactic validity): `php -r "echo file_exists('resources/css/app.css') ? 'ok' : 'missing';"` outputs "ok" AND `grep -c "^}" resources/css/app.css` is at least matching `grep -c "{$" resources/css/app.css` (brace balance sanity check)
  </acceptance_criteria>
  <done>
    Sketch 006 CSS rules appended to app.css. .wiz-card--wide locked class preserved verbatim. No new theme tokens added. The Blade view (next task) can reference the new classes with confidence.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite first-import-step.blade.php layout chrome to use the new sub-card frames</name>
  <files>Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php</files>
  <read_first>
    - Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php (FULL FILE — read every line; the @php precomputation at lines 62-95 stays verbatim, only the layout chrome changes)
    - Modules/Onboarding/Resources/views/livewire/starting-balance-card.blade.php (246 LOC — confirm the .balance-card atom shape stays; no structural changes per PATTERNS.md "starting-balance-card.blade.php — A-3 polish" — the 3-up grid is added at the container level only)
    - Modules/Onboarding/Resources/views/components/consolidated-preview-section.blade.php (confirm the component shape after Plan 03's footer swap — wraps fine inside <section class="source-subcard">)
    - .claude/skills/sketch-findings-diederik/SKILL.md (auto-loads)
    - .claude/skills/sketch-findings-diederik/references/first-import-review-step.md (lines 237-287 — the literal HTML skeleton to emit; lines 290-298 — per-source sub-card explanation; lines 301-335 — "What to avoid")
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-CONTEXT.md (D-14, D-16, D-17)
    - .planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-PATTERNS.md (section "first-import-step.blade.php — A-4 layout rewrite (sketch 006 winner B)")
    - CLAUDE.md
  </read_first>
  <action>
    Rewrite the LAYOUT CHROME of `Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` to match sketch 006 winner B per `first-import-review-step.md:237-287`. Concrete edits:

    1. KEEP VERBATIM:
       - The Blade view file's existing `@php ... @endphp` precomputation block (currently around lines 62-95) — `$hasAnyReadySection`, `$sourceCount`, `$balancesByAccount`, `$detectedCount`, `$commitButtonLabel`, `$commitDisabled` all stay as-is. Only the markup that consumes them changes.
       - The outer `<x-onboarding::wiz-card :wide="true">` wrapper — the wiz-card atom already maps `:wide="true"` to the `.wiz-card--wide` class (verified by FirstImportStepWidthTest). DO NOT change to `:wide` prop variants or hard-code `class="wiz-card wide"`.
       - The wiz-eyebrow / wiz-h1 / wiz-lede block — per "What to avoid" in the skill, the eyebrow/H1/lede hierarchy lives at the wizard-card level, NOT inside a sub-card.
       - The `@error('commitError')` / `$commitError` inline error band (if present) — keep wherever it currently sits in the layout.
       - The `<livewire:onboarding.starting-balance-card :key="..." ... />` invocations — only the container wrapper around the foreach changes.

    2. CHANGE — wrap each preview section in a `<section class="source-subcard">`:

       Replace the existing foreach over `$preview->sections` (which currently renders `<x-onboarding::consolidated-preview-section :section="$section" />` flush against the wiz-card) with:

       ```blade
       @foreach ($preview->sections as $section)
           <section class="source-subcard {{ $section->status === 'empty' ? 'empty' : '' }}">
               <x-onboarding::consolidated-preview-section :section="$section" />
           </section>
       @endforeach
       ```

       The conditional `empty` modifier triggers the fill swap (--color-surface-2) on empty-status sections per sketch 006.

    3. CHANGE — wrap the balances block in a `<section class="balance-section-subcard">` with a `.starting-balance-stack` grid:

       Replace the existing `.starting-balance-section` / `.starting-balance-stack` wrapper (currently around lines 110-150) with the sketch's framed-block structure:

       ```blade
       @if ($detectedCount > 0)
           <section class="balance-section-subcard">
               <p class="preview-section-eyebrow">
                   🧮 <span class="preview-section-label">STARTING BALANCES</span>
                   <span class="preview-section-count">· {{ $detectedCount }} {{ $detectedCount === 1 ? 'ACCOUNT DETECTED' : 'ACCOUNTS DETECTED' }}</span>
               </p>
               <p class="starting-balance-lede">
                   We detected the starting balance for each account. Confirm or edit before we commit.
               </p>
               <div class="starting-balance-stack">
                   @foreach ($balancesByAccount as $accountId => $candidates)
                       {{-- Keep the existing livewire:onboarding.starting-balance-card invocation verbatim, including all current props (account, candidates, balanceConfirmations binding, etc.) --}}
                       <livewire:onboarding.starting-balance-card :key="'sb-'.$accountId" {{-- ...existing props... --}} />
                   @endforeach
               </div>
           </section>
       @endif
       ```

       READ THE CURRENT BALANCE-CARD INVOCATION ON DISK and copy ALL its existing props into the new structure verbatim — the only structural change is the `<section class="balance-section-subcard">` wrapper, the `.preview-section-eyebrow` + `.starting-balance-lede` block, and the `<div class="starting-balance-stack">` grid container around the foreach. Do NOT touch the starting-balance-card component itself or its props.

    4. CHANGE — replace the existing commit footer with the flat `.commit-footer` structure:

       Replace any current `<x-onboarding::wiz-actions>` slot on this page (the page-level commit affordance, NOT the per-step skip/continue) with:

       ```blade
       <div class="commit-footer">
           <p class="commit-counter" role="status" aria-live="polite">
               <strong class="tnum">{{ $preview->dedupedTotalCount }}</strong>
               {{ $preview->dedupedTotalCount === 1 ? 'transaction' : 'transactions' }} to commit ·
               <span class="tnum">{{ $preview->alreadyImportedCount }}</span> already imported
           </p>
           <button type="button" class="commit-btn-primary" wire:click="commit"
               @if ($commitDisabled) aria-disabled="true" disabled @endif>
               {{ $commitButtonLabel }}
           </button>
       </div>
       ```

       Use Blade auto-escape on every interpolation (`{{ }}` only — no `{!! !!}` anywhere). The `role="status"` + `aria-live="polite"` on the counter come from the sketch HTML for accessibility (the count updates live as user expands sections via Plan 03's Load-more).

    5. PRESERVE the FirstImportStep wizard chrome — wiz-page wrapper, top progress dots, footer privacy pill, "Resume later" — unchanged per sketch 006 "Removing the wizard chrome around FirstImportStep" anti-pattern in the skill.

    6. Per CLAUDE.md docs-describe-current-state: do NOT add Blade comments like `{{-- new layout per sketch 006 winner B --}}` or `{{-- D-14 sketch contract --}}`. The structure speaks for itself.

    7. Per CLAUDE.md fix-all-severities — if Pint flags anything in the touched Blade file, fix it.

    Edge cases to verify by inspection (NOT by adding code):
    - When `$preview->sections` is empty, the @foreach produces zero sub-cards — handled implicitly.
    - When `$detectedCount === 0`, the entire balance block is absent — `@if ($detectedCount > 0)` guard.
    - When `$sourceCount === 1`, the wiz-lede singular/plural shape uses the existing `{{ $sourceCount === 1 ? 'source' : 'sources' }}` ternary (preserve verbatim from the existing file).
    - The .empty-status fill swap is conditional on `$section->status === 'empty'` — confirm the ConsolidatedPreviewSection DTO's `status` field accepts this literal (it does — see Plan 03's tests).
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php Modules/Onboarding/tests/Feature/StartingBalanceConfirmCardsTest.php Modules/Onboarding/tests/Feature/FirstImportStepLoadMoreTest.php --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Sub-card wrapper applied to each preview section: `grep -c "<section class=\"source-subcard" Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` is at least 1
    - Empty-status modifier present (conditional): `grep -c "source-subcard.*'empty'" Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` is at least 1
    - Balance block frame present (conditional on $detectedCount > 0): `grep -c "<section class=\"balance-section-subcard\"" Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` equals 1
    - 3-up grid container present: `grep -c "class=\"starting-balance-stack\"" Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` equals 1
    - Commit footer added: `grep -c "class=\"commit-footer\"" Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` equals 1 AND `grep -c "class=\"commit-btn-primary\"" Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` equals 1
    - Locked .wiz-card--wide preserved (via :wide="true" prop): `grep -c ':wide="true"\\|:wide ' Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` is at least 1
    - Blade auto-escape only (no unescape): `grep -c "{!!" Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` equals 0
    - The eyebrow/h1/lede block is NOT framed in a sub-card (anti-pattern guard). The first line in the file matching EITHER `wiz-eyebrow` OR `source-subcard` MUST contain `wiz-eyebrow`. This deterministically asserts that no `source-subcard` marker appears before the first `wiz-eyebrow`. Exit code 0 → eyebrow precedes the first sub-card (passes); exit code 1 → a sub-card appears before any eyebrow (fails): `grep -n 'wiz-eyebrow\\|source-subcard' Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php | head -1 | grep -q 'wiz-eyebrow'`
    - FirstImportStepWidthTest still passes (the locked .wiz-card--wide assertion): `vendor/bin/pest Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php --no-coverage` exits 0
    - ConsolidatedPreviewLoadTest still passes: `vendor/bin/pest Modules/Onboarding/tests/Feature/ConsolidatedPreviewLoadTest.php --no-coverage` exits 0
    - StartingBalanceConfirmCardsTest still passes (no balance-card behavior change): `vendor/bin/pest Modules/Onboarding/tests/Feature/StartingBalanceConfirmCardsTest.php --no-coverage` exits 0
    - FirstImportStepLoadMoreTest from Plan 03 still passes: `vendor/bin/pest Modules/Onboarding/tests/Feature/FirstImportStepLoadMoreTest.php --no-coverage` exits 0
    - Pint clean: `vendor/bin/pint --test Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` exits 0
    - No GSD-leakage in Blade comments: `grep -E "\\.planning|PLAN\\.md|RESEARCH\\.md|D-[0-9]+" Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php` returns no matches
  </acceptance_criteria>
  <done>
    The FirstImportStep page renders per sketch 006 winner B: framed sub-card per source on `--color-bg-subtle` page wash, framed starting-balance block below the previews holding cards in a 3-up grid, flat commit footer below. Empty-status sections quietly swap fill to `--color-surface-2`. The .wiz-card--wide locked width contract is unchanged (FirstImportStepWidthTest stays green). No behavior changes — purely a visual polish.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — sketch 006 winner B rendered correctly</name>
  <files>Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php, resources/css/app.css</files>
  <action>Pause execution. Surface the verification steps below to the user and wait for an "approved" response (or a described defect) before continuing to Plan 05. No automated change is performed here — Tasks 1 + 2 already shipped the CSS + Blade; this task verifies they render correctly across light/dark + responsive breakpoints.</action>
  <verify>Human approval via the resume-signal below; the automated FirstImportStepWidthTest run in step 6 of how-to-verify serves as the last machine-level regression gate before the user signs off.</verify>
  <done>User responded "approved" (layout matches sketch 006 winner B) OR the executor captured the user's defect description and Task 1/Task 2 were re-opened to address it.</done>
  <what-built>
    Sketch 006 winner B layout for FirstImportStep:
    - Framed sub-card per source on bg-subtle page wash
    - Empty-status sections swap to surface-2 fill
    - Framed starting-balance block below previews with 3-up CSS grid
    - Responsive collapse 3-up → 2-up at 1024px → 1-up at 720px
    - Flat commit footer with counter + primary CTA, 1px top border separator
    - .wiz-card--wide locked class preserved (1120px max-width)
  </what-built>
  <how-to-verify>
    1. Make sure Herd is serving the app: `open https://diederik.test/setup`
    2. Sign up (or log in) and walk the wizard until you reach FirstImportStep with at least one source loaded — easiest path:
       - Connect Bank: drop the sample ASN CAMT.053 from `Modules/Ingestion/tests/fixtures/asn/asn-camt053-sample.xml` (or any locally-cached fixture)
       - Skip Connect PayPal
       - Skip Connect Card
       - Skip Connect Email
    3. On the FirstImportStep page, confirm visually:
       a. Each preview section renders inside its own framed sub-card with a subtle border + `--color-bg-subtle` background (calm slate)
       b. If any section has status=empty, its sub-card uses `--color-surface-2` instead (slightly different fill)
       c. The eyebrow + H1 + lede at the top are NOT framed — they sit directly on the wiz-card surface
       d. The starting-balance block (if `$detectedCount > 0`) sits below the last preview sub-card with the same frame treatment, containing balance cards laid out in a 3-up grid
       e. The commit footer at the bottom has a 1px top border, the counter line on the left, and the "Commit everything (N transactions) →" button on the right
       f. The wizard chrome (top progress dots, footer privacy pill) is unchanged
    4. Resize the window:
       a. Narrow below 1024px → balance grid drops to 2-up
       b. Narrow below 720px → balance grid drops to 1-up + the wiz-card padding tightens
    5. Confirm dark mode still works: toggle Settings → Theme → Dark and reload /setup → all four primitives (sub-card border, sub-card fill, balance frame, commit button) render correctly against the dark page wash
    6. Run the FirstImportStepWidthTest as a final regression check: `vendor/bin/pest Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php --no-coverage` (must exit 0)
  </how-to-verify>
  <resume-signal>Type "approved" if the layout matches sketch 006 winner B, or describe what doesn't render right (e.g. "balance grid not collapsing at 720px" or "empty sections still showing bg-subtle").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blade-rendered cells | $section properties + $preview->dedupedTotalCount + $preview->alreadyImportedCount + $commitButtonLabel all flow through `{{ }}` Blade auto-escape |
| (none new) | Plan 04 is presentation-only — no new auth boundaries, no new persistence, no new external services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.2.04-01 | Tampering | Blade interpolations in first-import-step.blade.php | mitigate | every interpolation uses `{{ }}` Blade auto-escape; `{!!` count == 0 enforced in acceptance criteria |
| T-16.1.2.04-02 | Information Disclosure | preview cell rendering | accept | the cell content already flows through ConsolidatedPreviewSection DTO populated from PreviewCache; no new data path introduced by Plan 04 |
| T-16.1.2.04-03 | Tampering | $section->status used in class attribute | mitigate | status is a DTO enum-like literal ('ready', 'empty', 'filtered', 'error') sourced from BuildConsolidatedPreviewQuery; concatenated into a class string via the ternary `$section->status === 'empty' ? 'empty' : ''` — only the literal 'empty' ever reaches the class attribute, ruling out class-injection from arbitrary values |
| T-16.1.2.04-04 | Denial of Service (CSS) | $detectedCount with large value rendering 100 grid cells | accept | $detectedCount caps at the user's account count (single-user local app, typically ≤5 accounts); 3-up grid degrades gracefully via auto-flow |
</threat_model>

<verification>
End-to-end manual smoke is captured in Task 3 (checkpoint). Automated regression:

```bash
vendor/bin/pest Modules/Onboarding/tests/Feature/ --no-coverage
vendor/bin/pint --test Modules/Onboarding/Resources/views/livewire/steps/first-import-step.blade.php resources/css/app.css
# CSS sanity: no orphaned selectors, balanced braces
grep -v '^#' resources/css/app.css | grep -cE '\\{$' | head -1
grep -c '^}' resources/css/app.css | head -1
```
</verification>

<success_criteria>
- resources/css/app.css has the .source-subcard, .source-subcard.empty, .balance-section-subcard, .starting-balance-stack, .starting-balance-lede, .commit-footer, .commit-counter, .commit-btn-primary rules + the @media (max-width: 1024px) and (max-width: 720px) responsive blocks
- The locked .wiz-card--wide class is preserved (FirstImportStepWidthTest stays green)
- No new theme tokens added — git diff shows zero new --color/--radius/--text/--tx/--space declarations
- first-import-step.blade.php wraps each preview section in `<section class="source-subcard">`, wraps the balance block in `<section class="balance-section-subcard">`, places balance cards in a `<div class="starting-balance-stack">` grid, and ends with the flat `<div class="commit-footer">` containing the counter + commit button
- The eyebrow/h1/lede block is NOT framed in a sub-card (anti-pattern guard)
- All FirstImportStep tests still green: FirstImportStepWidthTest, ConsolidatedPreviewLoadTest, StartingBalanceConfirmCardsTest, FirstImportStepLoadMoreTest (Plan 03)
- Pint clean on both files; no GSD-leakage in either
- Human verification (Task 3 checkpoint) confirms the layout matches sketch 006 winner B at desktop + 1024px + 720px breakpoints in both light and dark mode
</success_criteria>

<output>
Create `.planning/phases/16.1.2-first-run-wizard-polish-paypal-connector-step-preview-pagina/16.1.2-04-SUMMARY.md` with: files modified, the CSS rule count added (10 selectors + 2 media queries), the human-verification result captured at the checkpoint, and a one-line note that resources/css/app.css and first-import-step.blade.php are now stable for downstream Plan 05 (regression tests).
</output>
