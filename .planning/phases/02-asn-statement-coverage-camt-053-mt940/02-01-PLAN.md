---
phase: 02-asn-statement-coverage-camt-053-mt940
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - composer.json
  - composer.lock
  - tests/Pest.php
  - tests/fixtures/asn-camt053-sample-1.xml
  - tests/fixtures/asn-camt053-sample-1.md
  - tests/fixtures/asn-mt940-sample-1.sta
  - tests/fixtures/asn-mt940-sample-1.md
  - tests/fixtures/asn-cross-format/february.csv
  - tests/fixtures/asn-cross-format/february.camt053.xml
  - tests/fixtures/asn-cross-format/february.mt940.sta
  - tests/fixtures/asn-cross-format/README.md
autonomous: false
requirements:
  - ING-02
  - ING-03
  - ING-06
tags:
  - wave-0
  - fixtures
  - composer
  - test-infrastructure
user_setup:
  - service: anonymised-asn-fixtures
    why: "Adapter snapshot tests and cross-format dedup tests need real ASN exports (CAMT.053 + MT940) the user must download from the ASN portal and hand to Claude for anonymisation."
    dashboard_config:
      - task: "Download a single-statement CAMT.053 XML export (preferably 001.08 sub-version) and a single-statement MT940 .sta export from ASN online banking"
        location: "ASN Online Bankieren -> Rekeningoverzicht -> Download als"
      - task: "Download a second CAMT.053 export AND the matching CSV export for the SAME statement period (used for cross-format dedup test). Optionally also the MT940 for that same period."
        location: "ASN Online Bankieren -> Rekeningoverzicht -> Download als (CSV + CAMT.053 + MT940 for same date range)"
      - task: "Hand the four/five raw files to Claude. Claude will anonymise counterparty names + counterparty IBANs while keeping balances, dates, amounts, and own-IBAN structurally real (same protocol as tests/fixtures/asn-sample-1.md)."
        location: "Drop into a working directory on the local machine and instruct Claude to anonymise"

must_haves:
  truths:
    - "User has handed over real ASN CAMT.053 + MT940 export samples for fixture seeding"
    - "Anonymised CAMT.053 + MT940 fixtures + their audit markdown files exist under tests/fixtures/"
    - "A cross-format fixture trio (same period CSV + CAMT.053 + MT940) exists under tests/fixtures/asn-cross-format/"
    - "genkgo/camt ^2.10 is installed in vendor/ and listed in composer.json require"
    - "tests/Pest.php registers the phase-2 group so vendor/bin/pest --group=phase-2 filters cleanly"
    - "Larastan still passes at level 10 strict after the new dependency is added"
  artifacts:
    - path: "tests/fixtures/asn-camt053-sample-1.xml"
      provides: "Anonymised single-statement CAMT.053 XML (V08 preferred) used by AsnCamt053AdapterTest snapshot"
      min_lines: 30
    - path: "tests/fixtures/asn-camt053-sample-1.md"
      provides: "Anonymisation audit + structural notes (mirrors asn-sample-1.md format)"
      min_lines: 20
    - path: "tests/fixtures/asn-mt940-sample-1.sta"
      provides: "Anonymised single-statement MT940 with at least one SEPA direct debit + one bank transfer + one fee/charge"
      min_lines: 10
    - path: "tests/fixtures/asn-mt940-sample-1.md"
      provides: "Anonymisation audit for MT940 fixture"
      min_lines: 20
    - path: "tests/fixtures/asn-cross-format/february.csv"
      provides: "Same-period CSV used in CrossFormatDedupTest::csv_then_camt053"
    - path: "tests/fixtures/asn-cross-format/february.camt053.xml"
      provides: "Same-period CAMT.053 covering the SAME real-world transactions as february.csv"
    - path: "tests/fixtures/asn-cross-format/README.md"
      provides: "Documents the cross-format pair: same period, same real-world transactions, same own IBAN"
    - path: "composer.json"
      provides: "Adds genkgo/camt:^2.10 to the require block"
      contains: "genkgo/camt"
    - path: "composer.lock"
      provides: "Lockfile pinning the genkgo/camt version actually installed"
      contains: "genkgo/camt"
    - path: "tests/Pest.php"
      provides: "Registers phase-2 Pest group so --group=phase-2 filters work"
      contains: "phase-2"
  key_links:
    - from: "tests/fixtures/asn-cross-format/february.csv"
      to: "tests/fixtures/asn-cross-format/february.camt053.xml"
      via: "Both files cover the same real-world transactions, same own IBAN, same period — required for CrossFormatDedupTest"
      pattern: "Cross-format fixture pair"
    - from: "composer.json"
      to: "vendor/genkgo/camt/"
      via: "composer install"
      pattern: "genkgo/camt"
---

<objective>
Land the Wave 0 enablement work for Phase 2: install the `genkgo/camt` library, commit anonymised real ASN CAMT.053 + MT940 fixtures (plus the cross-format fixture trio), and register the `@group phase-2` Pest group. Without these artefacts the downstream waves cannot snapshot-test, cross-format-dedup-test, or run the v3 fingerprint command's collision pre-check against realistic data.

Purpose: every Wave 1+ plan in this phase depends on at least one of (a) the `genkgo/camt` typed API, (b) a real anonymised CAMT/MT940 fixture, or (c) the ability to scope Pest runs to `--group=phase-2`. Shipping these in a dedicated Wave 0 plan keeps the slice plans deterministic and lets the user hand-deliver the fixture corpus before any adapter code lands.

Output: an installed `genkgo/camt` dependency, an anonymised fixture corpus committed under `tests/fixtures/`, a registered Pest group, and an unchanged green test suite + Larastan baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-asn-statement-coverage-camt-053-mt940/02-CONTEXT.md
@.planning/phases/02-asn-statement-coverage-camt-053-mt940/02-RESEARCH.md
@.planning/phases/02-asn-statement-coverage-camt-053-mt940/02-PATTERNS.md
@.planning/phases/02-asn-statement-coverage-camt-053-mt940/02-VALIDATION.md
@CLAUDE.md
@tests/fixtures/asn-sample-1.csv
@tests/fixtures/asn-sample-1.md
@composer.json
@tests/Pest.php

<interfaces>
<!-- The contract every adapter test consumes — names and shape of canonical anonymisation rules already locked by Phase 1. -->

From tests/fixtures/asn-sample-1.md (canonical anonymisation protocol, applied verbatim to the new fixtures):
- Counterparty NAMES → replaced with public-domain merchant names (e.g. STARBUCKS AMSTERDAM, ALBERT HEIJN 1234, SHELL ROTTERDAM)
- Counterparty IBANs → replaced with synthetic-but-checksum-valid IBANs in the same country code as the original
- Own IBAN → pinned to a single fixed test IBAN: NL00ASNB0123456789 (re-use the value already used in asn-sample-1.csv)
- Balances, dates, amounts, currencies, and ALL SEPA reference IDs (EndToEndId, AcctSvcrRef, MsgId) → kept structurally real (they carry no PII)
- Description / remittance text → reviewed: replace any personally-identifying free text with a generic equivalent ("Invoice 2025-04 Spotify" stays, "Birthday gift Wessel" becomes "Personal transfer")

From composer.json (current require block — extend, do not replace):
```json
"require": {
    "php": "^8.5",
    "laravel/framework": "^13.0",
    "laravel/fortify": "^1.21",
    "livewire/livewire": "^4.0",
    "livewire/flux": "^2.0",
    "nwidart/laravel-modules": "^13.0",
    "brick/money": "^0.11",
    "league/csv": "^9.28",
    "spatie/laravel-data": "^4.0"
}
```
After this plan: `"genkgo/camt": "^2.10"` added (alphabetical position between `brick/money` and `laravel/fortify`).

From tests/Pest.php (current bootstrap shape — extend at the bottom, after the existing per-module `pest()->extend(...)` foreach):
The file currently has zero `addGroups` / `group()` registrations. Phase 2 adds a phase-level group registration so `vendor/bin/pest --group=phase-2` filters to tests that opt in via `->group('phase-2')` on each `it(...)` call. Per RESEARCH.md VALIDATION quick-run command on line 22 of 02-VALIDATION.md: `vendor/bin/pest --group=phase-2 --bail`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Receive anonymised ASN fixture corpus from user</name>
  <what-built>
    Nothing has been built yet. This checkpoint blocks Wave 0 until the user has handed over real (or anonymised) ASN export samples that Claude can normalise into the fixture corpus required by Phase 2's adapter + cross-format tests.
  </what-built>
  <how-to-verify>
    The user must provide the following four raw files (paths anywhere on the local machine — Claude will then copy + anonymise into `tests/fixtures/`):

    1. **Single-statement CAMT.053 XML export** from ASN. Preferred sub-version: `001.08` (most common 2026 export). At minimum 5–10 statement entries, including at least one SEPA direct debit (Bank Transaction Code family `DDBT`) and one bank transfer (`RCDT` / `ICDT`). File extension: `.xml`.

    2. **Single-statement MT940 `.sta` export** from ASN covering an older statement period (Phase 2 D-25 frames MT940 as "legacy fallback"). At minimum 5–10 `:61:` entries with corresponding `:86:` narrative fields, including at least one row with a SEPA `EREF` GVC keyword inside the `:86:` text, and one row with no `EREF`.

    3. **Same-period CSV export** AND **same-period CAMT.053 export** for one full statement month (the cross-format pair). These two files MUST cover the exact same calendar period and the exact same real-world transactions, downloaded from ASN within a few minutes of each other. This is the data backbone of `CrossFormatDedupTest::csv_then_camt053`.

    4. **(Optional but strongly recommended)** Same-period MT940 export covering the same month as #3. Used for `CrossFormatDedupTest::mt940_after_csv` and `mt940_then_camt053` scenarios. If the user cannot produce this, document the gap in `tests/fixtures/asn-cross-format/README.md` and skip the MT940-side cross-format scenarios (the CAMT side still ships).

    Hand the raw files to Claude with their absolute paths (or drop them in a temp directory and tell Claude where). Claude will:
    - Validate each file parses with the chosen library before anonymising (genkgo/camt for CAMT, a quick `grep '^:61:'` for MT940)
    - Apply the Phase 1 anonymisation protocol from `tests/fixtures/asn-sample-1.md` verbatim (counterparty names → public-domain merchants, counterparty IBANs → synthetic-checksum-valid, own IBAN → fixed test value `NL00ASNB0123456789`, balances/dates/amounts/SEPA refs preserved)
    - Commit the anonymised files into `tests/fixtures/` and `tests/fixtures/asn-cross-format/`
  </how-to-verify>
  <resume-signal>
    Type "approved" with the absolute paths of the four/five raw files. Example:
    ```
    approved
    /Users/wessel/Downloads/asn-camt053-april.xml
    /Users/wessel/Downloads/asn-mt940-january.sta
    /Users/wessel/Downloads/asn-cross-feb.csv
    /Users/wessel/Downloads/asn-cross-feb.camt053.xml
    /Users/wessel/Downloads/asn-cross-feb.mt940.sta  (optional)
    ```
    OR type "fixtures-blocked: <reason>" to abort this plan with a documented gap.
  </resume-signal>
  <files>tests/fixtures/ (user-supplied raw files — paths handed in via resume-signal)</files>
  <action>Receive the absolute paths of the user-delivered raw ASN export files via the resume-signal payload. Validate each path exists and is readable. Confirm the CAMT.053 file parses with simplexml_load_file; confirm the MT940 file has at least one :61: tag via grep. Record the raw paths in this plan SUMMARY so Task 3 can reference them. NO files are written in this task — anonymisation happens in Task 3.</action>
  <verify>
    <automated>echo "Manual checkpoint — resume-signal carries file paths; no automated gate."</automated>
  </verify>
  <done>User has handed over four (or five with optional MT940 cross-format) raw ASN export file paths via the resume-signal. Paths verified as readable. Plan resumes to Task 2 (independent, can run in parallel) and queues Task 3 for anonymisation.</done>
</task>

<task type="auto">
  <name>Task 2: Install genkgo/camt and register the phase-2 Pest group</name>
  <files>composer.json, composer.lock, tests/Pest.php, phpunit.xml</files>
  <read_first>
    - composer.json (current require block — append `genkgo/camt: ^2.10`)
    - tests/Pest.php (current bootstrap — append the phase-2 group registration AFTER the existing per-module foreach)
    - phpunit.xml (read to confirm Pest's group filter wiring — `<phpunit>` element typically has `<groups>` or relies on Pest's native CLI flag; per 02-VALIDATION.md the quick-run command is `vendor/bin/pest --group=phase-2 --bail`)
    - .planning/phases/02-asn-statement-coverage-camt-053-mt940/02-RESEARCH.md §"Standard Stack" (line ~99) and §"Library / API Surface" (line ~129) confirm `genkgo/camt 2.10.3` requires `php ^8.1`, `ext-dom`, `ext-libxml`, `ext-simplexml` — verify these are present
    - .planning/phases/02-asn-statement-coverage-camt-053-mt940/02-PATTERNS.md §"Critical Discovery Items for the Planner" item 5 (line ~1161): `vendor/genkgo/` does not exist — `composer install` must run after the manifest change
  </read_first>
  <action>
    Implements **D-26** (composer require genkgo/camt ^2.10) and the Wave 0 group-registration prerequisite.

    Step 1 — Add `genkgo/camt` to `composer.json` `require` block. Per CLAUDE.md the version pin is `^2.10`. Place the new line alphabetically (after `brick/money`, before `laravel/fortify`). DO NOT use `composer require` from the CLI for the manifest edit — edit `composer.json` directly so the diff is reviewable and Pint does not reformat the entire file. Then run `composer install --no-interaction` to populate `composer.lock` and `vendor/genkgo/`.

    Concrete diff to apply to `composer.json` "require":
    ```json
        "brick/money": "^0.11",
        "genkgo/camt": "^2.10",
        "laravel/fortify": "^1.21",
    ```

    Step 2 — Verify the install succeeded and the library type surface is reachable: run `vendor/bin/phpstan analyse --level=max Modules/Ingestion --no-progress` (the existing baseline must remain green — no new errors from the genkgo/camt typing). If Larastan complains about missing genkgo types, regenerate the baseline with `vendor/bin/phpstan analyse --level=max --generate-baseline` and commit the updated baseline file (the path is at the repo root — `phpstan-baseline.neon` or `phpstan.neon` — check first with `ls *.neon`).

    Step 3 — Register the phase-2 Pest group. Append the following block at the BOTTOM of `tests/Pest.php`, after the closing brace of the existing `foreach` loop:

    ```php
    /*
     * Phase 2 (ASN CAMT.053 + MT940) groups every test it introduces under
     * the `phase-2` group so the dev loop can run a focused subset via
     *     vendor/bin/pest --group=phase-2 --bail
     * (see .planning/phases/02-asn-statement-coverage-camt-053-mt940/02-VALIDATION.md).
     *
     * Tests opt in by chaining `->group('phase-2')` on each `it(...)` call.
     */
    ```

    Per CLAUDE.md "feedback_codebase_gsd_agnostic" — DO NOT cite `.planning/`, `PLAN.md`, `RESEARCH.md`, or `CONTEXT.md` in any code, PHPDoc, or comment. The comment above mentions `.planning/phases/.../02-VALIDATION.md` — STRIP that reference before committing. Final comment text:

    ```php
    /*
     * Phase 2 tests opt in to the `phase-2` group by chaining
     *     ->group('phase-2')
     * on each `it(...)` call, so the focused dev loop can run:
     *     vendor/bin/pest --group=phase-2 --bail
     */
    ```

    Step 4 — Confirm `phpunit.xml` does not need editing for group filtering. Pest 4 + PHPUnit 11 group filtering works via the CLI `--group=` flag and the test-level `->group('name')` chain; no XML configuration is required. Read `phpunit.xml` to verify there is no `<groups><exclude>phase-2</exclude></groups>` block that would silently skip our tests. If such a block exists, fail loudly — do not silently edit.

    Step 5 — Run the full test suite to confirm zero regression. The phase-2 group is empty at this point (no test has chained `->group('phase-2')` yet) so the group-filter run will pass with "No tests executed" — that is the expected and correct outcome at the end of Wave 0 Task 2.
  </action>
  <verify>
    <automated>composer show genkgo/camt | grep -E "versions : \* 2\." && grep -q '"genkgo/camt": "\^2\.10"' composer.json && grep -q "phase-2" tests/Pest.php && vendor/bin/pest --group=phase-2 --bail 2>&1 | grep -E "(No tests executed|0 passed|Tests:.*0 assertion)" && vendor/bin/pest --bail && vendor/bin/phpstan analyse --no-progress --memory-limit=2G</automated>
  </verify>
  <done>
    - `composer.json` `require` block contains `"genkgo/camt": "^2.10"`
    - `vendor/genkgo/camt/` directory exists with version 2.10.x
    - `composer.lock` updated and committed alongside `composer.json`
    - `tests/Pest.php` contains the `phase-2` group comment block at the bottom, with no `.planning/` references in the final comment
    - `vendor/bin/pest --group=phase-2 --bail` exits 0 (no tests yet — that's expected)
    - `vendor/bin/pest` (full suite) exits 0 — zero regression from the new dependency
    - `vendor/bin/phpstan analyse` exits 0 at the existing level
  </done>
</task>

<task type="auto" depends_on_task="Task 1">
  <name>Task 3: Anonymise and commit the ASN fixture corpus</name>
  <files>
    tests/fixtures/asn-camt053-sample-1.xml,
    tests/fixtures/asn-camt053-sample-1.md,
    tests/fixtures/asn-mt940-sample-1.sta,
    tests/fixtures/asn-mt940-sample-1.md,
    tests/fixtures/asn-cross-format/february.csv,
    tests/fixtures/asn-cross-format/february.camt053.xml,
    tests/fixtures/asn-cross-format/february.mt940.sta,
    tests/fixtures/asn-cross-format/README.md
  </files>
  <read_first>
    - tests/fixtures/asn-sample-1.md (canonical anonymisation protocol — apply VERBATIM)
    - tests/fixtures/asn-sample-1.csv (reference for "own IBAN pinned to NL00ASNB0123456789" pattern + the existing anonymised counterparty corpus to reuse — STARBUCKS AMSTERDAM, ALBERT HEIJN 1234, etc.)
    - The raw files the user handed over in Task 1 (paths from the resume-signal payload)
    - .planning/phases/02-asn-statement-coverage-camt-053-mt940/02-RESEARCH.md §"Fixture corpus needed from the user" (line ~1156) — fixture table specifies which scenarios each file must cover
    - .planning/phases/02-asn-statement-coverage-camt-053-mt940/02-VALIDATION.md §"Wave 0 Requirements" (line ~63) — what each fixture supports
  </read_first>
  <action>
    Implements **D-30** (statement-level metadata fixtures) + the cross-format-dedup acceptance-test prerequisite.

    For each raw file the user delivered in Task 1, perform the anonymisation protocol from `tests/fixtures/asn-sample-1.md`:

    **CAMT.053 anonymisation (`asn-camt053-sample-1.xml` + `february.camt053.xml`):**
    1. Load the raw XML with `simplexml_load_file()` (verify it parses — bail with "raw CAMT does not parse" if not).
    2. Replace every `<Othr><Nm>` (own party name) with literal `Anonymised Account Holder`.
    3. Replace every `<Othr><Id><IBAN>` for the own party with `NL00ASNB0123456789` (the fixed test IBAN reused from Phase 1).
    4. For counterparty `<RltdPties>/<Cdtr>/<Nm>` and `<RltdPties>/<Dbtr>/<Nm>`, replace with a deterministic public-domain merchant name from the existing fixture corpus (`STARBUCKS AMSTERDAM`, `ALBERT HEIJN 1234`, `SHELL ROTTERDAM`, `BOL.COM B.V.`, `SPOTIFY AB`, `NETFLIX INTERNATIONAL B.V.`, `IDEALER B.V.`, `KPN B.V.`). Use a stable mapping (`md5(original_name) % len(corpus)`) so the same raw name always maps to the same anonymised name across both CAMT samples.
    5. For counterparty IBANs in `<RltdPties>/<Cdtr>/<Id><IBAN>` and `<DbtrAcct>/<Id><IBAN>`, replace with synthetic-but-checksum-valid IBANs in the same country (Dutch counterparties get `NL` IBANs with a valid mod-97 checksum — re-use the IBAN generator pattern from `tests/fixtures/asn-sample-1.md` if it documents one, otherwise generate via `iban_mod97()` algorithm).
    6. **PRESERVE VERBATIM**: every `<EndToEndId>`, `<AcctSvcrRef>`, `<InstrId>`, `<TxId>`, `<MsgId>`, `<MndtId>`, all balances, all dates, all amounts, all currencies, all `<BkTxCd>` codes. These carry no PII and Phase 5's chain resolution depends on their structural realism.
    7. Review `<RmtInf>/<Ustrd>` (unstructured remittance) per row and redact any human names appearing in transfer descriptions ("Birthday gift Wessel" → "Personal transfer"). Public merchant invoice references like "Invoice 2025-04 Spotify" stay as-is.
    8. Save with `dom_import_simplexml($xml)->ownerDocument->save($outPath)` — preserve formatting + the original XML declaration.

    **MT940 anonymisation (`asn-mt940-sample-1.sta` + `february.mt940.sta`):**
    1. Open the raw file. Walk each line.
    2. Replace the `:25:` (own IBAN) line value with `NL00ASNB0123456789`.
    3. For every `:86:` block, scan for `?32` / `?33` (counterparty name continuations) and replace with the stable-mapped public-domain merchant name (same merchant corpus as the CAMT step).
    4. For every `?31` (counterparty IBAN), replace with the synthetic-checksum-valid IBAN.
    5. Inside the SVWZ unstructured remittance text in `?20–?29`, redact human names ("Wessel" → "User", "Verheij" → "Account Holder") but PRESERVE every `EREF+...+` and `MREF+...+` token verbatim.
    6. **PRESERVE VERBATIM**: every `:20:`, `:28C:`, `:60F:`, `:62F:`, `:64:`, every `:61:` line's value-date / entry-date / status / amount / transaction-code / customer-reference / bank-reference, the GVC code at the start of `:86:`, all line-ending bytes (CR/LF must match the source).
    7. Validate the anonymised file parses by counting tags: `grep -c '^:61:' file.sta` must equal `grep -c '^:61:' raw.sta`, `grep -c '^:86:' file.sta` must equal `grep -c '^:86:' raw.sta`.

    **CSV anonymisation (`february.csv`):** apply the existing Phase 1 protocol from `tests/fixtures/asn-sample-1.md` verbatim. Same own IBAN, same merchant mapping, same IBAN scheme.

    **Audit markdown for each fixture (mirror `tests/fixtures/asn-sample-1.md` structure):**

    `tests/fixtures/asn-camt053-sample-1.md` MUST contain:
    - Date the raw export was downloaded
    - CAMT.053 sub-version detected (read from `<Document xmlns="urn:iso:std:iso:20022:tech:xsd:camt.053.001.XX">`)
    - Statement period (`<Stmt>/<FrDt>` to `<ToDt>`)
    - Opening balance + closing balance (preserved verbatim from raw)
    - Number of `<Ntry>` entries
    - For each entry: `(index, BkTxCd domain/family/subFamily, EndToEndId presence)` so Phase 2 adapter tests can reference the expected structure without re-parsing
    - The anonymisation protocol applied (counterparty name → merchant corpus, counterparty IBAN → synthetic checksum, own IBAN → NL00ASNB0123456789, SEPA refs preserved)
    - **NO references to .planning/, PLAN.md, RESEARCH.md, or CONTEXT.md** per CLAUDE.md "feedback_codebase_gsd_agnostic"

    `tests/fixtures/asn-mt940-sample-1.md` MUST contain:
    - Date the raw export was downloaded
    - Number of `:61:` / `:86:` pairs
    - For each pair: `(index, transaction-type-code from :61: e.g. NTRF/NDDT/NMSC, presence of EREF in :86:)`
    - The full GVC-code distribution observed (`?20–?29` keywords seen, e.g. `EREF` 4×, `SVWZ` 6×, `MREF` 2×)
    - Whether the file uses a `{1:F01...}` SWIFT envelope (for HeaderSniffer signature testing)
    - The anonymisation protocol applied

    `tests/fixtures/asn-cross-format/README.md` MUST document:
    - The shared statement period covered by all files in the directory
    - The own IBAN (NL00ASNB0123456789)
    - The number of real-world transactions expected (so cross-format dedup tests can assert `expect($camtImportResult->enriched)->toBe(N)`)
    - For each transaction, a one-line summary: `(YYYY-MM-DD, amount, counterparty merchant, EndToEndId-or-NULL)` so downstream tests can pin specific expectations without re-parsing
    - If the MT940 cross-format sample is missing (user could not produce it), explicitly state "MT940 cross-format sample unavailable — `CrossFormatDedupTest::mt940_after_csv` and `mt940_then_camt053` will be skipped"

    Commit all eight fixture files + their audit Markdown to git. Do NOT add `.snap` files yet — those land in Wave 2 when the adapter tests first run.
  </action>
  <verify>
    <automated>test -f tests/fixtures/asn-camt053-sample-1.xml && test -f tests/fixtures/asn-camt053-sample-1.md && test -f tests/fixtures/asn-mt940-sample-1.sta && test -f tests/fixtures/asn-mt940-sample-1.md && test -f tests/fixtures/asn-cross-format/february.csv && test -f tests/fixtures/asn-cross-format/february.camt053.xml && test -f tests/fixtures/asn-cross-format/README.md && php -r 'exit(simplexml_load_file("tests/fixtures/asn-camt053-sample-1.xml") === false ? 1 : 0);' && grep -q '^:20:' tests/fixtures/asn-mt940-sample-1.sta && grep -q '^:61:' tests/fixtures/asn-mt940-sample-1.sta && grep -q '^:86:' tests/fixtures/asn-mt940-sample-1.sta && grep -q 'NL00ASNB0123456789' tests/fixtures/asn-camt053-sample-1.xml && grep -q 'NL00ASNB0123456789' tests/fixtures/asn-mt940-sample-1.sta && grep -q 'NL00ASNB0123456789' tests/fixtures/asn-cross-format/february.csv && grep -q 'urn:iso:std:iso:20022:tech:xsd:camt\.053\.001\.' tests/fixtures/asn-camt053-sample-1.xml && ! grep -rE '\.planning|PLAN\.md|RESEARCH\.md|CONTEXT\.md' tests/fixtures/asn-camt053-sample-1.md tests/fixtures/asn-mt940-sample-1.md tests/fixtures/asn-cross-format/README.md</automated>
  </verify>
  <done>
    - All eight fixture files exist and parse:
      - CAMT.053 XML files parse via `simplexml_load_file`
      - MT940 .sta files contain valid `:20:`, `:61:`, `:86:` tags
      - CSV file passes `tests/fixtures/asn-cross-format/february.csv` byte-structure (header row begins with `"Datum","Je rekening"`)
    - Every fixture's own IBAN is `NL00ASNB0123456789`
    - CAMT XML files declare an ISO 20022 CAMT.053 family namespace
    - No audit markdown file mentions `.planning/`, `PLAN.md`, `RESEARCH.md`, or `CONTEXT.md`
    - Cross-format pair's README documents the expected transaction count and per-row summary
    - If MT940 cross-format sample is absent, the README explicitly flags that limitation
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied raw exports → anonymised fixtures | The user's raw ASN files may contain real PII (counterparty names, IBANs, free-text remittance). Anonymisation is the trust boundary — committed fixtures must carry zero PII. |
| composer.json → vendor/ install | New `genkgo/camt` dependency is installed from Packagist. The transitive `moneyphp/money` is already coexisting with `brick/money` per Phase 1 STACK.md — no new transitive risk in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information disclosure | Anonymised fixture files committed to git | mitigate | Task 3 anonymisation protocol replaces counterparty names with public-domain merchants, counterparty IBANs with synthetic-checksum-valid IBANs, redacts human names in free-text remittance, pins own IBAN to a fixed test value. Audit markdown documents every replacement. Task 3 `<verify>` greps for the fixed test IBAN as proof. |
| T-02-01-02 | Tampering | composer.lock not regenerated after manifest edit | mitigate | Task 2 explicitly runs `composer install --no-interaction` AFTER editing `composer.json`, and the verify gate greps `composer.json` for the new pin. Without the lock regeneration step, CI would install whatever happens to be latest. |
| T-02-01-03 | Information disclosure | `.planning/` references leaking into committed fixture audit markdown (violates CLAUDE.md "feedback_codebase_gsd_agnostic") | mitigate | Task 3 acceptance criteria includes `! grep -rE '\.planning\|PLAN\.md\|RESEARCH\.md\|CONTEXT\.md'` over the audit files — fails the gate if any GSD-internal path leaks. |
| T-02-01-04 | Denial of service | Larastan baseline silently breaks after genkgo/camt is added | accept | Task 2 verify includes `vendor/bin/phpstan analyse` — fails the gate if so. Baseline regeneration is the documented escape hatch; the impact is local-only (this is a localhost-only single-user app per FND-01). |
| T-02-01-05 | Spoofing | User supplies a fixture with a counterparty IBAN that fails mod-97 checksum, downstream IBAN-validating libraries (genkgo/camt's `jschaedl/iban-validation`) reject it | mitigate | Task 3 step 5 explicitly calls for synthetic-but-checksum-valid IBANs. If the generator step skips the mod-97 check, Wave 2's adapter tests will fail loudly with `IbanFormatException` from `jschaedl/iban-validation` — failure mode is visible, not silent. |
</threat_model>

<verification>
1. **Composer install confirmed:** `composer show genkgo/camt` reports a 2.10.x version. `composer.lock` is committed alongside `composer.json`.
2. **Group registration works:** `vendor/bin/pest --group=phase-2 --bail` exits 0 (with "No tests executed" before any phase-2 tests are written — that is the correct empty-baseline state).
3. **Fixture corpus committed:** all eight fixture files + their three audit markdown files + the cross-format README exist under `tests/fixtures/`. Every file passes its format-specific parse check.
4. **Anonymisation correct:** every fixture's own IBAN is the fixed `NL00ASNB0123456789`. No GSD-internal path (`.planning`, `PLAN.md`, `RESEARCH.md`, `CONTEXT.md`) appears in any committed audit file.
5. **Zero regression:** `vendor/bin/pest`, `vendor/bin/phpstan analyse`, and `vendor/bin/pint --test` all exit 0.
</verification>

<success_criteria>
- `composer.json` declares `"genkgo/camt": "^2.10"`; `composer.lock` is updated; `vendor/genkgo/camt/` is installed and ready for use by Wave 2's `AsnCamt053Adapter`.
- `tests/Pest.php` registers the `phase-2` group; the group filter works (empty baseline OK).
- Anonymised CAMT.053 + MT940 fixtures + cross-format trio exist with audit markdown that documents the protocol and the expected transaction structure.
- Full test suite + Larastan level 10 strict + Pint pass with zero regression.
- User has explicitly handed over the raw fixture corpus; the audit markdown documents anything that was unavailable (e.g., missing MT940 cross-format sample).
</success_criteria>

<output>
After completion, create `.planning/phases/02-asn-statement-coverage-camt-053-mt940/02-01-SUMMARY.md` covering:
- The committed fixture corpus (one bullet per file with its byte-size, transaction count, and notable structural features — e.g. CAMT.053 sub-version detected, MT940 GVC distribution)
- The user-supplied raw-file paths and the anonymisation protocol applied (mirror the Phase 1 `01-04-SUMMARY.md` audit style)
- Whether the MT940 cross-format sample was delivered or skipped
- The `genkgo/camt` version actually installed (read from `composer.lock`)
- Any Larastan baseline regeneration that was required, with the diff committed
- The `phase-2` Pest group registration confirmation
- Open follow-ups for Wave 2 (e.g. "If CAMT.053 sub-version observed is 001.02 only, Wave 2 must add a 001.08 fixture later" or similar coverage gaps)
</output>
