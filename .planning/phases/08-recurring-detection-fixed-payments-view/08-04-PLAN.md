---
phase: 08-recurring-detection-fixed-payments-view
plan: 04
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php
  - Modules/Recurring/Internal/Http/Livewire/RecurringPage.php
  - Modules/Recurring/Public/Services/FixedPaymentsViewQuery.php
  - Modules/Recurring/Resources/views/livewire/recurring-page.blade.php
  - Modules/Recurring/Routes/web.php
  - Modules/Recurring/Providers/RecurringServiceProvider.php
  - Modules/Recurring/tests/Feature/IncomeDetectorTest.php
  - Modules/Recurring/tests/Feature/RecurringPageTest.php
  - Modules/Recurring/tests/Feature/FixedPaymentsViewQueryTest.php
  - tests/Contracts/RecurringDetectionContractTest.php
autonomous: true
requirements:
  - LED-06
  - REC-04

user_setup: []

must_haves:
  truths:
    - "User can open `/recurring` while authenticated and see approved series grouped into 'Recurring expenses' and 'Recurring income' sections with a net-flow summary at the top"
    - "Income detector clusters monthly salary transactions (counterparty IBAN-primary, normalized-description fallback) into one approved-pending income series per cluster — running ONLY on transactions classified `type='income'` by Phase 4 LED-05, trusting upstream classification (D-822)"
    - "Income below `users.recurring_income_min_amount_minor` (default €2000) is filtered out — small refunds and cashbacks never appear as suggestions"
    - "Two-employer payroll lands as two distinct income series (D-821)"
    - "Each `/recurring` row's amount column shows the latest occurrence value as primary text plus a small drift-indicator chip when it differs from the prior occurrence (D-824)"
    - "Each `/recurring` row's monthly-equivalent column is computed as `latest_amount × cadence_multiplier` (weekly × 4.33, monthly × 1, quarterly ÷ 3, yearly ÷ 12) so quarterly and yearly series are comparable to monthly subscriptions (D-826)"
    - "Each `/recurring` row renders the funding-chain icon stack as an account-icon-stack with a chain-link badge — empty icon when no chain exists, ASN/ICS/PayPal icon when one does, with fallback to the previous occurrence's chain when the latest is unresolved (D-828, D-829)"
    - "Next-expected-charge text on each `/recurring` row renders as relative + absolute date (`in 5 days · May 22` for near dates, `Jun 18` for dates beyond ~14 days), dim/italic when cadence variance is high (stddev > 5 days) per the low-confidence indicator (D-830)"
    - "Income shows up on the dashboard via the existing in/out/remaining tile — no new dedicated income tile is added; the recurring breakdown lives in the fixed-payments card's income section instead (D-836)"
    - "Multi-currency rows render original-currency primary + EUR shadow (D-840); the section subtotals sum in EUR using each series' latest FX rate (D-841)"
    - "`FixedPaymentsViewQuery::topByMonthlyEquivalent` runs in ≤ 3 queries for any N — no N+1 on chain link joins (RESEARCH Q6 mitigation)"
  artifacts:
    - path: Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php
      provides: Implements SeriesDetector; clusters transactions WHERE type='income' AND amount_minor >= recurring_income_min_amount_minor; IBAN-primary + normalized-description fallback (D-817)
      exports: ["IncomeSeriesDetector"]
    - path: Modules/Recurring/Internal/Http/Livewire/RecurringPage.php
      provides: /recurring Livewire SFC with grouped expense + income sections, net-flow summary, per-row funding chain icon, multi-currency rendering
      contains: "RecurringPage"
    - path: Modules/Recurring/Public/Services/FixedPaymentsViewQuery.php
      provides: viewForUser(User), topByMonthlyEquivalent(User, int limit), monthlyEquivalentTotals(User) — single-query joins to chain_links + merchant_memories
      exports: ["FixedPaymentsViewQuery"]
    - path: Modules/Recurring/Resources/views/livewire/recurring-page.blade.php
      provides: Grouped expense + income + collapsed transfers section per D-818/D-819; calm Linear/Notion layout
  key_links:
    - from: Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php
      to: SeriesDetector contract + container tag 'recurring.detector'
      via: implements interface; provider tags it alongside ExpenseSeriesDetector
      pattern: "tag\\(\\[ExpenseSeriesDetector"
    - from: Modules/Recurring/Internal/Http/Livewire/RecurringPage.php
      to: FixedPaymentsViewQuery
      via: method-parameter DI on render()
      pattern: "FixedPaymentsViewQuery"
    - from: Modules/Recurring/Public/Services/FixedPaymentsViewQuery.php
      to: Modules/Chains/Public/Services/ChainLinkQuery + merchant_memories (via Categorization Public)
      via: pre-joined query — no N+1
      pattern: "leftJoin"
    - from: Modules/Recurring/Routes/web.php
      to: RecurringPage Livewire component
      via: Route::get('/recurring', RecurringPage::class)
      pattern: "Route::get\\('/recurring'"
---

<objective>
Ship the income side of the detector + the `/recurring` list page. After this plan, the user can see their approved recurring expenses AND approved recurring income in one calm view, grouped by direction with a net-flow summary, with funding-chain icons and multi-currency rendering. The drill-in page, dashboard inline card, top-nav badge, and bulk-action wiring stay in Plan 05.

Purpose: Close LED-06 (recurring income detected the same way) and REC-04 (fixed-monthly-payments overview with name + monthly-equivalent + funding + category + next-expected). Unifies the detector code path so Phase 9/10 reads come from one stable Public surface.

Output: Working income detector, `/recurring` Livewire SFC, FixedPaymentsViewQuery Public service. Demo: "Run the detector, approve a salary suggestion, watch it show up in `/recurring` alongside the Spotify subscription with a chain icon and an EUR shadow on the Netflix-USD row."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-VALIDATION.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-03-SUMMARY.md
@CLAUDE.md
@Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php
@Modules/Recurring/Internal/CadenceInferrer.php
@Modules/Recurring/Internal/Detection/ClusterKeyComposer.php
@Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php
@Modules/Recurring/Public/Services/RecurringSeriesQuery.php
@Modules/Recurring/Public/Dto/RecurringSeriesDto.php
@Modules/Recurring/Public/Contracts/SeriesDetector.php
@Modules/Recurring/Providers/RecurringServiceProvider.php
@Modules/Chains/Public/Services/ChainLinkQuery.php
@Modules/Categorization/Public/Services/MerchantMemoryQuery.php
@Modules/Ledger/Public/ValueObjects/Money.php
@Modules/Chains/Resources/views/livewire/chain-review-queue.blade.php
@Modules/Chains/Routes/web.php
@Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IncomeSeriesDetector + container tagging + contract test coverage</name>
  <files>
    Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php,
    Modules/Recurring/Providers/RecurringServiceProvider.php,
    Modules/Recurring/tests/Feature/IncomeDetectorTest.php,
    tests/Contracts/RecurringDetectionContractTest.php
  </files>
  <read_first>
    Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php (Plan 03 — copy the constructor DI shape, the detection window read from users, the cluster-then-insert algorithm; swap counterparty_normalized clustering for IBAN-primary + description-fallback per D-817),
    Modules/Recurring/Internal/CadenceInferrer.php (Plan 03 — the income detector calls the same `infer()` method),
    Modules/Recurring/Internal/Detection/ClusterKeyComposer.php (Plan 03 — `compose('income', counterpartyKey, currency, cadence)` produces the income cluster key),
    Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php (Plan 03 — the job already iterates container-tagged 'recurring.detector' implementations; this plan only adds IncomeSeriesDetector to the tag array),
    Modules/Recurring/Providers/RecurringServiceProvider.php (Plan 03 — append IncomeSeriesDetector to the tag([...]) call),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-816 (unified table with direction enum), D-817 (IBAN-primary clustering), D-820 (€2000 threshold), D-821 (each IBAN cluster = own series), D-822 (income detector reads only type='income'),
    Modules/Recurring/tests/fixtures/synthesised/monthly-salary.php + Modules/Recurring/tests/fixtures/synthesised/two-employer-salary.php (Plan 01 — the expected behaviour each fixture asserts)
  </read_first>
  <behavior>
    - `IncomeSeriesDetector::detectForUser(User $user): void` implements `SeriesDetector`:
       1. Loads `users.recurring_income_min_amount_minor` (default 200000 = €2000); income transactions below this threshold are excluded from clustering.
       2. Queries `transactions` WHERE `type='income'` AND `posted_at >= now - detection_window_months` AND `original_amount_minor >= threshold` AND `user_id = $user->id` — raw query builder via injected DatabaseManager.
       3. Clusters in PHP with the IBAN-primary fallback to normalized-description per D-817: if `counterparty_iban` is non-null and consistent across at least 2 occurrences in a group, use IBAN as the cluster key; otherwise fall back to `counterparty_normalized`.
       4. For each cluster with ≥ 2 occurrences (D-803), computes cadence via `CadenceInferrer::infer($timestamps)`.
       5. Skip irregular-cadence clusters (D-843).
       6. Compose `cluster_key` via `ClusterKeyComposer::compose('income', $ibanOrDescription, $currency, $cadence)`.
       7. New cluster → insert pending row + occurrence rows; dispatch `RecurringSeriesDetected`.
       8. Existing cluster — metric refresh / cadence-flip check (same logic as ExpenseSeriesDetector).
       9. Multi-IBAN payroll (the `two-employer-salary.php` fixture) produces TWO distinct series (D-821).
       10. Income < threshold (small refund, cashback) is silently dropped — does NOT show up in `recurring_series_occurrences`.
    - `IncomeSeriesDetector` is registered as a singleton in `RecurringServiceProvider::register()` and appended to the existing `tag([...], 'recurring.detector')` array so `DetectRecurringSeriesJob` runs BOTH detectors in one sweep.
    - The full contract test in `tests/Contracts/RecurringDetectionContractTest.php` now asserts the income-side fixture expectations (the `expected['skip_until_income_detector']` skip from Plan 03 is removed).
    - `vendor/bin/pest Modules/Recurring/tests/Feature/IncomeDetectorTest.php --filter='income-cluster'` exits 0 (monthly salary → 1 income series, cadence=monthly).
    - `vendor/bin/pest Modules/Recurring/tests/Feature/IncomeDetectorTest.php --filter='income-threshold'` exits 0 (below-threshold income does not cluster — no rows in `recurring_series`).
    - `vendor/bin/pest Modules/Recurring/tests/Feature/IncomeDetectorTest.php --filter='two-employer'` exits 0 (two distinct IBAN clusters → two series).
  </behavior>
  <action>
    Create `Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php`. `final class` implementing `SeriesDetector`. Constructor DI mirroring `ExpenseSeriesDetector` exactly:
    ```
    public function __construct(
        private readonly DatabaseManager $db,
        private readonly Clock $clock,
        private readonly CadenceInferrer $cadenceInferrer,
        private readonly ClusterKeyComposer $clusterKeyComposer,
        private readonly RecurringSeriesStateMachine $stateMachine,
        private readonly Dispatcher $events,
    ) {}
    ```
    (No MerchantMemoryQuery — income series don't decorate via merchant_memories; categories are expense-side.)

    Implement `detectForUser(User $user): void`:
    1. Read `$user->recurring_detection_window_months` (default 18) and `$user->recurring_income_min_amount_minor` (default 200000).
    2. Compute `$since = $this->clock->now()->subMonths($windowMonths)`.
    3. Query transactions: `select id, posted_at, booked_at, original_amount_minor, original_currency, settled_amount_minor, settled_currency, fx_rate_used, counterparty_iban, counterparty_normalized from transactions where user_id = ? and type = 'income' and original_amount_minor >= ? and posted_at >= ? order by posted_at asc`. Bind userId, threshold, since.
    4. Group rows in PHP by IBAN first: rows with `counterparty_iban` non-null + non-empty group on `counterparty_iban`; rows with null/empty IBAN fall back to `counterparty_normalized`. Same currency required per D-839.
    5. For each cluster with ≥ 2 occurrences, compute timestamps + amounts; call `CadenceInferrer::infer($timestamps)`.
    6. Skip irregular clusters.
    7. Compose `cluster_key = ClusterKeyComposer::compose('income', $ibanOrDesc, $currency, $cadence)`.
    8. Existing series lookup: `where('user_id', $user->id)->where('direction', 'income')->where('cluster_key', $clusterKey)->where('latest_currency', $currency)->first()`.
    9. New cluster → INSERT row: `direction='income'`, `state='pending'`, `detected_name = $counterparty_normalized` (NOT the IBAN — calm aesthetic), `latest_amount_minor = $latest_amount`, `latest_currency = $currency`, `monthly_equivalent_minor = monthlyEquivalent($latestAmount, $cadence)` (cadence multiplier: weekly × 4.33, monthly × 1, quarterly ÷ 3, yearly ÷ 12 per D-826), `cadence`, `next_expected_at`, `next_expected_confidence_low`, `cluster_key`, timestamps. INSERT-OR-IGNORE matching occurrence rows. Dispatch `RecurringSeriesDetected`.
    10. Existing approved/cadence_changed/pending → metric refresh same as ExpenseSeriesDetector; cadence flip → state machine transition + `RecurringSeriesCadenceFlipped` event.
    11. Existing rejected → SKIP (D-808).

    The detector NEVER writes to `transactions` (arch test enforces).

    Modify `Modules/Recurring/Providers/RecurringServiceProvider.php`. In `register()`:
    ```
    $this->app->singleton(IncomeSeriesDetector::class);
    $this->app->tag([
        ExpenseSeriesDetector::class,
        IncomeSeriesDetector::class,
    ], 'recurring.detector');
    ```
    (Replace the single-element `tag([ExpenseSeriesDetector::class], 'recurring.detector')` from Plan 03 with the two-element array.)

    Create `Modules/Recurring/tests/Feature/IncomeDetectorTest.php` using `RefreshDatabase`. Test slices:
    - `income-cluster`: seed `monthly-salary.php` fixture transactions; dispatch `DetectRecurringSeriesJob` synchronously; assert one row in `recurring_series` with `direction='income'`, `state='pending'`, `cadence='monthly'`.
    - `income-threshold`: seed income transactions of +€500 monthly (below the €2000 default threshold); run the job; assert zero rows in `recurring_series` for that user.
    - `two-employer`: seed `two-employer-salary.php` fixture transactions (6 from IBAN_A + 6 from IBAN_B at +€2200 each); run the job; assert TWO income series with different `cluster_key` values.
    - `iban-missing-falls-back-to-description`: seed 12 income transactions where counterparty_iban is null but counterparty_normalized is consistent ('FREELANCE-CLIENT-X'); run the job; assert one income series clustered on the description.
    - `mixed-currency-income`: seed 12 income transactions where 6 are EUR +€3500 and 6 are USD +$2500 from the same employer; assert TWO series (each clustered per D-839 by currency).
    - `idempotent-re-run`: run the job twice; assert no duplicate series or occurrence rows.

    Update `tests/Contracts/RecurringDetectionContractTest.php`: remove any `expected['skip_until_income_detector']` skips from the income fixtures (monthly-salary, two-employer-salary). After this plan ships, the contract test asserts the full expense + income expected set across all 11 synthesised fixtures.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Recurring/tests/Feature/IncomeDetectorTest.php tests/Contracts/RecurringDetectionContractTest.php tests/Contracts/BoundaryArchTest.php --stop-on-failure</automated>
  </verify>
  <done>
    - `Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php` exists and implements `SeriesDetector`.
    - `RecurringServiceProvider::register()` contains a `tag([ExpenseSeriesDetector::class, IncomeSeriesDetector::class], 'recurring.detector')` call.
    - All six `--filter` slices on `IncomeDetectorTest` pass.
    - `tests/Contracts/RecurringDetectionContractTest.php` asserts both expense AND income expectations across the 11 synthesised fixtures + exits 0.
    - `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` exits 0 (no transaction writes from income detector; container-tag arch invariants stay green).
    - `composer test && composer analyse` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FixedPaymentsViewQuery + RecurringPage Livewire SFC + /recurring route + Blade view + tests</name>
  <files>
    Modules/Recurring/Public/Services/FixedPaymentsViewQuery.php,
    Modules/Recurring/Internal/Http/Livewire/RecurringPage.php,
    Modules/Recurring/Resources/views/livewire/recurring-page.blade.php,
    Modules/Recurring/Routes/web.php,
    Modules/Recurring/Providers/RecurringServiceProvider.php,
    Modules/Recurring/tests/Feature/FixedPaymentsViewQueryTest.php,
    Modules/Recurring/tests/Feature/RecurringPageTest.php
  </files>
  <read_first>
    Modules/Recurring/Public/Services/RecurringSeriesQuery.php (Plan 03 — final readonly class with DatabaseManager DI, raw query builder per ChainLinkQuery precedent),
    Modules/Chains/Public/Services/ChainLinkQuery.php (the chain-link read API the funding icon consumes — particularly the cursor-pagination shape and the way it maps raw rows to DTOs),
    Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php (Plan 03 — the action-method DI shape; copy verbatim for RecurringPage which has different actions but the same render() structure),
    Modules/Chains/Resources/views/livewire/chain-review-queue.blade.php (the Blade structure for the analog Chains review page — adapt for the grouped section layout D-818 mandates),
    Modules/Categorization/Public/Services/MerchantMemoryQuery.php (the `forCounterpartiesNormalized` batch method from Plan 01 — used by FixedPaymentsViewQuery to decorate expense rows with category data without N+1),
    Modules/Recurring/Public/Dto/RecurringSeriesDto.php (Plan 02 — the DTO shape every list row uses),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-818 (grouped sections + net-flow summary), D-819 (collapsed transfers section), D-824 (drift indicator chip — primary text is latest amount), D-826 (monthly equivalent multiplier), D-828/D-829 (funding chain icon + fallback to previous occurrence), D-840 (original-currency primary + EUR shadow), D-841 (dashboard EUR total — applies to Plan 05 card but the per-row currency formatting matches here),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md Q6 (N+1 prevention — single eager-loaded query),
    Modules/Recurring/Models/RecurringSeries.php (Plan 02 — the latestFundingChainLink BelongsTo relation that FixedPaymentsViewQuery's joined query mirrors at the raw SQL level)
  </read_first>
  <behavior>
    - `FixedPaymentsViewQuery` is a `final readonly class` with constructor DI: `DatabaseManager $db`, `MerchantMemoryQuery $merchantMemory`, `ChainLinkQuery $chainLinks`.
    - `viewForUser(User $user): array{expenses: array<RecurringSeriesDto>, income: array<RecurringSeriesDto>, transfers: array<RecurringSeriesDto>}` returns the entire `/recurring` payload in one logical call. Each section is sorted by `monthly_equivalent_minor` descending per D-818. Internally uses a SINGLE query with LEFT JOINs against `chain_links` (for funding icon) and a separate batch lookup against `merchant_memories` via the Plan 01 batch method (so total query count is 2: one for the series list with chain join, one for merchant memories batch).
    - The chain-link join handles the D-829 fallback: prefer the latest occurrence's `latest_funding_chain_link_id`; if NULL or its state is not `confirmed`/`candidate`, the query takes the next-latest occurrence's chain via a subquery — verified by the `chain-fallback` feature test.
    - `topByMonthlyEquivalent(User $user, int $limit = 6): array<RecurringSeriesDto>` returns the dashboard card payload (used by Plan 05); limit defaults to 6 per D-835.
    - `monthlyEquivalentTotals(User $user): array{expense_eur_minor: int, income_eur_minor: int, net_eur_minor: int}` returns the net-flow summary for the top of `/recurring` per D-818, summing in EUR via each series' latest FX rate per D-841.
    - The query layer NEVER N+1s. Asserted by `FixedPaymentsViewQueryTest` using Laravel's `DB::listen` / `DatabaseManager::listen()` query-count assertion: for N ≥ 10 series the total query count is ≤ 3.
    - `RecurringPage` is a `final class extends Livewire\Component` with method-parameter DI (no constructor DI per Pattern S-2).
       - Public state: `bool $transfersExpanded = false` (D-852 — default collapsed; clicking expands).
       - Action methods: `toggleTransfers()` flips `$transfersExpanded`. `reDetect(CurrentUser $currentUser, Dispatcher $bus)` lands in Plan 05 — NOT in this plan; the button placeholder may render but stays disabled until Plan 05.
       - `render(CurrentUser $currentUser, FixedPaymentsViewQuery $query, ViewFactory $views): View` resolves the user, calls `$query->viewForUser($user)` for sections, calls `$query->monthlyEquivalentTotals($user)` for the header, and renders the view extending `layouts.app` with title `'Recurring · diederik'`.
    - Route `/recurring` resolves the Livewire component behind `web` + `auth` middleware. The `noSynchronousDetectionInRequestLifecycle` arch test stays green (no `SeriesDetector` import from any Internal/Http file).
    - The Blade view layout follows D-818:
       - Header: `−€X.YZ expenses + €A.BC income = €N.MN net/month` net-flow summary (latest_amount × cadence_multiplier sums per direction; EUR-converted per series).
       - Section "Recurring expenses": rows sorted by monthly_equivalent_minor DESC; per-row: display_name, latest_amount (Money formatted, original-currency primary + EUR shadow when distinct per D-840), small drift-indicator chip when `latest_amount` differs from the immediately-prior occurrence (D-824), monthly_equivalent EUR, funding-chain icon stack (account icon for ASN/ICS/PayPal + small chain-link badge when chain_links row exists per D-828; click opens the Phase 5 chain drawer — that interaction works because the chain drawer is already shipped), category badge (read-only via MerchantMemoryQuery), next-expected-charge text (relative + absolute per D-830; dim/italic when `next_expected_confidence_low=true`).
       - Section "Recurring income": same row shape, just direction='income'.
       - Section "Recurring transfers": COLLAPSED by default per D-819/D-852; the row above the section is the `wire:click="toggleTransfers"` toggle. Rows inside are informational only — never sum into the net-flow header.
    - The view uses Flux UI components (table, chip, badge) per the project stack.
  </behavior>
  <action>
    Create `Modules/Recurring/Public/Services/FixedPaymentsViewQuery.php` per `RecurringSeriesQuery` precedent. `final readonly class`. Constructor DI:
    ```
    public function __construct(
        private DatabaseManager $db,
        private MerchantMemoryQuery $merchantMemory,
        private ChainLinkQuery $chainLinks,
    ) {}
    ```

    Implement `viewForUser(User $user): array`:
    1. Single query: `select rs.*, ml.confirmed_chain_link_id from recurring_series rs left join (subquery selecting chain_link.id where state in ('confirmed','candidate') for each series via correlated lookup) ml on ... where rs.user_id = ? and rs.state = 'approved'`. The exact join shape: a LEFT JOIN against `chain_links` keyed on `rs.latest_funding_chain_link_id`. Use raw query builder via `$this->db->connection()`.
    2. Partition results in PHP by direction: rows where `direction='expense'` AND `latest_funding_chain_link_id` is NULL OR the joined chain_link is `confirmed` → "expenses". Rows where `direction='income'` → "income". Rows where pair_transaction_id-based transfers — actually transfers don't land in `recurring_series` directly (the detector reads only expense/fee/refund and income types per D-822). So the "transfers" section is empty by default and reserved for Phase 9/10 promotion of internal moves; in Phase 8 it stays empty and is only shown if any income series has a clear "transfer" classification — for v1 leave this empty list returned. Document the empty-transfers reality in the docblock.

       Implementation note: D-819 says recurring transfers are visible in a "Recurring transfers" section. Per the CONTEXT analysis, transfers are NOT detected by either detector (the detectors filter on type='expense'/'fee'/'refund' for expense and type='income' for income — `transfer_out`/`transfer_in` types are excluded). So Phase 8's "Recurring transfers" section is reserved structure that ships empty in v1; Phase 9/10 may populate it. Document this in the FixedPaymentsViewQuery docblock — the executor should NOT detect transfers as series in this plan.

    3. For each series row, batch-look-up merchant_memories via `MerchantMemoryQuery::forCounterpartiesNormalized($user, $listOfDetectedNames)` → ONE additional query for all rows.
    4. Map each row to `RecurringSeriesDto`. Construct `Money::ofMinor($latest_amount_minor, $latest_currency)`. Compute `eurEquivalent` only when `latest_currency !== 'EUR'` using `latest_fx_rate_used` (D-851). Compute `monthlyEquivalent` in EUR using cadence multiplier (weekly × 4.33, monthly × 1, quarterly ÷ 3, yearly ÷ 12 per D-826).
    5. Sort each section DESC by monthly_equivalent.
    6. Return `['expenses' => [...], 'income' => [...], 'transfers' => []]`.

    Implement `topByMonthlyEquivalent(User $user, int $limit = 6): array<RecurringSeriesDto>`: same single-query+batch shape, take top `$limit` across BOTH directions ordered DESC by monthly_equivalent_minor. Used by Plan 05's dashboard card.

    Implement `monthlyEquivalentTotals(User $user): array{expense_eur_minor: int, income_eur_minor: int, net_eur_minor: int}`: a single SUM query partitioned by direction, scoped to `state='approved'`. Sum in EUR using `monthly_equivalent_minor` (already EUR per D-841).

    Implement the D-829 chain-fallback as a private helper `latestResolvedFundingChainId(RecurringSeries $series): ?int`: if `latest_funding_chain_link_id` is null OR the linked chain_link is in `unresolved`/null state, walk back through `recurring_series_occurrences` ordered by `observed_at DESC` and return the first occurrence's chain id where the joined chain_link is `confirmed` or `candidate`. To avoid N+1 on the main `viewForUser` path: pre-compute the fallback id per series in the main query using a correlated subquery OR a second batch lookup. Choose whichever produces a clean SQL plan against SQLite — the FixedPaymentsViewQueryTest's query-count assertion enforces ≤ 3 total queries for N≥10 series.

    Register `FixedPaymentsViewQuery` as a singleton in `RecurringServiceProvider::register()`.

    Create `Modules/Recurring/Internal/Http/Livewire/RecurringPage.php`. `final class extends Component`. Public state: `bool $transfersExpanded = false`. Action methods:
    ```
    public function toggleTransfers(): void {
        $this->transfersExpanded = ! $this->transfersExpanded;
    }
    ```
    `render(CurrentUser $currentUser, FixedPaymentsViewQuery $query, ViewFactory $views): View`:
    1. `$user = $currentUser->user()`.
    2. `$sections = $query->viewForUser($user)`.
    3. `$totals = $query->monthlyEquivalentTotals($user)`.
    4. `$view = $views->make('recurring::livewire.recurring-page', ['sections' => $sections, 'totals' => $totals, 'transfersExpanded' => $this->transfersExpanded]);`
    5. `$view->extends('layouts.app', ['title' => 'Recurring · diederik']);` (with the standard phpstan ignore comment).
    6. Return `$view`.

    Register the Livewire component in `RecurringServiceProvider::boot()`:
    ```
    $livewire->component('recurring.recurring-page', RecurringPage::class);
    ```

    Modify `Modules/Recurring/Routes/web.php`. Add inside the existing `Route::middleware(['web', 'auth'])->group(...)` block:
    ```
    Route::get('/recurring', RecurringPage::class)->name('recurring.index');
    ```
    Update the import block to include `use Modules\Recurring\Internal\Http\Livewire\RecurringPage;`.

    Create `Modules/Recurring/Resources/views/livewire/recurring-page.blade.php`. Calm Linear/Notion layout per UI-05. Structure:
    - Top: net-flow summary `&lt;div&gt;−€{{ expense }} + €{{ income }} = €{{ net }} net/month&lt;/div&gt;`. Use the project's Money formatter (probably resolved via a Blade component or Money::format) for currency formatting.
    - Section "Recurring expenses": rendered when `$sections['expenses']` is non-empty. Title + count chip. Each row: avatar/icon stack (use `&lt;x-flux::avatar.group&gt;` or similar — the executor verifies the exact Flux component name by reading the existing chains drawer view), display_name, drift chip (small `&lt;flux:badge variant="subtle"&gt;↑ €{{ delta }}&lt;/flux:badge&gt;` when delta is non-zero per D-824), latest_amount (original-currency primary + EUR shadow in smaller dim text per D-840), monthly_equivalent EUR, funding-chain icon group (account icon + chain badge when `latestFundingChainLinkId` is non-null, with `wire:click` opening the Phase 5 chain drawer — the drawer is already shipped under `chains.chain-drawer`), category badge (read-only), next-expected-charge text (relative + absolute, dim/italic when `nextExpectedConfidenceLow`).
    - Section "Recurring income": same shape, omits the drift chip (income drift handling lives in Phase 9), shows counterparty IBAN as a subtitle when present (Pitfall 6).
    - Section "Recurring transfers": collapsed `&lt;details&gt;` element (or Flux `&lt;flux:accordion&gt;`) with `wire:click="toggleTransfers"` toggle; empty body in v1.

    Create `Modules/Recurring/tests/Feature/FixedPaymentsViewQueryTest.php` using `RefreshDatabase`. Test slices:
    - `view-for-user-returns-grouped-sections`: seed approved expense + income series; assert returned shape has `expenses`, `income`, `transfers` keys; sections sorted by monthly_equivalent DESC.
    - `cross-user-empty`: seed user A's series; query for user B; assert all sections are empty.
    - `chain-fallback`: seed a series with `latest_funding_chain_link_id` pointing at a `chain_links` row in state `unresolved`; seed a prior occurrence with a `confirmed` chain; assert the DTO's `latestFundingChainLinkId` is the prior occurrence's chain id (D-829).
    - `n-plus-one-budget`: seed 12 approved series across both directions; wrap `$query->viewForUser($user)` in `DB::listen` counting; assert query count ≤ 3.
    - `monthly-equivalent-multiplier`: seed series with cadence in each class (weekly/monthly/quarterly/yearly); assert `monthlyEquivalentTotals` totals match the cadence multipliers per D-826.
    - `top-by-monthly-equivalent-limits-to-6`: seed 10 approved series; call `topByMonthlyEquivalent($user, 6)`; assert exactly 6 rows returned ordered DESC.

    Create `Modules/Recurring/tests/Feature/RecurringPageTest.php` using Livewire's testing harness:
    - `renders-grouped-sections`: seed approved expense + income series; open `/recurring`; assert HTML contains both display names + the net-flow header.
    - `chain-fallback-renders-correct-icon`: seed series with prior-occurrence chain; assert the rendered HTML contains the chain badge (look for the badge marker class or a `data-test="chain-badge"` attribute the Blade view emits).
    - `multi-currency-row-shows-eur-shadow`: seed a USD-priced approved series; assert HTML contains both the USD primary text and the EUR shadow text.
    - `transfers-section-collapsed-by-default`: assert the transfers section's `&lt;details&gt;` is not `open`.
    - `cross-user-empty`: log in as user B; assert the rendered HTML shows zero series for user A's data.
    - `confidence-low-renders-dim-text`: seed an approved series with `next_expected_confidence_low=true`; assert the rendered next-expected element has the dim/italic class (look for a `data-confidence-low="true"` attribute or the Tailwind class string).

    Run `composer test && composer analyse` to verify the full suite stays green.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Recurring/tests/Feature/FixedPaymentsViewQueryTest.php Modules/Recurring/tests/Feature/RecurringPageTest.php tests/Contracts/BoundaryArchTest.php --stop-on-failure &amp;&amp; composer analyse</automated>
  </verify>
  <done>
    - `Modules/Recurring/Public/Services/FixedPaymentsViewQuery.php` exists with the three required methods (`viewForUser`, `topByMonthlyEquivalent`, `monthlyEquivalentTotals`).
    - `Modules/Recurring/Internal/Http/Livewire/RecurringPage.php` exists with `final class extends Component` and no constructor (method-parameter DI only — verified with `grep -c 'public function __construct' Modules/Recurring/Internal/Http/Livewire/RecurringPage.php` returning 0).
    - `Modules/Recurring/Routes/web.php` contains the route `Route::get('/recurring', RecurringPage::class)`.
    - `Modules/Recurring/Resources/views/livewire/recurring-page.blade.php` exists.
    - All six `FixedPaymentsViewQueryTest` slices and all six `RecurringPageTest` slices pass.
    - The N+1 budget assertion (`n-plus-one-budget` slice) confirms ≤ 3 queries for N≥10 series.
    - `composer test && composer analyse && composer format:check` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Income detector → threshold-filtered transactions | The €2000 threshold prevents small refunds from polluting the income series surface — a misclassified business inflow is the threat. |
| FixedPaymentsViewQuery → ChainLinkQuery (cross-module read) | Reading chain link data goes through the Public surface; the `crossModuleAccessGoesThroughPublic` arch invariant from Plan 01 enforces this. |
| /recurring HTTP route → Livewire SFC | Authenticated user opens the page; every render-time query scopes by `user_id`; cross-user reads return empty (no 404 needed for list pages — empty list is the safe default). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Information disclosure | RecurringPage rendering cross-user series via a missing user_id scope in FixedPaymentsViewQuery | mitigate | Every query method scopes by `user_id`; cross-user-empty feature test asserts this directly for both methods. |
| T-08-17 | Tampering | Income detector writing to transactions table | mitigate | `noTransactionWritesFromRecurring` arch invariant from Plan 01 scans the income detector file. |
| T-08-18 | Denial of service | N+1 explosion on `/recurring` with 100s of series | mitigate | `FixedPaymentsViewQuery` uses LEFT JOIN + a single batch lookup for merchant memories (RESEARCH Q6); the n-plus-one-budget feature test enforces ≤ 3 queries for N≥10 series. |
| T-08-19 | Information disclosure | Income series leaking employer IBAN to the UI | accept | IBAN is the user's own banking data; rendering on the user's own private dashboard is the intended UX (Pitfall 6). The localhost-only PLT-01 invariant prevents external exposure. |
| T-08-20 | Spoofing | A foreign user opening /recurring sees their own empty page | accept | Authenticated route; empty-list default is safe; cross-user-empty feature test confirms. |
</threat_model>

<verification>
- `vendor/bin/pest --filter='Recurring' --parallel --stop-on-failure` exits 0 (full Recurring suite green: income detector + 6 query tests + 6 page tests + all prior plan tests still pass).
- `vendor/bin/pest tests/Contracts/RecurringDetectionContractTest.php` exits 0 (the end-to-end contract test now asserts both expense AND income expectations across the full 11-fixture corpus).
- `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` exits 0 (all five Recurring arch invariants pass + cross-module reads via ChainLinkQuery + MerchantMemoryQuery confirm Public-only access).
- `composer test && composer analyse && composer format:check` exits 0.
- The N+1 budget assertion enforces ≤ 3 queries for the `/recurring` page payload.
</verification>

<success_criteria>
- LED-06 satisfied: recurring income is detected by the same algorithm as recurring expense; income clusters survive multi-IBAN payroll (two distinct series); income below the €2000 threshold is dropped silently.
- REC-04 partial: `/recurring` shows approved expense + income series with name, latest amount, monthly equivalent, funding-chain icon (with D-829 fallback), and next-expected-charge text. (Drill-in + dashboard card lands in Plan 05.)
- D-818/D-819/D-820/D-821/D-822/D-826/D-829/D-840/D-852 are all encoded in shipped code.
- The `RecurringPage` Livewire SFC follows the method-parameter DI pattern (no constructor); the `noSynchronousDetectionInRequestLifecycle` arch test stays green.
- `FixedPaymentsViewQuery` is a Public surface that Plan 05's dashboard card consumes without any further query changes.
</success_criteria>

<output>
Create `.planning/phases/08-recurring-detection-fixed-payments-view/08-04-SUMMARY.md` when done. Record:
- The exact SQL plan FixedPaymentsViewQuery emits (capture via `DB::listen` log) so Phase 9/10 can review for future optimisation.
- Confirmation that both ExpenseSeriesDetector and IncomeSeriesDetector are tagged 'recurring.detector' (verify with `grep -c 'IncomeSeriesDetector' Modules/Recurring/Providers/RecurringServiceProvider.php` returning ≥ 2 — one in the use block, one in the tag call).
- Any deviations from D-829 chain fallback (if executor finds the correlated subquery too slow on SQLite, they may switch to a per-row fallback lookup — document the trade and the new query count).
- That the `tests/Contracts/RecurringDetectionContractTest.php` now asserts the FULL Wave 0 fixture corpus including income fixtures.
</output>
