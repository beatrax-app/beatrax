---
phase: 08-recurring-detection-fixed-payments-view
plan: 05
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
  - 08-04
files_modified:
  - Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php
  - Modules/Recurring/Internal/Http/Livewire/FixedPaymentsCard.php
  - Modules/Recurring/Internal/Http/Livewire/RecurringPage.php
  - Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php
  - Modules/Recurring/Public/Services/RecurringSeriesQuery.php
  - Modules/Recurring/Public/Dto/RecurringSeriesAmountTrendDto.php
  - Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php
  - Modules/Recurring/Resources/views/livewire/fixed-payments-card.blade.php
  - Modules/Recurring/Resources/views/livewire/recurring-page.blade.php
  - Modules/Recurring/Resources/views/livewire/recurring-review-page.blade.php
  - Modules/Recurring/Routes/web.php
  - Modules/Recurring/Providers/RecurringServiceProvider.php
  - Modules/Recurring/tests/Feature/RecurringSeriesDetailPageTest.php
  - Modules/Recurring/tests/Feature/FixedPaymentsCardTest.php
  - Modules/Recurring/tests/Feature/RecurringPageReDetectTest.php
  - Modules/Recurring/tests/Feature/RecurringReviewPageBulkActionsTest.php
  - Modules/Recurring/tests/Feature/TopNavBadgeComposerTest.php
  - Modules/Recurring/Public/Actions/EditRecurringSeriesVarianceTolerance.php
  - Modules/Recurring/tests/Feature/EditRecurringSeriesVarianceToleranceTest.php
autonomous: true
requirements:
  - REC-04
  - REC-05
  - UI-03

user_setup: []

must_haves:
  truths:
    - "User can open `/recurring/series/{id}` while authenticated and see a full ApexCharts line/bar chart of amount-over-time (original-currency primary + EUR shadow when distinct) PLUS a table of every occurrence linking to its underlying transaction; cross-user IDs return 404 (UI-03 / REC-05, D-827)"
    - "Approved series have their metrics re-evaluated on every sweep — amount / monthly-equivalent / latest-funding-chain / next-expected-charge / variance-tolerance refresh in place WITHOUT touching state; only a cadence-class flip pushes the series to `cadence_changed` where it waits for explicit re-approval (D-804)"
    - "Dashboard surfacing of recurring suggestions is split between a top-nav `Recurring` pending-count badge AND an inline dashboard card — the user never has to visit `/recurring/review` to know there is something to triage (D-809)"
    - "Dashboard `/` renders an inline 'Fixed monthly payments' card with the top 6 approved series by monthly equivalent + a 'View all →' link to `/recurring`; the card has an 'All series / This month only' toggle (D-835/D-838) persisted via `#[Url]` query string"
    - "Top-nav `Recurring` badge shows the pending-suggestion count via View Factory composer (no `view()` helper) — issue #12 fix pattern from Phase 5"
    - "The Phase 5 'Next ICS settlement' dashboard tile stays as-is — recurring transfers are a separate context and the two tiles do not merge (D-837)"
    - "Bulk Approve / Bulk Reject sticky action bar on `/recurring/review` approves/rejects N selected series in one click via row checkboxes + dispatches a 10-second Undo toast — essential after the first big backfill when ~20 suggestions land at once (D-812)"
    - "'Re-detect now' button on `/recurring` dispatches the same `DetectRecurringSeriesJob` synchronously (spam-clicks are no-ops via `ShouldBeUniqueUntilProcessing`); user sees a 'detecting…' toast"
    - "FX drift on multi-currency series is shown ONLY in the drill-in chart (EUR shadow line beside the original-currency line) — it is never rendered on the `/recurring` list, keeping the calm aesthetic intact (D-842)"
    - "End-to-end `composer test && composer analyse && composer format:check && npm run build` all green; the full Phase 8 demo runs"
    - "User can change a series' `variance_tolerance_percent` via the dropdown on `/recurring/series/{id}` (steps 10% / 25% / 50%); the choice persists to the row and is honoured by the next detector sweep; cross-user IDs return 404 (D-825 / D-854)"
  artifacts:
    - path: Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php
      provides: /recurring/series/{id} Livewire SFC with ApexCharts dataset + occurrences table + cross-user 404 + amount-trend rendering
      contains: "RecurringSeriesDetailPage"
    - path: Modules/Recurring/Internal/Http/Livewire/FixedPaymentsCard.php
      provides: Dashboard inline card — top 6 series sourced from FixedPaymentsViewQuery; All series / This month only toggle
      contains: "FixedPaymentsCard"
    - path: Modules/Recurring/Public/Services/RecurringSeriesQuery.php
      provides: occurrencesForSeries(int seriesId, User user) + amountTrendForSeries(int seriesId, User user) — methods added in this plan
      contains: "occurrencesForSeries"
    - path: Modules/Recurring/Providers/RecurringServiceProvider.php
      provides: Top-nav badge composer + dashboard card View composer via View Factory contract; FixedPaymentsCard registered as Livewire component
      contains: "registerTopNavBadgeComposer"
    - path: Modules/Recurring/Routes/web.php
      provides: GET /recurring/series/{seriesId} route
      contains: "recurring.series.show"
    - path: Modules/Recurring/Public/Actions/EditRecurringSeriesVarianceTolerance.php
      provides: Public Action — cross-user 404 + idempotent no-op (same target percent) + direct metric-style write to `variance_tolerance_percent` (no state transition per Pitfall 3)
      contains: "EditRecurringSeriesVarianceTolerance"
    - path: Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php
      provides: Drill-in header includes a Flux dropdown for variance tolerance (10% / 25% / 50%) wired to `editVarianceTolerance` action
      contains: "editVarianceTolerance"
  key_links:
    - from: Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php
      to: window.ApexCharts (initialized via Alpine x-init from Blade JSON injection)
      via: $apexOptions array → @json blade → x-init `new ApexCharts(...)` call
      pattern: "ApexCharts"
    - from: Modules/Recurring/Providers/RecurringServiceProvider.php
      to: core::livewire.top-nav view composer (pending count) + core::livewire.dashboard view composer (fixed-payments card)
      via: ViewFactoryContract::composer (resolved via $this->app->make(ViewFactoryContract::class) — never view() helper)
      pattern: "ViewFactoryContract"
    - from: Modules/Recurring/Internal/Http/Livewire/RecurringPage.php
      to: DetectRecurringSeriesJob
      via: re-detect action dispatches the job through injected Dispatcher
      pattern: "DetectRecurringSeriesJob::dispatch"
    - from: Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php
      to: ApproveRecurringSeries + RejectRecurringSeries (bulk versions)
      via: method-parameter DI on bulkApprove / bulkReject action methods
      pattern: "bulkApprove"
    - from: Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php
      to: EditRecurringSeriesVarianceTolerance (Public Action)
      via: method-parameter DI on `editVarianceTolerance` action method
      pattern: "EditRecurringSeriesVarianceTolerance"
---

<objective>
Ship the final Phase 8 vertical slice: the `/recurring/series/{id}` drill-in page with full ApexCharts amount-over-time chart + occurrences table (UI-03 + REC-05), the dashboard inline "Fixed monthly payments" card (D-835/D-838), the top-nav Recurring pending-suggestion badge (View Factory composer per Phase 5 issue #12 fix), the "Re-detect now" button + "detecting…" toast (D-805), and the bulk Approve / Bulk Reject sticky action bar on `/recurring/review` (D-812) with 10-second Undo toast (D-814).

Purpose: Close every remaining REC-04 / REC-05 / UI-03 requirement, plus the dashboard integration and the bulk-action UX that Pitfall 7 (wizard-time backfill overload) requires. After this plan, the full Phase 8 demo runs end-to-end.

Output: Drill-in page + dashboard card + top-nav badge + re-detect button + bulk actions + Undo toasts. Plan signals Phase 8 ready for verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-VALIDATION.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-04-SUMMARY.md
@CLAUDE.md
@Modules/Recurring/Internal/Http/Livewire/RecurringPage.php
@Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php
@Modules/Recurring/Public/Services/FixedPaymentsViewQuery.php
@Modules/Recurring/Public/Services/RecurringSeriesQuery.php
@Modules/Recurring/Public/Dto/RecurringSeriesAmountTrendDto.php
@Modules/Recurring/Public/Dto/RecurringOccurrenceDto.php
@Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php
@Modules/Recurring/Public/Actions/ApproveRecurringSeries.php
@Modules/Recurring/Public/Actions/RejectRecurringSeries.php
@Modules/Recurring/Providers/RecurringServiceProvider.php
@Modules/Chains/Providers/ChainsServiceProvider.php
@Modules/Chains/Internal/Http/Livewire/ChainDrawer.php
@Modules/Chains/Internal/Http/Livewire/ChainReviewQueue.php
@resources/js/app.js
@resources/views/components/apex-chart-smoke.blade.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drill-in page (/recurring/series/{id}) with ApexCharts chart + occurrences table + RecurringSeriesAmountTrendDto wiring + drill-in tests</name>
  <files>
    Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php,
    Modules/Recurring/Public/Services/RecurringSeriesQuery.php,
    Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php,
    Modules/Recurring/Routes/web.php,
    Modules/Recurring/Providers/RecurringServiceProvider.php,
    Modules/Recurring/Resources/views/livewire/recurring-page.blade.php,
    Modules/Recurring/tests/Feature/RecurringSeriesDetailPageTest.php
  </files>
  <read_first>
    Modules/Chains/Internal/Http/Livewire/ChainDrawer.php (the closest in-repo drill-in pattern — Livewire SFC that takes a route param and pulls a tree-shaped DTO; copy its DI shape and route-binding pattern),
    Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php (Plan 03 — the method-parameter DI + render() with ViewFactory shape every Livewire SFC in this module uses),
    Modules/Recurring/Public/Services/RecurringSeriesQuery.php (Plan 03 — extend with two methods this plan needs),
    Modules/Recurring/Public/Dto/RecurringSeriesAmountTrendDto.php (Plan 02 — the DTO shape the chart consumes; the `points` list type must match what ApexCharts expects),
    Modules/Recurring/Public/Dto/RecurringOccurrenceDto.php (Plan 02 — the occurrence row shape the table consumes),
    resources/js/app.js (Plan 01 — confirm `window.ApexCharts` is exposed; the Blade view's Alpine `x-init` calls `new window.ApexCharts(...)`),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md "Pattern 7: Drill-in Chart" lines 701–732 (the Blade JSON injection + Alpine init template),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-827 (full ApexCharts visualisation — line/bar chart with original-currency primary + EUR shadow when distinct + drift markers), D-840/D-842 (FX drift shown only in drill-in chart, not on the list view's amount column), Pitfall 9 (cap points to 24 by default with view-all toggle to render full set with `sampling: true`)
  </read_first>
  <behavior>
    - `RecurringSeriesQuery::occurrencesForSeries(int $seriesId, User $user): array<RecurringOccurrenceDto>` returns every occurrence row for the series ordered by `observed_at DESC`. Cross-user lookups return an empty array (the route layer maps to 404 via the `forSeries` lookup before getting here).
    - `RecurringSeriesQuery::amountTrendForSeries(int $seriesId, User $user, int $maxPoints = 24): RecurringSeriesAmountTrendDto` returns the chart payload: a list of `{date, amount_minor, eur_amount_minor}` triples capped at the most recent `$maxPoints` per Pitfall 9. EUR amount is `null` when the original currency is EUR (no shadow needed).
    - `RecurringSeriesDetailPage` is a `final class extends Livewire\Component`:
       - Public state: `int $seriesId` (bound from route), `bool $showAllPoints = false` (the "view all" toggle from Pitfall 9 — when true, the chart re-renders with the full occurrence set instead of the 24-cap).
       - Action methods (method-parameter DI only): `toggleAllPoints()`.
       - `mount(int $seriesId, CurrentUser $currentUser, RecurringSeriesQuery $query): void` — sets `$this->seriesId` from route param; calls `$query->forSeries($seriesId, $currentUser->user())` and throws `NotFoundHttpException` if null. The mount-time cross-user 404 is the load-bearing guard.
       - `render(CurrentUser $currentUser, RecurringSeriesQuery $query, ViewFactory $views): View`:
          1. `$series = $query->forSeries($this->seriesId, $currentUser->user());` (returns `RecurringSeriesDto`; null is impossible here because mount() already enforced the 404 — but defensive `if (null) throw NotFoundHttpException` regardless).
          2. `$occurrences = $query->occurrencesForSeries($this->seriesId, $currentUser->user());`
          3. `$trend = $query->amountTrendForSeries($this->seriesId, $currentUser->user(), maxPoints: $this->showAllPoints ? 1000 : 24);`
          4. Build `$apexOptions` (PHP array → JSON in the view) with chart type `line`, x-axis dates, y-axis amounts. If any point has `eur_amount_minor !== null`, add a second series (EUR shadow, dimmer color per D-827). Drift markers at points where amount-class changes (e.g. €9.99 → €11.49 transition).
          5. `$view = $views->make('recurring::livewire.recurring-series-detail-page', ['series' => $series, 'occurrences' => $occurrences, 'apexOptions' => $apexOptions, 'showAllPoints' => $this->showAllPoints]);`
          6. `$view->extends('layouts.app', ['title' => $series->displayName().' · diederik']);`
          7. Return.
    - Route `/recurring/series/{seriesId}` binds the Livewire component behind `web` + `auth` middleware; route parameter is an integer.
    - The `noSynchronousDetectionInRequestLifecycle` arch test stays green: the detail page imports `RecurringSeriesQuery` (Public surface), NOT any `SeriesDetector` implementation.
    - The drill-in chart renders client-side via ApexCharts initialised through Alpine `x-init`. The Blade view uses `@json($apexOptions)` to safely inject the chart config; the Alpine handler reads `$el.dataset.options` and calls `new window.ApexCharts(...)`. This requires the `window.ApexCharts` global from Plan 01.
    - `/recurring` rows link to `/recurring/series/{id}` via a Livewire navigate or plain anchor — the executor adds the link to the row markup in `recurring-page.blade.php`.
    - Cross-user 404 verified by the feature test slice `--filter='cross-user-404'`.
  </behavior>
  <action>
    Extend `Modules/Recurring/Public/Services/RecurringSeriesQuery.php` (Plan 03) with two new methods:

    `public function occurrencesForSeries(int $seriesId, User $user): array`:
    1. Validate series ownership: SELECT 1 FROM recurring_series WHERE id = ? AND user_id = ? — if empty, return `[]` (the route layer's mount() enforces 404 above).
    2. Query `recurring_series_occurrences` WHERE recurring_series_id = ? AND user_id = ? ORDER BY observed_at DESC.
    3. Map rows to `RecurringOccurrenceDto`. Construct `Money::ofMinor($observed_amount_minor, $observed_currency)`.
    4. Return `array<RecurringOccurrenceDto>`.

    `public function amountTrendForSeries(int $seriesId, User $user, int $maxPoints = 24): RecurringSeriesAmountTrendDto`:
    1. Same ownership validation.
    2. Load the series to get currency + cluster_key + cadence.
    3. Query occurrences (same scope) ORDER BY observed_at DESC LIMIT $maxPoints.
    4. Reverse to ascending (chart x-axis is left-to-right oldest-to-newest).
    5. For each occurrence, JOIN against `transactions` (read-only) to pull `fx_rate_used` and `settled_amount_minor` for the EUR shadow.
    6. Build `points` list: `[ ['date' => 'YYYY-MM-DD', 'amount_minor' => int, 'eur_amount_minor' => int|null], ... ]`. Set `eur_amount_minor` to null when `original_currency === 'EUR'` (no shadow needed).
    7. Return `new RecurringSeriesAmountTrendDto(seriesId: $seriesId, currency: $latestCurrency, points: $points, maxPoints: $maxPoints)`.

    Create `Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php` per the analog `ChainDrawer` adapted to the method-parameter DI pattern from `RecurringReviewPage`. `final class extends Component`. Public state: `int $seriesId = 0`, `bool $showAllPoints = false`.

    Implement `mount(int $seriesId, CurrentUser $currentUser, RecurringSeriesQuery $query): void`:
    `$this->seriesId = $seriesId; $series = $query->forSeries($seriesId, $currentUser->user()); if ($series === null) { throw new NotFoundHttpException('Recurring series not found.'); }`

    Implement `public function toggleAllPoints(): void { $this->showAllPoints = ! $this->showAllPoints; }`.

    Implement `render(CurrentUser $currentUser, RecurringSeriesQuery $query, ViewFactory $views): View` per the behavior section above. Build the `$apexOptions` array carefully — match ApexCharts' expected schema for a line chart with optional secondary series. Use the official docs (https://apexcharts.com/docs/chart-types/line-chart/) for the option keys: `chart.type='line'`, `series=[{name,data}]`, `xaxis.type='datetime'`, `xaxis.categories=$dates`, `tooltip.shared=true`, `markers.size=4`. When any point in `$trend->points` carries a non-null `eur_amount_minor`, append a second series with `name='EUR equivalent'` and a dimmer color per D-827.

    Create `Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php`. Layout:
    - Header: series displayName + state badge + cadence chip + monthly_equivalent text + a placeholder slot for the variance tolerance editor. The editor itself (the dropdown wired to `EditRecurringSeriesVarianceTolerance`) ships in Task 4 below — this Blade view leaves a clearly-marked `{{-- variance-tolerance-editor: see Task 4 --}}` comment in the header markup so the executor of Task 4 inserts the dropdown markup at a known anchor.
    - Chart container: a div with `x-data="{ chart: null }"` and `x-init="chart = new window.ApexCharts($el.querySelector('#series-chart-{{ $series->seriesId }}'), JSON.parse($el.dataset.options)); chart.render();"` and `data-options='@json($apexOptions)'`. Inside, a div with id `series-chart-{{ $series->seriesId }}`. Guard against `window.ApexCharts` being undefined with a graceful fallback paragraph; the Plan 01 smoke component confirms it loads in production.
    - "View all points" toggle wired to `wire:click="toggleAllPoints"`.
    - Occurrences table: a Flux table with columns Date, Amount (Money formatted), Transaction link via `route('transactions.show', $occ->transactionId)` per the existing transactions detail route from Phase 3. Sort DESC by observed_at.

    Modify `Modules/Recurring/Routes/web.php`. Add inside the existing `Route::middleware(['web', 'auth'])->group(...)` block: `Route::get('/recurring/series/{seriesId}', RecurringSeriesDetailPage::class)->whereNumber('seriesId')->name('recurring.series.show');`. Add the corresponding `use Modules\Recurring\Internal\Http\Livewire\RecurringSeriesDetailPage;` import.

    Modify `Modules/Recurring/Providers/RecurringServiceProvider.php` `boot()`: add `$livewire->component('recurring.recurring-series-detail-page', RecurringSeriesDetailPage::class);`.

    Modify `Modules/Recurring/Resources/views/livewire/recurring-page.blade.php` (Plan 04): wrap each row's display name in an anchor pointing at `route('recurring.series.show', $row->seriesId)` so the user can click into the drill-in from the list view.

    Create `Modules/Recurring/tests/Feature/RecurringSeriesDetailPageTest.php` using Livewire's testing harness:
    - `renders-for-owner`: seed an approved series with 12 occurrences for user A; log in as A; open `/recurring/series/{id}`; assert HTTP 200 + HTML contains the series displayName + chart container + the occurrence count.
    - `cross-user-404`: seed series for user A; log in as user B; open `/recurring/series/{id}`; assert HTTP 404.
    - `chart-dataset`: assert the rendered HTML contains the `data-options` attribute with a valid JSON-encoded ApexCharts config (parse it back and assert `chart.type === 'line'` + the `series` array has the expected length).
    - `eur-shadow-renders-for-usd-series`: seed a USD-priced series; assert the JSON-encoded options array contains TWO series (the primary USD line + the EUR shadow).
    - `view-all-toggle-expands-points`: assert that calling `$component->call('toggleAllPoints')` flips `$showAllPoints` and the next render fetches with a higher `maxPoints`.
    - `occurrences-table-links-to-transaction`: assert the rendered HTML contains the `route('transactions.show', $transactionId)` URL for each occurrence row.

    Run `composer test` and `composer analyse` to verify the suite stays green.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Recurring/tests/Feature/RecurringSeriesDetailPageTest.php tests/Contracts/BoundaryArchTest.php --stop-on-failure &amp;&amp; npm run build &amp;&amp; composer analyse</automated>
  </verify>
  <done>
    - `Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php` exists with `final class extends Component`, `mount()` enforcing cross-user 404. Verify `grep -c 'final class RecurringSeriesDetailPage' Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php` returns 1.
    - `Modules/Recurring/Public/Services/RecurringSeriesQuery.php` contains the literal substrings `occurrencesForSeries` and `amountTrendForSeries`.
    - `Modules/Recurring/Routes/web.php` contains the literal substring `recurring.series.show`.
    - `Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php` exists.
    - `Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php` contains the literal anchor comment `variance-tolerance-editor: see Task 4` in the header markup (verify `grep -c 'variance-tolerance-editor: see Task 4' Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php` returns 1). Task 4 replaces it with the live dropdown.
    - `vendor/bin/pest Modules/Recurring/tests/Feature/RecurringSeriesDetailPageTest.php` exits 0 (all six tests pass).
    - `npm run build` exits 0 (Vite bundle still compiles with ApexCharts present from Plan 01).
    - `composer test && composer analyse` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dashboard inline FixedPaymentsCard + top-nav Recurring badge + View Factory composers + tests</name>
  <files>
    Modules/Recurring/Internal/Http/Livewire/FixedPaymentsCard.php,
    Modules/Recurring/Resources/views/livewire/fixed-payments-card.blade.php,
    Modules/Recurring/Providers/RecurringServiceProvider.php,
    Modules/Recurring/tests/Feature/FixedPaymentsCardTest.php,
    Modules/Recurring/tests/Feature/TopNavBadgeComposerTest.php
  </files>
  <read_first>
    Modules/Chains/Providers/ChainsServiceProvider.php lines 122–154 (registerTopNavBadgeComposer — the issue #12 fix that uses `$this->app->make(ViewFactoryContract::class)` instead of the `view()` helper; verbatim template),
    Modules/Recurring/Internal/Http/Livewire/RecurringPage.php (Plan 04 — method-parameter DI pattern; FixedPaymentsCard uses the same shape),
    Modules/Recurring/Public/Services/FixedPaymentsViewQuery.php (Plan 04 — `topByMonthlyEquivalent` returns the card payload),
    Modules/Recurring/Public/Services/RecurringSeriesQuery.php (Plan 03 — `pendingCountForUser` returns the top-nav badge integer),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-835 (card top ~6 series + View-all link), D-838 (All series / This month only toggle), D-855 (planner-picks: persist via `#[Url]` query string per Phase 3 D-44 precedent — pick `#[Url]`),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md "Pattern S-5: View Factory contract for composers"
  </read_first>
  <behavior>
    - `FixedPaymentsCard` is a `final class extends Livewire\Component` with method-parameter DI.
       - Public state: `#[Url(as: 'fp-filter')] string $filter = 'all';` — `'all'` (every approved series, monthly equivalent normalised) or `'this-month'` (filter to series whose `next_expected_at` falls in the current month per D-838). The `#[Url]` attribute makes the filter shareable via URL + persists across page refresh per D-855.
       - Action methods: `setFilter(string $filter): void` toggles between `'all'` and `'this-month'`.
       - `render(CurrentUser $currentUser, FixedPaymentsViewQuery $query, ViewFactory $views, Clock $clock): View`:
          1. `$user = $currentUser->user();`
          2. `$rows = $query->topByMonthlyEquivalent($user, limit: 6);`
          3. If `$this->filter === 'this-month'`: PHP-side filter on `$rows` keeping only series whose `nextExpectedAt` falls in the current month (`$clock->now()->startOfMonth()` to `endOfMonth()`).
          4. `$totals = $query->monthlyEquivalentTotals($user);`
          5. `$view = $views->make('recurring::livewire.fixed-payments-card', ['rows' => $rows, 'totals' => $totals, 'filter' => $this->filter]);`
          6. Return.
    - The card renders inside the existing `/` dashboard layout. Inject the component via the existing dashboard Blade using `@livewire('recurring.fixed-payments-card')`.
    - The top-nav badge composer mirrors `ChainsServiceProvider::registerTopNavBadgeComposer` line-for-line: resolve `ViewFactoryContract` via `$this->app->make()`, attach a composer to `core::livewire.top-nav`, inside the closure resolve `CurrentUser` and `RecurringSeriesQuery`, call `$query->pendingCountForUser($user)`, set the variable `$recurringPendingCount` on the view.
    - `view()` global helper is FORBIDDEN (Phase 5 issue #12 fix); the executor MUST use `$this->app->make(ViewFactoryContract::class)` instead. The arch test in Plan 01 catches violations.
    - The card has a "View all →" link to `route('recurring.index')`.
    - Each card row clicks through to the drill-in `/recurring/series/{id}` (link added in Task 1).
    - Feature tests: card renders for the owner, cross-user empty list, filter toggle changes the row set, top-nav badge integer matches the pending count from the query.
  </behavior>
  <action>
    Create `Modules/Recurring/Internal/Http/Livewire/FixedPaymentsCard.php`. `final class extends Component`. Use `Livewire\Attributes\Url`. Public property: `#[Url(as: 'fp-filter')] public string $filter = 'all';`. Action method `setFilter(string $filter): void` whitelists `['all', 'this-month']` and falls back to `'all'` for any other input. `render` signature: `render(CurrentUser $currentUser, FixedPaymentsViewQuery $query, ViewFactory $views, Clock $clock): View` per the behaviour section.

    Create `Modules/Recurring/Resources/views/livewire/fixed-payments-card.blade.php`. A calm card layout:
    - Card header: "Fixed monthly payments" title + totals chip showing expense_eur and income_eur from totals.
    - Filter toggle: two pill buttons (`All series` / `This month only`) — `wire:click="setFilter('all')"` and `wire:click="setFilter('this-month')"`. The active pill carries a class indicating selection (Flux tab.group or similar).
    - List of up to 6 rows: each row shows displayName as a link to `route('recurring.series.show', $row->seriesId)`, monthlyEquivalent (EUR formatted), direction badge (expense / income), funding-chain mini-icon (when present).
    - Footer: "View all →" link to `route('recurring.index')`.

    Modify `Modules/Recurring/Providers/RecurringServiceProvider.php`. In `register()` add `$this->app->singleton(FixedPaymentsCard::class);`. In `boot()` add `$livewire->component('recurring.fixed-payments-card', FixedPaymentsCard::class); $this->registerTopNavBadgeComposer(); $this->registerDashboardCardComposer();`.

    Implement two private methods on the provider, mirroring `ChainsServiceProvider::registerTopNavBadgeComposer` lines 139–154:

    `registerTopNavBadgeComposer(): void` — capture `$app = $this->app;`, resolve `$factory = $app->make(ViewFactoryContract::class);`, then call `$factory->composer('core::livewire.top-nav', static function (View $compose) use ($app): void { ... })`. Inside the closure resolve `CurrentUser`; if not authenticated, set `$compose->with('recurringPendingCount', 0);` and return; otherwise resolve `RecurringSeriesQuery` and set `$compose->with('recurringPendingCount', $query->pendingCountForUser($currentUser->user()));`.

    `registerDashboardCardComposer(): void` — empty/no-op for Phase 8 with a docblock explaining that the dashboard Blade injects the card via `@livewire('recurring.fixed-payments-card')` directly, so no composer wiring is needed. The method exists as a placeholder for Phase 9/10 to populate if cross-card data injection becomes necessary.

    Note on dashboard integration: render the card by ADDING a `@livewire('recurring.fixed-payments-card')` Blade directive to the existing dashboard view. Identify the exact dashboard view file by reading `Modules/Core/Resources/views/livewire/dashboard.blade.php` (or wherever `/` resolves — confirm via `routes/web.php` from Phase 1). Insert the directive adjacent to the existing "this month at a glance" tile + "Next ICS settlement" tile (per D-837 — existing tiles stay untouched).

    The executor MUST also update the top-nav Blade view (`Modules/Core/Resources/views/livewire/top-nav.blade.php` or equivalent — confirm by reading the file referenced from `ChainsServiceProvider::registerTopNavBadgeComposer`) to render the `Recurring` nav slot with a badge using `{{ $recurringPendingCount ?? 0 }}`. Pattern: an anchor with `href="{{ route('recurring.index') }}"`, the text `Recurring`, followed by a Flux badge wrapped in `@if ($recurringPendingCount > 0) ... @endif`. Position adjacent to the existing `Chains review` badge anchor (D-853 — UI-SPEC pass owns positioning).

    Create `Modules/Recurring/tests/Feature/FixedPaymentsCardTest.php`:
    - `renders-top-six`: seed 10 approved series for user A; log in as A; mount the FixedPaymentsCard component via Livewire's testing harness; assert exactly 6 rows are returned ordered DESC by monthly_equivalent.
    - `cross-user-empty`: seed approved series for user A; log in as B; assert card is empty.
    - `this-month-filter`: seed two approved series — one with `next_expected_at` in the current month, one in next month; set `$filter = 'this-month'`; assert only the current-month row appears.
    - `view-all-link-points-at-recurring-index`: assert the rendered HTML contains the literal `route('recurring.index')` URL.
    - `url-attribute-binds-filter`: set `$component->set('filter', 'this-month')`; assert the URL query string serialised by Livewire's testing harness contains `fp-filter=this-month`.

    Create `Modules/Recurring/tests/Feature/TopNavBadgeComposerTest.php`:
    - `badge-equals-pending-count`: seed 3 pending series + 1 approved series + 1 rejected series for user A; log in as A; render the top-nav view; assert `$recurringPendingCount === 3`.
    - `badge-is-zero-when-unauthenticated`: don't log in; render the top-nav view; assert `$recurringPendingCount === 0`.
    - `badge-is-zero-when-no-pending`: log in as a user with zero pending series; assert `$recurringPendingCount === 0`.
    - `no-view-helper-used`: assert `file_get_contents(base_path('Modules/Recurring/Providers/RecurringServiceProvider.php'))` does NOT contain the string `view()->composer`.

    Run `composer test && composer analyse && composer format:check`.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Recurring/tests/Feature/FixedPaymentsCardTest.php Modules/Recurring/tests/Feature/TopNavBadgeComposerTest.php tests/Contracts/BoundaryArchTest.php --stop-on-failure &amp;&amp; composer analyse</automated>
  </verify>
  <done>
    - `Modules/Recurring/Internal/Http/Livewire/FixedPaymentsCard.php` exists with `#[Url(as: 'fp-filter')]` attribute.
    - `Modules/Recurring/Resources/views/livewire/fixed-payments-card.blade.php` exists.
    - `Modules/Recurring/Providers/RecurringServiceProvider.php` contains a `registerTopNavBadgeComposer` private method using `$this->app->make(ViewFactoryContract::class)`. Verify `grep -c 'ViewFactoryContract' Modules/Recurring/Providers/RecurringServiceProvider.php` returns ≥ 1; verify forbidden helper absent with `grep -c 'view()->composer' Modules/Recurring/Providers/RecurringServiceProvider.php` returns 0.
    - Top-nav Blade includes a `Recurring` link with `{{ $recurringPendingCount }}` badge variable.
    - Dashboard Blade renders `@livewire('recurring.fixed-payments-card')`.
    - All five `FixedPaymentsCardTest` slices + all four `TopNavBadgeComposerTest` slices pass.
    - `composer test && composer analyse && composer format:check` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bulk action bar on /recurring/review + Re-detect button on /recurring + closing tests</name>
  <files>
    Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php,
    Modules/Recurring/Internal/Http/Livewire/RecurringPage.php,
    Modules/Recurring/Resources/views/livewire/recurring-review-page.blade.php,
    Modules/Recurring/Resources/views/livewire/recurring-page.blade.php,
    Modules/Recurring/tests/Feature/RecurringReviewPageBulkActionsTest.php,
    Modules/Recurring/tests/Feature/RecurringPageReDetectTest.php,
    tests/Contracts/RecurringDetectionContractTest.php
  </files>
  <read_first>
    Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php (Plan 03 — extend with bulkApprove / bulkReject action methods using method-parameter DI),
    Modules/Recurring/Internal/Http/Livewire/RecurringPage.php (Plan 04 — extend with reDetect action method that dispatches DetectRecurringSeriesJob via injected Dispatcher),
    Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php (Plan 03 — the job's ShouldBeUniqueUntilProcessing lock makes spam-clicks safe),
    Modules/Chains/Internal/Http/Livewire/ChainReviewQueue.php (the bulk-action prior art if Phase 5 shipped one — check for a `$selectedIds` pattern; if absent, follow the RESEARCH Pattern 5 template lines 657–688),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-805 (re-detect dispatches same job, "detecting…" toast), D-812 (bulk Approve / Reject sticky action bar), D-814 (10-second Undo toast on every action),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md Pitfall 7 (wizard-time backfill overload — bulk approve is the mitigation)
  </read_first>
  <behavior>
    - `RecurringReviewPage` gains two new action methods (extending Plan 03's `approve`/`reject`/`snooze`/`editName`/`unReject`):
       - `bulkApprove(CurrentUser $currentUser, ApproveRecurringSeries $action): void` — loops `$this->selectedIds`, calls `($action)((int) $id, $user)` for each, clears `$selectedIds`, dispatches a single Undo toast `"{$count} approved"`.
       - `bulkReject(CurrentUser $currentUser, RejectRecurringSeries $action): void` — same shape, calls `RejectRecurringSeries`.
       - Cross-user 404 from the underlying Public Actions is caught + the id is skipped silently (calm UX — a partially-poisoned select must not break the entire batch). Document this in the method docblock.
       - The Blade view adds the sticky action bar visible only when `count($selectedIds) > 0` (calm UX — the bar appears only when the user has actually selected rows).
    - `RecurringPage` gains a `reDetect(CurrentUser $currentUser, Dispatcher $bus): void` action method:
       - Dispatches `new DetectRecurringSeriesJob($currentUser->user()->id)` via the injected `Dispatcher` (the job's `ShouldBeUniqueUntilProcessing` lock makes spam-clicks safe — repeated clicks during an in-flight job are collapsed into the existing queued pass).
       - Dispatches a toast with message `'Detecting recurring series…'`.
       - The Blade view adds a "Re-detect now" button at the top of `/recurring` wired to `wire:click="reDetect"`.
    - The `--filter='bulk-approve'` slice from VALIDATION.md feature-test mapping passes: seed N pending series (test N=20 per Pitfall 7); select all via the bulk-action bar; assert all N flip to approved + transitions rows are written.
    - The Re-detect button stays compliant with `noSynchronousDetectionInRequestLifecycle`: the `reDetect` action imports `DetectRecurringSeriesJob` (a Job class, not a `SeriesDetector` directly), so the arch test stays green — verify after the change.
    - The contract test in `tests/Contracts/RecurringDetectionContractTest.php` is updated to dispatch the full sweep against the entire 11-fixture corpus + the real-export stub (the stub returns an empty `transactions` list so it's a no-op), confirming the full end-to-end Phase 8 pipeline produces the expected series set + cadences + metrics.
  </behavior>
  <action>
    Modify `Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php` (Plan 03). Add two bulk action methods:

    `bulkApprove(CurrentUser $currentUser, ApproveRecurringSeries $action): void` — captures `$user = $currentUser->user();` and `$applied = 0;`; iterates `$this->selectedIds`; for each id calls `($action)((int) $id, $user);` inside a try/catch that swallows `NotFoundHttpException` to skip foreign-user ids silently; increments `$applied` on each success; resets `$this->selectedIds = [];` after the loop; dispatches `$this->dispatch('toast', message: "{$applied} approved", undoAction: 'bulkUndo', undoPayload: null);`.

    `bulkReject(CurrentUser $currentUser, RejectRecurringSeries $action): void` — identical shape calling `RejectRecurringSeries` with the toast message `"{$applied} rejected"`.

    Modify `Modules/Recurring/Resources/views/livewire/recurring-review-page.blade.php`. Add:
    - Checkboxes per row: `<input type="checkbox" wire:model.live="selectedIds" value="{{ $row->seriesId }}">`. The `.live` modifier keeps the bulk-action bar reactive.
    - Sticky bottom bar visible when `@if (count($selectedIds) > 0)`: contains "Approve N" and "Reject N" buttons wired to `wire:click="bulkApprove"` and `wire:click="bulkReject"` with the N count interpolated.
    - Reuse the Plan 03 toast handler.

    Modify `Modules/Recurring/Internal/Http/Livewire/RecurringPage.php` (Plan 04). Add a `reDetect(CurrentUser $currentUser, Dispatcher $bus): void` method that calls `$bus->dispatch(new DetectRecurringSeriesJob($currentUser->user()->id));` and then `$this->dispatch('toast', message: 'Detecting recurring series…', undoAction: '', undoPayload: null);`. Add `use Modules\Recurring\Internal\Jobs\DetectRecurringSeriesJob;` and `use Illuminate\Contracts\Bus\Dispatcher;` imports.

    Modify `Modules/Recurring/Resources/views/livewire/recurring-page.blade.php` (Plan 04). Add at the top of the page (above the net-flow summary): a Flux ghost button wired with `wire:click="reDetect"` and label "Re-detect now".

    Update `tests/Contracts/RecurringDetectionContractTest.php`. The contract test from Plan 03 (extended in Plan 04 for income) is extended once more: assert the FULL end-to-end run against the entire Wave 0 corpus including the empty real-export stub. Add a final assertion that re-running the job twice produces no duplicates (idempotency across the full corpus, not just per-fixture as in Plan 03 — this is the load-bearing Phase 8 end-to-end integrity gate).

    Create `Modules/Recurring/tests/Feature/RecurringReviewPageBulkActionsTest.php`:
    - `bulk-approve`: seed 20 pending series for user A; log in as A; mount `RecurringReviewPage`; set `selectedIds` to all 20 ids; call `bulkApprove`; assert all 20 series have `state='approved'`, 20 `recurring_series_transitions` rows are inserted, `selectedIds` is reset to empty, and a "20 approved" toast was dispatched.
    - `bulk-reject`: same shape for reject.
    - `bulk-approve-skips-foreign-user-ids`: seed 10 series for user A + 5 for user B; log in as A; select all 15 ids; call `bulkApprove`; assert only A's 10 series flip to approved (B's 5 stay unchanged) and the toast reads "10 approved".
    - `bulk-action-bar-renders-only-when-selection-non-empty`: assert HTML contains the bar markup when `selectedIds` is non-empty and lacks it when empty.

    Create `Modules/Recurring/tests/Feature/RecurringPageReDetectTest.php`:
    - `re-detect-dispatches-job`: log in; mount `RecurringPage`; call `reDetect`; assert `Queue::fake()` recorded one `DetectRecurringSeriesJob` dispatch with the correct `userId`.
    - `re-detect-spam-click-collapses`: dispatch twice in quick succession (test via `Queue::assertPushed(DetectRecurringSeriesJob::class, 2)`); document that both pushes succeed but the queue worker's `ShouldBeUniqueUntilProcessing` lock collapses them at execution time.
    - `re-detect-toast-fires`: assert the toast dispatch event records the message `'Detecting recurring series…'`.
    - `re-detect-arch-test-still-green`: run `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noSynchronousDetectionInRequestLifecycle'` and assert exit 0.

    Run the full Phase 8 verification: `composer test && composer analyse && composer format:check && npm run build`. All four MUST exit 0.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Recurring/tests/Feature/RecurringReviewPageBulkActionsTest.php Modules/Recurring/tests/Feature/RecurringPageReDetectTest.php tests/Contracts/RecurringDetectionContractTest.php tests/Contracts/BoundaryArchTest.php --stop-on-failure &amp;&amp; composer test &amp;&amp; composer analyse &amp;&amp; composer format:check &amp;&amp; npm run build</automated>
  </verify>
  <done>
    - `Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php` contains the literal substrings `bulkApprove` and `bulkReject`.
    - `Modules/Recurring/Internal/Http/Livewire/RecurringPage.php` contains the literal substring `reDetect` and a `use ...DetectRecurringSeriesJob` import.
    - `tests/Contracts/RecurringDetectionContractTest.php` no longer carries `->skip(...)` directives and asserts the FULL Wave 0 corpus.
    - All four `RecurringReviewPageBulkActionsTest` slices + all four `RecurringPageReDetectTest` slices pass.
    - `composer test && composer analyse && composer format:check && npm run build` exits 0.
    - `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --parallel` exits 0 (all five arch invariants from Plan 01 stay green: Internal containment, no facade calls except the DetectRecurringSeriesJob carve-out, noTransactionWritesFromRecurring, noSynchronousDetectionInRequestLifecycle, noOtherRecurringSeriesStateMutator).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Variance tolerance dropdown editor on the drill-in page + EditRecurringSeriesVarianceTolerance Public Action</name>
  <files>
    Modules/Recurring/Public/Actions/EditRecurringSeriesVarianceTolerance.php,
    Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php,
    Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php,
    Modules/Recurring/Providers/RecurringServiceProvider.php,
    Modules/Recurring/tests/Feature/EditRecurringSeriesVarianceToleranceTest.php
  </files>
  <read_first>
    Modules/Recurring/Public/Actions/EditRecurringSeriesName.php (Plan 03 — the metric-style Public Action template: constructor DI, cross-user 404, direct DatabaseManager update, NO state transition, NO event; mirror this shape exactly),
    Modules/Recurring/Public/Actions/ApproveRecurringSeries.php (Plan 03 — for the cross-user 404 NotFoundHttpException pattern),
    Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php (Task 1 above — extend with the `editVarianceTolerance` action method using method-parameter DI),
    Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php (Task 1 above — find the `{{-- variance-tolerance-editor: see Task 4 --}}` anchor comment in the header and replace it with the dropdown markup),
    Modules/Recurring/Models/RecurringSeries.php (Plan 02 — `variance_tolerance_percent` is `unsignedTinyInteger` default 25; valid values are 10, 25, 50),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-825 (per-series variance_tolerance_percent default 25 — user-editable) + D-854 (planner pick: dropdown of fixed steps 10% / 25% / 50%)
  </read_first>
  <behavior>
    - `EditRecurringSeriesVarianceTolerance` is a `final class` Public Action. Constructor DI: `DatabaseManager $db`, `Clock $clock` (no state machine — this is a metric-style write per Pitfall 3; no events — Phase 9 does not listen for tolerance edits).
    - `__invoke(int $seriesId, User $user, int $newTolerancePercent): void`:
       1. Whitelist `$newTolerancePercent` against the allowed set `[10, 25, 50]`; any other value raises `InvalidArgumentException` with a calm message.
       2. Cross-user 404 guard: `RecurringSeries::query()->where('id', $seriesId)->where('user_id', $user->id)->first()`; null → throw `NotFoundHttpException('Recurring series not found.')`.
       3. Idempotent no-op: if `$series->variance_tolerance_percent === $newTolerancePercent` → return.
       4. Direct UPDATE via injected DatabaseManager: `update recurring_series set variance_tolerance_percent = ?, updated_at = ? where id = ? and user_id = ?` (the redundant `user_id` predicate is defence-in-depth — same posture as `EditRecurringSeriesName`).
       5. NO state transition (Pitfall 3 — only `state` writes go through the state machine; the `noOtherRecurringSeriesStateMutator` arch test only watches the `state` column).
       6. NO event.
    - `RecurringSeriesDetailPage` gains an `editVarianceTolerance(int $newTolerancePercent, CurrentUser $currentUser, EditRecurringSeriesVarianceTolerance $action): void` method (method-parameter DI per the existing pattern in the file). Captures `$user = $currentUser->user()`, calls `($action)($this->seriesId, $user, $newTolerancePercent)`, dispatches a small toast `"Tolerance: {$newTolerancePercent}%"` (no Undo — tolerance changes are not destructive; the user can simply pick another value).
    - The drill-in Blade view at the `{{-- variance-tolerance-editor: see Task 4 --}}` anchor in the header now renders a Flux dropdown (`<flux:dropdown>` or `<flux:select>` — confirm the exact component by reading existing drill-in/page Blades) with three options labelled `10%`, `25%`, `50%`. The current value is pre-selected from `$series->varianceTolerancePercent`. The dropdown emits `wire:click="editVarianceTolerance(10)"` etc. (or `wire:change` on a select; either pattern is fine — pick whichever the Flux dropdown component expects).
    - `EditRecurringSeriesVarianceToleranceTest` covers six slices: happy path (25 → 50 persists), idempotent no-op (same target percent → no SQL UPDATE measured via `DB::listen` query count), cross-user 404, invalid-percent rejection (e.g. `7` → `InvalidArgumentException`, row unchanged), the Livewire-component action method dispatches the toast and calls the Public Action, the next detector sweep honours the new tolerance (seed a series with tolerance=50, seed transactions that span the wider band, run `DetectRecurringSeriesJob`, assert no fragmentation).
    - `RecurringServiceProvider::register()` adds `$this->app->singleton(EditRecurringSeriesVarianceTolerance::class);`.
  </behavior>
  <action>
    Create `Modules/Recurring/Public/Actions/EditRecurringSeriesVarianceTolerance.php`. `final class`. Constructor DI:
    ```
    public function __construct(
        private readonly DatabaseManager $db,
        private readonly Clock $clock,
    ) {}
    ```
    Declare `public const ALLOWED_TOLERANCE_PERCENTS = [10, 25, 50];` so consumers (including the Blade dropdown markup builder if needed) can reference the canonical list.

    Implement `public function __invoke(int $seriesId, User $user, int $newTolerancePercent): void`:
    1. `if (! in_array($newTolerancePercent, self::ALLOWED_TOLERANCE_PERCENTS, true)) { throw new InvalidArgumentException("Variance tolerance must be one of: 10, 25, 50"); }`
    2. Load `$series = RecurringSeries::query()->where('id', $seriesId)->where('user_id', $user->id)->first();`
    3. `if ($series === null) { throw new NotFoundHttpException('Recurring series not found.'); }`
    4. `if ((int) $series->variance_tolerance_percent === $newTolerancePercent) { return; }` (idempotent no-op).
    5. `$this->db->connection()->table('recurring_series')->where('id', $seriesId)->where('user_id', $user->id)->update(['variance_tolerance_percent' => $newTolerancePercent, 'updated_at' => $this->clock->now()->toDateTimeString()]);`

    Class docblock describes the action's current role (per CLAUDE.md docs-describe-current-state): "Updates per-series variance tolerance. Metric-style write — never transitions state — so `noOtherRecurringSeriesStateMutator` stays green. Cross-user 404 via the WHERE-user_id predicate. Idempotent no-op when the new value equals the current one."

    Modify `Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php` (Task 1 above). Add the action method:
    ```
    public function editVarianceTolerance(int $newTolerancePercent, CurrentUser $currentUser, EditRecurringSeriesVarianceTolerance $action): void
    {
        ($action)($this->seriesId, $currentUser->user(), $newTolerancePercent);
        $this->dispatch('toast', message: "Tolerance: {$newTolerancePercent}%", undoAction: '', undoPayload: null);
    }
    ```
    Add `use Modules\Recurring\Public\Actions\EditRecurringSeriesVarianceTolerance;` import.

    Modify `Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php` (Task 1 above). Locate the `{{-- variance-tolerance-editor: see Task 4 --}}` anchor comment in the header markup. Replace it with a Flux dropdown rendering three options (10% / 25% / 50%) with the current value pre-selected from `$series->varianceTolerancePercent`. Each option emits `wire:click="editVarianceTolerance({{ $percent }})"`. The label preceding the dropdown reads "Variance tolerance" (no help-text bloat — the dropdown is self-explanatory; the calm aesthetic forbids paragraphs of explainer text next to inline controls).

    Modify `Modules/Recurring/Providers/RecurringServiceProvider.php`. In `register()`, add `$this->app->singleton(EditRecurringSeriesVarianceTolerance::class);` alongside the other Public Action singletons from Plan 03.

    Create `Modules/Recurring/tests/Feature/EditRecurringSeriesVarianceToleranceTest.php` using `RefreshDatabase`. Six slices:
    - `happy-path-persists`: seed a series with `variance_tolerance_percent=25` for user A; invoke action with `$newTolerancePercent=50`; assert the row's column updated to 50 and `updated_at` is newer.
    - `idempotent-no-op`: seed a series with `variance_tolerance_percent=25`; wrap the action call in `DB::listen` query counting; invoke with `$newTolerancePercent=25`; assert query count for UPDATE statements is 0 (only the SELECT cross-user guard ran).
    - `cross-user-404`: seed series for user A; invoke action with user B; assert `NotFoundHttpException`; row unchanged.
    - `rejects-invalid-percent`: invoke with `$newTolerancePercent=7`; assert `InvalidArgumentException`; row unchanged.
    - `livewire-action-dispatches-toast`: log in as A; `Livewire::test(RecurringSeriesDetailPage::class, ['seriesId' => $series->id])->call('editVarianceTolerance', 50)`; assert the row updated AND the toast event was dispatched with message `"Tolerance: 50%"`.
    - `detector-honours-new-tolerance`: seed an approved series with `variance_tolerance_percent=50`; seed transactions in a band ±40% (would fragment under default 25, must stay one cluster under 50); run `DetectRecurringSeriesJob` synchronously; assert the cluster stays as one series.

    Run `composer test && composer analyse && composer format:check` to verify no regression.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Recurring/tests/Feature/EditRecurringSeriesVarianceToleranceTest.php Modules/Recurring/tests/Feature/RecurringSeriesDetailPageTest.php tests/Contracts/BoundaryArchTest.php --stop-on-failure &amp;&amp; composer analyse</automated>
  </verify>
  <done>
    - `Modules/Recurring/Public/Actions/EditRecurringSeriesVarianceTolerance.php` exists with `final class` and `ALLOWED_TOLERANCE_PERCENTS = [10, 25, 50]` (verify `grep -c "ALLOWED_TOLERANCE_PERCENTS" Modules/Recurring/Public/Actions/EditRecurringSeriesVarianceTolerance.php` returns ≥ 1).
    - `Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php` contains a `editVarianceTolerance` action method (verify `grep -c "editVarianceTolerance" Modules/Recurring/Internal/Http/Livewire/RecurringSeriesDetailPage.php` returns ≥ 1).
    - `Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php` contains the dropdown markup wired to `editVarianceTolerance` (verify `grep -c "editVarianceTolerance" Modules/Recurring/Resources/views/livewire/recurring-series-detail-page.blade.php` returns ≥ 1) and no longer contains the `variance-tolerance-editor: see Task 4` placeholder comment.
    - `Modules/Recurring/Providers/RecurringServiceProvider.php` registers the new singleton (verify `grep -c "EditRecurringSeriesVarianceTolerance" Modules/Recurring/Providers/RecurringServiceProvider.php` returns ≥ 1).
    - All six `EditRecurringSeriesVarianceToleranceTest` slices pass.
    - `composer test && composer analyse && composer format:check` exits 0; `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --parallel` exits 0 (the `noOtherRecurringSeriesStateMutator` invariant stays green — this Action writes `variance_tolerance_percent`, never `state`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → ApexCharts (`window.ApexCharts`) | Client-side chart consumes JSON-serialised chart config from Blade; the config is generated server-side; tampering risk is on the JS bundle integrity. |
| Drill-in route → cross-user 404 | `mount()` enforces the 404 before any rendering happens. |
| Dashboard `@livewire(...)` directive → FixedPaymentsCard | The card resolves through Livewire's container; cross-user reads scope by `user_id` via `topByMonthlyEquivalent`. |
| Top-nav badge composer → CurrentUser → RecurringSeriesQuery | The composer never runs without `CurrentUser->isAuthenticated()` guard; counts scope by `user_id`. |
| Re-detect button → DetectRecurringSeriesJob | The HTTP request only dispatches a queued job; no detector runs synchronously; the `noSynchronousDetectionInRequestLifecycle` arch test enforces this. |
| Bulk action bar → multiple Public Actions | Each Action enforces its own cross-user 404; the bulk loop catches and skips foreign ids without leaking them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-21 | Cross-site scripting | Blade-rendered ApexCharts JSON injection | mitigate | Use `@json($apexOptions)` (Laravel's safe encoder, not raw echo); chart options never include user-supplied strings unsanitised — only the displayName which Blade escapes by default. |
| T-08-22 | Information disclosure | Drill-in cross-user data leak | mitigate | `mount()` calls `RecurringSeriesQuery::forSeries($id, $user)` and throws `NotFoundHttpException` on null; verified by `cross-user-404` feature test. |
| T-08-23 | Information disclosure | Bulk action surfacing foreign user ids in the toast | mitigate | The bulk methods count only successful applications; the toast message contains the count, not the ids. Foreign-user ids are silently skipped — no error message reveals their existence. |
| T-08-24 | Denial of service | Spam-clicking "Re-detect now" overloads the queue | mitigate | `ShouldBeUniqueUntilProcessing` collapses duplicates at the queue worker; the HTTP-layer push count is fine because Redis lock guarantees only one worker runs the job concurrently per user. |
| T-08-25 | Tampering | `view()->composer` global helper bypassing DI | mitigate | The `TopNavBadgeComposerTest::no-view-helper-used` test asserts the forbidden string never appears in the provider; CLAUDE.md DI-only feedback covers the broader pattern. |
| T-08-26 | Tampering | Top-nav badge composer firing on unauthenticated requests | mitigate | The composer closure checks `CurrentUser->isAuthenticated()` first and falls back to 0 — verified by `badge-is-zero-when-unauthenticated`. |
| T-08-27 | Tampering | Bulk action bar dispatching when no items selected | accept | The Blade gate `@if (count($selectedIds) > 0)` prevents the UI from showing the bar; even if a malicious client crafts a Livewire payload, the empty loop is a no-op (zero applied, zero transitions). |
| T-08-SC | Tampering | Supply chain (no new packages in this plan) | accept | No new composer or npm dependencies installed in Plan 05; ApexCharts already audited in Plan 01. |
</threat_model>

<verification>
- `vendor/bin/pest --filter='Recurring' --parallel` exits 0 (full Recurring suite green: detail page, fixed payments card, top-nav composer, bulk actions, re-detect, plus all prior-plan tests).
- `vendor/bin/pest tests/Contracts/RecurringDetectionContractTest.php` exits 0 (end-to-end sweep over the full 11-fixture corpus, including idempotency assertion).
- `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --parallel` exits 0 (all five Recurring arch invariants stay green).
- `composer test && composer analyse && composer format:check && npm run build` all exit 0.
- `grep -c 'view()->composer' Modules/Recurring/Providers/RecurringServiceProvider.php` returns 0 (DI-only invariant respected — Phase 5 issue #12 fix carry-forward).
- `vendor/bin/pest Modules/Recurring/tests/Feature/EditRecurringSeriesVarianceToleranceTest.php` exits 0 (variance tolerance editor end-to-end including detector-honours-new-tolerance assertion).
- `grep -c 'ViewFactoryContract' Modules/Recurring/Providers/RecurringServiceProvider.php` returns ≥ 1.
- `grep -c '@livewire.*recurring.fixed-payments-card' Modules/Core/Resources/views/livewire/dashboard.blade.php` returns ≥ 1 (or equivalent dashboard view; executor confirms the exact file).
- `grep -c 'recurring.series.show' Modules/Recurring/Routes/web.php` returns ≥ 1.
</verification>

<success_criteria>
- REC-04 satisfied in full: dashboard inline card with top 6 + View-all link + All series / This month only toggle; `/recurring` rows now click through to the drill-in; funding-chain icon stack works with D-829 fallback.
- REC-05 + UI-03 satisfied: `/recurring/series/{id}` drill-in renders a full ApexCharts amount-over-time chart with original-currency primary + EUR shadow + drift markers; occurrences table links each row to its underlying transaction; cross-user 404 enforced via `mount()`.
- D-805 satisfied: "Re-detect now" button dispatches the same `DetectRecurringSeriesJob` with `ShouldBeUniqueUntilProcessing` lock; spam-click is safe; "detecting…" toast fires.
- D-812 + Pitfall 7 satisfied: checkbox-select + bulk Approve / Reject sticky action bar works end-to-end with 20-series bulk approval in one click; Undo toast dispatches per D-814.
- D-835 + D-836 + D-838 + D-855 satisfied: dashboard card with #[Url]-persisted "This month only" toggle; income surfaces via the existing in/out/remaining tile alongside the new card; Phase 5 "Next ICS settlement" tile stays untouched (D-837 carry-forward).
- Top-nav badge integer matches pending count via View Factory contract composer (no `view()` helper); Phase 5 issue #12 fix carry-forward verified.
- All five Plan 01 arch invariants stay green; the `noSynchronousDetectionInRequestLifecycle` arch test confirms `RecurringPage::reDetect` imports the job (not a detector directly) so the rule does not trip.
- D-825 + D-854 satisfied: `EditRecurringSeriesVarianceTolerance` Public Action ships with a Flux dropdown editor (10% / 25% / 50%) on the drill-in page; cross-user 404 enforced; idempotent no-op; the next detector sweep honours the new tolerance.
- End-to-end contract test (`RecurringDetectionContractTest`) asserts the full expected series set + cadences + metrics across the entire Wave 0 corpus, with idempotency on re-run.
- Phase 8 demo runs end-to-end: import a CSV in Phase 1 wizard → wait for daily sweep (or click Re-detect now) → review pending suggestions → bulk-approve 20 of them → open `/recurring` → click into Spotify → see the full ApexCharts trend with the €9.99 → €11.49 jump → close the drill-in → see the inline dashboard card showing top 6 fixed payments → see the top-nav `Recurring` badge updating to zero pending.
</success_criteria>

<output>
Create `.planning/phases/08-recurring-detection-fixed-payments-view/08-05-SUMMARY.md` when done. Record:
- Confirmation that `npm run build` emits a bundle including ApexCharts (verify via the manifest reference or via a smoke fixture test).
- The exact view file name + line number where `@livewire('recurring.fixed-payments-card')` was added.
- The exact view file name + line number where the top-nav `Recurring` link was added.
- Confirmation that `tests/Contracts/RecurringDetectionContractTest.php` asserts the FULL fixture corpus (no `->skip` directives remaining) and that re-running the sweep is idempotent.
- The total Phase 8 test count (`vendor/bin/pest --filter='Recurring' --parallel --pass-through="--no-coverage"` reports it in the footer).
- Any deviations: chart point-cap default, variance editor implementation (stub-only or full), or dashboard card position. Document each with rationale.
- Confirmation that the variance tolerance dropdown is wired on the drill-in page (capture the exact Flux component used + the resulting Blade fragment).
- Phase 8 ready for `/gsd:verify-work` — set the VALIDATION.md frontmatter `nyquist_compliant: true` if all sampling-rate criteria from `08-VALIDATION.md` are met.
</output>
