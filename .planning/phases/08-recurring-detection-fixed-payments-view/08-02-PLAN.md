---
phase: 08-recurring-detection-fixed-payments-view
plan: 02
type: execute
wave: 1
depends_on:
  - 08-01
files_modified:
  - Modules/Recurring/Database/Migrations/2026_05_18_010001_create_recurring_series_table.php
  - Modules/Recurring/Database/Migrations/2026_05_18_010002_create_recurring_series_occurrences_table.php
  - Modules/Recurring/Database/Migrations/2026_05_18_010003_create_recurring_series_transitions_table.php
  - Modules/Recurring/Database/Migrations/2026_05_18_010004_add_recurring_settings_to_users.php
  - Modules/Recurring/Models/RecurringSeries.php
  - Modules/Recurring/Models/RecurringSeriesOccurrence.php
  - Modules/Recurring/Models/RecurringSeriesTransition.php
  - Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php
  - Modules/Recurring/Public/Contracts/SeriesDetector.php
  - Modules/Recurring/Public/Dto/RecurringSeriesDto.php
  - Modules/Recurring/Public/Dto/RecurringOccurrenceDto.php
  - Modules/Recurring/Public/Dto/NextExpectedChargeDto.php
  - Modules/Recurring/Public/Dto/RecurringSeriesAmountTrendDto.php
  - Modules/Recurring/Public/Events/RecurringSeriesDetected.php
  - Modules/Recurring/Public/Events/RecurringSeriesApproved.php
  - Modules/Recurring/Public/Events/RecurringSeriesRejected.php
  - Modules/Recurring/Public/Events/RecurringSeriesCadenceFlipped.php
  - Modules/Recurring/Providers/RecurringServiceProvider.php
  - Modules/Recurring/tests/Unit/RecurringSeriesStateMachineTest.php
  - Modules/Recurring/tests/Unit/RecurringSeriesDtoTest.php
  - Modules/Core/Models/User.php
  - Modules/Core/Internal/Http/Livewire/SettingsPage.php
  - Modules/Core/Resources/views/livewire/settings-page.blade.php
  - Modules/Core/tests/Feature/SettingsRecurringFieldsTest.php
autonomous: true
requirements:
  - REC-03
  - LED-06

user_setup: []

must_haves:
  truths:
    - "Three new tables (recurring_series, recurring_series_occurrences, recurring_series_transitions) exist after `php artisan migrate` with the locked column shapes — a single unified `recurring_series` table with a `direction` enum (`expense` / `income`) so the same detector code path serves both sides (D-816)"
    - "Two new columns on `users` (recurring_detection_window_months default 18, recurring_income_min_amount_minor default 200000) exist"
    - "RecurringSeriesStateMachine is the only path that writes `recurring_series.state` AND inserts `recurring_series_transitions` rows, enforced by SQLite triggers + the `noOtherRecurringSeriesStateMutator` arch invariant from Plan 01"
    - "The Public surface separation is locked: Queries + DTOs + Events + Actions live under `Modules/Recurring/Public/`, while detector internals, state-machine internals, and projection queries stay Internal — five Public DTOs and four Public Events exist as final readonly classes that downstream waves (Plans 03–05) and Phase 9/10 consume only via this Public layer (D-832)"
    - "The `SeriesDetector` Public contract exists so the `noSynchronousDetectionInRequestLifecycle` arch test (Plan 01) becomes load-bearing once Plan 03 ships implementations"
    - "User can change `recurring_detection_window_months` and `recurring_income_min_amount_minor` on `/settings`; valid values persist to the users row, boundary violations are rejected with calm validation messages (D-802 / D-820)"
  artifacts:
    - path: Modules/Recurring/Database/Migrations/2026_05_18_010001_create_recurring_series_table.php
      provides: recurring_series DDL — id, user_id, direction, detected_name, display_name_override, state, cadence, latest_amount_minor, latest_currency, latest_fx_rate_used, monthly_equivalent_minor, variance_tolerance_percent, latest_funding_chain_link_id, snoozed_until, next_expected_at, next_expected_confidence_low, cluster_key, timestamps + SQLite state-validation triggers
      contains: "recurring_series"
    - path: Modules/Recurring/Database/Migrations/2026_05_18_010002_create_recurring_series_occurrences_table.php
      provides: recurring_series_occurrences DDL — link table from series to underlying transactions, with composite index (recurring_series_id, observed_at DESC)
      contains: "recurring_series_occurrences"
    - path: Modules/Recurring/Database/Migrations/2026_05_18_010003_create_recurring_series_transitions_table.php
      provides: recurring_series_transitions audit table — full state-machine history (D-815)
      contains: "recurring_series_transitions"
    - path: Modules/Recurring/Database/Migrations/2026_05_18_010004_add_recurring_settings_to_users.php
      provides: users.recurring_detection_window_months (default 18) + users.recurring_income_min_amount_minor (default 200000)
      contains: "recurring_detection_window_months"
    - path: Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php
      provides: Sole-mutator transition method with ALLOWED_TRANSITIONS const + lockForUpdate + PRAGMA busy_timeout + atomic transitions row insert
      contains: "ALLOWED_TRANSITIONS"
    - path: Modules/Recurring/Public/Contracts/SeriesDetector.php
      provides: Public interface implemented by Internal detectors (container-tagged 'recurring.detector')
      contains: "interface SeriesDetector"
    - path: Modules/Core/Internal/Http/Livewire/SettingsPage.php
      provides: /settings Livewire SFC now exposes the two recurring user preferences (detection window + income minimum) with validation
      contains: "recurringDetectionWindowMonths"
    - path: Modules/Core/tests/Feature/SettingsRecurringFieldsTest.php
      provides: Feature test covering pre-fill from users row, persist round-trip, boundary validation, and unchanged-row guarantee for the two new fields
  key_links:
    - from: Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php
      to: recurring_series.state + recurring_series_transitions (atomic write pair)
      via: DatabaseManager::connection()->transaction() with lockForUpdate + PRAGMA busy_timeout=5000
      pattern: "PRAGMA busy_timeout"
    - from: Modules/Recurring/Providers/RecurringServiceProvider.php
      to: RecurringSeriesStateMachine + DTO classes + Public Events
      via: singleton bindings in register()
      pattern: "RecurringSeriesStateMachine::class"
    - from: Modules/Recurring/Models/RecurringSeries.php
      to: chain_links (foreign key — latest_funding_chain_link_id)
      via: BelongsTo relation
      pattern: "latestFundingChainLink"
    - from: Modules/Core/Internal/Http/Livewire/SettingsPage.php
      to: users.recurring_detection_window_months + users.recurring_income_min_amount_minor
      via: SettingsPage::save() round-trips both fields through Eloquent
      pattern: "recurring_detection_window_months"
---

<objective>
Land the persistence layer + state machine + Public contract surface for Phase 8. Migrations create three new tables and add two columns to `users`; Eloquent models bind them with the standard `BelongsToUser` concern; the sole-mutator state machine arrives with column-targeted SQLite triggers; the Public DTOs, Events, and `SeriesDetector` contract land so the four arch invariants from Plan 01 become load-bearing and Plans 03–05 can build against a stable Public API.

Purpose: Lock the schema before any detector code lands — every later wave reads against these column names and trigger constraints. Ship the state machine alongside its arch test so the sole-mutator invariant is enforced from day one. Define the Public surface (contracts + DTOs + events) so detector and UI plans have well-typed call sites.

Output: Migrations + Eloquent models + state machine + Public surface. No detector code, no UI, no jobs — that comes in Plans 03–05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-VALIDATION.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-01-SUMMARY.md
@CLAUDE.md
@Modules/Chains/Database/Migrations/2026_05_16_010002_create_card_statements_table.php
@Modules/Chains/Database/Migrations/2026_05_16_010003_create_card_statement_credits_table.php
@Modules/Chains/Models/ChainLink.php
@Modules/EmailScan/Internal/InboxScanStateMachine.php
@Modules/Chains/Public/Dto/ChainLinkRow.php
@Modules/Chains/Public/Actions/ConfirmChainLink.php
@Modules/Core/Public/Concerns/BelongsToUser.php
@Modules/Core/Public/Contracts/Clock.php
@Modules/Ledger/Public/ValueObjects/Money.php
@Modules/Recurring/Providers/RecurringServiceProvider.php
@tests/Contracts/BoundaryArchTest.php
@Modules/Core/Internal/Http/Livewire/SettingsPage.php
@Modules/Core/Resources/views/livewire/settings-page.blade.php
@Modules/Core/tests/Feature/SettingsPageTest.php
@Modules/Core/Models/User.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the four migrations + three Eloquent models</name>
  <files>
    Modules/Recurring/Database/Migrations/2026_05_18_010001_create_recurring_series_table.php,
    Modules/Recurring/Database/Migrations/2026_05_18_010002_create_recurring_series_occurrences_table.php,
    Modules/Recurring/Database/Migrations/2026_05_18_010003_create_recurring_series_transitions_table.php,
    Modules/Recurring/Database/Migrations/2026_05_18_010004_add_recurring_settings_to_users.php,
    Modules/Recurring/Models/RecurringSeries.php,
    Modules/Recurring/Models/RecurringSeriesOccurrence.php,
    Modules/Recurring/Models/RecurringSeriesTransition.php,
    Modules/Recurring/tests/Feature/RecurringMigrationTest.php
  </files>
  <read_first>
    Modules/Chains/Database/Migrations/2026_05_16_010002_create_card_statements_table.php (entire 98-line file — the anonymous-class migration shape with private DatabaseManager + schema() and db() helpers; the state-trigger CREATE TRIGGER ... BEFORE INSERT / UPDATE OF state pattern that Pattern S-4 mandates),
    Modules/Chains/Database/Migrations/2026_05_16_010003_create_card_statement_credits_table.php (the child-table migration shape including the composite index pattern for the (parent_id, observed_at DESC) ordering),
    Modules/Chains/Models/ChainLink.php (final class extends Model, BelongsToUser concern, casts() method shape, @property docblock pattern, BelongsTo relations),
    Modules/Core/Public/Concerns/BelongsToUser.php (the trait that every model under Modules/ uses for cross-user isolation),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md "Migrations (Wave 1)" + "Migration: create_recurring_series_table.php" sections,
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-815 (transitions table column list) + D-808/D-815 (state set: pending/approved/rejected/snoozed/cadence_changed) + D-825 (variance_tolerance_percent default 25) + D-810 (snoozed_until) + D-820 (income min amount default 200000 = €2000) + D-802 (detection window default 18 months) + D-848 (cadence as `string(24)` per RESEARCH migration template line 994)
  </read_first>
  <behavior>
    - `php artisan migrate --pretend` previews three CREATE TABLE statements and one ALTER TABLE statement against an empty schema.
    - `php artisan migrate` applied to a fresh in-memory SQLite produces three tables (`recurring_series`, `recurring_series_occurrences`, `recurring_series_transitions`) and adds two columns to `users`. Verified by the migration feature test.
    - SQLite triggers `recurring_series_state_check_insert` and `recurring_series_state_check_update` raise ABORT when a non-allowed state value is written.
    - The `recurring_series` UNIQUE constraint on `(user_id, direction, cluster_key, latest_currency)` prevents duplicate series for the same cluster.
    - The `(user_id, state)` index supports the top-nav pending-count query (Plan 05 dashboard composer).
    - The `(user_id, state, next_expected_at)` index supports the "This month only" toggle (D-838).
    - Eloquent models hydrate from raw rows correctly (casts: snoozed_until → immutable_datetime; next_expected_at → immutable_date; next_expected_confidence_low → boolean).
    - The `RecurringSeries::latestFundingChainLink()` relation resolves to a `BelongsTo&lt;ChainLink&gt;` and returns the linked row.
    - The migration feature test runs `composer test` clean on a fresh in-memory SQLite.
  </behavior>
  <action>
    Create `Modules/Recurring/Database/Migrations/2026_05_18_010001_create_recurring_series_table.php`. Use the anonymous-class shape from `card_statements` (private `?DatabaseManager $resolvedDb`, `schema()`, `db()`, `getConnection()` helpers). The `up()` method calls `$this->schema()-&gt;create('recurring_series', ...)` and configures the Blueprint with the columns listed below; the `down()` method drops the triggers first then the table.

    Columns on `recurring_series` (per CONTEXT D-815/D-820/D-825 + RESEARCH lines 985–1013):
    - `id()` — auto-increment primary key
    - `foreignId('user_id')->nullable()->constrained('users')->cascadeOnDelete()` — FND-03 multi-user-ready
    - `enum('direction', ['expense', 'income'])` — D-816
    - `string('detected_name')`
    - `string('display_name_override')->nullable()` — D-813
    - `string('state', 24)->default('pending')` — string, not enum, because SQLite enum support is limited; the SQLite triggers enforce the allowed set
    - `string('cadence', 24)->default('irregular')` — D-848 (planner picks string per RESEARCH migration template line 994)
    - `bigInteger('latest_amount_minor')` — FND-04 signed BIGINT
    - `string('latest_currency', 3)`
    - `string('latest_fx_rate_used')->nullable()` — D-851
    - `bigInteger('monthly_equivalent_minor')->nullable()` — D-826, populated by detectors
    - `unsignedTinyInteger('variance_tolerance_percent')->default(25)` — D-825
    - `foreignId('latest_funding_chain_link_id')->nullable()->constrained('chain_links')->nullOnDelete()` — D-828/D-829
    - `timestamp('snoozed_until')->nullable()` — D-810
    - `date('next_expected_at')->nullable()`
    - `boolean('next_expected_confidence_low')->default(false)` — D-830
    - `string('cluster_key')` — D-839 deterministic shape, composed by the `ClusterKeyComposer` (lands in Plan 03)
    - `timestamps()`

    Indexes on `recurring_series`:
    - `unique(['user_id', 'direction', 'cluster_key', 'latest_currency'], 'rec_series_uniq')`
    - `index(['user_id', 'state'])` — top-nav badge query (Plan 05)
    - `index(['user_id', 'state', 'next_expected_at'])` — "This month only" toggle (D-838)

    SQLite trigger DDL on the same migration (mirror card_statements lines 78–97 — direct facade `Schema::` and `DB::` calls are forbidden so use `$this->db()-&gt;connection($this-&gt;getConnection())-&gt;statement(...)` per the pattern in card_statements):
    ```
    CREATE TRIGGER recurring_series_state_check_insert BEFORE INSERT ON recurring_series FOR EACH ROW
    WHEN NEW.state NOT IN ('pending','approved','rejected','snoozed','cadence_changed')
    BEGIN SELECT RAISE(ABORT, 'Invalid recurring_series.state value'); END
    ```
    And the matching `BEFORE UPDATE OF state` trigger. The `down()` method drops the triggers first.

    Create `Modules/Recurring/Database/Migrations/2026_05_18_010002_create_recurring_series_occurrences_table.php`. Same anonymous-class shape. Columns:
    - `id()`
    - `foreignId('user_id')->nullable()->constrained('users')->cascadeOnDelete()` — FND-03 (the row carries user_id directly so the cross-user 404 query doesn't require a JOIN)
    - `foreignId('recurring_series_id')->constrained('recurring_series')->cascadeOnDelete()`
    - `foreignId('transaction_id')->constrained('transactions')->cascadeOnDelete()` — link back to the underlying transaction (read-only — `noTransactionWritesFromRecurring` arch test ensures we never UPDATE/INSERT on `transactions`)
    - `date('observed_at')`
    - `bigInteger('observed_amount_minor')` — FND-04
    - `string('observed_currency', 3)`
    - `timestamps()`

    Indexes:
    - `unique(['recurring_series_id', 'transaction_id'], 'rec_occ_uniq')` — idempotency: re-running the sweep cannot duplicate an occurrence row for the same (series, transaction) pair
    - `index(['recurring_series_id', 'observed_at'])` — drill-in chart query in Plan 05 reads occurrences ordered by date DESC

    Create `Modules/Recurring/Database/Migrations/2026_05_18_010003_create_recurring_series_transitions_table.php`. Append-only audit table (D-815):
    - `id()`
    - `foreignId('user_id')->nullable()->constrained('users')->cascadeOnDelete()` — FND-03
    - `foreignId('recurring_series_id')->constrained('recurring_series')->cascadeOnDelete()`
    - `string('from_state', 24)`
    - `string('to_state', 24)`
    - `string('transition_reason', 64)` — enum string (`user_action` | `detector_cadence_flip` | `detector_promoted` | `snooze_expired` | ...)
    - `string('actor', 16)` — `user` | `detector`
    - `timestamp('transitioned_at')`
    - `text('notes')->nullable()`
    - `timestamps()`

    Index: `index(['recurring_series_id', 'transitioned_at'])` — drill-in audit query is series-scoped + time-ordered.

    Create `Modules/Recurring/Database/Migrations/2026_05_18_010004_add_recurring_settings_to_users.php`. Schema::table('users') ALTER with:
    - `unsignedSmallInteger('recurring_detection_window_months')->default(18)` — D-802
    - `bigInteger('recurring_income_min_amount_minor')->default(200000)` — D-820 (€2000.00 in BIGINT minor units)

    Down method drops both columns. SQLite supports `ALTER TABLE ADD COLUMN` and `ALTER TABLE DROP COLUMN` in modern versions (verify Laravel's Schema builder emits DROP COLUMN cleanly on this project's SQLite version; if not, use `Schema::table` with `dropColumn` wrapped in a `Schema::disableForeignKeyConstraints` block matching the Phase 3 `default_currency_view` precedent).

    Create `Modules/Recurring/Models/RecurringSeries.php`. `final class extends Model` with `use BelongsToUser;`. `$fillable` lists every non-id column. `casts()` returns:
    - `snoozed_until` → `immutable_datetime`
    - `next_expected_at` → `immutable_date`
    - `next_expected_confidence_low` → `boolean`
    - `created_at`, `updated_at` → `immutable_datetime`
    Add `@property` docblock matching ChainLink lines 14–34. Add a `latestFundingChainLink(): BelongsTo` relation pointing at `Modules\Chains\Models\ChainLink::class` on foreign key `latest_funding_chain_link_id`. Add a `transitions(): HasMany` relation pointing at `RecurringSeriesTransition` ordered by `transitioned_at` ASC. Add an `occurrences(): HasMany` relation pointing at `RecurringSeriesOccurrence` ordered by `observed_at` DESC.

    Create `Modules/Recurring/Models/RecurringSeriesOccurrence.php` — `final class extends Model` with `BelongsToUser`. `$fillable` lists every non-id column. `casts()` returns `observed_at` → `immutable_date`. Add `series(): BelongsTo` to `RecurringSeries::class` and `transaction(): BelongsTo` to `Modules\Ledger\Models\Transaction::class` (use the existing Transaction model from Ledger — confirm exact namespace by reading `Modules/Ledger/Models/Transaction.php` if needed before referencing it).

    Create `Modules/Recurring/Models/RecurringSeriesTransition.php` — `final class extends Model` with `BelongsToUser`. `$fillable` lists every non-id column. `casts()` returns `transitioned_at` → `immutable_datetime`. Add `series(): BelongsTo` to `RecurringSeries::class`.

    Create `Modules/Recurring/tests/Feature/RecurringMigrationTest.php` covering:
    - `RefreshDatabase` runs the migrations cleanly (all four), producing the three new tables and the two new user columns
    - Inserting a row with `state='invalid'` raises an ABORT (Schema trigger fires) — assert via try/catch on `Illuminate\Database\QueryException`
    - The UNIQUE `rec_series_uniq` constraint prevents duplicate (user_id, direction, cluster_key, latest_currency) inserts
    - `users.recurring_detection_window_months` defaults to 18 and `users.recurring_income_min_amount_minor` defaults to 200000 on a freshly created user
    - `RecurringSeries::find($id)-&gt;latestFundingChainLink` resolves to the related `ChainLink` row (or null when foreign key is null)

    Run `composer test` and confirm the full suite stays green.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Recurring/tests/Feature/RecurringMigrationTest.php --stop-on-failure &amp;&amp; composer test &amp;&amp; composer analyse</automated>
  </verify>
  <done>
    - Four migration files exist under `Modules/Recurring/Database/Migrations/`.
    - Three Eloquent models exist under `Modules/Recurring/Models/`.
    - `php artisan migrate:fresh --env=testing` exits 0 on a fresh in-memory SQLite (verified via the migration feature test running under `RefreshDatabase`).
    - `vendor/bin/pest Modules/Recurring/tests/Feature/RecurringMigrationTest.php` exits 0.
    - `grep -c 'recurring_series_state_check_insert' Modules/Recurring/Database/Migrations/2026_05_18_010001_create_recurring_series_table.php` returns ≥ 1.
    - `grep -c 'rec_series_uniq' Modules/Recurring/Database/Migrations/2026_05_18_010001_create_recurring_series_table.php` returns ≥ 1.
    - `composer test && composer analyse` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RecurringSeriesStateMachine (sole mutator) + its unit test</name>
  <files>
    Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php,
    Modules/Recurring/Internal/StateMachines/InvalidStateTransitionException.php,
    Modules/Recurring/tests/Unit/RecurringSeriesStateMachineTest.php
  </files>
  <read_first>
    Modules/EmailScan/Internal/InboxScanStateMachine.php (entire 347-line file — copy the ALLOWED_TRANSITIONS const map shape, the constructor DI shape with `DatabaseManager` + `Clock`, the `transition()` body with `$this->db->connection()->transaction(...)` + `PRAGMA busy_timeout = 5000` + `lockForUpdate()` + atomic state-row + audit-row write; the static `toString()` / `toInt()` scalar coercion helpers; the `guardTransition()` private method that throws `InvalidStateTransitionException`),
    Modules/Core/Public/Contracts/Clock.php (the Clock contract — never use Carbon facades; constructor-inject Clock and call `$this-&gt;clock-&gt;now()`),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md "Pattern 2: Sole-Mutator State Machine" (the ALLOWED_TRANSITIONS map matching CONTEXT D-815 + the load-bearing PRAGMA busy_timeout fence),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-815 (the full state set + transition reasons + actor enum) + RESEARCH Pitfall 3 (metric refresh writes the row WITHOUT touching state and WITHOUT inserting a transitions row — so the state machine ONLY handles state changes)
  </read_first>
  <behavior>
    - `RecurringSeriesStateMachine::transition(RecurringSeries, string toState, string reason, string actor, ?string notes): void` enforces the ALLOWED_TRANSITIONS map: `pending→{approved,rejected,snoozed}`, `approved→{cadence_changed,rejected}`, `cadence_changed→{approved,rejected}`, `snoozed→{pending,approved,rejected}`, `rejected→{pending}`.
    - An illegal transition (e.g. `pending → cadence_changed`) throws `InvalidStateTransitionException`.
    - A legal transition writes `recurring_series.state` AND inserts one row in `recurring_series_transitions` atomically within a single DB transaction.
    - The row read uses `lockForUpdate()` so two concurrent sweep jobs (Pitfall 6) serialise on the same series row.
    - `PRAGMA busy_timeout = 5000` is set on the connection inside every transition, so SQLite retries lock contention for 5 seconds before raising.
    - `toState` and `actor` validation: invalid `toState` outside the allowed set raises an exception; `actor` MUST be `user` or `detector` (anything else raises).
    - Transitioning to the same state (e.g. approved → approved) is rejected by the ALLOWED_TRANSITIONS map (idempotent no-ops live in Public Actions, not here — per Pitfall 3).
    - The state machine NEVER writes any column other than `state` and `updated_at` on `recurring_series`; metric refresh is the detector's job (Plan 03) writing the row directly via `DatabaseManager` without touching `state`.
  </behavior>
  <action>
    Create `Modules/Recurring/Internal/StateMachines/InvalidStateTransitionException.php` as `final class extends RuntimeException` with a static constructor `forTransition(int $seriesId, string $from, string $to): self` that formats the message `"Illegal recurring_series transition for id={$seriesId}: {$from} -> {$to}"`. Mirror the EmailScan equivalent if present.

    Create `Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php`. `final class` (NOT final readonly — the class is a service with state-machine semantics, not a value object). Constructor DI:
    ```
    public function __construct(
        private readonly DatabaseManager $db,
        private readonly Clock $clock,
    ) {}
    ```

    Add a `private const ALLOWED_TRANSITIONS = [...]` const map exactly matching D-815:
    ```
    'pending'         => ['approved', 'rejected', 'snoozed'],
    'approved'        => ['cadence_changed', 'rejected'],
    'cadence_changed' => ['approved', 'rejected'],
    'snoozed'         => ['pending', 'approved', 'rejected'],
    'rejected'        => ['pending'],
    ```

    Implement `public function transition(RecurringSeries $series, string $toState, string $reason, string $actor, ?string $notes = null): void`:
    1. Validate `$actor` is `user` or `detector` (whitelist constant `ALLOWED_ACTORS`).
    2. Open a DB transaction via `$this->db->connection()->transaction(function () use (...) { ... })`.
    3. Inside the transaction: call `$connection->statement('PRAGMA busy_timeout = 5000')`.
    4. Read the row via `$connection->table('recurring_series')->where('id', $series->id)->lockForUpdate()->first()`. If null, throw `RuntimeException` with message `"RecurringSeriesStateMachine: recurring_series row {$series->id} not found."`.
    5. Validate the transition via `$this->guardTransition($series->id, $currentState, $toState)` — private method that checks `self::ALLOWED_TRANSITIONS[$currentState] ?? [] contains $toState`; throws `InvalidStateTransitionException::forTransition(...)` otherwise.
    6. Compute `$now = $this->clock->now()->toDateTimeString()`.
    7. Update `recurring_series` SET state = $toState, updated_at = $now WHERE id = $series->id.
    8. Insert a `recurring_series_transitions` row with `user_id`, `recurring_series_id`, `from_state` = $currentState, `to_state` = $toState, `transition_reason` = $reason, `actor` = $actor, `transitioned_at` = $now, `notes` = $notes, `created_at` = $now, `updated_at` = $now.

    Add the static scalar coercion helpers `private static function toString(mixed): string` and `private static function toInt(mixed): int` mirroring InboxScanStateMachine.

    Class-level docblock describes what the state machine currently does — the ALLOWED_TRANSITIONS set, the SQLite busy_timeout fence, and that metric-refresh writes go elsewhere (so the `noOtherRecurringSeriesStateMutator` arch test only watches the `state` column). No D-XXX references; no plan/wave history per CLAUDE.md.

    Register the singleton in `Modules/Recurring/Providers/RecurringServiceProvider.php`: add `$this->app->singleton(RecurringSeriesStateMachine::class);` to `register()`. Keep imports clean.

    Create `Modules/Recurring/tests/Unit/RecurringSeriesStateMachineTest.php` as Pest tests using `RefreshDatabase`:
    - Happy path: `pending → approved` writes `state='approved'` on the series row AND inserts exactly one `recurring_series_transitions` row with `from_state='pending'`, `to_state='approved'`, `actor='user'`, `transition_reason='user_action'`.
    - Illegal transition: `pending → cadence_changed` throws `InvalidStateTransitionException` and leaves the row unchanged + the transitions table empty.
    - Same-state transition: `approved → approved` throws (not in ALLOWED_TRANSITIONS).
    - Series not found: passing a `RecurringSeries` with a non-existent id throws `RuntimeException`.
    - Invalid actor: `actor='robot'` throws.
    - Audit row carries the `notes` argument when provided; null when omitted.
    - Each legal transition in the ALLOWED_TRANSITIONS map (a dataset over `[ [from, to], ... ]`) succeeds.
    - The SQLite trigger from Task 1 fires when the test bypasses the state machine and tries to write `state='garbage'` directly via Eloquent — assert via `expect(fn() => RecurringSeries::query()->where('id', $id)->update(['state' => 'garbage']))->toThrow(QueryException::class)`.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Recurring/tests/Unit/RecurringSeriesStateMachineTest.php --stop-on-failure &amp;&amp; composer analyse</automated>
  </verify>
  <done>
    - `Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php` exists with `final class` declaration and contains the literal substring `ALLOWED_TRANSITIONS`.
    - `Modules/Recurring/Internal/StateMachines/InvalidStateTransitionException.php` exists.
    - `grep -c 'PRAGMA busy_timeout' Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php` returns ≥ 1.
    - `grep -c 'lockForUpdate' Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php` returns ≥ 1.
    - `vendor/bin/pest Modules/Recurring/tests/Unit/RecurringSeriesStateMachineTest.php` exits 0.
    - `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noOtherRecurringSeriesStateMutator'` exits 0 (the arch invariant lands in Plan 01 and now has a real state-machine class to point at, so it scans real code instead of trivially passing on a missing directory).
    - `composer analyse` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create the Public contract + DTOs + events + register provider bindings</name>
  <files>
    Modules/Recurring/Public/Contracts/SeriesDetector.php,
    Modules/Recurring/Public/Dto/RecurringSeriesDto.php,
    Modules/Recurring/Public/Dto/RecurringOccurrenceDto.php,
    Modules/Recurring/Public/Dto/NextExpectedChargeDto.php,
    Modules/Recurring/Public/Dto/RecurringSeriesAmountTrendDto.php,
    Modules/Recurring/Public/Events/RecurringSeriesDetected.php,
    Modules/Recurring/Public/Events/RecurringSeriesApproved.php,
    Modules/Recurring/Public/Events/RecurringSeriesRejected.php,
    Modules/Recurring/Public/Events/RecurringSeriesCadenceFlipped.php,
    Modules/Recurring/Providers/RecurringServiceProvider.php,
    Modules/Recurring/tests/Unit/RecurringSeriesDtoTest.php
  </files>
  <read_first>
    Modules/Chains/Public/Dto/ChainLinkRow.php (final class extends Data; constructor-promoted readonly props; Money VO usage),
    Modules/Chains/Public/Dto/CardStatementForecastTile.php (a Spatie Data DTO with optional fields shaped like `NextExpectedChargeDto`),
    Modules/Chains/Public/Dto/ChainTree.php (collection-shaped DTO matching `RecurringSeriesAmountTrendDto`),
    Modules/Chains/Public/Events/* (the event class shape — final readonly with constructor-promoted props, dispatched via `event(new ...)`),
    Modules/Receipts/Public/Events/ChainHintDetected.php (alternative final-readonly event example),
    Modules/Chains/Public/Contracts/DispatchesChainResolution.php (interface example matching `SeriesDetector` shape),
    Modules/Ledger/Public/ValueObjects/Money.php (the Money VO every DTO uses),
    Modules/Core/Models/User.php (the User class events accept as a property),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md "Public surface" + "Pattern S-1" sections,
    .planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md "Example 2: Public DTO" (lines 900–944 — the RecurringSeriesDto template)
  </read_first>
  <behavior>
    - `Modules\Recurring\Public\Contracts\SeriesDetector` is a PHP interface with a single method `detectForUser(User $user): void`.
    - The contract's docblock declares the container tag `'recurring.detector'` so Plan 03 implementations register cleanly.
    - Four DTOs (`RecurringSeriesDto`, `RecurringOccurrenceDto`, `NextExpectedChargeDto`, `RecurringSeriesAmountTrendDto`) each extend `Spatie\LaravelData\Data`, use `final class`, constructor-promoted `public readonly` props, accept `Money` VO instances for monetary values, and accept `CarbonImmutable` for dates.
    - Four events (`RecurringSeriesDetected`, `RecurringSeriesApproved`, `RecurringSeriesRejected`, `RecurringSeriesCadenceFlipped`) each are `final readonly class` with constructor-promoted props that carry `int $seriesId`, `int $userId`, and any event-specific extras (e.g. the cadence-flip event carries old + new cadence strings).
    - `RecurringServiceProvider::register()` binds: `RecurringSeriesStateMachine` (already in Task 2), and is now extended to bind no additional services yet (detectors land in Plan 03; queries land in Plan 03/04; actions are wired in Plan 03 with the review queue) — Task 3 only ensures the provider has the structural slots needed for later waves to extend cleanly.
    - The `noSynchronousDetectionInRequestLifecycle` arch test from Plan 01 now scans against a real `SeriesDetector` Public class, not a missing one. The test still passes (no Internal/Http file imports the contract yet).
    - The `RecurringSeriesDtoTest` round-trips a DTO through `Data::from()` and back, asserting all properties survive serialization.
  </behavior>
  <action>
    Create `Modules/Recurring/Public/Contracts/SeriesDetector.php`:
    ```
    interface SeriesDetector
    {
        public function detectForUser(User $user): void;
    }
    ```
    With docblock declaring: implementations register as singletons in `RecurringServiceProvider::register()`, container-tagged `'recurring.detector'`; `DetectRecurringSeriesJob` (lands in Plan 03) injects `iterable $detectors` via `$this-&gt;app-&gt;tagged('recurring.detector')`. Document the analytical-only invariant: implementations MUST NOT write the `transactions` table; the `noTransactionWritesFromRecurring` arch test from Plan 01 enforces this at the static-analysis level.

    Create `Modules/Recurring/Public/Dto/RecurringSeriesDto.php` matching the shape in RESEARCH lines 920–944 + PATTERNS section "Public Dto RecurringSeriesDto":
    - `final class extends Data`
    - Constructor-promoted readonly props: `int $seriesId`, `string $direction` ('expense'|'income'), `string $detectedName`, `?string $displayNameOverride`, `string $state`, `string $cadence`, `Money $latestAmount` (original currency), `?Money $eurEquivalent` (null when original = EUR), `Money $monthlyEquivalent` (EUR), `?int $latestFundingChainLinkId`, `?CarbonImmutable $nextExpectedAt`, `bool $nextExpectedConfidenceLow`, `int $varianceTolerancePercent`, `?CarbonImmutable $snoozedUntil`
    - Helper method `displayName(): string` returning `$this->displayNameOverride ?? $this->detectedName`

    Create `Modules/Recurring/Public/Dto/RecurringOccurrenceDto.php`:
    - `final class extends Data`
    - Props: `int $occurrenceId`, `int $recurringSeriesId`, `int $transactionId`, `CarbonImmutable $observedAt`, `Money $observedAmount`, `string $observedCurrency`

    Create `Modules/Recurring/Public/Dto/NextExpectedChargeDto.php`:
    - `final class extends Data`
    - Props: `?CarbonImmutable $nextExpectedAt`, `string $cadence`, `bool $confidenceLow`, `int $stddevDays`
    - Used by `/recurring` row rendering (Plan 04) to format "in 5 days · May 22" relative-absolute text per D-830

    Create `Modules/Recurring/Public/Dto/RecurringSeriesAmountTrendDto.php`:
    - `final class extends Data`
    - Props: `int $seriesId`, `string $currency`, `list&lt;array{date:string,amount_minor:int,eur_amount_minor:?int}&gt; $points`, `int $maxPoints` (default 24 per Pitfall 9)
    - Used by Plan 05's drill-in chart (ApexCharts dataset payload)

    Create the four Public Events:
    - `RecurringSeriesDetected` — `final readonly class` with props `int $seriesId`, `int $userId`, `string $direction`, `string $detectedName`, `string $cadence`. Dispatched by the detector when a new pending series is created.
    - `RecurringSeriesApproved` — props `int $seriesId`, `int $userId`. Dispatched by `ApproveRecurringSeries` (Plan 03).
    - `RecurringSeriesRejected` — props `int $seriesId`, `int $userId`. Dispatched by `RejectRecurringSeries` (Plan 03).
    - `RecurringSeriesCadenceFlipped` — props `int $seriesId`, `int $userId`, `string $oldCadence`, `string $newCadence`. Dispatched by the detector when an approved series flips cadence class per D-804.

    No event listeners are wired in this plan — Phase 9 and Phase 10 attach listeners later. Phase 8 itself does not consume any of its own events.

    Modify `Modules/Recurring/Providers/RecurringServiceProvider.php`: ensure `register()` contains the `RecurringSeriesStateMachine::class` singleton binding from Task 2. No further singletons in this plan (queries and actions arrive in Plan 03).

    Create `Modules/Recurring/tests/Unit/RecurringSeriesDtoTest.php` with assertions:
    - `RecurringSeriesDto::from([...])` constructs from an array and `->toArray()` round-trips back without data loss.
    - `displayName()` returns `displayNameOverride` when set, falling back to `detectedName`.
    - Constructing with `Money::EUR(999)` for `latestAmount` retains the Money instance and its currency.
    - `RecurringOccurrenceDto::from([...])` round-trips.
    - `NextExpectedChargeDto` correctly carries `confidenceLow: true` when `stddevDays > 5` (the test asserts a small computed example; the actual confidence calculation lives in `CadenceInferrer` in Plan 03 — this test only verifies the DTO does not coerce the boolean away).
    - `RecurringSeriesAmountTrendDto` enforces non-empty `points` list type (the static analyser already catches misuse, but a smoke assertion here documents the contract).

    Run `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` to confirm the `noSynchronousDetectionInRequestLifecycle` arch test still passes now that `SeriesDetector` is a real class (the test asserts no Internal/Http file imports it; until Plan 04 lands the Livewire pages, this remains trivially true).
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Recurring/tests/Unit/RecurringSeriesDtoTest.php tests/Contracts/BoundaryArchTest.php --stop-on-failure &amp;&amp; composer test &amp;&amp; composer analyse &amp;&amp; composer format:check</automated>
  </verify>
  <done>
    - `Modules/Recurring/Public/Contracts/SeriesDetector.php` exists and `php -r "require 'vendor/autoload.php'; new ReflectionClass(\"Modules\\\\Recurring\\\\Public\\\\Contracts\\\\SeriesDetector\");"` reports it as an interface.
    - Four DTO files exist under `Modules/Recurring/Public/Dto/` and each extends `Spatie\LaravelData\Data`.
    - Four event files exist under `Modules/Recurring/Public/Events/` and each is declared `final readonly class`.
    - `vendor/bin/pest Modules/Recurring/tests/Unit/RecurringSeriesDtoTest.php tests/Contracts/BoundaryArchTest.php` exits 0.
    - `composer test && composer analyse && composer format:check` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend /settings Livewire SFC with recurring window + income-minimum fields</name>
  <files>
    Modules/Core/Models/User.php,
    Modules/Core/Internal/Http/Livewire/SettingsPage.php,
    Modules/Core/Resources/views/livewire/settings-page.blade.php,
    Modules/Core/tests/Feature/SettingsRecurringFieldsTest.php
  </files>
  <read_first>
    Modules/Core/Internal/Http/Livewire/SettingsPage.php (entire 135-line file — the existing #[Validate] attribute usage, mount() pre-fill pattern, save() Eloquent round-trip pattern, messages() per-rule mapping; the two new fields land alongside the existing defaultCurrencyView + periodStartDay + autoImportFromDropFolder properties),
    Modules/Core/Models/User.php (the $fillable + casts() shape — add the two new columns to both arrays so save() works without manual column assignment; verify the column casts BIGINT minor units to int per FND-04 convention),
    Modules/Core/Resources/views/livewire/settings-page.blade.php (the current Blade layout — add a new "Recurring detection" section adjacent to the existing currency-display + period-start-day sections; use the same wire:model.live / wire:change / save-button pattern documented in the existing file),
    Modules/Core/tests/Feature/SettingsPageTest.php (entire 100-line file — copy the test shape verbatim, swap field names + boundary values; the beforeEach() seed + Livewire::test() pattern transfers directly),
    Modules/Recurring/Database/Migrations/2026_05_18_010004_add_recurring_settings_to_users.php (Task 1 above — confirms column names + defaults the SettingsPage reads),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-802 (detection window default 18 months, lives in `/settings`) + D-820 (income minimum default €2000, lives in `/settings`)
  </read_first>
  <behavior>
    - `User::$fillable` includes `recurring_detection_window_months` and `recurring_income_min_amount_minor`; `User::casts()` casts both as `int` (FND-04 minor units stay integer).
    - `SettingsPage` exposes two new public properties:
       - `#[Validate('required|integer|min:3|max:60')] public int $recurringDetectionWindowMonths = 18;` — bounds per D-802 (3 months minimum so monthly cadence has at least 3 samples; 60 month upper bound matches Phase 5's max-history precedent).
       - `#[Validate('required|integer|min:0|max:100000000')] public int $recurringIncomeMinAmountMinor = 200000;` — BIGINT minor units; 0 means "no threshold" (D-820 explicitly allows lowering this), 100000000 = €1,000,000 upper sanity bound.
    - `mount(CurrentUser $currentUser): void` pre-fills both fields from the users row alongside the existing three properties.
    - `save(CurrentUser $currentUser): void` writes both fields onto the User model + saves; the existing `validate()` call covers the new attributes via the `#[Validate]` attributes.
    - `messages()` adds calm, single-sentence error strings for the new fields (one message per attribute, regardless of which sub-rule fires — mirror the existing `periodStartDay.*` rollup).
    - The Blade view renders the two new fields in a new "Recurring detection" section: a numeric input for the months (with helper text "How many months of history to scan for recurring patterns"), and a numeric input for the income minimum in EUR (with brick/money formatter rendering helper text "Currently: €{{ amount }} — incomes below this are not auto-clustered"). The Blade displays minor units as EUR via the existing `Money::ofMinor(...)->formatTo('en_NL')` helper or equivalent — confirm the project Money formatter convention by reading the existing dashboard/transactions Blade.
    - `SettingsRecurringFieldsTest` covers six cases per the `SettingsPageTest` precedent: pre-fill from users row, persist round-trip for the window, persist round-trip for the income minimum, reject out-of-range window (0 and 61), reject negative income minimum, unchanged-row guarantee when validation fails.
  </behavior>
  <action>
    Modify `Modules/Core/Models/User.php`. Append `'recurring_detection_window_months'` and `'recurring_income_min_amount_minor'` to the `$fillable` array (alphabetical position alongside the other recurring/auto-import fields). Add both columns to the `casts()` return array as `'integer'`. Add `@property int $recurring_detection_window_months` and `@property int $recurring_income_min_amount_minor` lines to the class-level `@property` docblock so Larastan strict resolves them.

    Modify `Modules/Core/Internal/Http/Livewire/SettingsPage.php`. Add two public properties immediately after the existing `$autoImportFromDropFolder` property (preserving alphabetical-by-domain grouping per the class):
    ```
    #[Validate('required|integer|min:3|max:60')]
    public int $recurringDetectionWindowMonths = 18;

    #[Validate('required|integer|min:0|max:100000000')]
    public int $recurringIncomeMinAmountMinor = 200000;
    ```
    Add corresponding `@property`-style docblock entries on each so the executor records intent (per D-802 / D-820 — the docblock describes the field's role today, never history).

    Extend `mount()` to pre-fill both:
    ```
    $this->recurringDetectionWindowMonths = (int) $user->recurring_detection_window_months;
    $this->recurringIncomeMinAmountMinor = (int) $user->recurring_income_min_amount_minor;
    ```

    Extend `save()` to write both:
    ```
    $user->recurring_detection_window_months = $this->recurringDetectionWindowMonths;
    $user->recurring_income_min_amount_minor = $this->recurringIncomeMinAmountMinor;
    ```
    (The existing `$user->save();` call covers the new columns once they're on the model.)

    Extend `messages()` with four new entries — one calm sentence per attribute, applied uniformly across all sub-rules:
    ```
    'recurringDetectionWindowMonths.required' => 'Choose between 3 and 60 months.',
    'recurringDetectionWindowMonths.integer'  => 'Choose between 3 and 60 months.',
    'recurringDetectionWindowMonths.min'      => 'Choose between 3 and 60 months.',
    'recurringDetectionWindowMonths.max'      => 'Choose between 3 and 60 months.',
    'recurringIncomeMinAmountMinor.required'  => 'Enter an amount from €0 upward.',
    'recurringIncomeMinAmountMinor.integer'   => 'Enter an amount from €0 upward.',
    'recurringIncomeMinAmountMinor.min'       => 'Enter an amount from €0 upward.',
    'recurringIncomeMinAmountMinor.max'       => 'Enter an amount from €0 upward.',
    ```

    Modify `Modules/Core/Resources/views/livewire/settings-page.blade.php`. Add a new section labelled "Recurring detection" between the existing currency-display section and the auto-import section (or wherever fits the calm layout — the executor verifies the existing section order by reading the file). The section contains two numeric `<input wire:model="recurringDetectionWindowMonths" type="number" min="3" max="60">` and `<input wire:model="recurringIncomeMinAmountMinor" type="number" min="0">` controls with helper text describing each field. The income minimum input accepts BIGINT minor units; the helper text below renders the user-friendly EUR equivalent via the project Money formatter (confirm the formatter path by reading the existing Blade files that already format minor units — typically `Modules/Ledger/Public/ValueObjects/Money.php::format()` or a Flux helper). Error messages display per-attribute under each input using the existing `@error('recurringDetectionWindowMonths') ... @enderror` pattern visible in the file.

    Create `Modules/Core/tests/Feature/SettingsRecurringFieldsTest.php` mirroring `SettingsPageTest.php` (entire file pattern — `beforeEach` user seed + `actingAs` + `Livewire::test(SettingsPage::class)`). Test cases:
    - `renders-with-current-prefs`: update the user row to `recurring_detection_window_months=24` + `recurring_income_min_amount_minor=150000`; mount the page; assert both Livewire properties equal the seeded values.
    - `persists-detection-window-change`: set `recurringDetectionWindowMonths=12`; call `save`; assert no errors + `users.recurring_detection_window_months == 12`.
    - `persists-income-minimum-change`: set `recurringIncomeMinAmountMinor=150000`; call `save`; assert no errors + `users.recurring_income_min_amount_minor == 150000`.
    - `rejects-detection-window-below-3`: set `recurringDetectionWindowMonths=2`; call `save`; assert error `'recurringDetectionWindowMonths' => 'Choose between 3 and 60 months.'` + row unchanged.
    - `rejects-detection-window-above-60`: set `recurringDetectionWindowMonths=61`; assert same error shape.
    - `rejects-negative-income-minimum`: set `recurringIncomeMinAmountMinor=-1`; call `save`; assert error + row unchanged.
    - `round-trips-both-fields`: set both to new values; save; reload user; assert both persisted (locks the dual-field write).
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Core/tests/Feature/SettingsRecurringFieldsTest.php Modules/Core/tests/Feature/SettingsPageTest.php --stop-on-failure &amp;&amp; composer analyse</automated>
  </verify>
  <done>
    - `Modules/Core/Models/User.php` `$fillable` includes both new columns (verify `grep -c "recurring_detection_window_months" Modules/Core/Models/User.php` returns ≥ 1).
    - `Modules/Core/Internal/Http/Livewire/SettingsPage.php` contains both `recurringDetectionWindowMonths` and `recurringIncomeMinAmountMinor` public properties annotated with `#[Validate]`.
    - `Modules/Core/Resources/views/livewire/settings-page.blade.php` renders both new inputs (verify `grep -c "recurringDetectionWindowMonths\|recurringIncomeMinAmountMinor" Modules/Core/Resources/views/livewire/settings-page.blade.php` returns ≥ 2).
    - `Modules/Core/tests/Feature/SettingsRecurringFieldsTest.php` exists and all seven slices pass.
    - The existing `SettingsPageTest` continues to pass (no regression in defaultCurrencyView / periodStartDay / autoImportFromDropFolder behaviour).
    - `composer analyse` exits 0 (Larastan max + strict accepts the two new typed properties).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migrations → SQLite triggers | The state-validation triggers fire before INSERT/UPDATE; bypassing them via raw SQL is the threat model. |
| State machine → recurring_series.state column | Only `RecurringSeriesStateMachine` may write the column; the arch test from Plan 01 + the SQLite trigger enforce this. |
| Public DTOs → downstream consumers (Phase 9/10) | Property shape changes here cascade into Phase 9 + Phase 10 reads; the DTOs are the contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | recurring_series.state via raw Eloquent UPDATE | mitigate | Two layers: (1) `noOtherRecurringSeriesStateMutator` arch test (Plan 01) scans for ->update on state outside the state-machine path; (2) SQLite triggers raise ABORT on any state value not in the allowed set. |
| T-08-06 | Tampering | recurring_series_transitions append-only invariant | mitigate | The state machine is the sole inserter; the audit table has no UPDATE/DELETE path in any module code. Standard schema-level invariant — no triggers needed. |
| T-08-07 | Repudiation | Audit trail missing actor / reason | mitigate | The state machine REQUIRES `$reason` and `$actor` arguments — no overload accepts null. The unit test covers the "no defaults" assertion. |
| T-08-08 | Denial of service | Concurrent transitions racing on the same series | mitigate | `lockForUpdate()` serialises within the SQLite WAL connection; `PRAGMA busy_timeout=5000` gives 5 seconds of contention retry before raising. |
| T-08-09 | Information disclosure | Cross-user model access via Eloquent global scope | mitigate | `BelongsToUser` concern is applied to all three models; Public Actions enforce explicit `where('user_id', $user->id)` per Plan 03 patterns. |
| T-08-09b | Tampering | `/settings` user preferences (recurring detection window + income minimum) via crafted Livewire payload | mitigate | `#[Validate]` attributes enforce server-side bounds (`min:3|max:60` for window, `min:0|max:100000000` for income minimum); the test suite asserts out-of-range values are rejected and the users row stays unchanged. `CurrentUser` resolves the authenticated user — never a request-supplied id — so cross-user writes are structurally impossible (mirror of the existing `SettingsPage` posture). |
</threat_model>

<verification>
- `vendor/bin/pest Modules/Recurring/tests/ --stop-on-failure` exits 0 (migration + state machine + DTO tests all green).
- `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --stop-on-failure` exits 0 (the `noOtherRecurringSeriesStateMutator` invariant scans against a real state machine class; the `noSynchronousDetectionInRequestLifecycle` scans against the real `SeriesDetector` contract).
- `vendor/bin/pest Modules/Core/tests/Feature/SettingsRecurringFieldsTest.php Modules/Core/tests/Feature/SettingsPageTest.php --stop-on-failure` exits 0 (new settings fields + existing Phase 3 settings behaviour both green).
- `composer test && composer analyse && composer format:check` exits 0.
- `php artisan migrate:fresh --env=testing` exits 0.
- `grep -c 'BelongsToUser' Modules/Recurring/Models/RecurringSeries.php` returns 1 (FND-03 compliance verified statically).
- `grep -c 'recurringDetectionWindowMonths' Modules/Core/Internal/Http/Livewire/SettingsPage.php` returns ≥ 1.
</verification>

<success_criteria>
- Three new tables and two new columns on `users` exist after migrate; the SQLite triggers reject invalid state values; the UNIQUE constraint on `(user_id, direction, cluster_key, latest_currency)` enforces detector idempotency.
- `RecurringSeriesStateMachine` is the only legal mutator of `recurring_series.state`; the ALLOWED_TRANSITIONS map matches CONTEXT D-815 exactly; transition writes are atomic with audit-row inserts under `lockForUpdate` + `PRAGMA busy_timeout=5000`.
- Public surface (1 contract + 4 DTOs + 4 events) exists and is stable for Plans 03/04/05 to build against.
- All Plan 01 arch invariants stay green; the `noOtherRecurringSeriesStateMutator` invariant now scans real code (state machine exists) instead of trivially passing.
- `/settings` exposes the two new user-configurable preferences (`recurring_detection_window_months`, `recurring_income_min_amount_minor`) with validation that rejects out-of-bounds values and persists valid ones to the users row (D-802 / D-820).
</success_criteria>

<output>
Create `.planning/phases/08-recurring-detection-fixed-payments-view/08-02-SUMMARY.md` when done. Record:
- The four migration filenames + their final ordering vis-à-vis Phase 6/7 migrations on `users`.
- Confirmation that the SQLite triggers fire as expected (capture the QueryException message string for the "garbage state" test).
- The exact list of singleton bindings in `RecurringServiceProvider::register()` (currently just `RecurringSeriesStateMachine`).
- Confirmation that the two new `/settings` inputs round-trip values to the users row (capture the saved row from one of the SettingsRecurringFieldsTest slices).
- Any deviation from the locked column shape (none expected — RESEARCH lines 985–1013 is the lock).
</output>
