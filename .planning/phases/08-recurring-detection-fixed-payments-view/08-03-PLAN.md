---
phase: 08-recurring-detection-fixed-payments-view
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - Modules/Recurring/Internal/CadenceInferrer.php
  - Modules/Recurring/Internal/Detection/ClusterKeyComposer.php
  - Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php
  - Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php
  - Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php
  - Modules/Recurring/Public/Services/RecurringSeriesQuery.php
  - Modules/Recurring/Public/Actions/ApproveRecurringSeries.php
  - Modules/Recurring/Public/Actions/RejectRecurringSeries.php
  - Modules/Recurring/Public/Actions/SnoozeRecurringSeries.php
  - Modules/Recurring/Public/Actions/EditRecurringSeriesName.php
  - Modules/Recurring/Public/Actions/UnRejectRecurringSeries.php
  - Modules/Recurring/Resources/views/livewire/recurring-review-page.blade.php
  - Modules/Recurring/Routes/web.php
  - Modules/Recurring/Providers/RecurringServiceProvider.php
  - routes/console.php
  - Modules/Recurring/tests/Unit/CadenceInferenceTest.php
  - Modules/Recurring/tests/Unit/ClusterKeyComposerTest.php
  - Modules/Recurring/tests/Feature/DetectRecurringSeriesJobTest.php
  - Modules/Recurring/tests/Feature/RecurringReviewPageTest.php
  - Modules/Recurring/tests/Feature/ApproveRecurringSeriesTest.php
  - Modules/Recurring/tests/Feature/RejectRecurringSeriesTest.php
  - Modules/Recurring/tests/Feature/SnoozeRecurringSeriesTest.php
  - Modules/Recurring/tests/Feature/EditRecurringSeriesNameTest.php
  - Modules/Recurring/tests/Feature/UnRejectRecurringSeriesTest.php
  - Modules/Recurring/tests/Feature/CrossUserRecurringSeriesIsolationTest.php
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-03

user_setup: []

must_haves:
  truths:
    - "Hybrid trigger model in place: the detector runs as a scheduled daily sweep AND the `/recurring/re-detect` button dispatches the same job on demand; user can run `php artisan recurring:detect --user=1` (or wait for the daily schedule) and see expense-side recurring suggestions land in `recurring_series` with state='pending' (D-801)"
    - "On-demand re-detect re-queues the same `DetectRecurringSeriesJob` with a `ShouldBeUniqueUntilProcessing` lock so spam-clicking is a no-op; the synchronous in-request alternative is rejected (D-805)"
    - "Detector emits a candidate as soon as 2 occurrences cluster — minimum-2-occurrences rule is honoured by ExpenseSeriesDetector and the CadenceInferrer treats 2 timestamps as a valid yearly anchor (D-803)"
    - "User can open `/recurring/review` (the dedicated review queue, NOT inline filters on `/recurring`) while authenticated and see the pending suggestions in a Livewire SFC (D-806)"
    - "Each pending row exposes exactly four actions: Approve / Reject / Edit name / Snooze (D-807)"
    - "Approving a series flips its state to 'approved' AND writes a `recurring_series_transitions` row atomically; the action is idempotent (approving twice is a no-op on the second call); cross-user IDs return 404"
    - "Rejecting a series is permanent until un-rejected — new occurrences of a rejected series never re-prompt, and a Rejected tab exposes the un-reject action; rejection does NOT mutate any underlying transaction (D-808, D-823)"
    - "Snooze uses a date-picker with 1 week / 1 month / 3 months / custom preset chips; snoozing writes `snoozed_until`; the next sweep flips it back to `pending` once the timestamp elapses (Pitfall 8 mitigation, D-810)"
    - "Approving a series flips ONLY the recurring state machine — it does NOT write `merchant_memories`; merchant categorization remains decoupled from series approval (D-811)"
    - "Edit-name persists across sweeps — the detector never clobbers `display_name_override`; the auto-derived `detected_name` keeps refreshing underneath so clearing the override falls back automatically (D-813)"
    - "Every Approve / Reject / Snooze dispatches a 10-second Undo toast via `$this->dispatch('toast', ...)` + Alpine `x-on:toast.window`, mirroring Phase 4/5 (D-814)"
    - "Expense detector reads transactions WHERE type IN ('expense','fee','refund'); income detection is reserved for Plan 04 reading ONLY type='income' (D-822)"
    - "Re-running the sweep against the same fixture twice produces the same series set with the same occurrence rows (idempotent)"
    - "Clustering keys on original currency + amount, not settled EUR — a USD $11.99 charge clusters with other USD $11.99 charges regardless of FX drift on the settled side (D-839)"
    - "Cadence inference uses median interval + nearest-class snap (weekly / monthly / quarterly / yearly bands) and skips irregular clusters; missed-interval tolerance keeps one bank-holiday gap from fragmenting a stable subscription (D-843)"
  artifacts:
    - path: Modules/Recurring/Internal/CadenceInferrer.php
      provides: Stateless `infer(list<CarbonImmutable>): array{cadence,median_interval_days,next_expected_at,confidence_low,missed_count}` using median-snap + D-844 missed-interval tolerance
      contains: "MISSED_INTERVAL_MULTIPLIER"
    - path: Modules/Recurring/Internal/Detection/ClusterKeyComposer.php
      provides: Deterministic `compose(direction, counterpartyKey, originalCurrencyCode, cadenceBand): string` producing strings like `expense::spotify-bv::eur::monthly` for the `recurring_series.cluster_key` UNIQUE constraint
      contains: "compose"
    - path: Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php
      provides: Reads transactions WHERE type IN ('expense','fee','refund'), clusters on (counterparty, original_currency, amount-within-tolerance), creates pending series + occurrence rows via the state machine
      exports: ["ExpenseSeriesDetector"]
    - path: Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php
      provides: ShouldBeUniqueUntilProcessing per-user queued job that runs snooze-expiry pass first (Pitfall 8), then iterates container-tagged 'recurring.detector' implementations
      contains: "ShouldBeUniqueUntilProcessing"
    - path: Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php
      provides: /recurring/review Livewire SFC with per-row Approve / Reject / Snooze / Edit-name action methods (method-parameter DI; no constructor injection)
      contains: "RecurringReviewPage"
    - path: Modules/Recurring/Public/Services/RecurringSeriesQuery.php
      provides: pendingForUser, pendingCountForUser, rejectedForUser, approvedForUser, forSeries (Public read API for review page + Plan 04/05)
      exports: ["RecurringSeriesQuery"]
    - path: Modules/Recurring/Public/Actions/ApproveRecurringSeries.php
      provides: Public Action — cross-user 404 + idempotent no-op + state-machine transition + RecurringSeriesApproved event
    - path: routes/console.php
      provides: `Schedule::call(...)->name('recurring.detect')->daily()->withoutOverlapping(30)` entry that dispatches DetectRecurringSeriesJob per user
      contains: "recurring.detect"
  key_links:
    - from: Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php
      to: ExpenseSeriesDetector (via container tag 'recurring.detector')
      via: iterable injection on handle()
      pattern: "tagged\\('recurring.detector'\\)"
    - from: Modules/Recurring/Public/Actions/ApproveRecurringSeries.php
      to: RecurringSeriesStateMachine
      via: constructor DI + state-machine transition
      pattern: "RecurringSeriesStateMachine"
    - from: Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php
      to: Modules/Categorization/Public/Services/MerchantMemoryQuery (forCounterpartiesNormalized batch method from Plan 01)
      via: constructor DI
      pattern: "MerchantMemoryQuery"
    - from: routes/console.php
      to: DetectRecurringSeriesJob
      via: Schedule::call() daily dispatch with per-user loop
      pattern: "recurring.detect"
    - from: Modules/Recurring/Routes/web.php
      to: RecurringReviewPage Livewire component
      via: Route::get('/recurring/review', RecurringReviewPage::class)
      pattern: "recurring.review"
---

<objective>
Ship the first end-to-end vertical slice: a user runs the detector (manually or via the daily schedule), the expense detector clusters transactions into pending recurring suggestions, the user opens `/recurring/review` and sees them, the user clicks Approve / Reject / Snooze / Edit-name, the state machine processes the transition, and a 10-second Undo toast surfaces. Income detection lands in Plan 04; the list view + dashboard card land in Plans 04/05.

Purpose: Lock the detector algorithm + state machine flow + review-queue UX in one demoable slice (REC-01 / REC-02 / REC-03). Multi-user safety is exercised end-to-end via the cross-user isolation feature test. Idempotency is locked via the contract test.

Output: Working expense detector + queued job + scheduled trigger + review page + five Public Actions + read-side query. After this plan ships, the user can demo: "Run the detector, approve a Spotify suggestion, watch it land in the approved state."
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-VALIDATION.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-01-SUMMARY.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-02-SUMMARY.md
@CLAUDE.md
@Modules/Chains/Internal/Jobs/ResolveChainLinksJob.php
@Modules/Chains/Internal/Http/Livewire/ChainReviewQueue.php
@Modules/Chains/Public/Actions/ConfirmChainLink.php
@Modules/Chains/Public/Actions/RejectChainLink.php
@Modules/Chains/Public/Services/ChainLinkQuery.php
@Modules/Categorization/Public/Services/MerchantMemoryQuery.php
@Modules/Core/Public/Contracts/CurrentUser.php
@Modules/Core/Public/Contracts/Clock.php
@Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php
@Modules/Recurring/Models/RecurringSeries.php
@Modules/Recurring/Public/Contracts/SeriesDetector.php
@Modules/Recurring/Public/Dto/RecurringSeriesDto.php
@Modules/Recurring/Public/Events/RecurringSeriesApproved.php
@routes/console.php
@tests/Contracts/RecurringDetectionContractTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CadenceInferrer + ClusterKeyComposer + ExpenseSeriesDetector + DetectRecurringSeriesJob + scheduler entry</name>
  <files>
    Modules/Recurring/Internal/CadenceInferrer.php,
    Modules/Recurring/Internal/Detection/ClusterKeyComposer.php,
    Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php,
    Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php,
    Modules/Recurring/Providers/RecurringServiceProvider.php,
    routes/console.php,
    Modules/Recurring/tests/Unit/CadenceInferenceTest.php,
    Modules/Recurring/tests/Unit/ClusterKeyComposerTest.php,
    Modules/Recurring/tests/Feature/DetectRecurringSeriesJobTest.php,
    tests/Contracts/RecurringDetectionContractTest.php
  </files>
  <read_first>
    Modules/Chains/Internal/Jobs/ResolveChainLinksJob.php (the entire 143-line file — exact ShouldBeUniqueUntilProcessing + ShouldQueue traits, $tries=3, $backoff=[60,300,900], uniqueId() returns userId, uniqueFor() returns 600, uniqueVia() returns Cache::driver('redis') with the docblock carve-out rationale),
    Modules/Chains/Internal/Resolvers/IcsSettlementResolver.php (the resolver shape — DI of DatabaseManager + Clock + helpers, scans transactions for a user, writes via a helper class; the ExpenseSeriesDetector mirrors this lifecycle but writes recurring_series + recurring_series_occurrences instead of chain_links),
    Modules/Categorization/Public/Services/MerchantMemoryQuery.php (the batch method `forCounterpartiesNormalized` from Plan 01 — the detector uses this to decorate clusters with categories),
    Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php (Plan 02 — the detector uses `transition()` only when flipping an approved series to `cadence_changed` or when expiring snoozes; for new pending series the detector INSERTs the row directly and dispatches `RecurringSeriesDetected` — no state transition because the row is born in `pending` state),
    routes/console.php lines 35–76 (the `email-scan.incremental` and `email-scan.discovery` per-user daily/hourly dispatch closure shape — copy exactly for the new `recurring.detect` entry),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md "Pattern 4: Cadence Inferrer" lines 596–646 (the algorithm template) + Pitfall 4 (missed-interval tolerance) + Pitfall 8 (snooze-expiry pass) + Open Q5 (ClusterKeyComposer shape),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md "Detectors + cadence math (Waves 2 + 3)" + "Queued job + scheduled trigger (Wave 2)",
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-801..D-805 (trigger model), D-843/D-844 (cadence math), D-849 (container tag mechanics)
  </read_first>
  <behavior>
    - `CadenceInferrer::infer(list&lt;CarbonImmutable&gt;): array` accepts a sorted-ascending list of timestamps and returns `['cadence' =&gt; 'weekly'|'monthly'|'quarterly'|'yearly'|'irregular', 'median_interval_days' =&gt; float, 'next_expected_at' =&gt; ?CarbonImmutable, 'confidence_low' =&gt; bool, 'missed_count' =&gt; int]`.
    - Snap bands per D-843: `<10d=weekly`, `10-45d=monthly`, `80-100d=quarterly`, `350-380d=yearly`; outside all bands → `irregular`.
    - Missed-interval tolerance per D-844: any single interval `> 1.8 * median` counts as 1 missed period and is excluded from the median calculation (the test for `missing-month-subscription.php` from Plan 01 covers this).
    - `confidence_low` is `true` when stddev of intervals > 5 days (per D-830).
    - `ClusterKeyComposer::compose(string $direction, string $counterpartyKey, string $originalCurrencyCode, string $cadenceBand): string` returns a normalized lowercase deterministic string like `expense::spotify-bv::eur::monthly`. The composer normalises whitespace, lowercases all parts, and strips disallowed characters so the UNIQUE constraint from Plan 02 doesn't collide on punctuation variance.
    - `ExpenseSeriesDetector::detectForUser(User)` implements `SeriesDetector`:
       1. Loads transactions for the user within the detection window (default 18 months per `users.recurring_detection_window_months`) WHERE `type IN ('expense','fee','refund')`.
       2. Groups by `(counterparty_normalized, original_currency)`; for each group with ≥ 2 occurrences (D-803), computes cadence via `CadenceInferrer`.
       3. Filters out clusters where the variance band is exceeded (D-825 — any occurrence outside `latest_amount × (1 ± tolerance)` where tolerance defaults to 25% is excluded from the cluster, potentially fragmenting it).
       4. For new clusters (no existing `recurring_series` row with matching `cluster_key`), inserts a new pending row and dispatches `RecurringSeriesDetected`.
       5. For existing approved clusters, refreshes metrics (latest_amount, latest_currency, latest_fx_rate_used, monthly_equivalent, latest_funding_chain_link_id, next_expected_at, next_expected_confidence_low) WITHOUT touching `state` (Pitfall 3).
       6. If an approved cluster's cadence class has flipped, calls `RecurringSeriesStateMachine::transition($series, 'cadence_changed', 'detector_cadence_flip', 'detector')` and dispatches `RecurringSeriesCadenceFlipped`.
       7. INSERT-OR-IGNORE matching `recurring_series_occurrences` rows for every observed transaction (UNIQUE `rec_occ_uniq` from Plan 02 makes this idempotent).
       8. NEVER writes to `transactions` (enforced by `noTransactionWritesFromRecurring` arch test from Plan 01).
    - `DetectRecurringSeriesJob` implements `ShouldBeUniqueUntilProcessing` + `ShouldQueue`; constructor takes `public readonly int $userId`; `uniqueId()` returns the userId; `uniqueFor()` returns 600 seconds; `uniqueVia()` returns `Cache::driver('redis')` (the single permitted facade carve-out from Plan 01).
    - `handle(DatabaseManager $db, Clock $clock, iterable $detectors)` first runs a snooze-expiry pass (`UPDATE recurring_series SET state='pending' ... WHERE state='snoozed' AND snoozed_until <= now()` — go through the state machine so the audit row is written with `transition_reason='snooze_expired'`, `actor='detector'`), then iterates the container-tagged detectors and calls `detectForUser($user)` on each. The `iterable` injection resolves via Laravel's `$this->app->tagged('recurring.detector')` lookup which is bound in the service provider.
    - The scheduled entry `Schedule::call(...)->name('recurring.detect')->daily()->withoutOverlapping(30)` runs once per day per user and dispatches the job for every user_id in the users table (matching the `email-scan.discovery` shape).
    - `vendor/bin/pest Modules/Recurring/tests/Unit/CadenceInferenceTest.php` passes a Pest dataset over ~15-20 interval-list/expected-class pairs covering: weekly stable, monthly stable, quarterly stable, yearly (2 occurrences), monthly with one missed period (the median tolerates), monthly with 3 missed periods in a rolling 6 window (flips to cadence_changed candidate), edge cases at the band boundaries (9 days → weekly, 10 days → monthly, 45 days → monthly, 46 days → irregular).
    - `vendor/bin/pest Modules/Recurring/tests/Feature/DetectRecurringSeriesJobTest.php --filter='expense-cluster'` exits 0 — runs the detector against the Plan 01 `stable-monthly-spotify.php` fixture and asserts one expense series with cadence=monthly is created. Similar `--filter='variance-tolerance'`, `--filter='variance-exceeded'`, `--filter='idempotent-re-run'` test slices cover REC-02 (drifting Spotify clusters as one), variance-violation (variable bills fragments), and idempotency (running the job twice produces the same series set).
  </behavior>
  <action>
    Create `Modules/Recurring/Internal/CadenceInferrer.php` per RESEARCH Pattern 4 lines 603–646. Class is `final class` (stateless, not readonly). The class declares the snap-band constants exactly: `WEEKLY_MAX=10`, `MONTHLY_MIN=10`, `MONTHLY_MAX=45`, `QUARTERLY_MIN=80`, `QUARTERLY_MAX=100`, `YEARLY_MIN=350`, `YEARLY_MAX=380`. Missed-tolerance constants (canonical names — RESEARCH.md Pattern 4 line 623 declares these verbatim): `MISSED_INTERVAL_MULTIPLIER=1.8`, `MAX_MISSED_PER_WINDOW=2`, `MISSED_WINDOW_SIZE=6`.

    Implement `public function infer(array $sortedTimestamps): array`:
    1. Compute interval days between consecutive `CarbonImmutable` entries via `diffInDays`.
    2. Compute provisional median.
    3. Mark intervals > `1.8 × provisional_median` as "missed periods" — exclude these from a refined-median pass.
    4. Compute final median + stddev on the filtered intervals.
    5. Snap median to nearest class per the constants above; outside all bands → `cadence='irregular'`.
    6. Project `next_expected_at = end($sortedTimestamps)->copy()->addDays((int) round($medianIntervalDays))` (null when cadence='irregular').
    7. Set `confidence_low = stddev > 5.0` per D-830.
    8. Return the structured array.

    Create `Modules/Recurring/Internal/Detection/ClusterKeyComposer.php`. `final readonly class` (stateless, deterministic, no DI). Implement `public function compose(string $direction, string $counterpartyKey, string $originalCurrencyCode, string $cadenceBand): string` returning a lowercased, hyphen-normalised, double-colon-separated string. Example output for Spotify expense in EUR with monthly cadence: `expense::spotify-bv::eur::monthly`. Normalisation rules: lowercase all parts; replace non-alphanumeric sequences in `counterpartyKey` with single hyphens; strip leading/trailing hyphens; max 60 characters per part to fit the `string('cluster_key')` column.

    Create `Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php`. `final class` (stateful — uses DI). Constructor DI:
    ```
    public function __construct(
        private readonly DatabaseManager $db,
        private readonly Clock $clock,
        private readonly CadenceInferrer $cadenceInferrer,
        private readonly ClusterKeyComposer $clusterKeyComposer,
        private readonly RecurringSeriesStateMachine $stateMachine,
        private readonly MerchantMemoryQuery $merchantMemory,
        private readonly Dispatcher $events,
    ) {}
    ```
    Implements `SeriesDetector::detectForUser(User $user): void`. Algorithm:
    1. Read `users.recurring_detection_window_months` and compute `since = $this->clock->now()->subMonths($windowMonths)`.
    2. Use the injected `DatabaseManager` to query transactions: `select id, posted_at, booked_at, original_amount_minor, original_currency, settled_amount_minor, settled_currency, fx_rate_used, counterparty_normalized, counterparty_iban from transactions where user_id=? and type in ('expense','fee','refund') and posted_at >= ?`. Use raw query-builder, not Eloquent (Larastan strict + the project convention).
    3. Group rows in PHP by `(counterparty_normalized, original_currency)`; for each group with ≥ 2 rows, compute the timestamp list + amount list.
    4. Apply variance-tolerance filter: load the existing `recurring_series` row (if any) to get its `variance_tolerance_percent` (default 25). Filter the cluster's amounts to those within `latest_amount × (1 ± tolerance / 100)` of the cluster's median amount. If the cluster splinters (rows excluded), recurse on the excluded subset for potential second-series creation.
    5. Call `CadenceInferrer::infer($timestamps)` → cadence result.
    6. Skip if `cadence === 'irregular'` (D-843).
    7. Compose the `cluster_key` via `ClusterKeyComposer::compose('expense', $counterpartyNormalized, $originalCurrency, $cadence)`.
    8. Look up existing series via `where('user_id', $user->id)->where('direction', 'expense')->where('cluster_key', $clusterKey)->where('latest_currency', $originalCurrency)->first()`.
    9. New cluster → INSERT into `recurring_series` (state='pending', direction='expense', detected_name = $counterpartyNormalized, latest_amount_minor, latest_currency, monthly_equivalent_minor via cadence multiplier per D-826, cadence, next_expected_at, next_expected_confidence_low). Then INSERT-OR-IGNORE matching `recurring_series_occurrences` rows. Dispatch `RecurringSeriesDetected`.
    10. Existing cluster, state='approved' or 'cadence_changed' → metric-refresh UPDATE on the row WITHOUT touching `state` (Pitfall 3). If `cadence` changed class, call `$this->stateMachine->transition($series, 'cadence_changed', 'detector_cadence_flip', 'detector')` and dispatch `RecurringSeriesCadenceFlipped`.
    11. Existing cluster, state='pending' → metric-refresh same as above; pending suggestions update in place.
    12. Existing cluster, state='rejected' → SKIP entirely (D-808 — reject is permanent until un-rejected; the detector never re-prompts rejected clusters).

    The detector NEVER writes the `transactions` table (per Plan 01 arch test). Read-only access to the categorization layer via `MerchantMemoryQuery::forCounterpartiesNormalized` decorates the row with the category id when the user has previously categorised the merchant.

    Create `Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php` exactly mirroring `Modules/Chains/Internal/Jobs/ResolveChainLinksJob.php`. Use the lines 57–96 boilerplate. The class docblock MUST explicitly describe the `Cache::driver('redis')` carve-out for the `noFacadeCallsFromRecurring` arch test (the FQN was added to the carve-out array in Plan 01). The `handle()` method DI signature:
    ```
    public function handle(
        DatabaseManager $db,
        Clock $clock,
        iterable $detectors,
        RecurringSeriesStateMachine $stateMachine,
    ): void
    ```
    Body:
    1. Load `$user = User::query()->where('id', $this->userId)->firstOrFail()`.
    2. SNOOZE-EXPIRY PASS (Pitfall 8) — Select `recurring_series` rows for the user where `state='snoozed'` AND `snoozed_until <= now()`; for each, call `$stateMachine->transition($series, 'pending', 'snooze_expired', 'detector')`.
    3. Iterate `foreach ($detectors as $detector) { $detector->detectForUser($user); }`.

    Modify `Modules/Recurring/Providers/RecurringServiceProvider.php`. Add to `register()`:
    ```
    $this->app->singleton(CadenceInferrer::class);
    $this->app->singleton(ClusterKeyComposer::class);
    $this->app->singleton(ExpenseSeriesDetector::class);
    $this->app->singleton(DetectRecurringSeriesJob::class);
    $this->app->tag([ExpenseSeriesDetector::class], 'recurring.detector');
    ```
    (Income detector lands in Plan 04 — it will be appended to the `tag([...])` array there.)

    Modify `routes/console.php`. Append after the existing `receipts.scan-drop-folder` entry (around line 112):
    ```
    // Daily recurring-series detection sweep: dispatches
    // DetectRecurringSeriesJob once per user per day. The job runs
    // a snooze-expiry pass first (flipping snoozed → pending where
    // snoozed_until has elapsed), then iterates every container-tagged
    // 'recurring.detector' implementation against the user's detection
    // window (default 18 months). The job's ShouldBeUniqueUntilProcessing
    // lock keyed on userId collapses a same-day re-dispatch (scheduled
    // or via the on-demand button in /recurring) into a single queued
    // pass.
    // Method order .name() BEFORE .daily()->withoutOverlapping(30) —
    // CallbackEvent::withoutOverlapping throws when description is not
    // set yet (same shape as the email-scan + receipts entries above).
    Schedule::call(function (DatabaseManager $db, Dispatcher $bus): void {
        $userIds = $db->connection()->table('users')->pluck('id');
        foreach ($userIds as $id) {
            $bus->dispatch(new DetectRecurringSeriesJob((int) $id));
        }
    })->name('recurring.detect')->daily()->withoutOverlapping(30);
    ```
    Add the `use Modules\Recurring\Internal\Jobs\DetectRecurringSeriesJob;` import at the top of the file in alphabetical position.

    Create `Modules/Recurring/tests/Unit/CadenceInferenceTest.php` as a Pest dataset test (~15-20 rows) covering D-843 boundaries + D-844 missed-tolerance + D-830 confidence-low. The dataset rows include:
    - 4 weekly timestamps spaced 7 days → cadence=weekly
    - 4 monthly timestamps spaced 30 days → cadence=monthly
    - 4 quarterly timestamps spaced 91 days → cadence=quarterly
    - 2 yearly timestamps spaced 365 days → cadence=yearly
    - intervals spaced 9, 9, 9 days → cadence=weekly (lower boundary)
    - intervals spaced 10 days → cadence=monthly (lower band boundary)
    - intervals spaced 45 days → cadence=monthly (upper band boundary)
    - intervals spaced 46 days → cadence=irregular (just outside)
    - mixed intervals 30, 60, 30, 30 (one missed period) → cadence=monthly (tolerance covers it)
    - mixed intervals 30, 30, 30, 30 → cadence=monthly with `confidence_low=false`
    - mixed intervals 25, 30, 35, 40 (stddev > 5) → cadence=monthly with `confidence_low=true`

    Create `Modules/Recurring/tests/Unit/ClusterKeyComposerTest.php` asserting deterministic shape for various inputs (spaces normalised, capitals lowercased, punctuation collapsed to hyphens, idempotent — same input twice produces same output).

    Create `Modules/Recurring/tests/Feature/DetectRecurringSeriesJobTest.php` using `RefreshDatabase`. Tests under named `->filter()` slices:
    - `expense-cluster`: load `stable-monthly-spotify.php` fixture, seed transactions for a user, dispatch the job synchronously (use `Queue::fake()` not needed — call `(new DetectRecurringSeriesJob($user->id))->handle($db, $clock, [app(ExpenseSeriesDetector::class)], app(RecurringSeriesStateMachine::class))` to bypass the queue), assert exactly one row in `recurring_series` with `direction='expense'`, `state='pending'`, `cadence='monthly'`, and 18 occurrence rows.
    - `variance-tolerance`: load `drifting-monthly-spotify.php` (€9.99 → €11.49), run the job, assert ONE series.
    - `variance-exceeded`: load `variable-amount-beyond-tolerance-bills.php`, assert MULTIPLE short series (fragmentation per D-825).
    - `idempotent-re-run`: run the job twice with the same fixture, assert `recurring_series` row count and `recurring_series_occurrences` row count are unchanged.
    - `snooze-expiry`: seed a series with `state='snoozed'`, `snoozed_until = now()->subHour()`; run the job; assert state has flipped to `pending` and a `transitions` row exists with `transition_reason='snooze_expired'`, `actor='detector'`.
    - `cadence-flip-flips-to-cadence_changed`: seed an approved monthly series; seed transactions matching quarterly cadence; run the job; assert state has flipped to `cadence_changed` and a `transitions` row exists with `transition_reason='detector_cadence_flip'`, `actor='detector'`; assert `RecurringSeriesCadenceFlipped` event was dispatched (use `Event::fake([RecurringSeriesCadenceFlipped::class])`).

    Update `tests/Contracts/RecurringDetectionContractTest.php` (the scaffold from Plan 01): remove the `->skip(...)` directive and populate with one end-to-end test that loads ALL Plan-01 synthesised fixtures, seeds the transactions, runs the detector once, and asserts the expected series count per fixture (per each fixture's `expected` block). The income-fixture expectations stay skipped until Plan 04 lands the IncomeSeriesDetector — the test handles this by checking `expected['skip_until_income_detector']` per row.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Recurring/tests/Unit/CadenceInferenceTest.php Modules/Recurring/tests/Unit/ClusterKeyComposerTest.php Modules/Recurring/tests/Feature/DetectRecurringSeriesJobTest.php tests/Contracts/RecurringDetectionContractTest.php tests/Contracts/BoundaryArchTest.php --stop-on-failure</automated>
  </verify>
  <done>
    - `Modules/Recurring/Internal/CadenceInferrer.php` exists with `MISSED_INTERVAL_MULTIPLIER` and snap-band constants matching D-843/D-844 (`grep -v '^[[:space:]]*[/*#]' Modules/Recurring/Internal/CadenceInferrer.php | grep -c 'MISSED_INTERVAL_MULTIPLIER'` returns ≥ 1).
    - `Modules/Recurring/Internal/Detection/ClusterKeyComposer.php` exists and the unit test asserts deterministic output.
    - `Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php` implements `SeriesDetector` interface.
    - `Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php` contains the literal substring `ShouldBeUniqueUntilProcessing` and `Cache::driver('redis')`.
    - `routes/console.php` contains the literal substring `recurring.detect`.
    - `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noFacadeCallsFromRecurring|no Laravel facade usage|noTransactionWritesFromRecurring|noSynchronousDetectionInRequestLifecycle'` exits 0.
    - All seven `--filter` slices on `DetectRecurringSeriesJobTest` pass.
    - `composer analyse` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Five Public Actions + RecurringSeriesQuery + RecurringReviewPage Livewire SFC + route + Blade view + tests</name>
  <files>
    Modules/Recurring/Public/Actions/ApproveRecurringSeries.php,
    Modules/Recurring/Public/Actions/RejectRecurringSeries.php,
    Modules/Recurring/Public/Actions/SnoozeRecurringSeries.php,
    Modules/Recurring/Public/Actions/EditRecurringSeriesName.php,
    Modules/Recurring/Public/Actions/UnRejectRecurringSeries.php,
    Modules/Recurring/Public/Services/RecurringSeriesQuery.php,
    Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php,
    Modules/Recurring/Resources/views/livewire/recurring-review-page.blade.php,
    Modules/Recurring/Routes/web.php,
    Modules/Recurring/Providers/RecurringServiceProvider.php,
    Modules/Recurring/tests/Feature/ApproveRecurringSeriesTest.php,
    Modules/Recurring/tests/Feature/RejectRecurringSeriesTest.php,
    Modules/Recurring/tests/Feature/SnoozeRecurringSeriesTest.php,
    Modules/Recurring/tests/Feature/EditRecurringSeriesNameTest.php,
    Modules/Recurring/tests/Feature/UnRejectRecurringSeriesTest.php,
    Modules/Recurring/tests/Feature/RecurringReviewPageTest.php,
    Modules/Recurring/tests/Feature/CrossUserRecurringSeriesIsolationTest.php
  </files>
  <read_first>
    Modules/Chains/Public/Actions/ConfirmChainLink.php (entire 106-line file — `final class`, constructor DI, `__invoke(int, User): void`, cross-user 404 guard via `where('user_id', $user->id)->first()` and `throw new NotFoundHttpException`, idempotent no-op when already in target state, state-machine transition + event dispatch),
    Modules/Chains/Public/Actions/RejectChainLink.php (the reject analog — same shape, different state transition),
    Modules/Chains/Internal/Http/Livewire/ChainReviewQueue.php (entire file, lines 42–96 — `final class extends Component`, public state properties `$cursorId`/`$cursorConfidence` for pagination, action methods take collaborators as parameters, `render(CurrentUser, Query, ViewFactory): View` signature with `$views->make('chains::livewire.chain-review-queue', [...])` and `$view->extends('layouts.app', [...])`),
    Modules/Chains/Public/Services/ChainLinkQuery.php (lines 30–80 — `final readonly class` Public read service, every method takes `User $user` as first param + `where('user_id', $user->id)` scope, returns Spatie Data DTO instances; uses raw query builder via injected DatabaseManager for whereBetween/whereIn shapes),
    Modules/Chains/Routes/web.php (entire 23-line file — the loopback + auth route shape with `Route::middleware(['web', 'auth'])->group(...)` + facade-permitted Routes/* carve-out documented inline),
    Modules/Chains/Resources/views/livewire/chain-review-queue.blade.php (the Blade view structure — `wire:click` actions, sticky bulk-action bar with `wire:model='selectedIds'` checkboxes, toast dispatch via `x-on:toast.window`),
    Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php (Plan 02 — the transition() signature actions invoke),
    Modules/Recurring/Public/Dto/RecurringSeriesDto.php (Plan 02 — the DTO shape the query returns),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md "Public Action — ApproveRecurringSeries" + "Public Read Query" + "Livewire SFC RecurringReviewPage" sections,
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-806/D-807/D-810/D-812/D-813/D-814 (review queue UX) + D-823 (rejecting does not touch transaction.type)
  </read_first>
  <behavior>
    - All five Public Actions follow the same shape: constructor DI (`DatabaseManager $db`, `RecurringSeriesStateMachine $stateMachine`, `Clock $clock`, `Dispatcher $events`), `__invoke(int $seriesId, User $user, ...action-specific args): void`, cross-user 404 on missing-or-foreign-user row, idempotent no-op when already in target state, state-machine transition + event dispatch.
    - `ApproveRecurringSeries(int seriesId, User user)`: pending/cadence_changed/snoozed → approved; emits `RecurringSeriesApproved`.
    - `RejectRecurringSeries(int seriesId, User user)`: pending/approved/cadence_changed/snoozed → rejected; emits `RecurringSeriesRejected`.
    - `SnoozeRecurringSeries(int seriesId, User user, CarbonImmutable until)`: pending → snoozed; writes `snoozed_until = $until` on the series row via `DatabaseManager` (this is a metric-style write to `snoozed_until` PLUS a state transition through the state machine — handle the column write inside the state-machine transition by extending its signature or by writing snoozed_until in a SEPARATE call right before the transition; mirror however InboxScanStateMachine handles per-transition extra column writes); cross-user 404; idempotent (snoozing while snoozed and the target `$until` matches → no-op).
    - `EditRecurringSeriesName(int seriesId, User user, ?string displayNameOverride)`: writes `display_name_override` directly via DatabaseManager (NO state transition — per Pitfall 3, this is a metric-style write); passing null clears the override (fall back to detected_name). Cross-user 404.
    - `UnRejectRecurringSeries(int seriesId, User user)`: rejected → pending; emits no new event (Phase 9 listens to detected/approved/rejected/cadence_flipped, not un-reject specifically).
    - `RecurringSeriesQuery` exposes: `pendingForUser(User $user, ?int $cursorId = null, int $limit = 26): array<RecurringSeriesDto>`, `pendingCountForUser(User $user): int`, `rejectedForUser(User $user, ?int $cursorId = null, int $limit = 26): array<RecurringSeriesDto>`, `approvedForUser(User $user, ?int $cursorId = null, int $limit = 26): array<RecurringSeriesDto>`, `cadenceChangedForUser(User $user): array<RecurringSeriesDto>`, `forSeries(int $seriesId, User $user): ?RecurringSeriesDto`. Every method scopes by `user_id`; cross-user reads return empty list / null (cross-user 404s land in the Actions, not the queries — that mirrors the Chains precedent).
    - `RecurringReviewPage` is a `final class extends Livewire\Component`:
       - Public state properties: `?int $cursorId = null`, `array $selectedIds = []` (for bulk-action bar — D-812), `string $tab = 'pending'` (`'pending'` | `'rejected'` | `'cadence_changed'`).
       - Action methods (one per Public Action) take collaborators as parameters: `approve(int $seriesId, CurrentUser $currentUser, ApproveRecurringSeries $action)`, `reject(int $seriesId, CurrentUser $currentUser, RejectRecurringSeries $action)`, `snooze(int $seriesId, string $untilIso, CurrentUser $currentUser, SnoozeRecurringSeries $action)`, `editName(int $seriesId, string $newName, CurrentUser $currentUser, EditRecurringSeriesName $action)`, `unReject(int $seriesId, CurrentUser $currentUser, UnRejectRecurringSeries $action)`.
       - Each action dispatches a 10-second Undo toast (D-814) via `$this->dispatch('toast', message: ..., undoAction: ..., undoPayload: $seriesId)` matching the Phase 4/5 pattern.
       - `render(CurrentUser $currentUser, RecurringSeriesQuery $query, ViewFactory $views): View` returns the view extended with the layout.
       - Constructor DI is BANNED on Livewire Components (phpstan-strict-rules) — all DI lives on action methods and `render()`.
       - A "Re-detect now" button is OUT of scope for this plan (lands in Plan 05). The page shows pending suggestions only; bulk-action bar checkboxes wire to a future `bulkApprove`/`bulkReject` action that lands in Plan 05.
    - Route `/recurring/review` resolves the Livewire component behind `web` + `auth` middleware; cross-user reads return empty (the page can be opened by a foreign user with zero rows).
    - `vendor/bin/pest Modules/Recurring/tests/Feature/RecurringReviewPageTest.php --filter='suggest-not-applied'` asserts: a pending series exists; opening `/recurring` returns zero rows (Plan 04 ships that route but the assertion here is the "not on `/recurring` until approved" half — until Plan 04 lands the page, the test asserts via `RecurringSeriesQuery::approvedForUser` returning empty for a series that is still pending).
    - `CrossUserRecurringSeriesIsolationTest` covers all five Public Actions for the cross-user 404 case + the query layer for empty-cross-user-reads.
    - The `noSynchronousDetectionInRequestLifecycle` arch test from Plan 01 stays green: the new Livewire SFC imports `ApproveRecurringSeries` etc. but NOT any `SeriesDetector` implementation — verify by running the arch suite after this task.
  </behavior>
  <action>
    Create `Modules/Recurring/Public/Actions/ApproveRecurringSeries.php` per the analog `ConfirmChainLink`. Constructor DI:
    ```
    public function __construct(
        private readonly DatabaseManager $db,
        private readonly RecurringSeriesStateMachine $stateMachine,
        private readonly Clock $clock,
        private readonly Dispatcher $events,
    ) {}
    ```
    `__invoke(int $seriesId, User $user): void`:
    1. Load `RecurringSeries::query()->where('id', $seriesId)->where('user_id', $user->id)->first()`.
    2. If null → throw `NotFoundHttpException('Recurring series not found.')`.
    3. If `$series->state === 'approved'` → return (idempotent no-op).
    4. Call `$this->stateMachine->transition($series, 'approved', 'user_action', 'user')`.
    5. Dispatch `new RecurringSeriesApproved(seriesId: $series->id, userId: $user->id)` via `$this->events->dispatch(...)`.

    Create `Modules/Recurring/Public/Actions/RejectRecurringSeries.php` — same shape with `toState='rejected'` and event `RecurringSeriesRejected`. Idempotent no-op when already rejected.

    Create `Modules/Recurring/Public/Actions/SnoozeRecurringSeries.php`. Constructor DI same as above. `__invoke(int $seriesId, User $user, CarbonImmutable $until): void`:
    1. Cross-user 404 guard.
    2. If `$series->state === 'snoozed'` AND existing `$series->snoozed_until` equals `$until` → return.
    3. Inside `$this->db->connection()->transaction(...)`:
       a. Update `recurring_series.snoozed_until = $until` directly (metric-style write — no state-machine call for the column).
       b. Call `$this->stateMachine->transition($series, 'snoozed', 'user_action', 'user', notes: "snoozed_until={$until->toDateTimeString()}")` — the notes column carries the snooze target for audit.
    4. No event dispatched (no Phase 9/10 listener for snooze).

    Create `Modules/Recurring/Public/Actions/EditRecurringSeriesName.php`. Constructor DI (`DatabaseManager $db`, `Clock $clock` — no state machine, no events). `__invoke(int $seriesId, User $user, ?string $displayNameOverride): void`:
    1. Cross-user 404 guard.
    2. Update `recurring_series.display_name_override = $displayNameOverride`, `updated_at = $now`.
    3. NO state transition — this is a metric-style write (Pitfall 3 — the arch test only watches the `state` column).
    4. NO event.

    Create `Modules/Recurring/Public/Actions/UnRejectRecurringSeries.php`. Same shape as Approve, with `toState='pending'`, `reason='user_action'`, `actor='user'`. Idempotent no-op when not currently rejected (returns silently for any non-rejected state).

    Create `Modules/Recurring/Public/Services/RecurringSeriesQuery.php` per `ChainLinkQuery` + `MerchantMemoryQuery` precedent. `final readonly class` with constructor DI: `public function __construct(private DatabaseManager $db) {}`. Implement:
    - `pendingForUser(User $user, ?int $cursorId = null, int $limit = 26): array` — returns `array<RecurringSeriesDto>` for state IN ('pending', 'cadence_changed'), ordered by `id DESC`, cursor by `id < $cursorId`. The `+1` over-fetch pattern from ChainLinkQuery for `loadMore` cursor signalling.
    - `pendingCountForUser(User $user): int` — single COUNT, used by the top-nav badge composer (Plan 05).
    - `rejectedForUser(User $user, ?int $cursorId = null, int $limit = 26): array` — same shape, scoped to `state='rejected'`.
    - `approvedForUser(User $user, ?int $cursorId = null, int $limit = 26): array` — same shape, scoped to `state='approved'`, ordered by `monthly_equivalent_minor DESC` then `id DESC` (calmest read for the dashboard tile + the "approved-only" feature-test assertions in `suggest-not-applied`). Cursor pagination keyed on `id` matches the rest of the class.
    - `cadenceChangedForUser(User $user): array` — scoped to `state='cadence_changed'`.
    - `forSeries(int $seriesId, User $user): ?RecurringSeriesDto` — returns the single row or null (caller maps null → 404 if the route demands it).
    Every method scopes by `user_id`. Each row is mapped to `RecurringSeriesDto` via a private `toDto(stdClass $row): RecurringSeriesDto` helper that constructs the Money VO via `Money::ofMinor($row->latest_amount_minor, $row->latest_currency)`.

    Register the singletons in `Modules/Recurring/Providers/RecurringServiceProvider.php`'s `register()`:
    ```
    $this->app->singleton(RecurringSeriesQuery::class);
    $this->app->singleton(ApproveRecurringSeries::class);
    $this->app->singleton(RejectRecurringSeries::class);
    $this->app->singleton(SnoozeRecurringSeries::class);
    $this->app->singleton(EditRecurringSeriesName::class);
    $this->app->singleton(UnRejectRecurringSeries::class);
    ```
    Also register the Livewire component in `boot()` via `$livewire->component('recurring.recurring-review-page', RecurringReviewPage::class);`.

    Create `Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php` per `ChainReviewQueue` analog. `final class extends Component`. Public state: `?int $cursorId = null`, `array $selectedIds = []`, `string $tab = 'pending'`.

    Action methods (no constructor DI — method-parameter DI only):
    ```
    public function approve(int $seriesId, CurrentUser $currentUser, ApproveRecurringSeries $action): void {
        ($action)($seriesId, $currentUser->user());
        $this->dispatch('toast', message: 'Approved', undoAction: 'reject', undoPayload: $seriesId);
    }
    ```
    Mirror the same shape for `reject`, `snooze` (extra `string $untilIso` parameter parsed to `CarbonImmutable` inside the method), `editName` (extra `string $newName` parameter; null when cleared), `unReject`.

    A `setTab(string $tab): void` method updates `$this->tab` so the Blade view can toggle between Pending / Rejected / Cadence-changed lists.

    `render(CurrentUser $currentUser, RecurringSeriesQuery $query, ViewFactory $views): View`:
    1. Resolve `$user = $currentUser->user()`.
    2. Branch on `$this->tab`: pending → `$query->pendingForUser($user, $this->cursorId, 26)`; rejected → `$query->rejectedForUser(...)`; cadence_changed → `$query->cadenceChangedForUser($user)`.
    3. `$view = $views->make('recurring::livewire.recurring-review-page', ['rows' => $rows, 'tab' => $this->tab]);`
    4. `$view->extends('layouts.app', ['title' => 'Review recurring · diederik']);` (mirror ChainReviewQueue lines 90–91 with the `/** @phpstan-ignore-next-line method.notFound */` annotation).
    5. Return `$view`.

    Create `Modules/Recurring/Resources/views/livewire/recurring-review-page.blade.php`. A clean, calm Linear/Notion-aesthetic layout (per UI-05 — even though this plan doesn't ship the polish, the structure must read as calm not admin-panel). Sections:
    - Top: three tab buttons (Pending / Rejected / Cadence-changed) wired to `wire:click="setTab('pending')"` etc.
    - Body: a list of rows. Each row shows: detected_name, latest_amount (Money formatted), cadence string, next_expected_at relative-text, four action buttons (Approve / Reject / Snooze / Edit-name). Approve via `wire:click="approve({{ $row->seriesId }})"`. Reject identically. Snooze opens a Flux popover with three preset chips (1 week / 1 month / 3 months) + a custom date input — D-810; the popover dispatches `wire:click="snooze({{ $row->seriesId }}, '{{ $isoDate }}')"`. Edit-name opens a small inline input dispatching `wire:click="editName({{ $row->seriesId }}, '{{ $newName }}')"`.
    - The bulk-action bar at the bottom is a placeholder — checkbox-select wiring lands in Plan 05.
    - Alpine `x-on:toast.window` handler at the top of the layout (or in a shared partial) consumes the dispatch and shows a 10-second Undo toast — but the toast renderer probably already lives in `layouts/app.blade.php` from Phase 4/5; verify by reading that layout. If absent, add a minimal Alpine handler here.

    Create `Modules/Recurring/Routes/web.php` mirroring `Modules/Chains/Routes/web.php`:
    ```
    use Illuminate\Support\Facades\Route;
    use Modules\Recurring\Internal\Http\Livewire\RecurringReviewPage;
    Route::middleware(['web', 'auth'])->group(static function (): void {
        Route::get('/recurring/review', RecurringReviewPage::class)->name('recurring.review');
    });
    ```
    Note: `Route::` facade is the documented carve-out in the BoundaryArchTest for `Modules\*\Routes` — match the inline comment that `Modules/Chains/Routes/web.php` carries.

    Create five Feature tests under `Modules/Recurring/tests/Feature/`:
    - `ApproveRecurringSeriesTest.php` — happy path (pending → approved, transitions row inserted, event dispatched); idempotent no-op (calling twice does not insert a second transitions row).
    - `RejectRecurringSeriesTest.php` — happy path for each starting state (pending, approved, cadence_changed, snoozed); idempotent no-op.
    - `SnoozeRecurringSeriesTest.php` — happy path: snoozed_until is written + state flips to snoozed + transitions row carries the `until` in `notes`; idempotent when re-snoozing to the same date; persists across a `DetectRecurringSeriesJob` sweep if `snoozed_until` is still in the future.
    - `EditRecurringSeriesNameTest.php` — happy path: display_name_override is written; the next sweep refreshes detected_name but DOES NOT clobber display_name_override (test seeds a sweep then re-loads the row); passing null clears the override.
    - `UnRejectRecurringSeriesTest.php` — happy path: rejected → pending + transitions row inserted; idempotent when the series is not currently rejected.

    Create `Modules/Recurring/tests/Feature/RecurringReviewPageTest.php` using Livewire's testing harness (`Livewire::test(RecurringReviewPage::class)`):
    - `pending-tab-shows-pending-series`: seed a pending series; render the component; assert the row id appears.
    - `bulk-action-bar-renders` (placeholder — full wiring in Plan 05): assert the checkbox markup exists in the view.
    - `approve-action-flips-state`: call `$component->call('approve', $seriesId)`; assert state is approved + the toast was dispatched.
    - `tab-switches-to-rejected`: seed a rejected series; switch the tab via `setTab('rejected')`; assert the rejected row renders.
    - `suggest-not-applied`: per the VALIDATION.md mapping — seed a pending series and assert it DOES NOT appear in `RecurringSeriesQuery::approvedForUser($user)` (the read API used by `/recurring` and the dashboard card); approve the series and assert it now appears in `approvedForUser`. This locks the "not on `/recurring` until Approve" contract at the query layer.

    Create `Modules/Recurring/tests/Feature/CrossUserRecurringSeriesIsolationTest.php`:
    - For each of the five Public Actions: seed two users; create a series owned by user A; invoke the action with `$seriesId` belonging to A but `$user` set to B; assert `NotFoundHttpException` is thrown; assert the series is unchanged.
    - `RecurringSeriesQuery::forSeries` returns null for a cross-user lookup; `pendingForUser` returns the user's own pending series only (assert against a two-user seed); `approvedForUser` returns the user's own approved series only.

    Run `composer test` and `composer analyse` to confirm full-suite green. Verify the `noSynchronousDetectionInRequestLifecycle` arch test still passes (no `SeriesDetector` import from any file under `Modules/Recurring/Internal/Http`).
  </action>
  <verify>
    <automated>vendor/bin/pest --filter='Recurring' --parallel --stop-on-failure &amp;&amp; composer analyse &amp;&amp; composer format:check</automated>
  </verify>
  <done>
    - Five Public Action files exist under `Modules/Recurring/Public/Actions/`.
    - `Modules/Recurring/Public/Services/RecurringSeriesQuery.php` exists with `final readonly class` and exposes `pendingForUser`, `pendingCountForUser`, `rejectedForUser`, `approvedForUser`, `cadenceChangedForUser`, `forSeries` (verify with `grep -c 'public function approvedForUser' Modules/Recurring/Public/Services/RecurringSeriesQuery.php` returning 1).
    - `Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php` exists with `final class extends Component` and uses method-parameter DI (verify with `grep -c 'public function __construct' Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php` returning 0).
    - `Modules/Recurring/Routes/web.php` exists with the `/recurring/review` route.
    - `Modules/Recurring/Resources/views/livewire/recurring-review-page.blade.php` exists.
    - All five Public Action feature tests pass; the cross-user isolation test passes for all five actions + the query layer (including `approvedForUser` cross-user empty).
    - `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noSynchronousDetectionInRequestLifecycle'` exits 0.
    - `composer test` exits 0; `composer analyse` exits 0; `composer format:check` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → Livewire SFC | Authenticated user submits Approve/Reject/Snooze/EditName actions; the cross-user 404 guard on every Public Action is the trust boundary. |
| Queue worker → Detector | The job runs as a queued background worker; the snooze-expiry pass + detector iteration both run with full DB write access. |
| Detector → transactions table | Read-only; the `noTransactionWritesFromRecurring` arch test enforces this. |
| Public Actions → state machine | Only the state machine writes `recurring_series.state`; arch test from Plan 01 enforces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Elevation of privilege | Cross-user series mutation via crafted Livewire payload | mitigate | Every Public Action loads via `where('user_id', $user->id)->first()` and throws NotFoundHttpException on miss; tested explicitly in CrossUserRecurringSeriesIsolationTest for all five actions. |
| T-08-11 | Tampering | SQL injection on detector's raw query builder | mitigate | All detector queries use parameter binding via DatabaseManager's query builder (`where('user_id', $user->id)` is bound, not interpolated); no string-concatenation SQL anywhere. |
| T-08-12 | Repudiation | State transition without audit trail | mitigate | RecurringSeriesStateMachine is the only legal mutator and ALWAYS inserts a transitions row in the same DB transaction; the audit table is append-only with no UPDATE/DELETE path in module code. |
| T-08-13 | Denial of service | Spam-click on /recurring/re-detect (button lands in Plan 05; the on-demand dispatch in Plan 03's scheduled trigger has the same lock) | mitigate | `ShouldBeUniqueUntilProcessing` with `uniqueId() = userId` collapses concurrent dispatches to a single queued job; `withoutOverlapping(30)` on the scheduler prevents the schedule from racing with itself. |
| T-08-14 | Information disclosure | RecurringSeriesQuery returning cross-user data via a missing `where('user_id', ...)` clause | mitigate | Every method on the class is documented as `user_id`-scoped; every test in CrossUserRecurringSeriesIsolationTest asserts empty cross-user reads (including approvedForUser). |
| T-08-15 | Tampering | Detector writes to transactions table | mitigate | `noTransactionWritesFromRecurring` arch test from Plan 01 scans the entire module for `Transaction::query|Transaction::where|Transaction::create` and `->table('transactions')->update|insert|delete`. |
</threat_model>

<verification>
- `vendor/bin/pest --filter='Recurring' --parallel` exits 0 (full Recurring suite green: cadence inference + cluster key + detector + 5 Public Actions + 5 action tests + review page test + cross-user isolation test).
- `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --parallel` exits 0 (all five arch invariants pass, the `noFacadeCallsFromRecurring` carve-out for `DetectRecurringSeriesJob` is exercised, the `noSynchronousDetectionInRequestLifecycle` and `noTransactionWritesFromRecurring` invariants stay green).
- `vendor/bin/pest tests/Contracts/RecurringDetectionContractTest.php` exits 0 (the end-to-end sweep test asserts expected series counts for the expense-side Wave 0 fixtures; income-side fixtures stay skipped until Plan 04).
- `composer test && composer analyse && composer format:check` exits 0.
- `php artisan schedule:list` lists the new `recurring.detect` entry (manual verification not required for CI — covered by the routes/console.php grep).
- `grep -v '^[[:space:]]*//' routes/console.php | grep -c 'recurring.detect'` returns ≥ 1 (filters comment-only lines per CLAUDE.md grep-gate hygiene).
- `grep -c 'tag(\\[ExpenseSeriesDetector' Modules/Recurring/Providers/RecurringServiceProvider.php` returns ≥ 1 (container-tag binding is in place).
- `grep -c 'public function approvedForUser' Modules/Recurring/Public/Services/RecurringSeriesQuery.php` returns 1.
</verification>

<success_criteria>
- The expense detector + queued job + scheduled trigger work end-to-end against the Wave 0 fixture corpus: stable-monthly clusters as one, drifting-monthly clusters as one (REC-02), irregular gym does not cluster, variable-amount bills fragment, and re-running the job is idempotent (no duplicate series or occurrence rows).
- `/recurring/review` is a working Livewire SFC behind `web`+`auth` middleware with per-row Approve / Reject / Snooze / Edit-name actions plus a 10-second Undo toast.
- All five Public Actions enforce cross-user 404 and idempotent no-ops; the state machine is the only path that touches `state`; snooze writes are atomic with the state transition.
- `RecurringSeriesQuery::approvedForUser` exists, is user-scoped, and is the read API the dashboard tile / `/recurring` page consume in Plans 04/05.
- The `recurring.detect` daily scheduler entry exists in `routes/console.php` and matches the email-scan + receipts entry shape (method order `.name() BEFORE .daily()->withoutOverlapping(30)`).
- Snooze-expiry sweep runs as the first pass inside `DetectRecurringSeriesJob::handle()` so snoozed suggestions surface again automatically (Pitfall 8 mitigation).
- All Plan 01 + Plan 02 arch invariants stay green.
</success_criteria>

<output>
Create `.planning/phases/08-recurring-detection-fixed-payments-view/08-03-SUMMARY.md` when done. Record:
- The cadence dataset row count (target: 15–20 per CONTEXT D-846).
- The detector's run-time against the largest Plan 01 fixture (rough wall-clock under sqlite — for future Phase 9/10 scaling reference).
- The exact line in `routes/console.php` where the new schedule entry lands.
- Confirmation that the `noFacadeCallsFromRecurring` ignoring array now points at a real `DetectRecurringSeriesJob` class (verify with `grep -A 1 'DetectRecurringSeriesJob' tests/Contracts/BoundaryArchTest.php`).
- The five RecurringSeriesQuery method signatures actually shipped (pendingForUser, pendingCountForUser, rejectedForUser, approvedForUser, cadenceChangedForUser, forSeries).
- Any deviation from the locked detector algorithm (none expected — D-843/D-844 lock).
</output>
