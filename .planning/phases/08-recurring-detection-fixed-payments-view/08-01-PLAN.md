---
phase: 08-recurring-detection-fixed-payments-view
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Modules/Recurring/composer.json
  - Modules/Recurring/Providers/RecurringServiceProvider.php
  - Modules/Recurring/tests/Pest.php
  - Modules/Recurring/tests/TestCase.php
  - Modules/Recurring/tests/fixtures/synthesised/stable-monthly-spotify.php
  - Modules/Recurring/tests/fixtures/synthesised/drifting-monthly-spotify.php
  - Modules/Recurring/tests/fixtures/synthesised/quarterly-insurance.php
  - Modules/Recurring/tests/fixtures/synthesised/yearly-domain.php
  - Modules/Recurring/tests/fixtures/synthesised/weekly-streaming.php
  - Modules/Recurring/tests/fixtures/synthesised/monthly-salary.php
  - Modules/Recurring/tests/fixtures/synthesised/two-employer-salary.php
  - Modules/Recurring/tests/fixtures/synthesised/irregular-gym-must-not-cluster.php
  - Modules/Recurring/tests/fixtures/synthesised/missing-month-subscription.php
  - Modules/Recurring/tests/fixtures/synthesised/mixed-currency-netflix-usd.php
  - Modules/Recurring/tests/fixtures/synthesised/variable-amount-beyond-tolerance-bills.php
  - Modules/Recurring/tests/fixtures/real/anonymised-asn-ics-6mo.php
  - Modules/Categorization/Public/Services/MerchantMemoryQuery.php
  - Modules/Categorization/tests/Unit/MerchantMemoryQueryBatchTest.php
  - tests/Contracts/BoundaryArchTest.php
  - tests/Contracts/RecurringDetectionContractTest.php
  - tests/Pest.php
  - phpunit.xml
  - composer.json
  - bootstrap/providers.php
  - package.json
  - package-lock.json
  - resources/js/app.js
  - resources/views/components/apex-chart-smoke.blade.php
autonomous: true
requirements:
  - REC-01
  - REC-05
  - UI-03
user_setup: []

must_haves:
  truths:
    - "An empty `Modules/Recurring/` skeleton boots inside the Laravel app without breaking the existing suite — new bounded module mirroring Receipts/Chains/Transfers with Public/Internal split from day one (D-831)"
    - "Four new BoundaryArchTest invariants (no facade calls in Recurring with DetectRecurringSeriesJob carve-out, Internal containment, no transaction writes, no synchronous detection in request lifecycle) compile and run (D-833)"
    - "The Wave 0 fixture corpus (12 synthesised scenarios + 1 anonymised real export stub) exists on disk and is consumable by future contract tests — controlled-time-series corpus covering stable monthly, drifting monthly, quarterly, yearly, weekly, salary, two-employer salary, irregular gym, missing-month, mixed-currency, variable-amount, plus one anonymised real ASN+ICS export (D-845)"
    - "Categorization's MerchantMemoryQuery exposes a batch lookup method without breaking its existing single-row API — formalises the cross-module read boundary so Recurring decorates rows via Public service rather than direct Eloquent reads (D-834)"
    - "ApexCharts is installed via npm and globally exposed on window from resources/js/app.js so Wave 4 drill-in chart will resolve at runtime"
    - "Test split is in place: unit tests under `Modules/Recurring/tests/Unit/` for cadence math + contract test under `tests/Contracts/RecurringDetectionContractTest.php` for end-to-end sweep against the Wave 0 corpus (D-846)"
    - "composer test && composer analyse && composer format:check all stay green after this plan ships"
  artifacts:
    - path: Modules/Recurring/composer.json
      provides: Bounded module composer manifest (PSR-4 autoload root + tests autoload-dev)
      contains: "diederik/recurring"
    - path: Modules/Recurring/Providers/RecurringServiceProvider.php
      provides: Empty-but-valid ServiceProvider that loads migrations/routes/views directories conditionally
      exports: ["RecurringServiceProvider"]
    - path: Modules/Recurring/tests/Pest.php
      provides: Inert per-module Pest bootstrap
    - path: Modules/Recurring/tests/TestCase.php
      provides: Module-local TestCase that extends the root Tests\TestCase
    - path: tests/Contracts/BoundaryArchTest.php
      provides: Four new Recurring arch invariants + facade carve-out for DetectRecurringSeriesJob
      contains: "noTransactionWritesFromRecurring"
    - path: tests/Contracts/RecurringDetectionContractTest.php
      provides: End-to-end contract scaffold (skipped placeholder for Wave 2/3 population)
    - path: Modules/Categorization/Public/Services/MerchantMemoryQuery.php
      provides: Batch lookup method forCounterpartiesNormalized
      contains: "forCounterpartiesNormalized"
    - path: package.json
      provides: apexcharts devDependency entry
      contains: "apexcharts"
    - path: resources/js/app.js
      provides: ApexCharts global on window for Livewire/Alpine consumers
      contains: "ApexCharts"
  key_links:
    - from: bootstrap/providers.php
      to: Modules\Recurring\Providers\RecurringServiceProvider
      via: framework provider registration
      pattern: "RecurringServiceProvider::class"
    - from: composer.json
      to: Modules/Recurring/tests/
      via: PSR-4 autoload-dev entry
      pattern: "Modules\\\\\\\\Recurring\\\\\\\\Tests\\\\\\\\"
    - from: tests/Pest.php
      to: Modules\Recurring\Tests\TestCase
      via: per-module TestCase map
      pattern: "'Modules/Recurring'"
    - from: phpunit.xml
      to: Modules/Recurring/tests/Unit + Modules/Recurring/tests/Feature
      via: Unit + Feature testsuite directory entries
      pattern: "Modules/Recurring/tests"
    - from: tests/Contracts/BoundaryArchTest.php
      to: Modules\Recurring\Internal\Jobs\DetectRecurringSeriesJob
      via: facade-carve-out ignoring entry (forward-declared FQN — the job lands in Plan 03)
      pattern: "Modules\\\\\\\\Recurring\\\\\\\\Internal\\\\\\\\Jobs\\\\\\\\DetectRecurringSeriesJob"
    - from: resources/js/app.js
      to: window.ApexCharts
      via: ES module import + global assignment
      pattern: "window.ApexCharts"
---

<objective>
Stand up the empty `Modules/Recurring/` bounded module, register its provider, wire its test suite into the project, extend `MerchantMemoryQuery` with a batch lookup, drop the synthesised fixture corpus on disk, install ApexCharts in the JS bundle, and add the four new BoundaryArchTest invariants. Nothing in this plan ships a behavioural feature — it makes Waves 1–4 runnable without scaffold friction.

Purpose: Eliminate scaffold cost from later waves; close the ApexCharts gap that Phase 5 silently dropped; lock the four boundary invariants before any code can violate them; pre-populate fixtures so detector tests in Waves 2/3 can be `pest --filter` smoke tests, not setup epics.

Output: Empty-but-green Recurring module, ApexCharts on the JS bundle, MerchantMemoryQuery batch capability, four arch tests, fixture corpus, contract test scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md
@.planning/phases/08-recurring-detection-fixed-payments-view/08-VALIDATION.md
@CLAUDE.md
@Modules/Chains/composer.json
@Modules/Chains/Providers/ChainsServiceProvider.php
@Modules/Receipts/tests/Pest.php
@Modules/Receipts/tests/TestCase.php
@Modules/Categorization/Public/Services/MerchantMemoryQuery.php
@Modules/Categorization/Public/Dto/MerchantMemoryDto.php
@tests/Contracts/BoundaryArchTest.php
@tests/Pest.php
@bootstrap/providers.php
@composer.json
@phpunit.xml
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Modules/Recurring/ skeleton + register provider + wire test suite</name>
  <files>
    Modules/Recurring/composer.json,
    Modules/Recurring/Providers/RecurringServiceProvider.php,
    Modules/Recurring/tests/Pest.php,
    Modules/Recurring/tests/TestCase.php,
    Modules/Recurring/Resources/.gitkeep,
    Modules/Recurring/Database/Migrations/.gitkeep,
    Modules/Recurring/Routes/.gitkeep,
    bootstrap/providers.php,
    composer.json,
    phpunit.xml,
    tests/Pest.php
  </files>
  <read_first>
    Modules/Chains/composer.json (verbatim shape for Modules/Recurring/composer.json — swap chains→recurring),
    Modules/Chains/Providers/ChainsServiceProvider.php (provider shape, lines 67–120 are the boot()/register() skeleton — strip Chains-specific singletons and listeners since this plan ships an empty provider),
    Modules/Receipts/tests/Pest.php and Modules/Receipts/tests/TestCase.php (inert per-module bootstrap — copy verbatim and swap the namespace),
    bootstrap/providers.php (insertion order — RecurringServiceProvider::class goes at the end of the array next to ReceiptsServiceProvider::class),
    composer.json lines 50 + 58–66 (Modules autoload root + per-module Tests autoload-dev block — append a Recurring entry),
    phpunit.xml lines 9–37 (Unit + Feature testsuite directory entries — append Modules/Recurring/tests/Unit and Modules/Recurring/tests/Feature; do NOT create a separate Recurring testsuite block in this plan — the Unit/Feature shared suites pick up via PSR-4 autoload),
    tests/Pest.php lines 21–56 (the per-module TestCase map — add `'Modules/Recurring' => Modules\Recurring\Tests\TestCase::class`),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md sections "Module skeleton + wiring (Wave 0)" and "Pattern S-7" (3-step test-suite registration)
  </read_first>
  <behavior>
    - composer test continues to pass with zero added tests (skeleton must not break the suite)
    - composer analyse continues to pass at level max with the new module on PSR-4 autoload
    - composer dump-autoload after this task resolves Modules\Recurring\Providers\RecurringServiceProvider from the bootstrap providers list without errors
    - The new module's Tests namespace resolves: `composer dump-autoload && php -r "require 'vendor/autoload.php'; new ReflectionClass(\"Modules\\\\Recurring\\\\Tests\\\\TestCase\");"` exits 0 (the abstract class can be reflected even though it cannot be instantiated)
  </behavior>
  <action>
    Create `Modules/Recurring/composer.json` mirroring `Modules/Chains/composer.json` verbatim with the name `diederik/recurring`, description `"Recurring module — series detection, fixed-payments view, drill-in chart."`, type `laravel-module`, license `proprietary`, autoload PSR-4 `"Modules\\Recurring\\": ""`, autoload-dev PSR-4 `"Modules\\Recurring\\Tests\\": "tests/"`.

    Do NOT create a `module.json` file in this module (per D-831 the convention from Phase 4+ — verified against Chains/Receipts which both lack module.json; the file is only present on Phase 1–3 modules; PATTERNS.md "Note" block under composer.json pattern documents this).

    Create `Modules/Recurring/Providers/RecurringServiceProvider.php` as a `final class RecurringServiceProvider extends ServiceProvider` with empty `register()` and a minimal `boot()` that conditionally loads migrations (`__DIR__.'/../Database/Migrations'`), routes (`__DIR__.'/../Routes/web.php'`), and views (`__DIR__.'/../Resources/views'` with namespace `'recurring'`) using the same `is_dir`/`is_file` guards from `ChainsServiceProvider::boot()` lines 95–104. No Livewire components registered yet (those land in Waves 2–4). No View composers yet (those land in Wave 4). No singletons yet (those land in Waves 1–2). Class docblock describes what the provider currently does — never reference plan/wave history per CLAUDE.md docs-describe-current-state rule.

    Create empty `Resources/`, `Database/Migrations/`, and `Routes/` placeholder directories (use a `.gitkeep` file in each so git tracks them — the empty-directory guards in the provider are belt-and-suspenders).

    Create `Modules/Recurring/tests/TestCase.php` as `namespace Modules\Recurring\Tests; abstract class TestCase extends \Tests\TestCase {}` exactly mirroring `Modules/Receipts/tests/TestCase.php`. Class docblock describes its current role only.

    Create `Modules/Recurring/tests/Pest.php` as the inert variant matching `Modules/Receipts/tests/Pest.php` (extends TestCase, uses RefreshDatabase on Feature, plain TestCase on Unit). The root `tests/Pest.php` is the load-bearing wirer; this file stays inert per project convention.

    Modify `bootstrap/providers.php`: add `use Modules\Recurring\Providers\RecurringServiceProvider;` to the use block (alphabetical insertion between Receipts and Transfers) and append `RecurringServiceProvider::class,` to the return array as the tenth entry. Match the existing alphabetical ordering pattern visible in the file.

    Modify root `composer.json` autoload-dev block: append `"Modules\\Recurring\\Tests\\": "Modules/Recurring/tests/"` as the tenth Tests entry after the Transfers one. Preserve trailing comma rules.

    Modify root `phpunit.xml`: add `<directory>Modules/Recurring/tests/Unit</directory>` to the `Unit` testsuite and `<directory>Modules/Recurring/tests/Feature</directory>` to the `Feature` testsuite, in alphabetical order alongside the existing module entries.

    Modify root `tests/Pest.php` foreach map (lines 22–32): insert `'Modules/Recurring' => Modules\Recurring\Tests\TestCase::class,` in alphabetical position between Ledger and Receipts.

    After all edits, run `composer dump-autoload` to refresh the classmap. Run `composer test` to confirm the existing suite still passes.
  </action>
  <verify>
    <automated>composer dump-autoload --no-interaction &amp;&amp; composer test -- --testsuite=Unit,Feature,Contracts --stop-on-failure</automated>
  </verify>
  <done>
    - File `Modules/Recurring/composer.json` exists with the literal string `"diederik/recurring"`.
    - File `Modules/Recurring/Providers/RecurringServiceProvider.php` exists and `php -r "require 'vendor/autoload.php'; new ReflectionClass(\"Modules\\\\Recurring\\\\Providers\\\\RecurringServiceProvider\");"` exits 0.
    - File `Modules/Recurring/tests/TestCase.php` and `Modules/Recurring/tests/Pest.php` exist.
    - `bootstrap/providers.php` contains the literal substring `RecurringServiceProvider::class`.
    - Root `composer.json` autoload-dev contains the literal substring `"Modules\\Recurring\\Tests\\"`.
    - Root `phpunit.xml` contains `Modules/Recurring/tests/Unit` and `Modules/Recurring/tests/Feature` substrings.
    - Root `tests/Pest.php` contains the literal substring `'Modules/Recurring' => Modules\Recurring\Tests\TestCase::class`.
    - `composer test` exits 0.
    - `composer analyse` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend MerchantMemoryQuery with batch lookup + add four BoundaryArchTest invariants + scaffold contract test</name>
  <files>
    Modules/Categorization/Public/Services/MerchantMemoryQuery.php,
    Modules/Categorization/tests/Unit/MerchantMemoryQueryBatchTest.php,
    tests/Contracts/BoundaryArchTest.php,
    tests/Contracts/RecurringDetectionContractTest.php
  </files>
  <read_first>
    Modules/Categorization/Public/Services/MerchantMemoryQuery.php (entire 78-line file — the existing `latestForCounterpartyNormalized` signature MUST stay byte-for-byte unchanged per assumption A4),
    Modules/Categorization/Public/Dto/MerchantMemoryDto.php (Spatie Data shape — the new batch method returns `array&lt;string, MerchantMemoryDto&gt;` keyed by `counterpartyNormalized`),
    tests/Contracts/BoundaryArchTest.php lines 25–95 (existing `arch('Modules\\X\\Internal is only used inside Modules\\X')` rules + the facade carve-out ignoring array — extend in place),
    tests/Contracts/BoundaryArchTest.php lines 237–286 (the `noTransactionWritesFromEmailScan` invariant — copy this regex/directory-scan shape verbatim and retarget at `Modules/Recurring`),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-PATTERNS.md sections "Arch test: noTransactionWritesFromRecurring", "Arch test: noSynchronousDetectionInRequestLifecycle", "Facade carve-out", and "Pattern S-7"
  </read_first>
  <behavior>
    - `MerchantMemoryQuery::latestForCounterpartyNormalized(User, string)` continues to behave exactly as today (regression test under Categorization stays green).
    - `MerchantMemoryQuery::forCounterpartiesNormalized(User, list&lt;string&gt;): array&lt;string, MerchantMemoryDto&gt;` returns a map keyed by `counterpartyNormalized`; missing keys are simply absent (not nulled — caller uses `??` to default).
    - The batch method scopes by `user_id` for every input — passing in 50 names runs ONE SQL query (whereIn), not 50.
    - The new arch invariants are valid Pest tests: the BoundaryArchTest file's existing tests still pass; the four new tests can run even before any Recurring code exists (they trivially pass when `Modules/Recurring/` has no module-code files yet, matching the `is_dir` early-return pattern used by `noTransactionWritesFromEmailScan` lines 240–245).
    - `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` exits 0.
    - `vendor/bin/pest tests/Contracts/RecurringDetectionContractTest.php` exits 0 (the scaffold test marks itself skipped with `Pest\\test()->skip('Populated in Wave 2/3 — see plan 03/04')` so it is discoverable but inert).
  </behavior>
  <action>
    Modify `Modules/Categorization/Public/Services/MerchantMemoryQuery.php`: add a public method `forCounterpartiesNormalized(User $user, array $counterpartyNormalizedList): array` that takes `list&lt;string&gt;` of normalized counterparty names and returns `array&lt;string, MerchantMemoryDto&gt;` keyed by the normalized name. Implementation uses a single `whereIn('m.normalized_name', $names)` query joined against `merchants` + `merchant_memories` (same JOIN shape as the existing single-row method, lines 41–50). Coerce each row to a MerchantMemoryDto via the same `self::toInt` helper. Skip empty strings on input. Add a docblock declaring the return shape `array&lt;string, MerchantMemoryDto&gt;` so Larastan understands it. Keep `final readonly class` modifier. Do NOT touch the existing `latestForCounterpartyNormalized` method body (per A4: backwards-compatible addition only).

    Create `Modules/Categorization/tests/Unit/MerchantMemoryQueryBatchTest.php` that covers four cases: empty input list returns `[]`; cross-user isolation (user A's memory is invisible to user B); two-name lookup returns two keyed entries; missing-name returns absent key (not null). Use `RefreshDatabase` via the module TestCase. Run only against in-memory SQLite per project test convention.

    Modify `tests/Contracts/BoundaryArchTest.php` with FIVE additions:

    1. After the existing `arch('Modules\\Receipts\\Internal is only used inside Modules\\Receipts')` block (lines 40–42), add:
       `arch('Modules\\Recurring\\Internal is only used inside Modules\\Recurring')->expect('Modules\\Recurring\\Internal')->toOnlyBeUsedIn('Modules\\Recurring');`

    2. In the `ignoring([...])` array of `arch('no Laravel facade usage in module code')` (lines 47–75), append after the existing `Modules\\Receipts\\Internal\\Jobs\\ScanInboxDropFolderJob` entry:
       `'Modules\\Recurring\\Internal\\Jobs\\DetectRecurringSeriesJob',`
       Include an inline comment in the same style as the existing carve-outs (lines 55–75) explaining the uniqueVia() rationale, forward-declaring that `DetectRecurringSeriesJob` lands in Plan 03.

    3. Add an `arch()` rule `arch('SeriesDetector implementors are never imported by Modules\\Recurring\\Internal\\Http (noSynchronousDetectionInRequestLifecycle)')` that uses `->expect('Modules\\Recurring\\Public\\Contracts\\SeriesDetector')->not->toBeUsedIn(['Modules\\Recurring\\Internal\\Http', 'Modules\\Recurring\\Resources']);`. The contract class lands in Plan 03 — until then Pest's `arch()` rule should resolve to a no-op silently (Pest skips missing-expectation classes). If the rule errors when the expectation class does not exist, wrap in an `if (interface_exists('Modules\\Recurring\\Public\\Contracts\\SeriesDetector')) { ... }` guard; otherwise prefer the cleaner unguarded form. Test both shapes locally and keep whichever lets `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` exit 0.

    4. Add an `it()` test block `it('does not allow any file under Modules/Recurring/ to mutate the transactions table (noTransactionWritesFromRecurring)')` that copies the `noTransactionWritesFromEmailScan` body (lines 237–286) verbatim, substituting `Modules/Recurring` for `Modules/EmailScan` and updating the message string accordingly. The early-return `is_dir(... )` guard plus the `if (str_contains($path, '/tests/'))` filter mean this test passes trivially while the module is empty AND when later waves populate the module without writing to transactions.

    5. Add an `it()` test block `it('does not allow any file other than RecurringSeriesStateMachine to mutate recurring_series.state (noOtherRecurringSeriesStateMutator)')` that copies `noOtherInboxScanStateMutator` (lines 288–343) verbatim, substituting:
       - `Modules/EmailScan` → `Modules/Recurring`
       - `inbox_scan_state` → `recurring_series` (the column-targeted UPDATE regex must read `update OF state` or the equivalent table-targeted shape per Pattern S-4 — the test only watches the `state` column so non-state UPDATEs to `recurring_series` (metric refresh) and INSERTs to `recurring_series_occurrences` stay legal, mirroring Pitfall 3)
       - The allowlisted-mutator path is `Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php` (file lands in Plan 02; the early-return `is_dir` guard keeps the test inert until then).

    Create `tests/Contracts/RecurringDetectionContractTest.php` as a Pest test file containing one inert test: `it('runs the recurring detection sweep end-to-end against the Wave 0 fixture corpus')->skip('Populated in Wave 2/3 — fixtures land in Plan 01; detectors land in Plans 03/04.');`. File header docblock describes the test's role — the load-bearing end-to-end sweep over the Wave 0 corpus that locks the full detector spec (D-846) once Plans 03/04 populate `assertSeriesSet()` / `assertCadences()` / `assertMetrics()` helpers.

    Run `vendor/bin/pest tests/Contracts/BoundaryArchTest.php tests/Contracts/RecurringDetectionContractTest.php` and confirm both exit 0.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/BoundaryArchTest.php tests/Contracts/RecurringDetectionContractTest.php Modules/Categorization/tests/Unit/MerchantMemoryQueryBatchTest.php --stop-on-failure</automated>
  </verify>
  <done>
    - `Modules/Categorization/Public/Services/MerchantMemoryQuery.php` contains the literal substring `forCounterpartiesNormalized` and the existing `latestForCounterpartyNormalized` signature is unchanged (verified by `grep -c 'public function latestForCounterpartyNormalized'` returning 1).
    - `Modules/Categorization/tests/Unit/MerchantMemoryQueryBatchTest.php` exists and contains assertions for empty-input, cross-user-isolation, two-name lookup, and missing-name cases.
    - `tests/Contracts/BoundaryArchTest.php` contains substrings `Modules\\Recurring\\Internal`, `Modules\\Recurring\\Internal\\Jobs\\DetectRecurringSeriesJob`, `noSynchronousDetectionInRequestLifecycle`, `noTransactionWritesFromRecurring`, and `noOtherRecurringSeriesStateMutator`.
    - `tests/Contracts/RecurringDetectionContractTest.php` exists.
    - All three test files run green: `vendor/bin/pest tests/Contracts/BoundaryArchTest.php tests/Contracts/RecurringDetectionContractTest.php Modules/Categorization/tests/Unit/MerchantMemoryQueryBatchTest.php` exits 0.
    - `composer analyse` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install ApexCharts + populate Wave 0 fixture corpus</name>
  <files>
    package.json,
    package-lock.json,
    resources/js/app.js,
    resources/views/components/apex-chart-smoke.blade.php,
    Modules/Recurring/tests/fixtures/synthesised/stable-monthly-spotify.php,
    Modules/Recurring/tests/fixtures/synthesised/drifting-monthly-spotify.php,
    Modules/Recurring/tests/fixtures/synthesised/quarterly-insurance.php,
    Modules/Recurring/tests/fixtures/synthesised/yearly-domain.php,
    Modules/Recurring/tests/fixtures/synthesised/weekly-streaming.php,
    Modules/Recurring/tests/fixtures/synthesised/monthly-salary.php,
    Modules/Recurring/tests/fixtures/synthesised/two-employer-salary.php,
    Modules/Recurring/tests/fixtures/synthesised/irregular-gym-must-not-cluster.php,
    Modules/Recurring/tests/fixtures/synthesised/missing-month-subscription.php,
    Modules/Recurring/tests/fixtures/synthesised/mixed-currency-netflix-usd.php,
    Modules/Recurring/tests/fixtures/synthesised/variable-amount-beyond-tolerance-bills.php,
    Modules/Recurring/tests/fixtures/real/anonymised-asn-ics-6mo.php,
    Modules/Recurring/tests/Unit/Wave0FixtureCorpusTest.php
  </files>
  <read_first>
    package.json (current devDependencies — apexcharts is NOT present; this task adds it as a devDependency since the Vite bundle is dev-time tooling per the existing `@tailwindcss/vite` entry),
    resources/js/app.js (if absent — this task creates it; if present, append imports without disturbing existing initialisation),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-RESEARCH.md Pattern 7 lines 701–732 (the Blade JSON injection + Alpine init template — used in Plan 05 Wave 4),
    Modules/Chains/tests/fixtures/ (PATTERNS.md mentions Chains fixtures as the analog for the synthesised corpus — read one example to mirror the `list&lt;CanonicalTransaction-shaped array&gt;` return shape),
    .planning/phases/08-recurring-detection-fixed-payments-view/08-CONTEXT.md D-845 (the exact fixture list locked: stable monthly, drifting monthly, quarterly insurance, yearly domain, weekly streaming, monthly salary, two-employer salary, irregular gym, missing-month subscription, mixed-currency Netflix, variable-amount bills, plus one anonymised real export)
  </read_first>
  <behavior>
    - `package.json` declares `apexcharts` under `devDependencies` pinned to a real version returned by the npm registry (use `^3` as the minimum range — the current major as of research date is 3.x).
    - `package-lock.json` regenerated to lock the resolved versions.
    - `resources/js/app.js` imports `ApexCharts` from the `apexcharts` package and assigns it to `window.ApexCharts` so Alpine `x-init` handlers in Plan 05's drill-in view can resolve the global.
    - `npm run build` (Vite production build) succeeds and emits a bundle that includes the ApexCharts module.
    - Each synthesised fixture file is a PHP script returning `list&lt;array&lt;string, mixed&gt;&gt;` — each row shaped as a CanonicalTransaction-compatible array with at minimum: `posted_at` (Y-m-d), `booked_at` (Y-m-d), `amount_minor` (signed BIGINT — negative for expense, positive for income), `currency` (ISO 4217), `original_amount_minor` (when distinct), `original_currency`, `counterparty_normalized`, `counterparty_iban` (nullable), `type` (`expense` | `income` | `fee` | `refund` | `transfer_out` | `transfer_in`), `account_id` placeholder (the contract test resolves this at consumption time). Each fixture also returns a sibling `expected` block (top-level array key) documenting what the detector should produce when fed this row set — series count, cadence, direction, monthly equivalent ballpark, fragmentation expectations — so Plan 04's contract test can assert against the corpus deterministically.
    - The anonymised real export stub `anonymised-asn-ics-6mo.php` returns an empty `list` plus a `TODO_REAL_FIXTURE` marker comment; the real anonymisation work is a checkpoint deferred to Phase 8 close-out (a smaller, separate task can mine the user's actual exports). The presence of the file unblocks Plans 03/04 contract-test scaffolding without forcing real-data anonymisation into Wave 0's critical path.
    - `Modules/Recurring/tests/Unit/Wave0FixtureCorpusTest.php` walks `Modules/Recurring/tests/fixtures/synthesised/*.php`, loads each, asserts the returned array has the expected shape (top-level keys `transactions` + `expected`), and that no transaction row is missing required keys.
  </behavior>
  <action>
    Install ApexCharts via npm with explicit version pinning. Run `npm install apexcharts@^3 --save-dev` from the project root. Confirm `package.json` devDependencies now contains `"apexcharts": "^3.x.x"` (whatever the latest 3.x is at install time) and `package-lock.json` reflects the resolved transitive tree. Run `npm view apexcharts version` and record it in the executor SUMMARY to satisfy the slopcheck `[ASSUMED]` audit from RESEARCH.md.

    Create or edit `resources/js/app.js`. If the file does not exist, create it with:
    ```
    import ApexCharts from 'apexcharts';
    window.ApexCharts = ApexCharts;
    ```
    If the file already exists (verify with `ls`), append the two lines preserving existing imports. The pattern: import as a named-default and stamp on `window` so Alpine `x-init` handlers in Plan 05 can call `new window.ApexCharts(...)` without further wiring.

    Confirm Vite picks up the new import by running `npm run build`. The build must exit 0 and the emitted `public/build/manifest.json` must reference an `app.js` (or hashed equivalent) chunk.

    Create a tiny smoke Blade component `resources/views/components/apex-chart-smoke.blade.php` containing a single `&lt;div x-data x-init="if (! window.ApexCharts) { throw new Error('ApexCharts not loaded'); }"&gt;&lt;/div&gt;` placeholder. This is a runtime canary — Plan 05 imports the real chart component; this stub exists only so a future feature test can assert ApexCharts is available without spinning up the full drill-in page.

    Drop the twelve synthesised fixture files into `Modules/Recurring/tests/fixtures/synthesised/`. Each file is shaped as:
    ```
    &lt;?php
    declare(strict_types=1);
    return [
        'transactions' =&gt; [ /* list of CanonicalTransaction-shaped arrays */ ],
        'expected' =&gt; [ /* asserter spec — series_count, cadences[], directions[], etc. */ ],
    ];
    ```
    Use the D-845 list as the exact 11 named fixtures (plus the irregular-gym-must-not-cluster file makes 12 with monthly-salary and two-employer-salary being separate entries). For each fixture choose plausible dates spanning the 18-month detection-window default (D-802) so the median-interval-snap algorithm sees enough samples. Currency is `EUR` for most; `USD` for the mixed-currency Netflix fixture. Amounts are signed BIGINT minor units (FND-04): expense rows negative, income rows positive. The `expected` block documents what `ExpenseSeriesDetector` and `IncomeSeriesDetector` (Plans 03/04) MUST produce so the contract test in `tests/Contracts/RecurringDetectionContractTest.php` is unambiguous.

    Specific scenario shapes (executor implements concrete amounts and dates per CONTEXT D-845):
    - `stable-monthly-spotify.php`: 18 rows of −€9.99 EUR on the same day-of-month each month from the same `counterparty_normalized='spotify'`. Expected: 1 expense series, monthly cadence.
    - `drifting-monthly-spotify.php`: 12 rows of −€9.99 followed by 6 rows of −€11.49, same counterparty. Expected: 1 expense series (variance tolerance 25% covers €1.50 drift), monthly cadence.
    - `quarterly-insurance.php`: 6 quarterly −€89.99 rows. Expected: 1 expense series, quarterly cadence.
    - `yearly-domain.php`: 2 rows of −€12.00 exactly one year apart. Expected: 1 expense series, yearly cadence (D-803 min-2-occurrences).
    - `weekly-streaming.php`: 12 weekly −€2.49 rows. Expected: 1 expense series, weekly cadence.
    - `monthly-salary.php`: 12 rows of +€3500 from the same `counterparty_iban='NL00ACME0000000001'`, `type='income'`. Expected: 1 income series, monthly cadence.
    - `two-employer-salary.php`: 6 rows from IBAN_A + 6 rows from IBAN_B both at +€2200, `type='income'`. Expected: TWO income series (D-821).
    - `irregular-gym-must-not-cluster.php`: 5 rows with random −€10..−€60 amounts at 5/40/70/120/200 day intervals, same counterparty. Expected: 0 series (irregular cadence → skip per D-843).
    - `missing-month-subscription.php`: 12 monthly rows with one gap (e.g. months 5–6 missing, then resumes). Expected: 1 expense series (D-844 tolerance covers the gap).
    - `mixed-currency-netflix-usd.php`: 12 monthly rows at −$11.99 USD with settled-EUR varying ±3% due to FX. Expected: 1 expense series clustered on USD (D-839).
    - `variable-amount-beyond-tolerance-bills.php`: 6 monthly rows where amounts swing −€40 to −€140 (well beyond ±25%). Expected: fragmentation — multiple short series, not one (D-825 enforcement).
    - `monthly-salary.php` separate row above counts; `two-employer-salary.php` is its own file → 12 fixture files total counting the two salary files separately. Recount: stable-monthly, drifting-monthly, quarterly, yearly, weekly, monthly-salary, two-employer-salary, irregular-gym, missing-month, mixed-currency, variable-amount = 11. Add the real-export stub = 12 total files in `tests/fixtures/`. CONTEXT D-845 says 12 fixtures = 11 synthesised + 1 real. Match this count exactly.

    Create `Modules/Recurring/tests/fixtures/real/anonymised-asn-ics-6mo.php`. Body: `&lt;?php declare(strict_types=1); /* Placeholder for the anonymised real ASN+ICS 6-month export. See Phase 8 close-out task. */ return ['transactions' =&gt; [], 'expected' =&gt; ['series_count' =&gt; 0, 'TODO_REAL_FIXTURE' =&gt; true]];`. This unblocks the contract test scaffold without committing real bank data.

    Create `Modules/Recurring/tests/Unit/Wave0FixtureCorpusTest.php`: a Pest test that walks `glob(__DIR__.'/../fixtures/synthesised/*.php')`, requires each, and asserts: the return is an array with keys `transactions` and `expected`; `transactions` is an array; if non-empty, each row has the required CanonicalTransaction keys (`posted_at`, `booked_at`, `amount_minor`, `currency`, `counterparty_normalized`, `type`). Adds a separate assertion that exactly 11 synthesised files exist (catches future drift if a fixture is accidentally dropped).

    Verify everything: `npm run build` exits 0, `vendor/bin/pest Modules/Recurring/tests/Unit/Wave0FixtureCorpusTest.php` exits 0, and `composer test` still exits 0.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; vendor/bin/pest Modules/Recurring/tests/Unit/Wave0FixtureCorpusTest.php --stop-on-failure &amp;&amp; grep -c 'apexcharts' package.json | grep -qv '^0$' &amp;&amp; grep -c 'window.ApexCharts' resources/js/app.js | grep -qv '^0$'</automated>
  </verify>
  <done>
    - `package.json` devDependencies block contains `"apexcharts"` (verify with `grep -v '^#' package.json | grep -c apexcharts` returning ≥ 1).
    - `package-lock.json` resolved tree contains `node_modules/apexcharts` (verify with `grep -c '"node_modules/apexcharts"' package-lock.json` returning ≥ 1).
    - `resources/js/app.js` contains the literal substring `window.ApexCharts`.
    - `npm run build` exits 0 and `public/build/manifest.json` exists.
    - Eleven synthesised fixture files exist under `Modules/Recurring/tests/fixtures/synthesised/` (verify with `ls Modules/Recurring/tests/fixtures/synthesised/*.php | wc -l` returning 11).
    - `Modules/Recurring/tests/fixtures/real/anonymised-asn-ics-6mo.php` exists.
    - `vendor/bin/pest Modules/Recurring/tests/Unit/Wave0FixtureCorpusTest.php` exits 0.
    - `composer test` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | apexcharts package is fetched from npm; supply-chain risk for the JS bundle. |
| Existing test suite → new module skeleton | Adding a provider registration changes the boot sequence; risk of breaking unrelated tests. |
| Categorization Public API → Recurring (Plan 02 onward) | The new MerchantMemoryQuery batch method becomes part of the Public surface — backwards-compatibility risk for other Phase 7 callers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | npm install of apexcharts (supply-chain) | mitigate | Pin to `^3` and record `npm view apexcharts version` in SUMMARY; record `package-lock.json` resolved integrity hash; the package has 700K weekly downloads + 8+ year registry age per RESEARCH.md Package Legitimacy Audit. |
| T-08-02 | Tampering | bootstrap/providers.php (new provider in boot chain) | mitigate | Verify `composer test` stays green after registration; the empty provider has zero side-effects beyond load-path declarations. |
| T-08-03 | Information disclosure | MerchantMemoryQuery::forCounterpartiesNormalized — cross-user read | mitigate | Every batch lookup scopes by `user_id` on both the `merchants` JOIN and the `merchant_memories` WHERE clause (mirror lines 41–50 of the existing single-row method); test in MerchantMemoryQueryBatchTest covers the cross-user case. |
| T-08-04 | Spoofing | Fixture files under tests/ (theoretically executable PHP) | accept | Fixtures return literal arrays and never execute side-effect code; the Wave0FixtureCorpusTest asserts the return shape. |
| T-08-SC | Tampering | npm/composer install paths in CI | mitigate | This plan installs ONE new npm package (apexcharts, `[ASSUMED]` per audit). RESEARCH §"Package Legitimacy Audit" requires the SUMMARY to record `npm view apexcharts version` to confirm registry identity. No composer-package installs. |
</threat_model>

<verification>
- `composer test` exits 0 (full suite green after skeleton + arch tests + fixtures land).
- `composer analyse` exits 0 at level max with strict + larastan-strict + larastan-livewire.
- `composer format:check` exits 0 (Pint default Laravel preset).
- `npm run build` exits 0 and emits a bundle that references ApexCharts.
- `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` exits 0 (existing tests stay green + four new invariants compile and run).
- `vendor/bin/pest Modules/Recurring/tests/Unit/Wave0FixtureCorpusTest.php` exits 0 (eleven synthesised + one real-stub fixtures are present and structurally valid).
- `vendor/bin/pest Modules/Categorization/tests/Unit/MerchantMemoryQueryBatchTest.php` exits 0.
- `grep -c 'RecurringServiceProvider::class' bootstrap/providers.php` returns 1.
- `grep -c 'Modules\\\\Recurring\\\\Tests\\\\' composer.json` returns 1.
- `grep -c 'apexcharts' package.json` returns ≥ 1.
</verification>

<success_criteria>
- An empty `Modules/Recurring/` module is registered in `bootstrap/providers.php` and `composer test` still exits 0.
- Four new BoundaryArchTest invariants (Recurring/Internal containment, facade-carve-out for `DetectRecurringSeriesJob`, `noTransactionWritesFromRecurring`, `noSynchronousDetectionInRequestLifecycle`) plus the `noOtherRecurringSeriesStateMutator` watcher exist and run green.
- `Modules\Categorization\Public\Services\MerchantMemoryQuery::forCounterpartiesNormalized` exists, is unit-tested for cross-user isolation, and the existing `latestForCounterpartyNormalized` signature is byte-for-byte unchanged.
- Eleven synthesised fixture files plus one real-export stub exist under `Modules/Recurring/tests/fixtures/` and the corpus test asserts their structure.
- `apexcharts` is in `package.json` devDependencies, imported in `resources/js/app.js`, exposed on `window.ApexCharts`, and `npm run build` produces a Vite bundle that includes it.
- `tests/Contracts/RecurringDetectionContractTest.php` exists as a discoverable-but-skipped scaffold ready for Plan 03/04 to populate.
</success_criteria>

<output>
Create `.planning/phases/08-recurring-detection-fixed-payments-view/08-01-SUMMARY.md` when done. Record:
- The resolved `apexcharts` version from `npm view apexcharts version` (slopcheck `[ASSUMED]` resolution).
- The exact line range where the new arch invariants land in `tests/Contracts/BoundaryArchTest.php`.
- Confirmation that `composer test && composer analyse && composer format:check && npm run build` are all green.
- Any deviations from the planned 11 fixture files (none expected — D-845 locks the list).
</output>
</output>
