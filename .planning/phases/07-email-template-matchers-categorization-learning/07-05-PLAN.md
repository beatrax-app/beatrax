---
phase: 07
plan: 05
type: execute
wave: 4
depends_on: [07-02, 07-03, 07-04]
files_modified:
  - Modules/Categorization/Public/Actions/CreateCategorizationRule.php
  - Modules/Categorization/Public/Actions/UpdateCategorizationRule.php
  - Modules/Categorization/Public/Actions/DeleteCategorizationRule.php
  - Modules/Categorization/Public/Actions/AssignCategory.php
  - Modules/Categorization/Public/Events/CategorizationDiverged.php
  - Modules/Categorization/Internal/Http/Livewire/RulesPage.php
  - Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php
  - Modules/Categorization/Internal/Http/Livewire/CategorizationProvenancePanel.php
  - Modules/Categorization/Internal/Http/Livewire/CorrectionDivergenceToast.php
  - Modules/Categorization/Resources/views/rules.blade.php
  - Modules/Categorization/Resources/views/livewire/rules-page.blade.php
  - Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php
  - Modules/Categorization/Resources/views/livewire/categorization-provenance-panel.blade.php
  - Modules/Categorization/Resources/views/livewire/correction-divergence-toast.blade.php
  - Modules/Categorization/Routes/web.php
  - Modules/Categorization/Providers/CategorizationServiceProvider.php
  - Modules/Core/Resources/views/livewire/top-nav.blade.php
  - Modules/Core/Internal/Http/Livewire/SettingsPage.php
  - Modules/Core/Resources/views/livewire/settings-page.blade.php
  - Modules/Ledger/Resources/views/livewire/transaction-detail.blade.php
  - Modules/Receipts/Internal/Jobs/ScanInboxDropFolderJob.php
  - Modules/Receipts/Providers/ReceiptsServiceProvider.php
  - Modules/Receipts/Routes/console.php
  - routes/console.php
  - resources/views/layouts/app.blade.php
  - tests/Contracts/BoundaryArchTest.php
  - Modules/Categorization/tests/Feature/RulesPageTest.php
  - Modules/Categorization/tests/Feature/RuleFormModalTest.php
  - Modules/Categorization/tests/Feature/CorrectionDivergenceTest.php
  - Modules/Categorization/tests/Feature/CategorizationProvenancePanelTest.php
  - Modules/Receipts/tests/Feature/ScanInboxDropFolderJobTest.php
autonomous: true
requirements: [CAT-04, EML-07]
must_haves:
  truths:
    - "User can visit /rules and see the list of their categorization rules (per UI-SPEC § /rules page); 'New rule' button opens the form modal"
    - "User creates a rule via the form modal with field-selector + match-type + value + category picker; new rule is enforced via D-709 shape and DB UNIQUE constraint"
    - "User edits a rule via the per-row Edit chip; modal re-hydrates from the existing rule"
    - "User deletes a rule via the two-step inline Delete chip pair (Delete? -> Yes, delete / Cancel)"
    - "Reclassifying a transaction whose initial suggestion came from a rule (auto_category_provenance.source='rule') fires the correction-divergence toast offering Update rule / Keep current rule (D-712)"
    - "Transaction detail drawer renders the CategorizationProvenancePanel: 'Rule that fired' card for rule provenance, lighter 'Auto-categorized from merchant history [Override]' card for memory provenance, NOTHING for manual"
    - "Top-nav includes the 'Rules' item between 'Uncategorized' and 'Review chains' (per UI-SPEC § Navigation Decision)"
    - "/settings page surfaces the 'Auto-import from drop folder' toggle row with UI-SPEC locked copy"
    - "ScanInboxDropFolderJob runs every 5 minutes (D-718) when users.auto_import_drop_folder=true; moves processed files to /processed/{YYYY-MM}/"
    - "Layout @auth block mounts the 3 global Livewire SFCs (RuleFormModal + CorrectionDivergenceToast + ReceiptConflictToast)"
    - "Cross-user 404 invariant: Update/Delete/Edit on another user's rule returns 404"
    - "Watched-folder secondary path off by default behind /settings toggle, /processed/{YYYY-MM}/ move semantics (D-704 secondary path)"
    - "/rules top-nav page with rule CRUD per UI-SPEC (D-713)"
  artifacts:
    - path: "Modules/Categorization/Public/Actions/CreateCategorizationRule.php"
      provides: "__invoke(User, field, match, value, categoryId): int (rule id)"
    - path: "Modules/Categorization/Public/Actions/UpdateCategorizationRule.php"
      provides: "__invoke(User, ruleId, updates): int (cross-user 404 via firstOrFail-or-NotFoundHttpException)"
    - path: "Modules/Categorization/Public/Actions/DeleteCategorizationRule.php"
      provides: "__invoke(User, ruleId): void"
    - path: "Modules/Categorization/Internal/Http/Livewire/RulesPage.php"
      provides: "/rules route Livewire SFC + Blade page wrapper"
    - path: "Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php"
      provides: "Create/Edit form modal (Flux modal posture; mounted globally in layout)"
    - path: "Modules/Categorization/Internal/Http/Livewire/CategorizationProvenancePanel.php"
      provides: "Inline provenance panel embedded in /transactions/{id} drawer"
    - path: "Modules/Categorization/Internal/Http/Livewire/CorrectionDivergenceToast.php"
      provides: "Listens for CategorizationDiverged; renders Filament-notifications-style toast"
    - path: "Modules/Receipts/Internal/Jobs/ScanInboxDropFolderJob.php"
      provides: "Per-user watched-folder scanner; 5-min cadence; idempotent /processed/{YYYY-MM}/ moves"
  key_links:
    - from: "Modules/Categorization/Public/Actions/AssignCategory.php"
      to: "Modules/Categorization/Public/Events/CategorizationDiverged.php"
      via: "Dispatcher.dispatch when prior provenance source === 'rule' AND new categoryId differs from prior"
      pattern: "CategorizationDiverged"
    - from: "Modules/Core/Resources/views/livewire/top-nav.blade.php"
      to: "route('rules')"
      via: "anchor inserted between Uncategorized and Review chains"
      pattern: "rules"
    - from: "resources/views/layouts/app.blade.php"
      to: "categorization.rule-form-modal, categorization.correction-divergence-toast, receipts.receipt-conflict-toast"
      via: "@auth block @livewire mounts"
      pattern: "rule-form-modal"
    - from: "routes/console.php"
      to: "Modules/Receipts/Internal/Jobs/ScanInboxDropFolderJob.php"
      via: "Schedule::call hourly per-user dispatch (5-min cadence)"
      pattern: "receipts.scan-drop-folder"
threat_model:
  - T-07-04
  - T-07-06
  - T-07-09
  - T-07-12
---

<objective>
Ship the user-facing categorization-learning surface end-to-end: /rules CRUD page + form modal, correction-divergence toast + inline drawer panel, top-nav extension, /settings watched-folder toggle, ScanInboxDropFolderJob with 5-min schedule and /processed/{YYYY-MM}/ idempotency, and global Livewire SFC mounts (3 globally-mounted SFCs in app.blade.php @auth block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-CONTEXT.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-RESEARCH.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-UI-SPEC.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-VALIDATION.md
@CLAUDE.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-01-PLAN.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-02-PLAN.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-03-PLAN.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-04-PLAN.md
@Modules/Categorization/Models/CategorizationRule.php
@Modules/Categorization/Public/Services/CategorizationRuleQuery.php
@Modules/Categorization/Public/Dto/CategorizationRuleDto.php
@Modules/Categorization/Public/Events/TransactionCategorized.php
@Modules/Categorization/Public/Actions/AssignCategory.php
@Modules/Categorization/Public/Contracts/AssignsCategory.php
@Modules/Categorization/Public/Services/CategoryOptionsQuery.php
@Modules/Categorization/Internal/Http/Livewire/InlineCategoryPicker.php
@Modules/Categorization/Internal/Http/Livewire/TriageInbox.php
@Modules/Categorization/Routes/web.php
@Modules/Categorization/Providers/CategorizationServiceProvider.php
@Modules/Categorization/Resources/views/triage.blade.php
@Modules/Chains/Public/Actions/ConfirmChainLink.php
@Modules/Chains/Internal/Http/Livewire/ChainReviewQueue.php
@Modules/Chains/Resources/views/livewire/chain-review-queue.blade.php
@Modules/EmailScan/Internal/Http/Livewire/BackfillWindowModal.php
@Modules/Core/Internal/Http/Livewire/SettingsPage.php
@Modules/Core/Resources/views/livewire/settings-page.blade.php
@Modules/Core/Resources/views/livewire/top-nav.blade.php
@Modules/Ledger/Resources/views/livewire/transaction-detail.blade.php
@Modules/Ledger/Internal/Http/Livewire/TransactionDetail.php
@Modules/Receipts/Internal/Http/Livewire/ReceiptConflictToast.php
@Modules/Receipts/Providers/ReceiptsServiceProvider.php
@Modules/EmailScan/Internal/Jobs/IncrementalScanJob.php
@Modules/Receipts/Public/Actions/RecordReceipt.php
@Modules/Receipts/Internal/Pipeline/MboxIterator.php
@Modules/Receipts/Internal/Pipeline/EmlMimeReader.php
@Modules/Receipts/Internal/Pipeline/FileDropEmlBlobStore.php
@resources/views/layouts/app.blade.php
@routes/console.php
@tests/Contracts/BoundaryArchTest.php

<interfaces>
CategorizationDiverged event (NEW, Public):
  public readonly int transactionId;
  public readonly int ruleId;
  public readonly int oldCategoryId;
  public readonly int newCategoryId;
  public readonly int userId;
  Dispatched by AssignCategory when the prior auto_category_provenance.source === 'rule' AND $newCategoryId !== $prior['category_id'].

CreateCategorizationRule.__invoke(User $user, string $field, string $match, string $value, int $categoryId): int
  - Whitelist $field ∈ {'merchant','description','counterparty'} + $match ∈ {'contains','equals','starts_with'}; throw InvalidArgumentException otherwise (DB triggers from plan 01 are the safety net but PHP-side rejection gives clearer error messages).
  - INSERT via raw query builder, scoped by user_id; catch QueryException on UNIQUE violation and translate to ValidationException with UI-SPEC locked copy "A rule with this field, match, and value already exists. Edit the existing rule instead."

UpdateCategorizationRule.__invoke(User $user, int $ruleId, array $updates): int
  - $row = CategorizationRule::query()->where('id', $ruleId)->where('user_id', $user->id)->first(); $row === null -> throw new NotFoundHttpException('Rule not found.') (cross-user 404 — Phase 5 pattern).
  - UPDATE inside DB transaction; same UNIQUE-violation translation.

DeleteCategorizationRule.__invoke(User $user, int $ruleId): void
  - Same cross-user firstOrFail-or-NotFoundHttpException; DELETE.

RulesPage Livewire SFC: route GET /rules -> RulesPage::class. Top-nav order per UI-SPEC § Navigation Decision: Dashboard, Transactions, Imports, Inboxes [N], Uncategorized [N], Rules, Review chains [N], Settings.

RuleFormModal: dispatched via 'rule-form:open' Livewire event with optional ruleId param. Globally mounted in app.blade.php @auth block per UI-SPEC § Mounting Strategy.

CategorizationProvenancePanel: mounted on /transactions/{id} drawer with `transactionId` prop. Renders three variants per UI-SPEC: rule (always-visible card with Update/Remove), memory (lighter "Auto-categorized from merchant history [Override]"), manual (nothing).

CorrectionDivergenceToast: listens for the Livewire-local event 'correction-divergence:fire' (NOT a broadcaster echo, NOT a session flash — locked B5 from checker feedback). TransactionDetail::reclassify() dispatches the local event AFTER AssignCategory returns, carrying every CategorizationDiverged field explicitly in the Livewire-local payload (transactionId, ruleId, oldCategoryId, newCategoryId, userId). The toast receives the payload in the same request lifecycle. 8s auto-dismiss per UI-SPEC.

ScanInboxDropFolderJob:
  - constructor: public readonly int $userId.
  - ShouldBeUniqueUntilProcessing + ShouldQueue; uniqueId = (string)$userId; uniqueFor=600.
  - uniqueVia returns Cache::driver('redis') — needs the BoundaryArchTest carve-out (add Modules\\Receipts\\Internal\\Jobs\\ScanInboxDropFolderJob to the existing list — that list already contains ProcessFetchedInboxMessagesJob from plan 01).
  - handle(): scans storage/app/inbox-drop/{userId}/ for .eml + .mbox files; for each, invokes RecordReceipt (file-drop path identical to wizard upload — same RecordReceipt entrypoint); on success moves the file to storage/app/inbox-drop/{userId}/processed/{YYYY-MM}/original-filename; on failure moves to storage/app/inbox-drop/{userId}/failed/{YYYY-MM}/ with sibling .error.txt.
  - Schedule entry in routes/console.php: Schedule::call iterates User::where('auto_import_drop_folder', true) ids and dispatches per-user; .name('receipts.scan-drop-folder')->everyFiveMinutes()->withoutOverlapping(10).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> /rules action endpoints | CSRF-protected via Laravel default; Loopback + Fortify auth gate inherited |
| ScanInboxDropFolderJob -> filesystem | inbox-drop/{userId}/ is per-user scoped; the rename to processed/ is atomic via PHP rename() |
| CorrectionDivergenceToast | Surfaces rule + category names rendered through Blade {{ }} auto-escape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | Stored XSS in rule.value rendered to /rules page or transaction-detail drawer | mitigate | Every rule.value render uses Blade {{ }} which auto-escapes via htmlspecialchars(ENT_QUOTES). Never use {!! !!}. Pest test: create a rule with value '<script>alert(1)</script>' and assert the rendered HTML body contains '&lt;script&gt;' verbatim. |
| T-07-04 | Spoofing | ScanInboxDropFolderJob writing into a foreign user's folder via path traversal | mitigate | Job constructor takes int $userId; path computation uses storage_path('app/inbox-drop/'.$userId.'/...') — the user id is an integer cast, never a user-supplied string. The inner foreach loop only scans the {userId} subfolder; never walks above it. |
| T-07-09 | Information Disclosure | Cross-user data leak via rule CRUD | mitigate | Every action's read step uses ->where('user_id', $user->id); foreign-user lookups throw NotFoundHttpException (404, not 403, so the existence of foreign-user rules is not leaked). Pest cross-user 404 tests on Create/Update/Delete + on the /rules route (a foreign user's ruleId in the URL returns 404). |
| T-07-12 | Tampering | CSRF on rule mutations | mitigate | Laravel CSRF middleware default-enabled; Livewire's wire:click + wire:submit ride CSRF tokens through livewireScripts. No bare POST endpoints. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rule CRUD actions + /rules route + RulesPage SFC + RuleFormModal SFC + top-nav extension</name>
  <files>
    Modules/Categorization/Public/Actions/CreateCategorizationRule.php,
    Modules/Categorization/Public/Actions/UpdateCategorizationRule.php,
    Modules/Categorization/Public/Actions/DeleteCategorizationRule.php,
    Modules/Categorization/Internal/Http/Livewire/RulesPage.php,
    Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php,
    Modules/Categorization/Resources/views/rules.blade.php,
    Modules/Categorization/Resources/views/livewire/rules-page.blade.php,
    Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php,
    Modules/Categorization/Routes/web.php,
    Modules/Categorization/Providers/CategorizationServiceProvider.php,
    Modules/Core/Resources/views/livewire/top-nav.blade.php,
    Modules/Categorization/tests/Feature/RulesPageTest.php,
    Modules/Categorization/tests/Feature/RuleFormModalTest.php
  </files>
  <read_first>
    Modules/Categorization/Models/CategorizationRule.php (plan 04),
    Modules/Categorization/Public/Services/CategorizationRuleQuery.php,
    Modules/Categorization/Public/Services/CategoryOptionsQuery.php (for the category picker),
    Modules/Categorization/Internal/Http/Livewire/InlineCategoryPicker.php (services on action method pattern — Shared Pattern 2),
    Modules/Categorization/Resources/views/triage.blade.php (page-wrapper Blade pattern),
    Modules/Categorization/Routes/web.php (existing Route::view pattern),
    Modules/Chains/Public/Actions/ConfirmChainLink.php (cross-user 404 + DB transaction pattern),
    Modules/Chains/Internal/Http/Livewire/ChainReviewQueue.php (Livewire SFC page-mount pattern, including ->extends('layouts.app') trick),
    Modules/Chains/Resources/views/livewire/chain-review-queue.blade.php (table-with-actions Blade structure),
    Modules/EmailScan/Internal/Http/Livewire/BackfillWindowModal.php (#[On] event listener; modal Blade),
    Modules/Core/Resources/views/livewire/top-nav.blade.php (existing nav anchor shape — Uncategorized anchor lines 52-63),
    Modules/Core/Public/Contracts/CurrentUser.php,
    .planning/phases/07-email-template-matchers-categorization-learning/07-UI-SPEC.md (every Copywriting Contract row for /rules + rule form modal; § Spacing, Typography, Color, Interaction contracts; § Navigation Decision; § Component Inventory + States Inventory + Accessibility Notes for /rules and Rule form modal),
    .planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md (Wave 4 Rule CRUD action excerpts lines 763-801; RulesPage Livewire excerpt lines 805-838; RuleFormModal excerpt lines 842-877; top-nav modification lines 916-924)
  </read_first>
  <behavior>
    - Test (Create): CreateCategorizationRule with valid (field='merchant', match='contains', value='SPOTIFY', categoryId=42) INSERTs a row scoped to the user, returns the new id, $hits_count=0, $active=true.
    - Test (Create — invalid field): throws InvalidArgumentException for field='bogus'.
    - Test (Create — duplicate): second call with the same (field, match, value) on the same user throws ValidationException with the UI-SPEC locked copy "A rule with this field, match, and value already exists. Edit the existing rule instead."
    - Test (Update): UpdateCategorizationRule(ruleId of another user) throws NotFoundHttpException (cross-user 404).
    - Test (Update — happy): updates the rule and returns affected row count.
    - Test (Delete): cross-user 404 + happy-path delete.
    - Test (RulesPage mount): renders the empty-state hero with locked copy "No rules yet" + "Create your first rule" when no rules exist.
    - Test (RulesPage mount): renders the rules table with field / match / value / category / hits / created columns when ≥1 rule exists.
    - Test (RulesPage interaction): click "New rule" dispatches 'rule-form:open' Livewire event; click per-row Edit dispatches the same event with ruleId.
    - Test (RulesPage delete): click Delete sets $confirmingDeleteId; assert two-step chip rendering; click "Yes, delete" invokes DeleteCategorizationRule and removes the row from $rules.
    - Test (RuleFormModal): #[On('rule-form:open')] listener hydrates fields from the rule when ruleId is provided; opens fresh when ruleId is null.
    - Test (RuleFormModal validation): submitting with empty value renders the locked copy "Value is required." under the field; submitting without a category renders "Pick a category to assign."
    - Test (RuleFormModal save): on successful create, dispatches 'rule-form:saved'; RulesPage re-renders the row list.
    - Test (top-nav): rendered HTML contains an anchor for "Rules" between "Uncategorized" and "Review chains" — assert via cssSelector / HTML order.
    - Test (XSS T-07-06): rule with value '<script>alert(1)</script>' renders as escaped text in both RulesPage and rule-form-modal Blade output.
  </behavior>
  <action>
Create the three actions per PATTERNS.md Wave 4 excerpt lines 763-801:

CreateCategorizationRule.php (final class):
  - constructor: private readonly DatabaseManager $db + private readonly Clock $clock.
  - __invoke(User $user, string $field, string $match, string $value, int $categoryId): int
    - Validate $field ∈ {'merchant','description','counterparty'} or throw InvalidArgumentException.
    - Validate $match ∈ {'contains','equals','starts_with'} or throw InvalidArgumentException.
    - Validate strlen($value) > 0 — empty is rejected at action layer (Livewire validation also catches it).
    - $now = $this->clock->now()->toDateTimeString();
    - try { return (int) $this->db->connection()->table('categorization_rules')->insertGetId([...]); }
    - catch (QueryException $e) — translate UNIQUE violation (SQLite error code) to ValidationException with the locked copy.

UpdateCategorizationRule.php:
  - constructor: same as Create + Dispatcher (no — only Create needs Dispatcher if it fires an event; v1 does not).
  - __invoke(User $user, int $ruleId, array $updates): int
    - $row = $this->db->connection()->table('categorization_rules')->where('id', $ruleId)->where('user_id', $user->id)->first().
    - if $row === null throw new NotFoundHttpException('Rule not found.').
    - Apply updates inside a DB transaction; UNIQUE violation translation.

DeleteCategorizationRule.php:
  - constructor: DatabaseManager only.
  - __invoke(User $user, int $ruleId): void
    - $row = ...; if null throw NotFoundHttpException.
    - DELETE inside transaction.

Create Modules/Categorization/Internal/Http/Livewire/RulesPage.php per PATTERNS.md excerpt lines 805-838:
  - public array $rules = [].
  - public ?int $confirmingDeleteId = null.
  - mount(CategorizationRuleQuery $query, CurrentUser $user): hydrate $rules.
  - openCreateModal(): $this->dispatch('rule-form:open');
  - openEditModal(int $ruleId): $this->dispatch('rule-form:open', ruleId: $ruleId);
  - confirmDelete(int $ruleId): $this->confirmingDeleteId = $ruleId;
  - cancelDelete(): $this->confirmingDeleteId = null;
  - deleteRule(int $ruleId, CurrentUser $currentUser, DeleteCategorizationRule $delete): ($delete)($currentUser->user(), $ruleId); $this->loadRules(...); $this->dispatch('rule-saved', message: 'Rule deleted.');
  - #[On('rule-form:saved')] handleSaved(): re-query.
  - render(CurrentUser $currentUser, CategorizationRuleQuery $query, ViewFactory $views): $view = $views->make('categorization::livewire.rules-page', ['rules' => $query->forUser($currentUser->user())]); $view->extends('layouts.app', ['title' => 'Rules · diederik']); return $view;

Create Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php per PATTERNS.md excerpt lines 842-877:
  - public ?int $editingRuleId = null; public string $field = ''; public string $match = ''; public string $value = ''; public ?int $categoryId = null; public string $errorMessage = '';
  - #[On('rule-form:open')] open(?int $ruleId = null, CategorizationRuleQuery $query, CurrentUser $currentUser): hydrate if $ruleId; else reset.
  - save(CurrentUser $currentUser, CreateCategorizationRule $create, UpdateCategorizationRule $update): validate + dispatch to correct action + fire 'rule-form:saved' + close modal via Livewire dispatch 'modal-hide' with payload {name: 'rule-form'}.
  - render(CurrentUser $currentUser, CategoryOptionsQuery $options, ViewFactory $views): return $views->make('categorization::livewire.rule-form-modal', ['categories' => $options->for($currentUser->user())]).

Create Modules/Categorization/Resources/views/rules.blade.php (3-line page wrapper per Categorization triage.blade.php precedent):
  @extends('layouts.app')
  @section('content')
      @livewire(\\Modules\\Categorization\\Internal\\Http\\Livewire\\RulesPage::class)
  @endsection

Create Modules/Categorization/Resources/views/livewire/rules-page.blade.php — implements UI-SPEC § /rules page verbatim:
  - max-w-5xl px-4 py-12 page chrome.
  - h1 "Rules" (text-2xl semibold tracking-tight).
  - subheading "Pre-categorize transactions on import. Rules apply to every source — bank, card, PayPal, and email receipts."
  - primary CTA "New rule" (emerald-600) -> wire:click="openCreateModal".
  - If empty: hero card with locked "No rules yet" + body + "Create your first rule" CTA.
  - If non-empty: <table> with locked column headers (Field / Match / Value / Category / Hits / Created / (actions)). Each row min-h-12. Tabular-nums on Hits and Created. font-mono on Value cell. Hits=0 dim (text-slate-400). Edit/Delete chips. Two-step inline Delete confirmation when $confirmingDeleteId matches.
  - Helper note at bottom (UI-SPEC locked copy).

Create Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php — wraps in <flux:modal name="rule-form" dismissible> per UI-SPEC § Component Inventory. Inside: <flux:heading size="lg"> with "New rule" or "Edit rule". Sentence-layout form: "When the [field-select] [match-select] the value [input]" followed by "Assign category [picker]" + actions. wire:model.live on field/match/categoryId; wire:model.lazy on value (matches UI-SPEC § Interaction Contracts step 2). Save button disabled when any required field empty; @error renders rose-text below each field. Use UI-SPEC locked copy verbatim (help text "Matching is case-insensitive. Use the exact text you see in the transaction.", placeholder "e.g. SPOTIFY", etc.). Embed InlineCategoryPicker as a Livewire sub-component for the category select OR re-use the picker logic inline — choose inline (a <flux:select> driven from $categories list) to keep the modal self-contained.

Modify Modules/Categorization/Routes/web.php — add Route::view('/rules', 'categorization::rules')->middleware(['web','auth'])->name('rules'); (mirror the existing /uncategorized route shape).

Modify Modules/Categorization/Providers/CategorizationServiceProvider.php:
  - register(): singleton-bind CreateCategorizationRule + UpdateCategorizationRule + DeleteCategorizationRule.
  - boot(): $livewire->component('categorization.rules-page', RulesPage::class) + $livewire->component('categorization.rule-form-modal', RuleFormModal::class).

Modify Modules/Core/Resources/views/livewire/top-nav.blade.php — insert the "Rules" anchor between "Uncategorized" and "Review chains" per UI-SPEC § Navigation Decision verbatim shape. No badge. Active state per existing pattern.

Create Modules/Categorization/tests/Feature/RulesPageTest.php + RuleFormModalTest.php covering the behavior list. Use Livewire::test(...) helpers + RefreshDatabase + the existing seedFixtureUserAndAccount.
  </action>
  <acceptance_criteria>
    - grep -q "final class CreateCategorizationRule" Modules/Categorization/Public/Actions/CreateCategorizationRule.php
    - grep -q "NotFoundHttpException" Modules/Categorization/Public/Actions/UpdateCategorizationRule.php (cross-user 404)
    - grep -q "NotFoundHttpException" Modules/Categorization/Public/Actions/DeleteCategorizationRule.php
    - grep -q "Route::view('/rules'" Modules/Categorization/Routes/web.php OR Route::view\\(\\s*'/rules' match
    - grep -q "rule-form:open" Modules/Categorization/Internal/Http/Livewire/RulesPage.php
    - grep -q "#\\[On.*rule-form:open" Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php (attribute-style listener)
    - grep -q "No rules yet" Modules/Categorization/Resources/views/livewire/rules-page.blade.php (UI-SPEC empty state heading)
    - grep -q "Pre-categorize transactions on import" Modules/Categorization/Resources/views/livewire/rules-page.blade.php (UI-SPEC subheading)
    - grep -q "Matching is case-insensitive" Modules/Categorization/Resources/views/livewire/rule-form-modal.blade.php (UI-SPEC help text verbatim)
    - grep -q "A rule with this field, match, and value already exists" Modules/Categorization/Public/Actions/CreateCategorizationRule.php (UI-SPEC duplicate error copy)
    - grep -q "Rules" Modules/Core/Resources/views/livewire/top-nav.blade.php
    - ./vendor/bin/pest --filter='RulesPageTest|RuleFormModalTest|CreateCategorizationRule|UpdateCategorizationRule|DeleteCategorizationRule' --parallel exits 0
    - ./vendor/bin/phpstan analyse Modules/Categorization Modules/Core --memory-limit=2G --no-progress exits 0
    - ./vendor/bin/pint --test Modules/Categorization Modules/Core exits 0
  </acceptance_criteria>
  <validation_axis>Axis 5 — rule CRUD precedence + UNIQUE constraint at DB level. Axis 8 — boundary invariants (cross-user 404 on Update/Delete; CSRF inherited).</validation_axis>
  <verify>
    <automated>./vendor/bin/pest --filter='RulesPageTest|RuleFormModalTest|CreateCategorizationRule|UpdateCategorizationRule|DeleteCategorizationRule' --parallel &amp;&amp; ./vendor/bin/phpstan analyse Modules/Categorization Modules/Core --memory-limit=2G --no-progress</automated>
  </verify>
  <done>3 CRUD actions + /rules route + RulesPage + RuleFormModal + top-nav extension shipped; UI-SPEC copy locked verbatim; cross-user 404 + XSS-escape Pest tests green; phpstan + pint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CategorizationDiverged event + AssignCategory divergence dispatch + CorrectionDivergenceToast + CategorizationProvenancePanel + transaction-detail integration + global SFC mounts</name>
  <files>
    Modules/Categorization/Public/Events/CategorizationDiverged.php,
    Modules/Categorization/Public/Actions/AssignCategory.php,
    Modules/Categorization/Internal/Http/Livewire/CategorizationProvenancePanel.php,
    Modules/Categorization/Internal/Http/Livewire/CorrectionDivergenceToast.php,
    Modules/Categorization/Resources/views/livewire/categorization-provenance-panel.blade.php,
    Modules/Categorization/Resources/views/livewire/correction-divergence-toast.blade.php,
    Modules/Categorization/Providers/CategorizationServiceProvider.php,
    Modules/Ledger/Resources/views/livewire/transaction-detail.blade.php,
    resources/views/layouts/app.blade.php,
    Modules/Categorization/tests/Feature/CorrectionDivergenceTest.php,
    Modules/Categorization/tests/Feature/CategorizationProvenancePanelTest.php
  </files>
  <read_first>
    Modules/Categorization/Public/Actions/AssignCategory.php (plan 04 — extend to detect divergence),
    Modules/Categorization/Public/Events/TransactionCategorized.php (shape precedent for the new event),
    Modules/Categorization/Public/Services/CategorizationRuleQuery.php,
    Modules/Categorization/Internal/Http/Livewire/InlineCategoryPicker.php (services on action method),
    Modules/Categorization/Providers/CategorizationServiceProvider.php,
    Modules/EmailScan/Internal/Http/Livewire/BackfillWindowModal.php (Livewire #[On] event listener),
    Modules/Receipts/Internal/Http/Livewire/ReceiptConflictToast.php (plan 04 — analog toast shape),
    Modules/Ledger/Resources/views/livewire/transaction-detail.blade.php,
    Modules/Ledger/Internal/Http/Livewire/TransactionDetail.php,
    resources/views/layouts/app.blade.php (Phase 6 @auth block, currently mounts core.top-nav only),
    .planning/phases/07-email-template-matchers-categorization-learning/07-UI-SPEC.md (Transaction detail provenance panels + Correction-divergence toast Copywriting Contract; § Mounting Strategy lines 484-495),
    .planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md (Wave 4 AssignCategory extension excerpt lines 879-914; CategorizationProvenancePanel notes lines 740-741; layout mount lines 962-969)
  </read_first>
  <behavior>
    - Test: AssignCategory dispatches CategorizationDiverged when prior auto_category_provenance.source === 'rule' AND $categoryId !== $prior['category_id'] AND $prior['rule_id'] !== null.
    - Test: NO CategorizationDiverged dispatch when prior provenance is null (manual) or 'memory'.
    - Test: NO dispatch when $categoryId === prior category_id (no actual divergence).
    - Test: CategorizationProvenancePanel mount(transactionId) renders the rule variant when transaction's auto_category_provenance has source='rule'; renders memory variant for 'memory'; renders nothing for null/'manual'.
    - Test: Rule variant panel renders UI-SPEC locked text 'Rule that fired' + field + match + quoted value + category, plus Update / Remove rule buttons.
    - Test: Memory variant renders UI-SPEC locked text 'Auto-categorized from merchant history' + Override button.
    - Test: CorrectionDivergenceToast listens for the Livewire-local event 'correction-divergence:fire' via #[On('correction-divergence:fire')] handleDiverged(); populates internal properties; renders UI-SPEC locked text 'Update the rule?' + "Update rule" + "Keep current rule". (Locked bridge per B5 — not echo:broadcaster, not session-flash.)
    - Test (DI signatures W5): mount(), handleDiverged(), update(), dismiss() each receive their dependencies via method-DI parameters typed against CurrentUser / UpdateCategorizationRule / CategorizationRuleQuery / ViewFactory — assert via reflection (Pest test) that NO method takes zero parameters when the production class expects an injected service. NO facade use anywhere in the class — grep MUST find zero `Auth::` / `Cache::` / `DB::` / `View::` / `auth()` / `config()` / `request()` in the class body.
    - Test: Toast primary action calls UpdateCategorizationRule with newCategoryId; secondary dismisses without action.
    - Test: 8s auto-dismiss attribute present on the toast container (x-init timer OR similar).
    - Test (cross-user T-07-09): handleDiverged() called with userId=999 while $currentUser->user()->id===1 does NOT populate the toast properties and does NOT render the toast for user 1 (defense: toast SFC asserts $userId === $currentUser->user()->id before populating properties). Signature shape: handleDiverged(int $transactionId, int $ruleId, int $oldCategoryId, int $newCategoryId, int $userId, CurrentUser $currentUser).
    - Test (layout mount): the rendered app.blade.php @auth block contains the three @livewire mounts (rule-form-modal, correction-divergence-toast, receipt-conflict-toast).
    - Test (transaction-detail integration): visiting /transactions/{id} renders the CategorizationProvenancePanel as a Livewire sub-component below Reclassify and above View chain (per UI-SPEC § Component Inventory).
  </behavior>
  <action>
Create Modules/Categorization/Public/Events/CategorizationDiverged.php — final readonly class with fields per the <interfaces> section.

Modify Modules/Categorization/Public/Actions/AssignCategory.php per PATTERNS.md excerpt lines 879-914:
  - Constructor extends: + private readonly DatabaseManager $db (to read prior provenance).
  - __invoke: BEFORE calling $this->updater, read $priorProvenance = $this->db->connection()->table('transactions')->where('id', $transactionId)->where('user_id', $user->id)->value('auto_category_provenance').
  - After successful update, dispatch existing TransactionCategorized as today.
  - Additionally: if $priorProvenance !== null, json_decode it; if ($decoded['source'] ?? null) === 'rule' && ($decoded['rule_id'] ?? null) !== null && $categoryId !== ($decoded['category_id'] ?? null), dispatch new CategorizationDiverged(transactionId, ruleId, oldCategoryId=$decoded['category_id'], newCategoryId=$categoryId, userId=$user->id).

Create Modules/Categorization/Internal/Http/Livewire/CategorizationProvenancePanel.php — Livewire SFC (services on action method, NOT constructor):
  - public int $transactionId = 0; public string $variant = 'none'; public ?array $ruleSummary = null; public ?bool $confirmingRemove = false.
  - mount(int $transactionId, DatabaseManager $db, CurrentUser $currentUser, CategorizationRuleQuery $rules):
    - Read transaction's auto_category_provenance scoped by user_id.
    - If source='rule': hydrate $ruleSummary with the rule's field/match/value/category-path via $rules->findForUser($currentUser->user(), $ruleId); $variant='rule'.
    - elseif source='memory': $variant='memory'.
    - else: $variant='none'.
  - updateRule(CurrentUser $currentUser, UpdateCategorizationRule $update, int $newCategoryId): dispatch 'rule-form:open' with the rule id so the global RuleFormModal opens pre-populated.
  - removeRule(CurrentUser $currentUser, DeleteCategorizationRule $delete): two-step inline confirmation (confirmingRemove flag); on yes -> call $delete -> re-render as memory or none variant.
  - overrideMemory(CurrentUser $currentUser): dispatch 'inline-category-picker:open' with $transactionId (existing flow from Phase 1 — InlineCategoryPicker handles the actual category change via AssignCategory).
  - render(ViewFactory $views): make 'categorization::livewire.categorization-provenance-panel'.

W3 — Cross-component dispatch sequence (TransactionDetail → CorrectionDivergenceToast):

  Modules/Ledger/Internal/Http/Livewire/TransactionDetail.php gains a reclassify() flow that invokes AssignCategory and then re-dispatches the CategorizationDiverged payload as a Livewire-local event so the globally-mounted CorrectionDivergenceToast SFC can receive it within the same request lifecycle:

  1. reclassify(CurrentUser $currentUser, AssignCategory $assign, Dispatcher $events, int $newCategoryId): void
  2. Capture the prior provenance shape (rule_id, category_id, source) by reading transactions.auto_category_provenance scoped to $currentUser->user()->id BEFORE invoking $assign.
  3. Invoke ($assign)($currentUser->user(), $this->transactionId, $newCategoryId). AssignCategory dispatches CategorizationDiverged on the event Dispatcher when the source==='rule' divergence condition holds (per the action edit above).
  4. AFTER AssignCategory returns, if the prior provenance had source==='rule' AND $newCategoryId !== $prior['category_id'] AND $prior['rule_id'] !== null, $this->dispatch('correction-divergence:fire', transactionId: $this->transactionId, ruleId: $prior['rule_id'], oldCategoryId: $prior['category_id'], newCategoryId: $newCategoryId, userId: $currentUser->user()->id). The userId is carried explicitly in the Livewire-local payload so the toast's handleDiverged() receives it as a named method parameter.
  5. The global CorrectionDivergenceToast SFC is mounted in app.blade.php @auth and listens via #[On('correction-divergence:fire')] handleDiverged(int $transactionId, int $ruleId, int $oldCategoryId, int $newCategoryId, int $userId, CurrentUser $currentUser). Same-request, same-payload, no broadcaster, no session flash.

  Why we re-dispatch from TransactionDetail rather than have the toast listen to the framework event directly: Livewire-local events flow through the same browser request lifecycle as the originating action; the framework Dispatcher event (CategorizationDiverged) is a backend-only signal that AssignCategory uses for OTHER listeners (e.g. analytics). The Livewire-local 'correction-divergence:fire' is the user-visible bridge.

Create Modules/Categorization/Internal/Http/Livewire/CorrectionDivergenceToast.php — Livewire 4 SFC. Services-via-method-DI throughout (no constructor — Livewire components instantiate via the framework, so dependencies arrive on method calls; matches Shared Pattern 2):

  - Public properties:
      public bool $visible = false;
      public ?int $ruleId = null;
      public ?int $newCategoryId = null;
      public ?int $oldCategoryId = null;
      public ?int $transactionId = null;
      public string $message = '';

  - Event subscription is LOCAL (not broadcast). The AssignCategory action is invoked synchronously inside TransactionDetail::reclassify() (the Reclassify Livewire component); after AssignCategory dispatches CategorizationDiverged on the backend Dispatcher, TransactionDetail re-emits a Livewire-local event 'correction-divergence:fire' carrying every field of the CategorizationDiverged payload (including userId) so the globally-mounted toast SFC receives it in the same request lifecycle. The toast SFC listens via #[On('correction-divergence:fire')].

      Why local-bridge over the alternatives:
        - Echo broadcaster (#[On('echo:CategorizationDiverged')]): requires Reverb/Pusher — over-engineered for a single-user, single-machine app.
        - Session flash: depends on a redirect or full page render, which Livewire's SPA-style reclassify() does NOT trigger.
        - Local Livewire event: Livewire 4's #[On] handles same-page cross-component dispatch natively in one request.

  - Method signatures (every service via method DI — no facades). Note: handleDiverged() takes both `int $userId` (5th parameter, the explicit payload field used for the cross-user guard) AND `CurrentUser $currentUser` (6th parameter, method-DI for resolving the active user). The 5th parameter is the assertion subject; the 6th parameter is the assertion oracle:

      #[On('correction-divergence:fire')]
      public function handleDiverged(int $transactionId, int $ruleId, int $oldCategoryId, int $newCategoryId, int $userId, CurrentUser $currentUser): void
          - Guard: if $currentUser->user()->id !== $userId, return without populating properties (T-07-09 cross-user defense). The $userId here is the 5th positional parameter received from the 'correction-divergence:fire' Livewire-local payload; the $currentUser is the 6th parameter (method-DI) used as the comparison oracle.
          - Populate properties; $this->visible = true.

      public function mount(CategorizationRuleQuery $rules, CurrentUser $currentUser): void
          - Initialises a hidden toast on first render. Properties stay null/false. No DB read (no need — the toast only renders when handleDiverged fires).

      public function update(CurrentUser $currentUser, UpdateCategorizationRule $updateRule): void
          - Guard: $this->ruleId !== null AND $this->newCategoryId !== null; else return.
          - ($updateRule)($currentUser->user(), $this->ruleId, ['category_id' => $this->newCategoryId]);
          - $this->visible = false; $this->reset(['ruleId','newCategoryId','oldCategoryId','transactionId']);
          - $this->dispatch('flash-success', message: 'Rule updated.');

      public function dismiss(): void
          - $this->visible = false; $this->reset(...) (same fields as update()).

      public function render(ViewFactory $views): View
          - return $views->make('categorization::livewire.correction-divergence-toast');

  Every service appears as a typed method parameter (CurrentUser, UpdateCategorizationRule, CategorizationRuleQuery, ViewFactory) — NEVER as a global helper (auth(), request(), view(), config()) or as a Facade. Project memory feedback_laravel_di_only.md is the source of truth on this pattern.

Create Modules/Categorization/Resources/views/livewire/categorization-provenance-panel.blade.php — UI-SPEC § Transaction detail provenance panels verbatim. Three @if branches keyed on $variant. role="region" aria-labelledby="provenance-heading-{txId}" per UI-SPEC § Accessibility Notes. font-mono on the quoted rule value.

Create Modules/Categorization/Resources/views/livewire/correction-divergence-toast.blade.php — UI-SPEC § Correction-divergence toast verbatim. Fixed bottom-right, max-w-sm, role="status" aria-live="polite". 8s auto-dismiss via Alpine x-init setTimeout.

Modify Modules/Categorization/Providers/CategorizationServiceProvider.php — boot(): $livewire->component('categorization.categorization-provenance-panel', CategorizationProvenancePanel::class); $livewire->component('categorization.correction-divergence-toast', CorrectionDivergenceToast::class).

Modify Modules/Ledger/Resources/views/livewire/transaction-detail.blade.php — embed @livewire('categorization.categorization-provenance-panel', ['transactionId' => $transaction->id], key('provenance-' . $transaction->id)) between the Reclassify section and the View chain section per UI-SPEC § Component Inventory.

Modify resources/views/layouts/app.blade.php @auth block per UI-SPEC § Mounting Strategy lines 484-495 verbatim:
  @auth
      @livewire('core.top-nav')
      @livewire('categorization.rule-form-modal')
      @livewire('categorization.correction-divergence-toast')
      @livewire('receipts.receipt-conflict-toast')
  @endauth

Create Modules/Categorization/tests/Feature/CorrectionDivergenceTest.php + CategorizationProvenancePanelTest.php covering the behavior list.
  </action>
  <acceptance_criteria>
    - grep -q "final readonly class CategorizationDiverged" Modules/Categorization/Public/Events/CategorizationDiverged.php
    - grep -q "CategorizationDiverged" Modules/Categorization/Public/Actions/AssignCategory.php (dispatch added)
    - grep -q "auto_category_provenance" Modules/Categorization/Public/Actions/AssignCategory.php (reads prior provenance)
    - grep -q "Rule that fired" Modules/Categorization/Resources/views/livewire/categorization-provenance-panel.blade.php (UI-SPEC locked)
    - grep -q "Auto-categorized from merchant history" Modules/Categorization/Resources/views/livewire/categorization-provenance-panel.blade.php
    - grep -q "Update the rule?" Modules/Categorization/Resources/views/livewire/correction-divergence-toast.blade.php (UI-SPEC locked)
    - grep -q "#\[On.*correction-divergence:fire" Modules/Categorization/Internal/Http/Livewire/CorrectionDivergenceToast.php (locked Livewire-local bridge per B5)
    - grep -Eq "handleDiverged\(.*int \\\$userId.*CurrentUser \\\$currentUser" Modules/Categorization/Internal/Http/Livewire/CorrectionDivergenceToast.php (signature carries both the int $userId payload AND the CurrentUser method-DI oracle)
    - grep -q "correction-divergence:fire" Modules/Ledger/Internal/Http/Livewire/TransactionDetail.php (TransactionDetail::reclassify re-dispatches the local bridge with the userId field)
    - grep -v "^[[:space:]]*//" Modules/Categorization/Internal/Http/Livewire/CorrectionDivergenceToast.php | grep -cE "echo:CategorizationDiverged|session\(\)->flash" | tr -d "[:space:]" equals 0 (NO broadcaster echo + NO session-flash bridge — only Livewire-local event)
    - grep -v "^[[:space:]]*//" Modules/Categorization/Internal/Http/Livewire/CorrectionDivergenceToast.php | grep -cE "Auth::|DB::|Cache::|View::|auth\(\)|config\(\)|request\(\)|view\(\)" | tr -d "[:space:]" equals 0 (W5 DI-only — services arrive via method parameters)
    - grep -q "categorization.categorization-provenance-panel" Modules/Ledger/Resources/views/livewire/transaction-detail.blade.php
    - grep -q "categorization.rule-form-modal" resources/views/layouts/app.blade.php
    - grep -q "categorization.correction-divergence-toast" resources/views/layouts/app.blade.php
    - grep -q "receipts.receipt-conflict-toast" resources/views/layouts/app.blade.php
    - ./vendor/bin/pest --filter='CorrectionDivergenceTest|CategorizationProvenancePanelTest|AssignCategory' --parallel exits 0
    - ./vendor/bin/pest --parallel exits 0 (full suite)
    - ./vendor/bin/phpstan analyse Modules/Categorization Modules/Ledger Modules/Receipts --memory-limit=2G --no-progress exits 0
    - ./vendor/bin/pint --test Modules/Categorization Modules/Ledger Modules/Receipts resources/views/layouts/app.blade.php exits 0
  </acceptance_criteria>
  <validation_axis>Axis 5 — Rule precedence + correction divergence loop closes (rule fires -> user corrects -> toast offers to update rule). Axis 8 — boundary invariants (cross-user event filtering in CorrectionDivergenceToast).</validation_axis>
  <verify>
    <automated>./vendor/bin/pest --filter='CorrectionDivergenceTest|CategorizationProvenancePanelTest|AssignCategory' --parallel &amp;&amp; ./vendor/bin/pest --parallel &amp;&amp; ./vendor/bin/phpstan analyse Modules/Categorization Modules/Ledger Modules/Receipts --memory-limit=2G --no-progress</automated>
  </verify>
  <done>CategorizationDiverged event + AssignCategory divergence dispatch + CorrectionDivergenceToast + CategorizationProvenancePanel + transaction-detail integration + 3 global SFC mounts in app.blade.php shipped; UI-SPEC copy verbatim; phpstan + pint green; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SettingsPage auto-import toggle + ScanInboxDropFolderJob + console schedule + BoundaryArchTest carve-out</name>
  <files>
    Modules/Core/Internal/Http/Livewire/SettingsPage.php,
    Modules/Core/Resources/views/livewire/settings-page.blade.php,
    Modules/Receipts/Internal/Jobs/ScanInboxDropFolderJob.php,
    Modules/Receipts/Providers/ReceiptsServiceProvider.php,
    Modules/Receipts/Routes/console.php,
    routes/console.php,
    tests/Contracts/BoundaryArchTest.php,
    Modules/Receipts/tests/Feature/ScanInboxDropFolderJobTest.php
  </files>
  <read_first>
    Modules/Core/Internal/Http/Livewire/SettingsPage.php (existing properties + validate-then-save pattern),
    Modules/Core/Resources/views/livewire/settings-page.blade.php (existing section template — currency-display section is closest analog),
    Modules/EmailScan/Internal/Jobs/IncrementalScanJob.php (ShouldBeUniqueUntilProcessing + Cache::driver('redis') + handle DI),
    Modules/EmailScan/Internal/Jobs/DiscoveryScanJob.php,
    Modules/Chains/Internal/Jobs/ResolveChainLinksJob.php (Cache facade carve-out comment shape),
    Modules/Receipts/Public/Actions/RecordReceipt.php (the entrypoint the scan job dispatches to per-file),
    Modules/Receipts/Internal/Pipeline/MboxIterator.php,
    Modules/Receipts/Internal/Pipeline/EmlMimeReader.php,
    Modules/Receipts/Internal/Pipeline/FileDropEmlBlobStore.php,
    Modules/Core/Database/Migrations/2026_05_17_010007_add_auto_import_drop_folder_to_users.php (plan 01),
    routes/console.php (existing hourly/daily Schedule::call entries),
    tests/Contracts/BoundaryArchTest.php (Cache carve-out list — already contains Receipts ProcessFetchedInboxMessagesJob from plan 01),
    .planning/phases/07-email-template-matchers-categorization-learning/07-UI-SPEC.md (/settings watched-folder section — Copywriting Contract + States Inventory),
    .planning/phases/07-email-template-matchers-categorization-learning/07-RESEARCH.md (Pattern 7 watched-folder lines 636-657),
    .planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md (Wave 4 ScanInboxDropFolderJob + SettingsPage extension excerpts lines 753-756 + 926-958)
  </read_first>
  <behavior>
    - Test (SettingsPage): renders the new "Auto-import" section with locked copy + toggle row. Initially off when users.auto_import_drop_folder=false; clicking the toggle (wire:click="toggleAutoImport") immediately UPDATEs the user column without a Save button.
    - Test (SettingsPage): help-text variant changes between off and on per UI-SPEC § /settings watched-folder section.
    - Test (ScanInboxDropFolderJob constructor): takes public readonly int $userId; uniqueId returns string form.
    - Test (ScanInboxDropFolderJob handle — happy path): seeds 2 .eml files under storage/app/inbox-drop/{userId}/ via Storage::fake; runs the job; asserts both .eml files moved to storage/app/inbox-drop/{userId}/processed/{YYYY-MM}/original-filename with the current YYYY-MM; asserts 2 transactions rows + 2 file_imports rows landed.
    - Test (ScanInboxDropFolderJob — failed parse): seeds a .eml whose body fails the matcher; asserts the file moves to storage/app/inbox-drop/{userId}/failed/{YYYY-MM}/ with a sibling .error.txt containing the exception message.
    - Test (idempotency): re-running the job on the same processed/{YYYY-MM}/ files is a no-op (the scan loop only walks the root inbox-drop/{userId}/ NOT the processed subfolder).
    - Test (cross-user T-07-04): seed files under storage/app/inbox-drop/999/; run ScanInboxDropFolderJob(userId=1); assert NO files under user 999's folder are touched.
    - Test (console schedule): assert routes/console.php registers an entry named 'receipts.scan-drop-folder' that dispatches one ScanInboxDropFolderJob per user with auto_import_drop_folder=true. (Use Schedule::getEvents and grep by name.)
    - Test (BoundaryArchTest): the carve-out list now contains Modules\\Receipts\\Internal\\Jobs\\ScanInboxDropFolderJob alongside ProcessFetchedInboxMessagesJob (Cache facade legal).
  </behavior>
  <action>
Modify Modules/Core/Internal/Http/Livewire/SettingsPage.php — add public bool $autoImportFromDropFolder = false; mount it from $user->auto_import_drop_folder; add toggleAutoImport(CurrentUser, DatabaseManager) action method that UPDATEs the column and flips the property. Mirror the existing currency-display section's pattern (no Save button for the toggle — instant-apply).

Modify Modules/Core/Resources/views/livewire/settings-page.blade.php — append a new <section class="space-y-2"> per UI-SPEC § /settings watched-folder section. Toggle row with wire:model.live="autoImportFromDropFolder"; help text below referenced via aria-describedby="auto-import-help". Locked copy verbatim from UI-SPEC.

Create Modules/Receipts/Internal/Jobs/ScanInboxDropFolderJob.php:
  - final class implements ShouldBeUniqueUntilProcessing, ShouldQueue.
  - use Dispatchable, InteractsWithQueue, Queueable, SerializesModels.
  - public int $tries = 3; public array $backoff = [60, 300, 900].
  - constructor: public readonly int $userId.
  - uniqueId returns (string) $this->userId; uniqueFor returns 600.
  - uniqueVia returns Cache::driver('redis') — class-level docblock includes the standard carve-out rationale.
  - handle(Filesystem $files, DatabaseManager $db, Clock $clock, RecordReceipt $recordReceipt, MboxIterator $mboxIterator, EmlMimeReader $reader, LoggerInterface $logger):
    1. Load User scoped by $this->userId.
    2. $baseDir = storage_path('app/inbox-drop/'.$this->userId);
    3. If $baseDir does not exist, return.
    4. Iterate top-level files (NOT directories — exclude processed/ and failed/ subfolders) under $baseDir; for each file with extension .eml or .mbox:
       a. Try: read bytes (or stream for .mbox); for .eml: $recordReceipt($bytes, $user, $originalFilename, importRunId=null); for .mbox: $mboxIterator->iterate($path); per-message $recordReceipt; collect outcomes.
       b. On success: $ym = $clock->now()->format('Y-m'); $files->ensureDirectoryExists($baseDir.'/processed/'.$ym, 0700, recursive: true); rename($path, $baseDir.'/processed/'.$ym.'/'.basename($path)).
       c. On exception (matcher throws / IO error): $files->ensureDirectoryExists($baseDir.'/failed/'.$ym, 0700, recursive: true); rename($path, $baseDir.'/failed/'.$ym.'/'.basename($path)); write sibling .error.txt with $e->getMessage().
       d. Wrap each file's processing in a try-catch so one bad file does not abort the whole scan.

Modify Modules/Receipts/Providers/ReceiptsServiceProvider.php — no new binding needed (ScanInboxDropFolderJob is dispatched, not container-resolved as a singleton).

Modify Modules/Receipts/Routes/console.php — keep empty (the scheduling lives in the root routes/console.php which already has the DI-friendly Schedule::call pattern; per CONTEXT.md D-718 the cadence is documented at the root).

Modify routes/console.php — append the new Schedule::call entry per PATTERNS.md excerpt lines 953-958:
  Schedule::call(function (DatabaseManager $db, Dispatcher $bus): void {
      $userIds = $db->connection()->table('users')->where('auto_import_drop_folder', true)->pluck('id');
      foreach ($userIds as $id) {
          $bus->dispatch(new ScanInboxDropFolderJob((int) $id));
      }
  })->name('receipts.scan-drop-folder')->everyFiveMinutes()->withoutOverlapping(10);
  Method order .name() BEFORE .everyFiveMinutes()->withoutOverlapping(...) per the existing email-scan.incremental + email-scan.discovery pattern (Laravel CallbackEvent::withoutOverlapping throws LogicException when description not set).

Modify tests/Contracts/BoundaryArchTest.php — extend the Cache facade carve-out list `->ignoring([...])` to include Modules\\Receipts\\Internal\\Jobs\\ScanInboxDropFolderJob (the plan 01 entry was for ProcessFetchedInboxMessagesJob; this plan adds the second receipts job).

Create Modules/Receipts/tests/Feature/ScanInboxDropFolderJobTest.php covering every behavior case. Use Storage::fake('local'), seed .eml files via the existing fixtures from plans 02/03 (paypal current-receipt.eml etc.).
  </action>
  <acceptance_criteria>
    - grep -q "auto_import_drop_folder" Modules/Core/Internal/Http/Livewire/SettingsPage.php
    - grep -q "toggleAutoImport" Modules/Core/Internal/Http/Livewire/SettingsPage.php
    - grep -q "Auto-import" Modules/Core/Resources/views/livewire/settings-page.blade.php (UI-SPEC locked section heading)
    - grep -q "Auto-import from drop folder" Modules/Core/Resources/views/livewire/settings-page.blade.php
    - grep -q "every 5 minutes" Modules/Core/Resources/views/livewire/settings-page.blade.php (UI-SPEC help text)
    - grep -q "final class ScanInboxDropFolderJob" Modules/Receipts/Internal/Jobs/ScanInboxDropFolderJob.php
    - grep -q "ShouldBeUniqueUntilProcessing" Modules/Receipts/Internal/Jobs/ScanInboxDropFolderJob.php
    - grep -q "public readonly int \\$userId" Modules/Receipts/Internal/Jobs/ScanInboxDropFolderJob.php
    - grep -q "Cache::driver('redis')" Modules/Receipts/Internal/Jobs/ScanInboxDropFolderJob.php
    - grep -q "receipts.scan-drop-folder" routes/console.php
    - grep -q "everyFiveMinutes" routes/console.php
    - grep -q "Modules\\\\\\\\Receipts\\\\\\\\Internal\\\\\\\\Jobs\\\\\\\\ScanInboxDropFolderJob" tests/Contracts/BoundaryArchTest.php (Cache carve-out)
    - ./vendor/bin/pest --filter='SettingsPage|ScanInboxDropFolderJobTest|BoundaryArchTest' --parallel exits 0
    - ./vendor/bin/pest --parallel exits 0 (full suite passes — entire Phase 7 plan-set committed)
    - ./vendor/bin/phpstan analyse Modules/Receipts Modules/Core routes/console.php tests/Contracts/BoundaryArchTest.php --memory-limit=2G --no-progress exits 0
    - ./vendor/bin/pint --test Modules/Receipts Modules/Core routes/console.php tests/Contracts/BoundaryArchTest.php exits 0
  </acceptance_criteria>
  <validation_axis>Axis 6 — Idempotency under re-scan (processed/{YYYY-MM}/ moves are atomic; re-running the job on processed files is a no-op). Axis 8 — boundary invariants (BoundaryArchTest carve-out lists the second receipts job; per-user folder isolation enforced).</validation_axis>
  <verify>
    <automated>./vendor/bin/pest --filter='SettingsPage|ScanInboxDropFolderJobTest|BoundaryArchTest' --parallel &amp;&amp; ./vendor/bin/pest --parallel &amp;&amp; ./vendor/bin/phpstan analyse Modules/Receipts Modules/Core routes/console.php tests/Contracts/BoundaryArchTest.php --memory-limit=2G --no-progress</automated>
  </verify>
  <done>Settings auto-import toggle live; ScanInboxDropFolderJob scheduled every 5 minutes; processed/failed folder semantics tested; BoundaryArchTest carve-out extended; full Phase 7 plan-set passes composer test + composer analyse + composer format:check.</done>
</task>

</tasks>

<verification>
- All Wave 4 tests green: RulesPageTest, RuleFormModalTest, CorrectionDivergenceTest, CategorizationProvenancePanelTest, SettingsPage extension, ScanInboxDropFolderJobTest.
- Full suite (composer test) green — Phase 7 demoable end-to-end.
- composer analyse + format:check green.
- BoundaryArchTest noEmailFetchFromReceipts still green; Cache carve-out list now contains both receipts jobs (ProcessFetchedInboxMessagesJob + ScanInboxDropFolderJob).
- Manual demo path:
  1. Visit /rules: create a rule "merchant contains SPOTIFY -> Streaming".
  2. Drop a PayPal Spotify .eml via /imports wizard: lands a transaction auto-categorized as Streaming (provenance='rule').
  3. Visit /transactions/{id}: see the "Rule that fired" provenance panel.
  4. Click Reclassify -> pick "Music" instead: see the correction-divergence toast "Update the rule?". Click Update rule -> the rule's category swaps to Music.
  5. Toggle /settings auto-import; drop the same .eml into storage/app/inbox-drop/{userId}/; wait <=5 min; verify it moves to processed/{YYYY-MM}/ and a new file_imports row appears.
- Validation axes 4, 5, 6, 7, 8 cumulatively covered across Phase 7.
</verification>

<rollback>
git revert <plan-commit-sha>. The 3 new actions + 4 new SFCs + 5 new Blade views + 1 new event + 1 new job + 1 schedule entry + layout/top-nav/transaction-detail edits all revert cleanly. The Cache facade carve-out for ScanInboxDropFolderJob also reverts — leaving only ProcessFetchedInboxMessagesJob in the list. No data loss path: rules + provenance JSON + user setting columns survive (they were introduced by plan 01 migrations); rollback only removes the UI surface so users cannot CRUD rules from the UI until plan 05 re-applies.
</rollback>

<success_criteria>
- /rules page renders empty state + create + edit + delete flows per UI-SPEC.
- Top-nav contains "Rules" item between Uncategorized and Review chains.
- Correction-divergence toast fires when reclassifying a rule-provenance transaction; Update rule button updates the rule.
- Transaction detail drawer renders the provenance panel (rule / memory / nothing variants per UI-SPEC).
- /settings shows the auto-import toggle with UI-SPEC locked copy.
- ScanInboxDropFolderJob runs every 5 minutes when toggled on; moves processed files to /processed/{YYYY-MM}/; failed files to /failed/{YYYY-MM}/ with sibling .error.txt.
- All 3 global SFCs (RuleFormModal + CorrectionDivergenceToast + ReceiptConflictToast) mounted in app.blade.php @auth block.
- BoundaryArchTest carve-out list extended; arch test green.
- Validation axes 4, 5, 6, 7, 8 covered; XSS + cross-user + CSRF defenses tested.
- Phase 7 demo path end-to-end functional.
</success_criteria>

<output>
Create `.planning/phases/07-email-template-matchers-categorization-learning/07-05-SUMMARY.md` when done.
</output>
</output>
