---
phase: 07
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - Modules/EmailScan/Public/Services/EmlBlobStore.php
  - Modules/EmailScan/Internal/EmlBlobStore.php
  - Modules/EmailScan/Providers/EmailScanServiceProvider.php
  - Modules/EmailScan/Internal/Jobs/IncrementalScanJob.php
  - Modules/EmailScan/Internal/Jobs/BackfillInboxJob.php
  - Modules/EmailScan/tests/Integration/EmlOrphanCleanupTest.php
  - Modules/EmailScan/tests/Integration/BackfillChunkedJobTest.php
  - Modules/EmailScan/tests/Integration/ResumeFromCursorTest.php
  - Modules/EmailScan/tests/Integration/ConcurrentBackfillTest.php
  - Modules/EmailScan/tests/Integration/BackfillGraphTest.php
  - Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php
  - Modules/Receipts/Internal/MatcherRegistry.php
  - Modules/Receipts/Internal/Pipeline/ReceiptSourceAdapter.php
  - Modules/Receipts/Internal/Pipeline/EmlHeaderProfile.php
  - Modules/Receipts/Internal/Pipeline/MboxHeaderProfile.php
  - Modules/Receipts/Internal/Jobs/ProcessFetchedInboxMessagesJob.php
  - Modules/Receipts/Internal/Http/Livewire/WizardEmailFileStep.php
  - Modules/Receipts/Public/Actions/RecordReceipt.php
  - Modules/Receipts/Public/Services/FileImportQuery.php
  - Modules/Receipts/Providers/ReceiptsServiceProvider.php
  - Modules/Receipts/Resources/views/livewire/wizard-email-file-step.blade.php
  - Modules/Receipts/Routes/console.php
  - Modules/Receipts/tests/fixtures/paypal/current-receipt.eml
  - Modules/Receipts/tests/fixtures/paypal/prior-generation-receipt.eml
  - Modules/Receipts/tests/fixtures/paypal/login-notification.eml
  - Modules/Receipts/tests/fixtures/paypal/spoofed-sender.eml
  - Modules/Receipts/tests/fixtures/paypal/foreign-currency-receipt.eml
  - Modules/Receipts/tests/fixtures/paypal/malformed-date-receipt.eml
  - Modules/Receipts/tests/fixtures/paypal/paired-csv-row.csv
  - Modules/Receipts/tests/Unit/Matchers/PaypalReceiptMatcherTest.php
  - Modules/Receipts/tests/Feature/EmlFileDropTest.php
  - Modules/Receipts/tests/Feature/MboxFileDropTest.php
  - Modules/Receipts/tests/Feature/ProcessFetchedInboxMessagesJobTest.php
  - Modules/Receipts/tests/Contracts/FingerprintParityTest.php
  - Modules/Ingestion/Public/Services/HeaderSniffer.php
  - Modules/Import/Internal/Http/Livewire/UploadWizard.php
  - Modules/Import/Resources/views/livewire/upload-wizard.blade.php
  - Modules/Import/Internal/Pipeline/Stages/ParseStage.php
  - Modules/Import/Public/Services/SourceRefRanker.php
  - Modules/Receipts/Routes/console.php
  - routes/console.php
  - tests/Contracts/IdempotencyContractTest.php
  - tests/Contracts/BoundaryArchTest.php
autonomous: true
requirements: [EML-05, EML-07]
must_haves:
  truths:
    - "User can drop a PayPal .eml via /imports wizard 'Email file' arm and see a canonical transaction land in /transactions (SC #1 + SC #2 vertical slice)"
    - "Re-dropping the same .eml is a no-op — UNIQUE (user_id, provider_message_id) enforced via D-705 + D-705a synthetic hash"
    - "User can drop a .mbox containing 5 messages and N file_imports rows + N parsed transactions land"
    - "PaypalReceiptMatcher claims @paypal.com receipts with exact suffix match — rejects @paypal.com.attacker.example (T-07-04 spoofing defense)"
    - "Receipt-derived SourceTransactionDto for a known PayPal CSV row produces an identical fingerprint (load-bearing invariant)"
    - "ProcessFetchedInboxMessagesJob transitions inbox_messages.status from 'fetched' to 'parsed'|'skipped'|'unmatched' (Phase 6 handoff)"
    - "PayPal receipts populate transaction.reference_id automatically so Phase 5 ResolveChainLinksJob picks them up (D-708 silent half)"
    - "SourceRefRanker ranks 'paypal-receipt' above 'paypal-csv' (D-706 cross-format dedup — receipts win on ENRICHED)"
    - "EmlBlobStore lives at Modules/EmailScan/Public/Services/EmlBlobStore.php (promoted from Internal/) — cross-module DI by ProcessFetchedInboxMessagesJob no longer trips BoundaryArchTest noInternalCrossModule (Task 0 precondition)"
    - "Per-sender SenderMatcher contract + container-tagged registry (D-702)"
    - "Receipt path reuses NormalizeStage → FingerprintComposer v3 → ApplyEnrichments → RecordTransactions pipeline (D-703)"
    - "Wizard primary .eml/.mbox arm under /imports (D-704 primary path)"
  artifacts:
    - path: "Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php"
      provides: "key=paypal-receipt, priority=100, canHandle suffix-match @paypal.com, match returns MatchOutcomeDto"
    - path: "Modules/Receipts/Internal/MatcherRegistry.php"
      provides: "dispatch(MatcherInputDto) returns MatchOutcomeDto — first-priority-canHandle winner"
    - path: "Modules/Receipts/Internal/Pipeline/ReceiptSourceAdapter.php"
      provides: "toSourceDto bridge from ParsedReceiptDto to SourceTransactionDto"
    - path: "Modules/Receipts/Internal/Jobs/ProcessFetchedInboxMessagesJob.php"
      provides: "Queued ShouldBeUniqueUntilProcessing per-user backlog walker"
    - path: "Modules/Receipts/Internal/Http/Livewire/WizardEmailFileStep.php"
      provides: "/imports wizard 'email-file' issuer arm (Livewire SFC)"
    - path: "Modules/Receipts/Public/Actions/RecordReceipt.php"
      provides: "Public entrypoint shared by consumer job + wizard upload"
    - path: "Modules/Receipts/Public/Services/FileImportQuery.php"
      provides: "Wizard preview + future drawer queries"
  key_links:
    - from: "Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php"
      to: "Modules/Receipts/Internal/Pipeline/EmlMimeReader.php"
      via: "constructor DI"
      pattern: "EmlMimeReader"
    - from: "Modules/Receipts/Internal/Jobs/ProcessFetchedInboxMessagesJob.php"
      to: "Modules/EmailScan/Public/Services/InboxMessageQuery.php"
      via: "handle() parameter"
      pattern: "InboxMessageQuery"
    - from: "Modules/Import/Public/Services/SourceRefRanker.php"
      to: "paypal-receipt"
      via: "rank table extension"
      pattern: "paypal-receipt"
    - from: "tests/Contracts/IdempotencyContractTest.php"
      to: "Modules/Receipts/tests/fixtures/paypal/current-receipt.eml"
      via: "new dataset row 'paypal-receipt-eml'"
      pattern: "paypal-receipt"
threat_model:
  - T-07-04
  - T-07-02
  - T-07-09
---

<objective>
Ship the PayPal vertical slice end-to-end: PayPal .eml -> PaypalReceiptMatcher -> ParsedReceiptDto -> ReceiptSourceAdapter -> existing NormalizeStage -> FingerprintComposer v3 -> ApplyEnrichments -> RecordTransactions, plus the /imports wizard "Email file" arm (.eml + .mbox), the ProcessFetchedInboxMessagesJob consumer for Phase 6 handoff, the SourceRefRanker extension, and the load-bearing ReceiptCsvFingerprintParityTest GREEN for PayPal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-CONTEXT.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-RESEARCH.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-UI-SPEC.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-VALIDATION.md
@CLAUDE.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-01-PLAN.md
@Modules/Ingestion/Public/Contracts/SourceAdapter.php
@Modules/Ingestion/Public/Dto/SourceTransactionDto.php
@Modules/Ingestion/Public/Services/HeaderSniffer.php
@Modules/Ingestion/Public/Services/SourceAdapterRegistry.php
@Modules/Ingestion/Internal/Adapters/Paypal/PaypalCsvAdapter.php
@Modules/Import/Internal/Http/Livewire/UploadWizard.php
@Modules/Import/Resources/views/livewire/upload-wizard.blade.php
@Modules/Import/Internal/Pipeline/ImportPipeline.php
@Modules/Import/Internal/Pipeline/Stages/ParseStage.php
@Modules/Import/Internal/Pipeline/Stages/FingerprintStage.php
@Modules/Import/Public/Services/SourceRefRanker.php
@Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php
@Modules/Import/Public/Actions/ApplyEnrichments.php
@Modules/EmailScan/Public/Services/InboxMessageQuery.php
@Modules/EmailScan/Public/Dto/InboxMessageDto.php
@Modules/EmailScan/Public/Services/EmlBlobStore.php
@Modules/EmailScan/Internal/MimeHeaderParser.php
@Modules/EmailScan/Internal/Jobs/IncrementalScanJob.php
@Modules/Chains/Internal/Jobs/ResolveChainLinksJob.php
@tests/Contracts/BoundaryArchTest.php
@tests/Contracts/IdempotencyContractTest.php
@Modules/Ingestion/tests/fixtures/paypal/paypal-sample-1.csv

<interfaces>
SourceAdapter contract already locked: format, parse, statementMetadata.

SourceTransactionDto required fields when bridged from ParsedReceiptDto:
  bookedAt = postedAt = valueDate = ParsedReceiptDto.bookedAt
  ownIban = 'PAYPAL'
  counterpartyIban = null
  counterpartyName = ParsedReceiptDto.merchantName
  currency = ParsedReceiptDto.currency
  amountMinor = ParsedReceiptDto.amountMinor
  sourceRef = ParsedReceiptDto.referenceId (PayPal Transaction ID, 17-char alphanumeric)
  description = ParsedReceiptDto.description
  rawPayload = ParsedReceiptDto.rawPayload (carries matcher_key for audit)
  sourceRowIndex = 0
  settledAmountMinor, settledCurrency = optional FX legs
  fxRateUsed = null (derived by NormalizeStage)

InboxMessageQuery.forStatus('fetched') yields a Generator of InboxMessageDto (Phase 6 locked).

UploadWizard.SUPPORTED_FORMATS extends with 'eml' + 'mbox'; issuer enum extends with 'email-file'.

SourceRefRanker rank table extension this plan adds 'paypal-receipt' = rank('paypal-csv') + 1. The 'ics-receipt' / 'google-play-receipt' entries land in plan 03 alongside their matchers.

ParseStage routing for sourceFormat in {'eml','mbox'} branches into the receipt path. For 'eml': RecordReceipt invoked once on the file bytes returning MatchOutcomeDto, then ReceiptSourceAdapter converts to SourceTransactionDto, then yield. For 'mbox': MboxIterator iterates the file, each .eml goes through the same path, yielding one SourceTransactionDto per parsed message.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> wizard upload | Untrusted file content |
| inbox_messages -> matcher | API-fetched (DKIM verified server-side) but still apply suffix match |
| file_imports -> matcher | User-dropped file; strict domain match prevents arbitrary newsletter mis-parsing |
| matcher -> ledger | Internal; fingerprint contract enforces dedup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Spoofing | PaypalReceiptMatcher.canHandle on 'From: service@paypal.com.attacker.example' | mitigate | Exact suffix-match on the email-domain part via substr(strrchr($from, '@'), 1) === 'paypal.com'; NEVER str_contains. Wave-1 fixture corpus includes one spoofed-look-alike negative case asserted by Pest. |
| T-07-02 | Tampering | File-path traversal via wizard upload original_filename | mitigate | UploadWizard.sanitiseFilename already extant; extend match arm for 'eml' and 'mbox'. Deterministic storage path uses sha256(bytes) per D-705a — never the user-supplied filename. FileDropEmlBlobStore.pathFor enforces the [A-Za-z0-9._-]{1,200} regex from Wave 0. |
| T-07-09 | Information Disclosure | Cross-user data leak in ProcessFetchedInboxMessagesJob | mitigate | Job constructor takes int $userId (per-user keyed via uniqueId); handle() loads User::query()->where('id', $this->userId)->firstOrFail() and scopes every read; InboxMessageQuery rows whose userId mismatches are filtered out in the handler. FileImportQuery scopes by user_id on every read. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 0: Promote EmlBlobStore from Modules/EmailScan/Internal/ to Modules/EmailScan/Public/Services/ (precondition for Task 3 cross-module DI)</name>
  <files>
    Modules/EmailScan/Public/Services/EmlBlobStore.php,
    Modules/EmailScan/Internal/EmlBlobStore.php,
    Modules/EmailScan/Providers/EmailScanServiceProvider.php,
    Modules/EmailScan/Internal/Jobs/IncrementalScanJob.php,
    Modules/EmailScan/Internal/Jobs/BackfillInboxJob.php,
    Modules/EmailScan/tests/Integration/EmlOrphanCleanupTest.php,
    Modules/EmailScan/tests/Integration/BackfillChunkedJobTest.php,
    Modules/EmailScan/tests/Integration/ResumeFromCursorTest.php,
    Modules/EmailScan/tests/Integration/ConcurrentBackfillTest.php,
    Modules/EmailScan/tests/Integration/BackfillGraphTest.php
  </files>
  <read_first>
    Modules/EmailScan/Internal/EmlBlobStore.php (current implementation — pathFor + put + delete + exists),
    Modules/EmailScan/Providers/EmailScanServiceProvider.php (singleton-bind site),
    Modules/EmailScan/Internal/Jobs/IncrementalScanJob.php (consumer via constructor + handle DI),
    Modules/EmailScan/Internal/Jobs/BackfillInboxJob.php (consumer via constructor + handle DI),
    Modules/EmailScan/tests/Integration/EmlOrphanCleanupTest.php (test consumer via $this-&gt;app-&gt;make),
    Modules/EmailScan/tests/Integration/BackfillChunkedJobTest.php,
    Modules/EmailScan/tests/Integration/ResumeFromCursorTest.php,
    Modules/EmailScan/tests/Integration/ConcurrentBackfillTest.php,
    Modules/EmailScan/tests/Integration/BackfillGraphTest.php,
    tests/Contracts/BoundaryArchTest.php (the noInternalCrossModule arch rule that the promotion satisfies)
  </read_first>
  <action>
ProcessFetchedInboxMessagesJob (Task 3) needs to read raw .eml bytes from the per-message blob store written by Phase 6's IncrementalScanJob + BackfillInboxJob. The blob store currently lives at Modules/EmailScan/Internal/EmlBlobStore.php. Cross-module DI from Modules/Receipts/Internal/Jobs/ into Modules/EmailScan/Internal/ violates the BoundaryArchTest arch rule "Modules\EmailScan\Internal is only used inside Modules\EmailScan" (tests/Contracts/BoundaryArchTest.php lines 36-38). Per the W3 fix from checker feedback, we PROMOTE the class to Public/Services (simpler than wrapping with a Public facade — EmlBlobStore is already a thin shape with no inner private surface that needs hiding).

Steps:
1. Create Modules/EmailScan/Public/Services/EmlBlobStore.php — copy the current Modules/EmailScan/Internal/EmlBlobStore.php verbatim and change the namespace from `Modules\EmailScan\Internal` to `Modules\EmailScan\Public\Services`. Preserve every const (MESSAGE_ID_PATTERN, DIR_MODE, FILE_MODE) + every method (pathFor, put, delete, exists) + every docblock + every imported symbol. Class is `final` with the same `final readonly`-equivalent constructor DI of Filesystem.
2. Delete Modules/EmailScan/Internal/EmlBlobStore.php (the Internal copy is gone — only the Public class exists).
3. Modify Modules/EmailScan/Providers/EmailScanServiceProvider.php — swap the import + singleton-bind FQN from `Modules\EmailScan\Internal\EmlBlobStore` to `Modules\EmailScan\Public\Services\EmlBlobStore`.
4. Modify Modules/EmailScan/Internal/Jobs/IncrementalScanJob.php — swap all 4 occurrences of the import + handle() type-hint + property type from Internal to Public\Services. Lines from grep: 24, 155, 287, 381.
5. Modify Modules/EmailScan/Internal/Jobs/BackfillInboxJob.php — swap all 5 occurrences. Lines from grep: 25, 146, 247, 335, 480.
6. Modify each of the 5 Phase 6 integration tests (EmlOrphanCleanupTest, BackfillChunkedJobTest, ResumeFromCursorTest, ConcurrentBackfillTest, BackfillGraphTest) — swap the import + the `$this-&gt;app-&gt;make(EmlBlobStore::class)` call (the FQN change carries through the import alias).

The move is purely a namespace + path change. Constructor signature, public API, behavior, on-disk layout — all unchanged. No new tests required: the existing Phase 6 Integration tests still exercise the class via its new location.

NO PHPDoc references to "promoted in Phase 7" or D-numbers (per project memory feedback_docs_describe_current_state.md — docs describe current state, never history). The docblock simply documents what the class does.
  </action>
  <acceptance_criteria>
    - test -f Modules/EmailScan/Public/Services/EmlBlobStore.php
    - ! test -f Modules/EmailScan/Internal/EmlBlobStore.php (Internal copy removed)
    - grep -q "namespace Modules\\\\EmailScan\\\\Public\\\\Services" Modules/EmailScan/Public/Services/EmlBlobStore.php
    - grep -q "final class EmlBlobStore" Modules/EmailScan/Public/Services/EmlBlobStore.php
    - grep -q "Modules\\\\EmailScan\\\\Public\\\\Services\\\\EmlBlobStore" Modules/EmailScan/Providers/EmailScanServiceProvider.php
    - grep -rn "Modules\\EmailScan\\Internal\\EmlBlobStore" Modules/ tests/ 2&gt;/dev/null | wc -l | tr -d "[:space:]" equals 0 (no stale Internal references anywhere)
    - ./vendor/bin/pest --filter='EmlOrphanCleanupTest|BackfillChunkedJobTest|ResumeFromCursorTest|ConcurrentBackfillTest|BackfillGraphTest|BoundaryArchTest' --parallel exits 0 (Phase 6 integration tests still green; arch test still green)
    - ./vendor/bin/phpstan analyse Modules/EmailScan --memory-limit=2G --no-progress exits 0
    - ./vendor/bin/pint --test Modules/EmailScan exits 0
  </acceptance_criteria>
  <validation_axis>Axis 8 — Boundary invariants (Internal-only-within-module arch rule still green AFTER the promotion; future Task 3 DI from Modules/Receipts/ into the Public surface stays legal).</validation_axis>
  <verify>
    <automated>./vendor/bin/pest --filter='EmlOrphanCleanupTest|BackfillChunkedJobTest|ResumeFromCursorTest|ConcurrentBackfillTest|BackfillGraphTest|BoundaryArchTest' --parallel &amp;&amp; ./vendor/bin/phpstan analyse Modules/EmailScan --memory-limit=2G --no-progress</automated>
  </verify>
  <done>EmlBlobStore lives at Modules/EmailScan/Public/Services/EmlBlobStore.php; Internal copy removed; 7 callers updated; Phase 6 integration tests + arch tests green; phpstan + pint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: PaypalReceiptMatcher + MatcherRegistry + ReceiptSourceAdapter + SourceRefRanker + fixtures + FingerprintParityTest GREEN</name>
  <files>
    Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php,
    Modules/Receipts/Internal/MatcherRegistry.php,
    Modules/Receipts/Internal/Pipeline/ReceiptSourceAdapter.php,
    Modules/Receipts/Providers/ReceiptsServiceProvider.php,
    Modules/Import/Public/Services/SourceRefRanker.php,
    Modules/Receipts/tests/fixtures/paypal/current-receipt.eml,
    Modules/Receipts/tests/fixtures/paypal/prior-generation-receipt.eml,
    Modules/Receipts/tests/fixtures/paypal/login-notification.eml,
    Modules/Receipts/tests/fixtures/paypal/spoofed-sender.eml,
    Modules/Receipts/tests/fixtures/paypal/foreign-currency-receipt.eml,
    Modules/Receipts/tests/fixtures/paypal/malformed-date-receipt.eml,
    Modules/Receipts/tests/fixtures/paypal/paired-csv-row.csv,
    Modules/Receipts/tests/Unit/Matchers/PaypalReceiptMatcherTest.php,
    Modules/Receipts/tests/Contracts/FingerprintParityTest.php
  </files>
  <read_first>
    Modules/Receipts/Internal/Pipeline/EmlMimeReader.php,
    Modules/Receipts/Public/Contracts/SenderMatcher.php,
    Modules/Receipts/Public/Dto/ParsedReceiptDto.php,
    Modules/Receipts/Public/Dto/MatchOutcomeDto.php,
    Modules/Receipts/Providers/ReceiptsServiceProvider.php,
    Modules/Ingestion/Public/Dto/SourceTransactionDto.php,
    Modules/Ingestion/Public/Services/SourceAdapterRegistry.php,
    Modules/Ingestion/Internal/Adapters/Paypal/PaypalCsvAdapter.php,
    Modules/Ingestion/tests/Unit/Adapters/Paypal/PaypalCsvAdapterTest.php,
    Modules/EmailScan/Internal/MimeHeaderParser.php,
    Modules/Import/Internal/Pipeline/Stages/NormalizeStage.php,
    Modules/Import/Public/Services/SourceRefRanker.php,
    Modules/Ledger/Public/Services/FingerprintComposer.php,
    Modules/Ingestion/tests/fixtures/paypal/paypal-sample-1.csv,
    .planning/phases/07-email-template-matchers-categorization-learning/07-RESEARCH.md (Per-Sender Matcher Patterns - PayPal lines 916-1010; Pattern 2 fingerprint parity lines 433-450; Code Examples - brick/money parsing lines 810-823),
    .planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md (Wave 1 PaypalReceiptMatcher excerpt lines 328-359; ReceiptSourceAdapter excerpt lines 366-388; MatcherRegistry excerpt lines 394-414)
  </read_first>
  <behavior>
    - Test: PaypalReceiptMatcher.canHandle returns true for senderEmail 'service@paypal.com'.
    - Test: PaypalReceiptMatcher.canHandle returns FALSE for senderEmail 'service@paypal.com.attacker.example' (T-07-04 — suffix match, not contains).
    - Test: PaypalReceiptMatcher.canHandle returns FALSE for senderEmail 'notifications@netflix.com'.
    - Test: PaypalReceiptMatcher.priority() === 100.
    - Test: PaypalReceiptMatcher.key() === 'paypal-receipt'.
    - Test: match(currentPaypalReceiptEml) returns MatchOutcomeDto.parsed with ParsedReceiptDto fields: merchantName matches fixture, amountMinor matches brick/money parse of Dutch comma decimal, currency='EUR', referenceId matches [A-Z0-9]{17}, bookedAt = Date header, ownIban='PAYPAL', rawPayload['matcher_key']='paypal-receipt'.
    - Test: match(priorGenerationPaypalEml) returns MatchOutcomeDto.parsed using fallback anchors.
    - Test: match(loginNotificationEml) returns MatchOutcomeDto.skipped('paypal-login-notification').
    - Test (FX dual-amount): match(foreignCurrencyReceiptEml — body has '$ 12.99 USD' line followed by 'Conversion to EUR: € 11,82') returns MatchOutcomeDto.parsed with ParsedReceiptDto.amountMinor=1299, currency='USD', settledAmountMinor=1182, settledCurrency='EUR'. Mirrors the Phase 3 Google Play FX dual-amount pattern. CONTEXT.md flags multi-currency from day 1 as a hard invariant — receipt parsing must preserve the FX leg, not lose it.
    - Test (FX absence): match(currentPaypalReceiptEml — EUR-only) returns ParsedReceiptDto with settledAmountMinor == amountMinor AND settledCurrency == currency (the SourceTransactionDto contract requires both fields non-null).
    - Test (W11 — malformed Date header): match(malformedDateReceiptEml — body identical to current-receipt.eml but Date header is 'Some-bogus-header value that Carbon cannot parse') returns MatchOutcomeDto.unmatched(reason: 'invalid_date_header'). MUST NOT throw to the caller — the consumer pipeline (ParseStage / ProcessFetchedInboxMessagesJob) treats unmatched as a row-level outcome, not a crash.
    - Test: MatcherRegistry.dispatch with PayPal sender routes to PaypalReceiptMatcher.
    - Test: MatcherRegistry.dispatch with unknown sender returns MatchOutcomeDto.unmatched.
    - Test: ReceiptSourceAdapter.toSourceDto maps every field per the interfaces table; sourceRef == ParsedReceiptDto.referenceId; ownIban == 'PAYPAL'.
    - Test (LOAD-BEARING FingerprintParityTest): given paired-csv-row.csv + current-receipt.eml (same Transaction ID + amount + Date), FingerprintComposer.compose returns IDENTICAL hash for both canonical rows.
    - Test (SourceRefRanker): rank('paypal-receipt') > rank('paypal-csv'); rank('paypal-receipt') < rank('asn-camt053').
  </behavior>
  <action>
Create 5 PayPal fixtures under Modules/Receipts/tests/fixtures/paypal/. Each anonymised (no real PII), byte-deterministic.

  current-receipt.eml: multipart/alternative with text/plain part containing 'Transaction ID: ABCDEFG1234567HJK', amount 'EUR 12,99', merchant line 'Aan: Netflix BV', Date header 'Mon, 17 May 2026 09:42:13 +0200', From 'service@paypal.com', Subject 'Je ontvangstbewijs van Netflix BV', Message-ID '<deterministic-1@paypal.com>'.

  prior-generation-receipt.eml: same shape but Subject 'Receipt for your payment to Netflix BV' (English), body label 'Merchant:' (instead of 'Aan:'). Exercises matcher fallback anchors.

  login-notification.eml: From 'service@paypal.com'; body lacks Transaction ID + amount; Subject 'New device sign-in'. Matcher returns skipped.

  spoofed-sender.eml: From 'service@paypal.com.attacker.example'; otherwise identical body to current-receipt.eml. canHandle returns FALSE.

  paired-csv-row.csv: one-row PayPal Activity Download CSV (Dutch locale per Phase 4 D-58) whose Transaction ID, amount, currency, Date, Naam columns match current-receipt.eml exactly. This is the load-bearing fingerprint-parity input.

  foreign-currency-receipt.eml: From 'service@paypal.com'; multipart/alternative with text/plain body containing two amount lines — a native '$ 12.99 USD' line followed by 'Conversion to EUR: € 11,82' on the next line. Same Transaction ID format + merchant line as current-receipt.eml. Exercises the FX-leg extraction.

  malformed-date-receipt.eml: From 'service@paypal.com'; body identical to current-receipt.eml; Date header is set to a deliberately unparseable string (e.g. 'totally-not-a-date'). Exercises the Date try/catch fallback.

Create Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php per the PATTERNS.md Wave 1 excerpt (lines 328-359):

  final class implements SenderMatcher.
  constructor: private readonly EmlMimeReader $reader.
  key returns 'paypal-receipt'. priority returns 100.
  canHandle(InboxMessageDto $msg): extract email-domain via substr(strrchr($msg->senderEmail, '@'), 1); return $domain === 'paypal.com'. Return false early on empty.
  match(string $emlRaw):
    1) $parsed = $this->reader->read($emlRaw).
    2) $body = $parsed->textBody ?? $parsed->htmlBody (Pitfall 2 prefers text/plain).
    3) Detect login-notification subjects via case-insensitive regex against $parsed->headers['Subject']; return MatchOutcomeDto.skipped('paypal-login-notification').
    4) Extract Transaction ID via /Transaction ID:\s*([A-Z0-9]{17})/i; if missing return MatchOutcomeDto.unmatched.
    5) Extract amount + currency. PayPal can charge a non-EUR amount and settle it to the EUR account in the same receipt (e.g. an Etsy/US-store transaction). Capture BOTH legs when present, mirroring the Phase 3 Google Play FX dual-amount handling and the existing PayPal CSV adapter (which preserves both Gross + Net + Currency-conversion-from + amount):
       a) Native amount: try in this order /EUR\s*([0-9.,]+)/i (Dutch PayPal default for EUR-only transactions), then /USD\s*([0-9.,]+)/i, then label-based fallback /(Bedrag|Amount):\s*([A-Z]{3})?\s*([0-9.,]+)/i (capture the optional currency tag). Normalise Dutch comma decimal via str_replace then Brick\Money\Money::of with RoundingMode::HALF_UP -> getMinorAmount -> toInt. The detected currency becomes ParsedReceiptDto.currency; the integer minor amount becomes ParsedReceiptDto.amountMinor.
       b) Settled-leg detection: if the body ALSO contains a second amount line in a different currency (typical pattern: native USD line above + 'Conversion to EUR: € 11,82' or 'Wisselkoers: 1 USD = 0,93 EUR' or a labelled '(€ 11,82 EUR)' parenthetical), parse the second amount with brick/money and populate ParsedReceiptDto.settledAmountMinor + settledCurrency. Regex hints: /Conversion to ([A-Z]{3}):?\s*[€$]?\s*([0-9.,]+)/i, /Wisselkoers:.*?=\s*([0-9.,]+)\s*([A-Z]{3})/i, /\(\s*([€$£])\s*([0-9.,]+)\s*([A-Z]{3})\s*\)/.
       c) When no second-currency leg is present (the common Dutch EUR-only case), settledAmountMinor and settledCurrency are left as the same value as amountMinor + currency (the SourceTransactionDto contract requires both fields to be non-null integers). ReceiptSourceAdapter.toSourceDto MUST be updated accordingly: pass ParsedReceiptDto.settledAmountMinor ?? amountMinor and settledCurrency ?? currency.
    6) Extract merchant via /(Aan|Merchant|To|Paid to):\s*(.+)/im trimmed; missing -> unmatched.
    7) bookedAt: parse $parsed->headers['Date'] via CarbonImmutable inside a try/catch (catch \Throwable). PayPal occasionally sends malformed Date headers (RFC 822 timezone-name leakage, missing day-of-week) that throw Carbon parse exceptions and crash the matcher consumer. On failure return MatchOutcomeDto::unmatched(reason: 'invalid_date_header') — the row stays in inbox_messages.status='unmatched' for the user to triage later.
    8) Build ParsedReceiptDto: ownIban='PAYPAL', counterpartyIban=null, rawPayload=['matcher_key'=>'paypal-receipt','transaction_id'=>$txnId,'subject'=>$subject,'sender'=>$from,'body_excerpt'=>substr($body,0,200)].
    9) Return MatchOutcomeDto.parsed($dto).

Create Modules/Receipts/Internal/MatcherRegistry.php per PATTERNS.md excerpt lines 394-414. final class. constructor takes list<SenderMatcher> sorted DESC by priority (the ServiceProvider closure does the usort). dispatch(MatcherInputDto $input) iterates; first one whose canHandle is true gets match(); else MatchOutcomeDto.unmatched. supportedKeys returns the matcher key list (used in plan 05 by the /inboxes Matcher column).

Create Modules/Receipts/Internal/Pipeline/ReceiptSourceAdapter.php per PATTERNS.md excerpt lines 366-388. final class; method toSourceDto(ParsedReceiptDto $parsed, int $sourceRowIndex = 0): SourceTransactionDto applying every field mapping per the interfaces table. Stateless, singleton-safe.

Modify Modules/Receipts/Providers/ReceiptsServiceProvider.php: drop the Wave 0 class_exists guard around PaypalReceiptMatcher in the tag() list (the class is now real). Keep guards around IcsReceiptMatcher + GooglePlayReceiptMatcher (land plan 03). Confirm MatcherRegistry, EmlMimeReader, MboxIterator, ReceiptSourceAdapter, FileDropEmlBlobStore are bound as singletons. No arch carve-outs in this task (the Cache facade carve-out for ProcessFetchedInboxMessagesJob lands in this plan's Task 3 alongside the job class).

Modify Modules/Import/Public/Services/SourceRefRanker.php: add 'paypal-receipt' to the rank map at rank('paypal-csv') + 1. Do NOT add 'ics-receipt' or 'google-play-receipt' (plan 03). Extend the existing SourceRefRanker Pest test (or create one if absent) asserting the new ordering.

Update Modules/Receipts/tests/Contracts/FingerprintParityTest.php (replace the Wave 0 skip for the 'paypal' row): run the full assertion path — load paired-csv-row.csv via PaypalCsvAdapter.parse, take the first SourceTransactionDto; load current-receipt.eml bytes, run PaypalReceiptMatcher.match, take ParsedReceiptDto, convert via ReceiptSourceAdapter.toSourceDto; push both through NormalizeStage; assert FingerprintComposer.compose returns identical SHA-256 hashes. Rows 'ics' and 'google-play' still skip.

Create Modules/Receipts/tests/Unit/Matchers/PaypalReceiptMatcherTest.php covering every behavior case. RefreshDatabase only used where DB needed (canHandle / match are pure functions).
  </action>
  <acceptance_criteria>
    - grep -q "implements SenderMatcher" Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php
    - grep -qE "(strrchr|substr).*paypal\\.com" Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php (suffix/exact domain extraction)
    - grep -v '^[[:space:]]*//' Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php | grep -cF 'str_contains' equals 0 (no contains-based domain matching anywhere — T-07-04)
    - grep -q "Brick\\\\Money\\\\Money" Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php
    - grep -q "'paypal-receipt'" Modules/Import/Public/Services/SourceRefRanker.php
    - test -f Modules/Receipts/tests/fixtures/paypal/current-receipt.eml && test -f Modules/Receipts/tests/fixtures/paypal/paired-csv-row.csv && test -f Modules/Receipts/tests/fixtures/paypal/spoofed-sender.eml
    - test -f Modules/Receipts/tests/fixtures/paypal/foreign-currency-receipt.eml && test -f Modules/Receipts/tests/fixtures/paypal/malformed-date-receipt.eml
    - grep -qE 'settledAmountMinor|settled_amount' Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php (FX dual-amount handling present)
    - grep -qE 'try\s*\{|catch\s*\(\s*\\?Throwable' Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php (Date try/catch fallback present)
    - grep -q 'invalid_date_header' Modules/Receipts/Internal/Matchers/PaypalReceiptMatcher.php (W11 fallback reason)
    - ./vendor/bin/pest --filter=PaypalReceiptMatcherTest --parallel exits 0 (all behavior cases green incl. spoofed-from negative)
    - ./vendor/bin/pest --filter=FingerprintParityTest --parallel exits 0; the 'paypal' dataset row passes (not skipped) — confirm via test output containing 'paypal' name and zero skipped count for paypal
    - ./vendor/bin/pest --filter=SourceRefRanker --parallel exits 0
    - ./vendor/bin/phpstan analyse Modules/Receipts Modules/Import/Public/Services/SourceRefRanker.php --memory-limit=2G --no-progress exits 0
    - ./vendor/bin/pint --test Modules/Receipts Modules/Import/Public/Services/SourceRefRanker.php exits 0
  </acceptance_criteria>
  <validation_axis>Axis 1 — Header-shape parser correctness (canHandle suffix match defeats spoofing). Axis 3 — DTO equivalence / cross-format fingerprint parity (PayPal receipt equals PayPal CSV at FingerprintComposer level).</validation_axis>
  <verify>
    <automated>./vendor/bin/pest --filter='PaypalReceiptMatcherTest|FingerprintParityTest|SourceRefRanker' --parallel &amp;&amp; ./vendor/bin/phpstan analyse Modules/Receipts Modules/Import/Public/Services/SourceRefRanker.php --memory-limit=2G --no-progress</automated>
  </verify>
  <done>PaypalReceiptMatcher + MatcherRegistry + ReceiptSourceAdapter shipped; 5 PayPal fixtures committed; FingerprintParityTest GREEN for paypal row; SourceRefRanker extended; phpstan max + pint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: HeaderSniffer + EmlHeaderProfile + MboxHeaderProfile + UploadWizard validation extension (Wizard frontend)</name>
  <files>
    Modules/Ingestion/Public/Services/HeaderSniffer.php,
    Modules/Receipts/Internal/Pipeline/EmlHeaderProfile.php,
    Modules/Receipts/Internal/Pipeline/MboxHeaderProfile.php,
    Modules/Import/Internal/Http/Livewire/UploadWizard.php,
    Modules/Import/Resources/views/livewire/upload-wizard.blade.php
  </files>
  <read_first>
    Modules/Ingestion/Public/Services/HeaderSniffer.php (lines 26-160 — sniff branches),
    Modules/Ingestion/Public/Dto/SniffResult.php,
    Modules/Ingestion/Public/Exceptions/SniffMismatchException.php,
    Modules/Ingestion/Internal/Adapters/Asn/AsnCsvHeaderProfile.php (FORMAT const pattern),
    Modules/Import/Internal/Http/Livewire/UploadWizard.php (SUPPORTED_FORMATS, rules, messages, availableFormats, updatedIssuer, submit, sanitiseFilename),
    Modules/Import/Resources/views/livewire/upload-wizard.blade.php,
    .planning/phases/07-email-template-matchers-categorization-learning/07-UI-SPEC.md (Section 4 + Copywriting Contract /imports wizard arm),
    .planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md (Wave 1 HeaderSniffer arms excerpt lines 471-502)
  </read_first>
  <behavior>
    - Test: HeaderSniffer.sniff('something.eml','eml') returns SniffResult(format='eml') when head contains 'Return-Path:' OR 'Received:' OR 'From:' OR 'Message-ID:'.
    - Test: HeaderSniffer.sniff('foo.txt','eml') throws SniffMismatchException with the locked copy "That file doesn't look like an email message. Drop in a .eml file."
    - Test: HeaderSniffer.sniff('something.mbox','mbox') returns SniffResult(format='mbox') when head starts with 'From '.
    - Test: HeaderSniffer.sniff('something.mbox','mbox') without 'From ' prefix throws SniffMismatchException with locked copy.
    - Test: UploadWizard.availableFormats('email-file') returns the 2-row array [{value:'eml',label:'Email message (.eml)'},{value:'mbox',label:'Mailbox archive (.mbox)'}] per UI-SPEC.
    - Test: UploadWizard validation accepts issuer='email-file' + sourceFormat='eml' + a fake .eml file under 20MB; rejects sourceFormat='asn-csv' when issuer='email-file'.
    - Test: UploadWizard validation enforces extensions: (NOT mimes:) for .eml + .mbox — a file with extension .eml passes regardless of detected MIME type; a file with extension .pdf fails the .eml/.mbox arm.
  </behavior>
  <action>
Modify Modules/Ingestion/Public/Services/HeaderSniffer.php: add two new arms to the match block. Create the two new HeaderProfile classes in Modules/Receipts/Internal/Pipeline/ (EmlHeaderProfile + MboxHeaderProfile each with a FORMAT const 'eml' / 'mbox' and a discriminator regex/constant) — mirror Modules/Ingestion/Internal/Adapters/Asn/AsnCsvHeaderProfile.php.

The sniffEml private method (PATTERNS.md lines 476-490): assert preg_match for /\.eml$/i on the path; reject otherwise with the locked copy from UI-SPEC ("That file doesn't look like an email message. Drop in a .eml file."). Then assert preg_match for ^(Return-Path|Received|From|Message-ID): on $head; reject otherwise. Return SniffResult(format='eml', delimiter='', hasHeader=false, encoding='UTF-8', columnCount=0).

The sniffMbox private method (PATTERNS.md lines 492-501): assert preg_match for /\.mbox$/i on path; reject otherwise. Assert $head starts with 'From '; reject otherwise with the locked copy. Return SniffResult(format='mbox', ...).

Add the new arms to HeaderSniffer.sniff's match block keyed on EmlHeaderProfile::FORMAT and MboxHeaderProfile::FORMAT.

Modify Modules/Import/Internal/Http/Livewire/UploadWizard.php:
  - Extend SUPPORTED_FORMATS const to include 'eml' and 'mbox'.
  - Extend issuer validation rule (#[Validate]) to accept 'email-file' alongside 'asn'/'ics'/'paypal'.
  - Extend sourceFormat validation rule (rules()) to accept 'eml','mbox'.
  - Extend availableFormats() match: 'email-file' returns the 2 entries per UI-SPEC copy contract (Section 4).
  - Extend file validation: max:1048576 (1 GB) when sourceFormat='mbox' (per UI-SPEC + RESEARCH Pitfall 6); 20480 (20 MB) when sourceFormat='eml'; keep 10240 default for other formats. Use dynamic rules via rules() returning conditional based on $this->sourceFormat. Replace the existing mimes-rule with an extensions-rule for the .eml/.mbox arms. Laravel resolves mimes: to detected MIME types via the mimetypes config — .eml and .mbox have no native MIME registration, so mimes: silently rejects every upload of those types. Use extensions: csv,txt,xml,sta,mt940,940,pdf,eml,mbox,zip (Laravel 9+ accepts files by file extension regardless of detected MIME). When sourceFormat in {eml,mbox} the rules() method MUST emit "extensions:csv,txt,xml,sta,mt940,940,pdf,eml,mbox,zip". Existing csv/pdf/sta/mt940/940/xml paths continue to work because extensions: covers them too.
  - Extend sanitiseFilename match arm to handle 'eml' => '.eml', 'mbox' => '.mbox'.
  - Subheading update per UI-SPEC: "Drop in an ASN, ICS, PayPal export, or an email receipt file." (the Blade view modification).

Modify Modules/Import/Resources/views/livewire/upload-wizard.blade.php:
  - Add the new <option value="email-file">Email file (.eml, .mbox)</option> to the issuer <select>.
  - Update the <input type="file" accept="..."> accept attr to add `,.eml,.mbox,.zip`.
  - Update the subheading literal per UI-SPEC copy.

NO PHPDoc references to D-numbers / .planning / REQ-IDs anywhere — plain technical prose only.
  </action>
  <acceptance_criteria>
    - grep -q "sniffEml\|EmlHeaderProfile::FORMAT" Modules/Ingestion/Public/Services/HeaderSniffer.php
    - grep -q "sniffMbox\|MboxHeaderProfile::FORMAT" Modules/Ingestion/Public/Services/HeaderSniffer.php
    - grep -q "FORMAT = 'eml'" Modules/Receipts/Internal/Pipeline/EmlHeaderProfile.php
    - grep -q "FORMAT = 'mbox'" Modules/Receipts/Internal/Pipeline/MboxHeaderProfile.php
    - grep -q "'eml'\|'mbox'" Modules/Import/Internal/Http/Livewire/UploadWizard.php (SUPPORTED_FORMATS extended)
    - grep -q "email-file" Modules/Import/Internal/Http/Livewire/UploadWizard.php
    - grep -q "email-file" Modules/Import/Resources/views/livewire/upload-wizard.blade.php
    - grep -q "Drop in an ASN, ICS, PayPal export, or an email receipt file" Modules/Import/Resources/views/livewire/upload-wizard.blade.php (UI-SPEC copy verbatim)
    - grep -qE "extensions:[^\"]*eml[^\"]*mbox|extensions:[^\"]*mbox[^\"]*eml" Modules/Import/Internal/Http/Livewire/UploadWizard.php (extensions:-rule covers .eml AND .mbox; the mimes: rule would silently reject both because Laravel mimes resolves to detected MIME types and .eml/.mbox have no native registration)
    - grep -v "^[[:space:]]*//" Modules/Import/Internal/Http/Livewire/UploadWizard.php | grep -cE "mimes:[^\"]*eml|mimes:[^\"]*mbox" | tr -d "[:space:]" equals 0 (no mimes-rule on .eml/.mbox; must use extensions-rule)
    - ./vendor/bin/pest --filter='HeaderSniffer|UploadWizard' --parallel exits 0
    - ./vendor/bin/phpstan analyse Modules/Ingestion Modules/Import Modules/Receipts/Internal/Pipeline --memory-limit=2G --no-progress exits 0
    - ./vendor/bin/pint --test Modules/Ingestion Modules/Import Modules/Receipts/Internal/Pipeline exits 0
  </acceptance_criteria>
  <validation_axis>Axis 2 — Body decoding correctness (HeaderSniffer routes .eml/.mbox into the receipt path). Axis 7 — Pipeline integration boundary (Wizard validation accepts the new format extensions WITHOUT regressing csv/pdf/sta/mt940/940/xml).</validation_axis>
  <verify>
    <automated>./vendor/bin/pest --filter='HeaderSniffer|UploadWizard' --parallel &amp;&amp; ./vendor/bin/phpstan analyse Modules/Ingestion Modules/Import Modules/Receipts/Internal/Pipeline --memory-limit=2G --no-progress</automated>
  </verify>
  <done>HeaderSniffer .eml/.mbox arms shipped; UploadWizard validation extended to extensions:csv,txt,xml,sta,mt940,940,pdf,eml,mbox,zip; subheading + accept attribute updated per UI-SPEC verbatim; arms compile and validate without regressing the Phase 1-6 csv/pdf branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: ParseStage routing + RecordReceipt + FileImportQuery + WizardEmailFileStep + IdempotencyContractTest dataset (Wizard backend)</name>
  <files>
    Modules/Import/Internal/Pipeline/Stages/ParseStage.php,
    Modules/Receipts/Internal/Http/Livewire/WizardEmailFileStep.php,
    Modules/Receipts/Resources/views/livewire/wizard-email-file-step.blade.php,
    Modules/Receipts/Public/Actions/RecordReceipt.php,
    Modules/Receipts/Public/Services/FileImportQuery.php,
    Modules/Receipts/Providers/ReceiptsServiceProvider.php,
    Modules/Receipts/tests/Feature/EmlFileDropTest.php,
    Modules/Receipts/tests/Feature/MboxFileDropTest.php,
    tests/Contracts/IdempotencyContractTest.php
  </files>
  <read_first>
    Modules/Import/Internal/Pipeline/ImportPipeline.php (preview loop),
    Modules/Import/Internal/Pipeline/Stages/ParseStage.php,
    Modules/Receipts/Internal/Pipeline/EmlMimeReader.php,
    Modules/Receipts/Internal/Pipeline/MboxIterator.php,
    Modules/Receipts/Internal/Pipeline/FileDropEmlBlobStore.php,
    Modules/Receipts/Internal/Pipeline/ReceiptSourceAdapter.php,
    Modules/Receipts/Internal/MatcherRegistry.php,
    Modules/Receipts/Public/Dto/MatcherInputDto.php,
    Modules/Receipts/tests/fixtures/paypal/current-receipt.eml,
    Modules/Receipts/tests/fixtures/mbox/small.mbox,
    tests/Contracts/IdempotencyContractTest.php,
    .planning/phases/07-email-template-matchers-categorization-learning/07-UI-SPEC.md (Section 4 + Copywriting Contract /imports wizard arm),
    .planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md (WizardEmailFileStep notes lines 461-468)
  </read_first>
  <behavior>
    - Test (Feature EmlFileDropTest): Livewire-test the wizard: upload current-receipt.eml as issuer=email-file/format=eml, submit, follow redirect; assert one file_imports row created with status='parsed', matcher_key='paypal-receipt', sender_email='service@paypal.com'; assert one transactions row created with correct counterparty + amount + sourceRef.
    - Test (Feature EmlFileDropTest idempotency): drop the same .eml twice; second drop creates ZERO new file_imports rows and ZERO new transactions rows (UNIQUE constraint on (user_id, provider_message_id) + fingerprint dedup).
    - Test (Feature MboxFileDropTest): drop small.mbox (5 messages, fixtures already exist); assert 5 file_imports rows; PayPal-recognised messages become transactions; others land status='unmatched'.
    - Test (IdempotencyContractTest dataset): new dataset row 'paypal-receipt-eml' with adapterFormat='eml', fixture=current-receipt.eml, overlapBase=current-receipt.eml, overlapNext=current-receipt.eml. The shared idempotency it() block asserts inserted > 0 on first, inserted == 0 on second.
  </behavior>
  <action>
Modify Modules/Import/Internal/Pipeline/Stages/ParseStage.php: when sourceFormat in {'eml','mbox'}, branch into the receipt path instead of SourceAdapterRegistry.for($format)->parse(). Inject MatcherRegistry + EmlMimeReader + MboxIterator + ReceiptSourceAdapter + FileDropEmlBlobStore + DatabaseManager + Clock via constructor (use a final readonly class). Algorithm:
  - For 'eml': read the file bytes; compute Message-ID via EmlMimeReader.read; if header missing use sha256(bytes) (D-705a); UPSERT a file_imports row (user_id, source_kind='eml', source_filename, provider_message_id, internal_date, sender_email, sender_name, subject, eml_path, status='fetched', fetched_at, timestamps); call FileDropEmlBlobStore.put($eml_path, $bytes); construct MatcherInputDto from the row; dispatch via MatcherRegistry; on parsed -> ReceiptSourceAdapter.toSourceDto -> yield once + UPDATE file_imports.status='parsed' + matcher_key=$matcher->key(); on skipped -> UPDATE status='skipped' + yield nothing; on unmatched -> UPDATE status='unmatched' + yield nothing.
  - For 'mbox': MboxIterator.iterate the path; per-message run the 'eml' branch above (the per-message Message-ID UNIQUE constraint handles idempotency row-by-row per D-705).
  - Wrap each per-row UPSERT + status transition in a DB transaction.
  - Keep the existing 'asn-csv'/'asn-camt053'/etc. branches untouched.

Create Modules/Receipts/Public/Actions/RecordReceipt.php — final class, constructor DI of EmlMimeReader + MatcherRegistry + FileDropEmlBlobStore + DatabaseManager + Clock + Dispatcher. Single __invoke($emlBytes, User $user, ?string $sourceFilename, ?int $importRunId): MatchOutcomeDto. Used by ParseStage (the wizard path) AND by ProcessFetchedInboxMessagesJob (the inbox-handoff path) — single source of truth so the two consumers cannot drift.

Create Modules/Receipts/Public/Services/FileImportQuery.php — final readonly class, DI DatabaseManager. Methods:
  - forUser(User $user): array of FileImportDto (define a sister readonly Data) with status counters for the wizard preview row.
  - latestForStatus(User $user, string $status): Generator of FileImportDto (mirrors InboxMessageQuery shape).

Create Modules/Receipts/Internal/Http/Livewire/WizardEmailFileStep.php — Livewire 4 SFC class. Per UI-SPEC Section 4, this is the arm extension to the existing wizard (NOT a standalone page). For Wave 1, the wizard works through UploadWizard's extension (Task 2a) — WizardEmailFileStep is registered with Livewire but stays a thin shell so that future deeper wizard customisation has a home. The Blade view (wizard-email-file-step.blade.php) wraps a minimal panel that defers to UploadWizard's existing chrome. Document this minimalism in the class docblock with plain technical prose (no D-numbers).

Modify Modules/Receipts/Providers/ReceiptsServiceProvider.php:
  - Register Livewire component: 'receipts.wizard-email-file-step' (the toast SFC 'receipts.receipt-conflict-toast' lands plan 04 — do NOT pre-register it here to avoid forward-referenced bindings).
  - Singleton-bind RecordReceipt + FileImportQuery.

Update tests/Contracts/IdempotencyContractTest.php: add a new dataset row keyed 'paypal-receipt-eml' with `adapterFormat => 'eml'`, `fixture => __DIR__.'/../../Modules/Receipts/tests/fixtures/paypal/current-receipt.eml'`, `overlapBase => same`, `overlapNext => same`. The shared it() block already asserts inserted>0 on first run and inserted==0 on second run.

Create Modules/Receipts/tests/Feature/EmlFileDropTest.php + MboxFileDropTest.php using Livewire's testing helpers (Livewire::test(...)). seed fixtureUser + fixture PayPal account (use the existing `TestCase::seedFixtureUserAndAccount` helper — Phase 4 D-80b extended it to include PayPal). Assert behavior per the test list above.

NO PHPDoc references to D-numbers / .planning / REQ-IDs anywhere — plain technical prose only.
  </action>
  <acceptance_criteria>
    - grep -qE "'eml'|'mbox'" Modules/Import/Internal/Pipeline/Stages/ParseStage.php (new routing arms)
    - grep -q "sha256" Modules/Import/Internal/Pipeline/Stages/ParseStage.php (synthetic Message-ID per D-705a — synthetic Message-ID is computed when the header is absent)
    - grep -q "final class RecordReceipt" Modules/Receipts/Public/Actions/RecordReceipt.php
    - grep -q "final readonly class FileImportQuery" Modules/Receipts/Public/Services/FileImportQuery.php OR equivalent (final + readonly modifiers)
    - grep -q "paypal-receipt-eml" tests/Contracts/IdempotencyContractTest.php
    - ./vendor/bin/pest --filter='EmlFileDropTest|MboxFileDropTest|IdempotencyContractTest' --parallel exits 0
    - ./vendor/bin/pest --parallel exits 0 (full suite — confirms no Phase 1-6 regression from the ParseStage extension)
    - ./vendor/bin/phpstan analyse Modules/Receipts Modules/Import --memory-limit=2G --no-progress exits 0
    - ./vendor/bin/pint --test Modules/Receipts Modules/Import exits 0
  </acceptance_criteria>
  <validation_axis>Axis 2 — Body decoding correctness (EmlMimeReader feeds matchers via ParseStage). Axis 6 — Idempotency under re-drop (UNIQUE constraint + sha256 synthetic Message-ID). Axis 7 — Pipeline integration boundary (ParseStage receipt arm slots cleanly without changing ImportPipeline shape).</validation_axis>
  <verify>
    <automated>./vendor/bin/pest --filter='EmlFileDropTest|MboxFileDropTest|IdempotencyContractTest' --parallel &amp;&amp; ./vendor/bin/phpstan analyse Modules/Receipts Modules/Import --memory-limit=2G --no-progress</automated>
  </verify>
  <done>ParseStage routing branch in place for .eml + .mbox; RecordReceipt + FileImportQuery + WizardEmailFileStep SFC shipped; .eml + .mbox file drop yields canonical transactions end-to-end; idempotency contract extended + green; phpstan max + pint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ProcessFetchedInboxMessagesJob consumer + Phase 6 handoff + console schedule + Cache facade carve-out</name>
  <files>
    Modules/Receipts/Internal/Jobs/ProcessFetchedInboxMessagesJob.php,
    Modules/Receipts/Providers/ReceiptsServiceProvider.php,
    Modules/Receipts/tests/Feature/ProcessFetchedInboxMessagesJobTest.php,
    Modules/Receipts/Routes/console.php,
    routes/console.php,
    tests/Contracts/BoundaryArchTest.php
  </files>
  <read_first>
    Modules/EmailScan/Internal/Jobs/IncrementalScanJob.php (queued + ShouldBeUniqueUntilProcessing + Cache facade uniqueVia carve-out),
    Modules/Chains/Internal/Jobs/ResolveChainLinksJob.php (audit-row lifecycle + Cache facade carve-out lines 89-96),
    Modules/EmailScan/Public/Services/InboxMessageQuery.php,
    Modules/EmailScan/Public/Dto/InboxMessageDto.php,
    Modules/EmailScan/Public/Services/EmlBlobStore.php (read pattern — promoted in Task 0),
    Modules/Receipts/Public/Actions/RecordReceipt.php,
    Modules/Receipts/Internal/MatcherRegistry.php,
    Modules/Receipts/Internal/Testing/FakeInboxMessageQuery.php,
    tests/Contracts/BoundaryArchTest.php (Cache facade carve-out list lines 40-60 — currently contains Chains + 3 EmailScan jobs; this task ADDS ProcessFetchedInboxMessagesJob),
    routes/console.php (existing hourly/daily Schedule::call closure DI pattern),
    .planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md (Wave 1 ProcessFetchedInboxMessagesJob excerpt lines 420-456)
  </read_first>
  <behavior>
    - Test: ProcessFetchedInboxMessagesJob constructor takes int $userId and uniqueId returns the string form of userId (Validation Axis 6 + T-07-09).
    - Test: handle() iterates InboxMessageQuery.forStatus('fetched') and filters to rows whose userId === $this->userId (cross-user defense).
    - Test (using FakeInboxMessageQuery): given 3 fetched rows (1 PayPal valid, 1 PayPal login, 1 unknown sender) and on-disk .eml blobs at EmlBlobStore.pathFor(), the job transitions row 1 to 'parsed' with matcher_key='paypal-receipt' AND creates 1 transaction; row 2 transitions to 'skipped'; row 3 transitions to 'unmatched'.
    - Test (idempotency): running the job twice in succession on the same 3 fetched rows results in zero new transactions on the second run (first run already transitioned them out of 'fetched').
    - Test: a row whose user_id mismatches $this->userId is NEVER touched (defense in depth).
    - Test: routes/console.php registers a Schedule entry named 'receipts.process-fetched-inbox-messages' that dispatches ProcessFetchedInboxMessagesJob per user — the existing per-minute scheduler tests pick this up via the same withoutOverlapping pattern.
  </behavior>
  <action>
Create Modules/Receipts/Internal/Jobs/ProcessFetchedInboxMessagesJob.php per PATTERNS.md Wave 1 excerpt lines 420-456:
  - final class implements ShouldBeUniqueUntilProcessing, ShouldQueue.
  - use Dispatchable, InteractsWithQueue, Queueable, SerializesModels.
  - public int $tries = 3; public array $backoff = [60, 300, 900];
  - constructor: public readonly int $userId.
  - uniqueId returns (string) $this->userId. uniqueFor returns 600.
  - uniqueVia returns Cache::driver('redis') — class-level docblock includes the rationale: "queue infrastructure calls uniqueVia at push time before DI completes, so the Cache facade is the only viable surface here". The class is autoloadable via PSR-4 the moment its file lands.

ALSO modify tests/Contracts/BoundaryArchTest.php (the `arch('no Laravel facade usage in module code')` rule's `->ignoring([...])` array, lines 40-60): add Modules\\Receipts\\Internal\\Jobs\\ProcessFetchedInboxMessagesJob to the list, preserving the existing ordering (Chains job first, then the 3 EmailScan jobs, then the new Receipts entry). Mirror the multi-line comment shape that documents the EmailScan jobs' carve-out rationale — a short technical paragraph above the new entry explaining the queue-infrastructure constraint. The carve-out add and the job's Cache facade use ship in the same commit so the arch test never goes red.
  - handle(DatabaseManager $db, Clock $clock, InboxMessageQuery $inboxes, FileImportQuery $files, MatcherRegistry $registry, \Modules\EmailScan\Public\Services\EmlBlobStore $blobs, RecordReceipt $recordReceipt):
    1. Load User via User::query()->where('id', $this->userId)->firstOrFail().
    2. Walk InboxMessageQuery.forStatus('fetched') with a generator. For each InboxMessageDto, skip if $dto->userId !== $this->userId (defense; FND-03 honours it but trust-but-verify).
    3. Resolve eml_path via EmlBlobStore.pathFor($dto->userId, $dto->inboxId, $dto->internalDate, $dto->providerMessageId).
    4. Read bytes via Filesystem (DI).
    5. Construct MatcherInputDto from the InboxMessageDto + eml bytes; dispatch via MatcherRegistry.
    6. On parsed: invoke RecordReceipt (which writes the canonical transaction via the locked pipeline); update inbox_messages.status = 'parsed' + matcher_key.
    7. On skipped: update inbox_messages.status = 'skipped' + matcher_key.
    8. On unmatched: update inbox_messages.status = 'unmatched'.
    9. Per-row work wrapped in DB transaction.
    10. Also walk file_imports.status='fetched' for the same user via FileImportQuery.latestForStatus($user, 'fetched') and apply the same dispatch flow (so file-drop + API-fetched both flow through one consumer).

The class is a queued job, but per RESEARCH Pattern 4 + D-719 the matcher dispatch flow inside is synchronous and pure — the queue is just a per-user backlog walker.

Modify routes/console.php: append a Schedule::call closure mirroring the email-scan hourly entry — receives DatabaseManager + Bus Dispatcher via DI, plucks user IDs from users table, dispatches ProcessFetchedInboxMessagesJob per id. Schedule cadence: hourly (matches Phase 6's incremental scan cadence — Phase 6 produces 'fetched' rows hourly so plan 07's consumer runs at the same rhythm). withoutOverlapping(30). Method order: .name('receipts.process-fetched-inbox-messages') BEFORE .hourly()->withoutOverlapping(...) per the existing pattern.

Create Modules/Receipts/Routes/console.php as an empty placeholder file (the watched-folder schedule lands in plan 05). Add a single docblock comment explaining the file is intentionally empty for now — the per-user hourly ProcessFetchedInboxMessagesJob schedule lives in the root routes/console.php to mirror the email-scan + chain-resolver entries.

Modify Modules/Receipts/Providers/ReceiptsServiceProvider.php: no new bindings required (ProcessFetchedInboxMessagesJob is dispatched, not container-resolved as a singleton). Confirm the class is autoloadable (PSR-4 already covered).

Create Modules/Receipts/tests/Feature/ProcessFetchedInboxMessagesJobTest.php using Pest. Use the FakeInboxMessageQuery stub (introduced plan 01) bound via the container in setUp(). Seed 3 inbox_messages rows + 3 .eml blobs on disk via Storage::fake('local'). Run the job synchronously (`(new ProcessFetchedInboxMessagesJob($user->id))->handle(...)`). Assert behavior per the test list above.
  </action>
  <acceptance_criteria>
    - grep -q "implements ShouldBeUniqueUntilProcessing" Modules/Receipts/Internal/Jobs/ProcessFetchedInboxMessagesJob.php
    - grep -q "implements.*ShouldQueue\\|ShouldQueue,\\|, ShouldQueue" Modules/Receipts/Internal/Jobs/ProcessFetchedInboxMessagesJob.php
    - grep -q "public readonly int \\$userId" Modules/Receipts/Internal/Jobs/ProcessFetchedInboxMessagesJob.php
    - grep -q "Cache::driver('redis')" Modules/Receipts/Internal/Jobs/ProcessFetchedInboxMessagesJob.php
    - grep -q "receipts.process-fetched-inbox-messages" routes/console.php
    - grep -q "Modules\\\\\\\\Receipts\\\\\\\\Internal\\\\\\\\Jobs\\\\\\\\ProcessFetchedInboxMessagesJob" tests/Contracts/BoundaryArchTest.php (confirms the Cache facade carve-out exists)
    - ./vendor/bin/pest --filter='ProcessFetchedInboxMessagesJobTest|BoundaryArchTest' --parallel exits 0
    - ./vendor/bin/pest --parallel exits 0 (full suite passes including the hourly scheduler smoke that exercises routes/console.php registration)
    - ./vendor/bin/phpstan analyse Modules/Receipts routes/console.php --memory-limit=2G --no-progress exits 0
    - ./vendor/bin/pint --test Modules/Receipts routes/console.php exits 0
  </acceptance_criteria>
  <validation_axis>Axis 6 — Idempotency under re-scan (job is safe to re-dispatch). Axis 7 — Pipeline boundary (job dispatches RecordReceipt which routes through the existing pipeline). Axis 8 — Boundary invariants (Cache facade carve-out limited to this single job class; arch test green).</validation_axis>
  <verify>
    <automated>./vendor/bin/pest --filter='ProcessFetchedInboxMessagesJobTest|BoundaryArchTest' --parallel &amp;&amp; ./vendor/bin/phpstan analyse Modules/Receipts routes/console.php --memory-limit=2G --no-progress</automated>
  </verify>
  <done>ProcessFetchedInboxMessagesJob shipped + queued via routes/console.php hourly Schedule entry; inbox_messages status transitions from 'fetched' to 'parsed'|'skipped'|'unmatched' end-to-end; phpstan max + pint green; arch test green.</done>
</task>

</tasks>

<verification>
- All Wave 1 tests green: PaypalReceiptMatcherTest, EmlFileDropTest, MboxFileDropTest, ProcessFetchedInboxMessagesJobTest, FingerprintParityTest (paypal row), IdempotencyContractTest (paypal-receipt-eml row), HeaderSniffer eml/mbox arms.
- Full suite (composer test) green — no Phase 1-6 regression.
- composer analyse + format:check green.
- BoundaryArchTest still green incl. noEmailFetchFromReceipts (PaypalReceiptMatcher does NOT import any Gmail/Graph/OAuth class).
- /imports wizard manual-test path: dropping current-receipt.eml lands a transaction visible at /transactions with reference_id populated (Phase 5 chain resolver picks it up — verify via the existing chain-drawer logic).
</verification>

<rollback>
Revert path: this plan adds 3 new SFC/job classes and modifies 6 existing files. Rollback = git revert <plan-commit-sha>. The IdempotencyContractTest dataset row + HeaderSniffer arms + UploadWizard format extensions cleanly revert. No data loss path: file_imports rows + inbox_messages.matcher_key column survive (introduced by plan 01); rollback only undoes the consumer + matcher code so 'fetched' rows simply accumulate again until plan 02 is re-applied.
</rollback>

<success_criteria>
- /imports wizard exposes the "Email file (.eml, .mbox)" option per UI-SPEC Section 4 copy contract.
- Uploading current-receipt.eml creates a file_imports row (status='parsed', matcher_key='paypal-receipt') AND a transactions row whose fingerprint matches the paired PayPal CSV row.
- Re-uploading the same .eml is a documented no-op (inserted=0, duplicates=1) — IdempotencyContractTest 'paypal-receipt-eml' dataset green.
- Uploading small.mbox creates 5 file_imports rows + ≥1 transactions row (PayPal-recognised messages from the synthesised mbox).
- ProcessFetchedInboxMessagesJob walks inbox_messages.status='fetched' for a user and transitions rows correctly — verified via the Pest feature test using FakeInboxMessageQuery.
- The PayPal Transaction ID populated in transactions.reference_id triggers Phase 5's existing ResolveChainLinksJob automatically (zero new code in Chains).
- Validation axes 1, 2, 3, 6, 7, 8 covered for the PayPal slice.
</success_criteria>

<output>
Create `.planning/phases/07-email-template-matchers-categorization-learning/07-02-SUMMARY.md` when done.
</output>
