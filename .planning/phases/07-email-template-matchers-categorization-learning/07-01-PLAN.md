---
phase: 07
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Modules/Receipts/composer.json
  - Modules/Receipts/Providers/ReceiptsServiceProvider.php
  - Modules/Receipts/tests/Pest.php
  - Modules/Receipts/tests/TestCase.php
  - Modules/Receipts/Public/Contracts/SenderMatcher.php
  - Modules/Receipts/Public/Dto/MatcherInputDto.php
  - Modules/Receipts/Public/Dto/ParsedReceiptDto.php
  - Modules/Receipts/Public/Dto/MatchOutcomeDto.php
  - Modules/Receipts/Public/Dto/ChainHintPayload/FundedByCardPayload.php
  - Modules/Receipts/Public/Dto/ChainHintPayload/RefundOfPayload.php
  - Modules/Receipts/Public/Events/ChainHintDetected.php
  - Modules/Receipts/Public/Events/ReceiptConflictDetected.php
  - Modules/Receipts/Internal/Pipeline/EmlMimeReader.php
  - Modules/Receipts/Internal/Pipeline/MboxIterator.php
  - Modules/Receipts/Internal/Pipeline/FileDropEmlBlobStore.php
  - Modules/Receipts/Internal/Testing/FakeInboxMessageQuery.php
  - Modules/Receipts/tests/fixtures/paypal/.gitkeep
  - Modules/Receipts/tests/fixtures/ics/.gitkeep
  - Modules/Receipts/tests/fixtures/googleplay/.gitkeep
  - Modules/Receipts/tests/fixtures/mbox/small.mbox
  - Modules/Receipts/tests/Unit/MboxIteratorTest.php
  - Modules/Receipts/tests/Unit/EmlMimeReaderTest.php
  - Modules/Receipts/tests/Contracts/FingerprintParityTest.php
  - Modules/Receipts/Database/Migrations/2026_05_17_010001_create_file_imports_table.php
  - Modules/Receipts/Database/Migrations/2026_05_17_010002_add_matcher_key_to_inbox_messages.php
  - Modules/Categorization/Database/Migrations/2026_05_17_010003_create_categorization_rules_table.php
  - Modules/Categorization/Database/Migrations/2026_05_17_010004_add_receipt_conflict_resolution_to_users.php
  - Modules/Categorization/Database/Migrations/2026_05_17_010005_create_pending_enrichment_conflicts_table.php
  - Modules/Categorization/Database/Migrations/2026_05_17_010006_add_auto_category_provenance_to_transactions.php
  - Modules/Core/Database/Migrations/2026_05_17_010007_add_auto_import_drop_folder_to_users.php
  - Modules/Categorization/Public/Dto/AutoCategorizationOutcomeDto.php
  - Modules/Categorization/Public/Dto/CategorizationRuleDto.php
  - tests/Contracts/BoundaryArchTest.php
  - composer.json
  - phpunit.xml
  - tests/Pest.php
  - scripts/anonymize_receipt_eml.php
autonomous: true
requirements: [EML-05, EML-07, CAT-02, CAT-04]
must_haves:
  truths:
    - "Modules/Receipts/ skeleton boots: ServiceProvider autoload-discovered + composer require runs clean (D-701)"
    - "Pest --filter=Receipts discovers and runs at least one passing test (D-714 Wave 0)"
    - "BoundaryArchTest noEmailFetchFromReceipts is green when Modules/Receipts/ is empty (Validation Axis 8)"
    - "BoundaryArchTest noTransactionWritesFromEmailScan carry-forward stays green (D-132 inherited)"
    - "All Phase 7 migrations migrate up + roll back cleanly under sqlite_testing (D-705, D-705a, D-707, D-709, D-715, D-722)"
    - "MboxIterator streams a synthetic 50MB mbox under 64MB peak memory (Pitfall 6)"
    - "FingerprintParityTest scaffold + dataset hook exist (failing — fixtures land Wave 1+) per Validation Axis 3"
  artifacts:
    - path: "Modules/Receipts/composer.json"
      provides: "diederik/receipts module manifest (autoload PSR-4 + autoload-dev)"
    - path: "Modules/Receipts/Providers/ReceiptsServiceProvider.php"
      provides: "Container tag binding for 'receipts.matcher' + MatcherRegistry singleton + conditional loaders"
    - path: "Modules/Receipts/Public/Contracts/SenderMatcher.php"
      provides: "key() + priority() + canHandle() + match() contract"
    - path: "Modules/Receipts/Public/Dto/ParsedReceiptDto.php"
      provides: "Immutable matcher output carrying merchant, amount, currency, settled pair, reference id, bookedAt, ownIban, description, rawPayload"
    - path: "Modules/Receipts/Internal/Pipeline/MboxIterator.php"
      provides: "Hand-rolled streaming mbox iterator per RESEARCH Pattern 3"
    - path: "Modules/Receipts/Database/Migrations/2026_05_17_010001_create_file_imports_table.php"
      provides: "file_imports table (D-705 shape) + status enum triggers"
    - path: "Modules/Categorization/Database/Migrations/2026_05_17_010003_create_categorization_rules_table.php"
      provides: "categorization_rules table per D-709 + D-722 (active + notes); UNIQUE (user_id, field, match, value); index (user_id, active)"
    - path: "Modules/Categorization/Database/Migrations/2026_05_17_010005_create_pending_enrichment_conflicts_table.php"
      provides: "pending_enrichment_conflicts table per D-715 recommendation"
  key_links:
    - from: "Modules/Receipts/Providers/ReceiptsServiceProvider.php"
      to: "Container::tagged('receipts.matcher')"
      via: "Wave 1+ matchers tag here"
      pattern: "->tag\\("
    - from: "composer.json"
      to: "Modules\\\\Receipts\\\\Tests\\\\"
      via: "autoload-dev PSR-4 row"
      pattern: "Receipts\\\\Tests"
    - from: "phpunit.xml"
      to: "Modules/Receipts/tests"
      via: "Unit + Feature + Contracts testsuite directory entries"
      pattern: "Modules/Receipts/tests"
threat_model:
  - T-07-01
  - T-07-02
  - T-07-08
  - T-07-SC
---

<objective>
Stand up the empty Modules/Receipts/ shell, register the 4 new tables + 3 schema additions, lock in the BoundaryArchTest invariants, ship the hand-rolled MboxIterator + EmlMimeReader wrapper + FakeInboxMessageQuery stub, and scaffold the load-bearing FingerprintParityTest dataset — so every subsequent wave plugs into a working module skeleton without any cross-cutting refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-CONTEXT.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-RESEARCH.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md
@.planning/phases/07-email-template-matchers-categorization-learning/07-VALIDATION.md
@CLAUDE.md
@composer.json
@phpunit.xml
@tests/Pest.php
@tests/Contracts/BoundaryArchTest.php
@Modules/Transfers/composer.json
@Modules/Transfers/Providers/TransfersServiceProvider.php
@Modules/Transfers/tests/Pest.php
@Modules/Transfers/tests/TestCase.php
@Modules/Chains/Providers/ChainsServiceProvider.php
@Modules/EmailScan/Public/Dto/InboxMessageDto.php
@Modules/EmailScan/Public/Services/InboxMessageQuery.php
@Modules/EmailScan/Internal/MimeHeaderParser.php
@Modules/EmailScan/Internal/EmlBlobStore.php
@Modules/EmailScan/Database/Migrations/2026_05_16_020003_create_inbox_messages_table.php
@Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php
@Modules/Ledger/Database/Migrations/2026_05_12_010007_create_merchant_memories_table.php
@Modules/Ledger/Database/Migrations/2026_05_15_010001_add_raw_payload_to_transactions.php
@Modules/Ingestion/Public/Contracts/SourceAdapter.php
@Modules/Ingestion/Public/Dto/SourceTransactionDto.php
@Modules/Categorization/Public/Dto/CategoryOption.php

<interfaces>
The receipt path consumes Phase 6's InboxMessageDto (read-only) and emits its own ParsedReceiptDto + MatchOutcomeDto. The pipeline bridge (ReceiptSourceAdapter, Wave 1) converts ParsedReceiptDto → SourceTransactionDto whose shape is locked above.

Locked enums for the new tables:
  file_imports.source_kind: 'eml' | 'mbox'
  file_imports.status: 'fetched' | 'parsed' | 'skipped' | 'unmatched'
  categorization_rules.field: 'merchant' | 'description' | 'counterparty'
  categorization_rules.match: 'contains' | 'equals' | 'starts_with'
  users.receipt_conflict_resolution: 'unset' | 'prefer_receipt' | 'prefer_first_write'
  transactions.auto_category_provenance: JSON `{ source: 'rule'|'memory', rule_id?: int, memory_id?: int, category_id: int }`

zbateson/mail-mime-parser 4.0.1 is already locked (no composer install needed). No new composer requires this wave.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → CLI | Developer runs migrate / pest commands — trusted |
| Phase 6 inbox_messages → Phase 7 Receipts module | Read-only; status transitions land Wave 1 |
| filesystem → MboxIterator | Untrusted: a malicious mbox could attempt zip-bomb-style expansion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | MboxIterator on untrusted .mbox | mitigate | Streaming via fopen('rb') + fgets() only; never file_get_contents(); per-message size cap = 50 MB; Pest test asserts peak memory <64 MB on 50 MB synthetic mbox (Validation Axis 6 + Pitfall 6) |
| T-07-02 | Tampering | file_imports.source_filename path traversal | mitigate | source_filename column stores user-supplied name verbatim for audit; the deterministic eml_path uses sha256(bytes) hash, never the user-supplied filename (D-705a); path generation enforced inside FileDropEmlBlobStore::pathFor() with the same [A-Za-z0-9._-]{1,200} allow-list as EmailScan's EmlBlobStore::MESSAGE_ID_PATTERN |
| T-07-08 | Information Disclosure | Cross-user data leak in file_imports / categorization_rules / pending_enrichment_conflicts | mitigate | FND-03 BelongsToUser invariant — every new table includes nullable user_id foreign key + composite UNIQUE constraints scoped on (user_id, …); migrations verified via Pest "table has user_id column" assertion (Validation Axis 8) |
| T-07-SC | Tampering | npm/composer install path | mitigate | Phase 7 introduces ZERO new composer dependencies (zbateson/mail-mime-parser 4.0.1 already in composer.lock per RESEARCH); composer audit run in verify task confirms no new direct deps; armin/mbox-parser REJECTED upstream per RESEARCH (incompatible with zbateson 4.0) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Module skeleton + test registration + arch invariants</name>
  <files>
    Modules/Receipts/composer.json,
    Modules/Receipts/Providers/ReceiptsServiceProvider.php,
    Modules/Receipts/tests/Pest.php,
    Modules/Receipts/tests/TestCase.php,
    Modules/Receipts/Public/Contracts/SenderMatcher.php,
    Modules/Receipts/Public/Dto/MatcherInputDto.php,
    Modules/Receipts/Public/Dto/ParsedReceiptDto.php,
    Modules/Receipts/Public/Dto/MatchOutcomeDto.php,
    Modules/Receipts/Public/Dto/ChainHintPayload/FundedByCardPayload.php,
    Modules/Receipts/Public/Dto/ChainHintPayload/RefundOfPayload.php,
    Modules/Receipts/Public/Events/ChainHintDetected.php,
    Modules/Receipts/Public/Events/ReceiptConflictDetected.php,
    Modules/Categorization/Public/Dto/AutoCategorizationOutcomeDto.php,
    Modules/Categorization/Public/Dto/CategorizationRuleDto.php,
    composer.json,
    phpunit.xml,
    tests/Pest.php,
    tests/Contracts/BoundaryArchTest.php
  </files>
  <read_first>
    Modules/Transfers/composer.json,
    Modules/Transfers/Providers/TransfersServiceProvider.php,
    Modules/Chains/Providers/ChainsServiceProvider.php,
    Modules/Transfers/tests/Pest.php,
    Modules/Transfers/tests/TestCase.php,
    Modules/Ingestion/Public/Contracts/SourceAdapter.php,
    Modules/EmailScan/Public/Dto/InboxMessageDto.php,
    Modules/Categorization/Public/Events/TransactionCategorized.php,
    Modules/Categorization/Public/Dto/CategoryOption.php,
    tests/Contracts/BoundaryArchTest.php,
    composer.json,
    phpunit.xml,
    .planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md (§ Wave 0 code excerpts + §"Arch test" snippet lines 183-219)
  </read_first>
  <action>
Create the diederik/receipts composer.json mirroring Modules/Transfers/composer.json verbatim (swap `transfers` → `receipts` in name, description, PSR-4 keys). Create ReceiptsServiceProvider per the Wave-0 code excerpt in PATTERNS.md: register() tags PaypalReceiptMatcher + IcsReceiptMatcher + GooglePlayReceiptMatcher with 'receipts.matcher' AND binds those FQNs as deferred singletons (use `class_exists()` guards so Wave 0 boots before matcher classes land); also singleton-bind EmlMimeReader, MboxIterator, FileDropEmlBlobStore, MatcherRegistry (closure resolves tagged collection + usort by priority DESC). boot() loads migrations from Database/Migrations, routes from Routes/web.php + Routes/console.php (conditional via is_file), views from Resources/views with namespace 'receipts'. Use the Wave-0 stub for tagged matcher FQNs guarded by `class_exists()` so the empty module boots.

Create Modules/Receipts/tests/Pest.php + tests/TestCase.php as exact clones of Modules/Transfers/tests/Pest.php + TestCase.php with namespace `Modules\\Receipts\\Tests`.

Create the SenderMatcher contract (key/priority/canHandle/match) per PATTERNS.md code excerpt lines 230-243.

Create MatcherInputDto as a final readonly Spatie Data union carrying:
  - `id: int`, `userId: int`, `source: 'inbox'|'file-drop'`, `providerMessageId: string`, `senderEmail: string`, `senderName: ?string`, `subject: ?string`, `internalDate: DateTimeImmutable`, `emlPath: string`
  - + helper `toInboxMessageDto(): InboxMessageDto` (synthesizes inboxId=0 for file-drop rows)

Create ParsedReceiptDto per PATTERNS.md excerpt lines 246-269 (readonly Spatie Data; merchantName, amountMinor, currency, settledAmountMinor, settledCurrency, referenceId, bookedAt, ownIban, description, rawPayload, plus optional `chainHints: list<object>` for D-708).

Create MatchOutcomeDto as a sum-type wrapper with static constructors `parsed(ParsedReceiptDto $r): self`, `skipped(string $reason): self`, `unmatched(): self` — final readonly Spatie Data with `kind: 'parsed'|'skipped'|'unmatched'`, `parsed: ?ParsedReceiptDto`, `skipReason: ?string`.

Create FundedByCardPayload (`cardLast4: string`) + RefundOfPayload (`originalReferenceId: string`) as readonly Spatie Data.

Create ChainHintDetected event per PATTERNS.md excerpt lines 275-286 (final readonly: sourceTransactionId, hintType, hintPayload object, evidence, userId). Create ReceiptConflictDetected event (final readonly: transactionId, userId, field, receiptValue, csvValue, importRunId).

Create AutoCategorizationOutcomeDto in Modules/Categorization/Public/Dto/ — final readonly Spatie Data with static constructors `auto(CanonicalTransaction $canonical, string $provenance, ?int $ruleId, ?int $memoryId): self`, `manual(CanonicalTransaction $canonical): self`. Fields: `canonical: CanonicalTransaction`, `provenance: 'rule'|'memory'|'manual'`, `ruleId: ?int`, `memoryId: ?int`.

Create CategorizationRuleDto in Modules/Categorization/Public/Dto/ — final readonly Spatie Data: `id: int`, `userId: int`, `field: string`, `match: string`, `value: string`, `categoryId: int`, `categoryPath: string`, `hitsCount: int`, `active: bool`, `notes: ?string`, `createdAt: DateTimeImmutable`.

Modify composer.json to add the autoload-dev PSR-4 row `"Modules\\\\Receipts\\\\Tests\\\\": "Modules/Receipts/tests/"`. Run `composer dump-autoload` as the verify step.

Modify phpunit.xml to add `<directory>Modules/Receipts/tests/Unit</directory>` under the Unit `<testsuite>`, `<directory>Modules/Receipts/tests/Feature</directory>` under Feature, and a new `<testsuite name="ReceiptsContracts">` with `<directory suffix="Test.php">./Modules/Receipts/tests/Contracts</directory>` (mirror ChainsContracts shape).

Modify tests/Pest.php (root): If it has a per-module foreach map (Phase 4 D-80b pattern), add `'Modules/Receipts' => Modules\\Receipts\\Tests\\TestCase::class` row.

Modify tests/Contracts/BoundaryArchTest.php: append `arch('Modules\\\\Receipts\\\\Internal is only used inside Modules\\\\Receipts')` rule + the `noEmailFetchFromReceipts` `it()` block from PATTERNS.md lines 183-219 verbatim (`GmailApiClient|GraphApiClientContract|GoogleOAuthProvider|MicrosoftOAuthProvider|OAuthStateRepository|OAuthSecretsRepository` blacklist, base_path('Modules/Receipts'), `tests/` carve-out). Do NOT yet add the Cache facade carve-out — that lands in plan 02 when ProcessFetchedInboxMessagesJob ships.

NO PHPDoc/comment references to D-numbers, REQ-IDs, or .planning/ paths — strictly technical prose only. Use `final readonly` on every new event class. Use constructor DI throughout — no facades, no `auth()`/`request()`/`config()`/`view()` helpers.
  </action>
  <acceptance_criteria>
    - `test -f Modules/Receipts/composer.json` returns 0
    - `grep -q '"diederik/receipts"' Modules/Receipts/composer.json` succeeds
    - `grep -q "namespace Modules\\\\Receipts\\\\Tests" Modules/Receipts/tests/TestCase.php` succeeds
    - `grep -q "'receipts.matcher'" Modules/Receipts/Providers/ReceiptsServiceProvider.php` succeeds
    - `grep -q "interface SenderMatcher" Modules/Receipts/Public/Contracts/SenderMatcher.php` succeeds
    - `grep -q "final class ParsedReceiptDto extends Data" Modules/Receipts/Public/Dto/ParsedReceiptDto.php` succeeds
    - `grep -q "final readonly class ChainHintDetected" Modules/Receipts/Public/Events/ChainHintDetected.php` succeeds
    - `grep -q "noEmailFetchFromReceipts" tests/Contracts/BoundaryArchTest.php` succeeds
    - `grep -q "Modules\\\\\\\\Receipts\\\\\\\\Internal" tests/Contracts/BoundaryArchTest.php` succeeds (the Internal-only-from-Receipts arch rule)
    - `grep -q "Modules\\\\\\\\Receipts\\\\\\\\Tests\\\\\\\\" composer.json` succeeds (autoload-dev PSR-4 row)
    - `grep -q "Modules/Receipts/tests/Unit" phpunit.xml && grep -q "Modules/Receipts/tests/Feature" phpunit.xml && grep -q "ReceiptsContracts" phpunit.xml` succeeds
    - `composer dump-autoload --no-scripts` exits 0
    - `./vendor/bin/pest --filter=BoundaryArchTest` exits 0 (existing arch tests still green; new noEmailFetchFromReceipts trivially passes since Modules/Receipts/ contains no GmailApiClient imports)
    - `./vendor/bin/phpstan analyse Modules/Receipts --level=max --memory-limit=1G --no-progress` exits 0 (skeleton compiles under strict)
    - Validation Axis 8 (Boundary invariants): both arch rules added.
  </acceptance_criteria>
  <validation_axis>Axis 8 — Boundary invariants (BoundaryArchTest::noEmailFetchFromReceipts + Modules\\Receipts\\Internal containment); Axis 3 — DTO equivalence shapes locked at the contract level.</validation_axis>
  <verify>
    <automated>composer dump-autoload --no-scripts &amp;&amp; ./vendor/bin/pest --filter=BoundaryArchTest --parallel</automated>
  </verify>
  <done>Module skeleton + 14 new files + 4 modified files committed; arch tests green; phpstan max green on new code; pest discovers Modules/Receipts/tests paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrations + EmlMimeReader + MboxIterator + FileDropEmlBlobStore + FakeInboxMessageQuery + FingerprintParityTest scaffold</name>
  <files>
    Modules/Receipts/Database/Migrations/2026_05_17_010001_create_file_imports_table.php,
    Modules/Receipts/Database/Migrations/2026_05_17_010002_add_matcher_key_to_inbox_messages.php,
    Modules/Categorization/Database/Migrations/2026_05_17_010003_create_categorization_rules_table.php,
    Modules/Categorization/Database/Migrations/2026_05_17_010004_add_receipt_conflict_resolution_to_users.php,
    Modules/Categorization/Database/Migrations/2026_05_17_010005_create_pending_enrichment_conflicts_table.php,
    Modules/Categorization/Database/Migrations/2026_05_17_010006_add_auto_category_provenance_to_transactions.php,
    Modules/Core/Database/Migrations/2026_05_17_010007_add_auto_import_drop_folder_to_users.php,
    Modules/Receipts/Internal/Pipeline/EmlMimeReader.php,
    Modules/Receipts/Internal/Pipeline/MboxIterator.php,
    Modules/Receipts/Internal/Pipeline/FileDropEmlBlobStore.php,
    Modules/Receipts/Internal/Testing/FakeInboxMessageQuery.php,
    Modules/Receipts/tests/fixtures/paypal/.gitkeep,
    Modules/Receipts/tests/fixtures/ics/.gitkeep,
    Modules/Receipts/tests/fixtures/googleplay/.gitkeep,
    Modules/Receipts/tests/fixtures/mbox/small.mbox,
    Modules/Receipts/tests/Unit/MboxIteratorTest.php,
    Modules/Receipts/tests/Unit/EmlMimeReaderTest.php,
    Modules/Receipts/tests/Contracts/FingerprintParityTest.php,
    scripts/anonymize_receipt_eml.php
  </files>
  <read_first>
    Modules/EmailScan/Database/Migrations/2026_05_16_020003_create_inbox_messages_table.php,
    Modules/Core/Database/Migrations/2026_05_13_010001_add_default_currency_view_to_users.php,
    Modules/Ledger/Database/Migrations/2026_05_12_010007_create_merchant_memories_table.php,
    Modules/Ledger/Database/Migrations/2026_05_15_010001_add_raw_payload_to_transactions.php,
    Modules/EmailScan/Internal/MimeHeaderParser.php,
    Modules/EmailScan/Internal/EmlBlobStore.php,
    Modules/EmailScan/Public/Services/InboxMessageQuery.php,
    Modules/EmailScan/Public/Dto/InboxMessageDto.php,
    .planning/phases/07-email-template-matchers-categorization-learning/07-RESEARCH.md (§ Pattern 3 mbox iterator code lines 825-866; § code example "categorization_rules schema" lines 870-890; § code example "pending_enrichment_conflicts schema" lines 892-910; § Pitfall 6 streaming bound),
    .planning/phases/07-email-template-matchers-categorization-learning/07-PATTERNS.md (§ Wave 0 migration excerpts; § Wave 1 EmlMimeReader excerpt lines 504-522)
  </read_first>
  <behavior>
    - Test: MboxIterator yields exactly 5 distinct .eml strings from small.mbox (5 synthesised messages).
    - Test: MboxIterator iterating a generated 50 MB mbox (built in-test via a helper that writes "From foo@bar Thu Jan  1 00:00:00 2026\n" + minimal headers + 8 KB body, repeated ~6400 times) keeps `memory_get_peak_usage(true)` under 64 MB (Pitfall 6 budget).
    - Test: MboxIterator correctly strips ONE leading `>` from `>+From ` lines (mboxrd unescape).
    - Test: EmlMimeReader::read() on a minimal RFC 822 string returns a ParsedMimeMessage with text body when text/plain is present and falls back to stripped text/html otherwise (Pitfall 2 — prefer text/plain).
    - Test: FingerprintParityTest is REGISTERED with a `dataset()` mapping `paypal`, `ics`, `google-play` keys to (eml-path, paired-csv-row, expected matcher key) tuples — fixtures intentionally absent so the test xfails / skips with a clear "Wave 1 must land fixture XYZ" message (use Pest's `->skip()` predicate so it does not red the CI gate).
    - Migrations: every up() succeeds + roll back via down() on sqlite_testing in CI; the categorization_rules UNIQUE (user_id, field, match, value) constraint blocks duplicate inserts at the DB layer (assert via Pest); the file_imports status enum trigger rejects an invalid value (assert via Pest).
  </behavior>
  <action>
Create the seven migrations using container-resolved schema builder (Shared Pattern 7 in PATTERNS.md):

(1) `2026_05_17_010001_create_file_imports_table.php` — copy the inbox_messages migration shape verbatim from PATTERNS.md Wave 0 excerpt lines 138-181: id, foreignId('user_id')->nullable()->constrained('users')->cascadeOnDelete(), source_kind 16, source_filename 512, provider_message_id 128, internal_date timestamp, sender_email 320, sender_name 320 nullable, subject 998 nullable, eml_path 1024, status 16 default 'fetched', fetched_at timestamp, timestamps. Add UNIQUE (user_id, provider_message_id) + index (user_id, status) + index (user_id, internal_date). Add paired BEFORE INSERT / BEFORE UPDATE triggers `file_imports_status_check_insert` + `_update` with allowed-statuses tuple `'fetched','parsed','skipped','unmatched'`. ALSO add a paired trigger validating source_kind ∈ {'eml','mbox'} (same shape).

(2) `2026_05_17_010002_add_matcher_key_to_inbox_messages.php` — Schema::table('inbox_messages', fn (Blueprint $t) => $t->string('matcher_key', 64)->nullable()->after('status')). Add index (inbox_id, matcher_key). down() drops the column + index.

(3) `2026_05_17_010003_create_categorization_rules_table.php` — schema per RESEARCH lines 870-890: id, foreignId('user_id')->nullable()->constrained('users')->cascadeOnDelete(), field 16, match 16, value (default 255), foreignId('category_id')->constrained('categories')->cascadeOnDelete(), unsignedInteger('hits_count')->default(0), boolean('active')->default(true), text('notes')->nullable(), timestamps. UNIQUE (user_id, field, match, value). Index (user_id, active). Add paired triggers `categorization_rules_field_check_*` validating field ∈ {'merchant','description','counterparty'} AND `categorization_rules_match_check_*` validating match ∈ {'contains','equals','starts_with'}.

(4) `2026_05_17_010004_add_receipt_conflict_resolution_to_users.php` — Schema::table('users', fn => $t->string('receipt_conflict_resolution', 32)->default('unset')->after('default_currency_view')). Add paired triggers validating the enum {'unset','prefer_receipt','prefer_first_write'} (small enum, PHP-side validation also exists in Wave 3 — but the DB trigger is defence-in-depth and mirrors inbox_messages.status pattern).

(5) `2026_05_17_010005_create_pending_enrichment_conflicts_table.php` — schema per RESEARCH lines 892-910: id, foreignId('user_id')->nullable()->constrained('users')->cascadeOnDelete(), foreignId('transaction_id')->constrained('transactions')->cascadeOnDelete(), string('field_name', 64), text('stored_value')->nullable(), text('incoming_value')->nullable(), string('incoming_source_format', 32), foreignId('import_run_id')->nullable()->constrained('import_runs')->nullOnDelete(), timestamps. UNIQUE (user_id, transaction_id, field_name). Index (user_id, created_at).

(6) `2026_05_17_010006_add_auto_category_provenance_to_transactions.php` — Schema::table('transactions', fn => $t->json('auto_category_provenance')->nullable()->after('category_id')). NOT in the v3 fingerprint tuple — no version bump. down() drops the column.

(7) `2026_05_17_010007_add_auto_import_drop_folder_to_users.php` — Schema::table('users', fn => $t->boolean('auto_import_drop_folder')->default(false)->after('receipt_conflict_resolution')).

Every migration uses the same private `?DatabaseManager $resolvedDb = null` + `private function schema(): Builder` + `private function db(): DatabaseManager` triad from the inbox_messages migration (Container::getInstance()->make(DatabaseManager::class)) — NO Schema facade.

Create Modules/Receipts/Internal/Pipeline/EmlMimeReader.php — final class with `read(string $rawEml): ParsedMimeMessage` (define a local final readonly ParsedMimeMessage with textBody:?string, htmlBody:?string, headers:array<string,string>, attachmentFilenames:list<string>). Mirror MimeHeaderParser zbateson facade pattern. Always prefer text/plain (zbateson auto-chooses first text/plain via `getTextContent()`); fall back to html_entity_decode(strip_tags(getHtmlContent())) when textContent is null. Stateless / singleton-safe.

Create Modules/Receipts/Internal/Pipeline/MboxIterator.php — final class with `iterate(string $mboxPath): Generator<int, array{eml: string, byteOffset: int, index: int}>`. Implementation per RESEARCH lines 825-866 verbatim (fopen 'rb', fgets line-by-line, From-line state machine, mboxrd `>+From ` unescape, throw RuntimeException on fopen failure). NEVER use file_get_contents.

Create Modules/Receipts/Internal/Pipeline/FileDropEmlBlobStore.php — clone Modules/EmailScan/Internal/EmlBlobStore.php verbatim BUT change pathFor() to `storage/app/inbox/{user_id}/file-drop/{YYYY}/{MM}/{messageIdHash}.eml` (messageIdHash is the 64-char sha256 hex from D-705a; matches the [A-Za-z0-9._-]{1,200} allow-list since sha256 hex is purely [0-9a-f]). Reuse the put() + delete() + atomic-write + chmod 0700 dir / 0600 file body verbatim. Constructor DI of Filesystem.

Create Modules/Receipts/Internal/Testing/FakeInboxMessageQuery.php — implements the contract of `InboxMessageQuery` (define a local interface InboxMessageReader if Phase 6 hasn't exposed one, OR extend InboxMessageQuery with a constructor that takes a `list<InboxMessageDto>` and yields them; prefer the latter to avoid a Phase-6 refactor). Used by Wave 1 tests to bypass real DB rows. Cite no D-numbers.

Create Modules/Receipts/tests/fixtures/mbox/small.mbox — 5 synthesised mbox messages, each ~200 bytes, mboxrd-conformant ("From sender@example.com Thu Jan  1 00:00:00 2026\n" + From/To/Subject/Date/Message-ID headers + blank line + tiny body). One message MUST contain a body line starting with `From ` to exercise the unescape path. One message MUST contain `>From ` to exercise the strip-one-> path. Commit verbatim.

Create scripts/anonymize_receipt_eml.php — copy the shape of scripts/anonymize_paypal_csv.php (zero composer deps; reads stdin, writes anonymised output to stdout, replaces email-local-parts + names + IBANs + amounts via deterministic redaction). The script will be invoked by the developer to mine real receipts into anonymised fixtures during Wave 1 prep; the script itself only needs to exist + be marked executable.

Create Modules/Receipts/tests/Unit/MboxIteratorTest.php — three Pest cases per <behavior> above. Use Storage::fake('local') if convenient or write to sys_get_temp_dir(). The 50MB streaming test asserts via `memory_get_peak_usage(true) < 64 * 1024 * 1024` AND `iterator_to_array(... false)` equivalent count.

Create Modules/Receipts/tests/Unit/EmlMimeReaderTest.php — two Pest cases per <behavior>.

Create Modules/Receipts/tests/Contracts/FingerprintParityTest.php — Pest test with a `dataset()` of three rows (paypal, ics, google-play). Each row carries `(eml: string fixture path, csv: string fixture path, matcherKey: string)`. The test asserts `FingerprintComposer::compose($csvCanonical) === FingerprintComposer::compose($receiptCanonical)`. WAVE 0 implementation: the test calls `->skip("Fixture {emlPath} missing — lands in Wave {1|2} per dataset row")` when either fixture file is absent. This is intentional — the test scaffold ships now so Wave 1+ executors only add fixtures and the gate auto-activates.
  </action>
  <acceptance_criteria>
    - All 7 migrations apply cleanly under sqlite_testing: `./vendor/bin/pest --filter=Phase7MigrationsTest` (creates this on the fly OR equivalent: run `php artisan migrate --pretend --env=testing` then `php artisan migrate --env=testing && php artisan migrate:rollback --step=7 --env=testing && php artisan migrate --env=testing`) exits 0.
    - `grep -q 'CREATE TRIGGER file_imports_status_check' Modules/Receipts/Database/Migrations/2026_05_17_010001_create_file_imports_table.php` succeeds.
    - `grep -q "UNIQUE (user_id, field, match, value)" -i Modules/Categorization/Database/Migrations/2026_05_17_010003_create_categorization_rules_table.php` OR equivalent (`unique\\(\\['user_id', 'field', 'match', 'value'\\]`) succeeds.
    - `grep -q "auto_category_provenance" Modules/Categorization/Database/Migrations/2026_05_17_010006_add_auto_category_provenance_to_transactions.php` succeeds.
    - `grep -q "json" Modules/Categorization/Database/Migrations/2026_05_17_010006_add_auto_category_provenance_to_transactions.php` succeeds (JSON column).
    - `grep -q "receipt_conflict_resolution" Modules/Categorization/Database/Migrations/2026_05_17_010004_add_receipt_conflict_resolution_to_users.php` succeeds.
    - `grep -q "auto_import_drop_folder" Modules/Core/Database/Migrations/2026_05_17_010007_add_auto_import_drop_folder_to_users.php` succeeds.
    - `grep -q "fopen.*'rb'" Modules/Receipts/Internal/Pipeline/MboxIterator.php` succeeds (binary streaming).
    - `grep -v '^[[:space:]]*//' Modules/Receipts/Internal/Pipeline/MboxIterator.php | grep -cF 'file_get_contents' | tr -d '[:space:]'` equals `0` (no whole-file load).
    - `test -f Modules/Receipts/tests/fixtures/mbox/small.mbox && wc -l Modules/Receipts/tests/fixtures/mbox/small.mbox | awk '{exit ($1>=5)?0:1}'` succeeds (mbox has ≥5 lines).
    - `./vendor/bin/pest --filter=MboxIteratorTest --parallel` exits 0 (all three behaviors green).
    - `./vendor/bin/pest --filter=EmlMimeReaderTest --parallel` exits 0.
    - `./vendor/bin/pest --filter=FingerprintParityTest --parallel` exits 0 (scaffold present; rows skip with informative messages — NOT red).
    - `./vendor/bin/phpstan analyse Modules/Receipts Modules/Categorization/Public/Dto --memory-limit=2G --no-progress` exits 0.
    - `./vendor/bin/pint --test Modules/Receipts Modules/Categorization/Database/Migrations Modules/Core/Database/Migrations` exits 0.
    - Composer audit confirms no new direct dependencies introduced: `diff <(git show HEAD:composer.json | sed -n '/"require":/,/^    }/p') <(sed -n '/"require":/,/^    }/p' composer.json)` shows only the autoload-dev PSR-4 entry change, no `require` additions (Validation Axis 8 — T-07-SC).
    - Validation Axis 6 (Idempotency under re-drop): file_imports UNIQUE (user_id, provider_message_id) confirmed via the migration trigger.
    - Validation Axis 1 + 2 (header/body parsing): EmlMimeReader fallback order verified.
  </acceptance_criteria>
  <validation_axis>Axis 1 (header parsing — EmlMimeReader), Axis 2 (body decoding — preference order), Axis 6 (idempotency — UNIQUE constraints + sha256 synthetic Message-ID), Axis 7 (pipeline boundary — none yet, scaffold only), Axis 8 (boundary invariants — migrations comply with DI-only schema builder pattern).</validation_axis>
  <verify>
    <automated>./vendor/bin/pest --filter='MboxIteratorTest|EmlMimeReaderTest|FingerprintParityTest|BoundaryArchTest' --parallel &amp;&amp; ./vendor/bin/phpstan analyse Modules/Receipts Modules/Categorization/Public/Dto --memory-limit=2G --no-progress &amp;&amp; ./vendor/bin/pint --test Modules/Receipts Modules/Categorization/Database/Migrations Modules/Core/Database/Migrations</automated>
  </verify>
  <done>7 migrations live + reversible; MboxIterator + EmlMimeReader + FileDropEmlBlobStore + FakeInboxMessageQuery shipped with passing unit tests; fixture directories scaffolded; FingerprintParityTest dataset registered and skipping cleanly; phpstan max + pint green; composer audit clean.</done>
</task>

</tasks>

<verification>
- All 17 new files + 4 modified files committed in a single atomic plan.
- `composer test` full suite green.
- `composer analyse` (phpstan level max + strict) green.
- `composer format:check` (Pint) green.
- BoundaryArchTest passes with new noEmailFetchFromReceipts invariant.
- Migrations table reflects 7 new entries; `php artisan migrate:status` shows all Ran.
</verification>

<rollback>
The plan is purely additive — no existing pipeline behaviour changes. Rollback path: `git revert <plan-commit-sha>` reverses the file creations + the four modified-file additions cleanly. The 7 migrations roll back via `php artisan migrate:rollback --step=7`. No data loss path (the empty tables are introduced by this plan).
</rollback>

<success_criteria>
- Modules/Receipts/ skeleton boots; `php artisan about` shows the module loaded.
- All Wave 0 tests pass (MboxIterator, EmlMimeReader, FingerprintParity scaffold-skipping, BoundaryArchTest).
- No new composer requires added; zbateson/mail-mime-parser 4.0.1 confirmed in composer.lock unchanged.
- Pest discovers all new Modules/Receipts/tests/ paths via phpunit.xml.
</success_criteria>

<output>
Create `.planning/phases/07-email-template-matchers-categorization-learning/07-01-SUMMARY.md` when done.
</output>
