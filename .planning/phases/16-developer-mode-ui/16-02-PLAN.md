---
phase: 16-developer-mode-ui
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - composer.json
  - config/app.php
  - config/nativephp.php
  - Modules/Core/Internal/Console/BackupDatabaseCommand.php
  - Modules/Core/Internal/Console/RestoreDatabaseCommand.php
  - Modules/Core/Internal/Console/FailedJobsCommand.php
  - Modules/Core/Internal/Console/InstallCommand.php
  - Modules/Core/Internal/Console/DoctorCommand.php
  - Modules/Auth/Internal/Console/ResetPasswordCommand.php
  - Modules/Ledger/Internal/Console/RederiveFingerprintsCommand.php
  - Modules/Auth/Public/Actions/ImpersonateUserAction.php
  - Modules/Auth/Public/Actions/EndImpersonationAction.php
  - Modules/Auth/Internal/Http/Middleware/ImpersonationBannerMiddleware.php
  - Modules/Auth/Resources/views/partials/impersonation-banner.blade.php
  - Modules/Auth/tests/Feature/ImpersonationBannerTest.php
  - Modules/Auth/tests/Feature/ImpersonationActionTest.php
  - Modules/Auth/tests/Feature/CrossUserIsolationTest.php
  - Modules/Auth/Routes/web.php
  - Modules/Desktop/Internal/Native/AppMenuBuilder.php
  - tests/Contracts/BoundaryArchTest.php
  - tests/Feature/BeatraxCommandsResolveTest.php
autonomous: true
requirements: []
mvp_mode: true

must_haves:
  truths:
    - "Every former diederik:* command resolves under beatrax:* (no diederik:* signature remains)"
    - "composer.json name is beatrax/beatrax; macOS bundle id in config/nativephp.php is com.beatrax.*"
    - "The Impersonate / EndImpersonation actions + middleware + Blade partial + two test files are deleted; CrossUserIsolationTest has its impersonation assertions removed but otherwise stays green"
    - "BoundaryArchTest::noDiederikLiteralAfterRename is green — no diederik / Diederik literal in Modules/* or tests/* (excluding documented allow-list entries)"
    - "BoundaryArchTest::impersonationSurfaceRemoved is green — the four impersonation class FQCNs are absent from Modules/Auth/"
    - "The full Pest suite stays green between every commit in this plan"
  artifacts:
    - path: "composer.json"
      provides: "package name flipped to beatrax/beatrax"
      contains: "beatrax/beatrax"
    - path: "tests/Feature/BeatraxCommandsResolveTest.php"
      provides: "Proves every renamed command resolves under its beatrax:* signature"
      contains: "beatrax:doctor"
    - path: "tests/Contracts/BoundaryArchTest.php"
      provides: "Two new invariants: noDiederikLiteralAfterRename + impersonationSurfaceRemoved"
      contains: "noDiederikLiteralAfterRename"
  key_links:
    - from: "Modules/Core/Internal/Console/DoctorCommand.php"
      to: "console kernel"
      via: "protected dollar-signature 'beatrax:doctor'"
      pattern: "beatrax:doctor"
    - from: "tests/Contracts/BoundaryArchTest.php"
      to: "Modules/* file tree"
      via: "noDiederikLiteralAfterRename grep over recursive iterator"
      pattern: "noDiederikLiteralAfterRename"
---

<objective>
Flip every `diederik` literal in source / tests / Blade / `.planning` / configs to `beatrax`, rename every `diederik:*` artisan signature to `beatrax:*`, change the composer package name + macOS bundle id + Herd hostname env, and in the same plan delete the entire Phase 12 "Act as partner" impersonation surface (D-11). Bind both invariants into `BoundaryArchTest` so regressions are caught at PR time.

Purpose: Every subsequent Phase 16 plan registers commands and writes copy under the post-rename names; landing this before the DevMode module skeleton (16-03) means D-15's DevCommandRegistry whitelist is authored with `beatrax:*` names from the start instead of being rewritten mid-phase. The impersonation deletion piggybacks because it is the same find/grep/rewrite workflow and the impersonation Blade include site was already excised by 16-01.

Output: A fully-renamed codebase with the impersonation surface gone, two arch invariants locking the result, a feature test proving every renamed command is callable, and the developer's local `.env` / Herd hostname / `.app` bundle documented as manual cut-over steps per D-09 (no upgrade-migration code).

## Phase Goal

**As a** developer working on the diederik codebase, **I want to** see every `diederik` literal flipped to `beatrax` (commands, env var, bundle id, hostname, composer name) AND the dropped impersonation surface fully removed, **so that** every downstream Phase 16 plan can author DevCommandRegistry entries, audit-log copy, and UI labels under the post-rename names without re-doing the work in a polish pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/16-developer-mode-ui/16-CONTEXT.md
@.planning/phases/16-developer-mode-ui/16-RESEARCH.md
@.planning/phases/16-developer-mode-ui/16-PATTERNS.md
@.planning/phases/16-developer-mode-ui/16-01-SUMMARY.md

<interfaces>
Locked decisions consumed (verbatim from CONTEXT.md):
- D-08: Full rename scope. composer `name`, every `diederik:*` artisan signature to `beatrax:*`, `DIEDERIK_DEV_MODE` to `BEATRAX_DEV_MODE` (the `config('app.dev_mode')` KEY itself stays unchanged — only the env var name flips), `config/nativephp.php` env pass-through list, macOS bundle id (`com.diederik.*` to `com.beatrax.*`), Herd hostname (`diederik.test` to `beatrax.test`), every literal `diederik` / `Diederik` in `.planning/*`, tests, system_alerts copy, Blade views, log channel name.
- D-09: No upgrade migration code. Clean cut; the developer hand-edits their own .env + Herd link as part of landing the plan.
- D-10: Typed-app-name string is `beatrax` (exact lowercase, case-sensitive). Already used in pre-rename plan copy (16-01 brand row).
- D-11: Drop the entire impersonation surface. Files / symbols to remove (full set in Task 3 below).

Renamed-command roster (post-rename names — D-12 + D-13 + research-discovered):
- `beatrax:doctor` (was `diederik:doctor`) — Modules/Core/Internal/Console/DoctorCommand.php
- `beatrax:failed-jobs` (was `diederik:failed-jobs`) — Modules/Core/Internal/Console/FailedJobsCommand.php
- `beatrax:reset-password` (was `diederik:reset-password`) — Modules/Auth/Internal/Console/ResetPasswordCommand.php
- `beatrax:rederive-fingerprints` (was `diederik:rederive-fingerprints`) — Modules/Ledger/Internal/Console/RederiveFingerprintsCommand.php
- `beatrax:install` (was `diederik:install`) — Modules/Core/Internal/Console/InstallCommand.php
- `db:backup` / `db:restore` are Laravel-native command names — NOT flipped.
- D-13 also lists `beatrax:grant-dev` + `beatrax:regenerate-recovery-codes`. The planner's discretion per CONTEXT D-13 is to verify these commands exist; if absent, scope their creation into the downstream artisan-runner plan 16-04 rather than this rename plan. The grep sweep in Task 1 surfaces whichever exist today.

The compositional CARE-TO point per CONTEXT D-08:
- `DIEDERIK_DEV_MODE` env var name flips to `BEATRAX_DEV_MODE`.
- The `config('app.dev_mode')` KEY stays unchanged so every consumer keeps working.
- `config/app.php` line that reads `env('DIEDERIK_DEV_MODE')` flips to `env('BEATRAX_DEV_MODE')`.

Find/replace strategy (planner's call per D-08 / Discretion):
- Per-area commits keep the suite green between commits and make the diff reviewable. Suggested ordering:
  1. composer.json `name` + run `composer dump-autoload` (no semantic change).
  2. Console command signatures + every in-repo `$this->artisan('diederik:...')` test call + add the BeatraxCommandsResolveTest skeleton.
  3. `config/nativephp.php` env pass-through list + bundle id + the env name flip in `config/app.php`.
  4. Modules/Desktop/Internal/Native/AppMenuBuilder.php verbatim consts.
  5. Every remaining literal `diederik` / `Diederik` in `Modules/*` / `tests/*` / `resources/*` / config / `.planning/` that the grep sweep surfaces.
  6. Add the `noDiederikLiteralAfterRename` arch invariant (it goes green only after step 5 finishes — keep it skipped or marked `->todo()` until then if it would fail the gate).
  7. Impersonation deletion (Task 3 below).

Wave-0 dependency: 16-01 already removed the impersonation-banner include site from `app.blade.php`. The Blade partial file + everything else stays until this plan deletes it. The two arch invariants are net-new — added in Task 4.

Test-callsite sweep:
- Grep for `'diederik:'` (note the colon) to catch every `$this->artisan('diederik:foo')` in tests; flip them all in the same commit that flips the corresponding command signature so the suite stays green per commit.

`.planning/*` content:
- Per CONTEXT D-08, `.planning/*.md` files are in scope. The grep sweep finds them all; the find/replace flips them to `beatrax`. Note that this plan and the other 16-* plans already use `beatrax` in copy (D-10 lock) — only older phase notes (12-*, 14-*, 15-*) need the flip. Cross-check with the user via the SUMMARY's "Hand-off Notes" section before bulk-flipping any older SUMMARY.

The Herd hostname + macOS bundle id + LaunchAgent plists are runtime artifacts the developer hand-cuts over per D-09. Document the cut-over checklist in this plan's SUMMARY's "Hand-off Notes" section.

Runtime State Inventory (RESEARCH § Runtime State Inventory):
- `system_alerts` rows written under the old name retain `diederik` content. Per D-09, that is acceptable (no migration code); new rows render `beatrax`. Document the known-issue in SUMMARY.

Allow-list strategy for the `noDiederikLiteralAfterRename` invariant:
- A small allow-list is unavoidable: this plan's own SUMMARY will reference "the rename from `diederik` to `beatrax`", historical phase SUMMARYs may retain `diederik` for historical accuracy, and this plan + the arch test itself need to use `diederik` literally to assert the absence. Bake an explicit allow-list of file paths in the invariant: e.g. `tests/Contracts/BoundaryArchTest.php` (the invariant body), `.planning/phases/16-developer-mode-ui/16-02-PLAN.md`, `.planning/phases/16-developer-mode-ui/16-02-SUMMARY.md`. Keep the list tight; rejection on any drift.

DI-only + GSD-agnostic project rules to honor:
- No facades / global helpers introduced by the rename.
- No `.planning/` / `D-NN` references introduced into source/comments/PHPDocs (this rule still applies to the new arch invariant code: keep its PHPDoc focused on the invariant's behavior, never the planning rationale).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename composer + every diederik artisan signature + every test artisan call</name>
  <files>composer.json, Modules/Core/Internal/Console/BackupDatabaseCommand.php, Modules/Core/Internal/Console/RestoreDatabaseCommand.php, Modules/Core/Internal/Console/FailedJobsCommand.php, Modules/Core/Internal/Console/InstallCommand.php, Modules/Core/Internal/Console/DoctorCommand.php, Modules/Auth/Internal/Console/ResetPasswordCommand.php, Modules/Ledger/Internal/Console/RederiveFingerprintsCommand.php, tests/Feature/BeatraxCommandsResolveTest.php</files>
  <behavior>
    - Test 1: `Artisan::all()` contains every renamed key (`beatrax:doctor`, `beatrax:failed-jobs`, `beatrax:reset-password`, `beatrax:rederive-fingerprints`, `beatrax:install`). Drive via a Pest dataset over the renamed-command roster from the &lt;interfaces&gt; block.
    - Test 2: `Artisan::all()` contains NO key starting with `diederik:` (regression guard against accidental re-introduction).
    - Test 3: `composer.json` `name` field reads `beatrax/beatrax`.
  </behavior>
  <action>
    Run a comprehensive grep first to inventory every site, then commit per area to keep the suite green at every commit boundary:

    Step 1: composer.json — flip the `name` field from `diederik/diederik` to `beatrax/beatrax`. Run `composer dump-autoload`. Run full Pest suite — should stay green (the package name has no semantic effect on tests).

    Step 2: Every `protected dollar-signature = 'diederik:...'` in `Modules/**/Internal/Console/*.php`. Grep `-rn` for the literal `'diederik:` across `Modules/`, flip each signature to `'beatrax:` while keeping the rest of the signature intact (args, options unchanged). The known roster is in the &lt;interfaces&gt; block; the grep is authoritative for any additions.

    Step 3: Every `$this->artisan('diederik:...'` in `tests/` AND in any `Modules/**/tests/` directory. Grep for `artisan('diederik:` and flip each. Commit alongside the corresponding signature flip so the suite stays green at every commit.

    Step 4: Every PHPDoc / comment / string referencing `diederik:doctor` / `diederik:install` / etc. in production code paths. Flip them all.

    Step 5: Author `tests/Feature/BeatraxCommandsResolveTest.php` with the three behaviors above. Use `Illuminate\Support\Facades\Artisan` from inside Pest tests — Artisan facade usage inside `tests/` is permitted (the facade ban applies to module code, not tests; verify against the existing test corpus pattern by grepping for `Artisan::` in tests).
  </action>
  <verify>
    <automated>cd /Users/wesselverheij/Development/diederik &amp;&amp; composer dump-autoload &amp;&amp; vendor/bin/pest --filter=BeatraxCommandsResolveTest &amp;&amp; vendor/bin/pest --parallel</automated>
  </verify>
  <done>composer.json name flipped; every diederik:* command signature flipped to beatrax:*; every test artisan-call literal flipped; BeatraxCommandsResolveTest passes (3 assertions); full Pest suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Flip env / bundle id / Herd hostname / brand literals across configs + Blade + planning docs</name>
  <files>config/app.php, config/nativephp.php, Modules/Desktop/Internal/Native/AppMenuBuilder.php, resources/views/layouts/app.blade.php, Modules/Core/Resources/views/livewire/app-sidebar.blade.php</files>
  <action>
    Continue the rename per-area. After each step, run the Pest suite + Larastan to confirm no regression.

    Step 1 — config/app.php: flip `env('DIEDERIK_DEV_MODE', ...)` to `env('BEATRAX_DEV_MODE', ...)`. The `config('app.dev_mode')` KEY stays unchanged — only the source env-var name flips.

    Step 2 — config/nativephp.php: in the env pass-through array, flip `DIEDERIK_DEV_MODE` to `BEATRAX_DEV_MODE`. In the macOS bundle id (likely `app_id` or `bundle_id` key), flip `com.diederik.*` to `com.beatrax.*`. Search the file for any other `diederik` literal and flip.

    Step 3 — Modules/Desktop/Internal/Native/AppMenuBuilder.php: read the file first to find the verbatim-label constants. Flip the "About diederik" literal to "About beatrax", flip `GITHUB_REPO_URL` from a `github.com/.../diederik` URL to a `github.com/.../beatrax` URL, flip `REPORT_ISSUE_URL` similarly. PATTERNS.md notes these constants are public class consts; the change is mechanical.

    Step 4 — resources/views/layouts/app.blade.php: any remaining `diederik` literal in the `<title>` fallback, meta tags, or comments to `beatrax`. (16-01 already flipped the `<title>` fallback as a precondition; this step is a confirmation grep.)

    Step 5 — Modules/Core/Resources/views/livewire/app-sidebar.blade.php: brand row text — 16-01 already uses `beatrax`; this is a confirmation grep.

    Step 6 — Grep `.planning/` for `diederik` and `Diederik`. Suggested grep: `grep -rln "diederik\|Diederik" .planning/ | grep -v "/16-"` to surface candidate files. Cross-reference with the developer before bulk-flipping any phase SUMMARY (Discretion item per CONTEXT D-08); for the rename plan + arch invariant + own SUMMARY, the literals stay (they belong to the allow-list noted in the &lt;interfaces&gt; block).

    Step 7 — Hand-off Notes for the SUMMARY (these go INTO 16-02-SUMMARY.md per the &lt;output&gt; block; not into any runtime artifact):
      - Edit local `.env`: rename `DIEDERIK_DEV_MODE=true` to `BEATRAX_DEV_MODE=true`.
      - Run `herd unlink diederik && herd link beatrax` from the project root (or edit `~/.config/herd/...`).
      - Uninstall the old `.app` bundle from `/Applications/diederik.app`. Next `php artisan native:build` produces `/Applications/beatrax.app`.
      - Re-register any `~/Library/LaunchAgents/com.diederik.*` plists as `com.beatrax.*` if the developer set them up per Phase 15-03.
      - `system_alerts` rows written under the old name retain `diederik` content (D-09: no migration). New rows render `beatrax`. Known-issue, intentional.

    Verify: `vendor/bin/pest --parallel && vendor/bin/phpstan analyse --memory-limit=2G` stays green.
  </action>
  <verify>
    <automated>cd /Users/wesselverheij/Development/diederik &amp;&amp; vendor/bin/pest --parallel &amp;&amp; vendor/bin/phpstan analyse --memory-limit=2G</automated>
  </verify>
  <done>config/app.php sources BEATRAX_DEV_MODE; config/nativephp.php uses com.beatrax.* bundle id; AppMenuBuilder consts flipped; every diederik literal in resources/views, Modules/Desktop, and config flipped; Pest + Larastan green.</done>
</task>

<task type="auto">
  <name>Task 3: Delete impersonation surface + arch invariants for rename + impersonation</name>
  <files>Modules/Auth/Public/Actions/ImpersonateUserAction.php, Modules/Auth/Public/Actions/EndImpersonationAction.php, Modules/Auth/Internal/Http/Middleware/ImpersonationBannerMiddleware.php, Modules/Auth/Resources/views/partials/impersonation-banner.blade.php, Modules/Auth/tests/Feature/ImpersonationBannerTest.php, Modules/Auth/tests/Feature/ImpersonationActionTest.php, Modules/Auth/tests/Feature/CrossUserIsolationTest.php, Modules/Auth/Routes/web.php, tests/Contracts/BoundaryArchTest.php</files>
  <action>
    Delete the impersonation surface per D-11, then add the two new arch invariants. The Blade include site was already removed by 16-01; everything else gets deleted here.

    Step 1 — Delete entire files:
      - Modules/Auth/Public/Actions/ImpersonateUserAction.php
      - Modules/Auth/Public/Actions/EndImpersonationAction.php
      - Modules/Auth/Internal/Http/Middleware/ImpersonationBannerMiddleware.php
      - Modules/Auth/Resources/views/partials/impersonation-banner.blade.php
      - Modules/Auth/tests/Feature/ImpersonationBannerTest.php
      - Modules/Auth/tests/Feature/ImpersonationActionTest.php

    Step 2 — Edit Modules/Auth/tests/Feature/CrossUserIsolationTest.php — read the file, find the impersonation-related `it(...)` blocks (search for `Impersonate`, `impersonating`, `original_user_id`, `original_username`), delete those blocks only, keep the rest of the file intact. The remaining tests in the file must stay green.

    Step 3 — Edit Modules/Auth/Routes/web.php — remove the impersonation route entries (grep for `ImpersonateUserAction`, `EndImpersonationAction`, `ImpersonationResult`). Delete the route definitions AND the now-unused `use` statements at the top.

    Step 4 — Edit tests/Contracts/BoundaryArchTest.php:
      (a) Delete the `noAuthFacadeOrHelper` allow-list entry referencing `ImpersonationBannerMiddleware` (approx line 1038 per PATTERNS.md). Do NOT touch the surrounding `it(...)` block.
      (b) Add the new `impersonationSurfaceRemoved` invariant — Pest `it('removes the impersonation surface entirely')` block that asserts each of the four FQCNs (`Modules\Auth\Public\Actions\ImpersonateUserAction`, `Modules\Auth\Public\Actions\EndImpersonationAction`, `Modules\Auth\Internal\Http\Middleware\ImpersonationBannerMiddleware`, `Modules\Auth\Resources\views\partials\impersonation-banner`) does NOT exist via `class_exists()` for the class FQCNs and via `file_exists()` for the Blade partial. Model after the existing `noNativePhpImportsOutsideDesktopModule` shape (PATTERNS.md line 1260 reference).
      (c) Add the new `noDiederikLiteralAfterRename` invariant — Pest `it('does not allow diederik / Diederik literal in source')` block. Iterate every file under `Modules/`, `tests/`, `resources/`, and `config/` via `RecursiveIteratorIterator(RecursiveDirectoryIterator(...))`. For each file, check whether its contents contain the regex `/diederik|Diederik/i`. Maintain an allow-list of expected hits (the arch test's own file path, this plan's PLAN.md, this plan's SUMMARY.md). Fail the test with a clear message listing every offending file. Use `grep -v '^[[:space:]]*//'` style comment filtering ONLY if necessary; preferred form is the full literal match because rename-residue inside a comment is still a rename residue.

    Step 5 — Search the entire `Modules/Auth/` tree for any remaining reference to `auth.impersonating.original_user_id` / `auth.impersonating.original_username` / `ImpersonateUserAction` / `EndImpersonationAction` / `ImpersonationBannerMiddleware` / `ImpersonationResult` (`grep -rn` on those strings). Any hit must be either justified (e.g. inside a now-stale comment that should also be removed) or deleted. Document any survivors in the SUMMARY for a follow-up.

    Step 6 — Run `vendor/bin/pest --parallel` — full suite stays green. Run `vendor/bin/phpstan analyse --memory-limit=2G` — Larastan stays clean. Both new invariants are green. Fix any imports or references the deletions broke.
  </action>
  <verify>
    <automated>cd /Users/wesselverheij/Development/diederik &amp;&amp; vendor/bin/pest --parallel &amp;&amp; vendor/bin/phpstan analyse --memory-limit=2G &amp;&amp; vendor/bin/pest --filter='noDiederikLiteralAfterRename|impersonationSurfaceRemoved'</automated>
  </verify>
  <done>Impersonation surface deleted (4 files + partial Blade + assertions trimmed from CrossUserIsolationTest + route entries removed + allow-list entry removed); both arch invariants green; full Pest suite green; Larastan clean; the grep for impersonation FQCNs in Modules/ returns nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var → config | `BEATRAX_DEV_MODE` flows from the developer's local `.env` into `config('app.dev_mode')`. Renaming the source env var does not change the trust model — the value still controls dev-mode-only feature gates (Horizon iframe, DevMode middleware in 16-03). |
| impersonation removal | Removing the entire "Act as partner" surface eliminates a Phase 12 elevation-of-privilege seam outright. Net positive on the security posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | rename find/replace | mitigate | The two arch invariants (`noDiederikLiteralAfterRename` + `impersonationSurfaceRemoved`) lock the result at PR time. The BeatraxCommandsResolveTest feature test verifies behavior, not just presence. Suite stays green between every commit. |
| T-16-05 | Elevation of Privilege | (deleted) Impersonation actions | mitigate | The Impersonate / EndImpersonation actions + the session-key state are deleted entirely; there is no reachable code path. The `impersonationSurfaceRemoved` invariant prevents accidental re-introduction. |
| T-16-06 | Information Disclosure | system_alerts rows authored under `diederik` | accept | Per D-09, no migration code. Old alert bodies retain the literal; new rows render `beatrax`. Acceptable trade-off because (a) the alerts are only visible to authenticated users on their own machine, and (b) writing a migration introduces more risk than it avoids. Documented in SUMMARY hand-off notes. |
| T-16-07 | Tampering | Developer's local `.env` flip | accept | The developer hand-edits their own `.env` per D-09. No automated migration. The Pest suite + Larastan + visual smoke test catches the case where the dev forgets to rename (Dev Console gates flip off when the env var is unset). |
</threat_model>

<verification>
- `vendor/bin/pest --filter=BeatraxCommandsResolveTest` — 3 assertions green.
- `vendor/bin/pest --filter='noDiederikLiteralAfterRename|impersonationSurfaceRemoved'` — both new invariants green.
- `vendor/bin/pest --parallel` — full suite green.
- `vendor/bin/phpstan analyse --memory-limit=2G` — Larastan L10 strict clean.
- `vendor/bin/pint --test` — Pint clean.
- `grep -rn "ImpersonateUserAction\|EndImpersonationAction\|ImpersonationBannerMiddleware\|auth.impersonating.original" Modules/ tests/` — returns nothing (defense-in-depth check on the deletion).
- `php artisan list | grep diederik` — returns nothing.
- `php artisan list | grep beatrax` — returns every renamed command.
- Manual smoke test (post-cut-over): `composer dev` after the developer hand-edits their `.env` to `BEATRAX_DEV_MODE=true`; the dashboard still loads, the sidebar (from 16-01) still renders with the `beatrax` brand row, no impersonation banner appears.
</verification>

<success_criteria>
- composer.json `name` is `beatrax/beatrax`; `composer install` works locally.
- Every former `diederik:*` artisan signature is now `beatrax:*`; `php artisan list` confirms.
- Every `$this->artisan('diederik:...')` test call is now `'beatrax:...'`; the suite stays green at every commit boundary.
- `config/app.php` sources `BEATRAX_DEV_MODE`; `config/nativephp.php` env pass-through + bundle id are `BEATRAX_*` / `com.beatrax.*`.
- AppMenuBuilder verbatim consts (`HELP_ABOUT`, GITHUB URLs) are `beatrax`.
- The impersonation surface is gone: 4 files deleted + Blade partial deleted + CrossUserIsolationTest's impersonation assertions removed + route entries removed + BoundaryArchTest allow-list entry removed.
- Two arch invariants are green: `noDiederikLiteralAfterRename` + `impersonationSurfaceRemoved`.
- SUMMARY documents the manual cut-over steps for `.env`, Herd hostname, `.app` bundle, LaunchAgent plists (D-09).
- Pest + Larastan + Pint all green.
</success_criteria>

<output>
Create `.planning/phases/16-developer-mode-ui/16-02-SUMMARY.md` when done. The SUMMARY MUST include the "Hand-off Notes" section enumerating the four manual cut-over steps (D-09) so the next developer landing the plan knows to edit `.env`, re-link Herd, uninstall the old `.app`, and re-register LaunchAgent plists.
</output>
