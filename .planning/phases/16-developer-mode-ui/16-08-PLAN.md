---
phase: 16-developer-mode-ui
plan: 08
type: execute
wave: 6
depends_on: [16-04, 16-05, 16-06, 16-07]
files_modified:
  - Modules/DevMode/Internal/Navigation/NavigationRegistryImpl.php
  - Modules/DevMode/Internal/Navigation/AppActionRegistryImpl.php
  - Modules/DevMode/Public/Dto/NavigationEntry.php
  - Modules/DevMode/Public/Dto/AppAction.php
  - Modules/DevMode/Internal/Http/Livewire/CommandPaletteModal.php
  - Modules/DevMode/Resources/views/livewire/command-palette-modal.blade.php
  - Modules/DevMode/Providers/DevModeServiceProvider.php
  - Modules/Desktop/Internal/Native/AppMenuBuilder.php
  - Modules/Core/Resources/views/livewire/app-sidebar.blade.php
  - Modules/Core/Internal/Http/Livewire/AppSidebar.php
  - resources/views/layouts/app.blade.php
  - Modules/DevMode/Resources/views/layouts/dev-shell.blade.php
  - Modules/DevMode/tests/Feature/CommandPaletteRegistryTest.php
  - Modules/DevMode/tests/Feature/AppMenuDeveloperSubmenuTest.php
  - Modules/Core/tests/Feature/AppSidebarDevBlockLiveDataTest.php
autonomous: true
requirements: [DEVUI-09]
mvp_mode: true

must_haves:
  truths:
    - "⌘K on macOS / Ctrl+K on Windows-Linux opens the command palette modal from any page (Alpine global keybind on base layout body — D-42)"
    - "⌘. opens the Dev Console at /dev (or focuses if already there) — D-42 extra keybind"
    - "The palette JSON registry emitted server-side contains every authenticated nav view (NavigationRegistry); + every SAFE-tier dev command (DevCommandRegistry — devs only); + every named app action (AppActionRegistry). DESTRUCTIVE-tier commands are excluded from the palette (D-41 — prevent muscle-memory disasters)"
    - "A non-developer's palette JSON contains ONLY the nav source (no DEV rows; no DESTRUCTIVE)"
    - "Fuse.js fuzzy ranking with the locked weights (label 0.65, hint 0.20, keywords 0.15; threshold 0.35, ignoreLocation true)"
    - "The sidebar Dev block in the main-app sidebar now shows LIVE data: 'Queue {N} · Worker {N}s ago' (reads cache from 16-04 heartbeat + 16-06 models); 16-01's static placeholders are replaced"
    - "The NativePHP app menu has a Developer submenu (only for is_developer=true) with 'Open Dev Console' (⌘.) and '⌘K Run a command' entries (Phase 16 menu integration — extends Modules/Desktop AppMenuBuilder)"
    - "Recent shortcuts: last 5 deduplicated palette selections per user cached in `dev_mode.palette_recent.{userId}` (TTL 30d, per UI-SPEC)"
  artifacts:
    - path: "Modules/DevMode/Internal/Http/Livewire/CommandPaletteModal.php"
      provides: "Global Livewire palette modal mounted in base layout + dev-shell"
      contains: "palette:open"
    - path: "Modules/DevMode/Internal/Navigation/NavigationRegistryImpl.php"
      provides: "Concrete NavigationRegistry replacing 16-03's Null binding"
      contains: "Dashboard|Transactions|Settings"
    - path: "Modules/DevMode/Internal/Navigation/AppActionRegistryImpl.php"
      provides: "Concrete AppActionRegistry; entries from Phase 15 app menu + others"
      contains: "AppAction"
    - path: "Modules/Desktop/Internal/Native/AppMenuBuilder.php"
      provides: "Modified to add Developer submenu when is_developer (Phase 16 menu integration)"
      contains: "Open Dev Console"
  key_links:
    - from: "resources/views/layouts/app.blade.php"
      to: "Modules\\DevMode\\Internal\\Http\\Livewire\\CommandPaletteModal"
      via: "Alpine x-data on <body> dispatches palette:open Livewire event"
      pattern: "palette:open"
    - from: "Modules/Core/Resources/views/livewire/app-sidebar.blade.php"
      to: "cache.dev_mode.queue_worker_heartbeat + Job::count() + FailedJob::count()"
      via: "AppSidebar component now reads heartbeat + queue counts via method-DI"
      pattern: "queue_worker_heartbeat"
---

<objective>
Land the entire command palette ecosystem: NavigationRegistry + AppActionRegistry concrete implementations (replacing 16-03's Null bindings), the global Livewire CommandPaletteModal mounted in both the main-app + dev-shell layouts, the Alpine `x-data` global keybind handler on `<body>` (⌘K / Ctrl+K to open palette; ⌘. to open Dev Console — D-42), Fuse.js client-side fuzzy ranking with the locked weights + threshold + ignoreLocation (D-40), the server-emitted JSON registry shape merging nav + dev (SAFE only, devs only) + actions (D-41), per-user Recent-shortcuts cache (5 entries; 30d TTL per UI-SPEC), the NativePHP app-menu Developer submenu extension (only when is_developer; Phase 16 menu integration), and the sidebar Dev-block live-data refresh (replacing 16-01's static "Queue 0 · Worker —" placeholders with the real cache-backed reads). Final Phase 16 polish.

Purpose: DEVUI-09 fully landed; Phase 16 completes. The palette becomes the *primary* command-entry point per sketch findings; the sidebar Dev block shows live system health at a glance; the app menu surfaces developer affordances natively. After this plan, every Phase 16 requirement (DEVUI-01..09) is satisfied; the only thing left is the verification gate.

Output: A working ⌘K palette on every authenticated page; the sidebar Dev block lit with live data; the app-menu Developer submenu visible for is_developer users. The phase is shippable.

## Phase Goal

**As a** developer in the diederik desktop app, **I want to** press ⌘K from any page and see a Linear/Raycast-style fuzzy-searchable palette covering every authenticated view + every SAFE-tier dev command + every named app action, AND see at a glance from the main-app sidebar's Dev block whether the queue is healthy + when the worker last ticked, AND find a Developer submenu in the native macOS menu bar with quick access to the Dev Console, **so that** the entire Phase 16 surface is wired end-to-end and the dev console feels like a deliberate room inside the same building (sketch-findings language) rather than a bolted-on extra.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/16-developer-mode-ui/16-CONTEXT.md
@.planning/phases/16-developer-mode-ui/16-RESEARCH.md
@.planning/phases/16-developer-mode-ui/16-PATTERNS.md
@.planning/phases/16-developer-mode-ui/16-UI-SPEC.md
@.planning/phases/16-developer-mode-ui/16-04-SUMMARY.md
@.planning/phases/16-developer-mode-ui/16-06-SUMMARY.md
@.planning/phases/16-developer-mode-ui/16-07-SUMMARY.md
@.claude/skills/sketch-findings-diederik/SKILL.md

<interfaces>
Locked decisions:
- D-40: Tech stack — Livewire 4 modal triggered by global Alpine x-data keybind handler on base layout body. Fuse.js (~12KB single CDN-able file, but per CLAUDE.md local-only constraint loaded via Vite bundle — installed in 16-03). Flux components for modal chrome.
- D-41: Sources — NavigationRegistry, DevCommandRegistry (SAFE only, devs only — DESTRUCTIVE excluded), AppActionRegistry. Each row: `[label, icon, hint, url|handler]`.
- D-42: Keybind handler — single `x-data` on `<body>`; `event.metaKey || event.ctrlKey` (macOS + Win/Linux parity).

PATTERNS.md analogs:
- `Modules/Receipts/Internal/MatcherRegistry.php` (58 LoC) — concrete-registry shape for NavigationRegistryImpl + AppActionRegistryImpl.
- `Modules/Categorization/Internal/Http/Livewire/RuleFormModal.php` (192 LoC) — Flux modal + Livewire.dispatch open pattern (Pattern H).
- `Modules/Desktop/Internal/Native/AppMenuBuilder.php` (94 LoC) — verbatim-label public consts; `build(): list<MenuItem>`. Inject CurrentUser to gate the Developer submenu on is_developer.

Cross-module nav-registry-population pattern:
- Each module that owns a view contributes its entry to NavigationRegistry. Two valid mechanisms:
  (a) Each module registers its entries from its own ServiceProvider via `app(NavigationRegistry::class)->register(...)`.
  (b) NavigationRegistryImpl reads from a central array.
- Recommendation: (a) per Phase 15 menu-extension pattern (Modules/Desktop's AppMenuBuilder). Each module file (Core/Auth/Ledger/Categorization/Chains/Forecasting/Recurring/Receipts/EmailScan/Import/Transfers/DriftAlerts) registers ITS OWN views via a one-time call in its provider boot. NavigationRegistryImpl is `final class` with `register(NavigationEntry $entry): void` + `all(): list<NavigationEntry>` — replaces NullNavigationRegistry binding from 16-03.

NavigationEntry DTO (16-03 stubbed; THIS plan creates the real shape):
- `label` (string) — visible label in the palette ("Dashboard", "Transactions", "Forecasts").
- `hint` (string) — secondary line ("Recent activity overview", "Browse transactions", "What-if scenarios").
- `icon` (string) — unicode glyph from the sketch icon set (◆, ≡, ›_, ~, etc.) per UI-SPEC.
- `url` (string) — route URL.
- `keywords` (list<string>) — extra fuzzy-match terms.

AppAction DTO:
- Same shape as NavigationEntry but uses `handlerEvent` (Livewire event name) OR `url` (one of them).

Registry roster — every authenticated app view (Phase 1-15 surfaces):
- Dashboard (route('dashboard'), icon: ◆)
- Transactions (route('transactions.index'), icon: ≡)
- Recurring (route('recurring.index'), icon: ↻)
- Chains (route('chains.index'), icon: ⇉)
- Forecasts (route('forecasts.index'), icon: ↗)
- Drift Alerts (route('drift-alerts.index'), icon: ⓘ)
- Imports (route('imports.index'), icon: ⌗)
- Receipts (route('receipts.index'), icon: ✓)
- Email Scan (route('email-scan.index'), icon: ⌕)
- Categorization (route('categorization.rules'), icon: ⊕)
- Settings (route('settings'), icon: ⚙)
- + the Dev Console sub-routes (Overview, Artisan, Audit, Logs, Queue, Doctor, SQL, Horizon — conditional, System) — registered by DevModeServiceProvider against the NavigationRegistry (devs-only filter applied at JSON-emit time, not at registry time).

AppActionRegistry roster (Phase 15 app-menu entries + others):
- "Scan email now" (Livewire event `email-scan.run`)
- "Run import" (URL `route('imports.create')`)
- "Open backups folder" (NativePHP shell action — TBD; if not yet implemented, skip or stub)
- "Toggle theme" (Livewire event `theme.cycle`)
- "Open profile" (URL `route('settings')` with anchor)
- + Phase 15-04 file-association intents if any are user-triggerable.

CommandPaletteModal Livewire shape:
- Mounted globally in BOTH `resources/views/layouts/app.blade.php` AND `Modules/DevMode/Resources/views/layouts/dev-shell.blade.php` (one instance per layout; both dispatch the same `palette:open` event).
- Public methods: `open()` (called by the Livewire event); `pickEntry(string $id)` (called from JS when an entry is selected — writes to Recent cache + navigates).
- `getRegistryJson(CurrentUser $user, NavigationRegistry $nav, DevCommandRegistry $commands, AppActionRegistry $actions): array` returns the merged list per RESEARCH § Common Op 3:
  ```
  $entries = [
      ...$nav->all()->map(fn ($e) => [...$e, 'source' => 'view']),
      ...($user->user()->is_developer
          ? $commands->safe()->map(fn ($c) => ['id' => 'dev.cmd.'.$c->name, 'label' => 'Run '.$c->name, 'hint' => $c->label, 'icon' => '›_', 'source' => 'dev', 'tier' => 'safe', 'url' => null, 'handler' => 'spawn-command'])
          : []),
      ...$actions->all()->map(fn ($a) => [...$a, 'source' => 'action']),
  ];
  ```
- The palette modal's Blade view embeds the JSON via `{{ Js::from($registry) }}` inside an Alpine x-data block; Fuse.js consumes it per RESEARCH § Common Op 4.

UI per UI-SPEC § Command palette + sketch-findings:
- Scrim: `<div class="scrim" style="background: rgba(8,12,20,0.45); backdrop-filter: blur(4px) saturate(120%);">`.
- Modal: 760px width at viewports ≥1100px; collapses to 640px below (Flux modal breakpoint handles).
- `.palette-input` row: ⌕ glyph + input + `<span class="kbd">esc</span>`.
- `.palette-rail` (180px on the left): category labels (View / Dev / Action) + a Recent divider with the user's last 5 picks.
- `.palette-row` (in the main area): icon · label/hint · source chip (`.palette-source` — `view` muted; `dev · safe` amber; `action` blue per UI-SPEC § Color) · `↩` kbd.
- `.palette-foot`: keybind hints (↑/↓ navigate, ↩ select, esc close) + `{N} results` right-aligned.
- Empty state: "Type to search views, commands, and actions. Press Esc to close." (verbatim per UI-SPEC).
- Selecting a `view` row → `window.location.href = entry.url`.
- Selecting an `action` row → `Livewire.dispatch(entry.handler)` OR navigate.
- Selecting a `dev` row → Livewire dispatches a `spawn-command` event to ArtisanRunnerPage with the command name + the ArgSpec rendered inline (small inline arg-form below the palette row — sketch-findings palette-dispatched runner pattern).

Fuse.js config per UI-SPEC § Component inventory:
- keys: [{ name: 'label', weight: 0.65 }, { name: 'hint', weight: 0.20 }, { name: 'keywords', weight: 0.15 }]
- threshold: 0.35
- ignoreLocation: true

Recent shortcuts cache:
- Cache key: `dev_mode.palette_recent.{userId}` — `list<string>` of last 5 entry IDs deduplicated, TTL 30d.
- Written from `pickEntry()` method.
- Read on mount() to seed the Recent rail divider.

Keybind handler — single x-data on `<body>`:
- Add to `resources/views/layouts/app.blade.php` body tag:
  ```
  <body x-data="{
      onKey(e) {
          if ((e.metaKey || e.ctrlKey) && e.key === 'k') { e.preventDefault(); Livewire.dispatch('palette:open'); }
          if ((e.metaKey || e.ctrlKey) && e.key === '.') { e.preventDefault(); window.location.href = '/dev'; }
      }
  }" x-on:keydown.window="onKey($event)">
  ```
- Add the same x-data to `Modules/DevMode/Resources/views/layouts/dev-shell.blade.php` body so the palette works inside /dev/* too.
- Modifier handling per D-42: `event.metaKey || event.ctrlKey` covers macOS + Win/Linux without special-casing.

Sidebar Dev block live data refresh:
- Edit `Modules/Core/Internal/Http/Livewire/AppSidebar.php` (16-01's component). Add method-DI on `render(CurrentUser $user, ViewFactory $views, CacheRepository $cache): View`. Compute:
  - `$workerHeartbeat = $cache->get('dev_mode.queue_worker_heartbeat')`.
  - `$queueCount = Job::count() + FailedJob::count()` (or a single composite count; UI-SPEC: "Queue {N}").
  - `$workerSecondsAgo = $workerHeartbeat ? (time() - $workerHeartbeat) : null`.
- Edit `Modules/Core/Resources/views/livewire/app-sidebar.blade.php` Dev block:
  - Replace 16-01's static "Queue 0 · Worker —" placeholder with `Queue {{ $queueCount }} · Worker {{ $workerSecondsAgo ? $workerSecondsAgo . 's ago' : '—' }}`.
  - Add `wire:poll.5s` on the Dev block subtree so the live values refresh.

NativePHP app-menu Developer submenu:
- Edit `Modules/Desktop/Internal/Native/AppMenuBuilder.php` (94 LoC). Add method-DI on `CurrentUser` to `build()` (read the existing signature to confirm it doesn't already inject). After the Help submenu, conditionally add:
  ```
  if ($this->currentUser->user()?->is_developer === true) {
      $menu[] = Menu::submenu('Developer', [
          Menu::link('Open Dev Console', '/dev', shortcut: 'Cmd+.'),
          Menu::link('⌘K Run a command', '/dev', shortcut: 'Cmd+K'),
      ]);
  }
  ```
  Verify the Menu facade methods + shortcut signature against the installed nativephp/desktop ^2.2.
- RESEARCH Pitfall 9: NativePHP app menu changes don't appear until full quit. Document in SUMMARY.
- DI-only carve-out: AppMenuBuilder is already on the BoundaryArchTest facade allow-list per PATTERNS.md (Native* facade allow-list). The CurrentUser DI is new — no facade addition needed.

NavigationRegistry registration mechanism:
- Each module's ServiceProvider gains a `boot()` line: `$this->app->extend(NavigationRegistry::class, function (NavigationRegistry $reg) { $reg->register(new NavigationEntry(...)); return $reg; });`. OR a centralised approach: declare the roster in DevModeServiceProvider.
- Recommendation: centralised approach for v1 (simpler, no cross-module ServiceProvider edits). Declare the roster inside `DevModeServiceProvider::register()` when binding NavigationRegistryImpl. The trade-off: any new view added in a future phase requires editing DevModeServiceProvider — but that's acceptable for now (Phase 19 REL might revisit).

Wave 6 file ownership:
- This plan modifies `resources/views/layouts/app.blade.php` (sole Wave 6 plan; no conflict).
- Modifies `Modules/DevMode/Resources/views/layouts/dev-shell.blade.php` (Wave 5 plans 16-05/06/07 modified this file but in different regions — sidebar nav-items via DevSidebarItems service; THIS plan adds the body Alpine x-data + the palette modal mount. Conflict-free.)
- Modifies `Modules/Core/Resources/views/livewire/app-sidebar.blade.php` (Wave 6 sole modifier).
- Modifies `Modules/Desktop/Internal/Native/AppMenuBuilder.php` (16-02 modified the same file for the rename; Wave 6 modifies the build() method — different region of the file).
- Modifies `Modules/DevMode/Providers/DevModeServiceProvider.php` (every Wave 5 plan also modifies this — sequential merge if conflict).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NavigationRegistry + AppActionRegistry concretes + CommandPaletteModal + Fuse.js integration + Recent cache</name>
  <files>Modules/DevMode/Internal/Navigation/NavigationRegistryImpl.php, Modules/DevMode/Internal/Navigation/AppActionRegistryImpl.php, Modules/DevMode/Public/Dto/NavigationEntry.php, Modules/DevMode/Public/Dto/AppAction.php, Modules/DevMode/Internal/Http/Livewire/CommandPaletteModal.php, Modules/DevMode/Resources/views/livewire/command-palette-modal.blade.php, Modules/DevMode/Providers/DevModeServiceProvider.php, Modules/DevMode/tests/Feature/CommandPaletteRegistryTest.php</files>
  <behavior>
    - Test 1: `app(NavigationRegistry::class)` resolves to NavigationRegistryImpl (NOT NullNavigationRegistry).
    - Test 2: `app(NavigationRegistry::class)->all()` returns the full ~11-entry main-app nav roster + the 9-entry Dev Console roster (when iterated; the dev entries are present in the registry but filtered at JSON-emit time).
    - Test 3 (palette JSON for developer): GET-mount the palette modal as is_developer=true; the emitted JSON contains: views (>=11 entries from nav), dev (>=9 SAFE-tier entries from DevCommandRegistry — 16-04's CommandRegistry binding), actions (>=2 entries from AppActionRegistry). NO `source: dev` row references a DESTRUCTIVE-tier command.
    - Test 4 (palette JSON for non-developer): GET-mount as is_developer=false; the emitted JSON contains ONLY `source: view` and `source: action` rows. ZERO `source: dev` rows.
    - Test 5 (Recent): `pickEntry('dashboard')` writes the entry ID to cache key `dev_mode.palette_recent.{userId}`; calling pickEntry again with the same ID does not duplicate; calling with 6 different IDs evicts the oldest.
  </behavior>
  <action>
    Step 1 — Create the DTOs:
      - `NavigationEntry` extends `Spatie\LaravelData\Data`. Fields per &lt;interfaces&gt; (label, hint, icon, url, keywords).
      - `AppAction` extends `Data`. Fields: label, hint, icon, handlerEvent OR url (xor-validated via the DTO), keywords.

    Step 2 — Create `NavigationRegistryImpl` per PATTERNS.md MatcherRegistry analog. `final class`. Constructor takes `array $entries`. `all(): list<NavigationEntry>` returns the array. `register(NavigationEntry $entry): void` appends for future cross-module wiring.

    Step 3 — Create `AppActionRegistryImpl` — same shape as NavigationRegistryImpl.

    Step 4 — In `DevModeServiceProvider::register()`, REPLACE the Null* bindings from 16-03:
      ```
      $this->app->singleton(NavigationRegistry::class, fn () => new NavigationRegistryImpl([
          new NavigationEntry(label: 'Dashboard', hint: 'Recent activity overview', icon: '◆', url: route('dashboard'), keywords: ['home', 'main']),
          new NavigationEntry(label: 'Transactions', hint: 'Browse transactions', icon: '≡', url: route('transactions.index'), keywords: ['txn']),
          ...  // full roster per &lt;interfaces&gt;
          // Dev Console sub-routes (filtered at JSON-emit time by is_developer)
          new NavigationEntry(label: 'Dev Overview', hint: 'System tiles', icon: '›_', url: route('dev.overview'), keywords: ['dev']),
          ...
      ]));

      $this->app->singleton(AppActionRegistry::class, fn () => new AppActionRegistryImpl([
          new AppAction(label: 'Scan email now', hint: 'Run email-scan immediately', icon: '⌕', handlerEvent: 'email-scan.run', keywords: ['email']),
          ...  // per &lt;interfaces&gt;
      ]));
      ```
      Wrap the dev-route NavigationEntries in `if (Route::has('dev.overview'))` to avoid blowing up on environments where DevMode isn't booted.

    Step 5 — Create `CommandPaletteModal` Livewire component. Method-DI on render + getRegistryJson per &lt;interfaces&gt;. The JSON-emit-time filter on dev entries:
      ```
      $entries = $nav->all();
      if (! $user->user()?->is_developer) {
          $entries = array_filter($entries, fn ($e) => $e->source !== 'dev');
      }
      // Or: don't put dev entries in NavigationRegistry at all; instead derive them from DevCommandRegistry at JSON-emit time only for developers.
      ```
      Pick the latter — it's cleaner: NavigationRegistry has nav-only; DevCommandRegistry SAFE-tier is mapped to palette rows at JSON-emit time only for is_developer.

    Step 6 — Create the Blade view per UI-SPEC § Component inventory § Command palette + sketch-findings. Full structure:
      ```
      <flux:modal name="palette" :dismissible="true" class="palette-modal">
          <div x-data="palette({{ Js::from($registry) }}, '{{ $recentJson }}')"
               class="scrim"
               x-on:keydown.window="onKey($event)">
              <div class="palette">
                  <div class="palette-input">
                      <span class="ic">⌕</span>
                      <input x-model="query" placeholder="Type to search views, commands, and actions. Press Esc to close.">
                      <span class="kbd">esc</span>
                  </div>
                  <div class="palette-body">
                      <aside class="palette-rail">
                          <div class="palette-rail-label">View</div>
                          <div class="palette-rail-label">Dev</div>
                          <div class="palette-rail-label">Action</div>
                          <div class="palette-rail-divider"></div>
                          <div class="palette-rail-label">Recent</div>
                          <template x-for="r in recent.slice(0, 5)">
                              <div class="palette-row palette-row--mini" x-text="r.label" @click="execute(r)"></div>
                          </template>
                      </aside>
                      <main class="palette-results">
                          <template x-for="(hit, i) in results.slice(0, 50)" :key="hit.item.id">
                              <div class="palette-row" :class="{ 'palette-row--active': i === activeIndex }" @click="execute(hit.item)">
                                  <span class="ic" x-text="hit.item.icon"></span>
                                  <div class="palette-row-text">
                                      <div class="palette-row-label" x-text="hit.item.label"></div>
                                      <div class="palette-row-hint" x-text="hit.item.hint"></div>
                                  </div>
                                  <span class="palette-source" :class="'palette-source--' + hit.item.source" x-text="hit.item.source"></span>
                                  <span class="kbd">↩</span>
                              </div>
                          </template>
                      </main>
                  </div>
                  <div class="palette-foot">
                      <span class="kbd">↑</span><span class="kbd">↓</span> navigate
                      <span class="kbd">↩</span> select
                      <span class="kbd">esc</span> close
                      <span x-text="results.length + ' results'"></span>
                  </div>
              </div>
          </div>
      </flux:modal>

      <script>
      window.palette = (registry, recentJson) => ({
          query: '',
          activeIndex: 0,
          recent: JSON.parse(recentJson || '[]'),
          fuse: new Fuse(registry, {
              keys: [{ name: 'label', weight: 0.65 }, { name: 'hint', weight: 0.20 }, { name: 'keywords', weight: 0.15 }],
              threshold: 0.35,
              ignoreLocation: true,
          }),
          get results() { return this.query ? this.fuse.search(this.query) : registry.map(item => ({ item })); },
          execute(item) { Livewire.dispatch('palette:picked', { id: item.id }); this.dispatchEntry(item); },
          dispatchEntry(item) {
              if (item.url) { window.location.href = item.url; }
              else if (item.handlerEvent) { Livewire.dispatch(item.handlerEvent); }
              this.$dispatch('modal-hide', { name: 'palette' });
          },
          onKey(e) {
              if (e.key === 'Escape') { this.$dispatch('modal-hide', { name: 'palette' }); }
              if (e.key === 'ArrowDown') { e.preventDefault(); this.activeIndex = Math.min(this.activeIndex + 1, this.results.length - 1); }
              if (e.key === 'ArrowUp') { e.preventDefault(); this.activeIndex = Math.max(this.activeIndex - 1, 0); }
              if (e.key === 'Enter' && this.results[this.activeIndex]) { e.preventDefault(); this.execute(this.results[this.activeIndex].item); }
          },
      });
      </script>
      ```
      The `<script>` block imports Fuse.js — wire via Vite's `import Fuse from 'fuse.js'` in `resources/js/app.js` so Fuse is on `window.Fuse` for the palette to consume. (Fuse.js was installed in 16-03's package.json edit.)

    Step 7 — Register CommandPaletteModal in `DevModeServiceProvider::boot()`:
      ```
      $livewire->component('dev.command-palette-modal', CommandPaletteModal::class);
      ```

    Step 8 — Implement `pickEntry(string $id, CurrentUser $user, CacheRepository $cache)` on CommandPaletteModal: read the existing list from `dev_mode.palette_recent.{userId}`, prepend the new ID, dedupe, cap at 5, write back with TTL 30 * 86400 seconds. Listen for `palette:picked` Livewire event from the JS dispatcher.

    Step 9 — Tests for the 5 behaviors. Use `Livewire::test(CommandPaletteModal::class)->actingAs(...)` for the JSON-emit assertions. For Recent cache test, assert the cache key contents after pickEntry calls.
  </action>
  <verify>
    <automated>cd /Users/wesselverheij/Development/diederik &amp;&amp; vendor/bin/pest --filter=CommandPaletteRegistry &amp;&amp; vendor/bin/pest --parallel</automated>
  </verify>
  <done>NavigationRegistryImpl + AppActionRegistryImpl bound; CommandPaletteModal emits correct JSON registry (devs see nav + dev SAFE + actions; non-devs see nav + actions); Fuse.js wired with locked weights; Recent cache works; 5 tests pass; Pest + Larastan green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Body keybind handler + palette mount in base layouts + sidebar Dev-block live data + app-menu Developer submenu</name>
  <files>resources/views/layouts/app.blade.php, Modules/DevMode/Resources/views/layouts/dev-shell.blade.php, Modules/Core/Resources/views/livewire/app-sidebar.blade.php, Modules/Core/Internal/Http/Livewire/AppSidebar.php, Modules/Desktop/Internal/Native/AppMenuBuilder.php, Modules/Core/tests/Feature/AppSidebarDevBlockLiveDataTest.php, Modules/DevMode/tests/Feature/AppMenuDeveloperSubmenuTest.php</files>
  <behavior>
    - Test 1 (keybind): the body tag in `resources/views/layouts/app.blade.php` has the x-data attribute with the keybind handler (assert via HTML grep on the rendered base layout).
    - Test 2 (keybind): the same x-data is present in dev-shell.blade.php.
    - Test 3 (palette mount): both layouts mount `@livewire('dev.command-palette-modal')` somewhere in the body.
    - Test 4 (sidebar live data): AppSidebar's rendered Dev block (for is_developer=true) shows "Queue 3" when Jobs count=2 + FailedJobs count=1, and "Worker 5s ago" when the cache key is now()-5.
    - Test 5 (sidebar live data): when cache key is absent, the Dev block shows "Worker —".
    - Test 6 (sidebar live data): the Dev block has `wire:poll.5s` attribute.
    - Test 7 (app menu): AppMenuBuilder->build() as is_developer=true contains a submenu titled "Developer" with two children "Open Dev Console" + "⌘K Run a command".
    - Test 8 (app menu): AppMenuBuilder->build() as is_developer=false does NOT contain a "Developer" submenu.
  </behavior>
  <action>
    Step 1 — Edit `resources/views/layouts/app.blade.php`:
      - Add the x-data + x-on:keydown.window per &lt;interfaces&gt; to the `<body>` tag.
      - Add `@livewire('dev.command-palette-modal')` before the closing `</body>` (after the Livewire scripts).

    Step 2 — Edit `Modules/DevMode/Resources/views/layouts/dev-shell.blade.php`:
      - Add the same x-data + x-on:keydown.window to its body.
      - Add `@livewire('dev.command-palette-modal')` mount.

    Step 3 — Edit `Modules/Core/Internal/Http/Livewire/AppSidebar.php` (16-01's component). Add `CacheRepository $cache` to the render() method-DI signature. Inject `Job::count() + FailedJob::count()` for the queue count (or just `Job::count()` — per UI-SPEC the Dev block shows "Queue {N}" without explicit split; pick the single count that matches the operator's intuition. Recommendation: Job::count() = pending only; failed jobs already show in queue inspector's failed tab. Document the choice in SUMMARY.) Compute `$workerSecondsAgo = ($ts = $cache->get('dev_mode.queue_worker_heartbeat')) ? (time() - $ts) : null`. Pass to view.

    Step 4 — Edit `Modules/Core/Resources/views/livewire/app-sidebar.blade.php` Dev block (16-01's static placeholder):
      - Replace `Queue 0 · Worker —` with `Queue {{ $queueCount }} · Worker {{ $workerSecondsAgo !== null ? $workerSecondsAgo . 's ago' : '—' }}`.
      - Wrap the entire Dev block (or the specific live-data row) with `<div wire:poll.5s>...</div>` so the live values refresh every 5 seconds.

    Step 5 — Edit `Modules/Desktop/Internal/Native/AppMenuBuilder.php` (read first to confirm the exact signature + Menu facade usage). Add `CurrentUser $currentUser` to constructor (or to `build()` method — pick based on the existing AppMenuBuilder pattern; if it doesn't take constructor DI today, add it now via a `final readonly class` upgrade with `private CurrentUser $currentUser`). After Help submenu in build():
      ```
      if ($this->currentUser->user()?->is_developer === true) {
          $menu[] = Menu::submenu('Developer', [
              Menu::link('Open Dev Console', 'http://beatrax.test/dev', shortcut: 'Cmd+.'),
              Menu::link('⌘K Run a command', 'http://beatrax.test/dev', shortcut: 'Cmd+K'),
          ]);
      }
      ```
      Verify the Menu facade method signatures against nativephp/desktop ^2.2 installed code. If the shortcut syntax differs, adapt — the goal is "menu entry with a hint of the cross-platform keybind". Update the menu URL host to whatever the NativePHP runtime resolves (typically `http://localhost:{port}` — see Phase 15-02 SUMMARY for the resolved host pattern).

    Step 6 — Tests for the 8 behaviors. For Tests 1-3, render the layouts via `view('layouts.app')` + HTML-grep on the result. For Tests 4-6, use Livewire::test(AppSidebar::class) with seeded jobs + cache. For Tests 7-8, mock CurrentUser via `actingAs(...)` and call AppMenuBuilder->build() directly.

    Step 7 — Manual smoke: `composer dev`; from any page press ⌘K → palette opens; type "dash" → "Dashboard" ranks first; press ↩ → navigate. Press ⌘. → /dev opens. Visit /; sidebar Dev block shows live Queue + Worker. Restart NativePHP build (per RESEARCH Pitfall 9 — menu changes need full quit + relaunch); the macOS menu bar shows the Developer submenu.

    Step 8 — Verify final phase requirements coverage:
      - DEVUI-01: 16-03.
      - DEVUI-02: 16-04.
      - DEVUI-03: 16-04.
      - DEVUI-04: 16-05.
      - DEVUI-05: 16-06.
      - DEVUI-06: 16-07.
      - DEVUI-07: 16-07.
      - DEVUI-08: 16-06.
      - DEVUI-09: THIS plan.
      All 9 DEVUI requirements satisfied; Phase 16 ready for `/gsd:verify-work`.
  </action>
  <verify>
    <automated>cd /Users/wesselverheij/Development/diederik &amp;&amp; vendor/bin/pest --filter='AppSidebarDevBlockLiveData|AppMenuDeveloperSubmenu' &amp;&amp; vendor/bin/pest --parallel &amp;&amp; vendor/bin/phpstan analyse --memory-limit=2G &amp;&amp; vendor/bin/pint --test</automated>
  </verify>
  <done>Body keybind handlers wired on both base layouts; palette mount present in both; sidebar Dev block shows live queue + heartbeat with wire:poll(5s); app-menu Developer submenu gated on is_developer; all DEVUI-01..09 satisfied; full test suite + Larastan + Pint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keybind handler on `<body>` | Pure client-side keyboard capture; cannot escalate (only dispatches Livewire events). The events themselves go through the server-side trust chain. |
| palette JSON server-emit -> client | Server filters dev entries by is_developer BEFORE emit. Non-devs literally do not receive the dev entries in their JSON registry. Tampering the client-side JSON does not grant access — the underlying routes still require EnsureDeveloperMode. |
| palette `pickEntry` cache write | Per-user cache key (`dev_mode.palette_recent.{userId}`); cannot poison another user's Recent. |
| app-menu Developer submenu | Server-rendered at NativePHP boot; gated on CurrentUser. A partner account would not see the submenu items even if they share the same machine (separate user sessions). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-33 | Information Disclosure | Palette JSON contains DESTRUCTIVE-tier dev commands | mitigate | Per D-41, the JSON-emit-time filter pulls only DevCommandRegistry::safe() — NOT destructive(). Test #3 asserts no `source: dev` row references a destructive command. Destructive commands are still reachable via /dev/artisan's fallback Flux modal (16-04 Step 3), but the palette is a calmer surface to keep mistakes rare. |
| T-16-34 | Information Disclosure | Non-developer's palette JSON leaks dev row labels | mitigate | Server filters at JSON-emit time per is_developer; non-devs receive ZERO `source: dev` rows. Test #4 asserts. |
| T-16-35 | Tampering | Palette `palette:picked` event from a forged Livewire payload | accept | The handler writes to per-user Recent cache only; navigation/handler-dispatch happens client-side via window.location or Livewire.dispatch — both of which already enforce server-side gates. Picking a dev row a non-developer somehow forged would navigate to /dev/* and hit EnsureDeveloperMode (404). |
| T-16-36 | Information Disclosure | NativePHP app-menu Developer submenu visible to partner | mitigate | AppMenuBuilder gates on `CurrentUser->user()?->is_developer === true`; the submenu is server-side absent for non-devs. The menu is recomposed at boot — RESEARCH Pitfall 9 means a partner who toggles is_developer off would still see the menu until full relaunch. Document. |
| T-16-37 | Information Disclosure | Sidebar Dev-block live counts visible to non-devs | mitigate | The entire Dev block is wrapped in `@if ($isDeveloper) ... @endif` (from 16-01). Non-devs see no Dev block AND no live data. T-16-01 (16-01 plan) covers this. |
</threat_model>

<verification>
- `vendor/bin/pest --filter='CommandPaletteRegistry|AppSidebarDevBlockLiveData|AppMenuDeveloperSubmenu'` — all green.
- `vendor/bin/pest --parallel` — full suite green.
- `vendor/bin/phpstan analyse --memory-limit=2G` — Larastan L10 strict clean.
- `vendor/bin/pint --test` — Pint clean.
- Manual smoke per Task 2 Step 7.
- DEVUI-09 fully satisfied.
- Final phase smoke: log in as developer; visit every Dev Console route; press ⌘K from each; verify palette renders + filters work + selections navigate; toggle Settings → Developer off → visit /dev → 404; sidebar Dev block disappears; palette JSON loses dev rows on next load.
</verification>

<success_criteria>
- ⌘K opens palette from any page (main app + /dev/*); ⌘. opens /dev.
- Palette JSON registry merges nav + dev (SAFE only, devs only) + actions; DESTRUCTIVE excluded.
- Non-developer's palette has zero dev rows.
- Fuse.js fuzzy ranking with locked weights + threshold + ignoreLocation.
- Recent shortcuts cache per user (5 entries, 30d TTL).
- Sidebar Dev block live data working with `wire:poll(5s)`.
- NativePHP app-menu Developer submenu visible only when is_developer.
- All 9 DEVUI-01..09 requirements satisfied across plans 16-03..16-08.
- Pest + Larastan + Pint all green.
- Phase 16 ready for `/gsd:verify-work`.
</success_criteria>

<output>
Create `.planning/phases/16-developer-mode-ui/16-08-SUMMARY.md` when done. The SUMMARY MUST document:
1. The palette JSON-emit-time filter for is_developer + the SAFE-only filter for dev commands.
2. The Recent cache key + dedupe + 30d TTL design.
3. The NativePHP app-menu Pitfall 9 caveat (changes need full bundle restart, not just PHP autoreload).
4. The sidebar Dev block queue-count choice (Job::count() only vs Job + FailedJob composite) and rationale.
5. The final DEVUI-01..09 satisfaction matrix mapping each requirement to its landing plan(s).
6. The full Phase 16 hand-off checklist for the verifier: ensure all 4 contract tests are green (BoundaryArchTest::everyDevModeRouteAppliesEnsureDeveloperModeMiddleware, ::impersonationSurfaceRemoved, ::noDiederikLiteralAfterRename, SelectOnlyValidatorContractTest); ensure the manual cut-over steps from 16-02 SUMMARY are completed locally (env rename, Herd hostname, app bundle re-install if applicable); ensure both layouts have the keybind handler.
</output>
