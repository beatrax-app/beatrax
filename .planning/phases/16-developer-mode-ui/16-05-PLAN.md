---
phase: 16-developer-mode-ui
plan: 05
type: execute
wave: 5
depends_on: [16-04b]
files_modified:
  - Modules/DevMode/Internal/Logging/RedactSecretsProcessor.php
  - Modules/DevMode/Internal/Services/OAuthScrubSet.php
  - Modules/DevMode/Internal/Listeners/BustOAuthScrubSetOnSecretChange.php
  - Modules/DevMode/Internal/Http/Controllers/LogStreamController.php
  - Modules/DevMode/Internal/Http/Livewire/LogTailerPage.php
  - Modules/DevMode/Resources/views/livewire/log-tailer-page.blade.php
  - Modules/DevMode/Internal/Audit/RedactionExcerptCap.php
  - Modules/DevMode/Providers/DevModeServiceProvider.php
  - Modules/DevMode/Routes/web.php
  - Modules/DevMode/Resources/views/layouts/dev-shell.blade.php
  - Modules/DevMode/tests/Unit/RedactSecretsProcessorTest.php
  - Modules/DevMode/tests/Feature/OAuthScrubSetBustTest.php
  - Modules/DevMode/tests/Feature/LogTailerPageTest.php
  - Modules/DevMode/tests/Feature/LogStreamControllerTest.php
autonomous: true
requirements: [DEVUI-04]
mvp_mode: true

must_haves:
  truths:
    - "A log line containing `Authorization: Bearer abc123` is REDACTED to `Authorization: Bearer [REDACTED]` BEFORE the line hits disk via the Monolog tap on every channel (D-29 on-write)"
    - "A log line containing a JWT-shape token (eyJ...) is redacted to [JWT_REDACTED] before write"
    - "A log line containing the literal of an oauth_secrets value is redacted to [REDACTED] via the OAuthScrubSet (D-30)"
    - "Writing or deleting an OAuthSecret model busts the scrub-set cache; the next log line correctly redacts the new/deleted secret value"
    - "GET /dev/logs as developer renders the LogTailerPage; an EventSource bound to /dev/logs/stream receives data: events for new log lines from storage/logs/laravel-YYYY-MM-DD.log"
    - "The stream re-applies redaction on the streamed chunks (belt + braces — D-29) so a log line that somehow survived on-write redaction is still scrubbed on-stream"
    - "The tailer detects file rotation (inode/size shrinkage per D-28) and re-opens at offset 0"
    - "Pause/Resume + severity filter + channel filter + contains-filter work end-to-end on the client (10k-line scrollback)"
    - "Sidebar Logs item is enabled (no nav-disabled class)"
  artifacts:
    - path: "Modules/DevMode/Internal/Logging/RedactSecretsProcessor.php"
      provides: "Monolog ProcessorInterface scrubbing Bearer + JWT + OAuth scrub-set"
      contains: "ProcessorInterface"
    - path: "Modules/DevMode/Internal/Services/OAuthScrubSet.php"
      provides: "Cached set of decrypted OAuth secret values; busted by OAuthSecret observer"
      contains: "all\\(\\):"
    - path: "Modules/DevMode/Internal/Http/Controllers/LogStreamController.php"
      provides: "SSE endpoint /dev/logs/stream with fseek + clearstatcache + rotation detection"
      contains: "fseek"
  key_links:
    - from: "Modules/DevMode/Internal/Logging/RedactSecretsProcessor.php (upgraded in place from 16-04b's baseline)"
      to: "Modules\\DevMode\\Internal\\Services\\OAuthScrubSet"
      via: "constructor DI; processor applies compiled scrub-set regex per log line"
      pattern: "OAuthScrubSet"
    - from: "Modules/DevMode/Internal/Listeners/BustOAuthScrubSetOnSecretChange.php"
      to: "Modules\\DevMode\\Internal\\Services\\OAuthScrubSet"
      via: "OAuthSecret::saved/deleted observer calls OAuthScrubSet::bust()"
      pattern: "OAuthSecret::observe"
---

<objective>
Land the complete log tailer: UPGRADE the Monolog redaction processor 16-04b installed (adds the OAuth scrub-set + on-stream re-application per D-29 belt+braces — 16-04b already filled the tap slot with the baseline Bearer+JWT regex via W-1 fix; this plan extends the SAME processor in place rather than installing it from scratch). Add OAuth scrub-set cached service + Eloquent observer that busts on `OAuthSecret` save/delete (D-30), SSE log stream controller with rotation detection (D-28: fseek + clearstatcache + inode/size shrinkage check via the shared FileTailer service 16-04 created), and the LogTailerPage Livewire component with the 10k-line client-side scrollback ring buffer + severity/channel/contains filters + Pause/Resume (D-31). Upgrade 16-04b's `RedactionExcerptCap` constructor to inject OAuthScrubSet so audit-log excerpts are also fully redacted.

Purpose: DEVUI-04 fully landed. The Phase 16 promise of "OAuth tokens never leak to disk or render in the tailer" is enforced in two independent places (on-write tap + on-stream pipeline). The OAuth scrub-set pattern (singleton + Eloquent observer bust) is the architectural answer to "stale secrets never linger". The audit-log excerpts written from 16-04's SpatieAuditWriter now benefit from the same full redaction path (no longer just Bearer + JWT regex; now also the OAuth scrub-set).

Output: A working `/dev/logs` page where a developer can watch live log lines stream in, filter by severity / channel / free-text, pause + resume, expand a line to ±10 lines of context (D-31). Sidebar Logs item enabled. Audit-log excerpts (16-04) now redacted via the full pipeline.

## Phase Goal

**As a** developer in the Dev Console, **I want to** browse to `/dev/logs` and see a live tail of `storage/logs/laravel-YYYY-MM-DD.log` with OAuth tokens, JWT tokens, and Bearer headers all redacted before they ever hit disk (and re-redacted on-stream as defense-in-depth), filter by severity and channel, pause + resume, and click any line to expand ±10 lines of context, **so that** debugging a production issue or an OAuth flow problem in dev never accidentally exposes a token to the browser viewport or the host filesystem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/16-developer-mode-ui/16-CONTEXT.md
@.planning/phases/16-developer-mode-ui/16-RESEARCH.md
@.planning/phases/16-developer-mode-ui/16-PATTERNS.md
@.planning/phases/16-developer-mode-ui/16-UI-SPEC.md
@.planning/phases/16-developer-mode-ui/16-04-SUMMARY.md

<interfaces>
Locked decisions:
- D-27: Logging channel `single` → `daily`. The `daily` rotation handler produces `laravel-YYYY-MM-DD.log`.
- D-28: SSE `/dev/logs/stream` opens the current laravel-*.log, seeks to EOF, polls via `clearstatcache()` + `fseek()` at 250ms. Inode/size shrinkage detection re-opens at offset 0. Cross-platform (no shell `tail`).
- D-29: Belt + braces — on WRITE via `tap` on every channel (stack/single/daily) in `config/logging.php`. On STREAM re-applied in the tailer pipeline.
- D-30: OAuth scrub-set = singleton service holding decrypted secret strings from `oauth_secrets` (client_secret + every string value in tokens_blob). Loaded at boot. Eloquent observer on `OAuthSecret` busts the cache on save/delete. Per log line: `foreach scrub_set as $secret → str_replace($secret, '[REDACTED]')`. Regex for `Authorization: Bearer …` + JWT shapes wraps as additional defense.
- D-31: Tailer UI — severity multi-select (DEBUG/INFO/NOTICE/WARNING/ERROR/CRITICAL/ALERT/EMERGENCY) + channel/module filter + contains-filter + 10k-line scrollback + Pause/Resume + click-to-expand ±10 lines of context.

Pre-existing analogs (PATTERNS.md):
- `Modules/EmailScan/Public/Services/OAuthSecretsRepository.php` — provides decrypted secrets via `allDecrypted()` (verify the exact method name on the existing service; if different, mirror its actual API).
- `Modules/EmailScan/Models/OAuthSecret.php` — the Eloquent model the observer attaches to.
- `Modules/Core/Internal/Listeners/HealthCheckListener.php` — listener-class shape.

RESEARCH § Pattern 4 (full code reference for RedactSecretsProcessor + PushRedactProcessor + the `tap` registration in config/logging.php). Read it in full before authoring.

RESEARCH § Pitfall 8: the redaction processor is O(n*m). Mitigation: pre-compile a single regex from the scrub-set once per request (`'/' . implode('|', array_map(preg_quote, $secrets)) . '/'`) and run one regex per line. Cache the compiled pattern across requests using the same observer-bust mechanism the set uses. THIS plan adopts the pre-compiled regex from day one — do not ship the naive `foreach str_replace` form.

LogStreamController shape (D-28):
- Single-invokable controller. Method-DI on `RedactSecretsProcessor`, `UserDataPathService` (resolves `storage/logs/` path).
- `__invoke(Request $request): StreamedResponse`. Uses the same SSE headers + loop pattern as `ArtisanStreamController` (PATTERNS.md K + RESEARCH Pattern 2).
- Open file with `fopen($path, 'r')`; `fseek($f, 0, SEEK_END)`; remember `clearstatcache(); $size = filesize($path); $inode = stat($path)['ino'];`.
- Loop:
  - `usleep(250_000);` (250ms cadence per D-28)
  - `clearstatcache(); $newSize = filesize($path); $newInode = stat($path)['ino'];`
  - If `$newInode !== $inode || $newSize < $size` (rotation): `fclose($f); $f = fopen($path, 'r');` (or open the new day's file if path computed by date and the rollover happened). Reset offset to 0; update $size + $inode.
  - Read new bytes via `fseek($f, $size)`. `$chunk = fread($f, 65536)` (or up to EOF). Update $size.
  - For each complete line in $chunk: apply `RedactSecretsProcessor::scrub($line)` (the on-stream re-application per D-29) → emit as `data: ` event with severity + channel + timestamp parsed out from the standard Laravel log line format.
  - `if (connection_aborted()) break;`

LogTailerPage Livewire shape:
- `final class LogTailerPage extends Component`. Method-DI render. `#[Url]` filter properties: `severities` (array, default all selected), `channel` (string?), `contains` (string).
- The 10k-line scrollback is a CLIENT-side ring buffer (Alpine `x-data`) — server does NOT hold the buffer. The EventSource consumer appends each incoming line to the Alpine ring; old lines are shifted off when length exceeds 10000.
- Pause/Resume: a single button that calls `eventSource.close()` / `new EventSource(...)` on the client. Server endpoint is unaware of pause.
- Click-to-expand a line: the line carries its file offset; expanding queries `/dev/logs/context?offset=N&radius=10` (a separate JSON endpoint also gated by EnsureDeveloperMode) which returns ±10 lines around that offset. Implement this endpoint in the same controller file (a `context($request)` method) or as a sibling LogContextController — planner discretion.

UI per UI-SPEC + sketch-findings:
- Severity multi-select chips (DEBUG..EMERGENCY) with the locked color mapping (D-31 + sketch component-library: DEBUG/INFO muted, WARNING amber, ERROR/CRITICAL rose, etc.).
- Channel dropdown (computed from observed channels in the recent log + a "(all channels)" option).
- Free-text contains-filter (debounced; client-side regex over the visible buffer).
- 10k-line scrollback `<pre>` with monospace JetBrains Mono 12px.
- Pause/Resume button (single).
- Click any `<pre>` line → inline expansion below the line with ±10 surrounding lines.
- Empty state: "Waiting for log lines…" + blinking cursor (per UI-SPEC § Empty states).
- Error state on stream interrupt: "Log stream interrupted. Reconnecting…" with auto-retry exponential backoff (Alpine).

RedactionExcerptCap upgrade:
- Replace 16-04's `RedactionExcerptCap` constructor — now takes `OAuthScrubSet` AND the existing Bearer/JWT regexes. `apply()` runs the scrub-set first (pre-compiled regex), then the Bearer/JWT regex, then truncates. This is the on-write redaction layer for audit-log excerpts.
- The SpatieAuditWriter binding stays unchanged (16-04 already injects RedactionExcerptCap); only the constructor swap re-wires the dependency. Test: a stdout containing an oauth_secrets value flows through SpatieAuditWriter → the audit row's stdout_excerpt is [REDACTED].

Sidebar update: enable the Logs item in `Modules/DevMode/Resources/views/layouts/dev-shell.blade.php` (remove from the `nav-disabled` allow-list).

Route registration in `Modules/DevMode/Routes/web.php` (append):
```
Route::get('/logs', LogTailerPage::class)->name('dev.logs');
Route::get('/logs/stream', LogStreamController::class)->name('dev.logs.stream');
Route::get('/logs/context', [LogStreamController::class, 'context'])->name('dev.logs.context');  // OR LogContextController
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RedactSecretsProcessor + OAuthScrubSet + Observer + PushRedactProcessor + config/logging.php tap fill</name>
  <files>Modules/DevMode/Internal/Logging/RedactSecretsProcessor.php, Modules/DevMode/Internal/Services/OAuthScrubSet.php, Modules/DevMode/Internal/Listeners/BustOAuthScrubSetOnSecretChange.php, Modules/DevMode/Internal/Audit/RedactionExcerptCap.php, Modules/DevMode/Providers/DevModeServiceProvider.php, Modules/DevMode/tests/Unit/RedactSecretsProcessorTest.php, Modules/DevMode/tests/Feature/OAuthScrubSetBustTest.php, Modules/DevMode/tests/Feature/OAuthSecretDeletionStopsScrubbingTest.php</files>
  <behavior>
    - Test 1 (unit): RedactSecretsProcessor scrubs `Authorization: Bearer abc123def456_xyz` to `Authorization: Bearer [REDACTED]`.
    - Test 2 (unit): scrubs `eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiIxMjM0NTY3ODkwIn0.SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c` to `[JWT_REDACTED]`.
    - Test 3 (unit): scrubs every string in OAuthScrubSet from the log message + context recursively.
    - Test 4 (feature): create an OAuthSecret with client_secret='SECRET_AAA'; `logger()->info('Used SECRET_AAA today')` → the resulting `laravel-YYYY-MM-DD.log` file contains `[REDACTED]` not `SECRET_AAA`. (Verify by reading the file post-write.)
    - Test 5 (feature): create OAuthSecret with client_secret='OLD_SECRET'; log "Used OLD_SECRET"; verify scrubbed. Update the OAuthSecret to client_secret='NEW_SECRET'; log "Used NEW_SECRET" → verify scrubbed (cache busted on save).
    - Test 6 (feature, I-4): delete the OAuthSecret('OLD_SECRET') → next log line "Used OLD_SECRET" is NOT scrubbed; this is acceptable + documented behavior — once a secret is rotated and removed from the oauth_secrets table, it ceases to be sensitive in this app's model. The Eloquent observer busts the cache on delete so the next compile excludes the deleted value. Lives in OAuthSecretDeletionStopsScrubbingTest as a NON-BLOCKING test (the behavior is intentional, but we still want a regression guard so a future change that secretly KEEPS scrubbing deleted values doesn't slip in unnoticed).
    - Test 7 (unit): SpatieAuditWriter's stdout_excerpt is scrubbed via the upgraded RedactionExcerptCap. Inject a fake stdout containing both a Bearer header AND a known oauth_secret → resulting audit row's stdout_excerpt contains [REDACTED] for both.
  </behavior>
  <action>
    Step 1 — Create `OAuthScrubSet` per RESEARCH Pattern 4 (the full code reference). Constructor DI on `OAuthSecretsRepository`. Internal `?string` compiled-pattern cache + `?array<string>` raw-list cache. `all(): array<string>` lazy-loads from the repo. `compiledPattern(): ?string` returns the pre-compiled regex (`'/' . implode('|', array_map(preg_quote, $set)) . '/'`); null if set is empty (per Pitfall 8 mitigation). `bust(): void` clears both caches. Bind as singleton in `DevModeServiceProvider::register()`.

    Step 2 — Create `BustOAuthScrubSetOnSecretChange` listener. Constructor DI on `OAuthScrubSet`. Implements `saved($model)` and `deleted($model)` handlers — both call `$this->scrubSet->bust();`. Wire in `DevModeServiceProvider::boot()`: `OAuthSecret::observe(BustOAuthScrubSetOnSecretChange::class);` (Eloquent observer pattern — model FQCN `Modules\EmailScan\Models\OAuthSecret`).

    Step 3 — UPGRADE the baseline `RedactSecretsProcessor` 16-04b installed (W-1 fix). The class exists already at `Modules/DevMode/Internal/Logging/RedactSecretsProcessor.php` with the baseline Bearer + JWT regex applied directly in its `__invoke` (NOT via RedactionExcerptCap — that is the separate audit-log excerpt layer at `Modules/DevMode/Internal/Audit/RedactionExcerptCap.php` consumed by SpatieAuditWriter; both classes upgrade in this plan with OAuthScrubSet, but they live at different paths and serve different threat boundaries — see Step 6 for the RedactionExcerptCap upgrade). This plan adds the OAuthScrubSet dependency to RedactSecretsProcessor: add `OAuthScrubSet $scrubSet` to the constructor, in `__invoke(LogRecord $record): LogRecord` apply the compiled OAuth scrub-set regex (single pass) BEFORE the existing Bearer + JWT scrubbing. Scrub message AND context (recurse into context arrays/strings). The existing tap-slot registrations in `config/logging.php` continue to point at the same FQCN — no config edit needed; the constructor change is invisible from config because the container resolves the new DI shape.

    Step 4 — NOTE: `PushRedactProcessor` was created in 16-04b alongside the baseline RedactSecretsProcessor. This plan does NOT modify PushRedactProcessor — it resolves the upgraded RedactSecretsProcessor from the container via `app(RedactSecretsProcessor::class)` so the constructor-DI upgrade in Step 3 propagates automatically.

    Step 5 — Verify `config/logging.php` tap slots: 16-04b filled them with `[\Modules\DevMode\Internal\Logging\PushRedactProcessor::class]` per W-1 fix. THIS plan does NOT edit config/logging.php — it only verifies via `php artisan tinker > config('logging.channels.daily.tap')` that the registration still points at the same FQCN. Verify the `daily` channel is still the default.

    Step 6 — UPGRADE `RedactionExcerptCap` (16-04's basic version): constructor now takes `OAuthScrubSet`. The `apply()` method:
      ```
      $text = $this->scrubBearerAndJwt($text);              // existing
      if ($pattern = $this->scrubSet->compiledPattern()) {  // new
          $text = preg_replace($pattern, '[REDACTED]', $text);
      }
      return mb_substr($text, 0, $maxBytes);                // existing
      ```
      No DI changes elsewhere — SpatieAuditWriter already injects RedactionExcerptCap. Re-run 16-04's AuditLogWriteTest to confirm it still passes (the basic Bearer redaction case stays green).

    Step 7 — Tests covering the 7 behaviors. Most are pure-PHP unit tests (RedactSecretsProcessorTest). **W-5 fix:** Tests 4-6 (the feature tests that actually write log lines and inspect them) use a per-test ephemeral log channel built in `beforeEach` via the DI form of LogManager — NOT the Log facade. Pattern:
      ```
      beforeEach(function () {
          $this->testChannel = app(\Illuminate\Log\LogManager::class)->build([
              'driver' => 'single',
              'path' => sys_get_temp_dir() . '/dev-mode-test-' . uniqid() . '.log',
          ]);
          // Wire the redaction tap directly on the built channel:
          foreach ($this->testChannel->getHandlers() as $handler) {
              $handler->pushProcessor(app(\Modules\DevMode\Internal\Logging\RedactSecretsProcessor::class));
          }
      });
      ```
      The test then uses `$this->testChannel->info('...')` and inspects the per-test tmp file directly. This sidesteps the Pest-parallel race on the real daily log file (a single shared `storage/logs/laravel-{date}.log` across parallel workers leads to flaky reads + writes interleaved between tests). The `tap` slot in `config/logging.php` is NOT used in tests — the per-channel handler-pushProcessor form is used instead so the test is fully isolated.
  </action>
  <verify>
    <automated>cd /Users/wesselverheij/Development/diederik &amp;&amp; vendor/bin/pest --filter='RedactSecretsProcessor|OAuthScrubSetBust|AuditLogWrite' &amp;&amp; vendor/bin/pest --parallel</automated>
  </verify>
  <done>config/logging.php tap slot filled on every channel; RedactSecretsProcessor scrubs Bearer + JWT + OAuth secrets; OAuthScrubSet caches the compiled regex + busts on save/delete; SpatieAuditWriter now applies the full redaction via upgraded RedactionExcerptCap; 7 tests pass; full Pest + Larastan green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LogStreamController + LogTailerPage with 10k scrollback + filters + Pause/Resume + ±10-line context</name>
  <files>Modules/DevMode/Internal/Http/Controllers/LogStreamController.php, Modules/DevMode/Internal/Http/Livewire/LogTailerPage.php, Modules/DevMode/Resources/views/livewire/log-tailer-page.blade.php, Modules/DevMode/Providers/DevModeServiceProvider.php, Modules/DevMode/Routes/web.php, Modules/DevMode/Resources/views/layouts/dev-shell.blade.php, Modules/DevMode/tests/Feature/LogStreamControllerTest.php, Modules/DevMode/tests/Feature/LogTailerPageTest.php</files>
  <behavior>
    - Test 1 (stream): seed the log file with 3 lines, GET /dev/logs/stream as developer → response is `text/event-stream` 200 → first response chunk contains all 3 lines as `data:` events. (Trick: in tests use a short-lived stream — controller exits when test client closes after N seconds.)
    - Test 2 (stream): after the controller has streamed initial lines, append a new line to the file; within ~500ms the controller emits the new line as a new `data:` event.
    - Test 3 (stream rotation): rename the log file to simulate rotation → the controller detects the inode change → re-opens the new file and continues from offset 0.
    - Test 4 (stream redaction defense-in-depth): the same RedactSecretsProcessor is applied to streamed chunks. Even if a log line somehow lacks on-write redaction (e.g. written via an external process), the streamed output is redacted.
    - Test 5 (page): GET /dev/logs as developer renders the page with the 8 severity filter chips, the channel dropdown, the contains-filter input, the Pause button, and an empty `<pre>` scrollback.
    - Test 6 (page): GET /dev/logs as non-dev returns 404.
    - Test 7 (context): GET /dev/logs/context?offset=N&radius=10 returns JSON with up to 21 lines around the requested offset, all redacted.
  </behavior>
  <action>
    Step 1 — Create LogStreamController per &lt;interfaces&gt;. Read RESEARCH § Pattern 2 again for the SSE skeleton; use the same headers + ignore_user_abort pattern as ArtisanStreamController. Apply RedactSecretsProcessor to every emitted chunk (on-stream re-application per D-29). For rotation detection, on every loop tick `stat($path)` + compare to last-known inode/size; if rotation, fclose + reopen.

    Step 2 — Add a `context(Request $request, RedactSecretsProcessor $processor): JsonResponse` method (or a sibling controller) for the ±10-line context endpoint. Read the log file with `SplFileObject` for line-indexed access; serve `radius` lines on each side of `offset`. Apply redaction to every returned line.

    Step 3 — Create LogTailerPage Livewire component per &lt;interfaces&gt;. Method-DI on render. `#[Url]` for severities + channel + contains. The page mounts the EventSource client-side via Alpine x-data.

    Step 4 — Create the Blade view per UI-SPEC § Component Inventory + sketch-findings:
      - Severity multi-select chips row (8 chips; toggle in client state).
      - Channel `<select>` (server-rendered with observed channel names).
      - Contains-filter `<input>` (debounced 250ms client-side).
      - Pause/Resume `<button>`.
      - 10k-line scrollback `<pre>` with monospace + tabular-nums; lines colored by severity (rose for ERROR/CRITICAL/ALERT/EMERGENCY, amber for WARNING/NOTICE, default for INFO/DEBUG).
      - Click any line → Alpine fetches `/dev/logs/context` and inline-expands ±10 lines.
      - Empty state: "Waiting for log lines…" + blinking cursor (CSS `@keyframes` 1s steps(1) infinite per UI-SPEC § Animation).
      - Stream-interrupted state: "Log stream interrupted. Reconnecting…" with Alpine exponential-backoff retry (250ms, 500ms, 1s, 2s, 5s cap).
      - Wrap in a `<div x-data="logTailer({ url: '/dev/logs/stream' })">` Alpine component declared at the bottom of the Blade view.

    Step 5 — Register the route + Livewire component. Enable the Logs sidebar item in dev-shell.blade.php.

    Step 6 — Tests covering the 7 behaviors. For Test 1-3, use `Process::fromShellCommandline()` to start the SSE controller in a subprocess + read the stdout; or use Laravel's testing HTTP client with `streamedResponse` assertions. For Test 7, use `actingAs($dev)->getJson('/dev/logs/context?offset=10&radius=5')`.

    Step 7 — Manual smoke test: `composer dev`; visit /dev/logs; click any severity chip; verify filter takes effect on visible lines; click Pause → stream halts; click Resume → stream resumes; click any line → ±10 context inline expands.
  </action>
  <verify>
    <automated>cd /Users/wesselverheij/Development/diederik &amp;&amp; vendor/bin/pest --filter='LogStream|LogTailer' &amp;&amp; vendor/bin/pest --parallel &amp;&amp; vendor/bin/phpstan analyse --memory-limit=2G</automated>
  </verify>
  <done>/dev/logs page renders with all filters + Pause/Resume; SSE stream emits redacted log lines; rotation detection works (inode + size shrinkage); ±10-line context endpoint works; sidebar Logs item enabled; 7 tests pass; Pest + Larastan + Pint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application log writes → disk | All Laravel logger calls (`logger()->info()`, `Log::error()`, etc.) pass through Monolog handlers; the `tap` array binds PushRedactProcessor to every handler so the processor runs BEFORE the formatter writes the record. Secrets never hit disk. |
| Log file → SSE stream | The on-stream redaction in LogStreamController is defense-in-depth. Even if a log line slipped on-write redaction (e.g. a 3rd-party tool wrote directly to the file), the stream re-applies the same processor. |
| OAuth secret rotation → scrub-set | Eloquent observer busts the cache on save/delete; the next log line picks up the new compiled regex. No stale-secret window. |
| /dev/logs/context endpoint | Accepts `offset` + `radius` params; both are integer-validated (Laravel form-request validation); path is fixed (UserDataPathService); no LFI possible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | OAuth tokens / Bearer headers / JWTs leak through log file or live stream | mitigate | Belt + braces (D-29). RedactSecretsProcessor runs at on-write (via tap on every channel) AND on-stream (via direct invocation in LogStreamController). Pest tests cover both layers explicitly (Test 4 unit + Test 4 feature). |
| T-16-17 | Information Disclosure | Stale OAuth secret in scrub-set after rotation | mitigate | OAuthScrubSet busts on `OAuthSecret` save/delete via Eloquent observer (D-30). Tested in OAuthScrubSetBustTest Test 5. |
| T-16-18 | Information Disclosure | Log tailer reads outside `storage/logs/` (path traversal) | mitigate | Path is pinned in LogStreamController via UserDataPathService → `storage_path('logs/laravel-' . date('Y-m-d') . '.log')`. No user input flows into the path computation. |
| T-16-19 | Information Disclosure | Context endpoint returns ±N lines around a forged offset (reads beyond the file) | mitigate | The endpoint clamps `offset` to `[0, lineCount-1]` and `radius` to `[0, 50]` via Laravel validation; SplFileObject naturally handles EOF. No LFI. |
| T-16-20 | DoS | SSE stream long-lived connection consumes a PHP worker indefinitely | accept | This is the inherent cost of SSE. Laravel's default request timeout is high; the controller's `connection_aborted()` check kills the loop when the browser closes the EventSource. NativePHP bundle runs a sufficient worker pool. Risk is "developer leaves /dev/logs open all day" which is the intended use — single-user, single-machine. |
| T-16-21 | Performance | Redaction processor adds CPU per log line | mitigate | Pre-compiled regex (per Pitfall 8) — single `preg_replace` per line, not `foreach str_replace`. OAuthScrubSet caches the compiled pattern across requests; observer bust forces re-compile only on secret rotation. |
</threat_model>

<verification>
- `vendor/bin/pest --filter='RedactSecretsProcessor|OAuthScrubSet|LogStream|LogTailer|AuditLogWrite'` — all green.
- `vendor/bin/pest --parallel` — full suite green.
- `vendor/bin/phpstan analyse --memory-limit=2G` — Larastan L10 strict clean.
- `vendor/bin/pint --test` — Pint clean.
- Manual smoke: `composer dev`; trigger an action that logs a Bearer header (e.g. failing OAuth refresh); tail `storage/logs/laravel-{today}.log` and confirm `[REDACTED]` not the raw token; visit /dev/logs and confirm the line streams in already redacted.
- `php artisan route:list | grep /dev/logs` — three routes (page, stream, context); all gated by `ensureDeveloperMode`.
- DEVUI-04 fully satisfied.
</verification>

<success_criteria>
- Belt + braces (D-29) enforced: on-write tap on every channel + on-stream re-application in LogStreamController.
- OAuth scrub-set (D-30) singleton + observer pattern working; cache busts on save/delete.
- Pre-compiled regex (Pitfall 8 mitigation) used from day one — no naive `foreach str_replace`.
- /dev/logs page renders the full UI per D-31 (severity multi-select, channel filter, contains-filter, Pause/Resume, 10k scrollback, click-to-expand ±10 lines).
- Rotation detection (inode + size shrinkage) re-opens the file at offset 0 (D-28).
- 16-04's RedactionExcerptCap upgraded to apply the full scrub-set (audit-log excerpts now fully redacted).
- Sidebar Logs item enabled.
- Pest + Larastan + Pint all green.
</success_criteria>

<output>
Create `.planning/phases/16-developer-mode-ui/16-05-SUMMARY.md` when done. The SUMMARY MUST document:
1. The on-write + on-stream belt+braces design and why both layers are kept.
2. The pre-compiled regex pattern (Pitfall 8 mitigation) and the OAuthScrubSet cache strategy.
3. The audit-log excerpt redaction upgrade (16-04's basic → full scrub-set).
4. The known limitation that deleting an OAuthSecret stops scrubbing that string in future logs (acceptable behavior; rotated-and-removed secrets cease to be sensitive in this app's model).
</output>
