---
phase: 05-pin-biometric-app-lock-seed-009
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - Modules/Desktop/Internal/Listeners/LockOnWindowHideOrClose.php
  - Modules/Desktop/Internal/Native/NativeBiometricUnlock.php
  - Modules/Desktop/Internal/Native/DesktopKeyCustodian.php
  - Modules/Desktop/Providers/DesktopServiceProvider.php
  - phpstan.neon
  - Modules/Auth/Internal/Http/Livewire/AppLockKeyProbe.php
  - Modules/Auth/Resources/views/livewire/app-lock-key-probe.blade.php
  - Modules/Auth/Providers/AuthServiceProvider.php
  - Modules/DevMode/Resources/views/livewire/dev-overview-page.blade.php
  - Modules/Desktop/tests/Feature/LockOnWindowHiddenTest.php
  - Modules/Auth/tests/Feature/AppLockKeyProbeTest.php
autonomous: false
requirements: [LOCK-03, LOCK-04]

must_haves:
  truths:
    - "On desktop, hiding or closing the window locks the session immediately with no grace (D-06)"
    - "On macOS desktop, the lock-screen biometric button can release the data key via Touch ID (System::promptTouchID)"
    - "The LOCK-04 key service is exercisable end-to-end: a dev-console probe shows 'released' when unlocked and 'withheld' when locked"
    - "AppLockKeyService.release returns the data key only while unlocked and null while locked — proven by the probe and Pest tests"
    - "The lock gates the UI, but long-lived workers retain the data key independent of the per-session lock state (D-22 — lock gates UI; workers keep the key)"
  artifacts:
    - path: "Modules/Desktop/Internal/Listeners/LockOnWindowHideOrClose.php"
      provides: "desktop immediate-lock on WindowHidden/WindowClosed (D-06)"
      contains: "AppLockKeyService"
    - path: "Modules/Desktop/Internal/Native/DesktopKeyCustodian.php"
      provides: "desktop key custody via OS keychain (System::encrypt/decrypt), encrypted-session fallback — never plaintext on disk (D-20)"
      contains: "DesktopKeyCustodian"
    - path: "Modules/Auth/Internal/Http/Livewire/AppLockKeyProbe.php"
      provides: "LOCK-04 release/withhold probe consuming AppLockKeyService (D-19)"
      contains: "AppLockKeyService"
    - path: "phpstan.neon"
      provides: "NativePHP facade allowlist entry for the new Desktop biometric/native files"
      contains: "Modules/Desktop/Internal"
  key_links:
    - from: "Modules/Desktop/Internal/Listeners/LockOnWindowHideOrClose.php"
      to: "Modules/Auth/Public/Services/AppLockKeyService.php"
      via: "withhold(session) on window hide/close — Public contract only (module boundary)"
      pattern: "AppLockKeyService"
    - from: "Modules/Auth/Internal/Http/Livewire/AppLockKeyProbe.php"
      to: "Modules/Auth/Public/Services/AppLockKeyService.php"
      via: "release(session) → display released/withheld"
      pattern: "release\\("
---

<objective>
Close LOCK-03 on desktop and prove LOCK-04 end-to-end: the desktop window
hide/close immediate-lock listener (D-06), the native macOS Touch ID unlock path
(System::promptTouchID), and a dev-console probe plus Pest tests that demonstrate
the AppLockKeyService releases the data key only while unlocked and withholds it
while locked — the exact contract Phase 14 will consume.

Purpose: this is the final wave — it wires the desktop-shell triggers that the web
veil cannot cover and ships the verifiable LOCK-04 consumer (D-19) that makes the
key-release gate exercisable before Phase 14 makes the key guard real data. Turns
the 05-01 LockOnWindowHiddenTest RED stub green.

Output: LockOnWindowHideOrClose listener, NativeBiometricUnlock native path, the
phpstan allowlist update, the AppLockKeyProbe Livewire panel mounted on the dev
overview, and the provider/event wiring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-CONTEXT.md
@.planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-RESEARCH.md
@.planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-PATTERNS.md
@.planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-UI-SPEC.md

<interfaces>
<!-- Consumed from prior plans -->
AppLockKeyService (Public): release(Session): ?string; withhold(Session): void
LockStateManager: lock(Session): void; unlock(Session, string): void; isLocked(Session): bool
BiometricDeviceStore: findForUser(int): Collection; resetAllForUser(int): void  (from 05-05)
AppLockKeyWrap/AppLockKdf: for unwrapping the macOS biometric wrap

<!-- NativePHP (nativephp/desktop 2.2.1, macOS only for Touch ID) -->
Native\Desktop\Facades\System::canPromptTouchID(): bool
Native\Desktop\Facades\System::promptTouchID(string $reason): bool
Native\Desktop\Events\Windows\WindowHidden, Native\Desktop\Events\Windows\WindowClosed
</interfaces>

# Desktop listener analog (constructor DI, handle(), Public-contract-only cross-module access)
@Modules/Desktop/Internal/Listeners/DispatchOsNotification.php

# Desktop provider boot — bundle-gated event subscriptions (nativephp-internal.running)
@Modules/Desktop/Providers/DesktopServiceProvider.php

# phpstan NativePHP facade allowlist (paths list to extend)
@phpstan.neon

# RESEARCH Pattern 4 (native macOS), Pattern 8 (WindowHidden→lock), Open Question 1 (WindowHidden vs WindowClosed — subscribe to both)
@.planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Desktop immediate-lock listener (WindowHidden + WindowClosed) + provider/phpstan wiring</name>
  <read_first>
    - Modules/Desktop/Internal/Listeners/DispatchOsNotification.php (listener shape: final class, constructor DI, handle method; Public-contract cross-module usage)
    - Modules/Desktop/Providers/DesktopServiceProvider.php (the nativephp-internal.running gated block ~lines 261-309 where WindowFocused/WindowBlurred listeners register)
    - Modules/Auth/Public/Services/AppLockKeyService.php (the Public withhold() to call)
    - Modules/Desktop/tests/Feature/LockOnWindowHiddenTest.php (the 05-01 RED stub to satisfy)
    - .planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-RESEARCH.md (Pattern 8 + Open Question 1: subscribe to both WindowHidden and WindowClosed; no grace D-06)
    - .planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-PATTERNS.md (LockOnWindowHidden lines 626-669 — note module-boundary rule: Public contract only)
  </read_first>
  <files>Modules/Desktop/Internal/Listeners/LockOnWindowHideOrClose.php, Modules/Desktop/Providers/DesktopServiceProvider.php, phpstan.neon, Modules/Desktop/tests/Feature/LockOnWindowHiddenTest.php</files>
  <behavior>
    - LockOnWindowHiddenTest: invoking handle() with a WindowHidden event calls AppLockKeyService->withhold(session) so LockStateManager::isLocked(session) becomes true and the data key is cleared from the session (D-06, no grace). Same for a WindowClosed event.
  </behavior>
  <action>
    Create LockOnWindowHideOrClose (final class, Modules\Desktop\Internal\Listeners) injecting AppLockKeyService (Auth module Public contract — NEVER Auth\Internal) and Session. handle(object $event): void calls $this->keyService->withhold($this->session) — immediate lock, no grace (D-06). Accept both WindowHidden and WindowClosed by typing the param as object (or add two handle methods); register the same listener for both events.

    Register in DesktopServiceProvider::boot() inside the existing `if ($config->get('nativephp-internal.running') !== true) { return; }` gated block: $events->listen(WindowHidden::class, [LockOnWindowHideOrClose::class, 'handle']); and $events->listen(WindowClosed::class, [LockOnWindowHideOrClose::class, 'handle']); (import both events). The listener calls no NativePHP facade, so no phpstan facade-allowlist entry is needed for IT — but Task 2's NativeBiometricUnlock does.

    The LockOnWindowHiddenTest does not require the NativePHP bundle (it tests the listener's handle() directly with a faked event + real session), so it runs in the normal Pest suite.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Desktop/tests/Feature/LockOnWindowHiddenTest.php</automated>
  </verify>
  <acceptance_criteria>
    - LockOnWindowHiddenTest passes (the 05-01 RED stub is now GREEN): WindowHidden and WindowClosed both lock the session via AppLockKeyService->withhold and clear the data key.
    - LockOnWindowHideOrClose.php imports only Modules\Auth\Public\Services\AppLockKeyService (no Auth\Internal import — module boundary preserved).
    - DesktopServiceProvider registers the listener for both WindowHidden and WindowClosed inside the nativephp-internal.running gate.
    - vendor/bin/phpstan analyse Modules/Desktop clean at level 10 (arch tests pass — no cross-module Internal access).
  </acceptance_criteria>
  <done>Desktop window hide/close locks the session immediately with no grace; the listener test is green and the module boundary holds.</done>
</task>

<task type="auto">
  <name>Task 2: Native macOS Touch ID unlock path + phpstan allowlist</name>
  <read_first>
    - Modules/Desktop/Internal/Native/OsThemeProbe.php (existing System facade usage pattern in a Native/ class + its phpstan allowlist entry)
    - Modules/Desktop/Internal/Listeners/DispatchOsNotification.php (System facade usage + allowlist)
    - phpstan.neon (the Native\Desktop\Facades allowlist paths block ~lines 78-100 to extend)
    - Modules/Auth/Public/Services/AppLockKeyService.php + Modules/Auth/Internal/Lock/BiometricDeviceStore.php (release path on success)
    - .planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-RESEARCH.md (Pattern 4: System::canPromptTouchID/promptTouchID, macOS-only, returns false elsewhere; A2 assumption)
  </read_first>
  <files>Modules/Desktop/Internal/Native/NativeBiometricUnlock.php, Modules/Desktop/Internal/Native/DesktopKeyCustodian.php, phpstan.neon, Modules/Desktop/Providers/DesktopServiceProvider.php</files>
  <action>
    Create NativeBiometricUnlock (final class, Modules\Desktop\Internal\Native) exposing isAvailable(): bool returning System::canPromptTouchID() (guard so it returns false when the NativePHP bundle is not running) and prompt(string $reason = 'Unlock beatrax'): bool returning System::promptTouchID($reason). This class is the only place that calls the System Touch ID facade; the macOS lock-screen biometric button (05-05 web path falls back here on macOS desktop) invokes it via a bundle-gated Livewire action or a small endpoint that, on a true result, releases the per-device 'nativephp_macos' wrap through AppLockKeyService/BiometricDeviceStore and unlocks the session. Keep the release logic in the Auth module (Public/Internal); NativeBiometricUnlock only answers "did Touch ID succeed?" so the crypto stays out of the Desktop module.

    Add Modules/Desktop/Internal/Native/NativeBiometricUnlock.php to the phpstan.neon Native\Desktop\Facades allowlist paths block (same list that already contains OsThemeProbe.php, DispatchOsNotification.php, etc.) so the System facade call passes Larastan level 10 strict.

    Wire the macOS biometric trigger: in DesktopServiceProvider (bundle-gated) bind NativeBiometricUnlock as a singleton; the 05-05 lock-screen biometric path, when PlatformDetector reports nativephp_macos, routes to a desktop-only unlock action that calls NativeBiometricUnlock->prompt() then releases the wrap. (If a thin endpoint is needed, register it in the Desktop routes following the existing Desktop route pattern; otherwise expose a Livewire method guarded by isAvailable().)

    D-20 (desktop key custody via OS keychain): on the desktop shell the unwrapped data key must be held in the OS keychain (Electron safeStorage via NativePHP System::encrypt/System::decrypt), NOT a plaintext disk/session value. Add a DesktopKeyCustodian (final class, Modules\Desktop\Internal\Native — same allow-list entry) with store(string $rawKey): string returning System::encrypt($rawKey) (base64 ciphertext) and read(string $blob): string returning System::decrypt($blob), guarded by System::canEncrypt() (RESEARCH A4: safeStorage may be unavailable on headless CI — when canEncrypt() is false, fall back to the encrypted-session custody used on web so the desktop path degrades gracefully). The web path keeps LockStateManager's encrypted-session custody unchanged; only the desktop bundle routes the unwrapped key through DesktopKeyCustodian. Keep the abstraction thin: the Auth module still owns wrap/unwrap; DesktopKeyCustodian only protects the already-unwrapped key at rest in the keychain while unlocked. Add DesktopKeyCustodian.php to the same phpstan.neon Native facade allow-list paths block as NativeBiometricUnlock.
  </action>
  <verify>
    <automated>vendor/bin/phpstan analyse Modules/Desktop 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - NativeBiometricUnlock.php calls System::canPromptTouchID() and System::promptTouchID() and is listed in the phpstan.neon Native\Desktop\Facades allowlist paths.
    - vendor/bin/phpstan analyse Modules/Desktop exits clean at level 10 (the System facade calls are allow-listed; no other module touches the facade).
    - The crypto/release logic lives in the Auth module, not Desktop — NativeBiometricUnlock only returns a bool (no AppLockKeyWrap/AppLockKdf import in the Desktop Native class).
    - isAvailable() returns false when the NativePHP bundle is not running (web/CI safe).
    - DesktopKeyCustodian routes the unwrapped key through System::encrypt/decrypt (OS keychain) while unlocked, guarded by System::canEncrypt(), and falls back to encrypted-session custody when safeStorage is unavailable (D-20 — key custody: OS keychain desktop / encrypted session web, never plaintext disk); it is listed in the phpstan Native facade allow-list.
  </acceptance_criteria>
  <done>The macOS Touch ID unlock path exists, is allow-listed for Larastan, and keeps the crypto in the Auth module; non-macOS/web safely returns unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LOCK-04 dev-console probe (release/withhold) + Pest proof + end-to-end gate</name>
  <read_first>
    - Modules/Auth/Public/Services/AppLockKeyService.php (release/withhold — the probe consumes this)
    - Modules/Auth/Internal/Http/Livewire/AppLockSettingsSection.php (Livewire component + view registration pattern in this module)
    - Modules/DevMode/Resources/views/livewire/dev-overview-page.blade.php (where to mount the probe panel on the dev overview; developer-gated surface)
    - Modules/Auth/Providers/AuthServiceProvider.php (register the probe Livewire component)
    - .planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-UI-SPEC.md (Copywriting: 'App-lock key: [released / withheld]' dev-console probe label)
    - .planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-CONTEXT.md (D-19 verifiable consumer; D-22 workers keep the key)
  </read_first>
  <files>Modules/Auth/Internal/Http/Livewire/AppLockKeyProbe.php, Modules/Auth/Resources/views/livewire/app-lock-key-probe.blade.php, Modules/Auth/Providers/AuthServiceProvider.php, Modules/DevMode/Resources/views/livewire/dev-overview-page.blade.php, Modules/Auth/tests/Feature/AppLockKeyProbeTest.php</files>
  <behavior>
    - AppLockKeyProbeTest: with the session unlocked (a data key held), the probe reports "released" and a non-null key fingerprint; after withhold()/lock the probe reports "withheld" and no key; the probe never renders the raw key bytes — only a short fingerprint (e.g. a SHA-256 prefix of the key) proving presence without disclosure.
  </behavior>
  <action>
    Create AppLockKeyProbe (final class extends Livewire\Component, namespace Modules\Auth\Internal\Http\Livewire). A status() / computed property calls AppLockKeyService->release($session): if non-null → status 'released' + a short non-reversible fingerprint (hash('sha256', $key) truncated to 8 hex chars) so the probe proves the key is present without ever rendering it; if null → 'withheld'. Provide a refresh() action and a lock() action (calls AppLockKeyService->withhold) so a developer can toggle and watch the gate. render() returns auth::livewire.app-lock-key-probe (a small panel, calm-slate, monospace fingerprint per the dev-console density).

    The probe also documents D-22 (lock gates UI; long-lived workers keep the key): the lock state gates the per-session UI release only — background queue/scheduler workers hold their own copy of the data key independent of any session lock, so locking the UI never withholds the key from a running worker. Note this distinction in the panel copy / a comment so the LOCK-04 consumer contract is unambiguous for Phase 14.

    Create app-lock-key-probe.blade.php: a compact dev panel showing the UI-SPEC label "App-lock key: released" / "App-lock key: withheld" with the truncated fingerprint in JetBrains Mono when released, plus a "Lock now" button. NEVER output the raw key.

    Mount the probe on the dev overview: add @livewire('auth.app-lock-key-probe') to Modules/DevMode/Resources/views/livewire/dev-overview-page.blade.php (developer-gated surface). Register $livewire->component('auth.app-lock-key-probe', AppLockKeyProbe::class) in AuthServiceProvider::boot().

    Implement AppLockKeyProbeTest (Feature) proving released-when-unlocked / withheld-when-locked and that the raw key never appears in the rendered output (assertDontSee the raw key bytes; assertSee the fingerprint).
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Auth/tests/Feature/AppLockKeyProbeTest.php</automated>
  </verify>
  <acceptance_criteria>
    - AppLockKeyProbeTest passes: 'released' + fingerprint when unlocked, 'withheld' when locked, raw key never rendered.
    - AppLockKeyProbe.php consumes Modules\Auth\Public\Services\AppLockKeyService (the same Public contract Phase 14 will use) — proving the LOCK-04 gate end-to-end (D-19).
    - The probe panel documents that the lock gates the UI release only while long-lived workers keep the key (D-22).
    - dev-overview-page.blade.php contains @livewire('auth.app-lock-key-probe') and AuthServiceProvider registers the component.
    - The probe renders the UI-SPEC label "App-lock key: released"/"App-lock key: withheld".
    - vendor/bin/phpstan analyse Modules/Auth clean at level 10.
  </acceptance_criteria>
  <done>The LOCK-04 key-release gate is exercisable via the dev-console probe and proven by Pest: released only while unlocked, withheld while locked, raw key never disclosed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 5 end-to-end human verification (OS-integration paths)</name>
  <action>Confirm the OS-integration paths that cannot be automated, after the full automated gate is green.</action>
  <what-built>
    Full Phase 5 app-lock: PIN setup/unlock, biometric (WebAuthn + macOS Touch ID),
    idle + background + desktop-hide re-lock, and the LOCK-04 key-release gate.
    Automated tests cover the server-side logic; the manual checks below cover the
    OS-integration paths that cannot be automated (05-VALIDATION Manual-Only table).
  </what-built>
  <how-to-verify>
    1. Run the full gate: `vendor/bin/pest Modules/Auth/tests Modules/Desktop/tests` (all green), `vendor/bin/phpstan analyse` (clean, level 10), `vendor/bin/pint --test` (clean). Apply dev DB migrations first per the project memory note (DB_CONNECTION=sqlite php artisan migrate).
    2. In the browser at http://localhost:8000: Settings → App lock → enable with a 6-digit PIN (confirm account password). Reload — you should hit the /lock screen. Enter the PIN → dashboard.
    3. Background the tab (switch apps) → the branded veil should cover content within a blink. Return within ~30s → veil lifts, no re-unlock. Wait past the idle timeout (set it to 1 minute) → next interaction shows /lock.
    4. Open a second tab → unlock in one → both tabs drop the veil together.
    5. Enroll biometric in settings (Touch ID on macOS / Windows Hello / platform) → on the lock screen tap the biometric button (it must NOT auto-fire) → OS prompt → unlock.
    6. Desktop build: hide/close the window → reopen → it shows the lock screen immediately (no grace).
    7. Dev Console overview → the "App-lock key: released/withheld" probe flips to "withheld" after you lock and "released" after you unlock.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desktop window lifecycle → lock state | Hiding/closing the window must lock before the next view; the OS app-switcher snapshot must not show data |
| macOS Touch ID result → key release | A true promptTouchID result releases the per-device wrap; a false result must not |
| dev-console probe → key material | The probe must prove key presence without ever rendering the raw key |
| Phase 14 consumer → AppLockKeyService | The probe exercises the exact Public contract Phase 14 will depend on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Information disclosure | Desktop window snapshot of data | mitigate | WindowHidden + WindowClosed lock immediately with no grace (D-06); the web veil covers backgrounding |
| T-05-25 | Spoofing | Native Touch ID bypass | mitigate | Key released only when System::promptTouchID returns true; crypto release stays in the Auth module, not the Desktop native class |
| T-05-26 | Information disclosure | Probe leaking the raw data key | mitigate | Probe renders only a truncated SHA-256 fingerprint, never the key bytes — asserted (assertDontSee raw key) |
| T-05-27 | Elevation of privilege | Probe usable by non-developers | mitigate | Probe mounts only on the developer-gated dev overview surface (existing `developer` middleware) |
| T-05-28 | Tampering | Cross-module boundary violation in Desktop listener | mitigate | LockOnWindowHideOrClose imports only AppLockKeyService Public contract; Larastan arch tests enforce no Auth\Internal access |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH: zero installs); slopcheck N/A — Package Legitimacy Audit "Not applicable" |
</threat_model>

<verification>
- vendor/bin/pest Modules/Auth/tests Modules/Desktop/tests all green (full phase suite).
- vendor/bin/phpstan analyse clean at level 10 (Desktop facade allowlist updated; no cross-module Internal access).
- vendor/bin/pint --test clean for all new files.
- The human-verify checkpoint confirms the OS-integration paths (manual per 05-VALIDATION).
</verification>

<success_criteria>
- Desktop window hide/close locks immediately (D-06); macOS Touch ID releases the key via the native path.
- The LOCK-04 gate is exercisable end-to-end: the dev-console probe shows released/withheld and Pest proves release-only-while-unlocked.
- The Public AppLockKeyService contract is the single release point — ready for Phase 14 to adopt with zero rework (D-19).
- Larastan level 10 + Pint + the full Pest suite green.
</success_criteria>

<output>
Create `.planning/phases/beatrax-05-pin-biometric-app-lock-seed-009/05-06-SUMMARY.md` when done.
</output>
</content>
