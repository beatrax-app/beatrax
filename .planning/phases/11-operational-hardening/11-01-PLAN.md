---
phase: 11-operational-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php
  - Modules/Core/Models/SystemAlert.php
  - Modules/Core/Public/Services/SystemAlertQuery.php
  - Modules/Core/Public/Actions/AcknowledgeSystemAlert.php
  - Modules/Core/Providers/CoreServiceProvider.php
  - Modules/Core/tests/Unit/SystemAlertsMigrationTest.php
  - Modules/Core/tests/Unit/SystemAlertModelTest.php
  - Modules/Core/tests/Unit/SystemAlertQueryTest.php
  - Modules/Core/tests/Unit/AcknowledgeSystemAlertTest.php
  - tests/Contracts/BoundaryArchTest.php
  - tests/Helpers/RealSqliteFixture.php
autonomous: true
requirements: [FND-05]

must_haves:
  truths:
    - "system_alerts table exists with id/user_id/kind/severity/message/metadata/created_at/acknowledged_at columns and the two locked indexes."
    - "SystemAlert Eloquent model casts metadata to array, acknowledged_at to immutable_datetime, and exposes scopeActive + scopeByKind."
    - "SystemAlertQuery::active(?User) returns per-user rows plus system-wide (user_id IS NULL) rows in a single Collection."
    - "AcknowledgeSystemAlert stamps acknowledged_at = now() inside a DB transaction, is idempotent on already-acknowledged rows, and 404s on a foreign-user alert."
    - "BoundaryArchTest::systemAlertsTableNotJoinedToTransactions invariant passes against the current Modules tree."
    - "Real-on-disk SQLite fixture helper exists at tests/Helpers/RealSqliteFixture.php for downstream Wave 1+ backup/restore tests."
  decisions: [D-1108, D-1110]
  artifacts:
    - path: "Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php"
      provides: "system_alerts DDL + severity trigger pair"
      contains: "system_alerts"
    - path: "Modules/Core/Models/SystemAlert.php"
      provides: "Eloquent model with BelongsToUser + scopes"
      contains: "scopeActive"
    - path: "Modules/Core/Public/Services/SystemAlertQuery.php"
      provides: "Per-user + system-wide active() + count() reads"
      exports: ["active", "count"]
    - path: "Modules/Core/Public/Actions/AcknowledgeSystemAlert.php"
      provides: "Per-user-safe acknowledge action"
      contains: "__invoke"
    - path: "tests/Helpers/RealSqliteFixture.php"
      provides: "Pest helper that builds a real on-disk SQLite file for VACUUM INTO + integrity_check tests"
      contains: "RealSqliteFixture"
  key_links:
    - from: "Modules/Core/Providers/CoreServiceProvider.php"
      to: "SystemAlertQuery + AcknowledgeSystemAlert singleton bindings"
      via: "register()"
      pattern: "SystemAlertQuery"
    - from: "Modules/Core/Models/SystemAlert.php"
      to: "users.id"
      via: "BelongsToUser trait + nullable user_id FK"
      pattern: "BelongsToUser"
    - from: "tests/Contracts/BoundaryArchTest.php"
      to: "systemAlertsTableNotJoinedToTransactions"
      via: "appended it(...) block"
      pattern: "systemAlertsTableNotJoinedToTransactions"
---

<objective>
Wave 0 foundation for Phase 11. Ship the system_alerts persistence surface (migration + Eloquent model + Public read service + Public action) plus the load-bearing arch invariant that pins it as an operational-only surface. Add the real-on-disk SQLite Pest helper that subsequent plans (11-02 backup + 11-03 restore) depend on. Without this plan, no other Phase 11 plan can write or read alerts.

Purpose: Every downstream Phase 11 surface (BackupDatabaseCommand corrupt path, BackupFreshnessProbe, HealthCheckServiceProvider, SystemAlertsBanner) writes or reads system_alerts. Establishing the table + model + Public surface in Wave 0 keeps later waves vertical (each shipping a runnable user-visible slice rather than a layer).
Output: Migrated system_alerts table on the local SQLite DB, working SystemAlertQuery::active + AcknowledgeSystemAlert callable via `tinker`, BoundaryArchTest extended with the systemAlertsTableNotJoinedToTransactions invariant, and a reusable RealSqliteFixture helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-operational-hardening/11-CONTEXT.md
@.planning/phases/11-operational-hardening/11-RESEARCH.md
@.planning/phases/11-operational-hardening/11-PATTERNS.md
@.planning/phases/11-operational-hardening/11-VALIDATION.md

<interfaces>
<!-- Verbatim shapes the executor must mirror. Source: Phase 9 DriftAlerts surface read at planning time. -->

From Modules/DriftAlerts/Database/Migrations/2026_05_19_010001_create_drift_alerts_table.php:
- Anonymous-class migration with `private ?DatabaseManager $resolvedDb = null` cache + `schema()` + `db()` helpers
- Builder helper resolves via `Container::getInstance()->make(DatabaseManager::class)` (NO facade)
- Severity / state CHECK enforced via BEFORE INSERT + BEFORE UPDATE trigger pair using `RAISE(ABORT, '<msg>')`

From Modules/DriftAlerts/Models/DriftAlert.php:
- `final class DriftAlert extends Model` + `use BelongsToUser;` + `use HasFactory;`
- `protected $fillable` array, `protected function casts(): array` returning `['metadata' => 'array', 'acknowledged_at' => 'immutable_datetime', 'created_at' => 'immutable_datetime']`

From Modules/DriftAlerts/Public/Services/DriftAlertQuery.php:
- `final readonly class` with constructor-promoted `DatabaseManager $db` + `Clock $clock`
- Per-user scope: `->where('user_id', $user->id)`; Phase 11 EXTENDS with `->orWhereNull('user_id')` to include system-wide rows
- Returns Eloquent `Collection<int, SystemAlert>` from `SystemAlert::query()->...->get()` (Eloquent statics are allowed; DI feedback memory)

From Modules/DriftAlerts/Public/Actions/AcknowledgeDriftAlert.php:
- `final class` (no `readonly`), constructor DI of `Clock` + `DatabaseManager` + `Dispatcher`
- Signature: `public function __invoke(int $alertId, User $user): SystemAlert` (Phase 11 returns the updated model per CONTEXT D-1108 + Pattern Map)
- Cross-user miss → `throw new NotFoundHttpException('System alert not found.');`
- Idempotent: if already acknowledged, return the existing model without write
- Transactional: wrap the `$alert->update(...)` in `$this->db->connection()->transaction(...)`

From Modules/Core/Public/Contracts/Clock.php:
- `interface Clock { public function now(): CarbonImmutable; }` — used wherever `now()` was previously read

From tests/Contracts/BoundaryArchTest.php lines 834–879 (noScenarioMutationsJoinedToTransactionQueries):
- `it('...', function (): void { ... })` with `RecursiveIteratorIterator` + comment-strip regex `preg_replace('#/\*.*?\*/|//[^\n]*#s', '', $contents)`
- Hits collected into `$hits[]`, final `expect($hits)->toBe([], "<msg>")`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: system_alerts migration + Eloquent model + register migrate</name>
  <files>Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php, Modules/Core/Models/SystemAlert.php, Modules/Core/tests/Unit/SystemAlertsMigrationTest.php, Modules/Core/tests/Unit/SystemAlertModelTest.php</files>
  <read_first>
    - Modules/DriftAlerts/Database/Migrations/2026_05_19_010001_create_drift_alerts_table.php (analog migration shape — anonymous class, schema()/db() helpers, severity trigger pair)
    - Modules/DriftAlerts/Models/DriftAlert.php (analog Eloquent model — BelongsToUser, HasFactory optional, casts(), fillable)
    - Modules/Core/Public/Concerns/BelongsToUser.php (trait the model uses)
    - Modules/DriftAlerts/tests/Unit/DriftAlertsMigrationTest.php (analog migration test — column loop, trigger reject, index assertions, severity dataset)
    - .planning/phases/11-operational-hardening/11-CONTEXT.md `<domain>` section (lines covering system_alerts shape) and `<decisions>` D-1108 + D-1110
    - .planning/phases/11-operational-hardening/11-RESEARCH.md "Migration shape for system_alerts" Code Examples section + Pitfall 7
    - .planning/phases/11-operational-hardening/11-PATTERNS.md "create_system_alerts_table" section + "Eloquent model" section
  </read_first>
  <behavior>
    - Migration up() creates table `system_alerts` with: `id` (bigint PK), `user_id` (foreignId nullable, constrained on users, cascadeOnDelete), `kind` (string(64)), `severity` (string(16)), `message` (text), `metadata` (json nullable), `created_at` (timestamp default current), `acknowledged_at` (timestamp nullable). Two indexes: `(user_id, acknowledged_at)` and `(kind, acknowledged_at)`.
    - Two SQLite BEFORE-INSERT and BEFORE-UPDATE OF severity triggers reject severity values outside `'info','warning','critical'` via `RAISE(ABORT, 'Invalid system_alerts.severity value')`.
    - down() drops both triggers (`DROP TRIGGER IF EXISTS`) then drops the table.
    - SystemAlert model is `final class SystemAlert extends Model`, uses `BelongsToUser`, defines `$fillable` covering user_id/kind/severity/message/metadata/acknowledged_at, defines `casts(): array` returning metadata=>'array' + acknowledged_at=>'immutable_datetime' + created_at=>'immutable_datetime', defines scopeActive(Builder $q) returning $q->whereNull('acknowledged_at') and scopeByKind(Builder $q, string $kind) returning $q->where('kind', $kind). No HasFactory in v1 (DriftAlert keeps it for factories; SystemAlert does not need one for Phase 11 tests — raw DatabaseManager inserts cover the test seed path).
    - SystemAlertsMigrationTest asserts all 8 columns exist on the table via `Schema::hasColumns('system_alerts', [...])` (resolved via injected DatabaseManager); asserts both indexes via raw `PRAGMA index_list('system_alerts')`; asserts a severity='invalid' insert raises a PDOException with message containing 'Invalid system_alerts.severity value' (Pest dataset over `'info'`/`'warning'`/`'critical'` succeeds; `'invalid'`/`''` fails).
    - SystemAlertModelTest asserts: casts return the documented map; scopeActive() compiles to `WHERE acknowledged_at IS NULL`; scopeByKind('backup_corrupt') compiles to a `WHERE kind = 'backup_corrupt'` predicate; relation `user()` returns BelongsTo against `Modules\Core\Models\User`.
  </behavior>
  <action>Create the migration file at `Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php` (anonymous-class shape) and the model at `Modules/Core/Models/SystemAlert.php`. Migration MUST mirror Phase 9's drift_alerts migration verbatim for the `private ?DatabaseManager $resolvedDb` / `schema()` / `db()` helper triple (resolved via `Container::getInstance()->make(DatabaseManager::class)` — no facade). Column types and indexes are exactly the values listed in `<behavior>`. Add the severity trigger pair using `RAISE(ABORT, 'Invalid system_alerts.severity value')` per D-1108 and the RESEARCH Code Examples block; allowed values quoted as `'info','warning','critical'`. The model class uses `BelongsToUser` (from `Modules\Core\Public\Concerns\BelongsToUser`) which auto-applies the user-scope global scope + provides the `user()` relation; do NOT redefine the relation. Write both Pest test files under `Modules/Core/tests/Unit/`, declaring `uses(RefreshDatabase::class);` ONLY if they need RefreshDatabase — module Pest.php already binds RefreshDatabase to Feature/, so Unit migration tests use `Schema::hasColumns` via the injected DatabaseManager, NOT RefreshDatabase. After writing the migration, run `php artisan migrate --force` against the local SQLite DB so subsequent Larastan + Pest passes compile against the new table (per planner Step 5.7 + additional_context). Larastan level 10: add the `@use HasFactory<SystemAlertFactory>` PHPDoc ONLY if you also create a factory — Phase 11 does NOT create one, so omit the HasFactory trait entirely.</action>
  <verify>
    <automated>./vendor/bin/pest --filter='SystemAlertsMigrationTest|SystemAlertModelTest' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - File `Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php` exists; grep `system_alerts` returns the create_table call.
    - `php artisan migrate --force` exits 0; `sqlite3 database/database.sqlite ".schema system_alerts"` shows all 8 columns + the two indexes + both triggers.
    - File `Modules/Core/Models/SystemAlert.php` exists; grep `scopeActive` returns the method definition; grep `BelongsToUser` returns the trait use.
    - `./vendor/bin/pest --filter='SystemAlertsMigrationTest|SystemAlertModelTest' --parallel` exits 0.
    - `./vendor/bin/phpstan analyse --memory-limit=2G Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php Modules/Core/Models/SystemAlert.php` exits 0 (Larastan level 10 strict).
    - `./vendor/bin/pint --test Modules/Core/Database/Migrations/2026_05_20_010001_create_system_alerts_table.php Modules/Core/Models/SystemAlert.php` exits 0.
  </acceptance_criteria>
  <done>system_alerts table is migrated locally, SystemAlert model casts/scopes work, both Pest tests pass, Larastan + Pint clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SystemAlertQuery (read) + AcknowledgeSystemAlert (mutate) + CoreServiceProvider bindings</name>
  <files>Modules/Core/Public/Services/SystemAlertQuery.php, Modules/Core/Public/Actions/AcknowledgeSystemAlert.php, Modules/Core/Providers/CoreServiceProvider.php, Modules/Core/tests/Unit/SystemAlertQueryTest.php, Modules/Core/tests/Unit/AcknowledgeSystemAlertTest.php</files>
  <read_first>
    - Modules/DriftAlerts/Public/Services/DriftAlertQuery.php (analog Public service — constructor-DI'd DatabaseManager + Clock, per-user scoping, Eloquent ::query() statics)
    - Modules/DriftAlerts/Public/Actions/AcknowledgeDriftAlert.php (analog Public action — constructor DI, __invoke signature, 404 + idempotent posture, transactional)
    - Modules/DriftAlerts/tests/Feature/AcknowledgeDriftAlertTest.php (analog action test — cross-user 404, idempotent, DB assertion)
    - Modules/Core/Models/SystemAlert.php (just-created in Task 1; use it for ::query() statics + scopes)
    - Modules/Core/Providers/CoreServiceProvider.php (extension point — add singleton bindings)
    - Modules/Core/Public/Contracts/Clock.php (DI argument for now())
    - .planning/phases/11-operational-hardening/11-CONTEXT.md `<decisions>` D-1108 + `<code_context>` "Established Patterns" section (DI-only feedback rule)
    - .planning/phases/11-operational-hardening/11-PATTERNS.md sections on SystemAlertQuery + AcknowledgeSystemAlert (verbatim where() shapes including the orWhereNull system-wide carve-out)
    - .planning/phases/11-operational-hardening/11-RESEARCH.md Pitfall 8 (banner re-render N+1 — eager loading hint)
  </read_first>
  <behavior>
    - `SystemAlertQuery` is a `final readonly class` with constructor-promoted `DatabaseManager $db` + `Clock $clock` (Clock is unused inside this query in v1 but kept for parity with DriftAlertQuery and for future "alerts created in last 24h" filters — keep it injected, do not call `now()` for v1).
    - `active(?User $user): Collection<int, SystemAlert>` returns `SystemAlert::query()->whereNull('acknowledged_at')->where(function (Builder $q) use ($user): void { if ($user !== null) { $q->where('user_id', $user->id)->orWhereNull('user_id'); } else { $q->whereNull('user_id'); } })->orderByRaw("CASE severity WHEN 'critical' THEN 0 WHEN 'warning' THEN 1 ELSE 2 END")->orderBy('created_at', 'asc')->get()`. The orderByRaw clause sorts critical → warning → info per UI-SPEC State Matrix (severity order, then chronological within each tier).
    - `count(?User $user): int` returns the integer count using the same per-user scope (use raw DatabaseManager `->table('system_alerts')->whereNull(...)->where(...)->count()` rather than Eloquent for the count to avoid strict-rules staticMethod.dynamicCall — mirror the PreviewWizard::needsIcsAccountName hybrid pattern from prior phases).
    - Add `@return Collection<int, SystemAlert>` PHPDoc on `active()` (Larastan level 10 requires explicit generic typing on Eloquent Collection returns).
    - `AcknowledgeSystemAlert` is a `final class` (NOT readonly — needs to call methods on mutable state during the transaction) with constructor-DI'd `DatabaseManager $db` + `Clock $clock`.
    - Signature: `public function __invoke(int $alertId, User $user): SystemAlert`.
    - Lookup: `SystemAlert::query()->where('id', $alertId)->where(function ($q) use ($user) { $q->where('user_id', $user->id)->orWhereNull('user_id'); })->first()`. NULL → throw `Symfony\Component\HttpKernel\Exception\NotFoundHttpException('System alert not found.')`.
    - Idempotent: if `$alert->acknowledged_at !== null`, return `$alert` without writing.
    - Transactional write: wrap `$alert->update(['acknowledged_at' => $this->clock->now()])` in `$this->db->connection()->transaction(...)`. Return the refreshed `$alert`.
    - CoreServiceProvider extended: in `register()`, bind both `SystemAlertQuery::class` and `AcknowledgeSystemAlert::class` as singletons (mirror the existing DriftAlerts provider's binding shape).
    - SystemAlertQueryTest: Pest dataset with three scenarios — (a) user A sees own alerts but not user B's; (b) user A sees user_id IS NULL system-wide alerts; (c) critical alerts come before warnings before infos in the Collection. Each test seeds rows via raw `DatabaseManager::table('system_alerts')->insert(...)` and asserts the Collection IDs in expected order.
    - AcknowledgeSystemAlertTest: (a) acknowledging a own-user alert stamps `acknowledged_at`; (b) acknowledging a system-wide (user_id NULL) alert as user A succeeds; (c) acknowledging user B's alert as user A throws NotFoundHttpException AND leaves the row's `acknowledged_at` IS NULL; (d) double-acknowledge is idempotent (second call returns same model, does NOT update `acknowledged_at` to a new timestamp).
  </behavior>
  <action>Write `SystemAlertQuery` at `Modules/Core/Public/Services/SystemAlertQuery.php` and `AcknowledgeSystemAlert` at `Modules/Core/Public/Actions/AcknowledgeSystemAlert.php`. Use constructor DI (no facades) per the project DI feedback memory; Eloquent ::query() statics ARE allowed. Order the active() Collection with the `orderByRaw("CASE severity ...")` clause exactly so the banner's critical-first stack renders in correct order. The system-wide carve-out (`orWhereNull('user_id')`) is load-bearing for the AppBootHealthCheckTest in plan 11-03 — get the predicate shape exactly right. Extend `Modules/Core/Providers/CoreServiceProvider.php`'s `register()` method by adding `$this->app->singleton(SystemAlertQuery::class);` and `$this->app->singleton(AcknowledgeSystemAlert::class);` after any existing Public-service bindings. Test files go to `Modules/Core/tests/Unit/` — both use `uses(RefreshDatabase::class);` on the first line after the opening `<?php` because they seed multiple rows per test. Test seeds use raw `DatabaseManager::table('system_alerts')->insert([...])` (NOT factory because no factory exists) with `created_at` = `now()->subSeconds(N)` to control ordering. Cross-user test creates a second User via `User::factory()->create()` to anchor user_id values. Note for tests: the `orderByRaw` SQL fragment must be unit-tested by asserting the explicit ID order across three rows of mixed severity — do NOT rely on insertion order alone.</action>
  <verify>
    <automated>./vendor/bin/pest --filter='SystemAlertQueryTest|AcknowledgeSystemAlertTest' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - File `Modules/Core/Public/Services/SystemAlertQuery.php` exists; grep `orWhereNull` returns the system-wide-scope predicate; grep `orderByRaw` returns the severity-sort clause.
    - File `Modules/Core/Public/Actions/AcknowledgeSystemAlert.php` exists; grep `NotFoundHttpException` returns the throw site; grep `connection()->transaction(` returns the transactional wrapper.
    - File `Modules/Core/Providers/CoreServiceProvider.php` contains two new lines binding `SystemAlertQuery` and `AcknowledgeSystemAlert` as singletons (grep `SystemAlertQuery::class)` returns the register call).
    - `./vendor/bin/pest --filter='SystemAlertQueryTest|AcknowledgeSystemAlertTest' --parallel` exits 0.
    - `php artisan tinker --execute='echo app(\Modules\Core\Public\Services\SystemAlertQuery::class)::class;'` prints `Modules\Core\Public\Services\SystemAlertQuery` (verifies the singleton resolves).
    - `./vendor/bin/phpstan analyse --memory-limit=2G Modules/Core/Public/Services/SystemAlertQuery.php Modules/Core/Public/Actions/AcknowledgeSystemAlert.php Modules/Core/Providers/CoreServiceProvider.php` exits 0.
    - `./vendor/bin/pint --test Modules/Core/Public/Services/SystemAlertQuery.php Modules/Core/Public/Actions/AcknowledgeSystemAlert.php Modules/Core/Providers/CoreServiceProvider.php` exits 0.
  </acceptance_criteria>
  <done>Public read service + Public action ship, both wired into the container as singletons, both unit-tested for per-user + system-wide + cross-user + idempotent paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BoundaryArchTest::systemAlertsTableNotJoinedToTransactions invariant + RealSqliteFixture helper</name>
  <files>tests/Contracts/BoundaryArchTest.php, tests/Helpers/RealSqliteFixture.php</files>
  <read_first>
    - tests/Contracts/BoundaryArchTest.php lines 834–879 (`noScenarioMutationsJoinedToTransactionQueries` block — the verbatim shape to mirror)
    - .planning/phases/11-operational-hardening/11-RESEARCH.md "BoundaryArchTest new invariants" Code Examples (lines 859–881 — the verbatim function body for systemAlertsTableNotJoinedToTransactions)
    - .planning/phases/11-operational-hardening/11-RESEARCH.md Open Question #7 (real-on-disk SQLite fixture — the helper this task creates)
    - Modules/Core/tests/TestCase.php (Core module's TestCase — see what's already available before bolting on a new helper)
    - Modules/Ledger/tests/Feature/RederiveFingerprintsCommandTest.php (precedent for seeding rows via raw DatabaseManager)
  </read_first>
  <behavior>
    - Append a new `it('does not allow system_alerts to be JOINed onto the transactions table (systemAlertsTableNotJoinedToTransactions)', function (): void { ... })` block to `tests/Contracts/BoundaryArchTest.php` immediately after the `noScenarioMutationsJoinedToTransactionQueries` block.
    - The new block walks `Modules/` recursively, skipping `/tests/` paths and non-`.php` files, comment-strips each file via `preg_replace('#/\*.*?\*/|//[^\n]*#s', ...)` and detects co-occurrence of `->join|leftJoin|rightJoin|crossJoin\(['"]system_alerts['"]` with `['"]transactions['"]` (regex from RESEARCH §Code Examples). Failure mode is `$hits[]` collection + final `expect($hits)->toBe([], "system_alerts must never be JOINed onto the transactions table. Offenders:\n  ".implode("\n  ", $hits))`.
    - Create `tests/Helpers/RealSqliteFixture.php`: a final class with two static methods. `RealSqliteFixture::create(string $name = 'fixture', array $schemas = self::DEFAULT_SCHEMAS): string` returns the absolute path to a freshly-created on-disk SQLite file (suffixed by `.sqlite`) located under `storage/framework/testing/<random>/<name>.sqlite`, opens it via a fresh `PDO` connection, and applies each DDL statement in `$schemas` in order. The `DEFAULT_SCHEMAS` class constant holds the stripped-down `transactions` + `system_alerts` `CREATE TABLE` statements sufficient for Phase 11 backup/restore tests. A future phase that needs additional tables passes its own DDL list without forking the helper. `RealSqliteFixture::cleanup(string $path): void` removes the file and its containing temp dir.
    - The `DEFAULT_SCHEMAS` constant is a `public const array` (Larastan level 10 + PHP 8.5 typed-constant support) containing the two `CREATE TABLE` strings as separate array entries. Callers can pass `[...RealSqliteFixture::DEFAULT_SCHEMAS, 'CREATE TABLE my_extra_table (...)']` to add tables without losing the defaults.
    - The helper does NOT use any facade. It uses `new \PDO('sqlite:'.$path)` directly + native `mkdir()` / `unlink()` / `rmdir()`. It is intentionally outside the Modules tree (it lives under `tests/Helpers/`) so the arch invariants do not apply.
    - Sanity-check the helper with a tiny Pest test inline at `tests/Helpers/RealSqliteFixtureTest.php` (NOT under Modules) asserting `create()` returns a path that `is_file()` and `cleanup()` removes the file.
  </behavior>
  <action>Append one `it(...)` block to `tests/Contracts/BoundaryArchTest.php` containing the verbatim function body sketched in RESEARCH §Code Examples for `systemAlertsTableNotJoinedToTransactions`. Do NOT add the `noFacadeCallsFromCoreConsoleCommands` block in THIS task — that invariant is deferred to plan 11-04 because the production code surfaces it scans against don't exist until then (premature insertion would fail-on-empty-dir which is fine but the planner pins it to its semantic owner-plan for traceability). Then create `tests/Helpers/RealSqliteFixture.php` with the two static methods described; the create() body opens a fresh `PDO` and iterates `$schemas` calling `$pdo->exec($ddl)` on each entry. The `DEFAULT_SCHEMAS` constant holds two entries: `CREATE TABLE transactions (id INTEGER PRIMARY KEY, user_id INTEGER NULL, amount_minor INTEGER, currency TEXT, booked_at TEXT)` and `CREATE TABLE system_alerts (id INTEGER PRIMARY KEY, user_id INTEGER NULL, kind TEXT, severity TEXT, message TEXT, metadata TEXT, created_at TEXT, acknowledged_at TEXT NULL)`. Passing a custom `$schemas` array is the documented extension hook for future phases. After writing the fixture, run `PRAGMA journal_mode = WAL` on the file so the helper's output matches the substrate's production posture (downstream BackupDatabaseCommandTest expects WAL on the source). Add the inline `tests/Helpers/RealSqliteFixtureTest.php` sanity test that asserts round-trip create/cleanup.</action>
  <verify>
    <automated>./vendor/bin/pest --filter='BoundaryArchTest|RealSqliteFixture' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'systemAlertsTableNotJoinedToTransactions' tests/Contracts/BoundaryArchTest.php` returns at least 1 after stripping comments.
    - `./vendor/bin/pest --filter='BoundaryArchTest::systemAlertsTableNotJoinedToTransactions'` exits 0 (passes against the current Modules tree — no existing offenders).
    - `./vendor/bin/pest --filter='RealSqliteFixtureTest'` exits 0; fixture file is created and removed within the test.
    - File `tests/Helpers/RealSqliteFixture.php` exists; grep `VACUUM INTO` is absent (helper does not invoke VACUUM — that is the consumer's job); grep `PRAGMA journal_mode = WAL` returns the journal-mode setup call; grep `DEFAULT_SCHEMAS` returns the class constant (proves the schemas-parameter extension hook exists).
    - The Pest sanity test in `tests/Helpers/RealSqliteFixtureTest.php` includes ONE scenario exercising the override: `RealSqliteFixture::create('custom', [...RealSqliteFixture::DEFAULT_SCHEMAS, 'CREATE TABLE custom_table (id INTEGER PRIMARY KEY)'])` returns a path whose `PRAGMA table_info('custom_table')` shows the override applied alongside the defaults.
    - `./vendor/bin/phpstan analyse --memory-limit=2G tests/Contracts/BoundaryArchTest.php tests/Helpers/RealSqliteFixture.php` exits 0.
    - `./vendor/bin/pint --test tests/Contracts/BoundaryArchTest.php tests/Helpers/RealSqliteFixture.php` exits 0.
  </acceptance_criteria>
  <done>BoundaryArchTest extended with the system_alerts/transactions JOIN invariant + reusable on-disk SQLite fixture helper available to Wave 1+ backup/restore tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-session → AcknowledgeSystemAlert | Authenticated user attempts to acknowledge alerts owned by other users |
| migration up() → SQLite triggers | DDL migration adds CHECK enforcement via BEFORE INSERT/UPDATE triggers |
| Public read service → multi-user data | `SystemAlertQuery::active(?User)` must scope per-user AND include system-wide (NULL) rows without leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | system_alerts migration | mitigate | Severity column gated by BEFORE INSERT and BEFORE UPDATE OF severity triggers raising ABORT on values outside `'info','warning','critical'`; Pest dataset proves invalid severities throw PDOException. |
| T-11-01-02 | Information Disclosure | SystemAlertQuery cross-user leak | mitigate | Per-user scope predicate is the load-bearing safety property; SystemAlertQueryTest scenario (a) seeds user A + user B rows and asserts user A's Collection contains only user A's row IDs (plus any NULL user_id rows). Larastan level 10 + the new arch invariant prevent regressions. |
| T-11-01-03 | Information Disclosure | AcknowledgeSystemAlert cross-user mutation | mitigate | Action lookup uses `->where('id', $alertId)->where(orWhereNull-scope-clause)`; cross-user miss throws NotFoundHttpException; AcknowledgeSystemAlertTest scenario (c) asserts the throw AND that the row's `acknowledged_at` IS NULL after the failed call. |
| T-11-01-04 | Tampering | systemAlertsTableNotJoinedToTransactions arch invariant | mitigate | Arch test fails any module-tree file that JOINs system_alerts onto transactions; locks the operational/domain separation D-1108 calls out. |
| T-11-01-05 | Information Disclosure | RealSqliteFixture file on disk | accept | Helper writes under `storage/framework/testing/<random>/`; permissions follow PHP default umask. No production-secret data; cleanup() removes the file. v1 acceptable. |
| T-11-01-SC | Tampering | composer install | accept | This plan installs NO new packages; legitimacy gate inapplicable per RESEARCH §Package Legitimacy Audit. |
</threat_model>

<verification>
- `./vendor/bin/pest --filter='SystemAlertsMigrationTest|SystemAlertModelTest|SystemAlertQueryTest|AcknowledgeSystemAlertTest|BoundaryArchTest::systemAlertsTableNotJoinedToTransactions|RealSqliteFixtureTest'` exits 0.
- `./vendor/bin/phpstan analyse --memory-limit=2G` (whole tree) exits 0.
- `./vendor/bin/pint --test` exits 0.
- `php artisan migrate --force` exits 0 against a fresh `database/database.sqlite`; `sqlite3 database/database.sqlite ".schema system_alerts"` shows all 8 columns + 2 indexes + 2 triggers.
- `php artisan tinker --execute='echo \Modules\Core\Models\SystemAlert::query()->count();'` prints `0` (table is reachable and empty).
</verification>

<success_criteria>
- system_alerts migration applied; table + indexes + triggers visible via `.schema`.
- SystemAlert model + SystemAlertQuery + AcknowledgeSystemAlert all resolvable via the container and unit-tested.
- BoundaryArchTest extended with systemAlertsTableNotJoinedToTransactions and passes.
- RealSqliteFixture helper available + sanity-tested.
- All Pest + Larastan + Pint gates pass on the plan's touched files.
- Phase 11 Wave 1 (plan 11-02) can immediately consume `SystemAlert::create(...)` for the backup_corrupt path and `RealSqliteFixture` for the backup-command on-disk tests.
</success_criteria>

<output>
Create `.planning/phases/11-operational-hardening/11-01-SUMMARY.md` when done.
</output>
