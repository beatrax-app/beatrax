---
phase: 11-operational-hardening
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - Modules/Core/Internal/Console/RestoreDatabaseCommand.php
  - Modules/Core/Internal/Console/Probes/Probe.php
  - Modules/Core/Internal/Console/Probes/ProbeResult.php
  - Modules/Core/Internal/Console/Probes/BootProbeState.php
  - Modules/Core/Internal/Console/Probes/WalModeProbe.php
  - Modules/Core/Internal/Console/Probes/SynchronousModeProbe.php
  - Modules/Core/Internal/Console/Probes/BackupFreshnessProbe.php
  - Modules/Core/Internal/Console/DoctorCommand.php
  - Modules/Core/Internal/Providers/HealthCheckServiceProvider.php
  - Modules/Core/Providers/CoreServiceProvider.php
  - Modules/Core/tests/Feature/RestoreDatabaseCommandTest.php
  - Modules/Core/tests/Feature/RestoreSuccessPathTest.php
  - Modules/Core/tests/Unit/DoctorProbesTest.php
  - Modules/Core/tests/Feature/DoctorCommandTest.php
  - Modules/Core/tests/Feature/AppBootHealthCheckTest.php
autonomous: true
requirements: [FND-05]

must_haves:
  truths:
    - "User can run `php artisan db:restore --confirm --force-maintenance <path>` and round-trip a backup file back over the live DB safely, with a pre-restore snapshot saved automatically."
    - "Restore refuses without --confirm, without maintenance mode (and without --force-maintenance), AND when the source file fails integrity_check pre-swap."
    - "`php artisan diederik:doctor` prints WAL mode + synchronous mode + backup freshness probes, exits non-zero on any warning/critical."
    - "BackupFreshnessProbe writes a `system_alerts(kind=backup_overdue, severity=warning)` row when the newest verified backup is >48h old."
    - "HealthCheckServiceProvider writes a `system_alerts` warning row on boot if PRAGMA journal_mode != WAL or PRAGMA synchronous != NORMAL, but does NOT halt boot."
  decisions: [D-1112, D-1113, D-1114, D-1115, D-1116, D-1118, D-1119]
  artifacts:
    - path: "Modules/Core/Internal/Console/RestoreDatabaseCommand.php"
      provides: "db:restore artisan command with triple-rail safety"
      contains: "VACUUM INTO"
    - path: "Modules/Core/Internal/Console/Probes/Probe.php"
      provides: "Probe interface (label() + run())"
      contains: "interface Probe"
    - path: "Modules/Core/Internal/Console/Probes/ProbeResult.php"
      provides: "Immutable ProbeResult value object"
      contains: "final readonly class ProbeResult"
    - path: "Modules/Core/Internal/Console/Probes/BootProbeState.php"
      provides: "Mutable singleton holding the per-process `booted` flag for HealthCheckServiceProvider's dedupe gate"
      contains: "BootProbeState"
    - path: "Modules/Core/Internal/Providers/HealthCheckServiceProvider.php"
      provides: "Boot-time PRAGMA verification + alert write (non-halting)"
      contains: "ConnectionEstablished"
  key_links:
    - from: "Modules/Core/Internal/Console/RestoreDatabaseCommand.php"
      to: "DatabaseManager::purge() before file copy"
      via: "DI'd DatabaseManager"
      pattern: "purge\\("
    - from: "Modules/Core/Internal/Console/DoctorCommand.php"
      to: "Probe[] iteration"
      via: "constructor-DI of 3 probes (existing version checks kept inline)"
      pattern: "WalModeProbe"
    - from: "Modules/Core/Internal/Providers/HealthCheckServiceProvider.php"
      to: "ConnectionEstablished listener"
      via: "Dispatcher::listen"
      pattern: "ConnectionEstablished"
---

<objective>
Wave 2 vertical slice: ship `db:restore` (triple-rail safety) + the three Doctor probes built on a shared Probe contract + a non-halting app-boot PRAGMA health check. After this plan, the user can round-trip a backup file via `php artisan db:restore --confirm --force-maintenance <path>`, observe SQLite health via `php artisan diederik:doctor`, and trust that a misconfigured PRAGMA drift surfaces as a warning banner on the next page load.

Purpose: 11-02 ships backup; 11-03 closes the operational loop with restore + diagnostics + boot-time drift detection. Together they satisfy FND-05 SC #2 ("The latest backup is automatically verified by re-opening it and running PRAGMA integrity_check, with failures surfaced to the user"). This plan delivers three independently-runnable surfaces, each a vertical slice.
Output: Working `db:restore` + three new Probes + extended `diederik:doctor` + boot health-check provider; system_alerts surfaces backup_overdue / wal_mode_missing / synchronous_misconfigured kinds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-operational-hardening/11-CONTEXT.md
@.planning/phases/11-operational-hardening/11-RESEARCH.md
@.planning/phases/11-operational-hardening/11-PATTERNS.md
@.planning/phases/11-operational-hardening/11-VALIDATION.md
@.planning/phases/11-operational-hardening/11-01-SUMMARY.md
@.planning/phases/11-operational-hardening/11-02-SUMMARY.md

<interfaces>
<!-- Key surfaces this plan composes against. -->

From Modules/Core/Internal/Providers/SqliteOptimizationsProvider.php (analog for HealthCheckServiceProvider):
- `final class … extends ServiceProvider` with `public function boot(Dispatcher $events): void`
- Listens `ConnectionEstablished::class` via `$events->listen(...)`
- Reads `$event->connection`, guards on `getDriverName() === 'sqlite'`
- Calls `$connection->statement('PRAGMA journal_mode = WAL')` etc — Phase 11's HealthCheckServiceProvider READS PRAGMAs via `$connection->select('PRAGMA journal_mode')` instead

From Modules/Core/Internal/Console/DoctorCommand.php (existing — extension point):
- `class DoctorCommand extends Command` (currently NOT final, ok to leave so) with `protected $signature = 'diederik:doctor'`
- Existing `handle()` uses inline `reportTool()` helpers + `$blockers` + `$warnings` arrays; exit code map: 0 ok / 1 warning(s) / 2 blocker(s). Preserve this exit code semantic.

From Modules/Core/Models/SystemAlert.php (Wave 0):
- `SystemAlert::create(['user_id' => null, 'kind' => 'backup_overdue'|'wal_mode_missing'|'synchronous_misconfigured', 'severity' => 'warning', 'message' => '...', 'metadata' => [...]])`

From Modules/Core/Internal/Console/InstallCommand.php:
- DI shape — Repository $config, Dispatcher $events, DatabaseManager $db, Filesystem $files. RestoreDatabaseCommand adds `Kernel $artisan` for `Artisan::call('down')` / `'up'` (Kernel = `Illuminate\Contracts\Console\Kernel`)

From Modules/Core/Internal/Console/BackupDatabaseCommand.php (just-built in 11-02):
- Reusable VACUUM INTO pattern for the pre-restore snapshot; reusable fresh-PDO integrity_check pattern
- Sidecar atomic-write pattern
- The `'core.backups_directory'` container binding (singleton returning `base_path('storage/app/backups')`) is reused by `BackupFreshnessProbe` so test overrides apply uniformly across probes + command.

Maintenance mode primitives:
- `$this->artisan->call('down')` puts the app down (writes `storage/framework/down`)
- `$this->artisan->call('up')` brings it back up
- Check current state: `$this->files->exists(base_path('storage/framework/down'))` returns true while down
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Probe contract + ProbeResult value object + three probes + extended DoctorCommand</name>
  <files>Modules/Core/Internal/Console/Probes/Probe.php, Modules/Core/Internal/Console/Probes/ProbeResult.php, Modules/Core/Internal/Console/Probes/WalModeProbe.php, Modules/Core/Internal/Console/Probes/SynchronousModeProbe.php, Modules/Core/Internal/Console/Probes/BackupFreshnessProbe.php, Modules/Core/Internal/Console/DoctorCommand.php, Modules/Core/tests/Unit/DoctorProbesTest.php, Modules/Core/tests/Feature/DoctorCommandTest.php</files>
  <read_first>
    - Modules/Core/Internal/Console/DoctorCommand.php (CURRENT file — read full content; the extension is in-place)
    - Modules/Core/tests/Feature/DoctorCommandTest.php (CURRENT file — currently ~11 lines; extend)
    - Modules/Core/Internal/Providers/SqliteOptimizationsProvider.php (PRAGMA read mechanic via `$connection->select('PRAGMA ...')`)
    - Modules/Core/Public/Contracts/Clock.php (interface — DI argument for BackupFreshnessProbe's 48h cutoff)
    - .planning/phases/11-operational-hardening/11-CONTEXT.md `<decisions>` D-1115, D-1116, D-1110 (severity assignment per kind) AND the `### Claude's Discretion` block ("The internal name of the `Probe` contract and whether existing DoctorCommand internals get refactored as part of this phase or deferred.")
    - .planning/phases/11-operational-hardening/11-RESEARCH.md Pattern 7 (Probe contract + ProbeResult — verbatim sketch in §Code Examples) + Open Question #6 (whether to refactor existing inline doctor probes — decision: leave existing inline tool-version checks AS-IS for v1; the three new probes append; defer further refactor to a future polish phase)
    - .planning/phases/11-operational-hardening/11-PATTERNS.md "Probe.php" + "ProbeResult.php" + "WalModeProbe / SynchronousModeProbe" + "BackupFreshnessProbe" sections
  </read_first>
  <behavior>
    - `Probe` interface (`Modules\Core\Internal\Console\Probes`) declares `label(): string` + `run(): ProbeResult`. Probes MUST NOT throw — they wrap any IO/SQL in try/catch and surface failure as a critical `ProbeResult` with the exception message.
    - `ProbeResult` is a `final readonly class` with constructor-promoted `public string $severity` (literal `'ok'|'warning'|'critical'`), `public string $message`, `public array $metadata = []`. Add a PHPDoc declaration on the constructor: `@param 'ok'|'warning'|'critical' $severity` + `@param array<string, scalar|null> $metadata` for Larastan level 10.
    - `WalModeProbe` (`final class WalModeProbe implements Probe`) has constructor DI of `DatabaseManager $db`. `label()` returns `'SQLite WAL mode'`. `run()` calls `$this->db->connection()->select('PRAGMA journal_mode')`, reads the first row's `journal_mode` property, returns `ProbeResult('ok', 'WAL active.')` if equal to `'wal'` (case-insensitive), otherwise `ProbeResult('warning', "SQLite journal_mode is '{$current}' (expected 'wal').", ['current_mode' => $current])`. Wrap the PRAGMA read in try/catch returning a critical result on any exception.
    - `SynchronousModeProbe` (`final class SynchronousModeProbe implements Probe`) mirrors WalModeProbe but reads `PRAGMA synchronous`. SQLite returns synchronous as integer 0–3 (OFF=0, NORMAL=1, FULL=2, EXTRA=3). `run()` returns `'ok'` for value `1` (NORMAL) and `'warning'` otherwise with metadata `['current_level' => (int) $value]`.
    - `BackupFreshnessProbe` (`final class BackupFreshnessProbe implements Probe`) has constructor DI of `Filesystem $files`, `Clock $clock`, `DatabaseManager $db` (only used to write the SystemAlert via Eloquent), AND `string $backupsPath` (resolved via the `'core.backups_directory'` container binding established by Plan 11-02 — same binding, no second abstraction). The `string $backupsPath` parameter is wired via a contextual binding in `CoreServiceProvider::register()`: `$this->app->when(BackupFreshnessProbe::class)->needs('$backupsPath')->give(fn () => $this->app->make('core.backups_directory'));`. Tests override the binding via `$this->app->instance('core.backups_directory', $tempDir.'/storage/app/backups')` BEFORE the container resolves the probe — no new `BackupsDirectory` value object is created.
    - `label()` returns `'Backup freshness'`. `run()` lists files under `$this->backupsPath` matching `*.meta.json` (use `$this->files->files($this->backupsPath)`), picks the newest by mtime, reads its `completed_at` ISO timestamp, computes hours-since-now. If no sidecar exists OR newest is older than 48 hours: write a `SystemAlert::create([user_id=>null, kind=>'backup_overdue', severity=>'warning', message=>"Most recent verified backup is {$hours}h old.", metadata=>['hours_old' => $hours]])` AND return `ProbeResult('warning', "Most recent verified backup is {$hours}h old.", ['hours_old' => $hours])`. If newest ≤48h: return `ProbeResult('ok', "Most recent verified backup is {$hours}h old.", ['hours_old' => $hours])` and do NOT write.
    - DoctorCommand extended: add the three new probes via constructor parameters (`WalModeProbe $walProbe`, `SynchronousModeProbe $synchronousProbe`, `BackupFreshnessProbe $backupFreshnessProbe`). In `handle()`, AFTER the existing inline tool-version checks, run each probe via `$result = $probe->run()`, print a row in the summary table (`{$probe->label()}: {$result->severity} — {$result->message}`), and bump the exit code per the existing convention (ok → no change; warning → exit ≥1; critical → exit 2). DO NOT refactor the existing inline checks — append.
    - DoctorProbesTest unit-tests each probe in isolation: (a) WalModeProbe ok on real Herd DB (WAL active); (b) WalModeProbe warning after a forced `PRAGMA journal_mode = DELETE` (use RealSqliteFixture or an inline DI swap that returns 'delete' from the select); (c) SynchronousModeProbe ok / warning across synchronous values; (d) BackupFreshnessProbe with no sidecars → warning + SystemAlert written; (e) BackupFreshnessProbe with a fresh sidecar (<48h) → ok + NO SystemAlert written; (f) BackupFreshnessProbe with a stale sidecar (>48h) → warning + SystemAlert written; (g) probe try/catch: forcing the PRAGMA read to throw via DI swap returns a critical ProbeResult (no exception bubbles out).
    - DoctorCommandTest (extended) asserts: (a) clean state → exit 0 + all three new probe lines visible (`Modules/Core/tests/Feature/DoctorCommandTest.php` already exists; extend, do NOT replace); (b) forced PRAGMA drift → exit 1 + the right probe line marked warning.
    - Container registration: bind WalModeProbe, SynchronousModeProbe, BackupFreshnessProbe as transient (default) — no singleton binding needed (each is small + stateless except for the BackupFreshnessProbe's filesystem read which we want fresh per run). The `'core.backups_directory'` singleton binding is already established in Plan 11-02; this plan only adds the contextual `needs('$backupsPath')` wiring for BackupFreshnessProbe (and later for HealthCheckServiceProvider if it ever needs the path).
  </behavior>
  <action>Create `Probe.php` + `ProbeResult.php` under `Modules/Core/Internal/Console/Probes/` — verbatim from RESEARCH §Pattern 7. Then create `WalModeProbe.php`, `SynchronousModeProbe.php`, `BackupFreshnessProbe.php` with the constructor signatures + run() bodies described.

For `BackupFreshnessProbe`, accept `string $backupsPath` as the final constructor parameter (after `DatabaseManager`, `Filesystem`, `Clock`). Wire it via `$this->app->when(BackupFreshnessProbe::class)->needs('$backupsPath')->give(fn () => $this->app->make('core.backups_directory'));` inside `CoreServiceProvider::register()` (the `'core.backups_directory'` singleton was added in Plan 11-02 Task 2). Tests inject a temp dir via `$this->app->instance('core.backups_directory', $tempDir);` in `beforeEach` BEFORE resolving the probe. **Do NOT introduce a `BackupsDirectory` value-object class** — the contextual string binding is enough; less code, same DI testability, no new abstraction to maintain.

Modify `DoctorCommand.php` IN PLACE — read the file first, add the three new constructor parameters (preserve any existing ones), and APPEND the probe iteration to the existing `handle()` flow.

<discretion_note>
Per CONTEXT.md `### Claude's Discretion`: "The internal name of the `Probe` contract and whether existing DoctorCommand internals get refactored as part of this phase or deferred." — choosing to DEFER the inline tool-version-check refactor to keep this phase scoped. The three new probes append to the existing inline checks; the inline checks are NOT migrated onto the `Probe` interface in Phase 11. Rationale: the inline checks already work, work that doesn't break shouldn't ship a refactor, and the contract is locked in this phase so a future polish phase can migrate them at its own pace. This is the explicit "deferral" path Claude's Discretion allows; D-1115's prose ("Any existing probes in the current `DoctorCommand` get refactored into the same contract during this phase — stylistic carry-cost worth paying once") is OVERRIDDEN by the Discretion list. Document the deferral as a one-paragraph comment in `DoctorCommand`'s class docblock so a future contributor sees the intentional split.
</discretion_note>

The probe iteration is a small loop: `foreach ([$this->walProbe, $this->synchronousProbe, $this->backupFreshnessProbe] as $probe) { $result = $probe->run(); /* print + bump exit code */ }`. Extend the existing DoctorCommandTest (read first; add new test() blocks; do NOT delete existing ones). For the DoctorProbesTest, use the RealSqliteFixture helper from plan 11-01 to give the WAL/synchronous probes a real on-disk DB to read against — substitute the DatabaseManager via `$this->app->instance(DatabaseManager::class, $fakeManager)` ONLY if needed; the simpler shape is constructing the probe with a `DatabaseManager` resolved against a test-config-overridden sqlite connection. For BackupFreshnessProbe, use `sys_get_temp_dir().'/diederik-test-'.uniqid().'/storage/app/backups/'` as the backups dir + the `$this->app->instance('core.backups_directory', $tempDir);` override BEFORE the probe resolves.</action>
  <verify>
    <automated>./vendor/bin/pest --filter='DoctorProbesTest|DoctorCommandTest' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `Modules/Core/Internal/Console/Probes/{Probe.php,ProbeResult.php,WalModeProbe.php,SynchronousModeProbe.php,BackupFreshnessProbe.php}`.
    - `grep -c "implements Probe" Modules/Core/Internal/Console/Probes/*.php` returns 3 (one per implementation, not the interface itself).
    - `php artisan diederik:doctor` runs end-to-end against the local DB, prints lines for `SQLite WAL mode`, `SQLite synchronous mode`, `Backup freshness`, exits 0 (clean state).
    - `./vendor/bin/pest --filter='DoctorProbesTest|DoctorCommandTest' --parallel` exits 0.
    - `./vendor/bin/phpstan analyse --memory-limit=2G Modules/Core/Internal/Console/Probes/ Modules/Core/Internal/Console/DoctorCommand.php` exits 0.
    - `./vendor/bin/pint --test Modules/Core/Internal/Console/Probes/ Modules/Core/Internal/Console/DoctorCommand.php` exits 0.
    - `find Modules/Core -name BackupsDirectory.php | wc -l` returns 0 (no new value-object class was introduced — contextual string binding is the contract).
  </acceptance_criteria>
  <done>Three new probes ship under a shared contract; diederik:doctor prints + exits-by-severity correctly; backup-overdue path writes the system_alerts row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RestoreDatabaseCommand (db:restore) — triple safety rail + pre-restore snapshot + DB::purge + post-swap integrity_check</name>
  <files>Modules/Core/Internal/Console/RestoreDatabaseCommand.php, Modules/Core/Providers/CoreServiceProvider.php, Modules/Core/tests/Feature/RestoreDatabaseCommandTest.php, Modules/Core/tests/Feature/RestoreSuccessPathTest.php</files>
  <read_first>
    - Modules/Core/Internal/Console/InstallCommand.php (analog command shape)
    - Modules/Core/Internal/Console/BackupDatabaseCommand.php (just-built in 11-02 — reusable VACUUM INTO + fresh-PDO integrity_check patterns)
    - Modules/Core/Internal/Providers/SqliteOptimizationsProvider.php (proves the ConnectionEstablished listener auto-reapplies WAL + synchronous on the swapped-in file — RESEARCH §Pitfall 6)
    - tests/Helpers/RealSqliteFixture.php (Wave 0 — used to build both the source backup AND the live DB in tests)
    - .planning/phases/11-operational-hardening/11-CONTEXT.md `<decisions>` D-1112, D-1113, D-1114, D-1119
    - .planning/phases/11-operational-hardening/11-RESEARCH.md Pattern 6 + Pitfalls 2, 6 (maintenance mode + DB::purge + post-swap PRAGMA re-application)
    - .planning/phases/11-operational-hardening/11-PATTERNS.md "RestoreDatabaseCommand" section
  </read_first>
  <behavior>
    - `final class RestoreDatabaseCommand extends Command` under `Modules\Core\Internal\Console`. Signature: `db:restore {path : Path to the .sqlite backup file to restore} {--confirm : Skip the interactive y/N prompt} {--force-maintenance : Bring the app down/up automatically around the swap}`. Description: `Restore the SQLite database from a backup file with triple safety rails.`
    - Constructor DI: `Repository $config`, `DatabaseManager $db`, `Filesystem $files`, `Kernel $artisan` (= `Illuminate\Contracts\Console\Kernel`), `Clock $clock`.
    - `handle(): int` walks these guard clauses, returning `self::FAILURE` (= 1) on each refusal:
      1. Resolve `$sourcePath = $this->argument('path')`. If `! $this->files->exists($sourcePath)` → `$this->error('Source file not found: '.$sourcePath); return self::FAILURE;`.
      2. Determine maintenance state via `$this->files->exists(base_path('storage/framework/down'))`. If false AND `--force-maintenance` is false → `$this->error('App must be in maintenance mode. Run `php artisan down` first or pass --force-maintenance.'); return self::FAILURE;`. If `--force-maintenance` is true AND not currently down → call `$this->artisan->call('down')` and set `$broughtDown = true` so the `finally` block can `up` it.
      3. If `--confirm` is NOT set AND we have a TTY (`stream_isatty(STDIN)`): print the y/N prompt via `$this->confirm("Restore {$sourcePath} over current DB? A pre-restore snapshot will be saved. [y/N]", false)`; if the user answers no → return `self::FAILURE`. If `--confirm` is NOT set AND no TTY → return `self::FAILURE` with "Non-interactive context — pass --confirm."
      4. Pre-swap source integrity_check: open `new \PDO('sqlite:'.$sourcePath, ...)`, run `PRAGMA integrity_check`, expect `['ok']`. If not → `$this->error('Source file failed integrity check. Refusing to restore.'); return self::FAILURE;`.
      5. Write a pre-restore snapshot of the CURRENT live DB: build the destination path `base_path('storage/app/backups/pre-restore-'.$clock->now()->format('Y-m-d-His').'.sqlite')`, run `$this->db->connection()->statement("VACUUM INTO '".str_replace("'", "''", $preRestorePath)."'")`, then `$this->files->chmod($preRestorePath, 0o600)`. Log `$this->info('Pre-restore snapshot saved: '.$preRestorePath)`.
      6. Close all DB connections: `$this->db->purge()` (no args → default connection). Per RESEARCH §Pitfall 2 + §Pattern 6, this releases the file handle on the current `database.sqlite`.
      7. Copy source over live: `$livePath = $this->config->get('database.connections.sqlite.database')`; `$this->files->copy($sourcePath, $livePath)`. If copy fails → write a critical `system_alerts` row (kind=`backup_corrupt`, severity=`critical`, message="Restore copy failed mid-swap. Pre-restore snapshot at {$preRestorePath}."), keep maintenance mode ON, return `self::FAILURE`.
      8. Post-swap integrity_check via the framework's connection (NOT a fresh PDO — RESEARCH §Pitfall 6 explicitly recommends the framework connection here so the SqliteOptimizationsProvider's ConnectionEstablished listener auto-reapplies WAL + synchronous against the swapped-in file). Call `$result = $this->db->connection()->select('PRAGMA integrity_check')`; check first row's `integrity_check` property equals `'ok'`. If not → write critical `system_alerts` row (kind=`backup_corrupt`, severity=`critical`), keep maintenance mode ON (do NOT bring it `up` even if `$broughtDown === true`), return `self::FAILURE`.
      9. (HAPPY PATH) If `$broughtDown === true`, call `$this->artisan->call('up')`. Log `$this->info('Restore complete from: '.$sourcePath)`. Return `self::SUCCESS`.
    - Wrap steps 3–9 in `try { ... } finally { if (\$broughtDown && \$leaveDown === false) { \$this->artisan->call('up'); } }` so a throw between down and up still releases maintenance mode UNLESS step 7/8 explicitly set `$leaveDown = true` (post-swap-failure case).
    - RestoreDatabaseCommandTest covers the four refusal paths: (a) source file missing → exit 1 + "Source file not found"; (b) source corrupt → exit 1 + "failed integrity check" + live DB unchanged; (c) not in maintenance mode + no `--force-maintenance` → exit 1 + "must be in maintenance mode" + live DB unchanged; (d) non-TTY + no `--confirm` → exit 1.
    - RestoreSuccessPathTest covers the happy path: build a fresh source DB via RealSqliteFixture::create('source'), seed a `transactions` row in the source; bring the app down via `$this->artisan->call('down')`; run `db:restore --confirm <source-path>`; assert (a) `pre-restore-*.sqlite` file exists; (b) `chmod 600` on the pre-restore file; (c) post-swap DB has the seeded transactions row visible via fresh `SystemAlert::query()` (or raw SELECT against the new DB); (d) exit code 0; (e) app is brought up (storage/framework/down absent).
    - Register the command in `CoreServiceProvider::boot()`'s `commands([...])` array.
  </behavior>
  <action>Write `RestoreDatabaseCommand` at `Modules/Core/Internal/Console/RestoreDatabaseCommand.php`. Constructor DI exactly as listed; NO facade usage. The `try/finally` block is the load-bearing safety property — make sure the `finally` block correctly handles the four paths (succeed-after-down → up; fail-pre-swap → up; fail-post-swap → leave down; throw-from-anywhere → up). The "leave down on post-swap failure" branch is RESEARCH §Pitfall 6's exact recommendation — comment in the code body documents why. After writing the command class, add `RestoreDatabaseCommand::class` to the `commands([...])` array in `Modules/Core/Providers/CoreServiceProvider.php` (read the file before editing — append exactly one entry). Both feature tests use `uses(RefreshDatabase::class);` for SystemAlert isolation. The tests run `$this->artisan->call('down')` / `'up'` directly via the injected Kernel inside test setup so the maintenance state is deterministic. Use `RealSqliteFixture` to build the source backup AND a separate fixture to act as the "current live" DB (override `$config->set('database.connections.sqlite.database', $livePath)` in beforeEach). The post-swap integrity_check uses the framework's connection per RESEARCH §Pitfall 6 — comment this choice in the test as well so the deliberate divergence from the pre-swap fresh-PDO pattern is visible. Keep the `confirm()` interactive prompt behind a `! $this->option('confirm') && stream_isatty(STDIN)` guard so non-TTY contexts (CI, scripts) get the explicit failure rather than hanging on stdin.</action>
  <verify>
    <automated>./vendor/bin/pest --filter='RestoreDatabaseCommandTest|RestoreSuccessPathTest' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - File `Modules/Core/Internal/Console/RestoreDatabaseCommand.php` exists; `grep -c "use Illuminate\\\\Support\\\\Facades\\\\" Modules/Core/Internal/Console/RestoreDatabaseCommand.php` returns 0 (no facade imports); grep `purge\(` returns the DB connection purge call; grep `VACUUM INTO` returns the pre-restore snapshot call.
    - Manual smoke: `php artisan db:restore --confirm --force-maintenance /tmp/example-backup.sqlite` round-trips a known-good source; `ls -la storage/app/backups/pre-restore-*.sqlite` shows the auto-snapshot at mode 0600.
    - `./vendor/bin/pest --filter='RestoreDatabaseCommandTest|RestoreSuccessPathTest' --parallel` exits 0.
    - `./vendor/bin/phpstan analyse --memory-limit=2G Modules/Core/Internal/Console/RestoreDatabaseCommand.php Modules/Core/Providers/CoreServiceProvider.php` exits 0.
    - `./vendor/bin/pint --test Modules/Core/Internal/Console/RestoreDatabaseCommand.php Modules/Core/Providers/CoreServiceProvider.php` exits 0.
  </acceptance_criteria>
  <done>db:restore ships with triple safety rail; refusal + happy paths both Pest-tested; pre-restore snapshot proven on disk.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HealthCheckServiceProvider (boot-time PRAGMA verification, non-halting) + BootProbeState singleton</name>
  <files>Modules/Core/Internal/Console/Probes/BootProbeState.php, Modules/Core/Internal/Providers/HealthCheckServiceProvider.php, Modules/Core/Providers/CoreServiceProvider.php, Modules/Core/tests/Feature/AppBootHealthCheckTest.php</files>
  <read_first>
    - Modules/Core/Internal/Providers/SqliteOptimizationsProvider.php (analog ServiceProvider — read FULL FILE; same Dispatcher DI + ConnectionEstablished listener + sqlite guard. HealthCheckServiceProvider mirrors this exactly except the listener READS the PRAGMA and conditionally WRITES a SystemAlert row instead of writing the PRAGMA.)
    - Modules/Core/Providers/CoreServiceProvider.php (registration extension point — add `$this->app->register(HealthCheckServiceProvider::class);` next to the existing `SqliteOptimizationsProvider::class` register line, AND bind `BootProbeState::class` as a singleton)
    - Modules/Core/Models/SystemAlert.php (used for the alert write)
    - .planning/phases/11-operational-hardening/11-CONTEXT.md `<decisions>` D-1118 (warns, does NOT halt boot)
    - .planning/phases/11-operational-hardening/11-RESEARCH.md Pattern 7 caveat + Pitfall 8 (gate the write so booting the app 100x doesn't insert 100 rows)
    - .planning/phases/11-operational-hardening/11-PATTERNS.md "HealthCheckServiceProvider" section
  </read_first>
  <behavior>
    - `final class BootProbeState` under `Modules\Core\Internal\Console\Probes`. Single mutable public property: `public bool $booted = false;`. No constructor, no methods. Bound as a singleton in `CoreServiceProvider::register()` via `$this->app->singleton(BootProbeState::class);` so it survives across multiple ConnectionEstablished events within the same PHP process. Tests reset it via `$this->app->instance(BootProbeState::class, new BootProbeState());` in `beforeEach`. **No `private static` field on the provider class** — the singleton-via-container pattern replaces it entirely so the W7 reflection risk under Larastan level 10 is eliminated.
    - `final class HealthCheckServiceProvider extends ServiceProvider` under `Modules\Core\Internal\Providers`.
    - `boot(Dispatcher $events, BootProbeState $state): void` registers a `ConnectionEstablished` listener. The provider's `boot()` takes the `BootProbeState` via constructor-DI-equivalent (Laravel resolves provider method args from the container), then closes over `$state` in the listener closure. The listener body:
      1. Guards on `$event->connection->getDriverName() === 'sqlite'` (early return otherwise).
      2. Guards on `$state->booted` — if true, return immediately. This is the in-process de-duplication gate.
      3. Reads `PRAGMA journal_mode` via `$event->connection->select('PRAGMA journal_mode')`. If the first row's `journal_mode` is not `'wal'` (case-insensitive), call `SystemAlert::create([user_id=>null, kind=>'wal_mode_missing', severity=>'warning', message=>"SQLite is not in WAL mode (currently {$current}).", metadata=>['current_mode' => $current]])` AND log via `$this->app->make(LoggerInterface::class)->warning(...)`. Wrap the alert-write in try/catch logging-only on failure (booting must not halt on a write failure).
      4. Reads `PRAGMA synchronous`. If not equal to integer `1` (NORMAL), call `SystemAlert::create([user_id=>null, kind=>'synchronous_misconfigured', severity=>'warning', message=>"SQLite synchronous level is {$current} (expected NORMAL/1).", metadata=>['current_level' => $current]])` + log.
      5. Sets `$state->booted = true;` and returns.
    - De-dupe BETWEEN process invocations: also check `SystemAlert::query()->where('kind', 'wal_mode_missing')->whereNull('acknowledged_at')->where('created_at', '>=', $clock->now()->subHour())->exists()` BEFORE writing — if a recent unacknowledged row already exists, skip the write. This is the "100 boots → 100 alerts" guard from RESEARCH §Pitfall 8 across process restarts. Inject `Clock` via the listener's container resolve (call `$this->app->make(Clock::class)` lazily inside the listener so the provider's `boot()` signature stays minimal).
    - Register: in `CoreServiceProvider::register()`, append `$this->app->singleton(BootProbeState::class);` AND `$this->app->register(HealthCheckServiceProvider::class);` immediately after the existing `SqliteOptimizationsProvider::class` register call.
    - AppBootHealthCheckTest covers: (a) clean state (WAL active + synchronous=1) → no `system_alerts` row written after firing the ConnectionEstablished event; (b) forced `PRAGMA journal_mode = DELETE` against a RealSqliteFixture-backed test connection → exactly ONE `system_alerts(kind=wal_mode_missing)` row exists after firing the listener; (c) firing the listener twice in the same process → still exactly ONE row (BootProbeState singleton guard); (d) firing the listener after acknowledging the previous alert + ≥1h gap simulated via clock manipulation → a NEW row may be written. The simulation pattern: rebind `BootProbeState` via `$this->app->instance(BootProbeState::class, new BootProbeState());` in `beforeEach` to reset between sub-tests; for scenario (c), do NOT rebind — call `dispatch()` twice in a row against the same `$state` instance to verify the guard.
  </behavior>
  <action>Create `BootProbeState` at `Modules/Core/Internal/Console/Probes/BootProbeState.php` as a `final class` with a single public mutable `bool $booted = false;` property. No methods. No constructor. This is the explicit replacement for the W7 `private static bool $booted` reflection pattern — a DI'd singleton avoids reflection entirely.

Write `HealthCheckServiceProvider` at `Modules/Core/Internal/Providers/HealthCheckServiceProvider.php`. Mirror `SqliteOptimizationsProvider` line-for-line except (a) the `boot()` signature accepts `Dispatcher $events, BootProbeState $state` (Laravel resolves method args from the container), and (b) the listener body reads + conditionally writes instead of writing PRAGMAs. The order inside the listener is: check `$state->booted` → return if true; check the `SystemAlert` recency query → set `$state->booted = true` + return if true; do the PRAGMA reads + writes; set `$state->booted = true`. Register the provider in `CoreServiceProvider::register()` next to the existing SqliteOptimizationsProvider register call, and bind `BootProbeState` as a singleton on the same line block.

Test file at `Modules/Core/tests/Feature/AppBootHealthCheckTest.php` uses `uses(RefreshDatabase::class);` + manipulates the test connection's PRAGMAs in `beforeEach` to set up each scenario; fires the `ConnectionEstablished` event manually via `$this->app->make(Dispatcher::class)->dispatch(new ConnectionEstablished($connection));`. For the per-process re-fire test, reset the singleton between sub-tests via `$this->app->instance(BootProbeState::class, new BootProbeState());` — NOT via reflection. **No `ReflectionClass::setStaticPropertyValue` anywhere in the test file** — Larastan level 10 strict will flag reflection on static state, and replacing the pattern with the DI singleton is the W7 fix.</action>
  <verify>
    <automated>./vendor/bin/pest --filter='AppBootHealthCheckTest' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - File `Modules/Core/Internal/Console/Probes/BootProbeState.php` exists; grep `public bool $booted` returns the property declaration.
    - File `Modules/Core/Internal/Providers/HealthCheckServiceProvider.php` exists; grep `ConnectionEstablished` returns the listen call; grep `wal_mode_missing` returns the SystemAlert::create call; `grep -c "private static" Modules/Core/Internal/Providers/HealthCheckServiceProvider.php` returns 0 (no static state — the DI singleton replaces it).
    - `grep -c "ReflectionClass" Modules/Core/tests/Feature/AppBootHealthCheckTest.php` returns 0 (no reflection on probe state).
    - File `Modules/Core/Providers/CoreServiceProvider.php` contains `$this->app->singleton(BootProbeState::class)` AND `$this->app->register(HealthCheckServiceProvider::class)` lines in `register()`.
    - `./vendor/bin/pest --filter='AppBootHealthCheckTest' --parallel` exits 0; all four scenarios pass.
    - `php artisan tinker --execute='app(\Illuminate\Contracts\Events\Dispatcher::class)->dispatch(new \Illuminate\Database\Events\ConnectionEstablished(\DB::connection())); echo "boot ok";'` exits 0 and prints "boot ok" — confirms the listener does NOT halt boot (manual smoke; not Pest).
    - `./vendor/bin/phpstan analyse --memory-limit=2G Modules/Core/Internal/Providers/HealthCheckServiceProvider.php Modules/Core/Internal/Console/Probes/BootProbeState.php Modules/Core/Providers/CoreServiceProvider.php` exits 0.
    - `./vendor/bin/pint --test Modules/Core/Internal/Providers/HealthCheckServiceProvider.php Modules/Core/Internal/Console/Probes/BootProbeState.php Modules/Core/Providers/CoreServiceProvider.php Modules/Core/tests/Feature/AppBootHealthCheckTest.php` exits 0.
  </acceptance_criteria>
  <done>Boot-time PRAGMA verification ships, writes `system_alerts` on drift, does NOT halt boot, de-dupes per process (via BootProbeState DI singleton — no static state, no reflection) + per hour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db:restore operator → live DB | Restore command swaps the configured `database.sqlite` file — uniquely catastrophic if mis-invoked |
| boot-time listener → app availability | A throw in the boot listener would lock the user out of the app; the listener MUST be try/catch-protected |
| Probe contract → user-visible severity | Probe results drive `diederik:doctor` exit codes + alert writes — severity assignment must match D-1110 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | db:restore over-writes live DB without confirmation | mitigate | Triple safety rail: maintenance mode + `--confirm` + pre-swap integrity_check + auto pre-restore snapshot before the swap. RestoreDatabaseCommandTest covers all four refusal paths. |
| T-11-03-02 | Tampering | db:restore swaps in corrupt source file | mitigate | Step 4 pre-swap `PRAGMA integrity_check` via fresh PDO refuses the restore; step 8 post-swap re-check catches mid-copy corruption and KEEPS the app down (`$leaveDown = true`) so the operator notices. |
| T-11-03-03 | Spoofing | Operator confused about which file is which (live vs pre-restore vs source) | mitigate | Distinct filename patterns: `diederik-*.sqlite` (scheduled), `pre-restore-*.sqlite` (auto-snapshot before restore), `*.suspect` (corrupt). README rewrite in plan 11-05 documents each prefix. |
| T-11-03-04 | Denial of Service | App-boot health check throws → user locked out | mitigate | Listener body wrapped in try/catch; alert-write failures log + continue; `BootProbeState` singleton prevents repeated work per process; AppBootHealthCheckTest scenario (a) asserts clean boots produce no alerts AND no exceptions. |
| T-11-03-05 | Information Disclosure | Probe metadata leaks system info | accept | Probe metadata fields are `current_mode` (PRAGMA strings) and `hours_old` (integer). No paths, secrets, or user data. Threat acceptable. |
| T-11-03-06 | Tampering | Restore happens while Horizon worker writes to old DB | mitigate | `php artisan down` causes Horizon to pause processing (RESEARCH §A2 — assumption verified by plan 11-04's `HorizonForceFlagTest`). `DB::purge()` releases the file handle before `copy()`. |
| T-11-03-07 | Tampering | Boot listener writes 100 alerts on rapid restart loop | mitigate | Two-layer de-dupe: `BootProbeState` singleton (intra-process) + `SystemAlert::query()->where('kind', $kind)->whereNull('acknowledged_at')->where('created_at', '>=', $clock->now()->subHour())->exists()` recency check (cross-process). The DI singleton replaces the static-flag pattern so Larastan level 10 + the no-reflection rule both stay clean. |
| T-11-03-SC | Tampering | composer install | accept | This plan installs NO new packages. |
</threat_model>

<verification>
- `./vendor/bin/pest --filter='DoctorProbesTest|DoctorCommandTest|RestoreDatabaseCommandTest|RestoreSuccessPathTest|AppBootHealthCheckTest' --parallel` exits 0.
- `php artisan diederik:doctor` runs end-to-end on the local DB, prints WAL/synchronous/backup-freshness probe lines, exits 0 on a clean state.
- `php artisan db:restore --confirm --force-maintenance /tmp/example-backup.sqlite` round-trips on a known-good source (manual smoke).
- `./vendor/bin/phpstan analyse --memory-limit=2G` exits 0.
- `./vendor/bin/pint --test` exits 0.
</verification>

<success_criteria>
- Three Probe implementations + shared contract + ProbeResult value object all ship and unit-tested.
- DoctorCommand extended without breaking existing inline tool-version checks (inline-check refactor explicitly DEFERRED per Claude's Discretion).
- BackupFreshnessProbe writes `system_alerts(kind=backup_overdue)` correctly. Uses contextual `string $backupsPath` binding on the `'core.backups_directory'` container key (no new `BackupsDirectory` value object).
- db:restore ships with triple safety rail; refusal + happy paths both Pest-tested.
- HealthCheckServiceProvider boots without halting, writes alerts on PRAGMA drift, de-dupes correctly per process (via `BootProbeState` DI singleton) AND per hour. No reflection anywhere in the test or production code.
- All Pest + Larastan + Pint gates green.
</success_criteria>

<output>
Create `.planning/phases/11-operational-hardening/11-03-SUMMARY.md` when done.
</output>
