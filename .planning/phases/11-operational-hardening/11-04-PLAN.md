---
phase: 11-operational-hardening
plan: 04
type: execute
wave: 3
depends_on: [11-01, 11-02, 11-03]
files_modified:
  - Modules/Core/Internal/Console/FailedJobsCommand.php
  - Modules/Core/Internal/Console/Support/DurationParser.php
  - Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php
  - Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php
  - Modules/Core/Providers/CoreServiceProvider.php
  - resources/views/layouts/app.blade.php
  - Modules/Core/tests/Unit/DurationParserTest.php
  - Modules/Core/tests/Feature/FailedJobsCommandTest.php
  - Modules/Core/tests/Feature/SystemAlertsBannerTest.php
  - tests/Contracts/BoundaryArchTest.php
  - tests/Contracts/HorizonForceFlagTest.php
autonomous: true
requirements: [FND-05]

must_haves:
  truths:
    - "User can run `php artisan diederik:failed-jobs prune --older-than=30d --dry-run` and see which failed_jobs rows would be deleted without writing."
    - "User can run `php artisan diederik:failed-jobs prune --older-than=7d` and the matching rows are deleted from the `failed_jobs` table."
    - "SystemAlertsBanner Livewire SFC renders one row per active alert at the top of every authenticated page; clicking 'Mark as resolved' archives the row via AcknowledgeSystemAlert and the banner re-renders without that row."
    - "BoundaryArchTest::noFacadeCallsFromCoreConsoleCommands passes against Modules/Core/Internal/Console/."
    - "HorizonForceFlagTest passes — config/horizon.php contains no `'force' => true` entry on any supervisor (locks RESEARCH §A2 invariant)."
  decisions: [D-1109, D-1117]
  artifacts:
    - path: "Modules/Core/Internal/Console/FailedJobsCommand.php"
      provides: "diederik:failed-jobs prune --older-than=<d|h|w> [--dry-run]"
      contains: "failed_jobs"
    - path: "Modules/Core/Internal/Console/Support/DurationParser.php"
      provides: "Pure regex-based '<n>d|<n>h|<n>w' to CarbonImmutable parser"
      contains: "DurationParser"
    - path: "Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php"
      provides: "Livewire 4 Component with method-parameter DI on render() + acknowledge()"
      contains: "SystemAlertQuery"
    - path: "Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php"
      provides: "Severity-tinted banner row Blade view"
      contains: "wire:click=\"acknowledge"
    - path: "tests/Contracts/HorizonForceFlagTest.php"
      provides: "A2 invariant — no Horizon supervisor uses force: true"
      contains: "force"
  key_links:
    - from: "resources/views/layouts/app.blade.php"
      to: "@livewire('core.system-alerts-banner')"
      via: "one new line inside the existing @auth block"
      pattern: "core.system-alerts-banner"
    - from: "Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php"
      to: "AcknowledgeSystemAlert action"
      via: "method-parameter DI on acknowledge(int $alertId, AcknowledgeSystemAlert $action, CurrentUser $u)"
      pattern: "AcknowledgeSystemAlert"
    - from: "tests/Contracts/BoundaryArchTest.php"
      to: "noFacadeCallsFromCoreConsoleCommands"
      via: "appended it(...) block"
      pattern: "noFacadeCallsFromCoreConsoleCommands"
---

<objective>
Wave 3 vertical slice: ship the failed-jobs maintenance CLI, the user-visible SystemAlertsBanner Livewire SFC (the ONLY frontend deliverable of Phase 11), and the two arch invariants that lock the operational posture (`noFacadeCallsFromCoreConsoleCommands` for the new commands' DI-only contract, `HorizonForceFlagTest` for the maintenance-mode-pauses-Horizon assumption A2). After this plan, the user can see and dismiss alerts in the browser, the failed_jobs table is maintainable from CLI, and the test substrate forbids regressions on both DI-only and Horizon-force-flag invariants.

Purpose: Wave 1 + Wave 2 wrote alerts to the DB but had no rendering surface. This plan delivers the visualisation. The failed-jobs CLI is operationally adjacent (a daily-use reliability touch). The two arch tests lock the safety contracts that the previous waves silently relied on.
Output: Working banner at `https://diederik.test`, working `diederik:failed-jobs prune` CLI, BoundaryArchTest + HorizonForceFlagTest passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-operational-hardening/11-CONTEXT.md
@.planning/phases/11-operational-hardening/11-RESEARCH.md
@.planning/phases/11-operational-hardening/11-PATTERNS.md
@.planning/phases/11-operational-hardening/11-UI-SPEC.md
@.planning/phases/11-operational-hardening/11-VALIDATION.md
@.planning/phases/11-operational-hardening/11-01-SUMMARY.md
@.planning/phases/11-operational-hardening/11-02-SUMMARY.md
@.planning/phases/11-operational-hardening/11-03-SUMMARY.md

<interfaces>
<!-- Key surfaces this plan composes against. -->

From Modules/Core/Public/Services/SystemAlertQuery.php (Wave 0 — plan 11-01):
- `active(?User $user): Collection<int, SystemAlert>` — already orders critical->warning->info, then chronological ASC

From Modules/Core/Public/Actions/AcknowledgeSystemAlert.php (Wave 0 — plan 11-01):
- `__invoke(int $alertId, User $user): SystemAlert` — 404 on cross-user, idempotent on already-acknowledged

From Modules/DriftAlerts/Internal/Http/Livewire/DashboardDriftBadge.php (analog Livewire SFC):
- `final class … extends Component` with method-parameter DI ONLY on `render()` + action methods
- NEVER constructor DI on Livewire Component subclasses (phpstan-strict-rules)

From Modules/DriftAlerts/Resources/views/livewire/partials/drift-alert-row.blade.php (analog Blade SFC):
- Row shell `rounded-lg border p-4` + `flex items-start justify-between gap-4`
- Action button with `wire:click="acknowledge({{ $id }})"` + severity-tinted class stack

From Modules/Ledger/Internal/Console/RederiveFingerprintsCommand.php (analog command shape):
- `final class … extends Command` with `protected $signature` listing multiple `--flags`
- `--dry-run` flag pattern + private `report()` helper printing summary

From tests/Contracts/BoundaryArchTest.php lines 834–879 (`noScenarioMutationsJoinedToTransactionQueries`):
- The verbatim `it(...) { … RecursiveIteratorIterator + comment-strip regex + $hits[] + expect()->toBe([], …) }` shape

From config/horizon.php (project Horizon config — read at planning time):
- The file lives at the project root. Supervisor configuration arrays are indexed by environment (`local`, `production`) and contain keys like `connection`, `queue`, `processes`. The A2 invariant requires `'force'` key NEVER mapped to literal `true`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DurationParser value object + FailedJobsCommand (diederik:failed-jobs prune)</name>
  <files>Modules/Core/Internal/Console/Support/DurationParser.php, Modules/Core/Internal/Console/FailedJobsCommand.php, Modules/Core/Providers/CoreServiceProvider.php, Modules/Core/tests/Unit/DurationParserTest.php, Modules/Core/tests/Feature/FailedJobsCommandTest.php</files>
  <read_first>
    - Modules/Ledger/Internal/Console/RederiveFingerprintsCommand.php (analog command — `{action=prune}` subcommand pattern via the `action` argument, `--dry-run` flag, private `report()` helper)
    - Modules/Ledger/tests/Feature/RederiveFingerprintsCommandTest.php (analog feature test — `$this->artisan(...)` + `expectsOutputToContain(...)`)
    - Modules/Forecasting/Internal/Support/AmountStringParser.php (analog pure-string value object — final class, single public method, zero IO)
    - .planning/phases/11-operational-hardening/11-CONTEXT.md `<decisions>` D-1117, D-1119
    - .planning/phases/11-operational-hardening/11-RESEARCH.md Pattern 8 + Open Question #2 (decision: omit `m` for ambiguity; accept `d` / `h` / `w` only)
    - .planning/phases/11-operational-hardening/11-PATTERNS.md "FailedJobsCommand" + "DurationParser" sections
  </read_first>
  <behavior>
    - `final class DurationParser` under `Modules\Core\Internal\Console\Support`. Instance method `subFromNow(string $input, CarbonImmutable $now): CarbonImmutable` returning `$now - duration`.
    - Accepted tokens (regex): `/^(\d+)([dhw])$/i` — digits + ONE of `d` (days), `h` (hours), `w` (weeks). `m` is EXPLICITLY REJECTED per RESEARCH Open Question #2 (ambiguous minutes-vs-months).
    - Invalid input throws `InvalidArgumentException` with message `"Duration must match /^\\d+[dhw]\$/ (e.g. '30d', '7d', '12h', '2w'). Got: '{$input}'."`.
    - Pest dataset covers: valid `30d` → `now->subDays(30)`; `7d` → `now->subDays(7)`; `12h` → `now->subHours(12)`; `2w` → `now->subWeeks(2)`; INVALID `30m` → throws; `30s` → throws; `abc` → throws; empty string → throws; `1.5d` → throws (regex rejects decimal); negative `-5d` → throws.
    - `final class FailedJobsCommand extends Command` under `Modules\Core\Internal\Console`. Signature: `diederik:failed-jobs {action=prune : Action to run (currently only 'prune' is wired)} {--older-than=30d : Duration cutoff for the prune action} {--dry-run : Print rows that WOULD be deleted without writing}`. Description: `Maintenance operations on the Laravel-managed failed_jobs table.`
    - Constructor DI: `DatabaseManager $db`, `Clock $clock`, `DurationParser $duration`.
    - `handle(): int`:
      1. Read `$action = $this->argument('action')`. If not `'prune'` → `$this->error("Unknown action: {$action}. Supported: prune."); return self::FAILURE;`.
      2. Parse `--older-than`: try `$cutoff = $this->duration->subFromNow($this->option('older-than'), $this->clock->now());`. Catch InvalidArgumentException → `$this->error($e->getMessage()); return self::FAILURE;`.
      3. Query candidate rows via `$rows = $this->db->connection()->table('failed_jobs')->where('failed_at', '<', $cutoff->toDateTimeString())->get(['id', 'queue', 'failed_at', 'exception']);` — select only the minimal columns needed for the summary.
      4. If `--dry-run` is set: print a summary line per row (`#{$id}\tqueue={$queue}\tfailed_at={$failed_at}`) capped at the first 50 rows + a footer `"Would delete {$count} rows (--dry-run; nothing written)."`. Return `self::SUCCESS`.
      5. Otherwise: `$deleted = $this->db->connection()->table('failed_jobs')->where('failed_at', '<', $cutoff->toDateTimeString())->delete();` then print `"Removed {$deleted} rows; {$remaining} remaining."` (compute `$remaining` via a follow-up `count()`).
    - Register the command in `CoreServiceProvider::boot()`'s `commands([...])` array.
    - DurationParserTest covers each case in the Pest dataset above.
    - FailedJobsCommandTest covers: (a) seed 5 rows with `failed_at` spread across 7, 14, 31, 45, 60 days ago; run `--older-than=30d --dry-run` → 3 rows would-delete; assert NO rows actually deleted; (b) run `--older-than=30d` (no dry-run) → 3 rows deleted, 2 remain; (c) invalid `--older-than=30m` → exit 1 + error mentioning the regex; (d) unknown action `diederik:failed-jobs whatever` → exit 1 + "Unknown action: whatever".
  </behavior>
  <action>Write `DurationParser` at `Modules/Core/Internal/Console/Support/DurationParser.php` as an instance-method-only `final class`. The constructor takes no arguments (pure logic). Use `preg_match('/^(\d+)([dhw])$/i', $input, $m)` — explicitly omit `m` per Open Question #2; the test asserts this rejection. Throw `InvalidArgumentException` with the exact message string in `<behavior>`. Write `FailedJobsCommand` at `Modules/Core/Internal/Console/FailedJobsCommand.php`. Constructor DI exactly as listed; NO facade usage. The action argument default is `'prune'` so `php artisan diederik:failed-jobs` without arguments runs `prune`. Use the raw `DatabaseManager::connection()->table('failed_jobs')` query builder (the Laravel-managed `failed_jobs` table has no Eloquent model in this codebase — verify via grep before writing). Print summary via `$this->info(...)` for success + `$this->error(...)` for failure. The dry-run cap (first 50 rows printed) prevents flooding the console on large failure backlogs. Register the command in `CoreServiceProvider::boot()`'s `commands([...])` array (read the file before editing — append exactly one entry). Both tests live under `Modules/Core/tests/{Unit,Feature}/`. FailedJobsCommandTest uses `uses(RefreshDatabase::class);` so the `failed_jobs` table is fresh per test. Seed rows via raw `DatabaseManager::table('failed_jobs')->insert([...])` with the standard Laravel failed_jobs schema (`id`, `uuid`, `connection`, `queue`, `payload`, `exception`, `failed_at`).</action>
  <verify>
    <automated>./vendor/bin/pest --filter='DurationParserTest|FailedJobsCommandTest' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - File `Modules/Core/Internal/Console/Support/DurationParser.php` exists; grep `subFromNow` returns the method; grep `'m'` is absent from the regex character class (the `m` token is explicitly rejected).
    - File `Modules/Core/Internal/Console/FailedJobsCommand.php` exists; `grep -c "use Illuminate\\\\Support\\\\Facades\\\\" Modules/Core/Internal/Console/FailedJobsCommand.php` returns 0 (no facade imports).
    - `php artisan diederik:failed-jobs prune --older-than=30d --dry-run` runs end-to-end without writing (manual smoke).
    - `./vendor/bin/pest --filter='DurationParserTest|FailedJobsCommandTest' --parallel` exits 0.
    - `./vendor/bin/phpstan analyse --memory-limit=2G Modules/Core/Internal/Console/Support/DurationParser.php Modules/Core/Internal/Console/FailedJobsCommand.php Modules/Core/Providers/CoreServiceProvider.php` exits 0.
    - `./vendor/bin/pint --test Modules/Core/Internal/Console/Support/DurationParser.php Modules/Core/Internal/Console/FailedJobsCommand.php Modules/Core/Providers/CoreServiceProvider.php` exits 0.
  </acceptance_criteria>
  <done>diederik:failed-jobs prune ships with dry-run + duration parsing; duration parser unit-tested across valid + invalid inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SystemAlertsBanner Livewire SFC + Blade view + app.blade.php slot</name>
  <files>Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php, Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php, Modules/Core/Providers/CoreServiceProvider.php, resources/views/layouts/app.blade.php, Modules/Core/tests/Feature/SystemAlertsBannerTest.php</files>
  <read_first>
    - Modules/DriftAlerts/Internal/Http/Livewire/DashboardDriftBadge.php (analog Livewire 4 Component — entire file, ~39 lines; verbatim shape to mirror)
    - Modules/DriftAlerts/Resources/views/livewire/partials/drift-alert-row.blade.php (analog Blade row — row shell + action button shape)
    - Modules/DriftAlerts/Resources/views/livewire/dashboard-drift-badge.blade.php (analog Blade wrapper — empty-state guard)
    - Modules/DriftAlerts/tests/Feature/DashboardDriftBadgeTest.php (analog Livewire test — `Livewire::actingAs($user)->test(...)`, `->assertSee(...)`, `->call('acknowledge', $id)`)
    - resources/views/layouts/app.blade.php (CURRENT layout file — read lines 1–30 to understand the @auth block structure)
    - .planning/phases/11-operational-hardening/11-UI-SPEC.md (FULL FILE — verbatim copy, severity color tokens, accessibility roles, banner insertion point diff)
    - .planning/phases/11-operational-hardening/11-PATTERNS.md "SystemAlertsBanner" + "system-alerts-banner.blade.php" + "app.blade.php" sections
    - .planning/phases/11-operational-hardening/11-CONTEXT.md `<decisions>` D-1108, D-1109, D-1110, D-1111
  </read_first>
  <behavior>
    - `final class SystemAlertsBanner extends Component` under `Modules\Core\Internal\Http\Livewire`. NO constructor DI (phpstan-strict-rules forbids on Livewire Components).
    - `render(CurrentUser $currentUser, SystemAlertQuery $query, ViewFactory $views): View` reads `$user = $currentUser->user(); $alerts = $query->active($user);` and returns `$views->make('core::livewire.system-alerts-banner', ['alerts' => $alerts])`.
    - `acknowledge(int $alertId, AcknowledgeSystemAlert $action, CurrentUser $currentUser): void` calls `$action($alertId, $currentUser->user())`. No explicit re-render — Livewire 4 re-runs render() automatically after the action returns.
    - The Blade view at `Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php` uses **three explicit `<div>` branches per severity tier** (critical / warning / info) chosen via Blade `@switch ($alert->severity)` / `@case`. Each branch contains a literal Tailwind class string — NO dynamic interpolation of the severity into Tailwind class names, NO `@php $tier = ...` PurgeCSS workaround, NO HTML-comment safelist. Tailwind's content scanner sees each literal class string in the Blade source and includes it in the build.
    - Wrapper: `<div role="region" aria-label="System alerts" class="px-6 space-y-2">...</div>`. If `$alerts->isEmpty()`, the wrapper renders with NO inner content (banner is INVISIBLE in calm state — UI-SPEC §State Matrix locked).
    - The three explicit severity branches (per UI-SPEC §Color tokens — literal class strings):
      - critical: `<div role="alert" class="rounded-lg border border-rose-500 bg-rose-50 text-rose-900 p-4"> ... <button type="button" class="rounded bg-rose-600 text-white hover:bg-rose-700 focus-visible:ring-rose-600 px-3 py-1.5 text-sm font-medium" wire:click="acknowledge({{ $alert->id }})" wire:loading.attr="disabled" wire:target="acknowledge({{ $alert->id }})" aria-label="Mark system alert #{{ $alert->id }} as resolved">Mark as resolved</button></div>`
      - warning: `<div role="status" class="rounded-lg border border-amber-300 bg-amber-50 text-amber-900 p-4"> ... <button type="button" class="rounded bg-slate-100 text-slate-700 hover:bg-slate-200 focus-visible:ring-slate-900 px-3 py-1.5 text-sm font-medium" wire:click="acknowledge({{ $alert->id }})" wire:loading.attr="disabled" wire:target="acknowledge({{ $alert->id }})" aria-label="Mark system alert #{{ $alert->id }} as resolved">Mark as resolved</button></div>`
      - info: `<div role="status" class="rounded-lg border border-slate-200 bg-slate-50 text-slate-700 p-4"> ... <button type="button" class="rounded bg-slate-100 text-slate-700 hover:bg-slate-200 focus-visible:ring-slate-900 px-3 py-1.5 text-sm font-medium" wire:click="acknowledge({{ $alert->id }})" wire:loading.attr="disabled" wire:target="acknowledge({{ $alert->id }})" aria-label="Mark system alert #{{ $alert->id }} as resolved">Mark as resolved</button></div>`
    - Per-row layout inside each branch (per UI-SPEC §Anatomy): `<div class="flex items-start justify-between gap-4">` with left column `<div class="min-w-0 flex-1">` containing message (`text-sm font-medium`) + metadata line (`text-xs` + `font-variant-numeric: tabular-nums` inline style) and right column `<div class="shrink-0">` containing the per-tier `Mark as resolved` button.
    - Message template selection by kind (UI-SPEC §Severity x Kind Copywriting Contract) — Blade `@switch ($alert->kind) @case ('backup_corrupt') The backup written at {{ $alert->metadata['timestamp'] ?? $alert->created_at->format('d M Y · H:i') }} failed integrity check. Inspect {{ basename($alert->metadata['suspect_path'] ?? '') }}. Resolve before relying on backups. @break ... @endswitch`. Every interpolation uses `{{ }}` (NEVER `{!! !!}` — XSS guard from RESEARCH §Security).
    - Metadata line beneath message: `{{ $alert->created_at->format('d M Y · H:i') }} · #{{ $alert->id }}` with the inline `style="font-variant-numeric: tabular-nums;"`.
    - Register the Livewire component in `CoreServiceProvider::boot()`: `$livewire->component('core.system-alerts-banner', SystemAlertsBanner::class);` immediately after the existing core.* component registrations.
    - Append one new line to `resources/views/layouts/app.blade.php` inside the existing `@auth` block: `@livewire('core.system-alerts-banner')` placed AFTER `@livewire('core.top-nav')` and BEFORE `@livewire('categorization.rule-form-modal')` (verbatim diff from UI-SPEC §Banner Insertion Point).
    - SystemAlertsBannerTest covers: (a) calm state (no active alerts) → renders empty wrapper, asserts `$component->assertDontSee('Mark as resolved')` + asserts the wrapper div is present; (b) one critical alert (kind=`backup_corrupt`) → asserts the message contains "failed integrity check" + the `aria-label="Mark system alert #{id} as resolved"`; (c) clicking 'acknowledge' → calls AcknowledgeSystemAlert + the next render shows the row gone; (d) cross-user: user A's banner shows only user A's alerts + any user_id NULL alerts, never user B's alerts; (e) acknowledging user B's alert via direct call (simulating a tampered Livewire payload) → throws NotFoundHttpException; (f) critical-then-warning ordering: assert the critical row's DOM order is BEFORE the warning row.
  </behavior>
  <action>Write the Livewire Component class at `Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php` as a verbatim mirror of `DashboardDriftBadge.php` — same imports, same method-parameter DI shape, same `View` return type. Write the Blade view at `Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php` with the wrapper + per-alert loop. Inside the loop, use `@switch ($alert->severity)` with three `@case` arms (`'critical'`, `'warning'`, `'info'`) — each arm contains its own complete `<div class="...">...</div>` block with the literal Tailwind class strings listed in `<behavior>`. **NO Tailwind class interpolation (no `text-{tier}-900`, no `bg-{tier}-50`, no `@php $tier = ...`); NO PurgeCSS safelist HTML comments.** Each of the three severity branches is a self-contained explicit Blade snippet. The kind-to-message-template `@switch` (backup_corrupt / backup_overdue / wal_mode_missing / synchronous_misconfigured) lives inside the message slot of each severity branch — extract it to a Blade `@include('core::livewire.partials.system-alert-message', ['alert' => $alert])` if duplication grows unwieldy, but a copy-paste across the three branches is acceptable for v1 (each branch's message lookup is the same `@switch ($alert->kind)` block).

  Register the Livewire component in `CoreServiceProvider::boot()` next to the existing core.* registrations. Append the one-line slot to `resources/views/layouts/app.blade.php` — read the file before editing to confirm the exact `@auth` block lines (UI-SPEC §Banner Insertion Point gives the verbatim before/after diff). SystemAlertsBannerTest uses `Livewire::actingAs($user)->test(SystemAlertsBanner::class)->assertSee/assertSet/call('acknowledge', $id)` per the DashboardDriftBadgeTest pattern. Seed alert rows via raw `DatabaseManager::table('system_alerts')->insert([...])` with the JSON-encoded metadata payload.</action>
  <verify>
    <automated>./vendor/bin/pest --filter='SystemAlertsBannerTest' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - File `Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php` exists; grep `extends Component` returns the class declaration; grep `public function __construct` is ABSENT (no constructor DI — Livewire rule).
    - File `Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php` exists; grep `wire:click="acknowledge(` returns the button binding; `grep -c "{!! " Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php` returns 0 (no raw output — XSS guard).
    - `grep -c "border-rose-500" Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php` returns >= 1 (critical branch literal class present).
    - `grep -c "border-amber-300" Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php` returns >= 1 (warning branch literal class present).
    - `grep -c "border-slate-200" Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php` returns >= 1 (info branch literal class present).
    - `grep -cE "border-(rose|amber|slate)-\{" Modules/Core/Resources/views/livewire/system-alerts-banner.blade.php` returns 0 (no dynamic Tailwind class interpolation).
    - `grep -c "core.system-alerts-banner" resources/views/layouts/app.blade.php` returns at least 1.
    - `./vendor/bin/pest --filter='SystemAlertsBannerTest' --parallel` exits 0; all six scenarios pass.
    - Manual smoke at `https://diederik.test`: insert a `system_alerts(kind=backup_corrupt, severity=critical, message='test', user_id=null)` row via tinker; reload any authenticated page; banner visible at the top with the critical row and the "Mark as resolved" button. Click the button; the row disappears.
    - `./vendor/bin/phpstan analyse --memory-limit=2G Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php Modules/Core/Providers/CoreServiceProvider.php` exits 0.
    - `./vendor/bin/pint --test Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php Modules/Core/Providers/CoreServiceProvider.php resources/views/layouts/app.blade.php` exits 0.
  </acceptance_criteria>
  <done>Banner Livewire SFC ships, mounts on every authenticated page, renders + dismisses + cross-user-safe. Three explicit severity branches with literal Tailwind classes — PurgeCSS-safe by construction, greppable in the source, no safelist needed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BoundaryArchTest::noFacadeCallsFromCoreConsoleCommands + HorizonForceFlagTest</name>
  <files>tests/Contracts/BoundaryArchTest.php, tests/Contracts/HorizonForceFlagTest.php</files>
  <read_first>
    - tests/Contracts/BoundaryArchTest.php lines 834–879 (`noScenarioMutationsJoinedToTransactionQueries` block — verbatim shape to mirror)
    - .planning/phases/11-operational-hardening/11-RESEARCH.md "BoundaryArchTest new invariants" Code Examples lines 831–857 (verbatim noFacadeCallsFromCoreConsoleCommands function body) + §A2 (HorizonForceFlagTest assumption)
    - .planning/phases/11-operational-hardening/11-PATTERNS.md "BoundaryArchTest.php" + "HorizonForceFlagTest.php" sections
    - config/horizon.php (read the FULL file to confirm no `'force' => true` exists today — RESEARCH §A2 says assumption is currently true; the test locks the invariant)
    - All Modules/Core/Internal/Console/*.php files that exist at this point (Wave 1+2+3 outputs) — confirm none import `Illuminate\Support\Facades\*`
  </read_first>
  <behavior>
    - Append a new `it('does not allow Laravel facades inside Modules/Core/Internal/Console/ (noFacadeCallsFromCoreConsoleCommands)', function (): void { ... })` block to `tests/Contracts/BoundaryArchTest.php` immediately after the `systemAlertsTableNotJoinedToTransactions` block (which was added in plan 11-01).
    - The new block walks `Modules/Core/Internal/Console/` recursively, skips non-`.php` files, comment-strips each file via `preg_replace('#/\*.*?\*/|//[^\n]*#s', ...)`, and detects any `Illuminate\\Support\\Facades\\` namespace import (verbatim per RESEARCH §Code Examples). Failure mode: `$hits[]` collection + final `expect($hits)->toBe([], "Modules/Core/Internal/Console/ commands may not use Illuminate\\\\Support\\\\Facades. Offenders:\n  ".implode("\n  ", $hits))`.
    - Create `tests/Contracts/HorizonForceFlagTest.php` as a standalone Pest file containing one `it(...)` block: walks `config/horizon.php`, comment-strips, asserts the file does NOT match the regex `/['"]force['"]\\s*=>\\s*true/`. If `config/horizon.php` does NOT exist (unlikely — verified at planning time), the test asserts `true` (trivially passes) per the RESEARCH §A2 shape.
    - Both arch tests are intentionally STRICT: a single offender (e.g. someone adding `use Illuminate\Support\Facades\DB;` to a future command) causes the test to fail loudly. The failure message includes the offender's file path.
  </behavior>
  <action>Append one new `it(...)` block to `tests/Contracts/BoundaryArchTest.php` containing the verbatim function body sketched in RESEARCH §Code Examples lines 831–857 for `noFacadeCallsFromCoreConsoleCommands`. The block walks the directory tree, skips empty dirs gracefully (`is_dir()` guard returns `expect(true)->toBeTrue()` early), and applies the comment-strip regex before the facade-namespace grep. Then create `tests/Contracts/HorizonForceFlagTest.php` as a standalone Pest file (NOT a class — Pest's `it(...)` function-style; uses the same shape as the other arch tests). Read `config/horizon.php` once + strip comments + assert the regex does NOT match. Both tests live under `tests/Contracts/` per the existing convention. Verify the new BoundaryArchTest invariant by RUNNING IT BEFORE committing — it MUST pass against the current Wave 1+2+3 console files (which were written facade-clean per the plan-11-02/03 acceptance criteria); a failure here means an earlier plan accidentally pulled in a facade.</action>
  <verify>
    <automated>./vendor/bin/pest --filter='BoundaryArchTest::noFacadeCallsFromCoreConsoleCommands|HorizonForceFlagTest' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "noFacadeCallsFromCoreConsoleCommands" tests/Contracts/BoundaryArchTest.php` returns at least 1 after stripping comments.
    - File `tests/Contracts/HorizonForceFlagTest.php` exists; grep `'force'` appears inside the regex string but NOT as a key=>true assignment.
    - `./vendor/bin/pest --filter='BoundaryArchTest::noFacadeCallsFromCoreConsoleCommands' --parallel` exits 0 (no offenders in the current console directory).
    - `./vendor/bin/pest --filter='HorizonForceFlagTest' --parallel` exits 0 (config/horizon.php has no `'force' => true`).
    - `./vendor/bin/phpstan analyse --memory-limit=2G tests/Contracts/BoundaryArchTest.php tests/Contracts/HorizonForceFlagTest.php` exits 0.
    - `./vendor/bin/pint --test tests/Contracts/BoundaryArchTest.php tests/Contracts/HorizonForceFlagTest.php` exits 0.
  </acceptance_criteria>
  <done>Both arch invariants pass; future regressions (facade in console command OR Horizon force flag flip) loudly fail tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| failed_jobs CLI → DB delete | Mass DELETE on a Laravel-managed table; off-by-one on duration parser would delete too much |
| Livewire banner → user input | `wire:click="acknowledge({{ $alert->id }})"` ships the alert id from the client; AcknowledgeSystemAlert MUST enforce per-user scope server-side |
| Blade template → system_alerts.message | Alert messages may contain operator-controlled text; output must be HTML-escaped (XSS guard) |
| Arch tests → future contributors | The two invariants are the locks against silent regressions on DI-only and Horizon-force-flag posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | FailedJobsCommand deletes too much | mitigate | DurationParser is unit-tested across 4 valid + 5 invalid cases; `--dry-run` ALWAYS prints what would be deleted before any delete; cap at first 50 lines prevents flooding. |
| T-11-04-02 | Tampering | Banner XSS via system_alerts.message | mitigate | Blade `{{ }}` default escapes; grep gate in acceptance criteria asserts `{!! !!}` count is 0; UI-SPEC §F3 forbids raw output. Server-rendered with backtick-style commands that are operator-not-user-controlled in v1 — but Blade still escapes them. |
| T-11-04-03 | Information Disclosure | Cross-user alert acknowledgement via tampered Livewire payload | mitigate | AcknowledgeSystemAlert enforces the per-user scope predicate (Wave 0 plan 11-01); SystemAlertsBannerTest scenario (e) exercises this directly. Livewire's signed-payload mechanism is the transport defence; the action-level guard is the data defence. |
| T-11-04-04 | Tampering | Future contributor adds facade to a Core console command | mitigate | BoundaryArchTest::noFacadeCallsFromCoreConsoleCommands fails with the offender's filepath; CI gate prevents merge. |
| T-11-04-05 | Tampering | Future contributor sets Horizon supervisor `force: true` → restore correctness breaks | mitigate | HorizonForceFlagTest fails; CI gate prevents merge. Test failure message documents the consequence (db:restore safety story void). |
| T-11-04-06 | Denial of Service | Banner re-renders 100x per second from `wire:poll` | accept | UI-SPEC §F1 locks "no wire:poll in v1"; only natural re-renders after acknowledge() trigger; multi-tab live refresh deferred. |
| T-11-04-07 | Information Disclosure | Banner shows another user's alert | mitigate | SystemAlertQuery::active(?User) per-user scope; SystemAlertsBannerTest scenario (d) covers it directly. |
| T-11-04-08 | Tampering | Tailwind PurgeCSS strips severity-tier classes at build time | mitigate | Three explicit severity branches with literal Tailwind class strings — Tailwind's content scanner sees `border-rose-500`, `border-amber-300`, `border-slate-200` (plus the matching `bg-*` and `text-*` literals) as direct string occurrences in the Blade source. No dynamic interpolation, no safelist comment dependency. Grep gates in acceptance criteria assert each literal class string is present. |
| T-11-04-SC | Tampering | composer install | accept | This plan installs NO new packages. |
</threat_model>

<verification>
- `./vendor/bin/pest --filter='DurationParserTest|FailedJobsCommandTest|SystemAlertsBannerTest|BoundaryArchTest::noFacadeCallsFromCoreConsoleCommands|HorizonForceFlagTest' --parallel` exits 0.
- Manual smoke at `https://diederik.test`: insert a critical alert → banner renders → click dismiss → row archived. The full Phase 11 vertical UX is now visible.
- `php artisan diederik:failed-jobs prune --older-than=30d --dry-run` runs end-to-end.
- `./vendor/bin/phpstan analyse --memory-limit=2G` exits 0.
- `./vendor/bin/pint --test` exits 0.
</verification>

<success_criteria>
- diederik:failed-jobs CLI ships with prune + dry-run + duration parsing.
- SystemAlertsBanner Livewire SFC ships; renders + dismisses + cross-user-safe; mounted on every authenticated page. Three explicit severity branches with literal Tailwind classes (PurgeCSS-safe).
- BoundaryArchTest::noFacadeCallsFromCoreConsoleCommands locks the DI-only contract for Core console commands.
- HorizonForceFlagTest locks the A2 assumption that maintenance mode pauses Horizon workers.
- All Pest + Larastan + Pint gates green.
- Phase 11 has reached vertical UX completeness — the user can see, dismiss, and depend on alerts.
</success_criteria>

<output>
Create `.planning/phases/11-operational-hardening/11-04-SUMMARY.md` when done.
</output>
