---
phase: 11-operational-hardening
plan: 05
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified:
  - README.md
  - Modules/Core/tests/Feature/ReadmeOperationalDocsTest.php
  - Modules/Core/tests/Feature/Phase11AcceptanceTest.php
autonomous: false
requirements: [FND-05]

must_haves:
  truths:
    - "README `## Backups` section explicitly forbids `cp database.sqlite`, points at `php artisan db:backup` as the supported path, documents the 7-daily + 4-Sunday retention rule, and references the 03:00 schedule entry."
    - "README `## Operator recovery` section preserves the existing 'Stuck Redis unique-lock keys' subsection AND adds three new ones: 'Restoring from a backup', 'Corrupt-backup alert', 'Failed-jobs maintenance'."
    - "ReadmeOperationalDocsTest enforces the required + forbidden substrings via content assertion so future doc churn cannot regress the safety story."
    - "Phase11AcceptanceTest runs the full vertical: `db:backup --force` against a RealSqliteFixture, asserts file + sidecar, force-corrupts, asserts banner renders + system_alerts row + non-zero exit."
    - "Human-verify checkpoint confirms the banner + db:backup + db:restore behave as documented on the real Herd-mounted DB."
  artifacts:
    - path: "README.md"
      provides: "Operator-facing documentation of the backup + restore + alert surfaces"
      contains: "php artisan db:backup"
    - path: "Modules/Core/tests/Feature/ReadmeOperationalDocsTest.php"
      provides: "Content arch test for README sections"
      contains: "## Backups"
    - path: "Modules/Core/tests/Feature/Phase11AcceptanceTest.php"
      provides: "End-to-end acceptance test spanning db:backup happy + corrupt + banner render"
      contains: "Phase11AcceptanceTest"
  key_links:
    - from: "README.md"
      to: "php artisan db:backup + db:restore + diederik:doctor + diederik:failed-jobs"
      via: "Markdown headings + code fences"
      pattern: "db:backup"
    - from: "Modules/Core/tests/Feature/Phase11AcceptanceTest.php"
      to: "RealSqliteFixture + BackupDatabaseCommand + SystemAlertsBanner + AcknowledgeSystemAlert"
      via: "Single Pest test exercising all four surfaces"
      pattern: "RealSqliteFixture"
---

<objective>
Wave 4 close-out: rewrite the README's `## Backups` and `## Operator recovery` sections to make the daily-use operator story discoverable and pin its safety contract via a content arch test. Ship the Phase 11 end-to-end acceptance test that exercises the full vertical (`db:backup` happy → sidecar verify → banner does NOT render; force-corrupt → `.suspect` + `system_alerts` row + banner renders critical → acknowledge clears banner). Add a final human-verify checkpoint so the user confirms on their actual Herd-mounted machine.

Purpose: Without 11-05, Phase 11 has shipped working code but no operator documentation — and SC #3 ("Operator documentation explicitly forbids `cp database.sqlite` of the live WAL DB and points to `db:backup` as the supported path") would fail. The acceptance test runs all four user-visible surfaces against a single fixture so regressions across the cross-plan boundary surface immediately.
Output: Updated README, content arch test, end-to-end acceptance test, manual smoke checkpoint, fully-green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-operational-hardening/11-CONTEXT.md
@.planning/phases/11-operational-hardening/11-RESEARCH.md
@.planning/phases/11-operational-hardening/11-PATTERNS.md
@.planning/phases/11-operational-hardening/11-UI-SPEC.md
@.planning/phases/11-operational-hardening/11-VALIDATION.md
@.planning/phases/11-operational-hardening/11-01-SUMMARY.md
@.planning/phases/11-operational-hardening/11-02-SUMMARY.md
@.planning/phases/11-operational-hardening/11-03-SUMMARY.md
@.planning/phases/11-operational-hardening/11-04-SUMMARY.md

<interfaces>
<!-- Everything this plan composes against was built in earlier waves. -->

From README.md (existing — lines 125–167 contain the current ## Backups + ## Operator recovery placeholders):
- The "Stuck Redis unique-lock keys" subsection under ## Operator recovery is the template prose style + must be PRESERVED verbatim in the rewrite
- The existing "## Backups" placeholder is short and aspirational — Phase 11 replaces it with the operational recipe

From Modules/Core/Internal/Console/BackupDatabaseCommand.php (Wave 1):
- `php artisan db:backup` (smart-skip on unchanged data_version)
- `php artisan db:backup --force` (manual run)
- The BackupCorruptionPathTest in Plan 11-02 Task 2 established the deterministic corruption recipe used by Plan 11-05 Task 2 (port its solution verbatim).

From Modules/Core/Internal/Console/RestoreDatabaseCommand.php (Wave 2):
- `php artisan db:restore <path>` (triple safety rail)
- `php artisan db:restore --confirm --force-maintenance <path>` (scripted use)

From Modules/Core/Internal/Console/FailedJobsCommand.php (Wave 3):
- `php artisan diederik:failed-jobs prune --older-than=30d --dry-run` (preview)
- `php artisan diederik:failed-jobs prune --older-than=30d` (apply)

From Modules/Core/Internal/Console/DoctorCommand.php (Wave 2 — extended):
- `php artisan diederik:doctor` (WAL + synchronous + backup-freshness probes)

From tests/Helpers/RealSqliteFixture.php (Wave 0):
- Used by Phase11AcceptanceTest as the source DB for the full vertical run
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: README rewrite (## Backups + ## Operator recovery) + ReadmeOperationalDocsTest content arch test</name>
  <files>README.md, Modules/Core/tests/Feature/ReadmeOperationalDocsTest.php</files>
  <read_first>
    - README.md (FULL FILE — read the entire current file before rewriting; lines 125–167 are the rewrite target but earlier sections set the tone the rewrite must preserve)
    - .planning/phases/11-operational-hardening/11-CONTEXT.md `<domain>` (the "README rewrite" bullet at the end of the What Phase 11 delivers list)
    - .planning/phases/11-operational-hardening/11-PATTERNS.md "README.md (MODIFIED — rewrite ## Backups + ## Operator recovery)" section
    - .planning/phases/11-operational-hardening/11-RESEARCH.md §"Don't Hand-Roll" + §"State of the Art" rows on `cp database.sqlite`
    - The current "Stuck Redis unique-lock keys" subsection under ## Operator recovery (read its prose style — explicit reproduction recipe + clear "if the issue happens, do these steps" cadence)
  </read_first>
  <behavior>
    - Rewrite `## Backups` section to contain (sentence-cased headings, no GSD-internal references like decision IDs or phase labels):
      - One paragraph: what `db:backup` does (VACUUM INTO + integrity check + sidecar + retention pruning).
      - A `### Daily schedule` subsection naming the 03:00 schedule entry + the `withoutOverlapping(60)` lock (and how to manually clear the lock if a crash leaves it stuck — point at the Operator recovery subsection added in this task).
      - A `### Manual run` subsection showing `php artisan db:backup` (smart-skip) vs `php artisan db:backup --force` (always-run).
      - A `### Retention` subsection documenting "7 daily files + 4 most-recent Sunday-dated weekly snapshots; `.suspect` files and `pre-restore-*` files are never pruned automatically".
      - A `### Verifying a backup` subsection showing `sqlite3 storage/app/backups/<file>.sqlite "PRAGMA integrity_check;"` for ad-hoc verification.
      - A `### DO NOT cp database.sqlite` subsection — explicit, headed, warning-toned (preserve the existing `.sqlite-wal` / `.sqlite-shm` explanation if it exists; if not, add a one-paragraph explanation).
    - Rewrite `## Operator recovery` to contain:
      - The EXISTING "Stuck Redis unique-lock keys" subsection — PRESERVED verbatim (do not rename, do not paraphrase; copy-paste from current README into the new structure).
      - NEW `### Restoring from a backup` subsection: step-by-step recipe — `php artisan down` → `php artisan db:restore --confirm <path-to-backup>` → check `php artisan db:restore` output for the `pre-restore-*.sqlite` snapshot path → run `php artisan diederik:doctor` to verify the restored DB's PRAGMAs → `php artisan up`. Includes the `--force-maintenance` script-friendly variant.
      - NEW `### Corrupt-backup alert` subsection: explains what triggers a `system_alerts(kind=backup_corrupt)` row, where the `.suspect` file lives, how to inspect it (`sqlite3 <suspect-file> "PRAGMA integrity_check;"`), how to dismiss the alert (banner button OR `php artisan tinker` + Eloquent update), and how to manually delete the `.suspect` file after inspection.
      - NEW `### Failed-jobs maintenance` subsection: the `diederik:failed-jobs prune` recipe — `--dry-run` to preview, valid durations `d|h|w`, default `--older-than=30d`, links back to the Doctor command for queue health.
      - NEW `### Stuck withoutOverlapping lock` subsection: if a `db:backup` process is SIGKILL'd, the lock row sits in the `cache` table for 60 minutes blocking subsequent runs; the recipe is `php artisan cache:forget "framework/schedule-<hash>"` (operator can find the exact key in the `cache` table via SQLite query) OR wait the 60-minute TTL out.
    - The rewrite uses generic storage paths only (`storage/app/backups/<file>.sqlite`) — no live machine paths or user-specific locations leak (threat T-11-05 mitigation).
    - The rewrite does NOT mention `.planning/`, decision-ID codes matching `/D-\d{4}/`, `Phase 11`, "we added", "as of v1.0" — feedback memory rule (codebase agnostic from GSD, docs describe current state).
    - ReadmeOperationalDocsTest is a Pest feature test that uses the file-grep arch-test pattern (RecursiveIteratorIterator NOT needed — single file read). Required substring assertions (each must be present in README.md, comment-stripped):
      - `## Backups`
      - `## Operator recovery`
      - `php artisan db:backup`
      - `php artisan db:restore`
      - `php artisan diederik:doctor`
      - `php artisan diederik:failed-jobs`
      - `pre-restore-`
      - `.suspect`
      - `7 daily`
      - `Sunday`
      - `Stuck Redis unique-lock keys` (preservation of existing subsection)
      - `### Restoring from a backup`
      - `### Corrupt-backup alert`
      - `### Failed-jobs maintenance`
    - Forbidden substring assertions (each MUST NOT be present in README.md):
      - `.planning/` (no GSD-internal references)
      - Any 4-digit decision ID — implemented via regex `/D-\d{4}/` (this catches D-1101 through D-1119 AND any future D-XXXX code). The Pest assertion is `expect(preg_match('/D-\\d{4}/', $contents))->toBe(0);`. Do NOT use a literal `D-110` substring assertion — that misses `D-1110` and any code with more than three digits after the dash.
      - `Phase 11` (no phase labels in operator docs)
      - `cp database.sqlite` outside of the `### DO NOT cp database.sqlite` subsection — to enforce the warning is the ONLY occurrence, use a regex assertion that counts occurrences and asserts == 1 inside the warning subsection. Implementation: find the line index of `### DO NOT cp database.sqlite`, count `cp database.sqlite` substrings in the file, assert count == 1.
  </behavior>
  <action>Read the current `README.md` completely (the file is small — a single Read call captures it). Identify the existing `## Backups` and `## Operator recovery` sections. Rewrite both sections in-place, preserving any sections above and below. The "Stuck Redis unique-lock keys" subsection content is preserved verbatim — extract its prose before rewriting, then re-insert. The new subsections follow the cadence of the existing one: short reproduction context paragraph + a numbered or bulleted recipe + a one-sentence "after recovery, verify by..." closer. Use the exact subsection headings listed in `<behavior>` (heading levels: `###` for subsections, `##` for top-level — match existing README cadence).

  Write `ReadmeOperationalDocsTest.php` at `Modules/Core/tests/Feature/`. The test reads `base_path('README.md')` once (use the injected `Filesystem $files` resolved via `$this->app->make(Filesystem::class)` in the test body), comment-strips via `preg_replace('#<!--.*?-->#s', ...)` (markdown comments), then runs the required-substring loop + forbidden-substring loop.

  The decision-ID forbidden assertion is the regex `preg_match('/D-\\d{4}/', $contents)` — assert it returns 0. This catches D-1101 through D-1119 AND any four-digit-suffix decision code the team writes in the future. **Do NOT use a literal `'D-110'` substring assertion** — that pattern would (a) miss D-1110+, and (b) require a follow-up edit each time a decision ID rolls over a digit boundary.

  The `cp database.sqlite` count assertion is a small custom check: `expect(substr_count($contents, 'cp database.sqlite'))->toBe(1);` and `expect($contents)->toContain('### DO NOT cp database.sqlite');`. DO NOT use `uses(RefreshDatabase::class);` — this test reads a file, no DB needed.</action>
  <verify>
    <automated>./vendor/bin/pest --filter='ReadmeOperationalDocsTest' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - File `README.md` modified; `grep -c "## Backups" README.md` returns ≥1; `grep -c "## Operator recovery" README.md` returns ≥1; `grep -c "Stuck Redis unique-lock keys" README.md` returns ≥1 (preservation).
    - `grep -c ".planning/" README.md` returns 0 (no GSD-internal references).
    - `grep -c "Phase 11" README.md` returns 0 (no phase labels).
    - `grep -cE 'D-[0-9]{4}' README.md` returns 0 (no four-digit decision IDs of any form).
    - `grep -c "cp database.sqlite" README.md` returns 1 (only inside the DO NOT subsection).
    - All 14 required substrings present (verify via the test).
    - `./vendor/bin/pest --filter='ReadmeOperationalDocsTest' --parallel` exits 0.
    - The forbidden-substring assertion in the test uses `preg_match('/D-\\d{4}/', $contents)` (verify by grepping the test file: `grep -c 'D-\\\\d{4}' Modules/Core/tests/Feature/ReadmeOperationalDocsTest.php` returns ≥1).
    - `./vendor/bin/phpstan analyse --memory-limit=2G Modules/Core/tests/Feature/ReadmeOperationalDocsTest.php` exits 0.
    - `./vendor/bin/pint --test README.md Modules/Core/tests/Feature/ReadmeOperationalDocsTest.php` exits 0 (Pint will skip README — fine; check exit code for the .php file).
  </acceptance_criteria>
  <done>Operator-facing documentation rewritten in-place; content arch test pins the required + forbidden substrings with a future-proof decision-ID regex.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase11AcceptanceTest — end-to-end vertical (db:backup → banner-empty → corrupt → banner-renders-critical → acknowledge)</name>
  <files>Modules/Core/tests/Feature/Phase11AcceptanceTest.php</files>
  <read_first>
    - Modules/Core/tests/Feature/BackupDatabaseCommandTest.php (Wave 1 plan 11-02 — backup happy + corrupt path test scaffolding)
    - Modules/Core/tests/Feature/BackupCorruptionPathTest.php (Wave 1 plan 11-02 — the deterministic corruption recipe to port HERE verbatim)
    - Modules/Core/tests/Feature/SystemAlertsBannerTest.php (Wave 3 plan 11-04 — Livewire test scaffolding)
    - Modules/Core/tests/Feature/RestoreSuccessPathTest.php (Wave 2 plan 11-03 — RealSqliteFixture usage in feature context)
    - tests/Helpers/RealSqliteFixture.php (Wave 0 plan 11-01)
    - .planning/phases/11-operational-hardening/11-VALIDATION.md "Manual-Only Verifications" table (the visual-only steps stay manual; everything else is automated here)
  </read_first>
  <behavior>
    - `uses(RefreshDatabase::class);` at the top — fresh `system_alerts` table per test.
    - Single `test('phase 11 backup banner round-trip', function (): void { ... })` block covers:
      1. Build a source DB via `$path = RealSqliteFixture::create('phase11-acceptance');` and seed a transactions row via raw PDO.
      2. Override `$this->app->make(Repository::class)->set('database.connections.sqlite.database', $path);` so the BackupDatabaseCommand operates against the fixture.
      3. Set the backups directory: build `$backupsDir = sys_get_temp_dir().'/diederik-phase11-'.uniqid().'/storage/app/backups'`, `mkdir($backupsDir, 0o755, true)`, and override the `'core.backups_directory'` container binding via `$this->app->instance('core.backups_directory', $backupsDir);` (this is the same hook established in Plan 11-02 Task 2 — single canonical override point across all backup-related code paths).
      4. Run `$this->artisan('db:backup', ['--force' => true])->assertSuccessful();` — assert a `diederik-*.sqlite` file exists in `$backupsDir` + a matching `.meta.json` sidecar exists.
      5. Authenticate as a user via `Livewire::actingAs(User::factory()->create())`. Render the banner: `Livewire::test(SystemAlertsBanner::class)->assertDontSee('Mark as resolved');` — calm state, no alerts.
      6. **Corrupt the source DB via the verbatim recipe from Plan 11-02's BackupCorruptionPathTest** (DO NOT invent a new corruption strategy; DO NOT hand-seed `system_alerts` rows to shortcut the corrupt path). The recipe is the deterministic "VACUUM source → truncate source after VACUUM begins" or "malform source header" pattern that BackupCorruptionPathTest already proved works against this exact command. Concrete steps:
         - First, call `$this->app->make(DatabaseManager::class)->purge();` to release the framework's connection handle on `$path`.
         - Then truncate the source DB: `file_put_contents($path, str_repeat("\xFF", 4096));` — overwrites the first 4096 bytes with 0xFF, destroying the SQLite header magic bytes. (Alternatively, if BackupCorruptionPathTest in Plan 11-02 used a different recipe — e.g. truncating to N bytes, or pre-corrupting the VACUUM destination via an event listener — port THAT recipe instead. The rule: whatever BackupCorruptionPathTest does to reliably hit the `.suspect` branch, do that here.)
         - Re-run `$this->artisan('db:backup', ['--force' => true])->assertFailed();`. The command takes the corrupt-source path (either VACUUM INTO throws a `\PDOException` and the command's exception-bridge writes the `system_alerts` row + writes a `.suspect` file with whatever partial output exists, OR VACUUM INTO succeeds and the post-VACUUM `PRAGMA integrity_check` returns non-`'ok'` and the command takes the literal `.suspect` rename branch). Either way, the assertion target is the same: a `.suspect` file lives in `$backupsDir` AND a `system_alerts(kind=backup_corrupt, severity=critical)` row exists.
      7. Assert `glob($backupsDir.'/diederik-*.sqlite.suspect')` returns at least one file; assert `SystemAlert::query()->where('kind', 'backup_corrupt')->where('severity', 'critical')->count() === 1`.
      8. Re-render the banner: `Livewire::test(SystemAlertsBanner::class)->assertSee('failed integrity check');` — critical alert renders.
      9. Click acknowledge: capture the alert id via `$alertId = SystemAlert::query()->where('kind', 'backup_corrupt')->value('id');` then `Livewire::test(SystemAlertsBanner::class)->call('acknowledge', $alertId);`. Assert next render does NOT see "failed integrity check".
      10. Cleanup: `RealSqliteFixture::cleanup($path);` + recursive removal of the temp backups dir via the Filesystem.
    - The test asserts ALL FIVE artifacts of Phase 11 work together: the migration (system_alerts table is reachable), the Public action (acknowledge clears the banner), the command (db:backup runs the WHOLE pipeline + writes alert on corruption), the Livewire SFC (renders + dismisses), and the goal-backward truths of the phase.
    - If any single assertion fails, the test reports which surface broke — making the cross-plan boundary regressions visible.
  </behavior>
  <action>Write `Phase11AcceptanceTest.php` at `Modules/Core/tests/Feature/Phase11AcceptanceTest.php`. The test is intentionally LONG — a single `test(...)` block walking the 10 steps above with inline assertions per step. Use `expect(...)` chains for the file-existence assertions and `Livewire::test(...)->assertSee/assertDontSee/call(...)` for the SFC interactions.

  **Step 6 corruption recipe — port the deterministic pattern from `Modules/Core/tests/Feature/BackupCorruptionPathTest.php` verbatim.** Read that file first (it was written in Plan 11-02 Task 2). Use the same source-corruption mechanism it uses. The acceptance test MUST exercise the full BackupDatabaseCommand pipeline — no shortcuts:
  - **Forbidden:** hand-seeding a `system_alerts(kind=backup_corrupt, severity=critical)` row via `SystemAlert::create([...])` or raw `DatabaseManager::insert(...)` to simulate the post-corruption state. That degrades the acceptance test into duplicate coverage of SystemAlertsBannerTest.
  - **Forbidden:** any "ACCEPTED FALLBACK" branch that skips the `db:backup --force` invocation in step 6.
  - **Required:** the test invokes `$this->artisan('db:backup', ['--force' => true])` on a deliberately-corrupted source AND lets the command write the `system_alerts` row organically through its production code path.

  The cleanest implementation pattern (BackupCorruptionPathTest's likely solution): purge the DatabaseManager connection so the file handle is released, then overwrite the source DB file with malformed bytes (`file_put_contents($path, str_repeat("\xFF", 4096))` is one valid recipe — the first SQLite page is wholly invalid, so VACUUM INTO either throws a `\PDOException` that the command's catch-block converts into the corrupt-path response, OR VACUUM INTO produces a malformed output file that the post-VACUUM `PRAGMA integrity_check` rejects). The command's two-pronged handling (try/catch on VACUUM + post-VACUUM integrity_check) means either branch lands on the `.suspect` + `system_alerts` outcome. Document the exact recipe inline in the test as a `// Corruption recipe: ported from BackupCorruptionPathTest — ...` comment so future readers see the deliberate cross-test reuse.

  **Do NOT use Storage::fake()** — use `sys_get_temp_dir()` + the `'core.backups_directory'` container binding override (mirrors Plan 11-02's W6-locked test convention and Plan 11-03's BackupFreshnessProbe convention).</action>
  <verify>
    <automated>./vendor/bin/pest --filter='Phase11AcceptanceTest' --parallel</automated>
  </verify>
  <acceptance_criteria>
    - File `Modules/Core/tests/Feature/Phase11AcceptanceTest.php` exists.
    - `grep -c "artisan.*db:backup" Modules/Core/tests/Feature/Phase11AcceptanceTest.php` returns ≥ 2 (step 4 happy run + step 6 corrupt run — both must invoke the real command, not a shortcut).
    - `grep -c "SystemAlert::create" Modules/Core/tests/Feature/Phase11AcceptanceTest.php` returns 0 (the test must NOT hand-seed alert rows — the command writes them through production code paths).
    - `grep -c "ACCEPTED FALLBACK" Modules/Core/tests/Feature/Phase11AcceptanceTest.php` returns 0 (the BLOCKER-#2 fallback language is forbidden — the corrupt-path coverage is non-negotiable).
    - `./vendor/bin/pest --filter='Phase11AcceptanceTest' --parallel` exits 0.
    - The full Phase 11 test suite passes: `./vendor/bin/pest --filter='SystemAlertsMigrationTest|SystemAlertModelTest|SystemAlertQueryTest|AcknowledgeSystemAlertTest|BackupRetentionPolicyTest|BackupDatabaseCommandTest|BackupCorruptionPathTest|BackupScheduleTest|DoctorProbesTest|DoctorCommandTest|RestoreDatabaseCommandTest|RestoreSuccessPathTest|AppBootHealthCheckTest|DurationParserTest|FailedJobsCommandTest|SystemAlertsBannerTest|ReadmeOperationalDocsTest|Phase11AcceptanceTest|BoundaryArchTest|HorizonForceFlagTest|RealSqliteFixtureTest' --parallel` exits 0.
    - `./vendor/bin/phpstan analyse --memory-limit=2G Modules/Core/tests/Feature/Phase11AcceptanceTest.php` exits 0.
    - `./vendor/bin/pint --test Modules/Core/tests/Feature/Phase11AcceptanceTest.php` exits 0.
  </acceptance_criteria>
  <done>End-to-end acceptance test passes; the cross-plan boundary is exercised in a single Pest run. The corrupt-path branch invokes the real BackupDatabaseCommand pipeline — no hand-seeded alert rows, no fallback shortcut.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify smoke on the real Herd-mounted DB</name>
  <what-built>
    Phase 11 has shipped 4 vertical slices: db:backup (CLI + scheduler + retention + smart-skip + corrupt path), db:restore (triple safety rail + pre-restore snapshot), diederik:doctor probes + app-boot PRAGMA check, diederik:failed-jobs CLI, SystemAlertsBanner Livewire SFC at the top of every authenticated page, README rewrite + content arch test, BoundaryArchTest extensions + HorizonForceFlagTest, RealSqliteFixture helper. All Pest tests are green; the acceptance test runs end-to-end.
  </what-built>
  <how-to-verify>
    1. Open a terminal at the project root.
    2. Run `php artisan db:backup --force`. Confirm output mentions a new file path under `storage/app/backups/`. Run `ls -la storage/app/backups/` and verify the file is at `-rw-------` (0600) with a matching `.meta.json` sidecar also at 0600.
    3. Run `php artisan db:backup` (without --force). Confirm the output says `Skipped — no commits since last backup` and that no new file was created.
    4. Run `php artisan diederik:doctor`. Confirm three new probe lines appear: `SQLite WAL mode`, `SQLite synchronous mode`, `Backup freshness`. All should be `ok` on a freshly-backed-up DB. Exit code should be 0.
    5. Run `php artisan diederik:failed-jobs prune --older-than=30d --dry-run`. Confirm it prints a summary line (likely "Would delete 0 rows" if no failed jobs exist) and exits 0 without modifying any table.
    6. Open `https://diederik.test` in a browser. Confirm NO banner is visible on any page (calm state).
    7. From a terminal, run `php artisan tinker --execute='\Modules\Core\Models\SystemAlert::create(["user_id" => null, "kind" => "backup_corrupt", "severity" => "critical", "message" => "Test alert from human-verify checkpoint.", "metadata" => ["timestamp" => now()->toIso8601String(), "suspect_path" => "storage/app/backups/test.sqlite.suspect"]]); echo "created";'`. Confirm output says "created".
    8. Reload `https://diederik.test`. Confirm a ROSE-coloured banner now appears at the top of the page with the test message and a "Mark as resolved" button.
    9. Click the "Mark as resolved" button. Confirm the banner disappears within ~500ms and stays gone on subsequent page reloads.
    10. Open the README. Confirm the `## Backups` and `## Operator recovery` sections are present and contain the new subsections listed in plan 11-05.
  </how-to-verify>
  <resume-signal>Type "approved" once all 10 steps pass on the local Herd-mounted DB, or describe any failures encountered so the planner can route them through a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → operator behavior | The doc is the only thing standing between the operator and `cp database.sqlite` of a live WAL DB; the rewrite + content arch test are the load-bearing safety properties for SC #3 |
| Phase11AcceptanceTest → cross-plan regression detection | A single end-to-end test catches cross-plan boundary breakage that per-plan tests would miss; corrupt-path coverage must exercise the real command pipeline |
| Human-verify checkpoint → real-machine assumptions | Some behaviors (chmod 600 on the real Herd-mounted DB, Livewire rendering in the actual browser) cannot be fully automated; the checkpoint is the final gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Information Disclosure | README leaks user-specific paths | mitigate | Forbidden-substring assertion in ReadmeOperationalDocsTest rejects `.planning/`, `Phase 11`, and any `/D-\d{4}/` decision ID. All paths in the doc are generic (`storage/app/backups/<file>.sqlite`). |
| T-11-05-02 | Tampering | Future contributor reverts the "DO NOT cp database.sqlite" subsection | mitigate | ReadmeOperationalDocsTest asserts both the heading + that `cp database.sqlite` substring count is exactly 1 (= only inside the warning subsection). Removing the warning would fail the count assertion. |
| T-11-05-03 | Tampering | Cross-plan regression slips through per-plan tests | mitigate | Phase11AcceptanceTest exercises all four user-visible surfaces in a single Pest run. A break in any single surface fails the acceptance test. The corrupt-path branch ports the deterministic recipe from Plan 11-02's BackupCorruptionPathTest verbatim — no fallback to hand-seeded alert rows. |
| T-11-05-04 | Information Disclosure | README accidentally documents internal-only paths | accept | Generic-path policy in the rewrite + the content arch test cover the known categories. Edge cases caught at code review. |
| T-11-05-05 | Tampering | Operator runs db:restore on the wrong file | mitigate | README `### Restoring from a backup` documents the pre-restore-snapshot recovery path; the operator can always undo by restoring the pre-restore snapshot. The triple safety rail (built in plan 11-03) prevents the worst-case live-DB-overwrite-without-snapshot scenario. |
| T-11-05-06 | Tampering | New decision IDs (D-1110+) leak into README and the test misses them | mitigate | Decision-ID forbidden assertion uses regex `/D-\d{4}/` — catches all four-digit suffixes (D-1101 through D-9999) without a per-ID update. Future-proof against decision-set growth. |
| T-11-05-SC | Tampering | composer install | accept | This plan installs NO new packages. |
</threat_model>

<verification>
- `./vendor/bin/pest --parallel` (FULL SUITE) exits 0.
- `./vendor/bin/phpstan analyse --memory-limit=2G` exits 0.
- `./vendor/bin/pint --test` exits 0.
- README sections `## Backups` and `## Operator recovery` rewritten in-place; both pass the content arch test (including the `/D-\d{4}/` regex assertion).
- Phase11AcceptanceTest exercises the cross-plan end-to-end vertical — corrupt path runs through the real `db:backup --force` command, NOT a hand-seeded alert shortcut.
- Human-verify checkpoint approved by user.
- All three ROADMAP Success Criteria for Phase 11 satisfied:
  - SC #1: `php artisan db:backup` produces a consistent, restorable SQLite backup while the app is running (plan 11-02).
  - SC #2: The latest backup is automatically verified by re-opening it and running PRAGMA integrity_check, with failures surfaced to the user (plan 11-02 BackupCorruptionPathTest + plan 11-04 SystemAlertsBanner + plan 11-05 Phase11AcceptanceTest).
  - SC #3: Operator documentation explicitly forbids `cp database.sqlite` of the live WAL DB and points to `db:backup` as the supported path (plan 11-05 README rewrite + content arch test).
- FND-05 requirement status in REQUIREMENTS.md `## Traceability` table flippable from `Pending` to `Complete`.
</verification>

<success_criteria>
- README's `## Backups` + `## Operator recovery` sections rewritten; all 14 required substrings present; all forbidden substrings absent (including the `/D-\d{4}/` regex catching D-1101 through D-1119+ in one assertion).
- ReadmeOperationalDocsTest enforces the content contract against future doc churn.
- Phase11AcceptanceTest exercises db:backup + corruption-via-real-command + banner + acknowledge in a single Pest run; passes. No hand-seeded `SystemAlert::create` calls; no fallback branches.
- Human-verify checkpoint approved.
- Full test suite green: Pest + Larastan level 10 + Pint.
- Phase 11 = COMPLETE. v1 operational hardening shipped.
</success_criteria>

<output>
Create `.planning/phases/11-operational-hardening/11-05-SUMMARY.md` when done.
</output>
