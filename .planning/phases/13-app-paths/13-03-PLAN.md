---
phase: 13-app-paths
plan: 03
type: execute
wave: 3
depends_on: [13-01, 13-02]
files_modified:
  - Modules/Core/tests/Feature/UserDataPathResolutionTest.php
  - tests/Contracts/BoundaryArchTest.php
autonomous: true
requirements: [PKG-01]
must_haves:
  truths:
    - "Running migrate:fresh under a simulated NATIVEPHP_STORAGE_PATH=<tmp> creates the SQLite file under <tmp>/database/"
    - "db:backup under the simulated env writes to <tmp>/storage/app/backups/"
    - "The OAuth-secrets path resolves under <tmp>/storage/app/secrets/ under the simulated env"
    - "With no env var set, every resolved path is byte-identical to the project-rooted paths used today"
    - "BoundaryArchTest::noStoragePathHardCodedOutsideUserDataPathService is green and composer check:paths exits 0"
  artifacts:
    - path: "Modules/Core/tests/Feature/UserDataPathResolutionTest.php"
      provides: "Simulated-NativePHP-env feature test proving migrate:fresh / db:backup / OAuth secrets resolution"
      contains: "NATIVEPHP_STORAGE_PATH"
    - path: "tests/Contracts/BoundaryArchTest.php"
      provides: "Green noStoragePathHardCodedOutsideUserDataPathService invariant"
      contains: "noStoragePathHardCodedOutsideUserDataPathService"
  key_links:
    - from: "UserDataPathResolutionTest"
      to: "artisan migrate:fresh / db:backup"
      via: "putenv-simulated NativePHP env"
      pattern: "putenv\\('NATIVEPHP_STORAGE_PATH"
    - from: "BoundaryArchTest"
      to: "composer check:paths"
      via: "duplicate invariant encoding"
      pattern: "check:paths"
---

<objective>
Complete and prove the two ROADMAP success criteria for Phase 13: (1) the arch
test + CI grep gate are green, enforcing that no raw path helper or storage
literal survives outside `UserDataPathService`; (2) a Pest feature test proves
`migrate:fresh`, `db:backup`, and OAuth-secrets resolution all land under a
simulated `NATIVEPHP_STORAGE_PATH=<tmp>` root.

Purpose: This is the final enforcement slice of PKG-01. Plan 01 created the
service and the (deliberately RED) enforcement scaffolding; Plan 02 migrated
every call site. This plan fills in the real feature-test assertions and
confirms the invariant locked. After this plan the phase goal is observably
true: paths are NativePHP-routable and the codebase cannot regress.

Output: a complete `UserDataPathResolutionTest.php` (real assertions, not the
Plan 01 stub) and a verified-green `noStoragePathHardCodedOutsideUserDataPathService`
arch invariant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-app-paths/13-CONTEXT.md
@.planning/phases/13-app-paths/13-RESEARCH.md
@.planning/phases/13-app-paths/13-PATTERNS.md
@.planning/phases/13-app-paths/13-01-SUMMARY.md
@.planning/phases/13-app-paths/13-02-SUMMARY.md

<interfaces>
<!-- Artifacts this plan completes — created as stubs in Plan 01. -->
Modules/Core/tests/Feature/UserDataPathResolutionTest.php
  - already has beforeEach/afterEach scaffolding (temp dir, putenv set/clear,
    mkdir of <tmp>/database + <tmp>/storage/app/{backups,secrets},
    config-not-cached guard, teardown cascade) and a placeholder it() ->todo()
tests/Contracts/BoundaryArchTest.php
  - already has the noStoragePathHardCodedOutsideUserDataPathService it() block
    appended after line 1117

UserDataPathService static surface (from Plan 01):
  ::databaseFile() ::backupsPath() ::secretsPath() ::frameworkPath($sub)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill in the simulated-NativePHP-env feature test</name>
  <read_first>
    - Modules/Core/tests/Feature/UserDataPathResolutionTest.php — the Plan 01 scaffold (beforeEach/afterEach, the `->todo()` placeholder)
    - Modules/Core/tests/Feature/BackupDatabaseCommandTest.php — analog for driving `db:backup` and asserting on-disk artifacts; lines 39-45 show rebinding the sqlite connection
    - .planning/phases/13-app-paths/13-RESEARCH.md § "Pitfalls 3, 4, 5" (lines 291-311 — putenv/getenv, config-cache, mkdir-the-parent) and § "Validation Architecture" Phase-Requirements→Test-Map (lines 517-526)
    - .planning/phases/13-app-paths/13-PATTERNS.md § "Modules/Core/tests/Feature/UserDataPathResolutionTest.php" (lines 97-120)
    - Modules/Core/Public/Services/UserDataPathService.php — the accessor surface under test
  </read_first>
  <behavior>
    - Test: with `putenv('NATIVEPHP_STORAGE_PATH='.$tmp)`, running `artisan migrate:fresh` against an sqlite connection pointed at `UserDataPathService::databaseFile()` creates a real SQLite file under `<tmp>/database/database.sqlite` (assert `file_exists`)
    - Test: with the same simulated env, running `artisan db:backup` produces a backup file under `<tmp>/storage/app/backups/` (assert the directory is non-empty after the command, exit code 0)
    - Test: with the same simulated env, `UserDataPathService::secretsPath()` resolves to `<tmp>/storage/app/secrets` (assert string equality)
    - Test (no-env / Herd parity guard, RESEARCH A2): with `NATIVEPHP_STORAGE_PATH` cleared, `UserDataPathService::databaseFile()` / `backupsPath()` / `secretsPath()` resolve to the project-rooted paths byte-identical to today's `database_path()`/`storage_path()` output
    - Test: the suite asserts `! $this->app->configurationIsCached()` (or runs `config:clear`) before relying on resolved config (Pitfall 4)
  </behavior>
  <action>
    Replace the Plan 01 placeholder `it()->todo()` in `UserDataPathResolutionTest.php` with the real assertions covering the four behaviors above. The `beforeEach`/`afterEach` scaffolding (temp dir, `putenv` set/clear, `mkdir` of `<tmp>/database` + `<tmp>/storage/app/backups` + `<tmp>/storage/app/secrets`, teardown cascade) already exists from Plan 01 — reuse it; do not duplicate it.

    For the `migrate:fresh` test: point the active sqlite connection at `UserDataPathService::databaseFile()` (it resolves under `<tmp>` because the env var is set) — follow `BackupDatabaseCommandTest` lines 39-45's `Repository::set('database.connections.sqlite.database', ...)` rebind pattern, then run `$this->artisan('migrate:fresh')` and assert `file_exists()` on the `<tmp>/database/database.sqlite` path. The `<tmp>/database` directory already exists from `beforeEach` (Pitfall 5 — SQLite will not create the parent).

    For the `db:backup` test: run `$this->artisan('db:backup')` (the command resolves `UserDataPathService` via DI and writes under `backupsPath()` = `<tmp>/storage/app/backups`); assert the command exits 0 and the backups directory is non-empty.

    For the secrets-path test: a direct string-equality assertion on `UserDataPathService::secretsPath()` against `$tmp.'/storage/app/secrets'` — the real OAuth-secrets write happens in `OAuthSecretsRepository`/the Auth migration; this test proves the *path* resolves correctly under the simulated env (RESEARCH Test-Map row "OAuth secrets path resolves under <tmp>/storage/app/secrets/").

    For the Herd-parity test: clear the env (`putenv('NATIVEPHP_STORAGE_PATH')`), then assert the accessors equal the project-rooted equivalents. This is the RESEARCH A2 regression guard — a future refactor must not silently shift the dev-mode paths.

    Guard against Pitfall 4: assert `! $this->app->configurationIsCached()` at the top of the relevant tests (or call `config:clear`), so a stray `bootstrap/cache/config.php` cannot freeze a stale path and produce a false green/red.

    Remove the `->todo()` marker so the placeholder becomes a real executing test.
  </action>
  <verify>
    <automated>php artisan config:clear && php artisan test --filter=UserDataPathResolution</automated>
  </verify>
  <acceptance_criteria>
    - `Modules/Core/tests/Feature/UserDataPathResolutionTest.php` contains real `it()` assertions (no `->todo()` marker remaining)
    - The file contains `putenv('NATIVEPHP_STORAGE_PATH` and `migrate:fresh` and `db:backup`
    - The file asserts `file_exists` on a `<tmp>/database/database.sqlite` path and asserts the backups directory is non-empty
    - The file contains a no-env Herd-parity assertion against project-rooted paths
    - The file contains a `configurationIsCached` guard or a `config:clear` call
    - `php artisan test --filter=UserDataPathResolution` exits 0
  </acceptance_criteria>
  <done>The simulated-NativePHP-env feature test proves migrate:fresh, db:backup, and OAuth-secrets resolution all land under `<tmp>`, and the no-env branch resolves to today's project-rooted paths — ROADMAP success criterion 2 is observably met.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the arch invariant + CI grep gate are green and lock the phase</name>
  <read_first>
    - tests/Contracts/BoundaryArchTest.php — the `noStoragePathHardCodedOutsideUserDataPathService` block appended in Plan 01
    - bin/check-paths.sh — the CI grep gate created in Plan 01
    - .planning/phases/13-app-paths/13-RESEARCH.md § "Extending BoundaryArchTest" judgement calls (lines 388-389) and § "Pitfall 2" (allow-list hygiene, lines 285-289)
    - .planning/phases/13-app-paths/13-02-SUMMARY.md — confirm every call site reported migrated
  </read_first>
  <action>
    With Plan 02 complete, the `noStoragePathHardCodedOutsideUserDataPathService` arch test and `composer check:paths` should now pass — there is no longer any raw helper or storage literal outside `UserDataPathService`.

    Run `php artisan test --filter=noStoragePathHardCodedOutsideUserDataPathService` and `composer check:paths`. If EITHER still reports offenders:
    - For each offender file, determine whether it is a genuine missed call site (then migrate it using the appropriate pattern from Plan 02 — config = static accessor, DI consumer = injected `UserDataPathService`, migration = container resolution) OR a false positive.
    - False-positive cases the test should already handle (verify, do not re-fix): comment/docblock-only references (stripped before matching), `.blade.php` `<code>`-tag display text (excluded for the literal check), `UserDataPathService::` static calls in `config/` (the `(?<![>:])` lookbehind makes them invisible), the allow-listed `UserDataPathService.php` itself.
    - If a genuine offender exists, fixing it is in scope for this task — do not weaken the test or broaden the allow-list to make it pass. The allow-list must stay exactly the single entry `Modules/Core/Public/Services/UserDataPathService.php` (Pitfall 2 — never allow-list a directory).

    Confirm the arch test's banned-helpers regex covers `database_path|storage_path|base_path` and does NOT include `public_path` (consistent with Plan 01/02 abstracting `public_path` into a `publicPath()` accessor) — and that `bin/check-paths.sh` matches the same three helpers.

    Run the full suite (`php artisan test`) plus `composer analyse` and `composer format:check` to confirm the phase introduced no regressions across the codebase.
  </action>
  <verify>
    <automated>php artisan test --filter=noStoragePathHardCodedOutsideUserDataPathService && composer check:paths --no-interaction && php artisan test</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=noStoragePathHardCodedOutsideUserDataPathService` exits 0 (invariant green)
    - `composer check:paths` exits 0 and prints the OK line
    - The arch test's allow-list array has exactly one entry: `Modules/Core/Public/Services/UserDataPathService.php`
    - `bin/check-paths.sh` and the arch test both match exactly `database_path`, `storage_path`, `base_path` (not `public_path`)
    - `php artisan test` (full suite) exits 0
    - `composer analyse` and `composer format:check` exit 0
  </acceptance_criteria>
  <done>The arch invariant and the CI grep gate are both green; the full test suite, Larastan L10 strict, and Pint all pass — ROADMAP success criterion 1 is observably met and the phase invariant is locked against regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| simulated env → test process | The feature test injects `NATIVEPHP_STORAGE_PATH` via `putenv()` into a temp dir — a leaked env var across tests could corrupt sibling tests |
| CI gate → build | `composer check:paths` is the regression boundary — a weakened gate would let raw helpers re-enter the codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | `NATIVEPHP_STORAGE_PATH` env leaking between tests | mitigate | `afterEach` clears the var with `putenv('NATIVEPHP_STORAGE_PATH')` (no `=`); per-test temp dir via `bin2hex(random_bytes(8))` prevents cross-test collision (RESEARCH Pitfall 3) |
| T-13-08 | Tampering / EoP | Arch-test allow-list scope creep | mitigate | Allow-list stays exactly one file entry; never a directory glob (RESEARCH Pitfall 2) — Task 2 acceptance criterion enforces this |
| T-13-09 | Information Disclosure | Stale `config:cache` freezing a dev path into the test result | mitigate | `config:clear` / `configurationIsCached()` guard in the feature test (RESEARCH Pitfall 4); documented as a Phase 15 hand-off for the shipped bundle |
| T-13-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 13 installs no packages; no install task exists, so no slopcheck checkpoint is required (RESEARCH § Package Legitimacy Audit — not applicable) |
</threat_model>

<verification>
- `php artisan test --filter=UserDataPathResolution` green — migrate:fresh / db:backup / secrets-path all resolve under `<tmp>`
- `php artisan test --filter=noStoragePathHardCodedOutsideUserDataPathService` green
- `composer check:paths` exits 0
- `php artisan test` full suite green
- `composer analyse` (Larastan L10 strict) green
- `composer format:check` (Pint) green
</verification>

<success_criteria>
- ROADMAP criterion 1: `noStoragePathHardCodedOutsideUserDataPathService` green + `composer check:paths` enforces the same rule against string literals
- ROADMAP criterion 2: Pest feature test proves `migrate:fresh` / `db:backup` / OAuth-secrets all resolve under a simulated `NATIVEPHP_STORAGE_PATH=<tmp>`
- No-env (Herd) branch proven byte-identical to today's project-rooted paths
- Full test suite + Larastan L10 strict + Pint all pass — no phase-wide regression
</success_criteria>

<output>
Create `.planning/phases/13-app-paths/13-03-SUMMARY.md` when done

## Phase 15 hand-off notes (record in SUMMARY)
- `NATIVEPHP_STORAGE_PATH` is a project-invented convention, NOT a documented NativePHP env var. Phase 15 must actually set it (bundled `.env` or NativePHP appdata path injected at boot), otherwise `UserDataPathService` silently falls back to project-rooted paths.
- NativePHP independently rewrites `Application::storagePath()` / `storage_path()`. Phase 15 must decide whether NativePHP's native rewrite or `NATIVEPHP_STORAGE_PATH` is the source of truth and make `UserDataPathService` consistent with that choice (RESEARCH Pitfall 6).
- The shipped bundle must `config:clear` then `config:cache` AFTER the env var is set — never before (RESEARCH Pitfall 4).
- `deploy/launchd/` plist templates hard-code `cd ~/code/diederik` working directories — out of Phase 13 scope; Phase 15 desktop shell owns process launching.
- Larastan-L10-on-PHP-8.4 spike (STATE.md blocker) is a Phase 17 CI-matrix item — out of Phase 13's PKG-01 scope (touches no path code).
</output>
