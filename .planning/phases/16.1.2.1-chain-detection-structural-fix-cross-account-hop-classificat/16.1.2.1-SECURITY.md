---
phase: 16.1.2.1-chain-detection-structural-fix-cross-account-hop-classificat
audited: 2026-05-27
asvs_level: 1
block_on: high
status: SECURED
threats_total: 26
threats_closed: 26
threats_open: 0
mitigate_verified: 14
accepted_risks: 12
transfer_documented: 0
unregistered_flags: 0
---

# Phase 16.1.2.1 — Security Audit (Threat Verification)

ASVS L1 — single-user, local-only desktop dashboard, no network surface. Register was authored at plan time (`register_authored_at_plan_time: TRUE`); this audit verifies that every declared `mitigate` disposition is present in the implemented code, and records every `accept` disposition as documented accepted risk. No new threats are scanned.

## Summary

| Disposition | Count | Status |
|-------------|-------|--------|
| mitigate    | 14    | all verified present in code |
| accept      | 12    | all logged as accepted risk below |
| transfer    | 0     | n/a |
| **Total**   | **26**| **all closed** |

## Mitigate verification (14)

| Threat ID | Category | File:line | One-line evidence |
|-----------|----------|-----------|-------------------|
| T-16.1.2.1-01-02 | Information disclosure | Modules/Categorization/Database/Seeders/DefaultCategorizationRuleSeeder.php:66,79 + Modules/Categorization/Internal/Services/RuleEvaluator.php:82 | Seeder writes `'user_id' => $user->id` on every insert; RuleEvaluator filters `where('user_id', $userId)` on every read — per-user scoping locked at both ends |
| T-16.1.2.1-01-04 | Elevation of privilege | Modules/Categorization/Database/Seeders/DefaultCategorizationRuleSeeder.php:56-67 | Category resolved by slug via `Category::query()->where('slug', $slug)->first()`; unresolved slug triggers `$this->logger->warning(...)` + `continue` — never a wrong-category insert |
| T-16.1.2.1-02-01 | Tampering | Modules/Ingestion/Public/Paypal/PaypalCsvEventTypeMap.php:123-129,149-158 | `MAP[$language][$eventType]` allow-list; unknown event type throws `UnknownPaypalEventTypeException`; in-MAP-but-missing-TRANSACTION_TYPE throws narrower `MissingPaypalTransactionTypeMapException` |
| T-16.1.2.1-03-02 | Tampering | Modules/Import/Internal/Services/KnownCounterpartyIbanResolver.php:54-55,70-71 | Both queries use explicit `->where('user_id', $userId)`; alias query AND Account lookup both filter by caller's user_id |
| T-16.1.2.1-03-03 | Information disclosure | Modules/Import/Database/Migrations/2026_05_27_010001_create_known_counterparty_ibans_table.php:52 + Modules/Import/Models/KnownCounterpartyIban.php:9,48 | UNIQUE `(user_id, real_iban)` constraint at migration; model uses `BelongsToUser` trait + `UserScope` global scope; resolver adds explicit per-user `where` as load-bearing primary guard |
| T-16.1.2.1-03-05 | Repudiation | Modules/Import/Database/Seeders/DefaultKnownCounterpartyIbansSeeder.php:64-67 + migration:52 | `KnownCounterpartyIban::withoutGlobalScopes()->firstOrCreate(...)` keyed on `(user_id, real_iban)`; UNIQUE constraint at DB layer; re-runs produce zero new rows and preserve `notes` |
| T-16.1.2.1-03-06 | Elevation of privilege | Modules/Import/Database/Migrations/2026_05_27_010001_create_known_counterparty_ibans_table.php:58-69 | Paired BEFORE INSERT + BEFORE UPDATE triggers on `target_account_kind` with `RAISE(ABORT, ...)` enforcing the `'bank','ics_card','paypal'` allow-list at the DB boundary |
| T-16.1.2.1-04-02 | Tampering | Modules/Import/Internal/Pipeline/Stages/ClassifyTransactionType.php:105 + Modules/Transfers/Internal/Listeners/PairTransferCandidates.php:184,292,300,319,337 | Both consumers call `$aliasResolver->resolveAccount($iban, $user->id)`; PairTransferCandidates additionally scopes its own `where('user_id', $user->id)` on every query path (forward + reverse-lookup) |
| T-16.1.2.1-04-03 | Information disclosure | Modules/Transfers/Internal/Listeners/PairTransferCandidates.php:8-11 | Imports limited to `Modules\Core\Public\Contracts\Clock`, `Modules\Import\Public\Contracts\ResolvesKnownCounterpartyIban`, `Modules\Import\Public\Events\TransactionImported`, `Modules\Ledger\Models\Transaction` — `grep "Modules.Import.Internal"` returns ZERO matches; cross-module boundary intact |
| T-16.1.2.1-05-01 | Tampering | Modules/Chains/Internal/Services/CardStatementUpserter.php:69-71 | JOIN filters `statement_summaries.user_id = $user->id` AND `statement_summaries.import_run_id = $importRunId` AND `accounts.kind = 'ics_card'`; cross-user/cross-run/cross-kind promotion structurally impossible |
| T-16.1.2.1-05-02 | Information disclosure | Modules/Chains/Internal/Services/CardStatementUpserter.php:104 + Modules/Chains/Database/Migrations/2026_05_16_010002_create_card_statements_table.php:57 | `card_statements.user_id` written from `$user->id` directly; UNIQUE `(user_id, account_id, period_start, period_end)` constraint at migration ensures no cross-user FK reachability |
| T-16.1.2.1-05-04 | Repudiation | Modules/Chains/Internal/Services/CardStatementUpserter.php:103 + migration:57 | `insertOrIgnore(...)` against UNIQUE `(user_id, account_id, period_start, period_end)`; re-runs preserve the `state` column (locked by unit tests 2 + 3) |
| T-16.1.2.1-05-05 | Elevation of privilege | Modules/Chains/Internal/Services/CardStatementUpserter.php:71 | Strict `->where('accounts.kind', 'ics_card')` filter at the JOIN; ASN/PayPal summaries cannot become card_statements (locked by unit test 4) |
| T-16.1.2.1-05-06 | Information disclosure | Modules/Chains/Internal/Resolvers/IcsSettlementResolver.php:138,157,279,338 | Candidate iteration scopes `where('transactions.user_id', $user->id)`; per-row alias consult passes `$user->id` to `resolveAccount(...)`; all downstream writes carry `'user_id' => $user->id`; Plan 03 contract enforces per-user scoping on the alias side |

## Accepted risks (12)

These threats were dispositioned `accept` at plan time. They remain open by design — the residual risk is bounded, user-visible, and / or has no privilege-escalation surface.

| Threat ID | Category | Accepted because |
|-----------|----------|------------------|
| T-16.1.2.1-01-01 | Tampering | Default-categorization-rule fixture is source-controlled (`Modules/Categorization/Database/Seeders/default-categorization-rules.php`); modifications ship through PR review + Pint/Larastan/Pest gates |
| T-16.1.2.1-01-03 | Denial of service | ~80-row bulk INSERT is bounded by the source-controlled fixture, not user input — no DoS surface |
| T-16.1.2.1-01-05 | Denial of service | `UserInstalled` listener uses `User::query()->findOrFail($event->userId)`; missing user row throws `ModelNotFoundException` and aborts the install loudly — correct failure mode for a Core-module bug |
| T-16.1.2.1-02-02 | Spoofing | A spoofed PayPal `Bankstorting` row with no matching ASN side lands as an unpaired `transfer_in` in the ledger — user-visible, audit-trail intact, no privilege escalation |
| T-16.1.2.1-02-03 | Repudiation | Type-mapping change history is source-controlled in git; PHPDoc describes current state per `feedback_docs_describe_current_state` |
| T-16.1.2.1-03-01 | Tampering | A forged `LU89751000135104200E` row produces a user-visible `transfer_out` to the user's own PayPal — no privilege escalation; data scopes to user's own ledger |
| T-16.1.2.1-03-04 | Denial of service | Alias resolver issues 2 queries per call (alias lookup + Account lookup); ~1000 reads on a 500-row import is trivial at single-user scale |
| T-16.1.2.1-03-07 | Denial of service | Same as T-16.1.2.1-01-05 — `findOrFail` on an unknown userId throws `ModelNotFoundException` and aborts the install loudly |
| T-16.1.2.1-04-01 | Spoofing | Same residual surface as T-16.1.2.1-03-01 — forged counterparty_iban produces user-visible transfer; no privilege escalation |
| T-16.1.2.1-04-04 | Denial of service | Two queries per consult site; bounded by import size; not a DoS surface at single-user scale |
| T-16.1.2.1-04-05 | Repudiation | Alias-fallback decision is observable in the ledger: a `transfer_out` row with `pair_transaction_id` set demonstrates the full path fired; canonical IBAN literals locked by tests |
| T-16.1.2.1-05-03 | Tampering | A forged ASN `transfer_out` with `counterparty_iban='NL08ABNA0526650664'` whose amount matches the statement total within tolerance would create a user-visible `chain_links` row — audit trail intact, no privilege escalation |

## Unregistered flags

None. Each per-plan SUMMARY explicitly attests: "No new security-relevant surface was introduced" / "No threat flags."

## Audit method

- `register_authored_at_plan_time: TRUE` — register treated as complete; no scan for new vulnerabilities.
- Every `mitigate` threat verified by direct grep + line-number cite of the mitigation pattern in the cited file.
- Every `accept` threat logged as documented accepted risk above.
- Cross-module boundary check on `Modules/Transfers/Internal/Listeners/PairTransferCandidates.php` (`grep "Modules.Import.Internal"` returns zero) — the Public-contract dependency-inversion invariant is intact.
- Database-level guards (UNIQUE constraints, BEFORE INSERT/UPDATE triggers, allow-list `RAISE(ABORT)`) confirmed in migration source.

## Conclusion

All 14 declared `mitigate` mitigations are present in the implemented code, in the files cited by their mitigation plans. All 12 `accept` dispositions are documented above as accepted risk. No `transfer` dispositions. No unregistered flags from any SUMMARY.

**Phase status: SECURED. No threats open. block_on=high not triggered.**
