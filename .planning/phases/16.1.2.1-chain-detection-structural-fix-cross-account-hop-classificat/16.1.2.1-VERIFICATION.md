---
phase: 16.1.2.1-chain-detection-structural-fix-cross-account-hop-classificat
verified: 2026-05-27T21:30:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 16.1.2.1: Chain Detection Structural Fix — Verification Report

**Phase Goal:** Make the post-import enrichment pipeline actually produce results — both chain links AND categorized transactions — instead of leaving every imported row uncategorized and unchained. Closes six structural gaps surfaced during Phase 16 UAT.
**Verified:** 2026-05-27T21:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | gap-1-iban-alias-bridge: `ResolvesKnownCounterpartyIban` contract exists, is bound, and `ClassifyTransactionType` step 2 consults it before the literal-IBAN fallback | ✓ VERIFIED | Contract at `Modules/Import/Public/Contracts/ResolvesKnownCounterpartyIban.php`; bound in `ImportServiceProvider` (`bind(ResolvesKnownCounterpartyIban::class, KnownCounterpartyIbanResolver::class)`); `ClassifyTransactionType::run()` lines 105–108 show Arm A alias consult before Arm B literal check |
| 2 | gap-1b-pair-detection-iban-alias: `PairTransferCandidates` consults the alias contract AND handles both import orders via reverse lookup | ✓ VERIFIED | `PairTransferCandidates` constructor injects `ResolvesKnownCounterpartyIban`; forward direction on lines 168–189; `findPartnerByReverseLookup()` method (lines 290–348) handles the PayPal-side no-counterparty-iban case; BL-01 fix commit `62a5bd6` adds "ASN first" test in `PairTransferCandidatesAliasBridgeTest.php` line 154 |
| 3 | gap-2-paypal-funding-leg-mapping: `PaypalCsvEventTypeMap` maps `Bankstorting`/`General Withdrawal`/`Transfer to bank` as `parent` → `transfer_in`; two distinct exception types | ✓ VERIFIED | MAP entries verified at lines 74–76; TRANSACTION_TYPE entries at lines 115–117; `MissingPaypalTransactionTypeMapException` extends `UnknownPaypalEventTypeException` (confirmed); `transactionType()` throws narrow exception for in-MAP-but-missing-from-TRANSACTION_TYPE, broad for completely absent; `ClassifyTransactionType` re-throws the narrow one (line 154) |
| 4 | gap-3-card-statements-upsert-path: `UpsertsCardStatements` contract exists, `CardStatementUpserter` is bound, and `ConfirmImport` calls it unconditionally when `$dispatchChain=true` (Stage A, before the inserted/enriched gate) | ✓ VERIFIED | Contract at `Modules/Chains/Public/Contracts/UpsertsCardStatements.php`; `ChainsServiceProvider` binds it (line 86: `bind(UpsertsCardStatements::class, CardStatementUpserter::class)`); `ConfirmImport` constructor injects `UpsertsCardStatements` (line 83) and calls `upsertForImportRun` at line 216 inside `if ($dispatchChain)` but OUTSIDE the `if ($result->inserted > 0 || $result->enriched > 0)` inner gate — CR-02 fix verified |
| 5 | gap-3b-ics-settlement-chain-end-to-end: `IcsSettlementResolver` iterates ASN-side `transfer_out` via alias bridge (Option B), matches `card_statements` on the resolved ICS account, writes `chain_links` of `kind='ics_bulk_settle'` | ✓ VERIFIED | Candidate query at lines 129–152 uses `where('accounts.kind', 'bank')` and `where('transactions.type', 'transfer_out')`; per-row alias resolve at lines 157–165; no `ics_card` `transfer_in` iteration (grep gate confirmed zero matches); end-to-end test `IcsSettlementResolverAliasBridgeTest.php` line 218 asserts `chain_links.kind='ics_bulk_settle'` from a real ASN transfer_out without any injected ICS-side transfer_in |
| 6 | gap-4-seed-categorization-rules: `DefaultCategorizationRuleSeeder` produces 92 rules (≥80) per fresh user; `≥40%` gate test runs against 200-row live-distribution fixture; personal identifiers stay uncategorized | ✓ VERIFIED | `php -r "var_dump(count(require '...default-categorization-rules.php'));"` → `int(92)`; fixture size 200 rows confirmed; `SeedRulesEndToEndCategorizationTest.php` line 160 asserts `toBeGreaterThanOrEqual(0.4)` with SUMMARY reporting 91.5% actual; `firstOrCreate` only (no `updateOrCreate`); listener uses Option A (`findOrFail($event->userId)`) |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Modules/Import/Public/Contracts/ResolvesKnownCounterpartyIban.php` | Public contract for IBAN alias resolution | ✓ VERIFIED | Exists; interface with `resolveAccount(string $iban, int $userId): ?Account`; correct namespace |
| `Modules/Import/Internal/Services/KnownCounterpartyIbanResolver.php` | Concrete resolver, lowest-id disambiguation | ✓ VERIFIED | Exists; implements contract; `orderBy('id')` at line 72 (BL-02 fix); `withoutGlobalScopes` not needed here — explicit `where('user_id', $userId)` on both queries |
| `Modules/Import/Database/Seeders/DefaultKnownCounterpartyIbansSeeder.php` | Per-user idempotent seeder with two real IBANs | ✓ VERIFIED | Exists; `LU89751000135104200E` and `NL08ABNA0526650664` present; `withoutGlobalScopes()->firstOrCreate` (WR-01 fix at line 64) |
| `Modules/Import/Internal/Listeners/SeedDefaultKnownCounterpartyIbans.php` | UserInstalled listener, Option A | ✓ VERIFIED | Exists; `User::query()->findOrFail($event->userId)` at line 27 |
| `Modules/Import/Database/Migrations/2026_05_27_010001_create_known_counterparty_ibans_table.php` | Migration for known_counterparty_ibans | ✓ VERIFIED | File exists at confirmed path |
| `Modules/Import/Models/KnownCounterpartyIban.php` | Eloquent model with BelongsToUser | ✓ VERIFIED | Exists; uses `BelongsToUser` trait; `real_iban`, `target_account_kind` in fillable |
| `Modules/Ingestion/Public/Paypal/PaypalCsvEventTypeMap.php` | Extended with funding-leg mappings | ✓ VERIFIED | Three funding-leg MAP entries as `parent`; three TRANSACTION_TYPE entries as `transfer_in`; two distinct exception types thrown by `transactionType()` |
| `Modules/Chains/Public/Contracts/UpsertsCardStatements.php` | Public contract for card statement upsert | ✓ VERIFIED | Exists; single-method interface `upsertForImportRun(int $importRunId, User $user): int` |
| `Modules/Chains/Internal/Services/CardStatementUpserter.php` | Concrete upserter service | ✓ VERIFIED | Exists; `insertOrIgnore` used (IN-02 fix — no pre/post COUNT); idempotency via UNIQUE constraint |
| `Modules/Import/Public/Actions/ConfirmImport.php` | Wired with UpsertsCardStatements | ✓ VERIFIED | `UpsertsCardStatements` injected as constructor param; called unconditionally in Stage A; Eloquent `ChainResolutionRun::query()->create()` for WR-05 |
| `Modules/Chains/Internal/Resolvers/IcsSettlementResolver.php` | Option B rewire — ASN transfer_out iteration | ✓ VERIFIED | Candidate query iterates `bank`-kind `transfer_out` rows; alias resolve in per-row loop; `resolveOne()` takes `Account $icsAccount`; no dead `PairLookup` parameter (WR-02 fix); no `$settled` dead param (WR-04 fix); `diffInSeconds` for distance (WR-03 fix) |
| `Modules/Categorization/Database/Seeders/DefaultCategorizationRuleSeeder.php` | Per-user idempotent seeder, 92 rules | ✓ VERIFIED | Exists; `firstOrCreate` only; `withoutGlobalScopes()` used |
| `Modules/Categorization/Database/Seeders/default-categorization-rules.php` | Fixture returning 92 rules | ✓ VERIFIED | `php -r "var_dump(count(require ...))"` → `int(92)` |
| `Modules/Categorization/Internal/Listeners/SeedDefaultCategorizationRules.php` | UserInstalled listener, Option A | ✓ VERIFIED | `findOrFail($event->userId)` at line 27 |
| `Modules/Categorization/tests/Fixtures/seed-rules-live-distribution.php` | 200-row live-distribution fixture | ✓ VERIFIED | `php -r "var_dump(count(require ...))"` → `int(200)` |
| `Modules/Categorization/tests/Feature/SeedRulesEndToEndCategorizationTest.php` | ≥40% gate test | ✓ VERIFIED | Exists; `toBeGreaterThanOrEqual(0.4)` assertion at line 160; fixture-based (not hand-built 10-row table) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `CategorizationServiceProvider::boot()` | `SeedDefaultCategorizationRules` | `events->listen(UserInstalled::class, ...)` | ✓ WIRED | Confirmed at line 82 of provider |
| `ImportServiceProvider::register()` | `ResolvesKnownCounterpartyIban` | `bind(ResolvesKnownCounterpartyIban::class, KnownCounterpartyIbanResolver::class)` | ✓ WIRED | Confirmed at line 134 of provider |
| `ImportServiceProvider::boot()` | `SeedDefaultKnownCounterpartyIbans` | `events->listen(UserInstalled::class, ...)` | ✓ WIRED | Confirmed at line 237 of provider |
| `ClassifyTransactionType` | `ResolvesKnownCounterpartyIban` | constructor DI, Arm A at lines 105–108 | ✓ WIRED | `$aliasResolver` injected; called before literal fallback |
| `PairTransferCandidates` | `ResolvesKnownCounterpartyIban` | constructor DI; forward+reverse lookup | ✓ WIRED | `$aliasResolver` at line 90; used at line 184 (forward) and via `known_counterparty_ibans` table query in `findPartnerByReverseLookup()` |
| `ConfirmImport` (Stage A) | `UpsertsCardStatements` | constructor DI; called unconditionally in `if ($dispatchChain)` | ✓ WIRED | Line 216: `$this->cardStatementUpserter->upsertForImportRun(...)` before the `inserted/enriched` inner gate |
| `IcsSettlementResolver` | `ResolvesKnownCounterpartyIban` | constructor DI; per-row resolve | ✓ WIRED | `$aliasResolver` at line 108; `resolveAccount()` call at line 157 |
| `ChainsServiceProvider::register()` | `UpsertsCardStatements` | `bind(UpsertsCardStatements::class, CardStatementUpserter::class)` | ✓ WIRED | Line 86 of provider |
| `PairTransferCandidates` → `Import` module | Public contract only (not Internal) | import statement | ✓ INTACT | `grep "Modules.Import.Internal"` on `PairTransferCandidates.php` returns zero matches — cross-module boundary respected |
| `IcsSettlementResolver` → `Import` module | Public contract only (not Internal) | import statement | ✓ INTACT | `grep "Modules.Import.Internal"` on `IcsSettlementResolver.php` returns zero matches |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `ClassifyTransactionType::run()` step 2 | `$aliasAccount` | `KnownCounterpartyIbanResolver::resolveAccount()` → `known_counterparty_ibans` table JOIN `accounts` | Yes — DB query returns the real seeded Account | ✓ FLOWING |
| `PairTransferCandidates::handle()` reverse direction | `$candidateIbans` | `known_counterparty_ibans` table `pluck('real_iban')` | Yes — DB query against the seeded alias rows | ✓ FLOWING |
| `IcsSettlementResolver::resolveForUser()` candidate | `$candidateTransfers` | `transactions` JOIN `accounts` WHERE `kind='bank'` AND `type='transfer_out'` | Yes — real transaction rows | ✓ FLOWING |
| `CardStatementUpserter::upsertForImportRun()` | insert payload | `statement_summaries` JOIN `accounts` WHERE `kind='ics_card'` | Yes — real statement rows | ✓ FLOWING |
| `DefaultCategorizationRuleSeeder::run()` | rule rows | `default-categorization-rules.php` fixture (92 entries) + category slug → id lookup | Yes — real DB inserts via `firstOrCreate` | ✓ FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED (tests require a running Laravel app with a migrated DB — static file verification and grep-level checks completed above instead. The REVIEW-FIX.md documents 881 passed / 2 skipped / 3 todos / 0 failed across all touched modules after the 13 fix commits.)

### Probe Execution

Step 7c: No probe scripts declared in any PLAN file and no `scripts/*/tests/probe-*.sh` files found for this phase. Skipped.

### Requirements Coverage

The gap IDs (`gap-1-iban-alias-bridge`, `gap-1b-pair-detection-iban-alias`, `gap-2-paypal-funding-leg-mapping`, `gap-3-card-statements-upsert-path`, `gap-3b-ics-settlement-chain-end-to-end`, `gap-4-seed-categorization-rules`) are phase-internal gap identifiers surfaced from Phase 16 UAT. They are not mapped to v2.0 REQUIREMENTS.md IDs because this phase is a v1 structural fix sub-phase (Phase 16.1.2.1). Each plan's frontmatter `requirements:` field maps one-to-one to the phase's six gaps, and all six are verified as ✓ SATISFIED above.

| Requirement | Source Plan | Description | Status |
|-------------|------------|-------------|--------|
| gap-4-seed-categorization-rules | Plan 01 | Default categorization rule seed | ✓ SATISFIED |
| gap-2-paypal-funding-leg-mapping | Plan 02 | PayPal funding-leg → transfer_in mapping | ✓ SATISFIED |
| gap-1-iban-alias-bridge | Plan 03 | IBAN alias bridge foundation | ✓ SATISFIED |
| gap-1b-pair-detection-iban-alias | Plan 04 | Classifier + pair-listener alias bridge wiring | ✓ SATISFIED |
| gap-3-card-statements-upsert-path | Plan 05 | card_statements per-import upsert | ✓ SATISFIED |
| gap-3b-ics-settlement-chain-end-to-end | Plan 05 | ICS settlement chain end-to-end | ✓ SATISFIED |

### Anti-Patterns Found

Post-fix scan of key production files modified by this phase:

| File | Pattern | Severity | Assessment |
|------|---------|----------|------------|
| `KnownCounterpartyIbanResolver.php` | Three `return null` occurrences | — | All are legitimate guard paths (empty IBAN, no alias row, no matching account); data-flow confirmed real queries precede each guard |
| `IcsSettlementResolver.php` | `return null` in `resolveOne()` half-state | — | Legitimate: documented half-state when no candidate statement found yet |
| All production files | TBD / FIXME / XXX debt markers | — | Zero debt markers found across all key modified files |
| All production files | `.planning` / `PLAN.md` GSD leakage | — | Zero matches in production code |
| `ClassifyTransactionType.php` | `return $tx` (default step 5) | — | Legitimate default return; not a stub |

No blockers or warnings found in anti-pattern scan.

### Review Findings Resolution

The REVIEW.md documented 13 findings (2 BLOCKER, 2 CRITICAL, 6 WARNING, 5 INFO). The REVIEW-FIX.md claims `status: all_fixed`. All 13 fix commits are confirmed in git history:

| Finding | Commit | Verified |
|---------|--------|---------|
| BL-01: Pair-detection import-order dependency | `62a5bd6` | ✓ `findPartnerByReverseLookup()` exists; ASN-first test at line 154 of `PairTransferCandidatesAliasBridgeTest.php` |
| BL-02: Non-deterministic account lookup | `4e19774` | ✓ `orderBy('id')` at line 72 of `KnownCounterpartyIbanResolver.php` |
| CR-01: `MissingPaypalTransactionTypeMapException` swallowed | `0641ab4` | ✓ Narrower exception re-thrown at line 154 of `ClassifyTransactionType.php`; `PaypalCsvEventTypeMap.transactionType()` now distinguishes both miss conditions |
| CR-02: card-statements upsert gated on `inserted/enriched` | `f5230f6` | ✓ Stage A runs unconditionally in `if ($dispatchChain)` before the `inserted/enriched` inner gate |
| WR-01: No `withoutGlobalScopes()` in IBAN seeder | `78a8fa9` | ✓ Line 64: `KnownCounterpartyIban::withoutGlobalScopes()->firstOrCreate(...)` |
| WR-02/03/04: IcsSettlementResolver dead-signature + distance precision | `d45f2da` | ✓ No `PairLookup` param; no `$settled` param on `findCandidateStatement`; `diffInSeconds` used |
| WR-05: Raw insert for `chain_resolution_runs` | `49c8054` | ✓ `ChainResolutionRun::query()->create([...])` at line 231 of `ConfirmImport.php` |
| WR-06: Fixture drift detection | `de3aa8a` | ✓ Personal-identifier prefix regression test added to `SeedRulesEndToEndCategorizationTest.php` (line 235) |
| IN-01: Magic number `>=80` | `a14ad6a` | ✓ `fixtureRuleCount()` helper at line 22 of `DefaultCategorizationRuleSeederTest.php` |
| IN-02: Pre/post COUNT diff | `8737f33` | ✓ `insertOrIgnore()` return value summed directly |
| IN-03: IBAN format validation | `eb55753` | ✓ Documented as deferred (v1 acceptable; only writer is the seeder with compile-time literals) |
| IN-04: Constant fingerprint base | `ccd1030` | ✓ `$rowIndex = 1` default param in `aliasAsnTransferOut()` |
| IN-05: EN forward-compat PHPDoc drift | `7671a54` | ✓ Caveat documented in `PaypalCsvEventTypeMap.php` PHPDoc |

### Human Verification Required

No human verification items identified. All must-haves are verifiable through static code analysis and file inspection.

---

_Verified: 2026-05-27T21:30:00Z_
_Verifier: Claude (gsd-verifier)_
