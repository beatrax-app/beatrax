---
phase: 04-responsive-installable-pwa-seed-008
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/Ledger/Internal/Http/Livewire/TransactionsList.php
  - Modules/Ledger/Resources/views/livewire/transactions-list.blade.php
  - Modules/Ledger/tests/Feature/TransactionsListInfiniteScrollTest.php
autonomous: true
gap_closure: true
requirements: [PWA-01]

must_haves:
  truths:
    - "On phone width, loading more transactions APPENDS rows to the existing card list instead of replacing the visible page"
    - "Sequential loadMore() calls accumulate rows: after two loadMore calls the phone card list contains rows from all three pages"
    - "The wire:intersect sentinel re-keys / re-binds after each morph so it can fire again for the next page"
    - "Desktop table still uses the explicit 'Load more' button and shows only the current cursor page (desktop behavior unchanged)"
    - "TransactionsListCurrencyToggleTest still passes unchanged (EUR amount appears exactly 2x for a single row: one desktop row + one phone card)"
  artifacts:
    - path: "Modules/Ledger/Internal/Http/Livewire/TransactionsList.php"
      provides: "Accumulating phone-row state + loadMore that appends instead of replacing"
      contains: "accumulatedRows"
    - path: "Modules/Ledger/Resources/views/livewire/transactions-list.blade.php"
      provides: "Phone card-list looping accumulated rows; re-keyable sentinel"
    - path: "Modules/Ledger/tests/Feature/TransactionsListInfiniteScrollTest.php"
      provides: "Feature test proving accumulation across sequential loadMore calls"
  key_links:
    - from: "transactions-list.blade.php (phone wrapper)"
      to: "TransactionsList accumulated rows"
      via: "@foreach over the accumulated phone rows, not $page->rows"
      pattern: "accumulated"
    - from: "wire:intersect sentinel"
      to: "loadMore()"
      via: "wire:key re-binds the observer after each append so it can fire for the next cursor"
      pattern: "wire:key"
---

<objective>
Close UAT Gap 1 (major): phone-width "infinite scroll" on `/transactions` currently REPLACES the 50 visible rows with the next 50 instead of accumulating them, and the `wire:intersect` sentinel never fired on scroll during UAT (only a direct `component.call` advanced the cursor). UI-SPEC promised a true accumulating infinite-scroll list on phone.

Make the phone card-list ACCUMULATE rows across `loadMore()` calls while leaving the desktop table on its existing explicit "Load more" button + single-page cursor semantics. Prove accumulation and sentinel re-firing with a feature test.

Purpose: The phone ledger silently swaps pages today — only 50 of 168 rows in the 90-day window are ever reachable. Accumulation is the contracted behavior.
Output: An accumulating phone list, a re-bindable sentinel, and a feature test asserting accumulated row counts after sequential loadMore calls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/beatrax-04-responsive-installable-pwa-seed-008/04-UI-SPEC.md
@.planning/phases/beatrax-04-responsive-installable-pwa-seed-008/04-05-SUMMARY.md

<interfaces>
<!-- Existing contracts the executor must build against. No exploration needed. -->

From Modules/Ledger/Internal/Http/Livewire/TransactionsList.php (current state):
- public bool $fullHistory = false;
- public ?int $cursorId = null;
- public ?string $cursorPostedAt = null;
- #[Url(as: 'currency', except: '')] public string $currency = '';
- mount(CurrentUser $currentUser): void — resolves $currency from default_currency_view
- toggleFullHistory(): void — flips $fullHistory, resets cursorId/cursorPostedAt to null
- loadMore(int $nextCursorId, ?string $nextCursorPostedAt = null): void — currently ONLY sets cursorId/cursorPostedAt
- render(CurrentUser, TransactionListQuery, ViewFactory, DatabaseManager): View
  - $queryCurrency = $this->currency === 'eur' ? 'EUR' : null;
  - calls $listQuery->recent(...) or ->fullHistory(...) returning a TransactionListPage
  - passes ['page', 'fullHistory', 'currency', 'chainTxIds'] to ledger::livewire.transactions-list

From Modules/Ledger/Public/Services/TransactionListQuery.php:
- recent(User, int $daysBack=90, ?int $cursorId=null, int $limit=50, ?string $cursorPostedAt=null, ?string $currency=null): TransactionListPage
- fullHistory(User, ?int $cursorId=null, int $limit=50, ?string $cursorPostedAt=null, ?string $currency=null): TransactionListPage

From Modules/Ledger/Public/Dto/TransactionListPage (the $page object the blade receives):
- ->rows : array<TransactionRowDto>   (current page only, up to $limit)
- ->hasMore : bool
- ->nextCursorId : ?int
- ->nextCursorPostedAt : ?string

From Modules/Ledger/Public/Dto/TransactionRowDto (each row):
- ->id : int
- ->bookedAt : string (d-m-Y)
- ->counterpartyName : ?string
- ->categoryId : ?int
- ->categoryName : ?string
- ->amount : Money
- ->secondaryAmount : ?Money
- ->counterpartySlug : ?string

Test helpers available (from TransactionsListCurrencyToggleTest.php beforeEach):
- $this->seedFixtureUserAndAccount(); $this->fixtureUser; $this->account; $this->run = $this->makeImportRun($this->fixtureUser);
- $this->makeTransaction($user, $account, $run, [...attrs...]) — attrs include amount_minor, currency, settled_amount_minor, settled_currency, posted_at, booked_at, counterparty_name
- Livewire::test(TransactionsList::class)->set(...)->call('loadMore', $id, $postedAt) advances the cursor
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add accumulating phone-row state to TransactionsList and write the failing accumulation test</name>
  <read_first>
    - Modules/Ledger/Internal/Http/Livewire/TransactionsList.php (the component being modified)
    - Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php (test patterns, helper signatures, beforeEach setup to mirror)
    - Modules/Ledger/Public/Services/TransactionListQuery.php (page shape, cursor semantics, 50-row limit)
    - Modules/Ledger/Public/Dto/TransactionListPage.php and Modules/Ledger/Public/Dto/TransactionRowDto.php (DTO field names)
  </read_first>
  <behavior>
    - Test (new file Modules/Ledger/tests/Feature/TransactionsListInfiniteScrollTest.php), group 'phase-4':
      - Seed enough transactions to span at least 3 pages at the 50-row limit (seed >= 120 rows, e.g. 130, each with distinct posted_at/id so the (posted_at,id) cursor is well-ordered). Use $this->makeTransaction in a loop with descending posted_at dates.
      - Mount Livewire::test(TransactionsList::class). Assert the component exposes an accumulated phone-row collection whose count equals 50 on first render (one page).
      - Call loadMore with the page's nextCursorId / nextCursorPostedAt (read them off the component, or replicate the cursor the query would return). Assert accumulated count is now 100.
      - Call loadMore again with the next cursor. Assert accumulated count is now 130 (last partial page) and that hasMore is false.
      - Assert NO duplicate ids appear in the accumulated set (every accumulated row id is unique) — proves append, not re-fetch-from-top.
    - Test: a fresh mount (or toggleFullHistory) RESETS the accumulated rows back to the first page only (count 50), proving the window-switch clears accumulation.
  </behavior>
  <action>
    Add a public array property `$accumulatedRows` on TransactionsList holding the phone-list rows accumulated across loadMore calls (store the already-mapped row data the blade needs: id, bookedAt, counterpartyName, categoryId, formatted-or-Money amount, etc. — choose a serialization-safe shape; Livewire must be able to dehydrate it, so store scalar/array data rather than Money objects if Money is not dehydratable — verify Money's serializability and store toMinor()+currency() pairs plus a precomputed sign/format if needed).

    In render(): after building $page, when $this->cursorId === null (first page / fresh window) RESET $accumulatedRows to exactly the current page's rows; when $this->cursorId !== null (a loadMore advanced the cursor) APPEND the current page's rows to $accumulatedRows. Guard against double-append on re-render: key the append on cursor identity (e.g. track the last-appended nextCursorId, or rebuild $accumulatedRows deterministically from a list of consumed cursors) so a Livewire re-render that does not change the cursor does NOT append the same page twice. The simplest correct approach: maintain $accumulatedRows as the source of truth, and in loadMore() set the new cursor AND let the NEXT render append exactly once by comparing against an `$appendedCursorIds` guard array.

    In toggleFullHistory(): also clear $accumulatedRows = [] (alongside the existing cursorId/cursorPostedAt reset) so switching the history window starts a fresh accumulation.

    Keep loadMore()'s existing signature loadMore(int $nextCursorId, ?string $nextCursorPostedAt = null) — desktop "Load more" calls it too. Do NOT change desktop semantics.

    Pass $accumulatedRows (or a view-ready projection of it) to the view alongside the existing 'page' so the phone branch can loop the accumulated set while the desktop branch keeps looping $page->rows.

    Write Modules/Ledger/tests/Feature/TransactionsListInfiniteScrollTest.php per the behavior block. Run it RED first (state property / accumulation absent), then implement until GREEN.
  </action>
  <files>
    - Modules/Ledger/Internal/Http/Livewire/TransactionsList.php
    - Modules/Ledger/tests/Feature/TransactionsListInfiniteScrollTest.php
  </files>
  <verify>
    <automated>vendor/bin/pest Modules/Ledger/tests/Feature/TransactionsListInfiniteScrollTest.php</automated>
  </verify>
  <acceptance_criteria>
    - File Modules/Ledger/tests/Feature/TransactionsListInfiniteScrollTest.php exists and is in group 'phase-4'.
    - `grep -c 'accumulatedRows' Modules/Ledger/Internal/Http/Livewire/TransactionsList.php` returns >= 2 (property declaration + reset/append usage).
    - The test asserts accumulated counts 50 → 100 → 130 across two sequential loadMore calls and asserts unique ids (no duplicates).
    - The test asserts toggleFullHistory (or a fresh mount) resets the accumulated set to a single page.
    - `vendor/bin/pest Modules/Ledger/tests/Feature/TransactionsListInfiniteScrollTest.php` passes.
  </acceptance_criteria>
  <done>Phone-row accumulation logic exists on the component and is proven by a passing feature test asserting append-not-replace across sequential loadMore calls.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the phone card-list to the accumulated rows and make the wire:intersect sentinel re-bindable</name>
  <read_first>
    - Modules/Ledger/Resources/views/livewire/transactions-list.blade.php (phone wrapper lines ~50-95, desktop wrapper lines ~102-203, sentinel lines ~83-94)
    - Modules/Ledger/Internal/Http/Livewire/TransactionsList.php (the $accumulatedRows shape passed in Task 1)
    - .planning/phases/beatrax-04-responsive-installable-pwa-seed-008/04-05-SUMMARY.md (how the phone/desktop md:hidden split + sentinel were originally built)
    - Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php (the substr_count($body, $eur) === 2 expectation that MUST stay green — one desktop row + one phone card)
  </read_first>
  <action>
    In the phone-only wrapper (the `md:hidden` div): change the `@foreach ($page->rows as $row)` loop to iterate the accumulated rows projection passed from the component (the Task 1 $accumulatedRows). Render the same card markup (counterparty name, category chip, bookedAt, signed amount with the `positive` class) per accumulated row. Preserve the existing `$fmt` / `$isPositive` helpers — if the accumulated shape stores Money pairs, reconstruct Money via Money::ofMinor() in the @php block, OR precompute the formatted string + positive flag in the component and read them directly. Whichever you choose, the rendered phone-card amount for a single first-page row must still be the SAME formatted EUR string the desktop row prints (so the currency-toggle test's substr_count === 2 stays true on a one-row, no-loadMore render).

    Sentinel re-firing fix: the `wire:intersect` sentinel div must be re-keyed after each morph so Livewire 4 re-binds the IntersectionObserver to the new sentinel instance for the next cursor (the UAT symptom was the observer firing once then going dead). Add a `wire:key` to the sentinel that changes with the current cursor (e.g. key on $page->nextCursorId so each new page mounts a fresh sentinel node). Investigate and apply the minimal Livewire-4 directive fix that makes the observer re-arm: re-keying on nextCursorId is the primary fix; if the bundled directive supports threshold/rootMargin modifiers and the observer still does not re-fire after re-key, add a small rootMargin so the sentinel triggers slightly before reaching the viewport bottom. Do NOT use a `.once` modifier on the sentinel (that would explicitly prevent re-firing). Keep the sentinel inside the phone-only wrapper so it never arms at desktop width.

    Leave the desktop table branch (`hidden md:block`) and its explicit "Load more" button entirely unchanged — it keeps looping $page->rows and keeps the single-page cursor semantics.
  </action>
  <files>
    - Modules/Ledger/Resources/views/livewire/transactions-list.blade.php
  </files>
  <verify>
    <automated>vendor/bin/pest Modules/Ledger/tests/Feature/TransactionsListCurrencyToggleTest.php Modules/Ledger/tests/Feature/TransactionsListInfiniteScrollTest.php && vendor/bin/pint --test && vendor/bin/phpstan analyse Modules/Ledger --no-progress</automated>
  </verify>
  <acceptance_criteria>
    - The phone-only `md:hidden` wrapper's @foreach iterates the accumulated rows projection (not `$page->rows`). `grep -n "md:hidden" Modules/Ledger/Resources/views/livewire/transactions-list.blade.php` block no longer loops `$page->rows`.
    - The desktop `hidden md:block` branch still loops `$page->rows` unchanged.
    - The sentinel div carries a `wire:key` bound to the current cursor and has NO `.once` modifier on wire:intersect. `grep -n 'wire:intersect' Modules/Ledger/Resources/views/livewire/transactions-list.blade.php` shows the directive without `.once`.
    - TransactionsListCurrencyToggleTest passes unchanged — specifically the "renders one line for an EUR-native row in original mode" test's `substr_count($body, $eur) === 2` assertion stays green (one desktop row + one phone card on a single-row, no-loadMore render).
    - TransactionsListInfiniteScrollTest still passes.
    - `vendor/bin/pint --test` clean and `vendor/bin/phpstan analyse Modules/Ledger` clean (Larastan level 10).
  </acceptance_criteria>
  <done>The phone list renders accumulated rows and the sentinel re-arms per cursor; desktop behavior and the currency-toggle test are preserved.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pest Modules/Ledger` green (new infinite-scroll test + existing currency-toggle test).
- `vendor/bin/pint --test` clean.
- `vendor/bin/phpstan analyse Modules/Ledger` clean at level 10.
- Manual re-UAT (phase-level) will confirm the sentinel fires on real scroll and rows accumulate at 390px.
</verification>

<success_criteria>
- Sequential loadMore() calls accumulate rows (50 → 100 → 130) with no duplicate ids.
- Fresh mount / toggleFullHistory resets accumulation to a single page.
- Desktop "Load more" button + cursor semantics unchanged.
- TransactionsListCurrencyToggleTest passes without modification (or with a consciously documented update if the phone projection forces it — preserve the 2x EUR count either way).
- Sentinel has no `.once`, is re-keyed per cursor, and lives inside the phone-only wrapper.
</success_criteria>

<output>
Create `.planning/phases/beatrax-04-responsive-installable-pwa-seed-008/04-08-SUMMARY.md` when done.
</output>
