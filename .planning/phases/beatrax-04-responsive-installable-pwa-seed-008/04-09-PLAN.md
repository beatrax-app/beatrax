---
phase: 04-responsive-installable-pwa-seed-008
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/Counterparties/Resources/views/livewire/counterparty-index.blade.php
  - Modules/DevMode/Resources/views/livewire/dev-overview-page.blade.php
autonomous: true
gap_closure: true
requirements: [PWA-01]

must_haves:
  truths:
    - "At <768px the counterparty index shows ONLY the filter-sheet trigger row — the desktop toolbar (second search input, sort control, cards/list view toggle) is hidden"
    - "At >=768px the counterparty index desktop toolbar still renders inline (search · sort · view toggle) — desktop unchanged"
    - "At phone width the Dev Console overview stats tiles stack to a single column so the stats-card header text no longer overlaps"
    - "At >=640px the Dev Console overview stats tiles return to their three-up row — desktop/tablet unchanged"
  artifacts:
    - path: "Modules/Counterparties/Resources/views/livewire/counterparty-index.blade.php"
      provides: "Desktop toolbar hidden at phone width (no inline display override defeating .desktop-only)"
    - path: "Modules/DevMode/Resources/views/livewire/dev-overview-page.blade.php"
      provides: "Stats tiles stack at phone width"
  key_links:
    - from: "counterparty-index desktop toolbar div"
      to: ".desktop-only display:none at <768px"
      via: "remove the inline display:flex so the .desktop-only class rule wins at phone width"
      pattern: "desktop-only"
    - from: "dev-overview console-pane-head grid"
      to: "single column at phone width"
      via: "grid-cols-1 sm:grid-cols-3 instead of fixed grid-cols-3"
      pattern: "grid-cols-1"
---

<objective>
Close two small UAT gaps:

- Gap 2 (minor): the counterparty index renders BOTH the phone filter-sheet trigger AND the desktop toolbar (duplicate search input, sort control, cards/list view toggle) at <768px. The desktop toolbar carries `class="desktop-only"` but ALSO an inline `style="display: flex; ..."` — the inline declaration beats the `.desktop-only { display: none }` class rule, so it never hides at phone width. Fix the override so the desktop toolbar collapses to the filter sheet on phone (its controls are reachable via the sheet).
- Gap 3 (cosmetic, dev-only): the Dev Console overview's stats tiles use a fixed `grid grid-cols-3` that stays three-up at 390px, overlapping the stats-card header text. A cheap CSS-only fix exists (stack to one column at phone width), so it is planned rather than deferred.

Purpose: Phone surfaces show one toolbar, not two; the dev overview header is legible at phone width.
Output: One corrected counterparty toolbar branch + one responsive dev stats grid.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/beatrax-04-responsive-installable-pwa-seed-008/04-UI-SPEC.md
@.planning/phases/beatrax-04-responsive-installable-pwa-seed-008/04-05-SUMMARY.md

<interfaces>
<!-- Existing CSS contracts the executor must respect. No exploration needed. -->

From resources/css/app.css (phone/desktop utilities, do NOT modify these rules):
- .phone-only  { display: block; }   @media (min-width:768px){ .phone-only { display:none !important; } }
- .desktop-only{ display: none;  }   @media (min-width:768px){ .desktop-only{ display:block; } }
  NOTE: .desktop-only at >=768px sets display:block (not flex). An element needing a flex
  toolbar at desktop must achieve flex WITHOUT an inline `display:` that also leaks to phone
  width. Prefer a wrapper pattern or a utility/inner element so the OUTER .desktop-only element
  controls show/hide and an INNER element owns the flex layout.

Counterparty index desktop toolbar (current offending markup, ~line 51):
  <div class="desktop-only" style="display: flex; align-items: center; gap: var(--space-4); flex-wrap: wrap;">
The inline `display: flex` overrides `.desktop-only { display:none }` at <768px → toolbar visible on phone.

Dev overview stats row (current, ~line 22):
  <div class="grid grid-cols-3 gap-4 console-pane-head items-start">  ← fixed 3 columns at all widths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide the counterparty desktop toolbar at phone width</name>
  <read_first>
    - Modules/Counterparties/Resources/views/livewire/counterparty-index.blade.php (lines ~44-81: the phone-only filter trigger and the desktop-only toolbar)
    - resources/css/app.css lines ~2893-2916 (the .phone-only / .desktop-only rules — read to confirm .desktop-only sets display:none at <768px and display:block at >=768px; do NOT edit these rules)
    - Modules/Core/Resources/views/components/filter-sheet-trigger.blade.php (the phone trigger that already renders correctly via .phone-only)
  </read_first>
  <action>
    The root cause: the desktop toolbar div is `<div class="desktop-only" style="display: flex; ...">`. The inline `display: flex` wins over `.desktop-only { display: none }` at <768px, so the toolbar shows on phone.

    Fix without changing desktop appearance: remove the inline `display: flex` from the OUTER `.desktop-only` element so the class controls visibility (none at <768px, block at >=768px). Move the flex layout (align-items: center; gap: var(--space-4); flex-wrap: wrap;) onto an INNER wrapper div that always uses display:flex — the outer .desktop-only element shows/hides, the inner element lays the controls out. The three controls (search field, sort label, cards/list view toggle) stay byte-identical in copy and behavior; only the wrapping changes.

    Result: at <768px only the `.phone-only` filter-sheet-trigger row is visible; at >=768px the desktop toolbar renders inline exactly as before (search · sort · view toggle on one wrapping flex row). Do NOT touch the phone-only filter-trigger block, the filter-chips row, or the grid/list body.
  </action>
  <files>
    - Modules/Counterparties/Resources/views/livewire/counterparty-index.blade.php
  </files>
  <verify>
    <automated>! grep -n 'class="desktop-only".*display:' Modules/Counterparties/Resources/views/livewire/counterparty-index.blade.php && vendor/bin/pint --test Modules/Counterparties</automated>
  </verify>
  <acceptance_criteria>
    - The element carrying `class="desktop-only"` no longer has an inline `display: flex` (or any inline `display:` value) — verify via grep: the `desktop-only` toolbar opening tag contains no `display:` declaration.
    - The flex layout (align-items/gap/flex-wrap) now lives on an inner wrapper, so the desktop toolbar still lays out as a single wrapping flex row at >=768px.
    - The phone-only filter-sheet-trigger block, the filter-chips row, and the grid/list body are unchanged (no diff lines outside the toolbar region).
    - `vendor/bin/pint --test` clean for the file.
  </acceptance_criteria>
  <done>At phone width the counterparty index shows one toolbar (the filter sheet trigger); the desktop toolbar is hidden and unchanged at >=768px.</done>
</task>

<task type="auto">
  <name>Task 2: Stack the Dev Console overview stats tiles at phone width</name>
  <read_first>
    - Modules/DevMode/Resources/views/livewire/dev-overview-page.blade.php (line ~22: the `grid grid-cols-3 ... console-pane-head` row holding the three tiles)
    - Modules/DevMode/Resources/views/layouts/dev-shell.blade.php (the two-pane flex shell: fixed .dev-side aside + flex-1 main — context for why content is cramped at 390px)
  </read_first>
  <action>
    The overview's stats tiles use `grid grid-cols-3` which stays three-up at 390px, overlapping the stats-card header text inside the fixed dev-side two-pane layout.

    Change the tiles grid from `grid grid-cols-3` to `grid grid-cols-1 sm:grid-cols-3` so the three tiles (worker heartbeat, queue counts, last command) stack to a single column below the Tailwind `sm` breakpoint (640px) and return to three columns at >=640px. Keep `gap-4 console-pane-head items-start` intact. This is a CSS-only (utility-class-only) change to one element; no PHP, no component logic, no test churn.

    Do NOT alter the console-pane dark theme lock, the wire:poll, the log-tail list, or the recent-runs/open-alerts grid below (that one is already `grid-cols-1 md:grid-cols-2`).
  </action>
  <files>
    - Modules/DevMode/Resources/views/livewire/dev-overview-page.blade.php
  </files>
  <verify>
    <automated>grep -n 'grid-cols-1 sm:grid-cols-3' Modules/DevMode/Resources/views/livewire/dev-overview-page.blade.php && vendor/bin/pint --test Modules/DevMode</automated>
  </verify>
  <acceptance_criteria>
    - The stats-tiles row reads `grid grid-cols-1 sm:grid-cols-3 gap-4 console-pane-head items-start` (single column below 640px, three columns at >=640px).
    - The console-pane dark inline style, wire:poll.5s, log-tail, and the recent-runs/open-alerts grid are unchanged.
    - `vendor/bin/pint --test` clean for the file.
  </acceptance_criteria>
  <done>The Dev Console overview stats tiles stack to one column at phone width; tablet/desktop keep the three-up row.</done>
</task>

</tasks>

<verification>
- `grep` confirms the counterparty desktop toolbar has no inline `display:` and the dev stats grid is `grid-cols-1 sm:grid-cols-3`.
- `vendor/bin/pint --test` clean for both modules.
- `vendor/bin/phpstan analyse Modules/Counterparties Modules/DevMode --no-progress` stays clean (no PHP changes, but run to confirm no Blade-adjacent regression).
- Phase-level re-UAT confirms a single toolbar at 390px and legible dev-overview header.
</verification>

<success_criteria>
- Counterparty index: one toolbar at <768px (filter sheet trigger), full toolbar at >=768px.
- Dev overview: stats tiles stack at phone width, three-up at >=640px.
- No desktop regressions; no test churn.
</success_criteria>

<output>
Create `.planning/phases/beatrax-04-responsive-installable-pwa-seed-008/04-09-SUMMARY.md` when done.
</output>
