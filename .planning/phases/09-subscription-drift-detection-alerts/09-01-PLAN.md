---
phase: 09-subscription-drift-detection-alerts
plan: 01
plan_id: "09-01"
type: execute
wave: 0
depends_on: []
files_modified:
  - composer.json
  - phpunit.xml
  - tests/Pest.php
  - tests/Contracts/BoundaryArchTest.php
  - Modules/DriftAlerts/composer.json
  - Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php
  - Modules/DriftAlerts/Routes/web.php
  - Modules/DriftAlerts/tests/Pest.php
  - Modules/DriftAlerts/tests/TestCase.php
  - Modules/DriftAlerts/Database/Factories/DriftAlertFactory.php
  - Modules/DriftAlerts/Database/Factories/DriftAlertTransitionFactory.php
  - Modules/DriftAlerts/tests/Unit/FixtureCorpusTest.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/stable-monthly.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/small-drift-below-threshold.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/large-drift-above-threshold.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/income-raise.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/income-raise-large.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/income-cut.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/fx-only-swing.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/cadence-changed.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/multi-drift.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/per-series-override.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/prior-null.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/prior-zero.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/volatile-series.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/volatile-with-override.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/weekly-cadence.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/quarterly-cadence.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/yearly-cadence.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/mixed-currency-stable-usd.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/mixed-currency-real-usd-drift.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/pending-state-ignored.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/rejected-state-ignored.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/snoozed-at-series-level-ignored.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/irregular-cadence-ignored.php
  - Modules/DriftAlerts/tests/fixtures/drift-corpus/snooze-expiry-revival.php
autonomous: true
requirements:
  - REC-06
must_haves:
  truths:
    - "Modules/DriftAlerts module skeleton (composer.json + ServiceProvider + Routes/web.php) is loadable; `composer dump-autoload` succeeds and the new PSR-4 namespace `Modules\\DriftAlerts\\` resolves (D-901)"
    - "The five BoundaryArchTest invariants for DriftAlerts (noFacadeCallsFromDriftAlerts, noRecurringSeriesWritesFromDriftAlerts, crossModuleAccessGoesThroughPublic, noSynchronousDriftDetectionInRequestLifecycle, noOtherDriftAlertStateMutator) are present in tests/Contracts/BoundaryArchTest.php and stay green against the empty Phase 9 module (D-902)"
    - "DriftAlerts module honours the Public/Internal split from day one: Queries + DTOs + Events + Actions live under Modules/DriftAlerts/Public; detector + state-machine + Livewire + listeners + jobs live under Modules/DriftAlerts/Internal (D-903)"
    - "All 24 fixture corpus files under Modules/DriftAlerts/tests/fixtures/drift-corpus/ exist and return a shape `['transactions' => array, 'expected' => ['alerts' => array]]`"
    - "DriftAlertFactory + DriftAlertTransitionFactory produce valid model rows that satisfy the future drift_alerts schema column set"
  artifacts:
    - path: Modules/DriftAlerts/composer.json
      provides: PSR-4 namespace registration for Modules\\DriftAlerts\\ + Modules\\DriftAlerts\\Tests\\
      contains: '"Modules\\\\DriftAlerts\\\\": ""'
    - path: Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php
      provides: ServiceProvider with singleton bindings + loadMigrationsFrom + loadRoutesFrom + loadViewsFrom + Livewire component registration + listener registration + top-nav badge composer
      exports: ["DriftAlertsServiceProvider::register", "DriftAlertsServiceProvider::boot"]
    - path: Modules/DriftAlerts/Routes/web.php
      provides: GET /drift route placeholder mapped to DriftPage Livewire SFC under web + auth middleware
      contains: "Route::middleware(['web', 'auth'])"
    - path: tests/Contracts/BoundaryArchTest.php
      provides: 5 new arch test invariants for DriftAlerts module + facade carve-out for DetectDriftAlertsJob FQN
      contains: "noRecurringSeriesWritesFromDriftAlerts"
    - path: Modules/DriftAlerts/tests/fixtures/drift-corpus/
      provides: 24-scenario fixture corpus directory
      min_lines: 24
    - path: Modules/DriftAlerts/tests/Unit/FixtureCorpusTest.php
      provides: Corpus loader test asserting all 24 fixtures parse and satisfy the shape contract
  key_links:
    - from: Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php
      to: Modules/Recurring/Public/Events/RecurringSeriesMetricsRefreshed::class
      via: "$events->listen(RecurringSeriesMetricsRefreshed::class, EvaluateDriftOnMetricsRefreshed::class)"
      pattern: "RecurringSeriesMetricsRefreshed::class.*EvaluateDriftOnMetricsRefreshed::class"
    - from: Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php
      to: core::livewire.top-nav view
      via: "$factory->composer('core::livewire.top-nav', ...) injecting driftOpenCount"
      pattern: "composer\\('core::livewire\\.top-nav'"
    - from: composer.json (project root)
      to: Modules\\DriftAlerts\\ PSR-4 namespace
      via: autoload entry
      pattern: "Modules\\\\\\\\DriftAlerts\\\\\\\\"
---

<objective>
Wave 0 enablement. Stand up the bounded `Modules/DriftAlerts/` module skeleton — composer manifest, ServiceProvider, Routes file, test scaffolding — and lock in five BoundaryArchTest invariants that will police every Phase 9 file written in Waves 1-4. Land the 24-scenario synthesised drift fixture corpus + model factories so subsequent waves have deterministic test data without per-test boilerplate.

Purpose: Phase 9 introduces zero new domain primitives — every concept (state machine, sole-mutator arch test, View Factory composer, queued job, cross-user 404 guard, Public/Internal split) already exists in Phases 5-8. The phase is mechanical: clone the patterns, write the math. This wave reserves the namespace, registers the testsuite (3-step pattern: composer.json autoload-dev + phpunit.xml testsuite + tests/Pest.php map row), and locks the four-plus-one BoundaryArchTest invariants so Waves 1-4 cannot drift away from the Recurring/Chains precedents.

Output: A green test suite with the new module discovered, the new arch tests passing against the empty module, and the fixture corpus + factories ready for Wave 1's schema + Wave 2's detection contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-subscription-drift-detection-alerts/09-CONTEXT.md
@.planning/phases/09-subscription-drift-detection-alerts/09-RESEARCH.md
@.planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md
@.planning/phases/09-subscription-drift-detection-alerts/09-VALIDATION.md

# Analog source files the executor MUST read before writing (these are the locked patterns):
@Modules/Recurring/composer.json
@Modules/Recurring/Providers/RecurringServiceProvider.php
@Modules/Recurring/Routes/web.php
@Modules/Recurring/tests/Pest.php
@Modules/Recurring/tests/TestCase.php
@tests/Contracts/BoundaryArchTest.php
@Modules/Recurring/tests/fixtures/synthesised/drifting-monthly-spotify.php
@Modules/Recurring/tests/fixtures/synthesised/stable-monthly-spotify.php
@Modules/Recurring/Database/Factories/RecurringSeriesFactory.php

<interfaces>
<!-- Key contracts the executor needs. Phase 9 ServiceProvider must wire the listener via the FQN below. -->

From Modules/Recurring/Public/Events/RecurringSeriesCadenceFlipped.php (analog shape for the NEW
Phase 9 event Modules/Recurring/Public/Events/RecurringSeriesMetricsRefreshed.php that lives in
Plan 09-03; the listener wired in this plan's ServiceProvider references the FQN by string before
the class file exists — Wave 2 lands the class itself):

```
namespace Modules\Recurring\Public\Events;
final readonly class RecurringSeriesCadenceFlipped {
    public function __construct(
        public int $userId,
        public int $recurringSeriesId,
        public string $newCadence,
    ) {}
}
```

From Modules/Core/Public/Contracts/CurrentUser.php (DI-only contract every Phase 9 service uses):

```
namespace Modules\Core\Public\Contracts;
interface CurrentUser {
    public function isAuthenticated(): bool;
    public function user(): \Modules\Core\Models\User;
}
```

From tests/Contracts/BoundaryArchTest.php (the existing facade-carve-out shape Plan 09-01 extends — read
lines 48-87 for the `ignoring(...)` chain currently exempting Modules\\Recurring\\Internal\\Jobs\\DetectRecurringSeriesJob;
this plan adds Modules\\DriftAlerts\\Internal\\Jobs\\DetectDriftAlertsJob to the same list).

```
arch('Modules code does not use facades except documented carve-outs')
    ->expect('Modules')
    ->not->toUseFacades()
    ->ignoring([
        'Modules\\Recurring\\Internal\\Jobs\\DetectRecurringSeriesJob',
        // NEW (Plan 09-01): add the DriftAlerts job carve-out
    ]);
```
</interfaces>
</context>

<tasks>

<task type="auto" id="9-01-01">
  <name>Task 1: Module skeleton + 3-step test discovery wire-up + ServiceProvider DI graph</name>
  <read_first>
    - Modules/Recurring/composer.json (entire file — analog shape for the new Modules/DriftAlerts/composer.json)
    - Modules/Recurring/Providers/RecurringServiceProvider.php (entire file — analog ServiceProvider; mirror imports, singleton bindings in register(), and the boot() shape that calls loadMigrationsFrom + loadRoutesFrom + loadViewsFrom + Livewire component registration + private registerTopNavBadgeComposer())
    - Modules/Recurring/Routes/web.php (entire file — analog route file shape)
    - Modules/Recurring/tests/Pest.php + Modules/Recurring/tests/TestCase.php (per-module test bootstrap — Pest.php is documented inert; TestCase extends Tests\TestCase)
    - tests/Pest.php (project root — find the per-module wire-up `foreach` loop documented in Phase 04 Plan 03 STATE notes; the new row maps `Modules/DriftAlerts` to `Modules\DriftAlerts\Tests\TestCase::class`)
    - composer.json (project root) — find the existing Modules\\Recurring\\ and Modules\\Recurring\\Tests\\ autoload + autoload-dev entries; Phase 9 adds the same shape for DriftAlerts
    - phpunit.xml (project root) — find the existing `<testsuite>` entry pointing at `Modules/Recurring/tests/`; add a sibling `<testsuite name="DriftAlerts">` row pointing at `Modules/DriftAlerts/tests/`
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md (sections: "composer.json", "DriftAlertsServiceProvider.php", "Routes/web.php", "tests/Pest.php", "tests/TestCase.php", "Top-nav badge")
  </read_first>
  <files>
    - Modules/DriftAlerts/composer.json (NEW)
    - Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php (NEW)
    - Modules/DriftAlerts/Routes/web.php (NEW)
    - Modules/DriftAlerts/tests/Pest.php (NEW; documented inert)
    - Modules/DriftAlerts/tests/TestCase.php (NEW)
    - composer.json (MODIFY — add `"Modules\\DriftAlerts\\": "Modules/DriftAlerts/"` to `autoload.psr-4` and `"Modules\\DriftAlerts\\Tests\\": "Modules/DriftAlerts/tests/"` to `autoload-dev.psr-4`)
    - phpunit.xml (MODIFY — add `<testsuite name="DriftAlerts"><directory>Modules/DriftAlerts/tests</directory></testsuite>` entry)
    - tests/Pest.php (MODIFY — add `'Modules/DriftAlerts' => Modules\DriftAlerts\Tests\TestCase::class` row to the per-module map)
  </files>
  <action>
    Create the `Modules/DriftAlerts/` bounded module skeleton. Specifics:

    **Modules/DriftAlerts/composer.json**: PSR-4 manifest with `"name": "diederik/drift-alerts"`, `"type": "laravel-module"`, `"license": "proprietary"`, `autoload.psr-4` = `{"Modules\\DriftAlerts\\": ""}`, `autoload-dev.psr-4` = `{"Modules\\DriftAlerts\\Tests\\": "tests/"}`. Verbatim shape from `Modules/Recurring/composer.json` lines 1-16; only the name + description change. Description: "Drift Alerts module — subscription drift detection, /drift page, cancellation-impact query."

    **Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php**: `namespace Modules\DriftAlerts\Providers;` `final class DriftAlertsServiceProvider extends \Illuminate\Support\ServiceProvider`. Mirror `Modules/Recurring/Providers/RecurringServiceProvider.php` imports + structure verbatim with these adjustments:
    - `register()` method: bind as singleton the FQNs that will be implemented across Waves 1-4 — `DriftAlertStateMachine::class`, `DriftEvaluator::class`, `DetectDriftAlertsJob::class`, `DriftAlertQuery::class`, `CancellationImpactQuery::class`, `AcknowledgeDriftAlert::class`, `SnoozeDriftAlert::class`, `DismissDriftAlertAsCancelled::class`, `DashboardDriftBadge::class`. Use the `Modules\DriftAlerts\Internal\*` and `Modules\DriftAlerts\Public\*` FQN forms. Since these classes do not exist yet in Wave 0, this plan provides the wiring; Waves 1-4 land the implementations. The wiring is allowed to reference not-yet-existing FQNs as imports — Laravel's container only resolves them at runtime. Larastan will flag missing imports — see acceptance criteria below for the temporary skip approach.
    - `boot(LivewireManager $livewire)` method: call `$this->loadMigrationsFrom(__DIR__.'/../Database/Migrations')` (wrap in `is_dir()` check — directory does not exist in Wave 0; created in Wave 1), `$this->loadRoutesFrom(__DIR__.'/../Routes/web.php')` (wrap in `is_file()` check), `$this->loadViewsFrom(__DIR__.'/../Resources/views', 'drift-alerts')` (wrap in `is_dir()` check), `$livewire->component('drift-alerts.drift-page', DriftPage::class)`, `$livewire->component('drift-alerts.dashboard-drift-badge', DashboardDriftBadge::class)`, call `$this->registerListener()` and `$this->registerTopNavBadgeComposer()`.
    - Private `registerListener()` method: `$events = $this->app['events']; $events->listen(RecurringSeriesMetricsRefreshed::class, EvaluateDriftOnMetricsRefreshed::class);` — both FQNs reference Wave 2-3 deliveries; the listener registration is the Wave 0 wiring. Mirror `Modules/Recurring/Providers/RecurringServiceProvider.php` listener-registration shape.
    - Private `registerTopNavBadgeComposer()` method: verbatim mirror of `Modules/Recurring/Providers/RecurringServiceProvider.php` lines 111-126 — `$app = $this->app; $factory = $app->make(ViewFactoryContract::class);` then `$factory->composer('core::livewire.top-nav', static function (View $compose) use ($app): void { ... })`. Inside the closure: `$currentUser = $app->make(CurrentUser::class); if (! $currentUser->isAuthenticated()) { $compose->with('driftOpenCount', 0); return; } $query = $app->make(DriftAlertQuery::class); $compose->with('driftOpenCount', $query->openCountForUser($currentUser->user()));`. The composer key is `driftOpenCount` (NEW); the existing `recurringPendingCount` composer in `RecurringServiceProvider` is unchanged.

    **Modules/DriftAlerts/Routes/web.php**: `declare(strict_types=1);` + PHPDoc describing the module's `/drift` surface (no D-numbers, no REQ-IDs — describe what the route does in plain technical language per the GSD-agnostic invariant). Imports: `Illuminate\Support\Facades\Route` and `Modules\DriftAlerts\Internal\Http\Livewire\DriftPage` (the DriftPage class is delivered by Plan 09-04 — the route file references the FQN; runtime resolution happens at request time after Plan 09-04 lands). Single route: `Route::middleware(['web', 'auth'])->group(static function (): void { Route::get('/drift', DriftPage::class)->name('drift.index'); });`.

    **Modules/DriftAlerts/tests/TestCase.php**: `namespace Modules\DriftAlerts\Tests; final class TestCase extends \Tests\TestCase {}`. No body needed — mirrors `Modules/Recurring/tests/TestCase.php` shape.

    **Modules/DriftAlerts/tests/Pest.php**: Single inert file with a comment block noting it is intentionally empty — actual per-module Pest configuration is centralised in the project-root `tests/Pest.php` per the Phase 04 Plan 03 STATE note ("Per-module Pest.php is documented inert"). Mirror `Modules/Recurring/tests/Pest.php` verbatim.

    **composer.json (project root)**: Append `"Modules\\DriftAlerts\\": "Modules/DriftAlerts/"` to `autoload.psr-4`, and `"Modules\\DriftAlerts\\Tests\\": "Modules/DriftAlerts/tests/"` to `autoload-dev.psr-4`. Mirror the existing Recurring entries.

    **phpunit.xml (project root)**: Add a new `<testsuite name="DriftAlerts">` element containing `<directory>Modules/DriftAlerts/tests</directory>`. Mirror the existing Recurring testsuite element verbatim.

    **tests/Pest.php (project root)**: Find the per-module map (a `match` or array literal mapping module-path to TestCase FQN). Add the row `'Modules/DriftAlerts' => Modules\DriftAlerts\Tests\TestCase::class`. Position alphabetically after `Modules/Chains` (or wherever the existing alphabetical posture places it).

    After every file is created, run `composer dump-autoload` so the new namespace resolves before subsequent tasks run.
  </action>
  <verify>
    <automated>composer dump-autoload && vendor/bin/pest --filter="DriftAlerts" 2>&1 | head -50 ; vendor/bin/phpstan analyse Modules/DriftAlerts --memory-limit=2G --no-progress 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `composer dump-autoload` succeeds with zero warnings.
    - `php -r "require 'vendor/autoload.php'; var_dump(class_exists('Modules\\DriftAlerts\\Providers\\DriftAlertsServiceProvider'));"` prints `bool(true)`.
    - `php -r "require 'vendor/autoload.php'; var_dump(class_exists('Modules\\DriftAlerts\\Tests\\TestCase'));"` prints `bool(true)`.
    - `vendor/bin/pest --filter="DriftAlerts" --list-tests` exits 0 (testsuite discovered even if zero tests yet).
    - `grep -v '^[[:space:]]*//' Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php | grep -c "core::livewire.top-nav"` equals 1 (top-nav composer registered exactly once).
    - `grep -v '^[[:space:]]*//' Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php | grep -c "RecurringSeriesMetricsRefreshed::class"` equals 1 (listener wired exactly once).
    - `grep -E "Modules\\\\\\\\DriftAlerts\\\\\\\\:" composer.json` finds the autoload entry.
    - `grep -E "Modules/DriftAlerts/tests" phpunit.xml` finds the testsuite entry.
    - `grep "Modules/DriftAlerts" tests/Pest.php` finds the per-module map row.
    - PHPStan is allowed to flag missing-class errors for `DriftPage`, `DashboardDriftBadge`, `DriftEvaluator`, `DriftAlertStateMachine`, `DriftAlertQuery`, `CancellationImpactQuery`, `AcknowledgeDriftAlert`, `SnoozeDriftAlert`, `DismissDriftAlertAsCancelled`, `DetectDriftAlertsJob`, `EvaluateDriftOnMetricsRefreshed`, `RecurringSeriesMetricsRefreshed` (all delivered by Waves 1-3). The acceptance criterion is that the ServiceProvider itself parses and that the missing-class errors named above are the ONLY PHPStan errors against `Modules/DriftAlerts/`. No syntax errors, no namespace errors, no helper/facade-rule violations.
  </acceptance_criteria>
  <done>
    Module skeleton is discoverable on the autoloader, ServiceProvider parses without syntax errors, project-root composer.json + phpunit.xml + tests/Pest.php all reference the new module, and the only PHPStan errors are the not-yet-implemented FQNs that Waves 1-3 land. Pest discovers the new testsuite (zero tests, exit 0).
  </done>
</task>

<task type="auto" id="9-01-02">
  <name>Task 2: Five BoundaryArchTest invariants + facade carve-out for DetectDriftAlertsJob</name>
  <read_first>
    - tests/Contracts/BoundaryArchTest.php (entire file — read lines 1-50 for imports + the namespace-only-used-in arch tests for existing modules; lines 48-87 for the facade ignoring list; lines 109-114 for the SeriesDetector synchronous-detection invariant; lines 522-569 for the noTransactionWritesFromRecurring filesystem-walk pattern; lines 571-627 for noOtherRecurringSeriesStateMutator)
    - Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php (lines 1-100 — the existing facade-carve-out FQN that Phase 9 mirrors)
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md ("BoundaryArchTest invariants (4 new, possibly 5)" section)
    - .planning/phases/09-subscription-drift-detection-alerts/09-RESEARCH.md ("D-902" section in Locked Decisions + "Anti-Patterns to Avoid" section)
  </read_first>
  <files>
    - tests/Contracts/BoundaryArchTest.php (MODIFY — append 5 new arch tests + extend facade ignore list)
  </files>
  <action>
    Extend `tests/Contracts/BoundaryArchTest.php` with the following five invariants. All five must be appended at the BOTTOM of the file, after the existing Phase 5-8 invariants. Each invariant mirrors the closest analog pattern named below. Skip-list for filesystem-walk variants: skip files under `tests/`, skip `Database/Migrations/`, skip the state-machine file itself, skip block + line comments (strip `/* */` and `//` via the same regex the existing analog uses at lines 538-545).

    **Invariant A — Extend the existing `arch('Modules code does not use facades ...')` rule (lines 48-87)** by appending the FQN `Modules\\DriftAlerts\\Internal\\Jobs\\DetectDriftAlertsJob` to the `ignoring([...])` list. Mirror the existing comment block (analog lines 79-86) so a new comment line above the new entry reads: "// Cache::driver('redis') facade carve-out: ShouldBeUniqueUntilProcessing requires the lock store at queue-push time, before constructor DI completes." The Wave 0 module has no `DetectDriftAlertsJob.php` yet — the ignore-list entry references the future FQN; that is acceptable (the arch test only scans existing files).

    **Invariant B — `Modules\\DriftAlerts\\Internal is only used inside Modules\\DriftAlerts`** (namespace arch test mirroring lines 44-46 of the analog):
    ```
    arch('Modules\\DriftAlerts\\Internal is only used inside Modules\\DriftAlerts')
        ->expect('Modules\\DriftAlerts\\Internal')
        ->toOnlyBeUsedIn('Modules\\DriftAlerts');
    ```

    **Invariant C — `noRecurringSeriesWritesFromDriftAlerts`** (filesystem-walk; mirror analog lines 522-569 verbatim, swap `Modules/Recurring` → `Modules/DriftAlerts` for the scan-dir, and search the regex for forbidden writes against the `recurring_series` table: pattern A `RecurringSeries::query|RecurringSeries::where|RecurringSeries::create|RecurringSeries::firstOrCreate|RecurringSeries::updateOrCreate`; pattern B `->table\(['\"]recurring_series['\"]\)->(update|insert|delete|truncate)`; pattern C `update\(['\"]recurring_series['\"]`). The single permitted READ exception lives in `DriftEvaluator::effectiveThresholdPercent` (Plan 09-03) — reads of `drift_threshold_percent` via raw query builder are intentionally permitted; this invariant only fires on WRITE verbs (`update|insert|delete`). The walk skip list excludes `Modules/DriftAlerts/tests/`, comments, and the migration directory).

    **Invariant D — `crossModuleAccessGoesThroughPublic`** (namespace arch test; mirror analog `->not->toBeUsedIn(...)` shape from lines 16-22):
    ```
    arch('Modules\\Recurring\\Internal is never imported from Modules\\DriftAlerts (crossModuleAccessGoesThroughPublic)')
        ->expect('Modules\\Recurring\\Internal')
        ->not->toBeUsedIn('Modules\\DriftAlerts');
    ```

    **Invariant E — `noSynchronousDriftDetectionInRequestLifecycle`** (namespace arch test; mirror analog lines 109-114):
    ```
    arch('DriftEvaluator is never imported by Modules\\DriftAlerts\\Internal\\Http (noSynchronousDriftDetectionInRequestLifecycle)')
        ->expect('Modules\\DriftAlerts\\Internal\\DriftEvaluator')
        ->not->toBeUsedIn([
            'Modules\\DriftAlerts\\Internal\\Http',
            'Modules\\DriftAlerts\\Resources',
        ]);
    ```

    **Invariant F (5th — RESEARCH-recommended) — `noOtherDriftAlertStateMutator`** (filesystem-walk; mirror analog lines 571-627 verbatim, swap `recurring_series` → `drift_alerts` for the table-name regex, swap the allowed-file path to `Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php`). Scan pattern: `->update\(\[.*['\"]state['\"]\s*=>` AND `->table\(['\"]drift_alerts['\"]\)` near the same file location. Skip the allowed file. Skip `tests/`, `Database/Migrations/`, and comments.

    All five invariants must be added with `it('does not allow ...')` or `arch(...)` based on whether they are filesystem-walks (Invariants C, F use `it()`) or namespace assertions (B, D, E use `arch()`). Invariant A is a modification of an existing `arch()` block.

    Before committing, run the BoundaryArchTest against the empty Wave 0 module — all five new invariants MUST pass (no files exist yet under `Modules/DriftAlerts/` that would trigger them; the empty module is the trivially-correct baseline). Also verify Phase 5-8 invariants stay green.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/BoundaryArchTest.php 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` exits 0; no failures.
    - `grep -c "noRecurringSeriesWritesFromDriftAlerts" tests/Contracts/BoundaryArchTest.php` >= 1.
    - `grep -c "crossModuleAccessGoesThroughPublic" tests/Contracts/BoundaryArchTest.php` >= 1.
    - `grep -c "noSynchronousDriftDetectionInRequestLifecycle" tests/Contracts/BoundaryArchTest.php` >= 1.
    - `grep -c "noOtherDriftAlertStateMutator" tests/Contracts/BoundaryArchTest.php` >= 1.
    - `grep -c "Modules\\\\\\\\DriftAlerts\\\\\\\\Internal\\\\\\\\Jobs\\\\\\\\DetectDriftAlertsJob" tests/Contracts/BoundaryArchTest.php` >= 1 (facade carve-out present).
    - `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter=DriftAlerts` runs at least 4 cases (B, D, E as arch tests + C, F as it()-blocks).
    - All Phase 5-8 BoundaryArchTest invariants stay green (no regression). Verified by running the full file.
    - Synthetic violation check: drop a sentinel file `Modules/DriftAlerts/__synthetic_violation.php` with content `<?php namespace Modules\\DriftAlerts; \\Illuminate\\Support\\Facades\\DB::table('recurring_series')->update(['state' => 'approved']);` then run `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter=noRecurringSeriesWritesFromDriftAlerts` — the test MUST fail. Delete the sentinel file before committing.
  </acceptance_criteria>
  <done>
    All five new invariants exist in BoundaryArchTest.php, the facade carve-out is registered, the full BoundaryArchTest suite is green, and a synthetic violation under `Modules/DriftAlerts/` correctly fires `noRecurringSeriesWritesFromDriftAlerts`. Sentinel file deleted; commit is clean.
  </done>
</task>

<task type="auto" id="9-01-03">
  <name>Task 3: 24-scenario fixture corpus + corpus loader test + DriftAlertFactory pair</name>
  <read_first>
    - Modules/Recurring/tests/fixtures/synthesised/drifting-monthly-spotify.php (entire file — the analog shape for a single fixture: returns `['transactions' => $transactions, 'expected' => [...]]`)
    - Modules/Recurring/tests/fixtures/synthesised/stable-monthly-spotify.php (entire file — zero-drift fixture shape)
    - Modules/Recurring/tests/fixtures/synthesised/mixed-currency-netflix-usd.php (entire file — multi-currency fixture shape)
    - Modules/Recurring/Database/Factories/RecurringSeriesFactory.php (analog factory pattern — drift factory mirrors this)
    - .planning/phases/09-subscription-drift-detection-alerts/09-VALIDATION.md (Wave 0 Requirements section — the 24-scenario list)
    - .planning/phases/09-subscription-drift-detection-alerts/09-RESEARCH.md ("Wave 0 Fixture Corpus" table)
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md ("tests/fixtures/drift-corpus/*.php" section)
  </read_first>
  <files>
    - Modules/DriftAlerts/Database/Factories/DriftAlertFactory.php (NEW)
    - Modules/DriftAlerts/Database/Factories/DriftAlertTransitionFactory.php (NEW)
    - Modules/DriftAlerts/tests/Unit/FixtureCorpusTest.php (NEW)
    - Modules/DriftAlerts/tests/fixtures/drift-corpus/*.php (NEW — 24 fixture files; one file per scenario listed below)
  </files>
  <action>
    Create the 24-scenario fixture corpus, the factories, and the corpus loader test.

    **Fixture file shape** (verbatim mirror of `Modules/Recurring/tests/fixtures/synthesised/drifting-monthly-spotify.php`): each file is a `<?php ... return [...];` that returns an associative array with two top-level keys: `'transactions'` (a list of synthetic transaction rows the Phase 8 detector would cluster into a recurring series), and `'expected'` (a list of expected drift alerts each shaped `{state, direction, baseline_amount_minor, latest_amount_minor, delta_minor, annualized_impact_minor, threshold_percent_used, threshold_source, currency}`). The keys MUST match the eventual `drift_alerts` table column names exactly so Wave 2's contract test can assert via `assertDatabaseHas` directly.

    The 24 scenarios (one file each; filename matches the kebab-case scenario name):

    1. `stable-monthly.php` — Netflix €9.99 × 6 monthly occurrences. `expected.alerts = []`.
    2. `small-drift-below-threshold.php` — Spotify €9.99 → €10.20 (+2.1%) monthly, threshold ±5%. `expected.alerts = []`.
    3. `large-drift-above-threshold.php` — Spotify €9.99 → €11.49 (+15%) monthly, threshold ±5%. `expected.alerts` = 1 row: `state='open', direction='expense', baseline_amount_minor=-999, latest_amount_minor=-1149, delta_minor=-150, annualized_impact_minor=-1800, threshold_percent_used=5, threshold_source='global', currency='EUR'`.
    4. `income-raise.php` — Salary €3500 → €3650 (+4.3%) monthly, threshold ±5%. `expected.alerts = []`.
    5. `income-raise-large.php` — Salary €3500 → €3850 (+10%) monthly. `expected.alerts` = 1 row, direction=income, +€4200/yr annualized.
    6. `income-cut.php` — Salary €3500 → €3325 (-5.0%) monthly. `expected.alerts` = 1 row, direction=income, signed -€2100/yr annualized.
    7. `fx-only-swing.php` — Netflix $11.99 stable × 6 monthly; EUR-settled drifts from €11.20 to €10.80 due to FX. `expected.alerts = []` (D-909 carry-forward).
    8. `cadence-changed.php` — Subscription on `cadence_changed` state with +10% drift. `expected.alerts` = 1 row.
    9. `multi-drift.php` — Spotify €9.99 → €10.99 → €11.99 over 3 sweeps. `expected.alerts` = 2 rows (D-904 queue-all-as-open).
    10. `per-series-override.php` — Electricity €120 → €150 (+25%) with `recurring_series.drift_threshold_percent = 50`. `expected.alerts = []`.
    11. `prior-null.php` — First detected occurrence; no prior. `expected.alerts = []` (Pitfall 4 guard).
    12. `prior-zero.php` — Prior occurrence amount is 0 cents. `expected.alerts = []` (Pitfall 4 guard).
    13. `volatile-series.php` — Variable monthly bill with ±30% natural variance, threshold ±5%. `expected.alerts` length >= 3 (documents alert noise UX problem).
    14. `volatile-with-override.php` — Same as 13 but `recurring_series.drift_threshold_percent = 50`. `expected.alerts = []`.
    15. `weekly-cadence.php` — Streaming credit €10/wk → €11/wk (+10%). `expected.alerts` = 1 row, `annualized_impact_minor = 100 * 52 = 5200` (×52 multiplier per D-924 resolution).
    16. `quarterly-cadence.php` — Insurance €240/qtr → €270/qtr (+12.5%). `expected.alerts` = 1 row, annualized = +€120/yr (3000 * 4 = 12000 minor).
    17. `yearly-cadence.php` — Domain renewal €10/yr → €15/yr (+50%). `expected.alerts` = 1 row, annualized = +€5/yr (500 * 1 = 500 minor).
    18. `mixed-currency-stable-usd.php` — USD $11.99 stable × 6 monthly (no EUR drift either). `expected.alerts = []`.
    19. `mixed-currency-real-usd-drift.php` — USD $11.99 → $14.99 (+25%) monthly. `expected.alerts` = 1 row, `currency='USD'`, `delta_minor = -300` (signed in USD cents — expense).
    20. `pending-state-ignored.php` — Pending series with +20% drift. `expected.alerts = []` (D-911).
    21. `rejected-state-ignored.php` — Rejected series with +20% drift. `expected.alerts = []` (D-911).
    22. `snoozed-at-series-level-ignored.php` — Recurring-side snoozed series with +20% drift. `expected.alerts = []` (D-911).
    23. `irregular-cadence-ignored.php` — Series with `cadence='irregular'` and +20% drift. `expected.alerts = []` (D-911).
    24. `snooze-expiry-revival.php` — Acknowledged-then-snoozed alert; snoozed_until past. `expected.alerts` describes the revival outcome: a single transition row with `transition_reason='detector_revived_snooze'`. Wave 4 (Plan 09-05) actually exercises the revival sweep; this fixture's expected block describes the post-revival state.

    Every fixture's `expected.alerts` shape MUST be a list (possibly empty) of associative arrays whose keys are a subset of `{state, direction, baseline_amount_minor, latest_amount_minor, delta_minor, annualized_impact_minor, threshold_percent_used, threshold_source, currency, snoozed_until, actioned_at}`. Plan 09-03's `DriftDetectionContractTest` reads these tuples and asserts via `assertDatabaseHas('drift_alerts', $expected)`.

    **Modules/DriftAlerts/Database/Factories/DriftAlertFactory.php**: `namespace Modules\DriftAlerts\Database\Factories;` `final class DriftAlertFactory extends \Illuminate\Database\Eloquent\Factories\Factory`. Mirror `Modules/Recurring/Database/Factories/RecurringSeriesFactory.php` shape. `protected $model = \Modules\DriftAlerts\Models\DriftAlert::class` (the model class is delivered by Plan 09-02; the factory references the FQN by class name — Eloquent's factory resolution is lazy). `definition()` returns: `['user_id' => null, 'recurring_series_id' => null, 'state' => 'open', 'direction' => 'expense', 'baseline_amount_minor' => -999, 'latest_amount_minor' => -1149, 'currency' => 'EUR', 'delta_minor' => -150, 'annualized_impact_minor' => -1800, 'threshold_percent_used' => 5, 'threshold_source' => 'global', 'latest_occurrence_id' => null, 'detected_at' => $this->faker->dateTimeBetween('-30 days', 'now')]`. State factories: `open()`, `acknowledged()`, `snoozed(CarbonImmutable $until)`, `dismissedCancelled()`. Each sets state + relevant timestamp columns.

    **Modules/DriftAlerts/Database/Factories/DriftAlertTransitionFactory.php**: Same pattern. `protected $model = \Modules\DriftAlerts\Models\DriftAlertTransition::class`. `definition()` returns: `['user_id' => null, 'drift_alert_id' => null, 'from_state' => 'open', 'to_state' => 'acknowledged', 'transition_reason' => 'user_action', 'actor' => 'user', 'transitioned_at' => $this->faker->dateTimeBetween('-30 days', 'now'), 'notes' => null]`.

    **Modules/DriftAlerts/tests/Unit/FixtureCorpusTest.php**: Pest test that uses `dataset()` to iterate every file under `Modules/DriftAlerts/tests/fixtures/drift-corpus/*.php`. For each fixture, assert:
    - The file returns an array.
    - The array has both `'transactions'` (list) and `'expected'` (associative with `'alerts'` list) keys.
    - Every `expected.alerts` entry's keys are a subset of the allowed column list above.
    - If an alert has `direction`, it is `'expense'` or `'income'`.
    - If an alert has `state`, it is one of `{'open', 'acknowledged', 'snoozed', 'dismissed_cancelled'}`.
    - If an alert has `threshold_source`, it is `'global'` or `'series_override'`.
    The test must run all 24 fixtures and pass.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/DriftAlerts/tests/Unit/FixtureCorpusTest.php 2>&1 | tail -30 ; ls -1 Modules/DriftAlerts/tests/fixtures/drift-corpus/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls -1 Modules/DriftAlerts/tests/fixtures/drift-corpus/*.php | wc -l` equals 24.
    - All 24 expected filenames exist: `stable-monthly.php`, `small-drift-below-threshold.php`, `large-drift-above-threshold.php`, `income-raise.php`, `income-raise-large.php`, `income-cut.php`, `fx-only-swing.php`, `cadence-changed.php`, `multi-drift.php`, `per-series-override.php`, `prior-null.php`, `prior-zero.php`, `volatile-series.php`, `volatile-with-override.php`, `weekly-cadence.php`, `quarterly-cadence.php`, `yearly-cadence.php`, `mixed-currency-stable-usd.php`, `mixed-currency-real-usd-drift.php`, `pending-state-ignored.php`, `rejected-state-ignored.php`, `snoozed-at-series-level-ignored.php`, `irregular-cadence-ignored.php`, `snooze-expiry-revival.php`.
    - `vendor/bin/pest Modules/DriftAlerts/tests/Unit/FixtureCorpusTest.php` exits 0 and runs at least 24 dataset cases.
    - For each fixture, `php -r "var_dump(require 'Modules/DriftAlerts/tests/fixtures/drift-corpus/<NAME>.php')"` returns an array with `transactions` and `expected.alerts` keys.
    - `large-drift-above-threshold.php` returns `expected.alerts[0].delta_minor === -150` AND `expected.alerts[0].annualized_impact_minor === -1800`.
    - `fx-only-swing.php` returns `expected.alerts === []` (zero alerts).
    - `weekly-cadence.php` returns `expected.alerts[0].annualized_impact_minor` equal to `delta_minor × 52` (× 52 weekly multiplier locked).
    - `multi-drift.php` returns exactly 2 alert entries in `expected.alerts`.
    - `Modules\\DriftAlerts\\Database\\Factories\\DriftAlertFactory` and `DriftAlertTransitionFactory` classes exist and parse (PHPStan green; factory referencing not-yet-existing model is OK since `protected $model` is a string-FQN until invoked).
    - PHPStan against `Modules/DriftAlerts/Database/Factories/` reports no errors except the two `model` FQN references to Wave 1 deliverables (acceptable Wave 0 baseline).
  </acceptance_criteria>
  <done>
    24 fixture files exist with the documented shape, the corpus loader test runs all 24 datasets green, both factories parse without errors, and the corpus is ready for Plan 09-03 (`DriftEvaluatorTest` + `DriftDetectionContractTest`) to consume.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → autoloader | Composer autoload resolution at app boot; the new module must not pollute the autoload graph |
| module → module | DriftAlerts must not import from Recurring's Internal namespace; arch tests enforce |
| test fixtures → contract test | Fixture data must not embed live PII (synthesised only); checked by review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Tampering | Future drift_alerts code accidentally calls a facade or bypasses DI | mitigate | `noFacadeCallsFromDriftAlerts` arch test (Task 2 Invariant A) — extends the existing `Modules code does not use facades except documented carve-outs` rule with the DriftAlerts FQN carve-out; synthetic violation check in acceptance criteria proves it fires |
| T-9-02 | Tampering | Future drift_alerts code writes to recurring_series table directly (D-914 invariant violated) | mitigate | `noRecurringSeriesWritesFromDriftAlerts` arch test (Task 2 Invariant C) — filesystem-walk; synthetic violation check in acceptance criteria proves the regex catches write patterns |
| T-9-01b | Tampering | Future code mutates drift_alerts.state outside the state machine (after Plan 09-02 lands the table) | mitigate | `noOtherDriftAlertStateMutator` arch test (Task 2 Invariant F); Wave 0 only validates the invariant is wired — Plan 09-02 lands the actual state machine that the invariant whitelists |
| T-9-SC | Tampering | npm/composer install introduces unverified packages | accept | Phase 9 introduces ZERO new composer packages (verified in 09-RESEARCH.md `Package Legitimacy Audit`); no install step in this plan |
</threat_model>

<verification>
Wave 0 phase-level checks:
- `composer dump-autoload` succeeds.
- `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` is green (Phase 5-8 invariants + 5 new Phase 9 invariants).
- `vendor/bin/pest Modules/DriftAlerts/tests/Unit/FixtureCorpusTest.php` is green (24 datasets).
- `vendor/bin/pest --filter=DriftAlerts` discovers the new testsuite (zero feature/contract tests yet — Wave 0 ships only the corpus loader unit test).
- `vendor/bin/phpstan analyse Modules/DriftAlerts --memory-limit=2G` reports only missing-class errors for the FQNs that Waves 1-4 will land (no syntax errors, no helper/facade-rule violations).
- `vendor/bin/pint --test Modules/DriftAlerts/` reports green.
</verification>

<success_criteria>
- Module skeleton exists; ServiceProvider parses; routes file parses; testsuite registered (3-step pattern).
- Five BoundaryArchTest invariants pass against the empty Wave 0 module (trivially-correct baseline).
- 24-scenario fixture corpus on disk; corpus-loader unit test green.
- DriftAlertFactory + DriftAlertTransitionFactory parse and produce valid default arrays.
- Synthetic violation check confirms `noRecurringSeriesWritesFromDriftAlerts` fires correctly when a forbidden write is dropped into the module.
- The plan's `must_haves.truths` are all true; the new module is ready for Plan 09-02 (schema + models + state machine) to land on top.
</success_criteria>

<output>
Create `.planning/phases/09-subscription-drift-detection-alerts/09-01-SUMMARY.md` when done.
</output>
