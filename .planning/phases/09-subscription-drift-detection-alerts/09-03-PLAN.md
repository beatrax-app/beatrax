---
phase: 09-subscription-drift-detection-alerts
plan: 03
plan_id: "09-03"
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - Modules/Recurring/Public/Events/RecurringSeriesMetricsRefreshed.php
  - Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php
  - Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php
  - Modules/DriftAlerts/Internal/DriftEvaluator.php
  - Modules/DriftAlerts/Internal/Jobs/DetectDriftAlertsJob.php
  - Modules/DriftAlerts/Internal/Listeners/EvaluateDriftOnMetricsRefreshed.php
  - Modules/DriftAlerts/Public/Events/DriftAlertOpened.php
  - Modules/DriftAlerts/tests/Unit/DriftEvaluatorTest.php
  - Modules/DriftAlerts/tests/Unit/DriftEvaluatorEdgeCases.php
  - Modules/DriftAlerts/tests/Unit/DriftEvaluatorFxInvariant.php
  - Modules/DriftAlerts/tests/Unit/DriftEvaluatorCadenceChangedTest.php
  - Modules/DriftAlerts/tests/Unit/DriftEvaluatorEffectiveThresholdTest.php
  - Modules/DriftAlerts/tests/Unit/DetectDriftAlertsJobUniqueTest.php
  - Modules/DriftAlerts/tests/Unit/EvaluateDriftListenerTest.php
  - tests/Contracts/DriftDetectionContractTest.php
autonomous: true
requirements:
  - REC-06
  - REC-07
  - REC-08
must_haves:
  truths:
    - "Modules\\Recurring\\Public\\Events\\RecurringSeriesMetricsRefreshed final readonly event exists with public properties {userId, recurringSeriesId, direction, cadence, latestAmountMinor, latestCurrency} and is dispatched by both ExpenseSeriesDetector::refreshExistingSeries + ::insertNewSeries AND IncomeSeriesDetector equivalents; the on-demand /recurring/re-detect path therefore gets drift evaluation for free via the same event-driven trigger (D-913)"
    - "DriftEvaluator baseline = the immediately-prior occurrence's amount in the series's original currency; signed delta is computed against that baseline only (D-908)"
    - "DriftEvaluator math is FX-invariant: drift is evaluated in original currency only, and FX-only EUR swings on a stable original-currency series never fire an alert (D-909)"
    - "DriftEvaluator state filter fires on series in state IN ('approved', 'cadence_changed') — a cadence-flipped series still produces drift alerts (D-910)"
    - "DriftEvaluator excludes series in state IN ('pending', 'rejected', 'snoozed') AND series whose cadence is 'irregular' (D-911)"
    - "DriftEvaluator annualized impact = signed delta_minor × cadence-to-year multiplier (weekly=52, monthly=12, quarterly=4, yearly=1); stored signed on drift_alerts.annualized_impact_minor (D-917)"
    - "DriftEvaluator applies the effective-threshold lookup (series_override -> user.global -> hard 5%), guards prior_amount=0/NULL, and INSERTs one drift_alerts row when |delta/prior| × 100 > threshold AND prior_amount != 0"
    - "DriftAlerts is analytical-only: DriftEvaluator and the detection pipeline never write to recurring_series or transactions; only drift_alerts + drift_alert_transitions are written; reads of Recurring go exclusively through Modules\\Recurring\\Public\\Services\\RecurringSeriesQuery (D-914)"
    - "DetectDriftAlertsJob implements ShouldBeUniqueUntilProcessing with uniqueId() returning '{userId}:{seriesId}' (per-(user_id, recurring_series_id) single-flight), uniqueFor=600, uniqueVia()=Cache::driver('redis') (the documented facade carve-out), tries=3, backoff=[60,300,900] (D-912)"
    - "EvaluateDriftOnMetricsRefreshed listener is synchronous (not queued itself) and dispatches a single DetectDriftAlertsJob per event"
    - "DriftAlertOpened Public event is dispatched after every successful drift insert with {userId, driftAlertId, recurringSeriesId, direction, deltaMinor, annualizedImpactMinor, currency}"
    - "End-to-end DriftDetectionContractTest runs all 24 fixture corpus scenarios and produces the expected drift_alerts outcomes (zero alerts on stable/below-threshold/FX-only/prior-zero/prior-null/state-excluded fixtures; the expected alert count on drift-positive fixtures)"
  artifacts:
    - path: Modules/Recurring/Public/Events/RecurringSeriesMetricsRefreshed.php
      provides: Public event carrying post-refresh metric snapshot (planner-owned file under Recurring; cross-module deliverable for Phase 9)
    - path: Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php
      provides: Two new $this->events->dispatch(new RecurringSeriesMetricsRefreshed(...)) call sites
    - path: Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php
      provides: Two new dispatch call sites for income direction
    - path: Modules/DriftAlerts/Internal/DriftEvaluator.php
      provides: Math service (signed delta + threshold + annualized impact + drift_alerts insert + DriftAlertOpened dispatch)
    - path: Modules/DriftAlerts/Internal/Jobs/DetectDriftAlertsJob.php
      provides: Queued single-flight job keyed (userId, recurringSeriesId)
    - path: Modules/DriftAlerts/Internal/Listeners/EvaluateDriftOnMetricsRefreshed.php
      provides: Synchronous listener dispatching the queued job
    - path: Modules/DriftAlerts/Public/Events/DriftAlertOpened.php
      provides: Public event Phase 10 may subscribe to
    - path: tests/Contracts/DriftDetectionContractTest.php
      provides: End-to-end contract over the 24-scenario fixture corpus
  key_links:
    - from: Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php
      to: Modules/DriftAlerts/Internal/Listeners/EvaluateDriftOnMetricsRefreshed.php
      via: RecurringSeriesMetricsRefreshed event dispatch consumed by listener
      pattern: "dispatch\\(new RecurringSeriesMetricsRefreshed"
    - from: Modules/DriftAlerts/Internal/Listeners/EvaluateDriftOnMetricsRefreshed.php
      to: Modules/DriftAlerts/Internal/Jobs/DetectDriftAlertsJob.php
      via: Bus dispatch within the listener handle()
      pattern: "new DetectDriftAlertsJob"
    - from: Modules/DriftAlerts/Internal/DriftEvaluator.php
      to: Modules/Recurring/Public/Services/RecurringSeriesQuery
      via: Constructor injection; reads occurrencesForSeries + forSeries
      pattern: "RecurringSeriesQuery"
    - from: Modules/DriftAlerts/Internal/DriftEvaluator.php
      to: drift_alerts table
      via: db connection insertGetId
      pattern: "->table\\('drift_alerts'\\)->insertGetId"
---

<objective>
Wave 2 detection + queued evaluation pipeline. Land the new Recurring-side Public event (RecurringSeriesMetricsRefreshed), wire it into the existing ExpenseSeriesDetector + IncomeSeriesDetector at the two existing call sites (refreshExistingSeries + insertNewSeries), then build the DriftAlerts side of the pipeline: a synchronous listener, a queued ShouldBeUniqueUntilProcessing job, the DriftEvaluator math service, and the DriftAlertOpened Public event. End the wave with the DriftDetectionContractTest that runs all 24 fixtures end-to-end.

Purpose: This wave delivers the actual drift detection capability (REC-06). Detection runs entirely in the background via the existing Recurring sweep + a queued job; no synchronous detection touches a request lifecycle (BoundaryArchTest invariant E enforces this). The contract test against the 24-scenario fixture corpus from Plan 09-01 is the load-bearing evidence that the math is right: FX-only swings produce zero alerts; cadence-changed series still fire; prior-zero divides safely; weekly cadence uses x52 consistent with Phase 8's monthly-equivalent.

Output: The detection pipeline is live. After this wave completes, importing a new ASN transaction that bumps a Spotify subscription's amount above the threshold produces a drift_alerts row, a drift_alert_transitions row, and a dispatched DriftAlertOpened event - end-to-end - even though there's no UI yet. The UI lands in Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-subscription-drift-detection-alerts/09-CONTEXT.md
@.planning/phases/09-subscription-drift-detection-alerts/09-RESEARCH.md
@.planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md
@.planning/phases/09-subscription-drift-detection-alerts/09-01-PLAN.md
@.planning/phases/09-subscription-drift-detection-alerts/09-02-PLAN.md

# Analog source files the executor MUST read:
@Modules/Recurring/Public/Events/RecurringSeriesCadenceFlipped.php
@Modules/Recurring/Public/Events/RecurringSeriesApproved.php
@Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php
@Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php
@Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php
@Modules/Recurring/Public/Services/RecurringSeriesQuery.php
@tests/Contracts/RecurringDetectionContractTest.php

<interfaces>
From Modules/Recurring/Public/Services/RecurringSeriesQuery.php (read API DriftEvaluator consumes):

namespace Modules\Recurring\Public\Services;
final readonly class RecurringSeriesQuery {
    public function forSeries(int $seriesId, User $user): ?RecurringSeriesDto;
    // Returns null on cross-user / missing - safer than throwing.
    public function occurrencesForSeries(int $seriesId, User $user): array;
    // Returns list<RecurringSeriesOccurrenceDto> ordered observed_at DESC, id DESC.
    // Each Dto carries occurrenceId (int), observedAmount (Money - original currency), observedAt (CarbonImmutable).
}

From the RecurringSeriesDto shape (the planner verifies actual fields by reading the file):
$series->state in {'pending', 'approved', 'cadence_changed', 'rejected', 'snoozed', 'irregular', ...}
$series->cadence in {'monthly', 'weekly', 'quarterly', 'yearly', 'irregular'}
$series->direction in {'expense', 'income'}
$series->latestAmount is a Money (original currency)
$series->monthlyEquivalent is a Money (in latest_currency, NOT EUR - Pitfall 1)

From Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php lines 53-80 (the ShouldBeUniqueUntilProcessing idiom DriftAlerts mirrors):

class DetectRecurringSeriesJob implements ShouldBeUniqueUntilProcessing, ShouldQueue {
    public int $tries = 3;
    public array $backoff = [60, 300, 900];
    public function uniqueId(): string { return (string) $this->userId; }
    public function uniqueFor(): int { return 600; }
    public function uniqueVia(): \Illuminate\Contracts\Cache\Repository {
        return \Illuminate\Support\Facades\Cache::driver('redis');
    }
}

From Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php near lines 364-402:
The detector already injects \Illuminate\Contracts\Events\Dispatcher as $this->events.
refreshExistingSeries() currently ends after a cadence-flip block (line ~401).
insertNewSeries() currently ends after dispatching RecurringSeriesDetected (line ~339).
</interfaces>
</context>

<tasks>

<task type="auto" id="9-03-01">
  <name>Task 1: Publish RecurringSeriesMetricsRefreshed event + wire dispatch into both detectors</name>
  <read_first>
    - Modules/Recurring/Public/Events/RecurringSeriesCadenceFlipped.php (entire file - 23 lines - the final readonly event shape with 3+ public properties)
    - Modules/Recurring/Public/Events/RecurringSeriesApproved.php (entire file - simpler shape)
    - Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php (read lines 320-410, focus on refreshExistingSeries() end-of-method and insertNewSeries() end-of-method to identify the exact insertion points; note the $this->events Dispatcher collaborator already present)
    - Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php (locate equivalent methods - same structural pattern but for direction='income')
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md (sections: RecurringSeriesMetricsRefreshed.php + Detector insertion point)
  </read_first>
  <files>
    - Modules/Recurring/Public/Events/RecurringSeriesMetricsRefreshed.php (NEW)
    - Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php (MODIFY - add 2 dispatch call sites)
    - Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php (MODIFY - add 2 dispatch call sites)
  </files>
  <action>
    Modules/Recurring/Public/Events/RecurringSeriesMetricsRefreshed.php: `namespace Modules\Recurring\Public\Events; final readonly class RecurringSeriesMetricsRefreshed { public function __construct(public int $userId, public int $recurringSeriesId, public string $direction, public string $cadence, public int $latestAmountMinor, public string $latestCurrency) {} }`. Class-level PHPDoc describes the event purpose in plain technical language: "Dispatched after a recurring_series row's metric columns (latest_amount_minor, latest_currency, monthly_equivalent_minor, next_expected_at, cadence) are refreshed by the sweep detector. Fires once per refreshed series per sweep - not per occurrence. Carries the post-refresh metric snapshot inline so listeners can decide whether to do additional work without re-reading the row." NO D-numbers, NO REQ-IDs, NO planning references.

    Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php: Add an import for the new event at the top. Append the dispatch call site at the END of refreshExistingSeries() AFTER the existing cadence-flip block. The dispatch carries direction='expense' (hard-coded since this is the expense detector), cadence=$cadence (existing local), latestAmountMinor=$latestAmountMinor (existing local), latestCurrency=$currency (existing local). userId=$user->id, recurringSeriesId=$seriesId. Also append the same dispatch at the END of insertNewSeries() AFTER the existing RecurringSeriesDetected dispatch. Use the existing $this->events collaborator. NO new constructor parameter needed.

    Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php: Same two insertions; replace 'expense' with 'income'. Use whatever the local variable names are in the income detector (read the file to verify naming - they may differ slightly from the expense detector).

    The Recurring detectors currently dispatch RecurringSeriesCadenceFlipped under specific conditions; the new RecurringSeriesMetricsRefreshed dispatches UNCONDITIONALLY at the end of both methods - the listener decides whether to do work. This matches the RESEARCH.md recommendation in Pitfall 3 (per-series-per-sweep granularity, not per-state-transition).

    Verify the BoundaryArchTest invariants are unaffected: ExpenseSeriesDetector is in Modules\Recurring\Internal so this is intra-module wiring; the new Public event is the cross-module surface DriftAlerts subscribes to in Task 2.
  </action>
  <verify>
    <automated>grep -c "RecurringSeriesMetricsRefreshed" Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php ; grep -c "RecurringSeriesMetricsRefreshed" Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php ; vendor/bin/pest Modules/Recurring/tests/ 2>&1 | tail -10 ; vendor/bin/phpstan analyse Modules/Recurring --memory-limit=2G 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `Modules/Recurring/Public/Events/RecurringSeriesMetricsRefreshed.php` exists, is `final readonly class`, has 6 public constructor properties (userId, recurringSeriesId, direction, cadence, latestAmountMinor, latestCurrency).
    - `grep -c "RecurringSeriesMetricsRefreshed" Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php` returns at least 3 (1 import + 2 dispatch sites).
    - `grep -c "RecurringSeriesMetricsRefreshed" Modules/Recurring/Internal/Detectors/IncomeSeriesDetector.php` returns at least 3.
    - The expense dispatch carries `direction: 'expense'`; the income dispatch carries `direction: 'income'`. Verified with `grep -A 8 "new RecurringSeriesMetricsRefreshed" Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php | grep -c "direction: 'expense'"` returning at least 2.
    - All existing Phase 8 Recurring tests stay green (no regressions in ExpenseSeriesDetector / IncomeSeriesDetector / DetectRecurringSeriesJob test coverage).
    - PHPStan against Modules/Recurring is green.
    - Pint --test is green for the three modified files.
  </acceptance_criteria>
  <done>
    The new Recurring Public event exists; both detectors dispatch it at the two documented insertion points; no existing Phase 8 test regresses; PHPStan + Pint stay green.
  </done>
</task>

<task type="auto" id="9-03-02">
  <name>Task 2: DriftEvaluator + DriftAlertOpened event + edge-case unit tests</name>
  <read_first>
    - Modules/Recurring/Internal/Detectors/ExpenseSeriesDetector.php (lines 330-460 - the compute-then-insert envelope; specifically lines 433-445 for the monthlyEquivalent x52/12 multiplier the cadenceMultiplierForYear must agree with at the integer level)
    - Modules/Recurring/Public/Services/RecurringSeriesQuery.php (read forSeries + occurrencesForSeries methods - lines 91-150 - to understand the DTO shape DriftEvaluator consumes)
    - Modules/Recurring/Public/Dto/RecurringSeriesDto.php (entire file - the DTO DriftEvaluator reads)
    - Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php (Plan 09-02 - the state machine DriftEvaluator does NOT use for the insert path; the insert relies on the UNIQUE index for idempotency, NOT the state machine. The state machine is only invoked by Public Actions in Wave 3)
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md (DriftEvaluator section + Shared Patterns / Effective threshold lookup + Cadence-to-year multiplier)
    - .planning/phases/09-subscription-drift-detection-alerts/09-RESEARCH.md (Pitfalls 1, 4, 6 - critical for the implementation)
  </read_first>
  <files>
    - Modules/DriftAlerts/Public/Events/DriftAlertOpened.php (NEW)
    - Modules/DriftAlerts/Internal/DriftEvaluator.php (NEW)
    - Modules/DriftAlerts/tests/Unit/DriftEvaluatorTest.php (NEW)
    - Modules/DriftAlerts/tests/Unit/DriftEvaluatorEdgeCases.php (NEW)
    - Modules/DriftAlerts/tests/Unit/DriftEvaluatorFxInvariant.php (NEW)
    - Modules/DriftAlerts/tests/Unit/DriftEvaluatorCadenceChangedTest.php (NEW)
    - Modules/DriftAlerts/tests/Unit/DriftEvaluatorEffectiveThresholdTest.php (NEW)
  </files>
  <action>
    Modules/DriftAlerts/Public/Events/DriftAlertOpened.php: `namespace Modules\DriftAlerts\Public\Events; final readonly class DriftAlertOpened { public function __construct(public int $userId, public int $driftAlertId, public int $recurringSeriesId, public string $direction, public int $deltaMinor, public int $annualizedImpactMinor, public string $currency) {} }`. Class-level PHPDoc: "Dispatched after a drift_alerts row is inserted with state='open'. Phase 10 forecasting may subscribe." No D-numbers.

    Modules/DriftAlerts/Internal/DriftEvaluator.php: `namespace Modules\DriftAlerts\Internal; final readonly class DriftEvaluator`. Constructor DI: `private DatabaseManager $db, private Clock $clock, private RecurringSeriesQuery $recurringQuery, private Dispatcher $events`. Single public method: `public function evaluateForSeries(int $seriesId, User $user): void`.

    evaluateForSeries() body (mirror PATTERNS.md DriftEvaluator excerpt verbatim, with the 8-step layout):
    1. Read series via $this->recurringQuery->forSeries($seriesId, $user). If null, return (cross-user/missing - silent no-op).
    2. State filter: `if (! in_array($series->state, ['approved', 'cadence_changed'], true)) return;` This is the D-911 filter.
    3. Read last two occurrences via $this->recurringQuery->occurrencesForSeries($seriesId, $user). If count() < 2, return.
    4. $latest = $occurrences[0]; $prior = $occurrences[1]; (DESC ordering).
    5. Prior-amount guard: `$priorMinor = $prior->observedAmount->minorUnits(); if ($priorMinor === 0) return;` Note that observedAmount stored as integer minor units precludes null - the null case is handled at step 3 (count < 2). This explicitly covers Pitfall 4 (prior_amount=0 divides by zero).
    6. Compute signed delta in original currency: $latestMinor = $latest->observedAmount->minorUnits(); $deltaMinor = $latestMinor - $priorMinor; $ratio = abs($deltaMinor) * 100 / abs($priorMinor);
    7. Effective threshold lookup via private helper effectiveThresholdPercent($seriesId, $user) returning array{percent: int, source: string}. If $ratio <= $threshold['percent'], return (drift below threshold).
    8. Compute annualized impact: $annualized = $deltaMinor * $this->cadenceMultiplierForYear($series->cadence); If multiplier returns 0 (cadence='irregular'), return (don't alert on irregular series).
    9. INSERT drift_alerts row with all columns: user_id, recurring_series_id, state='open', direction=$series->direction, baseline_amount_minor=$priorMinor, latest_amount_minor=$latestMinor, currency=$series->latestAmount->currency()->getCurrencyCode(), delta_minor=$deltaMinor, annualized_impact_minor=$annualized, threshold_percent_used=$threshold['percent'], threshold_source=$threshold['source'], latest_occurrence_id=$latest->occurrenceId, detected_at=$now, created_at=$now, updated_at=$now. Wrap the INSERT in try/catch on \Illuminate\Database\QueryException to silently swallow UNIQUE constraint violations (the idempotency seam catches re-runs against the same series-occurrence pair).
    10. After successful insertGetId, dispatch DriftAlertOpened with the new alertId.

    Private helper effectiveThresholdPercent (verbatim from PATTERNS.md Shared Patterns / Effective threshold lookup):
    ```
    private function effectiveThresholdPercent(int $recurringSeriesId, User $user): array
    {
        $seriesRow = $this->db->connection()->table('recurring_series')
            ->where('id', $recurringSeriesId)
            ->where('user_id', $user->id)
            ->first(['drift_threshold_percent']);
        $seriesOverride = $seriesRow !== null && is_numeric($seriesRow->drift_threshold_percent)
            ? (int) $seriesRow->drift_threshold_percent
            : null;
        if ($seriesOverride !== null) {
            return ['percent' => $seriesOverride, 'source' => 'series_override'];
        }
        $userValue = $user->drift_alert_threshold_percent;
        if (is_int($userValue) && $userValue > 0) {
            return ['percent' => $userValue, 'source' => 'global'];
        }
        return ['percent' => 5, 'source' => 'global'];
    }
    ```
    PHPDoc on this method documents the carve-out: "Reads recurring_series.drift_threshold_percent directly via the injected DatabaseManager. The BoundaryArchTest invariant noRecurringSeriesWritesFromDriftAlerts fires only on WRITE verbs (update/insert/delete); this READ of a non-state column is intentionally permitted."

    Private helper cadenceMultiplierForYear (per RESEARCH.md D-924 resolution - locked at x52 weekly for Phase 8 consistency):
    ```
    private function cadenceMultiplierForYear(string $cadence): int
    {
        return match ($cadence) {
            'weekly' => 52,
            'monthly' => 12,
            'quarterly' => 4,
            'yearly' => 1,
            default => 0,
        };
    }
    ```

    DriftEvaluator's MAIN class PHPDoc: "Drift detection math. Reads the last two occurrences of an approved or cadence-changed recurring series via Modules\\Recurring\\Public\\Services\\RecurringSeriesQuery, computes signed delta in original currency, applies the effective threshold (series override beats user global beats hard 5%), guards against divide-by-zero, and INSERTs one drift_alerts row when the ratio crosses the threshold. Idempotency is enforced by the UNIQUE(recurring_series_id, latest_occurrence_id) index on drift_alerts; re-running against the same pair is a no-op." No D-numbers.

    Five test files (all extend Modules\DriftAlerts\Tests\TestCase, use RefreshDatabase):

    1. DriftEvaluatorTest.php - Pest dataset (prior_minor, latest_minor, threshold_percent, cadence, currency, direction) -> (expected_alerts_count, expected_delta_minor?, expected_annualized_minor?). Seed a recurring_series + 2 occurrences matching each row; call evaluateForSeries via container resolution; assert drift_alerts count + key columns. Cover at least: stable case (zero delta), small-drift-below-threshold (no alert), large-drift-above-threshold (1 alert with correct delta + annualized), monthly cadence vs weekly cadence vs quarterly vs yearly (different multipliers).

    2. DriftEvaluatorEdgeCases.php - prior_minor=0 case (no alert; divide-by-zero guard); occurrences count<2 case (no alert); irregular cadence (no alert because multiplier=0); rejected/pending/snoozed/irregular state filters (no alert).

    3. DriftEvaluatorFxInvariant.php - Pitfall 6: seed series with latest_currency='USD'; seed occurrences both with observed_currency='USD' with stable 1199 minor units; verify ZERO drift_alerts despite hypothetical EUR-shadow drift (the evaluator never reads settled_currency / EUR fields - all math is original-currency). This is the LOAD-BEARING D-909 carry-forward test.

    4. DriftEvaluatorCadenceChangedTest.php - Seed series in state='cadence_changed' with +10% drift; assert 1 drift_alerts row inserted (D-910 carry-forward).

    5. DriftEvaluatorEffectiveThresholdTest.php - Three cases: (a) recurring_series.drift_threshold_percent=50 + user.drift_alert_threshold_percent=10 + 25% drift => no alert (series override beats user global beats hard 5%); (b) recurring_series.drift_threshold_percent=NULL + user.drift_alert_threshold_percent=10 + 25% drift => alert with threshold_percent_used=10, threshold_source='global'; (c) recurring_series.drift_threshold_percent=NULL + user.drift_alert_threshold_percent=NULL/0 + 6% drift => alert with threshold_percent_used=5, threshold_source='global' (hard default).

    All tests register DriftEvaluator via container so DI works; use $this->app->make(DriftEvaluator::class) inside each test.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/DriftAlerts/tests/Unit/DriftEvaluator 2>&1 | tail -30 ; vendor/bin/phpstan analyse Modules/DriftAlerts/Internal/DriftEvaluator.php Modules/DriftAlerts/Public/Events/DriftAlertOpened.php --memory-limit=2G 2>&1 | tail -10 ; vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter="DriftAlerts" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All five DriftEvaluator* tests pass.
    - The five BoundaryArchTest DriftAlerts invariants from Plan 09-01 all stay GREEN. Critically: noRecurringSeriesWritesFromDriftAlerts MUST remain green - the new DriftEvaluator reads recurring_series.drift_threshold_percent (a non-state column via SELECT only) which the invariant's regex specifically does not flag (the regex matches `->update|insert|delete` verbs on the recurring_series table).
    - `grep "->table('drift_alerts')->insertGetId" Modules/DriftAlerts/Internal/DriftEvaluator.php` returns 1 line.
    - `grep "RecurringSeriesQuery" Modules/DriftAlerts/Internal/DriftEvaluator.php` returns at least 2 lines (import + injection).
    - `grep "Modules\\\\Recurring\\\\Internal" Modules/DriftAlerts/Internal/DriftEvaluator.php` returns 0 lines (crossModuleAccessGoesThroughPublic invariant).
    - `grep -E "match.*cadence.*'weekly'" Modules/DriftAlerts/Internal/DriftEvaluator.php` confirms the weekly=>52 multiplier line.
    - The FX invariant test demonstrates: a series with USD currency and stable USD amounts produces ZERO drift_alerts rows; this is the regression guard for D-909.
    - PHPStan strict-rules clean on Modules/DriftAlerts/Internal/DriftEvaluator.php.
    - Pint --test green.
  </acceptance_criteria>
  <done>
    DriftEvaluator implements the documented math; 5 unit tests cover happy path, edge cases, FX invariant, cadence-changed state, and effective threshold; all BoundaryArchTest invariants remain green; PHPStan + Pint pass.
  </done>
</task>

<task type="auto" id="9-03-03">
  <name>Task 3: DetectDriftAlertsJob + EvaluateDriftOnMetricsRefreshed listener + end-to-end contract test</name>
  <read_first>
    - Modules/Recurring/Internal/Jobs/DetectRecurringSeriesJob.php (entire file - the ShouldBeUniqueUntilProcessing idiom Phase 9 mirrors point-for-point; especially lines 1-100 for the uniqueVia carve-out comment + uniqueFor + tries/backoff)
    - tests/Contracts/RecurringDetectionContractTest.php (entire file - the precedent end-to-end contract test that Phase 9 mirrors for DriftDetectionContractTest)
    - Modules/Recurring/tests/Unit/* (find an analog test that verifies ShouldBeUniqueUntilProcessing - the existing DetectRecurringSeriesJob has one; mirror its shape)
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md (DetectDriftAlertsJob + EvaluateDriftOnMetricsRefreshed sections + Shared Patterns / ShouldBeUniqueUntilProcessing queued-job idiom)
    - .planning/phases/09-subscription-drift-detection-alerts/09-VALIDATION.md (Per-Task Verification Map rows 9-03-05 through 9-03-07)
  </read_first>
  <files>
    - Modules/DriftAlerts/Internal/Jobs/DetectDriftAlertsJob.php (NEW)
    - Modules/DriftAlerts/Internal/Listeners/EvaluateDriftOnMetricsRefreshed.php (NEW)
    - Modules/DriftAlerts/tests/Unit/DetectDriftAlertsJobUniqueTest.php (NEW)
    - Modules/DriftAlerts/tests/Unit/EvaluateDriftListenerTest.php (NEW)
    - tests/Contracts/DriftDetectionContractTest.php (NEW)
  </files>
  <action>
    Modules/DriftAlerts/Internal/Jobs/DetectDriftAlertsJob.php: Class-level PHPDoc (mirror PATTERNS.md excerpt verbatim): "Per-(user, series) drift evaluation. Dispatched by EvaluateDriftOnMetricsRefreshed after each Recurring sweep refreshes a series's metric columns. Concurrency contract: ShouldBeUniqueUntilProcessing keyed on '{userId}:{seriesId}'. tries=3, backoff=[60, 300, 900]. Single permitted facade exception: the Cache::driver('redis') call inside uniqueVia(). Laravel resolves the lock store at queue-push time before constructor DI completes - a constructor-injected Repository is not an option. BoundaryArchTest carve-out names this FQN explicitly."

    Class: `final class DetectDriftAlertsJob implements ShouldBeUniqueUntilProcessing, ShouldQueue`. Traits: Dispatchable, InteractsWithQueue, Queueable, SerializesModels. Properties: `public int $tries = 3; public array $backoff = [60, 300, 900];`. Constructor: `public function __construct(public readonly int $userId, public readonly int $recurringSeriesId) {}`.

    Methods:
    - uniqueId(): returns "{$this->userId}:{$this->recurringSeriesId}".
    - uniqueFor(): returns 600.
    - uniqueVia(): returns Cache::driver('redis') - the documented facade carve-out from BoundaryArchTest Plan 09-01.
    - handle(DriftEvaluator $evaluator): User firstOrFail $userId, then $evaluator->evaluateForSeries($this->recurringSeriesId, $user);

    Modules/DriftAlerts/Internal/Listeners/EvaluateDriftOnMetricsRefreshed.php: Class-level PHPDoc: "Subscribes to the Recurring-side per-series metric-refresh event and dispatches a queued DetectDriftAlertsJob for the affected series. The listener stays synchronous (no ShouldQueue on the listener itself - the JOB is queued; double-queueing would defeat the unique-job key on (userId, seriesId)). Cross-module: imports Modules\\Recurring\\Public\\Events only - never Modules\\Recurring\\Internal."

    Class: `final readonly class EvaluateDriftOnMetricsRefreshed`. Constructor: `public function __construct(private Dispatcher $bus) {}` (Illuminate\Contracts\Bus\Dispatcher). Method: `public function handle(RecurringSeriesMetricsRefreshed $event): void { $this->bus->dispatch(new DetectDriftAlertsJob(userId: $event->userId, recurringSeriesId: $event->recurringSeriesId)); }`.

    DetectDriftAlertsJobUniqueTest.php (Pest):
    - Verify the job declares ShouldBeUniqueUntilProcessing via `expect(new ReflectionClass(DetectDriftAlertsJob::class))->implementsInterface(ShouldBeUniqueUntilProcessing::class)->toBeTrue()`.
    - Verify uniqueId() output format: instantiate `new DetectDriftAlertsJob(42, 7)`, assert `$job->uniqueId() === '42:7'`.
    - Verify uniqueFor() === 600.
    - Verify uniqueVia() returns a Cache\Repository (use Cache::shouldReceive or set up the array driver as 'redis' fake per the project's existing pattern in tests/TestCase.php Phase 5 carry-forward).
    - Verify tries=3 and backoff=[60,300,900].

    EvaluateDriftListenerTest.php:
    - Use Bus::fake() to capture dispatched jobs.
    - Construct a synthetic RecurringSeriesMetricsRefreshed event with userId=42, recurringSeriesId=7.
    - Call $listener->handle($event).
    - Assert Bus::assertDispatched(DetectDriftAlertsJob::class, fn ($job) => $job->userId === 42 && $job->recurringSeriesId === 7).
    - Assert exactly 1 dispatch (no double-dispatch).

    tests/Contracts/DriftDetectionContractTest.php: End-to-end Pest contract test over the 24-scenario fixture corpus from Plan 09-01. Pattern mirrors tests/Contracts/RecurringDetectionContractTest.php verbatim.

    Helper function (mirroring RecurringDetectionContractTest.php's structure):
    ```
    function ddctFixtureExpectations(): array
    {
        return [
            'stable-monthly' => 0,
            'small-drift-below-threshold' => 0,
            'large-drift-above-threshold' => 1,
            'income-raise' => 0,
            'income-raise-large' => 1,
            'income-cut' => 1,
            'fx-only-swing' => 0,
            'cadence-changed' => 1,
            'multi-drift' => 2,
            'per-series-override' => 0,
            'prior-null' => 0,
            'prior-zero' => 0,
            'volatile-series' => 'multiple',
            'volatile-with-override' => 0,
            'weekly-cadence' => 1,
            'quarterly-cadence' => 1,
            'yearly-cadence' => 1,
            'mixed-currency-stable-usd' => 0,
            'mixed-currency-real-usd-drift' => 1,
            'pending-state-ignored' => 0,
            'rejected-state-ignored' => 0,
            'snoozed-at-series-level-ignored' => 0,
            'irregular-cadence-ignored' => 0,
            'snooze-expiry-revival' => 'skip',
        ];
    }
    ```

    For each fixture (use Pest dataset over the keys above):
    1. Load the fixture file via require: `$fixture = require Modules/DriftAlerts/tests/fixtures/drift-corpus/{name}.php`.
    2. Seed a User row.
    3. Seed a recurring_series row with the state/direction/cadence from the fixture (the fixture's `transactions` block contains hints about state - the test helper extracts them). When the fixture asks for a per-series override (per-series-override / volatile-with-override), set recurring_series.drift_threshold_percent to the override value.
    4. Seed 2+ recurring_series_occurrences rows matching the fixture's prior + latest amounts and currencies.
    5. Dispatch DetectDriftAlertsJob synchronously by calling handle() directly: `app(DetectDriftAlertsJob::class, ['userId' => $user->id, 'recurringSeriesId' => $seriesId])->handle(app(DriftEvaluator::class));`.
    6. Assert `DriftAlert::query()->where('recurring_series_id', $seriesId)->count()` matches the expected count from ddctFixtureExpectations.
    7. For drift-positive fixtures, additionally assert at least one inserted drift_alerts row has the expected delta_minor, annualized_impact_minor, threshold_percent_used, and threshold_source from the fixture's expected.alerts[0].

    The 'snooze-expiry-revival' fixture is skipped at the contract level here (it's exercised by Plan 09-05 Wave 4). The 'volatile-series' fixture has 'multiple' expected count; assert count > 1.

    For the per-series-override fixture: the recurring_series row needs drift_threshold_percent set to 50 in the seed step. The DriftEvaluator's effectiveThresholdPercent then returns 50; 25% drift falls below; zero alerts.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/DriftAlerts/tests/Unit/DetectDriftAlertsJobUniqueTest.php Modules/DriftAlerts/tests/Unit/EvaluateDriftListenerTest.php 2>&1 | tail -20 ; vendor/bin/pest tests/Contracts/DriftDetectionContractTest.php 2>&1 | tail -40 ; vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter="DriftAlerts" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - DetectDriftAlertsJobUniqueTest is green; all five sub-assertions pass.
    - EvaluateDriftListenerTest is green; Bus::assertDispatched fires correctly.
    - DriftDetectionContractTest is green and runs all 23 active fixtures (24 minus 'snooze-expiry-revival' skipped to Wave 4). At minimum: 12 expected-zero-alert fixtures produce zero drift_alerts; 10 expected-1+-alert fixtures produce the documented count.
    - The mixed-currency-real-usd-drift fixture inserts a drift_alerts row with currency='USD' (proving original-currency persistence; not EUR).
    - The volatile-with-override fixture inserts zero drift_alerts despite +30% variance (proves per-series override beats user/global).
    - The weekly-cadence fixture's inserted row has annualized_impact_minor = delta_minor x 52.
    - BoundaryArchTest invariants stay green (especially: noFacadeCallsFromDriftAlerts — Cache::driver carve-out is the only allowed facade; noSynchronousDriftDetectionInRequestLifecycle — DriftEvaluator is only imported by Internal/Jobs and Internal/Listeners, never by Internal/Http).
    - PHPStan strict-rules clean on Modules/DriftAlerts/Internal/Jobs and Modules/DriftAlerts/Internal/Listeners.
    - Pint --test green.
  </acceptance_criteria>
  <done>
    The complete background detection pipeline works end-to-end against the 24-scenario fixture corpus; the job's single-flight semantics are verified; the listener's dispatch is verified; BoundaryArchTest invariants remain green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recurring event -> DriftAlerts listener | Cross-module Public event consumption; only Modules\Recurring\Public\* may be imported |
| Queue worker -> DriftEvaluator | Job context; user identity is reconstructed from the integer userId param |
| DriftEvaluator -> drift_alerts | Sole write surface from Phase 9 detector code; never writes to recurring_series |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-04 | Spoofing/Tampering | FX-only EUR swing produces a false drift alert on a stable original-currency subscription (D-909 invariant violated) | mitigate | DriftEvaluator reads ONLY observedAmount in original currency from the Public Query; the evaluator never touches settled_currency / EUR shadow fields. Proven by DriftEvaluatorFxInvariant.php Pest test + the fx-only-swing fixture in DriftDetectionContractTest |
| T-9-02 | Tampering | DriftEvaluator accidentally writes to recurring_series | mitigate | (1) noRecurringSeriesWritesFromDriftAlerts arch test (Plan 09-01); (2) DriftEvaluator only INSERTs to drift_alerts; (3) the single READ of recurring_series.drift_threshold_percent is documented as a permitted carve-out (read-only, non-state column) |
| T-9-01 | Tampering | DetectDriftAlertsJob bypasses DI by calling facades | mitigate | Only Cache::driver('redis') is permitted (documented carve-out + BoundaryArchTest ignore-list entry from Plan 09-01); DriftEvaluator is injected into handle() |
| T-9-04b | Denial of Service | Per-occurrence event flood would queue thousands of jobs on a large user history | mitigate | Plan uses per-series-per-sweep event (RecurringSeriesMetricsRefreshed), not per-occurrence; ShouldBeUniqueUntilProcessing collapses any double-dispatch within 600s; if a user has 200 series the sweep produces 200 events, each producing at most 1 job. Documented in PATTERNS.md Pitfall 3 |
| T-9-03d | Tampering | Concurrent job runs against the same series race on INSERT | mitigate | UNIQUE(recurring_series_id, latest_occurrence_id) index on drift_alerts; DriftEvaluator catches QueryException on UNIQUE violation as a silent no-op |
</threat_model>

<verification>
Wave 2 phase-level checks:
- `php artisan migrate:fresh --env=testing` succeeds.
- All Wave 0 + Wave 1 + Wave 2 Pest tests are green; `vendor/bin/pest --filter=DriftAlerts` is green.
- DriftDetectionContractTest is green over 23 fixtures (24 minus snooze-expiry-revival which is deferred to Wave 4).
- BoundaryArchTest (full file) is green.
- PHPStan against Modules/DriftAlerts is fully green (Wave 2 closes the missing-class errors for DriftEvaluator + DetectDriftAlertsJob + EvaluateDriftOnMetricsRefreshed; the remaining unresolved imports are the Wave 3 deliverables — DriftPage, DashboardDriftBadge, three Public Actions, DriftAlertQuery, CancellationImpactQuery).
- PHPStan against Modules/Recurring is green (the two detector modifications + the new Public event must pass strict).
- Pint --test is green.
- Manual smoke test (optional, document in summary): run `php artisan tinker` and dispatch `event(new \Modules\Recurring\Public\Events\RecurringSeriesMetricsRefreshed(userId: 1, recurringSeriesId: 1, direction: 'expense', cadence: 'monthly', latestAmountMinor: -1149, latestCurrency: 'EUR'))` — the listener fires and dispatches a DetectDriftAlertsJob (visible in horizon dashboard or `php artisan queue:work` foreground).
</verification>

<success_criteria>
- The end-to-end detection pipeline works: a Recurring sweep fires RecurringSeriesMetricsRefreshed -> listener queues DetectDriftAlertsJob -> job runs DriftEvaluator -> drift_alerts row is inserted -> DriftAlertOpened event is dispatched.
- The 24-scenario fixture corpus is the load-bearing acceptance test; all 23 active fixtures produce the documented outcome.
- FX-only swing produces ZERO alerts (D-909 carry-forward).
- prior_amount=0 and count<2 cases produce ZERO alerts (Pitfall 4 guard).
- cadence_changed series produce alerts (D-910 carry-forward).
- Per-series threshold override beats user global beats hard 5% (D-915).
- BoundaryArchTest invariants remain green (especially the cross-module + facade + sync-detection + state-mutator invariants).
- PHPStan + Pint stay green.
</success_criteria>

<output>
Create `.planning/phases/09-subscription-drift-detection-alerts/09-03-SUMMARY.md` when done.
</output>
