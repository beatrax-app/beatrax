---
phase: 09-subscription-drift-detection-alerts
plan: 02
plan_id: "09-02"
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - Modules/DriftAlerts/Database/Migrations/2026_05_19_010001_create_drift_alerts_table.php
  - Modules/DriftAlerts/Database/Migrations/2026_05_19_010002_create_drift_alert_transitions_table.php
  - Modules/Recurring/Database/Migrations/2026_05_19_010002_add_drift_threshold_percent_to_recurring_series.php
  - Modules/Recurring/Database/Migrations/2026_05_19_010003_add_drift_alert_threshold_percent_to_users.php
  - Modules/DriftAlerts/Models/DriftAlert.php
  - Modules/DriftAlerts/Models/DriftAlertTransition.php
  - Modules/DriftAlerts/Public/Dto/DriftAlertDto.php
  - Modules/DriftAlerts/Public/Dto/CancellationImpactDto.php
  - Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php
  - Modules/DriftAlerts/Internal/StateMachines/InvalidStateTransitionException.php
  - Modules/DriftAlerts/tests/Unit/DriftAlertsMigrationTest.php
  - Modules/DriftAlerts/tests/Unit/DriftAlertTransitionsMigrationTest.php
  - Modules/DriftAlerts/tests/Unit/DriftThresholdMigrationsTest.php
  - Modules/DriftAlerts/tests/Unit/DriftAlertDtoTest.php
  - Modules/DriftAlerts/tests/Unit/DriftAlertStateMachineTest.php
  - Modules/Core/Models/User.php
autonomous: true
requirements:
  - REC-06
  - REC-08
must_haves:
  truths:
    - "`drift_alerts` table exists with all required columns (state/direction enums, BIGINT minor-unit amounts, threshold_percent_used + threshold_source, UNIQUE(recurring_series_id, latest_occurrence_id), 3 user-scoped indexes) AND a BEFORE INSERT + BEFORE UPDATE trigger pair that rejects any state value outside the allowed set; the UNIQUE seam encodes the one-row-per-drift-event contract (D-904)"
    - "`drift_alert_transitions` table mirrors the Phase 8 audit shape verbatim"
    - "Threshold schema is wired end-to-end: `users.drift_alert_threshold_percent` (unsignedTinyInteger default 5) + `recurring_series.drift_threshold_percent` (nullable unsignedTinyInteger override) + `drift_alerts.threshold_percent_used` + `drift_alerts.threshold_source` on every alert row (D-915)"
    - "drift_alerts rows persist forever — no auto-expiry, no pruning column, no scheduled cleanup; acknowledged / snoozed / dismissed alerts stay in the table for audit (D-907)"
    - "DriftAlertStateMachine is the SOLE legal mutator of drift_alerts.state; illegal transitions throw InvalidStateTransitionException; valid transitions write exactly one drift_alert_transitions row inside the same DB transaction"
    - "DriftAlert + DriftAlertTransition Eloquent models exist, use BelongsToUser, declare correct casts, and DriftAlert has three relations (recurringSeries, latestOccurrence, transitions)"
    - "DriftAlertDto and CancellationImpactDto Spatie Data DTOs exist with the documented final-readonly shape"
  artifacts:
    - path: Modules/DriftAlerts/Database/Migrations/2026_05_19_010001_create_drift_alerts_table.php
      provides: drift_alerts schema + state-trigger pair
      contains: CREATE TRIGGER drift_alerts_state_check_insert
    - path: Modules/DriftAlerts/Database/Migrations/2026_05_19_010002_create_drift_alert_transitions_table.php
      provides: drift_alert_transitions audit table
    - path: Modules/Recurring/Database/Migrations/2026_05_19_010002_add_drift_threshold_percent_to_recurring_series.php
      provides: nullable per-series threshold override column
    - path: Modules/Recurring/Database/Migrations/2026_05_19_010003_add_drift_alert_threshold_percent_to_users.php
      provides: per-user global threshold default (unsignedTinyInteger default 5)
    - path: Modules/DriftAlerts/Models/DriftAlert.php
      provides: Eloquent model + BelongsToUser + casts + three relations
    - path: Modules/DriftAlerts/Models/DriftAlertTransition.php
      provides: Eloquent model for audit rows
    - path: Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php
      provides: Sole mutator with PRAGMA busy_timeout + lockForUpdate + transition guard + audit row insert in one transaction
    - path: Modules/DriftAlerts/Public/Dto/DriftAlertDto.php
      provides: Spatie Data DTO with Money fields + immutable_datetime fields + threshold fields
    - path: Modules/DriftAlerts/Public/Dto/CancellationImpactDto.php
      provides: Phase 10 hand-off DTO shape
  key_links:
    - from: Modules/DriftAlerts/Database/Migrations/2026_05_19_010001_create_drift_alerts_table.php
      to: SQLite trigger pair on drift_alerts.state
      via: schema-level CHECK enforcement
      pattern: "CREATE TRIGGER drift_alerts_state_check_"
    - from: Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php
      to: drift_alerts + drift_alert_transitions tables
      via: db connection transaction with lockForUpdate
      pattern: "->lockForUpdate\\(\\)"
    - from: Modules/DriftAlerts/Models/DriftAlert.php
      to: BelongsToUser trait
      via: trait use clause
      pattern: "use BelongsToUser"
---

<objective>
Wave 1 schema + state. Land the four migrations (drift_alerts + drift_alert_transitions + recurring_series.drift_threshold_percent + users.drift_alert_threshold_percent), the two Eloquent models, the two Public DTOs, and the DriftAlertStateMachine sole-mutator with its supporting InvalidStateTransitionException. Mirror Phase 8's RecurringSeriesStateMachine triple-enforcement pattern (schema-level trigger + arch test + runtime guard).

Purpose: Phase 9's analytical surface needs persistent, schema-validated state plus an audit trail (REC-08). Putting the sole-mutator state machine and the audit table in place in Wave 1 means Wave 2's DriftEvaluator can simply INSERT against an already-protected table, and Wave 3's Public Actions can ride on a state-machine that already exists. The user-scoped threshold columns ship alongside so Wave 2 can perform the effective-threshold lookup without a second migration.

Output: A migrated, trigger-protected schema with two factory-backed models, two Spatie DTOs, and a working state machine plus exception pair. Five new unit tests prove the schema, models, DTOs, and state machine all work in isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-subscription-drift-detection-alerts/09-CONTEXT.md
@.planning/phases/09-subscription-drift-detection-alerts/09-RESEARCH.md
@.planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md
@.planning/phases/09-subscription-drift-detection-alerts/09-01-PLAN.md

# Analog source files the executor MUST read (locked Phase 8 patterns):
@Modules/Recurring/Database/Migrations/2026_05_18_010001_create_recurring_series_table.php
@Modules/Recurring/Database/Migrations/2026_05_18_010003_create_recurring_series_transitions_table.php
@Modules/Recurring/Database/Migrations/2026_05_18_010004_add_recurring_settings_to_users.php
@Modules/Recurring/Database/Migrations/2026_05_19_010001_add_cluster_counterparty_key_to_recurring_series.php
@Modules/Recurring/Models/RecurringSeries.php
@Modules/Recurring/Models/RecurringSeriesTransition.php
@Modules/Recurring/Public/Dto/RecurringSeriesDto.php
@Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php
@Modules/Recurring/Internal/StateMachines/InvalidStateTransitionException.php
@Modules/Core/Public/Concerns/BelongsToUser.php
@Modules/Core/Models/User.php

<interfaces>
From Modules/Core/Public/Concerns/BelongsToUser.php (the trait every DriftAlerts model uses):

namespace Modules\Core\Public\Concerns;
trait BelongsToUser {
    public function user(): \Illuminate\Database\Eloquent\Relations\BelongsTo;
}

From Modules/Core/Public/Contracts/Clock.php (injected into DriftAlertStateMachine):

namespace Modules\Core\Public\Contracts;
interface Clock {
    public function now(): \Carbon\CarbonImmutable;
}

From Modules/Ledger/Public/ValueObjects/Money.php (used by both DTOs):

namespace Modules\Ledger\Public\ValueObjects;
final class Money {
    public static function ofMinor(int $minorUnits, string $currency): self;
    public function minorUnits(): int;
    public function currency(): \Brick\Money\Currency;
}

From Modules/Recurring/Models/RecurringSeries.php (cross-module relation target):

namespace Modules\Recurring\Models;
final class RecurringSeries extends \Illuminate\Database\Eloquent\Model {
    // direction, state, cadence, latest_amount_minor, latest_currency, etc.
}
</interfaces>
</context>

<tasks>

<task type="auto" id="9-02-01">
  <name>Task 1: Four migrations + three migration unit tests</name>
  <read_first>
    - Modules/Recurring/Database/Migrations/2026_05_18_010001_create_recurring_series_table.php (entire file - anonymous-Migration class shape, db() resolver, schema() accessor, BEFORE INSERT/UPDATE trigger pair on recurring_series.state; lines 1-119)
    - Modules/Recurring/Database/Migrations/2026_05_18_010003_create_recurring_series_transitions_table.php (entire file - lines 1-73)
    - Modules/Recurring/Database/Migrations/2026_05_19_010001_add_cluster_counterparty_key_to_recurring_series.php (entire file - ALTER TABLE pattern)
    - Modules/Recurring/Database/Migrations/2026_05_18_010004_add_recurring_settings_to_users.php (entire file - ALTER users pattern)
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md (sections: create_drift_alerts_table, create_drift_alert_transitions_table, add_drift_threshold_percent_to_recurring_series, add_drift_alert_threshold_percent_to_users)
    - .planning/phases/09-subscription-drift-detection-alerts/09-RESEARCH.md (drift_alerts schema + drift_alert_transitions schema code blocks)
  </read_first>
  <files>
    - Modules/DriftAlerts/Database/Migrations/2026_05_19_010001_create_drift_alerts_table.php (NEW)
    - Modules/DriftAlerts/Database/Migrations/2026_05_19_010002_create_drift_alert_transitions_table.php (NEW)
    - Modules/Recurring/Database/Migrations/2026_05_19_010002_add_drift_threshold_percent_to_recurring_series.php (NEW; lives in Modules/Recurring/Database/Migrations/ because the column targets recurring_series, per Phase 4 D-80 cross-module migration precedent)
    - Modules/Recurring/Database/Migrations/2026_05_19_010003_add_drift_alert_threshold_percent_to_users.php (NEW; lives in Modules/Recurring/Database/Migrations/ mirroring the existing add_recurring_settings_to_users precedent)
    - Modules/Core/Models/User.php (MODIFY - add cast entry)
    - Modules/DriftAlerts/tests/Unit/DriftAlertsMigrationTest.php (NEW)
    - Modules/DriftAlerts/tests/Unit/DriftAlertTransitionsMigrationTest.php (NEW)
    - Modules/DriftAlerts/tests/Unit/DriftThresholdMigrationsTest.php (NEW)
  </files>
  <action>
    Create four migrations and three unit tests. All four migrations use the anonymous-class shape from Modules/Recurring/Database/Migrations/2026_05_18_010001_create_recurring_series_table.php: `return new class extends Migration { private ?DatabaseManager $resolvedDb = null; public function up(): void { ... } public function down(): void { ... } private function schema(): Builder { ... } private function db(): DatabaseManager { ... } };`. No facade calls; the db() resolver uses `Container::getInstance()->make(DatabaseManager::class)`. Mirror the analog verbatim.

    Migration 1 - create_drift_alerts_table.php. Creates drift_alerts with columns (BIGINT money invariant for FND-04):
    - id() auto-increment PK
    - foreignId('user_id')->nullable()->constrained('users')->cascadeOnDelete() (FND-03 nullable user_id)
    - foreignId('recurring_series_id')->constrained('recurring_series')->cascadeOnDelete()
    - string('state', 24)->default('open') (length 24 forward-compat; trigger pair enforces enum)
    - enum('direction', ['expense', 'income']) (denormalised from recurring_series.direction)
    - bigInteger('baseline_amount_minor') (prior occurrence original_amount_minor)
    - bigInteger('latest_amount_minor') (latest occurrence original_amount_minor)
    - string('currency', 3) (ISO 4217 original currency code)
    - bigInteger('delta_minor') (signed; latest - baseline)
    - bigInteger('annualized_impact_minor') (signed; delta x cadence_multiplier_for_year)
    - unsignedTinyInteger('threshold_percent_used')
    - string('threshold_source', 24) (values 'global' or 'series_override')
    - foreignId('latest_occurrence_id')->constrained('recurring_series_occurrences')->cascadeOnDelete() (idempotency seam)
    - timestamp('snoozed_until')->nullable()
    - timestamp('detected_at')
    - timestamp('actioned_at')->nullable()
    - timestamps() (created_at, updated_at)

    Indexes + constraints (declared inside the same Schema::create closure):
    - unique(['recurring_series_id', 'latest_occurrence_id'], 'drift_alerts_uniq') - idempotency UNIQUE
    - index(['user_id', 'state']) - openCountForUser
    - index(['user_id', 'state', 'detected_at']) - openForUser ORDER BY detected_at DESC
    - index(['user_id', 'recurring_series_id', 'state']) - grouped-by-series query

    Trigger pair (verbatim mirror of the analog's `recurring_series_state_check_insert/update` triggers - analog lines 76-94): inside up() after the Schema::create block, run two `$connection->statement(sprintf(...))` calls creating drift_alerts_state_check_insert (BEFORE INSERT) and drift_alerts_state_check_update (BEFORE UPDATE OF state) with `WHEN NEW.state NOT IN ('open','acknowledged','snoozed','dismissed_cancelled') BEGIN SELECT RAISE(ABORT, 'Invalid drift_alerts.state value'); END`. Drop the triggers explicitly in down() before dropIfExists('drift_alerts').

    Migration 2 - create_drift_alert_transitions_table.php. Verbatim mirror of Modules/Recurring/Database/Migrations/2026_05_18_010003_create_recurring_series_transitions_table.php, swapping recurring_series_transitions to drift_alert_transitions and recurring_series_id to drift_alert_id. Columns:
    - id()
    - foreignId('user_id')->nullable()->constrained('users')->cascadeOnDelete()
    - foreignId('drift_alert_id')->constrained('drift_alerts')->cascadeOnDelete()
    - string('from_state', 24)
    - string('to_state', 24)
    - string('transition_reason', 64) (values: 'user_action', 'user_dismissed_cancelled', 'detector_opened', 'detector_revived_snooze')
    - string('actor', 16) ('user' or 'detector')
    - timestamp('transitioned_at')
    - text('notes')->nullable()
    - timestamps()
    - index(['drift_alert_id', 'transitioned_at'])

    Migration 3 - add_drift_threshold_percent_to_recurring_series.php. File path `Modules/Recurring/Database/Migrations/2026_05_19_010002_add_drift_threshold_percent_to_recurring_series.php`. ALTER TABLE adds `unsignedTinyInteger('drift_threshold_percent')->nullable()->after('variance_tolerance_percent')`. Null means use user / global default. down() drops the column.

    Migration 4 - add_drift_alert_threshold_percent_to_users.php. File path `Modules/Recurring/Database/Migrations/2026_05_19_010003_add_drift_alert_threshold_percent_to_users.php`. ALTER TABLE users adds `unsignedTinyInteger('drift_alert_threshold_percent')->default(5)->after('recurring_income_min_amount_minor')`. Default 5 matches global drift threshold default. down() drops the column.

    After migrations are written:
    - Run `php artisan migrate:fresh --env=testing` to verify they apply.
    - Modify `Modules/Core/Models/User.php` casts() method: add `'drift_alert_threshold_percent' => 'integer'` (mirrors existing recurring_income_min_amount_minor cast).

    Tests:
    - DriftAlertsMigrationTest.php - Asserts drift_alerts table exists with every column at the correct type, four indexes by name, UNIQUE constraint, AND the two triggers exist and ABORT on illegal state values. Synthetic violation: insert with state='invalid' via raw query builder, expect PDOException/QueryException with message containing "Invalid drift_alerts.state value".
    - DriftAlertTransitionsMigrationTest.php - Asserts drift_alert_transitions table exists with all columns + the (drift_alert_id, transitioned_at) index.
    - DriftThresholdMigrationsTest.php - Asserts recurring_series.drift_threshold_percent is nullable AND users.drift_alert_threshold_percent has default 5 (insert a user without specifying the column; expect 5). Both columns are unsignedTinyInteger (cast int returned).

    Use the analog test pattern from Modules/Recurring/tests/Unit/. DI-only - inject DatabaseManager via `app(DatabaseManager::class)`. Tests extend Modules\DriftAlerts\Tests\TestCase.
  </action>
  <verify>
    <automated>php artisan migrate:fresh --env=testing 2>&1 | tail -10 ; vendor/bin/pest --filter "DriftAlertsMigrationTest|DriftAlertTransitionsMigrationTest|DriftThresholdMigrationsTest" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate:fresh --env=testing` succeeds; migrations apply in alphabetical order so the Recurring 010002/010003 ALTERs run before DriftAlerts 010001/010002 CREATEs (verify the ordering before committing).
    - `php artisan tinker --execute "echo Schema::hasTable('drift_alerts') ? 'yes' : 'no';"` prints yes.
    - `php artisan tinker --execute "echo Schema::hasColumn('drift_alerts', 'threshold_source') ? 'yes' : 'no';"` prints yes.
    - `php artisan tinker --execute "echo Schema::hasColumn('recurring_series', 'drift_threshold_percent') ? 'yes' : 'no';"` prints yes.
    - `php artisan tinker --execute "echo Schema::hasColumn('users', 'drift_alert_threshold_percent') ? 'yes' : 'no';"` prints yes.
    - All three migration tests are green.
    - Synthetic trigger check: attempting to insert a drift_alerts row with state='bogus' throws an exception whose message contains "Invalid drift_alerts.state value".
    - Modules/Core/Models/User.php casts() now includes the 'drift_alert_threshold_percent' => 'integer' entry. `grep "drift_alert_threshold_percent.*integer" Modules/Core/Models/User.php` returns 1 line.
    - `vendor/bin/phpstan analyse Modules/DriftAlerts/Database Modules/Recurring/Database/Migrations/2026_05_19_010002_add_drift_threshold_percent_to_recurring_series.php Modules/Recurring/Database/Migrations/2026_05_19_010003_add_drift_alert_threshold_percent_to_users.php Modules/Core/Models/User.php --memory-limit=2G` is green.
    - `vendor/bin/pint --test Modules/DriftAlerts/Database/Migrations/ Modules/Recurring/Database/Migrations/` is green.
  </acceptance_criteria>
  <done>
    Four migrations apply cleanly in CI-style fresh migrate; trigger pair rejects illegal state values at SQLite level; three threshold-related columns exist with correct types and defaults; three migration tests are green; User model casts updated.
  </done>
</task>

<task type="auto" id="9-02-02">
  <name>Task 2: Eloquent models (DriftAlert + DriftAlertTransition) + two Public DTOs + DTO unit test</name>
  <read_first>
    - Modules/Recurring/Models/RecurringSeries.php (entire file - class shape + BelongsToUser trait + fillable + casts + relations + booted hook)
    - Modules/Recurring/Models/RecurringSeriesTransition.php (entire file - analog audit model)
    - Modules/Recurring/Public/Dto/RecurringSeriesDto.php (entire file - Spatie Data DTO shape with Money fields + immutable_datetime fields)
    - Modules/Core/Public/Concerns/BelongsToUser.php (entire file - trait contract)
    - Modules/Ledger/Public/ValueObjects/Money.php (entire file - Money::ofMinor + minorUnits + currency)
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md (sections: Models/DriftAlert, Models/DriftAlertTransition, Public/Dto/DriftAlertDto, Public/Dto/CancellationImpactDto)
  </read_first>
  <files>
    - Modules/DriftAlerts/Models/DriftAlert.php (NEW)
    - Modules/DriftAlerts/Models/DriftAlertTransition.php (NEW)
    - Modules/DriftAlerts/Public/Dto/DriftAlertDto.php (NEW)
    - Modules/DriftAlerts/Public/Dto/CancellationImpactDto.php (NEW)
    - Modules/DriftAlerts/tests/Unit/DriftAlertDtoTest.php (NEW)
  </files>
  <action>
    Create the two Eloquent models, two Public Spatie Data DTOs, and a round-trip DTO unit test.

    Modules/DriftAlerts/Models/DriftAlert.php: `namespace Modules\DriftAlerts\Models; final class DriftAlert extends \Illuminate\Database\Eloquent\Model`. Imports: Carbon\CarbonImmutable, Illuminate\Database\Eloquent\Relations\BelongsTo, Illuminate\Database\Eloquent\Relations\HasMany, Modules\Core\Public\Concerns\BelongsToUser, Modules\Recurring\Models\RecurringSeries, Modules\Recurring\Models\RecurringSeriesOccurrence. Use BelongsToUser trait. Full property docblock with @property annotations for every column (id, user_id, recurring_series_id, state, direction, baseline_amount_minor, latest_amount_minor, currency, delta_minor, annualized_impact_minor, threshold_percent_used, threshold_source, latest_occurrence_id, snoozed_until, detected_at, actioned_at, created_at, updated_at) with cast types.

    protected $fillable: list of all 15 writable columns.
    protected function casts(): returns minor-unit columns to 'integer', timestamps to 'immutable_datetime', threshold_percent_used to 'integer'. State/direction/currency/threshold_source stay strings.

    Relations:
    - recurringSeries(): BelongsTo -> belongsTo(RecurringSeries::class, 'recurring_series_id')
    - latestOccurrence(): BelongsTo -> belongsTo(RecurringSeriesOccurrence::class, 'latest_occurrence_id')
    - transitions(): HasMany -> hasMany(DriftAlertTransition::class)

    Static factory binding (mirrors Modules/Recurring/Models/RecurringSeries.php): `protected static function newFactory() { return DriftAlertFactory::new(); }` so DriftAlert::factory() works (factory from Plan 09-01).

    Modules/DriftAlerts/Models/DriftAlertTransition.php: Verbatim mirror of Modules/Recurring/Models/RecurringSeriesTransition.php, swapping recurring_series_id to drift_alert_id and relation method `series()` to `alert(): BelongsTo` returning belongsTo(DriftAlert::class, 'drift_alert_id'). Use BelongsToUser. $fillable = ['user_id', 'drift_alert_id', 'from_state', 'to_state', 'transition_reason', 'actor', 'transitioned_at', 'notes']. Casts: transitioned_at / created_at / updated_at all 'immutable_datetime'. Static factory binding to DriftAlertTransitionFactory.

    Modules/DriftAlerts/Public/Dto/DriftAlertDto.php: `namespace Modules\DriftAlerts\Public\Dto; final class DriftAlertDto extends \Spatie\LaravelData\Data`. Constructor with readonly public properties:
    - int $driftAlertId
    - int $recurringSeriesId
    - string $direction ('expense' or 'income')
    - string $displayName
    - string $state (open/acknowledged/snoozed/dismissed_cancelled)
    - Money $baselineAmount (in original currency)
    - Money $latestAmount (in original currency)
    - Money $delta (signed; in original currency)
    - Money $annualizedImpact (signed; in original currency)
    - ?Money $eurEquivalent (null when currency='EUR')
    - int $thresholdPercentUsed
    - string $thresholdSource ('global' or 'series_override')
    - CarbonImmutable $detectedAt
    - ?CarbonImmutable $actionedAt
    - ?CarbonImmutable $snoozedUntil

    Modules/DriftAlerts/Public/Dto/CancellationImpactDto.php: `namespace Modules\DriftAlerts\Public\Dto; final class CancellationImpactDto extends \Spatie\LaravelData\Data`. Readonly public properties:
    - int $recurringSeriesId
    - Money $monthlySavings
    - Money $annualSavings
    - string $currency (the recurring series's latest_currency - NOT necessarily 'EUR')

    PHPDoc on CancellationImpactDto: "Phase 10 hand-off contract - projected savings if the user cancels a recurring series. Read-only; computed from recurring_series.monthly_equivalent_minor. The currency field is the recurring series's original currency, NOT necessarily EUR." NO D-numbers, NO REQ-IDs in PHPDoc (GSD-agnostic invariant).

    DriftAlertDtoTest.php: Pest unit test that constructs DriftAlertDto with synthetic values (EUR + USD scenarios), asserts every property returns the value passed in, asserts readonly properties throw \Error on reassignment. Similar test for CancellationImpactDto. Use the factory from Plan 09-01 to seed a DriftAlert model row, then construct DTO from it.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/DriftAlerts/tests/Unit/DriftAlertDtoTest.php 2>&1 | tail -20 ; vendor/bin/phpstan analyse Modules/DriftAlerts/Models Modules/DriftAlerts/Public/Dto --memory-limit=2G 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DriftAlert and DriftAlertTransition Eloquent classes parse, can be hydrated via factory(), and read/write to the migrated tables.
    - `php artisan tinker --execute "echo (new Modules\\DriftAlerts\\Models\\DriftAlert())->getTable();"` prints `drift_alerts`.
    - `php artisan tinker --execute "echo (new Modules\\DriftAlerts\\Models\\DriftAlertTransition())->getTable();"` prints `drift_alert_transitions`.
    - DriftAlert has three relation methods: recurringSeries(), latestOccurrence(), transitions(). Verified via reflection: `php -r "require 'vendor/autoload.php'; \$r = new ReflectionClass(Modules\\DriftAlerts\\Models\\DriftAlert::class); foreach (['recurringSeries', 'latestOccurrence', 'transitions'] as \$m) { var_dump(\$r->hasMethod(\$m)); }"` prints three `bool(true)` lines.
    - DriftAlertDto and CancellationImpactDto extend Spatie\LaravelData\Data; properties are readonly.
    - DriftAlertDtoTest passes including a USD scenario where eurEquivalent is non-null and an EUR scenario where eurEquivalent is null.
    - PHPStan against Modules/DriftAlerts/Models and Modules/DriftAlerts/Public/Dto is green (no missing-class errors; the state machine + actions are still missing from Wave 1 perspective for OTHER reads, but the models and DTOs themselves must be 100% clean).
    - Pint --test is green for the new files.
  </acceptance_criteria>
  <done>
    Two Eloquent models hydrate against the migrated tables, two Spatie DTOs round-trip correctly through the unit test, PHPStan + Pint stay green on the new artefacts.
  </done>
</task>

<task type="auto" id="9-02-03">
  <name>Task 3: DriftAlertStateMachine + InvalidStateTransitionException + state machine unit test</name>
  <read_first>
    - Modules/Recurring/Internal/StateMachines/RecurringSeriesStateMachine.php (entire file - the sole-mutator pattern. Read lines 1-172: ALLOWED_TRANSITIONS map, ALLOWED_ACTORS, constructor DI on DatabaseManager + Clock, transition() envelope with DB transaction + PRAGMA busy_timeout=5000 + lockForUpdate + guardTransition + audit row insert, helpers toInt/toString/toIntOrNull/guardTransition)
    - Modules/Recurring/Internal/StateMachines/InvalidStateTransitionException.php (entire file - the exception shape)
    - Modules/Recurring/tests/Unit/RecurringSeriesStateMachineTest.php (entire file - test pattern: transitions test matrix, illegal transition assertion, audit row count assertion)
    - Modules/Core/Public/Contracts/Clock.php (Clock contract)
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md (DriftAlertStateMachine + InvalidStateTransitionException sections)
    - .planning/phases/09-subscription-drift-detection-alerts/09-RESEARCH.md (Pattern 1 + Pitfall 5 + Component Responsibilities table row for DriftAlertStateMachine)
  </read_first>
  <files>
    - Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php (NEW)
    - Modules/DriftAlerts/Internal/StateMachines/InvalidStateTransitionException.php (NEW)
    - Modules/DriftAlerts/tests/Unit/DriftAlertStateMachineTest.php (NEW)
  </files>
  <action>
    Create the sole-mutator state machine, its exception class, and a comprehensive unit test.

    Modules/DriftAlerts/Internal/StateMachines/InvalidStateTransitionException.php: Verbatim mirror of Modules/Recurring/Internal/StateMachines/InvalidStateTransitionException.php. `namespace Modules\DriftAlerts\Internal\StateMachines; use RuntimeException; final class InvalidStateTransitionException extends RuntimeException { public static function forTransition(int $alertId, string $from, string $to): self { return new self("Illegal drift_alerts transition for id={$alertId}: {$from} -> {$to}"); } }`.

    Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php: `namespace Modules\DriftAlerts\Internal\StateMachines; final class DriftAlertStateMachine`. Imports: Illuminate\Database\DatabaseManager, InvalidArgumentException, Modules\Core\Public\Contracts\Clock, Modules\DriftAlerts\Models\DriftAlert, RuntimeException.

    Class-level PHPDoc: "The single legal mutator of drift_alerts.state and the sole inserter into drift_alert_transitions. Other module code reads the row and may UPDATE non-state columns; the schema-level trigger pair plus the BoundaryArchTest invariant noOtherDriftAlertStateMutator enforce the sole-mutator contract. Public surface mirrors RecurringSeriesStateMachine: a single transition() method that opens a transaction, sets PRAGMA busy_timeout = 5000, takes a row lock, validates against ALLOWED_TRANSITIONS, writes the state + updated_at, and inserts one audit row inside the same transaction." NO D-numbers, NO REQ-IDs.

    Class body:
    - private const ALLOWED_TRANSITIONS: ['open' => ['acknowledged', 'snoozed', 'dismissed_cancelled'], 'acknowledged' => [], 'snoozed' => ['open', 'acknowledged', 'dismissed_cancelled'], 'dismissed_cancelled' => []]. acknowledged and dismissed_cancelled are terminal (empty target arrays).
    - private const ALLOWED_ACTORS: ['user', 'detector'].
    - Constructor injects DatabaseManager $db and Clock $clock as private readonly properties.
    - public function transition(DriftAlert $alert, string $toState, string $reason, string $actor, ?string $notes = null, array $extraColumns = []): void

    transition() body (mirror analog lines 76-135 verbatim):
    1. Validate actor: if not in ALLOWED_ACTORS, throw InvalidArgumentException with explicit list.
    2. alertId = toInt($alert->id).
    3. $this->db->connection()->transaction(function () use (...): void { ... }):
       a. $connection = $this->db->connection(); $connection->statement('PRAGMA busy_timeout = 5000');
       b. $row = $connection->table('drift_alerts')->where('id', $alertId)->lockForUpdate()->first();
       c. if ($row === null) throw new RuntimeException("DriftAlertStateMachine: drift_alerts row {$alertId} not found.");
       d. $currentState = toString($row->state); $this->guardTransition($alertId, $currentState, $toState);
       e. $now = $this->clock->now()->toDateTimeString();
       f. $update = array_merge($extraColumns, ['state' => $toState, 'updated_at' => $now]);
       g. $connection->table('drift_alerts')->where('id', $alertId)->update($update);
       h. $userId = toIntOrNull($row->user_id);
       i. $connection->table('drift_alert_transitions')->insert(['user_id' => $userId, 'drift_alert_id' => $alertId, 'from_state' => $currentState, 'to_state' => $toState, 'transition_reason' => $reason, 'actor' => $actor, 'transitioned_at' => $now, 'notes' => $notes, 'created_at' => $now, 'updated_at' => $now]);

    Helpers (verbatim from analog lines 138-171): guardTransition(int $alertId, string $from, string $to): void - if $to not in (self::ALLOWED_TRANSITIONS[$from] ?? []), throw InvalidStateTransitionException::forTransition($alertId, $from, $to). toInt(mixed $v): int - cast scalar to int with strict guard. toIntOrNull(mixed $v): ?int - same but null-safe. toString(mixed $v): string - strict cast.

    DriftAlertStateMachineTest.php (Pest, extends Modules\DriftAlerts\Tests\TestCase + RefreshDatabase):
    - dataset 'allowed_transitions' covering every valid transition: (open, acknowledged, user_action, user), (open, snoozed, user_action, user), (open, dismissed_cancelled, user_dismissed_cancelled, user), (snoozed, open, detector_revived_snooze, detector), (snoozed, acknowledged, user_action, user), (snoozed, dismissed_cancelled, user_dismissed_cancelled, user). For each: seed a DriftAlert factory row at the from-state, call transition(...), assert drift_alerts.state column changed AND exactly one drift_alert_transitions row exists for this alert.
    - dataset 'illegal_transitions' covering every invalid transition: (acknowledged, snoozed), (acknowledged, open), (dismissed_cancelled, open), (dismissed_cancelled, acknowledged). For each: seed a DriftAlert factory at from-state, call transition(...), expect InvalidStateTransitionException thrown AND drift_alerts.state unchanged AND zero new drift_alert_transitions rows.
    - test 'rejects unknown actor': call transition with $actor='admin', expect InvalidArgumentException.
    - test 'rejects missing row': call transition with a DriftAlert whose id does not exist, expect RuntimeException.
    - test 'snooze extraColumns persist': transition(open -> snoozed) with extraColumns=['snoozed_until' => '2026-06-01 12:00:00'], assert drift_alerts.snoozed_until = '2026-06-01 12:00:00'.
    - test 'transition row records timestamps + actor + reason': call transition(open -> acknowledged, 'user_action', 'user', notes='custom note'). Assert the new drift_alert_transitions row has from_state='open', to_state='acknowledged', transition_reason='user_action', actor='user', notes='custom note', and transitioned_at is the Clock's now() (use Clock fake / setTestNow).
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/DriftAlerts/tests/Unit/DriftAlertStateMachineTest.php 2>&1 | tail -30 ; vendor/bin/phpstan analyse Modules/DriftAlerts/Internal/StateMachines --memory-limit=2G 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - DriftAlertStateMachine and InvalidStateTransitionException both parse and are PHPStan-clean.
    - DriftAlertStateMachineTest passes ALL cases: at minimum 6 allowed transitions, 4 illegal transitions, unknown-actor case, missing-row case, snooze-extraColumns case, transition-metadata case.
    - `grep -E "static::ALLOWED_TRANSITIONS|self::ALLOWED_TRANSITIONS" Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php` returns at least one line.
    - `grep "PRAGMA busy_timeout = 5000" Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php` returns 1 line.
    - `grep "lockForUpdate" Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php` returns 1 line.
    - Boundary check: running `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter=noOtherDriftAlertStateMutator` stays GREEN (the state machine file is the allowed mutator; no other file mutates drift_alerts.state yet).
    - Boundary check: `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter=noFacadeCallsFromDriftAlerts` stays GREEN (state machine uses constructor-injected DatabaseManager, no facade).
    - PHPStan strict-rules pass against Modules/DriftAlerts/Internal/StateMachines/.
    - Pint --test is green.
  </acceptance_criteria>
  <done>
    DriftAlertStateMachine is the working sole mutator with proper transaction/lock semantics, the exception class is in place, the test exhaustively proves the transition matrix, and both BoundaryArchTest invariants (noOtherDriftAlertStateMutator + noFacadeCallsFromDriftAlerts) remain green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eloquent model -> SQLite | All persistence flows through Eloquent; the trigger pair is the schema-level safety net |
| State machine -> drift_alerts table | The only legal mutator of drift_alerts.state; sole-mutator invariant + DB trigger + arch test triple-enforce |
| Drift alerts user_id -> requesting user | Every read/write is scoped by user_id (FND-03); ensures multi-user data isolation when v2 ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-03 | Tampering | Code path attempts an illegal drift_alerts state transition (e.g. acknowledged -> open) | mitigate | Three layers: (1) DriftAlertStateMachine::guardTransition checks ALLOWED_TRANSITIONS at runtime and throws InvalidStateTransitionException; (2) the BEFORE UPDATE OF state trigger ABORTs at SQLite level if state is outside the allowed enum; (3) noOtherDriftAlertStateMutator arch test (Plan 09-01) ensures only the state machine file can run such UPDATEs. Proven by the DriftAlertStateMachineTest illegal-transitions dataset |
| T-9-03b | Tampering | Direct DB::table('drift_alerts')->update(['state' => ...]) call from outside the state machine | mitigate | (1) DB trigger ABORTS on illegal enum values; (2) noOtherDriftAlertStateMutator filesystem-walk arch test rejects any non-allowed-file write to the state column; (3) sole-mutator pattern is documented in DriftAlertStateMachine PHPDoc |
| T-9-03c | Information Disclosure | Cross-user audit row exposure via drift_alert_transitions table | mitigate | drift_alert_transitions.user_id is set from the locked drift_alerts row inside the same transaction; BelongsToUser trait + every future query scoped by user_id |
</threat_model>

<verification>
Wave 1 phase-level checks:
- `php artisan migrate:fresh --env=testing` succeeds.
- `vendor/bin/pest Modules/DriftAlerts/tests/Unit/` is green (all 5 unit tests: 3 migrations + DTO + state machine).
- `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` is green (Wave 0 invariants still hold).
- `vendor/bin/phpstan analyse Modules/DriftAlerts --memory-limit=2G` reports only missing-class errors for Wave 2-4 deliverables (DriftEvaluator, DetectDriftAlertsJob, EvaluateDriftOnMetricsRefreshed, DriftPage, DashboardDriftBadge, three Public Actions, two Public Services). Models, DTOs, state machine, migrations themselves are clean.
- `vendor/bin/pint --test` is green for all new files.
</verification>

<success_criteria>
- All four migrations apply cleanly; schema matches the documented column set; trigger pair enforces enum at SQLite level.
- Two Eloquent models hydrate against the tables via factories.
- Two Public DTOs construct + round-trip correctly.
- DriftAlertStateMachine is the working sole-mutator with PRAGMA busy_timeout + lockForUpdate + transition guard + audit insert in one transaction.
- Five new unit tests are green.
- All BoundaryArchTest invariants from Wave 0 remain green; the new code does not violate any.
</success_criteria>

<output>
Create `.planning/phases/09-subscription-drift-detection-alerts/09-02-SUMMARY.md` when done.
</output>
