---
phase: 09-subscription-drift-detection-alerts
plan: 04
plan_id: "09-04"
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - Modules/DriftAlerts/Public/Actions/AcknowledgeDriftAlert.php
  - Modules/DriftAlerts/Public/Actions/SnoozeDriftAlert.php
  - Modules/DriftAlerts/Public/Actions/DismissDriftAlertAsCancelled.php
  - Modules/DriftAlerts/Public/Events/DriftAlertAcknowledged.php
  - Modules/DriftAlerts/Public/Events/DriftAlertSnoozed.php
  - Modules/DriftAlerts/Public/Events/DriftAlertDismissedCancelled.php
  - Modules/DriftAlerts/Public/Services/DriftAlertQuery.php
  - Modules/DriftAlerts/Internal/Mapping/DriftAlertDtoMapper.php
  - Modules/DriftAlerts/Internal/Http/Livewire/DriftPage.php
  - Modules/DriftAlerts/Internal/Http/Livewire/DashboardDriftBadge.php
  - Modules/DriftAlerts/Resources/views/livewire/drift-page.blade.php
  - Modules/DriftAlerts/Resources/views/livewire/dashboard-drift-badge.blade.php
  - resources/views/dashboard.blade.php
  - resources/views/components/core/livewire/top-nav.blade.php
  - Modules/DriftAlerts/tests/Feature/DriftPageTest.php
  - Modules/DriftAlerts/tests/Feature/AcknowledgeDriftAlertTest.php
  - Modules/DriftAlerts/tests/Feature/SnoozeDriftAlertTest.php
  - Modules/DriftAlerts/tests/Feature/DismissDriftAlertAsCancelledTest.php
  - Modules/DriftAlerts/tests/Feature/DriftAlertCrossUser404Test.php
  - Modules/DriftAlerts/tests/Feature/DashboardDriftBadgeTest.php
  - Modules/DriftAlerts/tests/Feature/TopNavDriftBadgeTest.php
autonomous: false
requirements:
  - REC-07
  - REC-08
user_setup: []
must_haves:
  truths:
    - "/drift route is registered (Modules/DriftAlerts/Routes/web.php Plan 09-01), mounts DriftPage Livewire SFC, sits behind web + auth middleware + LoopbackOnly (Phase 1 carry-forward), and the page renders Open / History / Dismissed tabs with #[Url] tab state"
    - "/drift Open tab lists open alerts sorted by detected_at DESC, grouped by series visually only; multiple alerts sharing recurring_series_id collapse under a grouped header using <flux:card> + Alpine x-data='{open: false}' (D-905)"
    - "Per-row Acknowledge / Snooze (popover reusing Phase 8 D-810 chrome) / I-cancelled-this actions invoke the three Public Actions and dispatch a toast on success. Dismiss-as-cancelled is a first-class user intent: it flips drift_alerts.state to dismissed_cancelled, writes one drift_alert_transitions audit row with transition_reason='user_dismissed_cancelled', emits DriftAlertDismissedCancelled, and NEVER mutates recurring_series.state (D-920)"
    - "Acknowledging a drift alert closes only the alert that was acted on; it has NO special effect on future drift detection — the immediately-prior baseline rule is symmetric, so if the price stabilises no future alert fires, and if it drifts again a fresh alert opens (D-906)"
    - "Direction-aware UI copy + math: drift fires on BOTH expense AND income series; the direction column on drift_alerts is denormalised from recurring_series.direction for query speed; row chrome renders rose-leaning vs emerald-leaning glyphs accordingly (D-916)"
    - "AcknowledgeDriftAlert / SnoozeDriftAlert / DismissDriftAlertAsCancelled actions throw NotFoundHttpException when invoked cross-user (Pest test proves)"
    - "Each Public Action writes one drift_alert_transitions row via DriftAlertStateMachine AND dispatches the corresponding Public event"
    - "DashboardDriftBadge Livewire SFC renders a count tile on / when DriftAlertQuery::openCountForUser > 0; tile is hidden when count is 0"
    - "Top-nav 'Recurring' slot renders a compound badge {pendingCount}+{driftCount} when both are non-zero, using bg-rose-50/text-rose-700 with an arrow-trending-up glyph for the drift secondary chip"
    - "DriftAlertQuery exposes openForUser, historyForUser, dismissedForUser (cursor-paginated with detected_at DESC, id DESC), openCountForUser, totalOpenAnnualizedImpactForUser methods"
  artifacts:
    - path: Modules/DriftAlerts/Public/Actions/AcknowledgeDriftAlert.php
    - path: Modules/DriftAlerts/Public/Actions/SnoozeDriftAlert.php
    - path: Modules/DriftAlerts/Public/Actions/DismissDriftAlertAsCancelled.php
    - path: Modules/DriftAlerts/Public/Services/DriftAlertQuery.php
    - path: Modules/DriftAlerts/Internal/Http/Livewire/DriftPage.php
    - path: Modules/DriftAlerts/Internal/Http/Livewire/DashboardDriftBadge.php
    - path: Modules/DriftAlerts/Resources/views/livewire/drift-page.blade.php
    - path: Modules/DriftAlerts/Resources/views/livewire/dashboard-drift-badge.blade.php
  key_links:
    - from: routes/web.php (Plan 09-01)
      to: Modules/DriftAlerts/Internal/Http/Livewire/DriftPage.php
      via: class-as-handler Route::get '/drift'
      pattern: "Route::get\\('/drift', DriftPage::class\\)"
    - from: Modules/DriftAlerts/Internal/Http/Livewire/DriftPage.php
      to: AcknowledgeDriftAlert + SnoozeDriftAlert + DismissDriftAlertAsCancelled
      via: method-parameter DI on action methods
      pattern: "AcknowledgeDriftAlert \\$action"
    - from: Modules/DriftAlerts/Public/Actions/*
      to: drift_alerts row + DriftAlertStateMachine + drift_alert_transitions
      via: action invokes state machine which writes both rows atomically
      pattern: "stateMachine->transition"
---

<objective>
Wave 3 user-facing surface. Land the three Public Actions (Acknowledge / Snooze / Dismiss-as-cancelled), the matching Public Events, the DriftAlertQuery read service, the internal DriftAlertDtoMapper, the DriftPage Livewire SFC + Blade view, the DashboardDriftBadge tile, and the top-nav compound badge composer integration. Cross-user 404 isolation is enforced by every action + every query method.

Purpose: After Wave 3 the user can see drift alerts (REC-07: persistent surface + count badge + cannot be silently missed) AND act on them (REC-08: three actions, each writing a timestamped transition row for auditability). The visual layout follows 09-UI-SPEC.md verbatim. The single human-verify checkpoint at the end of this plan confirms the calm-aesthetic + grouped-by-series collapsible + direction-aware copy + EUR-shadow rendering all work in the browser - the things automated tests cannot prove.

Output: A working /drift page accessible at https://diederik.test/drift, a dashboard tile that disappears when no alerts exist, a compound top-nav badge that surfaces drift signal on the existing Recurring slot, and seven new Pest feature tests proving the action layer + cross-user 404 + composer behaviour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-subscription-drift-detection-alerts/09-CONTEXT.md
@.planning/phases/09-subscription-drift-detection-alerts/09-RESEARCH.md
@.planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md
@.planning/phases/09-subscription-drift-detection-alerts/09-UI-SPEC.md
@.planning/phases/09-subscription-drift-detection-alerts/09-01-PLAN.md
@.planning/phases/09-subscription-drift-detection-alerts/09-02-PLAN.md
@.planning/phases/09-subscription-drift-detection-alerts/09-03-PLAN.md

# Analog source files the executor MUST read:
@Modules/Recurring/Public/Actions/ApproveRecurringSeries.php
@Modules/Recurring/Public/Actions/SnoozeRecurringSeries.php
@Modules/Recurring/Public/Actions/RejectRecurringSeries.php
@Modules/Recurring/Public/Services/RecurringSeriesQuery.php
@Modules/Recurring/Internal/Mapping/RecurringSeriesDtoMapper.php
@Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php
@Modules/Recurring/Internal/Http/Livewire/FixedPaymentsCard.php
@Modules/Recurring/Resources/views/livewire/recurring-review-page.blade.php
@Modules/Recurring/Resources/views/livewire/fixed-payments-card.blade.php
@Modules/Recurring/Providers/RecurringServiceProvider.php
@resources/views/components/core/livewire/top-nav.blade.php
@resources/views/dashboard.blade.php
@Modules/Recurring/tests/Feature/ApproveRecurringSeriesTest.php
@Modules/Recurring/tests/Feature/RecurringReviewPageTest.php
@Modules/Recurring/tests/Feature/CrossUserRecurringSeriesIsolationTest.php
@Modules/Recurring/tests/Feature/TopNavBadgeComposerTest.php
@Modules/Recurring/tests/Feature/FixedPaymentsCardTest.php

<interfaces>
From Modules/DriftAlerts/Public/Dto/DriftAlertDto.php (Plan 09-02 - the DTO DriftAlertQuery returns):

namespace Modules\DriftAlerts\Public\Dto;
final class DriftAlertDto extends \Spatie\LaravelData\Data {
    public function __construct(
        public readonly int $driftAlertId,
        public readonly int $recurringSeriesId,
        public readonly string $direction,
        public readonly string $displayName,
        public readonly string $state,
        public readonly Money $baselineAmount,
        public readonly Money $latestAmount,
        public readonly Money $delta,
        public readonly Money $annualizedImpact,
        public readonly ?Money $eurEquivalent,
        public readonly int $thresholdPercentUsed,
        public readonly string $thresholdSource,
        public readonly CarbonImmutable $detectedAt,
        public readonly ?CarbonImmutable $actionedAt,
        public readonly ?CarbonImmutable $snoozedUntil,
    ) {}
}

From Modules/DriftAlerts/Internal/StateMachines/DriftAlertStateMachine.php (Plan 09-02):

public function transition(
    DriftAlert $alert,
    string $toState,
    string $reason,
    string $actor,
    ?string $notes = null,
    array $extraColumns = [],
): void;
</interfaces>
</context>

<tasks>

<task type="auto" id="9-04-01">
  <name>Task 1: Three Public Actions + three Public Events + cross-user 404 feature tests</name>
  <read_first>
    - Modules/Recurring/Public/Actions/ApproveRecurringSeries.php (entire file - the canonical Public Action shape: constructor DI on state machine + dispatcher + clock, __invoke(int, User), cross-user where + first + NotFoundHttpException, idempotency guard, state machine call, event dispatch)
    - Modules/Recurring/Public/Actions/SnoozeRecurringSeries.php (entire file - the snooze pattern with extraColumns)
    - Modules/Recurring/Public/Actions/RejectRecurringSeries.php (entire file - the reject pattern with notes)
    - Modules/Recurring/Public/Events/RecurringSeriesApproved.php (analog event shape)
    - Modules/Recurring/tests/Feature/ApproveRecurringSeriesTest.php (entire file - test pattern for Public Actions: happy path + cross-user 404 + idempotency + state machine call + event dispatch + transition row assertion)
    - Modules/Recurring/tests/Feature/CrossUserRecurringSeriesIsolationTest.php (entire file - the dedicated cross-user test pattern)
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md (sections: Public/Actions/AcknowledgeDriftAlert + SnoozeDriftAlert + DismissDriftAlertAsCancelled + Public/Events/DriftAlertAcknowledged + Cross-user 404 invariant)
  </read_first>
  <files>
    - Modules/DriftAlerts/Public/Events/DriftAlertAcknowledged.php (NEW)
    - Modules/DriftAlerts/Public/Events/DriftAlertSnoozed.php (NEW)
    - Modules/DriftAlerts/Public/Events/DriftAlertDismissedCancelled.php (NEW)
    - Modules/DriftAlerts/Public/Actions/AcknowledgeDriftAlert.php (NEW)
    - Modules/DriftAlerts/Public/Actions/SnoozeDriftAlert.php (NEW)
    - Modules/DriftAlerts/Public/Actions/DismissDriftAlertAsCancelled.php (NEW)
    - Modules/DriftAlerts/tests/Feature/AcknowledgeDriftAlertTest.php (NEW)
    - Modules/DriftAlerts/tests/Feature/SnoozeDriftAlertTest.php (NEW)
    - Modules/DriftAlerts/tests/Feature/DismissDriftAlertAsCancelledTest.php (NEW)
    - Modules/DriftAlerts/tests/Feature/DriftAlertCrossUser404Test.php (NEW)
  </files>
  <action>
    Three Public Events (all `final readonly class` with public-property constructors in namespace Modules\DriftAlerts\Public\Events):
    - DriftAlertAcknowledged: int $userId, int $driftAlertId, CarbonImmutable $acknowledgedAt.
    - DriftAlertSnoozed: int $userId, int $driftAlertId, CarbonImmutable $snoozedUntil.
    - DriftAlertDismissedCancelled: int $userId, int $driftAlertId, int $recurringSeriesId.

    Each event has a one-line PHPDoc describing what triggers it. No D-numbers, no REQ-IDs.

    Three Public Actions in namespace Modules\DriftAlerts\Public\Actions. Each follows the canonical shape from Modules/Recurring/Public/Actions/ApproveRecurringSeries.php:

    AcknowledgeDriftAlert: Constructor injects DriftAlertStateMachine + Dispatcher + Clock as private readonly. __invoke(int $alertId, User $user): void:
    1. Fetch via DriftAlert::query()->where('id', $alertId)->where('user_id', $user->id)->first();
    2. If null, throw new NotFoundHttpException('Drift alert not found.');
    3. Idempotency guard: if ($alert->state === 'acknowledged') return;
    4. $now = $this->clock->now();
    5. Call $this->stateMachine->transition($alert, 'acknowledged', 'user_action', 'user', null, ['actioned_at' => $now->toDateTimeString()]);
    6. Dispatch new DriftAlertAcknowledged(userId: $user->id, driftAlertId: $alertId, acknowledgedAt: $now);

    SnoozeDriftAlert: Constructor injects DriftAlertStateMachine + Dispatcher (no Clock - the caller passes the until timestamp). __invoke(int $alertId, User $user, CarbonImmutable $until): void:
    1. Same cross-user fetch + 404.
    2. Idempotency guard: if ($alert->state === 'snoozed' && $alert->snoozed_until !== null && $alert->snoozed_until->toDateTimeString() === $until->toDateTimeString()) return;
    3. $untilString = $until->toDateTimeString();
    4. Call transition($alert, 'snoozed', 'user_action', 'user', 'snoozed_until='.$untilString, ['snoozed_until' => $untilString]);
    5. Dispatch new DriftAlertSnoozed(userId: $user->id, driftAlertId: $alertId, snoozedUntil: $until);

    DismissDriftAlertAsCancelled: Constructor injects DriftAlertStateMachine + Dispatcher + Clock. __invoke(int $alertId, User $user): void:
    1. Cross-user fetch + 404.
    2. Idempotency guard: if ($alert->state === 'dismissed_cancelled') return;
    3. $now = $this->clock->now();
    4. transition($alert, 'dismissed_cancelled', 'user_dismissed_cancelled', 'user', null, ['actioned_at' => $now->toDateTimeString()]);
    5. Dispatch new DriftAlertDismissedCancelled(userId: $user->id, driftAlertId: $alertId, recurringSeriesId: $alert->recurring_series_id);

    Crucially: NONE of the three actions modifies recurring_series in any way. The noRecurringSeriesWritesFromDriftAlerts arch test stays green.

    Four Pest feature tests (extend Modules\DriftAlerts\Tests\TestCase + RefreshDatabase):

    AcknowledgeDriftAlertTest:
    - 'acknowledges an open alert' - seed a drift_alerts row in state='open'; resolve AcknowledgeDriftAlert from container; invoke; assert drift_alerts.state='acknowledged', actioned_at is set, exactly 1 new drift_alert_transitions row exists with from_state='open' / to_state='acknowledged' / transition_reason='user_action' / actor='user'.
    - 'dispatches DriftAlertAcknowledged event' - Event::fake([DriftAlertAcknowledged::class]); invoke; Event::assertDispatched(DriftAlertAcknowledged::class, fn ($e) => $e->driftAlertId === $alertId && $e->userId === $user->id).
    - 'is idempotent on already-acknowledged' - seed alert in state='acknowledged'; invoke; assert NO new transition row added; state unchanged.
    - 'throws NotFoundHttpException on cross-user invocation' - seed alert for user A; invoke as user B; expect NotFoundHttpException with message containing 'Drift alert not found'.
    - 'throws NotFoundHttpException on missing alert id' - invoke with id=99999; expect NotFoundHttpException.

    SnoozeDriftAlertTest:
    - 'snoozes an open alert with snoozed_until' - seed open alert; invoke with $until = CarbonImmutable::now()->addWeek(); assert state='snoozed', snoozed_until matches; assert event dispatched; assert 1 transition row with reason='user_action', notes containing 'snoozed_until='.
    - 'is idempotent on same snoozed_until' - seed snoozed alert with X; invoke with same X; assert no new transition row added.
    - 'cross-user 404' (same pattern).

    DismissDriftAlertAsCancelledTest:
    - 'dismisses an open alert as cancelled' - seed open alert; invoke; assert state='dismissed_cancelled', actioned_at set, transition row with to_state='dismissed_cancelled' / transition_reason='user_dismissed_cancelled'.
    - 'dispatches DriftAlertDismissedCancelled event' - assert event has correct recurringSeriesId.
    - 'does NOT mutate recurring_series.state' - verify the underlying recurring_series row's state is unchanged (whatever it was before, it stays).
    - 'idempotent + cross-user 404' (same patterns).

    DriftAlertCrossUser404Test (dedicated cross-user test mirroring Modules/Recurring/tests/Feature/CrossUserRecurringSeriesIsolationTest.php):
    - For each action (Acknowledge / Snooze / Dismiss): seed an alert for user A; resolve the action as user B; expect NotFoundHttpException; assert the alert's state is unchanged and zero new transition rows for that alert.
    - Additional: invoke each action with $alertId from user A using container resolution under user B's authentication context.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/DriftAlerts/tests/Feature/AcknowledgeDriftAlertTest.php Modules/DriftAlerts/tests/Feature/SnoozeDriftAlertTest.php Modules/DriftAlerts/tests/Feature/DismissDriftAlertAsCancelledTest.php Modules/DriftAlerts/tests/Feature/DriftAlertCrossUser404Test.php 2>&1 | tail -30 ; vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter="DriftAlerts" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All four feature tests pass.
    - Each action throws NotFoundHttpException on cross-user invocation (the dedicated test proves it; HTTP status code becomes 404 via Laravel's exception handler).
    - For each successful action invocation, exactly 1 drift_alert_transitions row is inserted with the correct from_state / to_state / transition_reason / actor / notes / transitioned_at.
    - The three Public Events are dispatched on success and NOT dispatched on idempotent no-ops.
    - `grep -c "DriftAlertStateMachine" Modules/DriftAlerts/Public/Actions/*.php` returns at least 6 (3 imports + 3 transition() calls).
    - `grep "->where('user_id', \$user->id)" Modules/DriftAlerts/Public/Actions/*.php` returns 3 (one per action - cross-user invariant present in every action).
    - `grep "recurring_series" Modules/DriftAlerts/Public/Actions/*.php` returns 0 (zero references to recurring_series writes - boundary invariant).
    - BoundaryArchTest DriftAlerts invariants remain green.
    - PHPStan strict on Modules/DriftAlerts/Public/Actions + Public/Events is green.
    - Pint --test green.
  </acceptance_criteria>
  <done>
    Three Public Actions and three Public Events are wired; four feature tests (acknowledge happy path + snooze + dismiss + cross-user 404) all pass; BoundaryArchTest remains green.
  </done>
</task>

<task type="auto" id="9-04-02">
  <name>Task 2: DriftAlertQuery + DriftAlertDtoMapper + DriftPage Livewire SFC + Blade view + page feature test</name>
  <read_first>
    - Modules/Recurring/Public/Services/RecurringSeriesQuery.php (entire file - the read API shape: cursor pagination, scoped methods, count method, total aggregate method; lines 1-200)
    - Modules/Recurring/Internal/Mapping/RecurringSeriesDtoMapper.php (entire file - the static mapper hydrating from stdClass row to DTO)
    - Modules/Recurring/Internal/Http/Livewire/RecurringReviewPage.php (entire file - the Livewire SFC pattern: method-parameter DI on action handlers, #[Url] for tab state, render() with method-parameter DI, $view->extends('layouts.app'))
    - Modules/Recurring/Resources/views/livewire/recurring-review-page.blade.php (entire file - the Blade layout: page heading + subheading + tab bar + cursor pagination + per-row card chrome + snooze popover at lines ~130-160)
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md (sections: DriftAlertQuery + DriftAlertDtoMapper + DriftPage + drift-page blade)
    - .planning/phases/09-subscription-drift-detection-alerts/09-UI-SPEC.md (sections 1, 6 - /drift page layout, Spacing Scale, Typography, Color, the empty-state hero variants per tab)
  </read_first>
  <files>
    - Modules/DriftAlerts/Internal/Mapping/DriftAlertDtoMapper.php (NEW)
    - Modules/DriftAlerts/Public/Services/DriftAlertQuery.php (NEW)
    - Modules/DriftAlerts/Internal/Http/Livewire/DriftPage.php (NEW)
    - Modules/DriftAlerts/Resources/views/livewire/drift-page.blade.php (NEW)
    - Modules/DriftAlerts/tests/Feature/DriftPageTest.php (NEW)
  </files>
  <action>
    DriftAlertDtoMapper: namespace Modules\DriftAlerts\Internal\Mapping. Static-only hydrator class with method `public static function hydrate(stdClass $row, ?string $seriesDisplayName = null, ?int $eurEquivalentMinor = null): DriftAlertDto`. Mirror Modules/Recurring/Internal/Mapping/RecurringSeriesDtoMapper.php verbatim - hydrate every DriftAlertDto field from the stdClass row, construct Money via Money::ofMinor for baseline/latest/delta/annualizedImpact in original currency, construct optional eurEquivalent Money in 'EUR' when $eurEquivalentMinor is not null AND $currency !== 'EUR'. Parse timestamps via CarbonImmutable::parse. Include helper toInt + toString + toIntOrNull mirroring analog lines 85-97.

    DriftAlertQuery: namespace Modules\DriftAlerts\Public\Services. `final readonly class DriftAlertQuery`. Constructor: `private DatabaseManager $db`. Methods:
    - openForUser(User $user, ?int $cursorId = null, int $limit = 26): list<DriftAlertDto>
    - historyForUser(User $user, ?int $cursorId = null, int $limit = 26): list<DriftAlertDto> (state='acknowledged')
    - dismissedForUser(User $user, ?int $cursorId = null, int $limit = 26): list<DriftAlertDto> (state='dismissed_cancelled')
    - openCountForUser(User $user): int
    - totalOpenAnnualizedImpactForUser(User $user): int (SUM aggregate for the dashboard tile copy)
    - groupedByseriesForUser(User $user): array<int, list<DriftAlertDto>> (returns map from recurring_series_id to list of open alerts in that series; used by /drift Open tab's grouped-by-series collapsible)

    Private `scoped()` helper (mirror PATTERNS.md DriftAlertQuery excerpt verbatim):
    - Build query against drift_alerts via $this->db->connection()->table('drift_alerts').
    - WHERE user_id = $user->id.
    - whereIn('state', $states).
    - orderByDesc('detected_at')->orderByDesc('id').
    - limit($limit).
    - If $cursorId !== null, where('id', '<', $cursorId).
    - get() the rows.
    - Batch-resolve series display names: collect distinct recurring_series_id values, run a single SELECT against recurring_series ON the user_id filter; map id -> display_name_override ?? detected_name (fallback to '').
    - For each row, call DriftAlertDtoMapper::hydrate($row, $displayNames[$row->recurring_series_id] ?? '', $eurEquivalentMinor).

    EUR shadow computation: when row.currency !== 'EUR' AND the row has fx_rate_used available (via JOIN to recurring_series_occurrences ON drift_alerts.latest_occurrence_id), compute EUR shadow as (int) round(abs(row.delta_minor) * row.fx_rate_used) with the sign preserved. Document this in PHPDoc; the Wave 4 plan (09-05) wires the actual fx_rate_used JOIN as a polish step — Wave 3 may pass null for eurEquivalentMinor and let Plan 09-05 enhance the query.

    The single read of recurring_series for display name resolution is permitted: it is a SELECT, not a write. The noRecurringSeriesWritesFromDriftAlerts arch test only flags update/insert/delete verbs.

    DriftPage Livewire SFC: namespace Modules\DriftAlerts\Internal\Http\Livewire. `final class DriftPage extends \Livewire\Component`. Imports: Carbon\CarbonImmutable, Illuminate\Contracts\View\Factory as ViewFactory, Illuminate\Contracts\View\View, Livewire\Attributes\Url, Modules\Core\Public\Contracts\Clock, Modules\Core\Public\Contracts\CurrentUser, Modules\DriftAlerts\Public\Actions\AcknowledgeDriftAlert, SnoozeDriftAlert, DismissDriftAlertAsCancelled, Modules\DriftAlerts\Public\Services\DriftAlertQuery, Modules\DriftAlerts\Public\Services\CancellationImpactQuery (the Plan 09-05 surface; can be referenced now even though its body lands in Plan 09-05 - the action method that calls it isn't exercised by Wave 3 tests).

    Properties: `#[Url(as: 'tab', except: 'open')] public string $tab = 'open';` and `public ?int $cursorId = null;`.

    Action methods (each with method-parameter DI - NEVER constructor DI for Livewire components per project's locked phpstan-strict-rules carve-out):
    - acknowledge(int $alertId, CurrentUser $currentUser, AcknowledgeDriftAlert $action): void - ($action)($alertId, $currentUser->user()); $this->dispatch('toast', message: 'Acknowledged');
    - snooze(int $alertId, string $untilIso, CurrentUser $currentUser, SnoozeDriftAlert $action): void - $until = CarbonImmutable::parse($untilIso); ($action)($alertId, $currentUser->user(), $until); $this->dispatch('toast', message: 'Snoozed');
    - dismissAsCancelled(int $alertId, CurrentUser $currentUser, DismissDriftAlertAsCancelled $action): void - ($action)($alertId, $currentUser->user()); $this->dispatch('toast', message: 'Dismissed as cancelled');
    - setTab(string $tab): void - if not in ['open', 'history', 'dismissed'] return; otherwise $this->tab = $tab; $this->cursorId = null;

    render(CurrentUser $currentUser, DriftAlertQuery $query, ViewFactory $views, Clock $clock): View
    1. $user = $currentUser->user();
    2. $rows = match ($this->tab) { 'history' => $query->historyForUser($user, $this->cursorId), 'dismissed' => $query->dismissedForUser($user, $this->cursorId), default => $query->openForUser($user, $this->cursorId) };
    3. $grouped = $this->tab === 'open' ? $query->groupedByseriesForUser($user) : [];
    4. $snoozeTargets = ['1w' => $clock->now()->addWeek()->toIso8601String(), '1m' => $clock->now()->addMonth()->toIso8601String(), '3m' => $clock->now()->addMonths(3)->toIso8601String()];
    5. $view = $views->make('drift-alerts::livewire.drift-page', ['rows' => $rows, 'tab' => $this->tab, 'grouped' => $grouped, 'snoozeTargets' => $snoozeTargets]);
    6. /** @phpstan-ignore-next-line method.notFound */ $view->extends('layouts.app', ['title' => 'Drift alerts - diederik']);
    7. return $view;

    drift-page.blade.php (under Modules/DriftAlerts/Resources/views/livewire/): Full Blade with the UI-SPEC chrome:
    - Outer container max-w-5xl mx-auto py-12.
    - Page heading "Drift alerts" with `text-2xl font-semibold tracking-tight text-slate-900` per UI-SPEC Typography.
    - Subheading "Recurring series whose latest charge differs from your threshold." in slate-500 max 60ch.
    - Top-right helper link to /settings: "Default threshold: ±{{ $globalThreshold }}%" (slate-500 hover-underline).
    - Tab bar (verbatim mirror of recurring-review-page.blade.php lines 36-48): three tabs Open / History / Dismissed, active tab gets border-b-2 border-slate-900 font-medium.
    - Tab body: when $tab='open' AND empty($grouped), render the empty-state hero (UI-SPEC empty-state-hero block). Otherwise iterate $grouped: for each series-id-keyed group, render a <flux:card> group header with Alpine x-data="{open: false}" containing the series display name + stacked-count chip "{count} drifts open" + bulk Acknowledge-all chip (Wave 3 wires bulk Acknowledge as a method that iterates the group and calls the per-alert action). Below the header, when x-show='open', render each alert row.
    - Per-alert row chrome (UI-SPEC): rounded-lg border border-slate-200 bg-white p-4 with the single-line layout: arrow-trending-up/down icon + series name + delta amount + annualized impact + three action chips. Direction-aware sign-color (expense-up rose-700, expense-down emerald-700, income-up emerald-700, income-down rose-700). Snooze popover REUSED verbatim from Phase 8 (mirror lines 130-160 of recurring-review-page.blade.php).
    - When $tab='history' or 'dismissed', render the per-tab empty-state hero or the row list without grouping.
    - Cursor pagination: at the bottom of the row list, show "Load more" link that bumps $cursorId to the last row's id and re-fetches.
    - Cadence-flipped reciprocal hint: when row's underlying series is in state='cadence_changed', render a single-line meta "Cadence flipped - also showing in /recurring/review" with route('recurring.review') as the link.

    DriftPageTest.php (Pest feature; extends Modules\DriftAlerts\Tests\TestCase + RefreshDatabase):
    - 'renders the open tab as default with empty-state hero when no alerts' - login a User with zero drift_alerts; visit /drift; assertStatus 200; assertSee 'No open drift alerts'; assertDontSee 'Acknowledge'.
    - 'renders open alerts grouped by series sorted detected_at DESC' - seed 2 drift_alerts for the same recurring_series_id at different detected_at; visit /drift; assert both alerts appear; assert the newer one is rendered FIRST in the DOM (use Pest's livewire test or Laravel's view-assertion API).
    - 'renders three tabs and #[Url] persists tab in URL' - visit /drift?tab=history; assert the active tab is 'history'.
    - 'invokes Acknowledge action and dispatches toast' - seed an open alert; trigger Livewire method 'acknowledge' with the alert id; assert state='acknowledged'; assert toast dispatched (Livewire::dispatchedEvents().
    - 'invokes Snooze action with the popover untilIso' - seed an open alert; trigger 'snooze' with the 1w iso; assert state='snoozed' AND snoozed_until equals the now+1w timestamp.
    - 'invokes Dismiss action and emits DriftAlertDismissedCancelled' - seed an open alert; Event::fake; trigger 'dismissAsCancelled'; assert state='dismissed_cancelled' AND event dispatched.
    - 'is unreachable unauthenticated' - visit /drift without authentication; assertRedirect to login route.
    - 'shows cadence-flipped meta line on cadence_changed series' - seed an open alert whose recurring_series is in state='cadence_changed'; visit /drift; assertSee 'Cadence flipped'.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/DriftAlerts/tests/Feature/DriftPageTest.php 2>&1 | tail -30 ; vendor/bin/phpstan analyse Modules/DriftAlerts/Public/Services Modules/DriftAlerts/Internal/Mapping Modules/DriftAlerts/Internal/Http --memory-limit=2G 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - DriftPageTest passes ALL 8 cases listed above.
    - The /drift route mounts DriftPage and the Blade template renders without server-side errors. Visit /drift via PHPUnit's `$this->get('/drift')` returns 200 (when authenticated) and contains the expected DOM markers (page heading, tab bar, empty-state or row list).
    - DriftAlertQuery exposes all 6 documented methods; openCountForUser counts only state='open' for the given user; totalOpenAnnualizedImpactForUser returns the SUM of annualized_impact_minor for open alerts for the user.
    - The Acknowledge / Snooze / Dismiss methods on DriftPage successfully invoke the corresponding Public Action and dispatch a toast on the wire.
    - The grouped-by-series header markup uses `<flux:card>` and `x-data="{ open: false }"` (verified by grep on the Blade file).
    - BoundaryArchTest DriftAlerts invariants stay green.
    - PHPStan strict on Modules/DriftAlerts/Public/Services + Internal/Mapping + Internal/Http is green.
    - Pint --test green.
  </acceptance_criteria>
  <done>
    DriftAlertQuery + DriftAlertDtoMapper + DriftPage SFC + drift-page Blade are wired; the /drift page renders correctly across all three tabs; 8 feature tests cover the page behaviour end-to-end.
  </done>
</task>

<task type="auto" id="9-04-03">
  <name>Task 3: DashboardDriftBadge + top-nav compound badge integration + 2 composer feature tests</name>
  <read_first>
    - Modules/Recurring/Internal/Http/Livewire/FixedPaymentsCard.php (entire file - the dashboard tile pattern: method-parameter DI on render(), reads through a Public Query, returns a Blade view)
    - Modules/Recurring/Resources/views/livewire/fixed-payments-card.blade.php (entire file - tile chrome)
    - Modules/Recurring/Providers/RecurringServiceProvider.php lines 111-126 (the existing top-nav composer pattern injecting recurringPendingCount)
    - resources/views/components/core/livewire/top-nav.blade.php (entire file - the existing top-nav Blade; find the existing Recurring slot + its single-pill chrome; the compound-pill chrome lands here in Wave 3)
    - resources/views/dashboard.blade.php (entire file - find the existing tile-card markup pattern; the DashboardDriftBadge tile is inserted alongside the existing tiles)
    - Modules/Recurring/tests/Feature/FixedPaymentsCardTest.php (analog test - mount the Livewire component, assert injected props)
    - Modules/Recurring/tests/Feature/TopNavBadgeComposerTest.php (analog test - assert the View Factory composer fires AND the count is injected correctly per user state)
    - .planning/phases/09-subscription-drift-detection-alerts/09-PATTERNS.md (DashboardDriftBadge + Top-nav badge sections)
    - .planning/phases/09-subscription-drift-detection-alerts/09-UI-SPEC.md (sections 5, 6 - Dashboard tile chrome + Navigation Decision compound-badge chrome)
  </read_first>
  <files>
    - Modules/DriftAlerts/Internal/Http/Livewire/DashboardDriftBadge.php (NEW)
    - Modules/DriftAlerts/Resources/views/livewire/dashboard-drift-badge.blade.php (NEW)
    - resources/views/dashboard.blade.php (MODIFY - add @livewire('drift-alerts.dashboard-drift-badge') tile)
    - resources/views/components/core/livewire/top-nav.blade.php (MODIFY - render compound pill chrome on Recurring slot)
    - Modules/DriftAlerts/tests/Feature/DashboardDriftBadgeTest.php (NEW)
    - Modules/DriftAlerts/tests/Feature/TopNavDriftBadgeTest.php (NEW)
  </files>
  <action>
    Modules/DriftAlerts/Internal/Http/Livewire/DashboardDriftBadge.php: `namespace Modules\DriftAlerts\Internal\Http\Livewire; final class DashboardDriftBadge extends \Livewire\Component`. No constructor (Livewire Component subclasses ban constructor DI). Single method:
    ```
    public function render(CurrentUser $currentUser, DriftAlertQuery $query, ViewFactory $views): View
    {
        $user = $currentUser->user();
        $openCount = $query->openCountForUser($user);
        $totalAnnualizedImpact = $query->totalOpenAnnualizedImpactForUser($user);
        return $views->make('drift-alerts::livewire.dashboard-drift-badge', [
            'openCount' => $openCount,
            'totalAnnualizedImpact' => $totalAnnualizedImpact,
        ]);
    }
    ```

    dashboard-drift-badge.blade.php: Render the tile per UI-SPEC section 5. Hide entirely when $openCount === 0 (return an empty fragment). When $openCount > 0:
    - <a href="{{ route('drift.index') }}"> wrapping rounded-lg border border-slate-200 bg-white p-6 (matches the Next ICS settlement tile chrome verbatim).
    - Tile heading "Drift alerts" (text-base font-semibold text-slate-900 per UI-SPEC Typography Section heading).
    - Tabular-nums count: <span class="text-3xl font-semibold tabular-nums">{{ $openCount }}</span>.
    - Helper line: "open · ↗ {currencyAwareFormat($totalAnnualizedImpact, 'EUR')} annualized impact" in slate-500. The format helper computes the sign + value in EUR for now (Wave 4 Plan 09-05 expands to per-currency display if budget allows).

    dashboard.blade.php: Find the existing tile section (look for `@livewire('recurring.fixed-payments-card')` or the equivalent grid). Insert a new column/cell: `@livewire('drift-alerts.dashboard-drift-badge')`. Place it next to the existing "Next ICS settlement" tile per UI-SPEC.

    top-nav.blade.php: Find the existing `Recurring` slot. Currently it renders a single-pill badge when `recurringPendingCount > 0`. Wrap the existing chrome in compound logic:
    - When `recurringPendingCount > 0 && driftOpenCount > 0`: render two pills side by side per UI-SPEC § Navigation Decision. The first pill keeps bg-slate-900 text-white (the pending count); the second pill uses bg-rose-50 text-rose-700 with the arrow-trending-up glyph (the drift count).
    - When only `recurringPendingCount > 0`: render the existing single pill unchanged.
    - When only `driftOpenCount > 0`: render a single pill but with the rose-50/rose-700 chrome + ↗ glyph (drift-only single pill).
    - When both are 0: render no pill.

    Use the EXACT chrome from UI-SPEC § Navigation Decision (Compound-badge chrome section). Add aria-label attributes describing each count for screen readers.

    DashboardDriftBadgeTest:
    - 'renders nothing when count is 0' - login a user; visit /; assertDontSee 'Drift alerts'.
    - 'renders tile with count when openCount > 0' - seed 3 open drift_alerts for the user; visit /; assertSee 'Drift alerts'; assertSee '3'; assertSee route('drift.index') (the tile is a link).
    - 'tile total annualized impact equals SUM of annualized_impact_minor on open alerts' - seed 2 alerts with annualized_impact_minor=-1800 and -600 (total -2400); visit /; assert the tile copy contains '€24/yr' (-2400 cents = €24).
    - 'cross-user isolation' - seed alerts for user B; visit / as user A (different user); assertDontSee 'Drift alerts'.

    TopNavDriftBadgeTest:
    - 'injects driftOpenCount=0 when not authenticated' - visit any authenticated page as guest is impossible (LoopbackOnly forces auth); use the View Factory composer directly: call $factory->composer trigger and inspect injected props. Or alternatively: login a fresh user with 0 drift_alerts; visit /; assertDontSee the drift pill chrome.
    - 'injects driftOpenCount > 0 when alerts exist' - login user with 2 open drift_alerts; visit /; assertSee a `<span ... aria-label="2 open drift alerts">` in the top-nav.
    - 'renders compound pill when both pending and drift are non-zero' - seed 1 pending recurring_series suggestion AND 1 open drift_alert; visit /; assertSee BOTH the slate-900 pill AND the rose-50 drift pill.
    - 'renders only drift pill when pending is 0' - seed 0 pending suggestions + 1 open drift; visit /; assert only the drift pill renders (cross-check by absence of the bg-slate-900 pending chrome).
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/DriftAlerts/tests/Feature/DashboardDriftBadgeTest.php Modules/DriftAlerts/tests/Feature/TopNavDriftBadgeTest.php 2>&1 | tail -30 ; vendor/bin/phpstan analyse Modules/DriftAlerts/Internal/Http resources/views --memory-limit=2G 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Both feature tests pass.
    - DashboardDriftBadge tile renders only when openCount > 0 (hidden state for empty count).
    - Top-nav renders the compound pill chrome only when both counts are non-zero; single pill in either single-non-zero case; no pill when both are zero.
    - The DriftAlertsServiceProvider top-nav composer registered in Plan 09-01 successfully injects driftOpenCount into the core::livewire.top-nav view; verified by inspecting `View::composer` registrations or by the feature test's assertion that the rendered HTML contains the expected count.
    - `grep "@livewire('drift-alerts.dashboard-drift-badge')" resources/views/dashboard.blade.php` returns 1 line.
    - `grep "driftOpenCount" resources/views/components/core/livewire/top-nav.blade.php` returns at least 1 line.
    - BoundaryArchTest invariants stay green (especially noFacadeCallsFromDriftAlerts - the DashboardDriftBadge uses method-parameter DI; the top-nav Blade is in resources/ not Modules/DriftAlerts/, so the arch test scope does not cover Blade view files).
    - PHPStan strict on Modules/DriftAlerts/Internal/Http is green.
    - Pint --test green.
  </acceptance_criteria>
  <done>
    Dashboard tile + top-nav compound badge are wired; tests prove both composer integrations work; the visual chrome matches UI-SPEC.
  </done>
</task>

<task type="checkpoint:human-verify" id="9-04-04" gate="blocking">
  <what-built>
    /drift page (Open / History / Dismissed tabs), grouped-by-series collapsible group headers (flux:card + Alpine x-show), per-row Acknowledge / Snooze (popover) / I-cancelled-this actions with toast confirmation, dashboard "Drift alerts" count tile, top-nav compound badge on Recurring slot.
  </what-built>
  <how-to-verify>
    Manual browser walk-through against `https://diederik.test` (Laravel Herd):

    1. `php artisan tinker --execute "App\\Models\\User::query()->first()->update(['drift_alert_threshold_percent' => 5]);"` to ensure the test user has a threshold.
    2. Seed synthetic drift data using the corpus fixture loader: `php artisan tinker --execute "..."` with a 1-liner that loads `Modules/DriftAlerts/tests/fixtures/drift-corpus/large-drift-above-threshold.php` and inserts the recurring_series + occurrences + then dispatches DetectDriftAlertsJob synchronously for that series id.
    3. Open `https://diederik.test/drift` in the browser.

    Expected visuals (verify each):
    - Page heading "Drift alerts" + subheading below it; calm Linear/Notion aesthetic (max-w-5xl centered, slate palette, no shadow-heavy chrome).
    - Three tabs: Open (active by default), History, Dismissed. Active tab has border-b-2 border-slate-900.
    - Open tab: at least one alert row card visible. The card chrome is `rounded-lg border border-slate-200 bg-white p-4`. Row layout: arrow-trending-up icon (rose-700 if expense going up) + series name "Spotify" + "+€1.50/mo" (tabular-nums) + "→ +€18/yr" + three action chips on the right.
    - If you seeded 2+ alerts for the same series id: a grouped header card (flux:card with bg-slate-50) wraps them; click the chevron-down icon to expand/collapse the group; a stacked-count chip "{count} drifts open" + a "Acknowledge all" bulk chip live on the header.
    - Click "Acknowledge" on a row: a toast appears at bottom-center reading "Acknowledged"; the row disappears from the Open tab; appears in the History tab.
    - Click "Snooze" on a row: a popover opens with options "1w / 1m / 3m"; click "1m"; popover closes; toast reads "Snoozed"; row disappears from Open.
    - Click "I cancelled this" on a row: toast reads "Dismissed as cancelled"; row disappears from Open; appears in Dismissed tab.
    - Empty state per tab: visit Open after Acknowledging the only open alert; the empty-state hero renders "No open drift alerts" with the explanation copy + the inline text-link "Settings -> Default drift alert".
    - Income direction-aware copy: seed an income drift alert via tinker; verify the icon is emerald-600 (raise) or rose-600 (cut) and the copy reads "Salary up €X/mo" or "Salary down €X/mo".
    - EUR shadow on USD alert: seed a mixed-currency-real-usd-drift fixture; verify the row displays "+$3.00/mo → +$36/yr" with a slate-500 EUR-shadow line "(≈ +€33/yr)" below (Plan 09-05 owns the actual EUR-shadow JOIN; if Wave 3 renders without the shadow, that is acceptable here - the shadow is a Wave 4 polish item).

    Dashboard tile:
    - Visit `https://diederik.test/`. With 1+ open drift alerts: the "Drift alerts" tile renders alongside "This month at a glance", "Next ICS settlement", "Fixed monthly payments". Tile chrome `rounded-lg border border-slate-200 bg-white p-6`. Count "1" (or whatever) in tabular-nums; helper "open · ↗ €18/yr annualized impact" below.
    - Click the tile: navigates to /drift.
    - Acknowledge all open alerts; refresh /; the tile disappears entirely.

    Top-nav:
    - With at least one OPEN drift alert: the top-nav "Recurring" slot shows a compound pill chrome IF there is also a pending recurring suggestion. Otherwise the slot shows a single rose-50 pill with the drift count + ↗ glyph.
    - With zero pending and zero drift: the slot shows just the text "Recurring" with no pill.
    - With only pending: the slot shows the existing slate-900 pill unchanged.

    Cross-user 404:
    - Open the browser dev tools console; obtain an alert id belonging to user B; manually POST to the Livewire endpoint impersonating user A. Expect HTTP 404. (Skip if too complex for manual check - the Pest CrossUser404Test covers this automated.)

    Snooze popover does NOT close mid-interaction:
    - Click Snooze on a row; let the page sit for 30 seconds; the popover should STILL be open. (Pitfall 2 verification - wire:poll must NOT close the popover.)
  </how-to-verify>
  <resume-signal>Type "approved" if the visual + interaction checks all pass. Type a description of any issues otherwise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request -> /drift route | Authenticated session bound by web + auth + LoopbackOnly middleware |
| Livewire action invocation -> Public Action | Cross-user 404 enforced via where('user_id', $user->id) on every fetch |
| View Factory composer -> top-nav Blade | Composer fires on every render of core::livewire.top-nav; injects per-user driftOpenCount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-05 | Spoofing | Unauthenticated access to /drift | mitigate | Route is wrapped in middleware(['web', 'auth']) (Plan 09-01 Routes/web.php) + the global LoopbackOnly middleware from Phase 1 makes /drift unreachable from non-loopback origins |
| T-9-06 | Information Disclosure (IDOR) | User A invokes AcknowledgeDriftAlert with an alert id owned by user B | mitigate | AcknowledgeDriftAlertTest cross-user case + DriftAlertCrossUser404Test prove the action throws NotFoundHttpException via the where('user_id', $user->id) clause |
| T-9-07 | Information Disclosure (IDOR) | User A invokes SnoozeDriftAlert with user B's alert id | mitigate | Same pattern as T-9-06 + dedicated cross-user test case |
| T-9-08 | Information Disclosure (IDOR) | User A invokes DismissDriftAlertAsCancelled with user B's alert id | mitigate | Same pattern + dedicated cross-user test case |
| T-9-09 | Information Disclosure (IDOR) | User A loads /drift and sees user B's alerts via DriftAlertQuery | mitigate | Every DriftAlertQuery method scopes via where('user_id', $user->id); the DashboardDriftBadgeTest 'cross-user isolation' case confirms |
| T-9-05b | Information Disclosure | Cross-tab leakage via incorrect cursor | accept | Cursor-paginated reads are user-scoped at the SQL level (where user_id = $user->id) before the cursor clause; a stale cursor from another user would simply return empty results |
| T-9-06b | Tampering | Snooze action accepts arbitrary CarbonImmutable input from client | mitigate | The Livewire snooze() method parses the iso8601 string via CarbonImmutable::parse - malformed strings throw an exception; the snooze popover sends one of three fixed values (1w / 1m / 3m) calculated server-side and round-tripped, NOT arbitrary user input |
</threat_model>

<verification>
Wave 3 phase-level checks:
- `vendor/bin/pest Modules/DriftAlerts/tests/` is green (Wave 0 unit + Wave 1 unit + Wave 2 unit + Wave 3 feature; ~15+ test files at this point).
- `vendor/bin/pest tests/Contracts/` is green (BoundaryArchTest + DriftDetectionContractTest).
- `vendor/bin/phpstan analyse Modules/DriftAlerts resources/views/dashboard.blade.php resources/views/components/core/livewire/top-nav.blade.php --memory-limit=2G` is green.
- `vendor/bin/pint --test` is green.
- Manual checkpoint 9-04-04 is approved by the user.
- Cross-user 404 invariant has TWO layers of evidence: dedicated cross-user test + each per-action test includes a cross-user case.
- The /drift page is accessible via the browser at `https://diederik.test/drift` (LoopbackOnly + auth middleware).
</verification>

<success_criteria>
- User can navigate to /drift and see open alerts grouped by series.
- User can Acknowledge, Snooze (popover), or Dismiss-as-cancelled any open alert; each action writes one drift_alert_transitions row and dispatches the corresponding Public event.
- Dashboard count tile renders when openCount > 0 and hides when openCount = 0.
- Top-nav compound badge renders correctly across all four combinations of (pendingCount, driftCount).
- Every action enforces cross-user 404; the dedicated cross-user test passes.
- BoundaryArchTest invariants remain green throughout (no facade calls, no recurring_series writes, no synchronous detection, sole-mutator).
- Manual checkpoint verifies the calm-aesthetic chrome + the snooze-popover-does-not-close-on-poll invariant (Pitfall 2).
</success_criteria>

<output>
Create `.planning/phases/09-subscription-drift-detection-alerts/09-04-SUMMARY.md` when done.
</output>
