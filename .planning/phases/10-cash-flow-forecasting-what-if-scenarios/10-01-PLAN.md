---
phase: 10-cash-flow-forecasting-what-if-scenarios
plan: 01
plan_id: "10-01"
type: execute
wave: 0
depends_on: []
files_modified:
  - composer.json
  - phpunit.xml
  - tests/Pest.php
  - tests/Contracts/BoundaryArchTest.php
  - Modules/Forecasting/composer.json
  - Modules/Forecasting/Providers/ForecastingServiceProvider.php
  - Modules/Forecasting/Routes/web.php
  - Modules/Forecasting/tests/Pest.php
  - Modules/Forecasting/tests/TestCase.php
  - Modules/Forecasting/Internal/Listeners/ProjectForecastOnRecurringChange.php
  - Modules/Forecasting/Internal/Listeners/ProjectForecastOnDriftDismissed.php
  - Modules/Forecasting/Internal/Listeners/ProjectForecastOnScenarioChange.php
  - Modules/Forecasting/tests/Unit/FixtureCorpusTest.php
  - Modules/Forecasting/tests/fixtures/forecast-corpus/stable-monthly-subscription.php
  - Modules/Forecasting/tests/fixtures/forecast-corpus/drifting-subscription-midwindow.php
  - Modules/Forecasting/tests/fixtures/forecast-corpus/variable-utility.php
  - Modules/Forecasting/tests/fixtures/forecast-corpus/salary-and-side-income.php
  - Modules/Forecasting/tests/fixtures/forecast-corpus/ics-settlement-chain.php
  - Modules/Forecasting/tests/fixtures/forecast-corpus/fx-only-usd-subscription.php
  - Modules/Forecasting/tests/fixtures/forecast-corpus/zero-occurrence-edge-case.php
  - Modules/Forecasting/tests/fixtures/forecast-corpus/buffer-crossing.php
  - Modules/Forecasting/tests/fixtures/forecast-corpus/multi-account-baseline.php
  - Modules/Forecasting/tests/fixtures/forecast-corpus/scenario-with-each-mutation-kind.php
autonomous: true
requirements:
  - FCT-01
  - FCT-02
  - FCT-03
  - FCT-04
  - FCT-05
must_haves:
  truths:
    - "Modules/Forecasting module skeleton (composer.json + ForecastingServiceProvider + Routes/web.php + tests/TestCase + tests/Pest.php) loads via composer dump-autoload; the new PSR-4 namespace Modules\\Forecasting\\ resolves at runtime"
    - "Five BoundaryArchTest invariants for Forecasting (noFacadeCallsFromForecasting via carve-out extension, noTransactionWritesFromForecasting, crossModuleAccessGoesThroughPublic, noSynchronousForecastingInRequestLifecycle, noScenarioMutationsJoinedToTransactionQueries) live in tests/Contracts/BoundaryArchTest.php and stay green against the empty Phase 10 module"
    - "Three listener scaffold classes (ProjectForecastOnRecurringChange, ProjectForecastOnDriftDismissed, ProjectForecastOnScenarioChange) parse and are wired in ForecastingServiceProvider::registerListeners; their handle() bodies are stubs that throw RuntimeException('not implemented') and are exercised by Wave 2-4"
    - "Ten-scenario synthesised fixture corpus under Modules/Forecasting/tests/fixtures/forecast-corpus/ exists; FixtureCorpusTest iterates every file and asserts shape contract"
    - "Forecasting honours Public/Internal split from day one: empty Public/Contracts/ marker directory + Public/Dto/ + Public/Events/ + Public/Actions/ + Public/Services/ exist as empty namespaces ready for Wave 1+ to populate; Internal/Pipeline/ + Internal/Jobs/ + Internal/Listeners/ + Internal/Http/Livewire/ exist"
  artifacts:
    - path: Modules/Forecasting/composer.json
      provides: PSR-4 namespace registration for Modules\\Forecasting\\ + Modules\\Forecasting\\Tests\\
      contains: '"Modules\\\\Forecasting\\\\": ""'
    - path: Modules/Forecasting/Providers/ForecastingServiceProvider.php
      provides: ServiceProvider with singleton bindings, view + migration + route loaders, Livewire component registration, listener registration, and top-nav badge composer scaffold
      exports: ["ForecastingServiceProvider::register", "ForecastingServiceProvider::boot"]
    - path: Modules/Forecasting/Routes/web.php
      provides: GET /forecast route placeholder mapped to ForecastPage Livewire SFC under web + auth middleware
      contains: "Route::middleware(['web', 'auth'])"
    - path: tests/Contracts/BoundaryArchTest.php
      provides: Five new arch test invariants for Forecasting module + facade carve-out for ProjectForecastJob FQN
      contains: "noScenarioMutationsJoinedToTransactionQueries"
    - path: Modules/Forecasting/tests/fixtures/forecast-corpus/
      provides: 10-scenario synthesised fixture corpus directory
      min_lines: 10
    - path: Modules/Forecasting/tests/Unit/FixtureCorpusTest.php
      provides: Corpus loader test asserting all 10 fixtures parse and satisfy the shape contract
  key_links:
    - from: Modules/Forecasting/Providers/ForecastingServiceProvider.php
      to: Modules/Recurring/Public/Events/RecurringSeriesApproved::class
      via: "$events->listen(RecurringSeriesApproved::class, [ProjectForecastOnRecurringChange::class, 'handle'])"
      pattern: "RecurringSeriesApproved::class.*ProjectForecastOnRecurringChange"
    - from: Modules/Forecasting/Providers/ForecastingServiceProvider.php
      to: Modules/DriftAlerts/Public/Events/DriftAlertDismissedCancelled::class
      via: "$events->listen(DriftAlertDismissedCancelled::class, [ProjectForecastOnDriftDismissed::class, 'handle'])"
      pattern: "DriftAlertDismissedCancelled::class.*ProjectForecastOnDriftDismissed"
    - from: composer.json (project root)
      to: Modules\\Forecasting\\ PSR-4 namespace
      via: autoload entry
      pattern: "Modules\\\\\\\\Forecasting\\\\\\\\"
    - from: tests/Contracts/BoundaryArchTest.php
      to: "the load-bearing FCT-03 invariant for forecast_scenario_mutations"
      via: "it('noScenarioMutationsJoinedToTransactionQueries') filesystem-walk + regex"
      pattern: "noScenarioMutationsJoinedToTransactionQueries"
---

<objective>
Wave 0 enablement. Stand up the bounded `Modules/Forecasting/` module skeleton — composer manifest, ServiceProvider with event-listener wiring, Routes file, test scaffolding — and lock in FIVE BoundaryArchTest invariants that will police every Phase 10 file written in Waves 1-5. Land the ten-scenario synthesised forecasting fixture corpus + listener scaffolds so subsequent waves have deterministic test data and an event-bus skeleton without per-test boilerplate.

Purpose: Phase 10 is a composition phase — every primitive already has a project precedent (Phase 9 `Modules/DriftAlerts/` is the skeletal analog; Phase 8 `Modules/Recurring/` is the ApexCharts analog; Phase 5 `Modules/Chains/` is the chain-routing analog). This wave reserves the namespace, registers the testsuite via the three-step pattern (composer.json autoload-dev + phpunit.xml testsuite + tests/Pest.php map row), and locks the five BoundaryArchTest invariants — especially the load-bearing `noScenarioMutationsJoinedToTransactionQueries` rule that structurally enforces FCT-03's "what-if mutations do not persist into the transaction substrate" — so Waves 1-5 cannot drift away from the Phase 9 precedent or break the FCT-03 isolation.

Output: A green test suite with the new module discovered, the new arch tests passing against the empty module, the listener scaffolds wired (and stubbed so Wave 2-4 swap in real handlers without re-registering events), and the fixture corpus + factories ready for Wave 1's schema + Wave 2's projection contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-CONTEXT.md
@.planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-RESEARCH.md
@.planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-PATTERNS.md
@.planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-UI-SPEC.md
@.planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-VALIDATION.md

# Analog source files the executor MUST read before writing (these are the locked patterns):
@Modules/DriftAlerts/composer.json
@Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php
@Modules/DriftAlerts/Routes/web.php
@Modules/DriftAlerts/tests/Pest.php
@Modules/DriftAlerts/tests/TestCase.php
@tests/Contracts/BoundaryArchTest.php
@Modules/Recurring/tests/fixtures/synthesised/drifting-monthly-spotify.php
@Modules/Recurring/tests/fixtures/synthesised/stable-monthly-spotify.php
@Modules/DriftAlerts/Internal/Listeners/EvaluateDriftOnMetricsRefreshed.php
@Modules/Recurring/Public/Events/RecurringSeriesApproved.php
@Modules/Recurring/Public/Events/RecurringSeriesCadenceFlipped.php
@Modules/Recurring/Public/Events/RecurringSeriesRejected.php
@Modules/DriftAlerts/Public/Events/DriftAlertDismissedCancelled.php

<interfaces>
<!-- Key Phase 8/9 Public event signatures the new listener scaffolds reference. -->

From Modules/Recurring/Public/Events/RecurringSeriesApproved.php (analog shape — the four Phase 8
events Phase 10 listens to all carry $userId and $recurringSeriesId; ProjectForecastOnRecurringChange
fans out one ProjectForecastJob per (userId, scenarioId∈{null}∪saved, horizon∈{30,60,90})):

```
namespace Modules\Recurring\Public\Events;
final readonly class RecurringSeriesApproved {
    public function __construct(
        public int $userId,
        public int $recurringSeriesId,
    ) {}
}
```

From Modules/DriftAlerts/Public/Events/DriftAlertDismissedCancelled.php (Phase 9 event Phase 10
listens to via ProjectForecastOnDriftDismissed):

```
namespace Modules\DriftAlerts\Public\Events;
final readonly class DriftAlertDismissedCancelled {
    public function __construct(
        public int $userId,
        public int $driftAlertId,
        public int $recurringSeriesId,
    ) {}
}
```

From tests/Contracts/BoundaryArchTest.php (the existing facade-carve-out shape Plan 10-01 extends —
this plan adds Modules\\Forecasting\\Internal\\Jobs\\ProjectForecastJob to the same ignoring([...]) list
that already exempts Modules\\Recurring\\Internal\\Jobs\\DetectRecurringSeriesJob and
Modules\\DriftAlerts\\Internal\\Jobs\\DetectDriftAlertsJob):

```
arch('Modules code does not use facades except documented carve-outs')
    ->expect('Modules')
    ->not->toUseFacades()
    ->ignoring([
        'Modules\\Recurring\\Internal\\Jobs\\DetectRecurringSeriesJob',
        'Modules\\DriftAlerts\\Internal\\Jobs\\DetectDriftAlertsJob',
        // NEW (Plan 10-01): add the Forecasting job carve-out for the
        // Cache::driver('redis') uniqueVia() call (ShouldBeUniqueUntilProcessing).
        'Modules\\Forecasting\\Internal\\Jobs\\ProjectForecastJob',
    ]);
```

From Modules/DriftAlerts/Internal/Listeners/EvaluateDriftOnMetricsRefreshed.php (lines 1-35) — the
shape Phase 10's three listeners mirror. Each listener carries a single private Dispatcher $bus
property injected via constructor; handle() dispatches the projection job:

```
namespace Modules\DriftAlerts\Internal\Listeners;
final readonly class EvaluateDriftOnMetricsRefreshed {
    public function __construct(private Illuminate\Contracts\Bus\Dispatcher $bus) {}
    public function handle(Modules\Recurring\Public\Events\RecurringSeriesMetricsRefreshed $event): void {
        $this->bus->dispatch(new Modules\DriftAlerts\Internal\Jobs\DetectDriftAlertsJob(...));
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" id="10-01-01">
  <name>Task 1: Module skeleton + 3-step test discovery wire-up + ForecastingServiceProvider DI graph + event-listener registration</name>
  <read_first>
    - Modules/DriftAlerts/composer.json (entire file — analog manifest shape; copy verbatim and rename `diederik/drift-alerts` to `diederik/forecasting` and update description)
    - Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php (entire file — analog ServiceProvider; mirror imports, register() singleton bindings, boot() shape, registerListeners() multi-event extension, and registerTopNavBadgeComposer() with memo cache)
    - Modules/DriftAlerts/Routes/web.php (entire file — analog route file shape; one Route::get for the page surface)
    - Modules/DriftAlerts/tests/Pest.php + Modules/DriftAlerts/tests/TestCase.php (per-module test bootstrap; Pest.php documented inert; TestCase extends Tests\TestCase)
    - Modules/Recurring/Providers/RecurringServiceProvider.php (analog for the FOUR Phase 8 events Phase 10 listens to — same event-namespace; lines 80-95 area for the listener registration shape)
    - tests/Pest.php (project root — per-module wire-up `foreach` loop; the new row maps `Modules/Forecasting` to `Modules\\Forecasting\\Tests\\TestCase::class`; insert alphabetically between `Modules/DriftAlerts` and `Modules/Ingestion`)
    - composer.json (project root — find the existing `Modules\\DriftAlerts\\` and `Modules\\DriftAlerts\\Tests\\` autoload + autoload-dev entries; Phase 10 adds the same shape for Forecasting alphabetically before Ingestion)
    - phpunit.xml (project root — find the existing `<testsuite>` entry pointing at `Modules/DriftAlerts/tests/`; add a sibling `<testsuite name="Forecasting">` row pointing at `Modules/Forecasting/tests/`)
    - .planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-PATTERNS.md (sections: "Modules/Forecasting/composer.json", "ForecastingServiceProvider.php", "Migration analogs", "Three listener scaffold pattern", "Shared #7: three-step PSR-4 registration")
    - .planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-RESEARCH.md ("Standard Stack" section, "Recommended Project Structure" section, "Pattern 3: ShouldBeUniqueUntilProcessing per (user, scenario, horizon) with sentinel" section)
  </read_first>
  <files>
    - Modules/Forecasting/composer.json (NEW)
    - Modules/Forecasting/Providers/ForecastingServiceProvider.php (NEW)
    - Modules/Forecasting/Routes/web.php (NEW)
    - Modules/Forecasting/tests/Pest.php (NEW; documented inert)
    - Modules/Forecasting/tests/TestCase.php (NEW)
    - Modules/Forecasting/Internal/Listeners/ProjectForecastOnRecurringChange.php (NEW — Wave 0 scaffold; handle() throws RuntimeException; Wave 2 swaps body in)
    - Modules/Forecasting/Internal/Listeners/ProjectForecastOnDriftDismissed.php (NEW — scaffold; same posture)
    - Modules/Forecasting/Internal/Listeners/ProjectForecastOnScenarioChange.php (NEW — scaffold; same posture)
    - Modules/Forecasting/Public/Contracts/.gitkeep (NEW — empty marker so the namespace exists from day one and arch rules bind)
    - Modules/Forecasting/Public/Dto/.gitkeep (NEW — empty marker)
    - Modules/Forecasting/Public/Events/.gitkeep (NEW — empty marker)
    - Modules/Forecasting/Public/Actions/.gitkeep (NEW — empty marker)
    - Modules/Forecasting/Public/Services/.gitkeep (NEW — empty marker)
    - Modules/Forecasting/Internal/Pipeline/.gitkeep (NEW — empty marker)
    - Modules/Forecasting/Internal/Jobs/.gitkeep (NEW — empty marker)
    - Modules/Forecasting/Internal/Http/Livewire/.gitkeep (NEW — empty marker)
    - Modules/Forecasting/Resources/views/livewire/.gitkeep (NEW — empty marker)
    - composer.json (MODIFY — add `"Modules\\Forecasting\\": "Modules/Forecasting/"` to `autoload.psr-4` and `"Modules\\Forecasting\\Tests\\": "Modules/Forecasting/tests/"` to `autoload-dev.psr-4`)
    - phpunit.xml (MODIFY — add `<testsuite name="Forecasting"><directory>Modules/Forecasting/tests</directory></testsuite>` entry)
    - tests/Pest.php (MODIFY — add `'Modules/Forecasting' => Modules\\Forecasting\\Tests\\TestCase::class` row to the per-module map; insert alphabetically)
  </files>
  <action>
    Create the bounded `Modules/Forecasting/` module skeleton mirroring `Modules/DriftAlerts/` verbatim with these adjustments:

    **Modules/Forecasting/composer.json**: PSR-4 manifest verbatim shape from `Modules/DriftAlerts/composer.json` lines 1-16. Set `"name": "diederik/forecasting"`, `"type": "laravel-module"`, `"license": "proprietary"`, `autoload.psr-4` = `{"Modules\\Forecasting\\": ""}`, `autoload-dev.psr-4` = `{"Modules\\Forecasting\\Tests\\": "tests/"}`. Description: `"Forecasting module — 30/60/90-day cash-flow projection, what-if scenarios, /forecast page, dashboard highlights tile."` Do NOT mention D-numbers, REQ-IDs, or .planning/ paths in the manifest (GSD-agnostic invariant).

    **Modules/Forecasting/Providers/ForecastingServiceProvider.php**: `namespace Modules\\Forecasting\\Providers;` `final class ForecastingServiceProvider extends \\Illuminate\\Support\\ServiceProvider`. Mirror `Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php` structure verbatim with these adjustments:

    - **Imports** — `Illuminate\\Contracts\\Events\\Dispatcher`, `Illuminate\\Contracts\\View\\Factory as ViewFactoryContract`, `Illuminate\\Contracts\\View\\View`, `Illuminate\\Support\\ServiceProvider`, `Livewire\\LivewireManager`, `Modules\\Core\\Public\\Contracts\\CurrentUser`, plus the three listener FQNs from this plan and the FOUR Phase 8 event FQNs (`Modules\\Recurring\\Public\\Events\\{RecurringSeriesApproved, RecurringSeriesCadenceFlipped, RecurringSeriesRejected, RecurringSeriesMetricsRefreshed}`) plus the ONE Phase 9 event FQN (`Modules\\DriftAlerts\\Public\\Events\\DriftAlertDismissedCancelled`).

    - **register()** — empty for Wave 0 except for any singletons that exist after Wave 1+. In Wave 0 the only singleton-bindable class that exists is the listener scaffolds; bind each as a singleton so Laravel resolves them once per request: `$this->app->singleton(ProjectForecastOnRecurringChange::class);` (× 3). Do NOT reference not-yet-existing Pipeline / Jobs / Query / Action FQNs in Wave 0 — those bindings are added in their respective waves (Wave 1 = models + Public DTO; Wave 2 = Pipeline + Job; Wave 4 = Public Actions). Wave 0 keeps the register() method minimal-but-correct.

    - **boot(LivewireManager $livewire, Dispatcher $events)** — call `$this->loadMigrationsFrom(__DIR__.'/../Database/Migrations')` (wrap in `is_dir()` check — directory does not exist in Wave 0; Wave 1 lands it), `$this->loadRoutesFrom(__DIR__.'/../Routes/web.php')` (wrap in `is_file()` check — the file DOES exist in Wave 0), `$this->loadViewsFrom(__DIR__.'/../Resources/views', 'forecasting')` (wrap in `is_dir()` check — directory does not exist in Wave 0 beyond a .gitkeep; Wave 2 lands the Blade files). Skip Livewire component registration in Wave 0 (no Livewire classes exist yet). Call `$this->registerListeners($events)` and `$this->registerTopNavBadgeComposer()` from boot().

    - **registerListeners(Dispatcher $events)** — register the THREE Wave 0 listener scaffolds against the FIVE upstream events from Phase 8 + 9 + the THREE Phase 10 internal events. Phase 10 internal events are referenced by FQN-string until Wave 4 lands the event classes; reference by `\\Modules\\Forecasting\\Public\\Events\\ScenarioCreated::class` is acceptable in Wave 0 IF the class exists. For Wave 0, instead, register only the Phase 8 + Phase 9 listeners (the Phase 10 internal events are wired in Wave 4 Plan 10-05). Concretely:

    ```
    private function registerListeners(Dispatcher $events): void
    {
        // Phase 8 substrate-change events → recompute baseline + every saved scenario × every horizon.
        $events->listen(RecurringSeriesApproved::class, [ProjectForecastOnRecurringChange::class, 'handle']);
        $events->listen(RecurringSeriesCadenceFlipped::class, [ProjectForecastOnRecurringChange::class, 'handle']);
        $events->listen(RecurringSeriesRejected::class, [ProjectForecastOnRecurringChange::class, 'handle']);
        $events->listen(RecurringSeriesMetricsRefreshed::class, [ProjectForecastOnRecurringChange::class, 'handle']);

        // Phase 9 cancellation-dismissal event → cancelled series falls out of every projection.
        $events->listen(DriftAlertDismissedCancelled::class, [ProjectForecastOnDriftDismissed::class, 'handle']);

        // Phase 10 internal scenario events (ScenarioCreated / ScenarioMutated / ScenarioDeleted) are
        // registered in Plan 10-05 (Wave 4) after the event classes themselves land. Wave 0 reserves
        // the ProjectForecastOnScenarioChange listener scaffold so the wiring is a single-line drop-in
        // when Wave 4 ships.
    }
    ```

    - **registerTopNavBadgeComposer()** — verbatim mirror of `Modules/DriftAlerts/Providers/DriftAlertsServiceProvider.php` lines 113-136 (the memo-cached View Factory composer pattern). Swap the composer key from `driftOpenCount` → `forecastShortfallCount` and the Public service from `DriftAlertQuery::class` → `ForecastHighlightsQuery::class`. **Wave 0 caveat:** `ForecastHighlightsQuery` does not exist yet (Wave 1+ lands it). For Wave 0, comment out the body of the closure with a TODO note explaining "Wave 3 (Plan 10-04) populates the composer body; until then the closure shares forecastShortfallCount=0 unconditionally". Concretely the Wave 0 closure body reads: `$compose->with('forecastShortfallCount', 0);` and nothing else. Wave 3 replaces the body to read from `ForecastHighlightsQuery::activeShortfallCountForUser($user)`.

    **Modules/Forecasting/Routes/web.php**: Mirror `Modules/DriftAlerts/Routes/web.php` verbatim. `declare(strict_types=1);` + PHPDoc describing the module's `/forecast` surface (plain technical language, no D-numbers, no REQ-IDs). Imports: `Illuminate\\Support\\Facades\\Route` and `Modules\\Forecasting\\Internal\\Http\\Livewire\\ForecastPage` (the ForecastPage class is delivered by Plan 10-03 Wave 2; the Route file references the FQN — runtime resolution happens at request time after Plan 10-03 lands). Single route: `Route::middleware(['web', 'auth'])->group(static function (): void { Route::get('/forecast', ForecastPage::class)->name('forecast.index'); });`. The PHPDoc must note "Cross-user isolation is enforced by the underlying Public services + Actions (every read/write scopes by user_id)."

    **Modules/Forecasting/tests/TestCase.php**: `namespace Modules\\Forecasting\\Tests; final class TestCase extends \\Tests\\TestCase {}`. No body needed — mirrors `Modules/DriftAlerts/tests/TestCase.php` shape exactly.

    **Modules/Forecasting/tests/Pest.php**: Single inert file with a comment block noting it is intentionally empty — per-module Pest configuration is centralised in the project-root `tests/Pest.php`. Mirror `Modules/DriftAlerts/tests/Pest.php` verbatim.

    **Modules/Forecasting/Internal/Listeners/ProjectForecastOnRecurringChange.php**: Wave 0 scaffold. `namespace Modules\\Forecasting\\Internal\\Listeners;` `final readonly class ProjectForecastOnRecurringChange { public function __construct(private \\Illuminate\\Contracts\\Bus\\Dispatcher $bus) {} public function handle(\\Modules\\Recurring\\Public\\Events\\RecurringSeriesApproved|\\Modules\\Recurring\\Public\\Events\\RecurringSeriesCadenceFlipped|\\Modules\\Recurring\\Public\\Events\\RecurringSeriesRejected|\\Modules\\Recurring\\Public\\Events\\RecurringSeriesMetricsRefreshed $event): void { throw new \\RuntimeException('ProjectForecastOnRecurringChange::handle is a Wave 0 scaffold; Wave 2 (Plan 10-03) lands the real fan-out body.'); } }`. The constructor signature is the locked DI shape; the handle() body throws so Wave 2 swap-in is a body-only change (no signature breakage).

    **Modules/Forecasting/Internal/Listeners/ProjectForecastOnDriftDismissed.php**: Same scaffold shape. `handle(DriftAlertDismissedCancelled $event)`. Body throws RuntimeException with a Wave 4 note.

    **Modules/Forecasting/Internal/Listeners/ProjectForecastOnScenarioChange.php**: Same scaffold shape. `handle(object $event)` (the union type widens to a Phase 10 internal event union once Wave 4 lands the event classes). Body throws RuntimeException with a Wave 4 note.

    **Empty namespace .gitkeep markers**: Create empty `.gitkeep` files under `Modules/Forecasting/{Public/Contracts,Public/Dto,Public/Events,Public/Actions,Public/Services,Internal/Pipeline,Internal/Jobs,Internal/Http/Livewire,Resources/views/livewire}/`. This serves two purposes: (a) keeps the directory structure committed so subsequent waves can navigate it, and (b) gives the arch tests a target namespace to bind to from day one.

    **composer.json (project root)**: Append `"Modules\\Forecasting\\": "Modules/Forecasting/"` to `autoload.psr-4`, and `"Modules\\Forecasting\\Tests\\": "Modules/Forecasting/tests/"` to `autoload-dev.psr-4`. Mirror the existing DriftAlerts entries. Position alphabetically (after DriftAlerts, before Ingestion).

    **phpunit.xml (project root)**: Add a new `<testsuite name="Forecasting">` element containing `<directory>Modules/Forecasting/tests</directory>`. Mirror the existing DriftAlerts testsuite element verbatim.

    **tests/Pest.php (project root)**: Find the per-module map (a `foreach` loop or array literal mapping module-path to TestCase FQN). Add the row `'Modules/Forecasting' => Modules\\Forecasting\\Tests\\TestCase::class`. Position alphabetically (after DriftAlerts, before Ingestion).

    After every file is created, run `composer dump-autoload` so the new namespace resolves before Tasks 2 and 3 run.

    **Larastan posture for Wave 0**: PHPStan will flag missing-class errors for `ForecastPage` (Wave 2), `ForecastHighlightsQuery` (Wave 1), `ProjectForecastJob` (Wave 2), and the Phase 10 internal event classes (Wave 4). These are accepted Wave 0 errors and are listed in the acceptance criteria as the ONLY permitted PHPStan errors against the new module. No syntax errors, no namespace errors, no helper/facade-rule violations.
  </action>
  <verify>
    <automated>composer dump-autoload && vendor/bin/pest --filter="Forecasting" 2>&1 | head -50 ; vendor/bin/phpstan analyse Modules/Forecasting --memory-limit=2G --no-progress 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `composer dump-autoload` succeeds with zero warnings.
    - `php -r "require 'vendor/autoload.php'; var_dump(class_exists('Modules\\Forecasting\\Providers\\ForecastingServiceProvider'));"` prints `bool(true)`.
    - `php -r "require 'vendor/autoload.php'; var_dump(class_exists('Modules\\Forecasting\\Tests\\TestCase'));"` prints `bool(true)`.
    - `php -r "require 'vendor/autoload.php'; var_dump(class_exists('Modules\\Forecasting\\Internal\\Listeners\\ProjectForecastOnRecurringChange'));"` prints `bool(true)`.
    - `vendor/bin/pest --filter="Forecasting" --list-tests` exits 0 (testsuite discovered, zero tests yet).
    - `grep -v '^[[:space:]]*//' Modules/Forecasting/Providers/ForecastingServiceProvider.php | grep -c "core::livewire.top-nav"` equals 1 (top-nav composer registered exactly once).
    - `grep -v '^[[:space:]]*//' Modules/Forecasting/Providers/ForecastingServiceProvider.php | grep -c "RecurringSeriesApproved::class"` equals 1 (Phase 8 listener wired exactly once).
    - `grep -v '^[[:space:]]*//' Modules/Forecasting/Providers/ForecastingServiceProvider.php | grep -c "RecurringSeriesCadenceFlipped::class"` equals 1.
    - `grep -v '^[[:space:]]*//' Modules/Forecasting/Providers/ForecastingServiceProvider.php | grep -c "RecurringSeriesRejected::class"` equals 1.
    - `grep -v '^[[:space:]]*//' Modules/Forecasting/Providers/ForecastingServiceProvider.php | grep -c "RecurringSeriesMetricsRefreshed::class"` equals 1.
    - `grep -v '^[[:space:]]*//' Modules/Forecasting/Providers/ForecastingServiceProvider.php | grep -c "DriftAlertDismissedCancelled::class"` equals 1 (Phase 9 listener wired exactly once).
    - `grep -E "Modules\\\\\\\\Forecasting\\\\\\\\:" composer.json` finds the autoload entry.
    - `grep -E "Modules/Forecasting/tests" phpunit.xml` finds the testsuite entry.
    - `grep "Modules/Forecasting" tests/Pest.php` finds the per-module map row.
    - Listener-scaffold dispatch sanity check: `php -r "require 'vendor/autoload.php'; try { (new ReflectionClass('Modules\\Forecasting\\Internal\\Listeners\\ProjectForecastOnRecurringChange'))->getMethod('handle'); echo 'OK'; } catch (Throwable \$e) { echo \$e->getMessage(); }"` prints `OK`.
    - PHPStan is allowed to flag missing-class errors for `ForecastPage`, `ForecastHighlightsQuery`, `ProjectForecastJob`, `ScenarioCreated`, `ScenarioMutated`, `ScenarioDeleted` (all delivered by Waves 1-4). The acceptance criterion is that the ServiceProvider itself parses and that these missing-class errors are the ONLY PHPStan errors against `Modules/Forecasting/`. No syntax errors, no namespace errors, no helper/facade-rule violations against existing code paths.
    - `vendor/bin/pint --test Modules/Forecasting/` reports green for every new file.
  </acceptance_criteria>
  <done>
    Module skeleton is discoverable on the autoloader. ForecastingServiceProvider parses without syntax errors, wires the FIVE upstream Phase 8/9 events to the three Wave 0 listener scaffolds, and registers the top-nav composer with a Wave-0-safe zero default. Project-root composer.json + phpunit.xml + tests/Pest.php all reference the new module. The only PHPStan errors are the not-yet-implemented FQNs that Waves 1-4 land. Pest discovers the new testsuite (zero tests, exit 0). Pint is green.
  </done>
</task>

<task type="auto" id="10-01-02">
  <name>Task 2: Five BoundaryArchTest invariants — facade carve-out, namespace containment, transaction-substrate write guard, synchronous-projection guard, and load-bearing FCT-03 mutation-JOIN guard</name>
  <read_first>
    - tests/Contracts/BoundaryArchTest.php (entire file — read lines 1-50 for imports + the namespace-only-used-in arch tests for existing modules; lines 48-99 for the facade ignoring list; lines 109-126 for the SeriesDetector / DriftEvaluator synchronous-detection invariants; lines 522-581 for noTransactionWritesFromRecurring filesystem-walk pattern; lines 534-581 for noTransactionWritesFromEmailScan / noTransactionWritesFromRecurring precedents; lines 571-627 for noOtherRecurringSeriesStateMutator)
    - Modules/DriftAlerts/Internal/Jobs/DetectDriftAlertsJob.php (lines 1-100 — the existing facade-carve-out FQN pattern Phase 10 mirrors)
    - .planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-PATTERNS.md ("BoundaryArchTest extensions" section + "Pattern A/B/C/D/E" subsection)
    - .planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-RESEARCH.md ("Pattern 5: Pest arch invariants — mixed native + content-grep" section + "Example (D-1015 #5)" subsection at lines 715-755 — verbatim implementation of the load-bearing FCT-03 invariant)
  </read_first>
  <files>
    - tests/Contracts/BoundaryArchTest.php (MODIFY — append 5 new arch tests / extend facade ignore list)
  </files>
  <action>
    Extend `tests/Contracts/BoundaryArchTest.php` with the following five invariants. All five must be appended at the BOTTOM of the file, after the existing Phase 5-9 invariants. Each invariant mirrors the closest analog pattern named below. Skip-list for filesystem-walk variants: skip files under `tests/`, skip `Database/Migrations/`, skip the state-machine file itself (when relevant), strip block + line comments via the same regex the existing analog uses (lines 538-545).

    **Invariant 1 — `noFacadeCallsFromForecasting`** (native `arch()` carve-out extension). Extend the existing `arch('Modules code does not use facades except documented carve-outs')` rule (lines ~48-99) by appending the FQN `Modules\\Forecasting\\Internal\\Jobs\\ProjectForecastJob` to the `->ignoring([...])` list. Mirror the existing comment-block format so a new comment line above the new entry reads: `// Cache::driver('redis') facade carve-out: ShouldBeUniqueUntilProcessing requires the lock store at queue-push time, before constructor DI completes (mirrors DetectDriftAlertsJob).` The Wave 0 module has no `ProjectForecastJob.php` yet — the ignore-list entry references the future FQN; that is acceptable (the arch test only scans existing files).

    **Invariant 2 — `crossModuleAccessGoesThroughPublic` / `Modules\\Forecasting\\Internal is only used inside Modules\\Forecasting`** (native `arch()` mirroring the existing module rules at lines ~44-46):

    ```
    arch('Modules\\Forecasting\\Internal is only used inside Modules\\Forecasting (crossModuleAccessGoesThroughPublic)')
        ->expect('Modules\\Forecasting\\Internal')
        ->toOnlyBeUsedIn('Modules\\Forecasting');
    ```

    **Invariant 3 — `noSynchronousForecastingInRequestLifecycle`** (native `arch()` mirroring lines ~121-126). The ProjectionPipeline class is delivered in Wave 2 (Plan 10-03); the invariant references its future FQN; the arch test only scans existing files so it is trivially green in Wave 0:

    ```
    arch('ProjectionPipeline is never imported by Modules\\Forecasting\\Internal\\Http (noSynchronousForecastingInRequestLifecycle)')
        ->expect('Modules\\Forecasting\\Internal\\Pipeline\\ProjectionPipeline')
        ->not->toBeUsedIn([
            'Modules\\Forecasting\\Internal\\Http',
            'Modules\\Forecasting\\Resources',
        ]);
    ```

    **Invariant 4 — `noTransactionWritesFromForecasting`** (filesystem-walk; `it(...)` style mirroring lines ~534-581 of the analog `noTransactionWritesFromRecurring`). Copy the iterator + comment-strip identical to the analog, but widen the scan-dir to `Modules/Forecasting` and the regex to cover ALL FIVE forbidden write targets per D-1015 #2 (`transactions`, `recurring_series`, `card_statements`, `chain_links`, `drift_alerts`):

    ```
    it('does not allow any file under Modules/Forecasting/ to mutate transactions / recurring_series / card_statements / chain_links / drift_alerts tables (noTransactionWritesFromForecasting)', function (): void {
        $hits = [];
        $modulesDir = base_path('Modules/Forecasting');
        $iterator = new RecursiveIteratorIterator(
            new RecursiveDirectoryIterator($modulesDir, RecursiveDirectoryIterator::SKIP_DOTS),
        );
        foreach ($iterator as $file) {
            if (! $file->isFile() || preg_match('/\\.php$/', $file->getPathname()) !== 1) {
                continue;
            }
            $path = $file->getPathname();
            if (str_contains($path, '/tests/') || str_contains($path, '/Database/Migrations/')) {
                continue;
            }
            $contents = (string) file_get_contents($path);
            $stripped = preg_replace('#/\\*.*?\\*/|//[^\\n]*#s', '', $contents) ?? $contents;
            if (
                preg_match('/Transaction::query|Transaction::where|Transaction::create|RecurringSeries::query|RecurringSeries::create|RecurringSeries::firstOrCreate|RecurringSeries::updateOrCreate|CardStatement::query|CardStatement::create|ChainLink::query|ChainLink::create|DriftAlert::query|DriftAlert::create/', $stripped) === 1
                || preg_match("/->table\\(['\"](transactions|recurring_series|card_statements|chain_links|drift_alerts)['\"]\\)[^;]*->(update|insert|delete|truncate)\\s*\\(/", $stripped) === 1
            ) {
                $hits[] = $path;
            }
        }
        expect($hits)->toBe(
            [],
            "Modules/Forecasting must not write to transactions / recurring_series / card_statements / chain_links / drift_alerts. Offenders:\\n  ".implode("\\n  ", $hits),
        );
    });
    ```

    Important: this rule does NOT forbid READS — `Transaction::query()->where(...)` for a balance-since-anchor sum (Wave 2 BalanceAnchorResolver) is permitted. Only mutating verbs (`update|insert|delete|truncate`) and class-level static-call write methods (`::create`, `::firstOrCreate`, `::updateOrCreate`) are caught.

    **Invariant 5 — `noScenarioMutationsJoinedToTransactionQueries`** (filesystem-walk; the LOAD-BEARING FCT-03 invariant). Copy verbatim from `10-RESEARCH.md` lines 715-755 — full implementation provided there:

    ```
    it('does not allow any file to JOIN forecast_scenario_mutations onto transactions / recurring_series_occurrences / chain_links / card_statements (noScenarioMutationsJoinedToTransactionQueries)', function (): void {
        $hits = [];
        $modulesDir = base_path('Modules');
        $iterator = new RecursiveIteratorIterator(
            new RecursiveDirectoryIterator($modulesDir, RecursiveDirectoryIterator::SKIP_DOTS),
        );
        foreach ($iterator as $file) {
            if (! $file->isFile() || preg_match('/\\.php$/', $file->getPathname()) !== 1) {
                continue;
            }
            $path = $file->getPathname();
            if (str_contains($path, '/tests/')) {
                continue;
            }
            $contents = (string) file_get_contents($path);
            $stripped = preg_replace('#/\\*.*?\\*/|//[^\\n]*#s', '', $contents) ?? $contents;
            $hasMutationJoin = preg_match(
                "/->(join|leftJoin|rightJoin|crossJoin)\\(\\s*['\"]forecast_scenario_mutations['\"]/",
                $stripped,
            ) === 1;
            $hasForbiddenTable = preg_match(
                "/['\"](transactions|recurring_series_occurrences|chain_links|card_statements)['\"]/",
                $stripped,
            ) === 1;
            if ($hasMutationJoin && $hasForbiddenTable) {
                $hits[] = $path;
            }
        }
        expect($hits)->toBe(
            [],
            "forecast_scenario_mutations must never be JOINed onto transaction-substrate tables. Offenders:\\n  ".implode("\\n  ", $hits),
        );
    });
    ```

    This invariant scans the ENTIRE `Modules/` tree, not just `Modules/Forecasting/`, because the failure mode is "any future contributor (Forecasting OR another module) JOINs scenario mutations onto transactions for convenience". The structural arch test is the single most load-bearing invariant in Phase 10.

    Before committing, run BoundaryArchTest against the empty Wave 0 module — all five new invariants MUST pass (no files exist yet under `Modules/Forecasting/` that would trigger the filesystem-walk invariants; the empty module is the trivially-correct baseline). Also verify Phase 5-9 invariants stay green (zero regression).

    **Synthetic violation check** (acceptance criterion below): drop a sentinel file `Modules/Forecasting/__synthetic_violation.php` containing `<?php namespace Modules\\Forecasting; \\Illuminate\\Support\\Facades\\DB::table('forecast_scenario_mutations')->join('transactions', 'transactions.id', '=', 'forecast_scenario_mutations.target_series_id')->get();`, then run `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter=noScenarioMutationsJoinedToTransactionQueries` — the test MUST fail. Delete the sentinel before committing.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/BoundaryArchTest.php 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` exits 0; no failures.
    - `grep -c "noTransactionWritesFromForecasting" tests/Contracts/BoundaryArchTest.php` >= 1.
    - `grep -c "noScenarioMutationsJoinedToTransactionQueries" tests/Contracts/BoundaryArchTest.php` >= 1.
    - `grep -c "noSynchronousForecastingInRequestLifecycle" tests/Contracts/BoundaryArchTest.php` >= 1.
    - `grep -c "Modules\\\\\\\\Forecasting\\\\\\\\Internal is only used inside" tests/Contracts/BoundaryArchTest.php` >= 1.
    - `grep -c "Modules\\\\\\\\Forecasting\\\\\\\\Internal\\\\\\\\Jobs\\\\\\\\ProjectForecastJob" tests/Contracts/BoundaryArchTest.php` >= 1 (facade carve-out present).
    - All Phase 5-9 BoundaryArchTest invariants stay green (no regression).
    - **Synthetic violation check A** — `noScenarioMutationsJoinedToTransactionQueries`: drop a sentinel file `Modules/Forecasting/__synthetic_violation_join.php` containing the JOIN pattern described in the action; run `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter="noScenarioMutationsJoinedToTransactionQueries"` — MUST fail. Delete the sentinel; re-run — MUST pass.
    - **Synthetic violation check B** — `noTransactionWritesFromForecasting`: drop a sentinel file `Modules/Forecasting/__synthetic_violation_write.php` containing `<?php namespace Modules\\Forecasting; \\DB::table('transactions')->update(['type' => 'expense']);` — `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter="noTransactionWritesFromForecasting"` MUST fail. Delete the sentinel; re-run — MUST pass.
    - `vendor/bin/pint --test tests/Contracts/BoundaryArchTest.php` reports green.
  </acceptance_criteria>
  <done>
    All five new invariants exist in BoundaryArchTest.php, the facade carve-out is registered for ProjectForecastJob, the full BoundaryArchTest suite is green, and synthetic violations under `Modules/Forecasting/` correctly fire both content-grep invariants. Sentinel files deleted; commit is clean.
  </done>
</task>

<task type="auto" id="10-01-03">
  <name>Task 3: 10-scenario synthesised fixture corpus + corpus loader test</name>
  <read_first>
    - Modules/Recurring/tests/fixtures/synthesised/drifting-monthly-spotify.php (entire file — the analog shape for a single fixture: returns `['transactions' => $transactions, 'expected' => [...]]`)
    - Modules/Recurring/tests/fixtures/synthesised/stable-monthly-spotify.php (entire file — zero-drift fixture shape)
    - Modules/Recurring/tests/fixtures/synthesised/mixed-currency-netflix-usd.php (entire file — multi-currency fixture shape)
    - Modules/DriftAlerts/tests/fixtures/drift-corpus/stable-monthly.php (entire file — Phase 9 single-fixture shape)
    - Modules/DriftAlerts/tests/Unit/FixtureCorpusTest.php (entire file — Phase 9 corpus-loader test pattern Phase 10 mirrors)
    - .planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-CONTEXT.md (`<domain>` block "Synthesised fixture-first Wave 0" bullet — the authoritative 10-scenario list)
    - .planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-VALIDATION.md (Wave 0 Requirements section)
    - .planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-RESEARCH.md ("Synthesised fixture-first Wave 0" recommendations + the `Modules/Forecasting/tests/fixtures/` block in "Recommended Project Structure")
    - .planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-PATTERNS.md ("Shared #9: Wave-0 synthesised-fixture corpus" section)
  </read_first>
  <files>
    - Modules/Forecasting/tests/Unit/FixtureCorpusTest.php (NEW)
    - Modules/Forecasting/tests/fixtures/forecast-corpus/stable-monthly-subscription.php (NEW)
    - Modules/Forecasting/tests/fixtures/forecast-corpus/drifting-subscription-midwindow.php (NEW)
    - Modules/Forecasting/tests/fixtures/forecast-corpus/variable-utility.php (NEW)
    - Modules/Forecasting/tests/fixtures/forecast-corpus/salary-and-side-income.php (NEW)
    - Modules/Forecasting/tests/fixtures/forecast-corpus/ics-settlement-chain.php (NEW)
    - Modules/Forecasting/tests/fixtures/forecast-corpus/fx-only-usd-subscription.php (NEW)
    - Modules/Forecasting/tests/fixtures/forecast-corpus/zero-occurrence-edge-case.php (NEW)
    - Modules/Forecasting/tests/fixtures/forecast-corpus/buffer-crossing.php (NEW)
    - Modules/Forecasting/tests/fixtures/forecast-corpus/multi-account-baseline.php (NEW)
    - Modules/Forecasting/tests/fixtures/forecast-corpus/scenario-with-each-mutation-kind.php (NEW)
  </files>
  <action>
    Create the 10-scenario synthesised forecasting fixture corpus + the corpus loader test. The corpus is the authoritative dataset Wave 2 (`ForecastingProjectionContractTest`) and Wave 5 (`ScenarioIsolationContractTest`) iterate over.

    **Fixture file shape** (verbatim mirror of `Modules/DriftAlerts/tests/fixtures/drift-corpus/stable-monthly.php`): each file is a `<?php ... return [...];` file returning an associative array with three top-level keys:

    - `'series'` — a list of synthetic approved recurring-series rows (the Phase 8-substrate input the Forecasting pipeline would read). Each series row mirrors the `recurring_series` table column set: `{id, user_id, name, cadence ∈ {monthly, weekly, quarterly, yearly}, direction ∈ {expense, income}, account_id, latest_amount_minor, latest_currency, variance_tolerance_percent, state ∈ {approved, pending, rejected, irregular}, latest_fx_rate_used (nullable), next_expected_date (ISO 8601)}`. Include any historical `occurrences` (list of `{date, observed_amount_minor, observed_currency, fx_rate_used}`) when the fixture exercises the percentile tier or the FX-shadow rendering.
    - `'accounts'` — a list of synthetic account rows: `{id, user_id, name, kind ∈ {asn, ics_card, paypal}, default_currency, opening_balance_minor (nullable), opening_balance_as_of_date (nullable ISO date), forecast_min_buffer_minor (nullable)}`. At minimum one account; multi-account fixtures use 2-3.
    - `'expected'` — an associative array with three sub-keys:
      - `'projection'` — list of expected per-day points across the 30/60/90 horizons. Shape per entry: `{horizon_days ∈ {30, 60, 90}, account_id, date (ISO), low_minor, point_minor, high_minor, currency}`. Wave 2 contract test asserts the projector emits these points within a small tolerance.
      - `'shortfalls'` — list of expected `forecast_shortfall_windows` rows the projector should write. Shape: `{account_id, starts_at, ends_at, lowest_balance_minor, currency, buffer_used_minor}`. Empty list = no shortfalls expected.
      - `'scenarios'` (optional, only on `scenario-with-each-mutation-kind.php`) — list of expected scenario records the test pre-creates: `{id, name, mutations: [{kind ∈ {cancel_series, add_one_off, add_recurring, change_series_amount, shift_series_date}, payload: { kind-specific keys }}]}`. Wave 4 contract test uses these as the input scenario state.

    The keys in every `expected.*` entry MUST match the eventual Phase 10 schema column names exactly so Waves 2-5 can assert via `assertDatabaseHas('forecast_shortfall_windows', $row)` directly without a key remap.

    **The 10 fixtures** (one file each; filename matches the kebab-case scenario name; the list maps 1:1 to the CONTEXT.md `<domain>` "Synthesised fixture-first Wave 0" enumeration):

    1. **`stable-monthly-subscription.php`** — One ASN account (EUR, opening €1500, no buffer set) + one approved monthly expense series (`Netflix €11.99 × 6 stable occurrences`, `variance_tolerance_percent=5`). Expected projection: 30-day points show a flat band tightening to `±€11.99 × 0.05 = ±€0.60` per occurrence; one occurrence date in window; daily fold yields `low/high` close to point. Expected shortfalls: `[]`. This is the envelope-tier happy-path fixture Wave 2 will use to lock the projector's basic math.

    2. **`drifting-subscription-midwindow.php`** — Same ASN account + a Spotify series whose last occurrence was €9.99 but `latest_amount_minor=-1149` reflects a +15% drift (variance_tolerance_percent=5 still). Expected projection: 30-day points reflect the new €11.49 baseline; one Drift Alert from Phase 9 D-916 is referenced but not asserted (out of Phase 10 scope). Expected shortfalls: `[]` (drift alone doesn't cross the buffer here).

    3. **`variable-utility.php`** — Same ASN account + an Electricity series with `variance_tolerance_percent=45` (≥40% → percentile-tier trigger per D-1019) + 8 historical occurrences with observed_amount_minor varying from -€60 to -€220. Expected projection: 30-day points show a WIDE band (P10/P90 spread is large) on the occurrence date. Wave 5 (Plan 10-06) lands the percentile-tier impl; Wave 0 only locks the fixture's expected output structure. Expected shortfalls: `[]`.

    4. **`salary-and-side-income.php`** — Same ASN account + TWO approved income series (`Salary €3500/mo monthly`, direction=income; `Side income €450/mo monthly`, direction=income). Expected projection: 30-day points show net positive flow on payday(s); income series project with `+` direction in the daily fold. Expected shortfalls: `[]`. Exercises FCT-01's income-side coverage and LED-06 carry-forward.

    5. **`ics-settlement-chain.php`** — TWO accounts (ASN + ICS). ICS has 6 small expense lines (Adobe €19.99, Cloudflare $20, etc.) cleared since last settlement. Phase 5 `card_statements.nextSettlement` payload included in the fixture's `chain_state` sub-key: `{next_settlement_date, next_settlement_amount_minor, account_id}`. Expected projection: 30-day ASN band dips by the settlement amount on the settlement date (chain-aware routing — Wave 3 ChainAwareForecastRouter consumes this fixture). ICS view shows running owed amount unchanged (chain-aware deducts only from funder). Wave 0 only locks the fixture; Wave 3 (Plan 10-04) lands the router.

    6. **`fx-only-usd-subscription.php`** — Same ASN + a Netflix USD series (`latest_amount_minor=-1199 USD`, `latest_currency='USD'`, `latest_fx_rate_used=0.9050`). Expected projection: per-series legend renders `$11.99/mo (≈ €10.85/mo)`; per-account ASN chart converts to EUR via the stored fx_rate; daily fold runs in EUR; band reflects the EUR-converted amounts. Exercises D-1006 (FX preservation).

    7. **`zero-occurrence-edge-case.php`** — Same ASN + a single approved series with EMPTY occurrence history (just-approved series; latest_amount_minor set, but no prior occurrences). Expected projection: envelope tier applies (since there's no occurrence history to support percentile); 30-day points use `latest_amount_minor × variance_tolerance_percent` directly. Expected shortfalls: `[]`. Locks the "freshly-approved series with no history" edge case.

    8. **`buffer-crossing.php`** — Same ASN account but `forecast_min_buffer_minor = -50000` (€500 buffer) + opening balance €600 + 3 monthly expense series totaling -€200/mo. Expected projection: 30-day fold drops below €500 on day 7 (when the largest expense lands) and recovers by day 22. Expected shortfalls: one entry `{account_id: 1, starts_at: 'day 7', ends_at: 'day 22', lowest_balance_minor: 40000, currency: 'EUR', buffer_used_minor: 50000}`. The hand-rolled `starts_at`/`ends_at` are ISO dates relative to a fixed `as-of` date the test sets via Clock fake. This is the LOAD-BEARING fixture for FCT-05.

    9. **`multi-account-baseline.php`** — THREE accounts (ASN + ICS + PayPal) + a mix of 6 approved series spanning all three account_ids. Expected projection: per-account points listed for each of the three accounts at each of the 30/60/90 horizons. Expected shortfalls: `[]`. Locks the multi-account baseline read path Wave 3 exercises.

    10. **`scenario-with-each-mutation-kind.php`** — Same as fixture #9 + a `scenarios` sub-block with ONE saved scenario containing FIVE mutations — one per D-1007 kind: `cancel_series` (cancel one expense series), `add_one_off` (add a planned €200 expense in 14 days), `add_recurring` (add a new €15/mo subscription starting in 7 days), `change_series_amount` (override one series's amount to -€25), `shift_series_date` (shift one series's next_expected_date by +5 days, scope=`next` only). Expected: a `projection.scenario` sub-block listing the deltas per horizon. Wave 4 (Plan 10-05) is the consumer; Wave 0 only locks the input fixture + the expected output structure. This is the FCT-03/FCT-04 reference fixture.

    Every fixture's `expected.projection` entries MUST use BIGINT minor units (FND-04) and ISO-8601 dates (`YYYY-MM-DD`). Every `expected.shortfalls` entry MUST contain all six required columns (no NULLs except where the schema explicitly nullables them). Negative direction = expense (signed minor: -1199 = €11.99 outflow); positive direction = income.

    **Modules/Forecasting/tests/Unit/FixtureCorpusTest.php**: Pest test that uses `dataset()` to iterate every file under `Modules/Forecasting/tests/fixtures/forecast-corpus/*.php`. For each fixture, assert:
    - The file returns an array.
    - The array has the three required top-level keys: `'series'` (list), `'accounts'` (list), and `'expected'` (associative).
    - `'accounts'` is non-empty; each entry has `id`, `user_id`, `name`, `kind`, `default_currency` keys; `kind` ∈ `{asn, ics_card, paypal}`.
    - `'series'` is a list (possibly empty for the multi-scenario fixture); each entry has the required series-row keys; `direction` ∈ `{expense, income}`; `state` ∈ `{approved, pending, rejected, irregular}`; `cadence` ∈ `{monthly, weekly, quarterly, yearly}`.
    - `expected.projection` is a list; every entry has `{horizon_days, account_id, date, low_minor, point_minor, high_minor, currency}`; `horizon_days` ∈ `{30, 60, 90}`; `low_minor <= point_minor <= high_minor`.
    - `expected.shortfalls` is a list (possibly empty); every entry has `{account_id, starts_at, ends_at, lowest_balance_minor, currency, buffer_used_minor}`.
    - For fixture #10 only: `expected.scenarios` is present and each mutation's `kind` is one of the five locked kinds.

    The test must run all 10 fixtures and pass.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Forecasting/tests/Unit/FixtureCorpusTest.php 2>&1 | tail -30 ; ls -1 Modules/Forecasting/tests/fixtures/forecast-corpus/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls -1 Modules/Forecasting/tests/fixtures/forecast-corpus/*.php | wc -l` equals 10.
    - All 10 expected filenames exist: `stable-monthly-subscription.php`, `drifting-subscription-midwindow.php`, `variable-utility.php`, `salary-and-side-income.php`, `ics-settlement-chain.php`, `fx-only-usd-subscription.php`, `zero-occurrence-edge-case.php`, `buffer-crossing.php`, `multi-account-baseline.php`, `scenario-with-each-mutation-kind.php`.
    - `vendor/bin/pest Modules/Forecasting/tests/Unit/FixtureCorpusTest.php` exits 0 and runs at least 10 dataset cases.
    - For each fixture, `php -r "var_dump(array_keys(require 'Modules/Forecasting/tests/fixtures/forecast-corpus/<NAME>.php'))"` returns an array containing the three top-level keys `series`, `accounts`, `expected`.
    - `buffer-crossing.php` returns `expected.shortfalls` non-empty with at least one entry having `lowest_balance_minor < buffer_used_minor` (the shortfall semantic).
    - `scenario-with-each-mutation-kind.php` returns `expected.scenarios[0].mutations` with exactly 5 entries, one per kind (`cancel_series`, `add_one_off`, `add_recurring`, `change_series_amount`, `shift_series_date`).
    - `ics-settlement-chain.php` includes a `chain_state` sub-block with `next_settlement_date` + `next_settlement_amount_minor` keys.
    - `fx-only-usd-subscription.php` includes at least one series with `latest_currency='USD'` and `latest_fx_rate_used` not null.
    - PHPStan against `Modules/Forecasting/tests/` reports no errors (fixtures are pure data; the loader test is statically clean).
    - `vendor/bin/pint --test Modules/Forecasting/tests/` reports green.
  </acceptance_criteria>
  <done>
    All 10 forecasting fixture files exist with the documented shape, FixtureCorpusTest iterates all 10 datasets green, and the corpus is ready for Plan 10-03 (Wave 2 ForecastingProjectionContractTest), Plan 10-04 (Wave 3 shortfall + chain routing), and Plan 10-05/10-06 (Wave 4/5 scenario isolation + percentile tier).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → autoloader | Composer autoload resolution at app boot; the new module must not pollute the autoload graph |
| module → module | Forecasting must not import from any other module's Internal namespace; arch tests enforce |
| scenario-mutation substrate ↔ transaction-substrate | The single most load-bearing boundary in Phase 10. forecast_scenario_mutations MUST NOT JOIN onto transactions / recurring_series_occurrences / chain_links / card_statements (FCT-03 invariant) |
| test fixtures → contract tests | Fixture data must not embed live PII (synthesised only); verified by review of fixture contents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | Future forecasting code accidentally calls a facade or bypasses DI | mitigate | `noFacadeCallsFromForecasting` arch test (Task 2 Invariant 1) — extends the existing `Modules code does not use facades except documented carve-outs` rule with the Forecasting Job FQN carve-out; synthetic violation check in acceptance criteria proves it fires |
| T-10-01-02 | Tampering | Future forecasting code writes to transactions / recurring_series / card_statements / chain_links / drift_alerts (D-1015 #2 invariant violated) | mitigate | `noTransactionWritesFromForecasting` arch test (Task 2 Invariant 4) — filesystem-walk; synthetic violation check in acceptance criteria proves the regex catches mutation patterns across all five forbidden tables |
| T-10-01-03 | Tampering / Information Disclosure | A future developer JOINs forecast_scenario_mutations onto the transaction substrate (FCT-03 boundary violated, scenarios leak into real-money reads) | mitigate | `noScenarioMutationsJoinedToTransactionQueries` arch test (Task 2 Invariant 5) — the LOAD-BEARING FCT-03 invariant; synthetic violation check confirms it fires on join() / leftJoin() / rightJoin() / crossJoin() patterns against forecast_scenario_mutations + any forbidden table |
| T-10-01-04 | Tampering | Future code performs synchronous projection inside the request lifecycle (request hangs while quadrature folds 90 days × N series) | mitigate | `noSynchronousForecastingInRequestLifecycle` arch test (Task 2 Invariant 3) — pins ProjectionPipeline imports to Internal/Pipeline + Jobs; Wave 0 wires the invariant against the future FQN, Wave 2 brings the class in |
| T-10-01-05 | Tampering | Cross-module import bypasses Public surface (Forecasting reaches into Modules/Recurring/Internal/) | mitigate | `crossModuleAccessGoesThroughPublic` arch test (Task 2 Invariant 2) — pins Modules\Forecasting\Internal to toOnlyBeUsedIn(Modules\Forecasting); inverse symmetry preserves Phase 9 inheritance |
| T-10-01-SC | Tampering | composer install introduces unverified packages | accept | Phase 10 introduces ZERO new composer packages (verified in 10-RESEARCH.md `Package Legitimacy Audit`); no install step in this plan |
</threat_model>

<verification>
Wave 0 phase-level checks:
- `composer dump-autoload` succeeds.
- `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` is green (Phase 5-9 invariants + 5 new Phase 10 invariants).
- `vendor/bin/pest Modules/Forecasting/tests/Unit/FixtureCorpusTest.php` is green (10 datasets).
- `vendor/bin/pest --filter=Forecasting` discovers the new testsuite (only the corpus loader unit test yet — Wave 0 ships nothing else).
- `vendor/bin/phpstan analyse Modules/Forecasting --memory-limit=2G` reports only missing-class errors for the FQNs that Waves 1-5 will land (ForecastPage, ForecastHighlightsQuery, ProjectForecastJob, ProjectionPipeline, ScenarioCreated, ScenarioMutated, ScenarioDeleted). No syntax errors. No facade/helper-rule violations against existing code.
- `vendor/bin/pint --test Modules/Forecasting/ tests/Contracts/BoundaryArchTest.php` reports green.
- Listener scaffolds parse, are wired in ServiceProvider, and throw RuntimeException on handle() invocation (Wave 2-4 swaps body).
- The top-nav composer registers `forecastShortfallCount=0` unconditionally (Wave 3 lands the real read).
</verification>

<success_criteria>
- `Modules/Forecasting/` module skeleton exists; ForecastingServiceProvider parses; routes file parses; testsuite registered (3-step pattern verified by grep).
- Five BoundaryArchTest invariants pass against the empty Wave 0 module (trivially-correct baseline).
- Synthetic violation checks confirm `noScenarioMutationsJoinedToTransactionQueries` AND `noTransactionWritesFromForecasting` fire correctly when violations are dropped under `Modules/Forecasting/`.
- Three listener scaffolds parse, are singleton-bound in register(), are listener-mapped in registerListeners(), and throw on invocation (deliberately — Waves 2-4 swap the bodies).
- 10-scenario synthesised forecasting fixture corpus on disk; corpus-loader unit test green (all 10 datasets).
- The plan's `must_haves.truths` are all true; the module is ready for Plan 10-02 (Wave 1 — schema + Eloquent models + Public DTOs + state machine if any) to land on top.

Goal-backward proof of doneness: Run `composer dump-autoload && vendor/bin/pest --filter="Forecasting"` and `vendor/bin/pest tests/Contracts/BoundaryArchTest.php`. Both exit 0. Inspect `Modules/Forecasting/Providers/ForecastingServiceProvider.php` — five Phase 8/9 event listeners wired exactly once each via grep. Inspect `Modules/Forecasting/tests/fixtures/forecast-corpus/` — exactly 10 .php files present. Drop a sentinel JOIN violation file and confirm `noScenarioMutationsJoinedToTransactionQueries` fires red, then delete the sentinel and confirm green again. Wave 0 is then unambiguously complete.
</success_criteria>

<output>
Create `.planning/phases/10-cash-flow-forecasting-what-if-scenarios/10-01-SUMMARY.md` when done.
</output>
