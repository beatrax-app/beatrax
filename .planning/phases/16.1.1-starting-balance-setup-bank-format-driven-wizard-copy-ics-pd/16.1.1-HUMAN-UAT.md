---
status: partial
phase: 16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd
source: [16.1.1-VERIFICATION.md, 16.1.1-HANDOVER.md]
started: 2026-05-26T18:42:00Z
updated: 2026-05-26T23:50:00Z
---

## Current Test

[end-to-end commit flow still needs one final run after all the post-phase fixes — see item 1 below]

## Tests

### 1. End-to-end wizard commit flow
expected: Run the wizard with real bank CSV + ICS PDF files. Final step shows the consolidated commit surface, one section per ImportRun source, one `StartingBalanceCard` per detected account (bank + ICS card). Clicking "Commit everything (N transactions) →" wraps every `ConfirmImport(..., dispatchChain: false)` call + every `Account::setStartingBalance(...)` write in a single `DB::transaction`. The old `<livewire:import.upload-wizard />` step inside the wizard is gone. After Finish, you land on the dashboard with imports persisted and starting balances set.
result: pending — partially observed (counts correct, cards render correctly). Final Commit click + transition still needs verification after the `modal-close` fix lands.

### 2. ConnectEmailStep OAuth modal
expected: On the wizard's email step, "Connect Gmail" AND "Authorize with Outlook" both open the OAuth client wizard modal. Each modal Cancel button closes it. The X (Flux internal) also closes. In `/inboxes`, the reconnect buttons open the modal too.
result: passing — fixed via `@fluxScripts` + `@vite app.js` + `modal-close` event + `wire:key` on the modal (commits a43f28e, 1fafe4b, e0c7642).

### 3. ConnectBankStep format chips
expected: Format-first chip flow renders for available formats (CAMT.053, MT940, ASN-CSV, ING-CSV). CSV file picker is gated until a chip is selected. Copy follows D-18 calm tone. Wrong format-hint surfaces an error before parse.
result: pending visual verification.

### 4. ConnectCardStep multi-file drop
expected: Three ICS PDF statements dropped simultaneously render three per-file chips, each with its own status. `card_import_run_ids` stash carries all three IDs so the consolidated commit surface picks them up.
result: pending visual verification.

### 5. /dev/artisan page rendering
expected: HTTP 200, non-blank body, no white-page regression, no transparent background in dark mode. Non-developers get 404.
result: pending visual verification.

## Summary

total: 5
passed: 1
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

(No regression-class gaps recorded — all post-phase fixes have either passing tests or are queued under handover items.)

## Notes — items NOT in this UAT but tracked

See `16.1.1-HANDOVER.md` for the full follow-up backlog: new features (PayPal step, preview pagination, visual tidying), gap-closure tests (FirstImportStep commit-path coverage), and pre-existing test failures (separate concern from this phase).
