---
status: partial
phase: 16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd
source: [16.1.1-VERIFICATION.md]
started: 2026-05-26T18:42:00Z
updated: 2026-05-26T18:42:00Z
---

## Current Test

[awaiting human testing — rebuild the `.app` then walk through the items below]

## Tests

### 1. End-to-end wizard commit flow
expected: Run the wizard with real bank CSV + ICS PDF files (or PayPal CSV — should now show the actionable "wrong export type" message if it's a BRR). On the final step, the consolidated commit surface displays one section per ImportRun source AND one `StartingBalanceCard` per detected account. Clicking "Commit everything" (or whatever the Finish button is called) wraps every `ConfirmImport(..., dispatchChain: false)` call AND every `Account::setStartingBalance(...)` write inside a single `DB::transaction` — i.e. the wizard's Finish is atomic. The previously-visible `<livewire:import.upload-wizard />` step inside the wizard MUST be gone. After Finish, you should land on the dashboard with the imports persisted, starting balances set, and the wizard NOT showing an "import" step anymore.
result: [pending]

### 2. ConnectEmailStep OAuth modal
expected: On the wizard's email step, click "Connect Gmail" — the OAuth client wizard modal opens (rendered from `resources/views/layouts/app-wizard.blade.php`, not from the step's own blade). Same flow for "Connect Microsoft" / "Connect Outlook". The modal-open event is dispatched globally; modal lives outside the step. Separately, in the authenticated app at `/inboxes`, the "reconnect" buttons should also open the modal correctly (mounted from `resources/views/layouts/app.blade.php`).
result: [pending]

### 3. ConnectBankStep format chips
expected: Visual verification of the format-first chip flow — chips render for available formats (ASN, ING, and others if added), the CSV file picker is gated until a chip is selected, the wording matches D-18 copy constraint (calm, content-first, no "advanced" / "expert" jargon). Drop a CSV with the wrong format-hint and confirm the error surfaces before parse attempt.
result: [pending]

### 4. ConnectCardStep multi-file drop
expected: Drop three ICS PDF statements simultaneously on the card step. Three per-file chips render, each with its own status (parsing, parsed, error). The final `card_import_run_ids` stash contains all three ImportRun ids so the consolidated commit surface picks them up.
result: [pending]

### 5. /dev/artisan page rendering
expected: Visit `/dev/artisan` in the .app bundle as a developer user. HTTP 200, non-blank body, no white-page regression, no transparent background in dark mode. As a non-developer, the same URL must 404.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

(No gaps recorded yet — populate as you run the tests above.)

## Notes — items NOT in this UAT but tracked

Three FirstImportStep commit-path regression tests were not written before the prior executor was killed by the harness:
- `FirstImportStepCommitEverythingTest`
- `FirstImportStepCommitRollbackTest`
- `FirstImportStepStaleFilterTest`

The underlying `DB::transaction` + `dispatchChain: false` logic IS implemented at `FirstImportStep.php` lines 229–238 (and is what UAT item #1 above will exercise end-to-end). The 5 plan-required tests that DID land all pass. Recommend a small follow-up plan in a later phase to close the coverage gap.

PayPal third-locale support: `HeaderSniffer` currently locks PayPal CSV detection to Dutch + English. Other locales will be triaged separately (logged in `16.1.1-04-deferred-items.md`).
