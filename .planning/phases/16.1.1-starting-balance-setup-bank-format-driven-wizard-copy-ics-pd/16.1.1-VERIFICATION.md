---
phase: 16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd
verified: 2026-05-26T00:00:00Z
status: human_needed
score: 9/10 must-haves verified
overrides_applied: 0
human_verification:
  - test: "End-to-end wizard UAT: sign up fresh, run wizard through WelcomeStep → ConnectBankStep (drop a CAMT.053 or MT940 file) → ConnectCardStep (drop 2–3 ICS PDFs) → FirstImportStep (review consolidated preview + confirm starting balances) → click 'Commit everything (N transactions) →' → DoneStep"
    expected: "transactions table populates with rows from both sources; accounts.starting_balance_minor and starting_balance_date are written; wizard advances to DoneStep; no toast error; chain resolution job dispatched once"
    why_human: "The 3 commit-path regression tests (FirstImportStepCommitEverythingTest, FirstImportStepCommitRollbackTest, FirstImportStepStaleFilterTest) were not written before the harness killed plan 07b's executor. The underlying DB::transaction logic IS implemented (verified by grep and code read), but coverage of the full happy-path + rollback + stale-filter behaviors can only be confirmed by running the wizard against the built .app."
  - test: "ConnectEmailStep OAuth modal: sign up, run wizard to ConnectEmailStep, click 'Connect Gmail' — verify the OAuthClientWizardModal opens. Click 'Connect Microsoft' — verify same modal opens. Confirm it is NOT mounted inside the step's own HTML but in the layout body."
    expected: "Modal opens from the global layout mount; the step blade has no inline modal tag; both provider buttons trigger the modal"
    why_human: "Plan 02 test confirms dispatch contract and absence of inline mount, but the actual modal opening in the built .app bundle requires visual confirmation."
  - test: "ConnectBankStep format chips: navigate to ConnectBankStep, verify three format chips (CAMT.053, MT940, CSV) are present. Select CSV — verify drop zone is disabled with gating copy. Pick 'ASN' bank chip — verify drop zone activates with ASN-specific copy. Verify no copy in the page says '(ASN)' or 'Your bank (ASN)' outside the bank-list helper line."
    expected: "Format chips render; CSV gate works; drop zone activates on bank pick; D-18 copy constraint satisfied"
    why_human: "Visual inspection of the rendered UI is required to verify the exact copy contract (D-18 restriction) and the aria-disabled visual rendering."
  - test: "ConnectCardStep multi-file: navigate to ConnectCardStep, drop 3 ICS PDFs simultaneously. Verify per-file chips appear with filenames. Verify all 3 files are accepted and their run IDs stashed. Verify a non-PDF is rejected with an inline validation error."
    expected: "3 chips displayed; step advances; wizard_progress.data['card_import_run_ids'] has 3 entries; non-PDF shows validation message"
    why_human: "Livewire multi-file drop interaction in the built bundle requires real user interaction to verify chip rendering and per-file state transitions."
  - test: "/dev/artisan page: sign in as developer, navigate to /dev/artisan, verify the page renders with non-blank body and the 'Run a command' button is present and functions. Verify no inline style=\"background: var(...)\" occurs in the rendered page DOM."
    expected: "Page renders; Run a command button is functional; no transparent-background regression in dark mode"
    why_human: "Layer A/B fix from Plan 03 involved a no-repro outcome; regression is verified by Pest test but actual rendered appearance in the .app bundle (especially dark mode) requires visual check."
---

# Phase 16.1.1 Verification Report

**Phase Goal:** Close out all nine UAT findings from Phase 16.1 (G1–G9) so a new user can sign up → run the first-run wizard → see real transactions AND real starting balances on the dashboard.
**Verified:** 2026-05-26
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Brand SVG renders in authenticated sidebar top-left via Vite::asset() (G5) | ✓ VERIFIED | `app-sidebar.blade.php` line 21 uses `Vite::asset('resources/brand/logo.svg')`; `vite.config.js` input includes `'resources/brand/logo.svg'`; `BrandSvgRenderTest` passes |
| 2 | OAuthClientWizardModal mounted exactly once in BOTH `app.blade.php` AND `app-wizard.blade.php`; connect-email-step.blade.php has no inline mount (G6) | ✓ VERIFIED | `app.blade.php` line 136: `@livewire('email-scan.oauth-client-wizard-modal')`; `app-wizard.blade.php` line 71: same; connect-email-step.blade.php grep returns 0 matches; `ConnectEmailStepDispatchesGlobalOAuthOpenTest` (3 tests) passes |
| 3 | `/dev/artisan` renders non-blank HTTP 200 for developer user; no inline-var background regression (G2) | ✓ VERIFIED | `ArtisanRunnerNotWhitePageTest` (3 tests) passes: 200 check + no-inline-var check + 404 non-developer |
| 4 | ICS PDF preview produces non-empty rows with no ERROR-disposition for fixture (G4 — no-repro confirmed) | ✓ VERIFIED | `IcsPdfPreviewRegressionTest` (2 tests) passes against `ics-sample-tiny.pdf`; PayPal BRR shape detection fires before language-profile check (`HeaderSniffer.php` lines 175–184) |
| 5 | `accounts.starting_balance_minor` and `starting_balance_date` columns exist; backfill is idempotent; Account `$fillable` includes both (G1 schema — Plan 05) | ✓ VERIFIED | Migrations `2026_05_27_000001_*` and `2026_05_27_000002_*` present; Account model lines 39–48 show fillable + casts; `AccountStartingBalanceMigrationTest` (3 tests) passes |
| 6 | Four `DetectsStartingBalance` implementations tagged under `starting-balance.detector`; `DetectStartingBalancesQuery` applies D-04 conflict resolution (G1 detection — Plan 05b) | ✓ VERIFIED | All 4 detectors in `Modules/Import/Internal/Detectors/`; `ImportServiceProvider.php` has 4 occurrences of `starting-balance.detector`; `StartingBalanceDetectorRegistryTest` + `DetectStartingBalancesQueryConflictTest` (7 tests) pass |
| 7 | `BankCsvFormatHint` enum exists; ConnectBankStep renders 3 format chips + CSV gate; bank_import_run_id stashed; ConnectCardStep multi-file with card_import_run_ids stash (G3 + G8 — Plan 06) | ✓ VERIFIED | `BankCsvFormatHint.php` with Asn/Ing cases; blade has 3 format chips; `ConnectBankStep.php` lines 94+223; `ConnectCardStep.php` lines 78+180; `ConnectBankStepFormatGatingTest` (6 tests) + `ConnectCardStepMultiFileTest` (3 tests) + `ConnectCardStepStashesAllRunIdsTest` (2 tests) pass |
| 8 | `BuildConsolidatedPreviewQuery::build()` returns `ConsolidatedPreviewBatch` with per-source sections, stale/confirmed filters, user-id boundary; `ConfirmImport` has `dispatchChain: bool = true` parameter (G7 backend — Plan 07) | ✓ VERIFIED | `BuildConsolidatedPreviewQuery.php` `final readonly class` at line 52 with `build()` at line 83; `ConfirmImport.php` line 61 has `bool $dispatchChain = true`; `ConfirmsImports.php` line 28 matches; `BuildConsolidatedPreviewQueryTest` (4 tests, 23 assertions) pass |
| 9 | `FirstImportStep` rewrite: loads stash, renders consolidated preview sections + StartingBalanceCard per account, commit() runs DB::transaction wrapping ConfirmImport(dispatchChain: false) calls; old `<livewire:import.upload-wizard />` wrapper gone (G7/G9 UI — Plan 07b) | ✓ VERIFIED | `first-import-step.blade.php` grep for `upload-wizard` returns 0; blade contains `consolidated-preview-section` and `starting-balance-card`; `FirstImportStep.php` has DB::transaction (line 229), `dispatchChain: false` (line 238), `BuildConsolidatedPreviewQuery` (8 occurrences), `balanceConfirmations` (9 occurrences); `ConsolidatedPreviewLoadTest` + 4 StartingBalance tests (9 tests, 15 assertions) pass |
| 10 | ROADMAP.md Phase 16.1.1 entry reflects close-out title + all 9 G-ids + 10-plan list (D-22 — Plan 08) | ⚠ PARTIAL | Title and goal updated correctly: "First-run wizard UAT close-out" title present; goal text set; all 10 plan entries listed. However, the tracking counter reads "6/10 plans executed" instead of "10/10" — plans 04, 07, 07b are marked `[ ]` (unchecked) despite having commits, SUMMARY.md files, and passing tests. This is a ROADMAP tracking artifact, not a code gap. The D-22 substantive requirements (title, goal, 10-plan list) are met. |

**Score:** 9/10 truths fully verified (1 partial — tracking artifact only)

### Acknowledged Gap: Three Commit-Path Regression Tests Missing (Pre-Acknowledged)

Per the 07b SUMMARY and the verification request: `FirstImportStepCommitEverythingTest`, `FirstImportStepCommitRollbackTest`, and `FirstImportStepStaleFilterTest` were not written before the harness killed the plan 07b executor. These files do not exist on disk.

The underlying logic IS implemented:

- `FirstImportStep::commit()` at line 229 uses `$db->connection()->transaction(fn () => ...)`
- `dispatchChain: false` passed at line 238 for every ImportRun in the loop
- Chain dispatch happens after the outer transaction returns (line 250+)

This is regression-coverage missing, not behavior missing. Recommend a follow-up plan to add these 3 tests. Treated as a tracked gap per the pre-acknowledgment in the verification request, not a blocker.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|---------|--------|---------|
| `vite.config.js` | Includes resources/brand/logo.svg | ✓ VERIFIED | Line 8 |
| `Modules/Core/Resources/views/livewire/app-sidebar.blade.php` | Renders SVG via Vite::asset | ✓ VERIFIED | Line 21 |
| `resources/views/layouts/app.blade.php` | Global OAuth modal mount | ✓ VERIFIED | Line 136 |
| `Modules/Onboarding/Resources/views/layouts/app-wizard.blade.php` | Global OAuth modal mount | ✓ VERIFIED | Line 71 |
| `Modules/Onboarding/Resources/views/livewire/steps/connect-email-step.blade.php` | No inline modal mount | ✓ VERIFIED | grep count = 0 |
| `Modules/Ledger/Database/Migrations/2026_05_27_000001_*` | Schema migration | ✓ VERIFIED | File exists |
| `Modules/Ledger/Database/Migrations/2026_05_27_000002_*` | Backfill migration | ✓ VERIFIED | File exists |
| `Modules/Ledger/Internal/Services/BackfillStartingBalanceFromStatementSummaries.php` | Idempotent backfill service | ✓ VERIFIED | File exists |
| `Modules/Ledger/Models/Account.php` | $fillable has starting_balance_* | ✓ VERIFIED | Lines 39–48 |
| `Modules/Import/Public/Contracts/DetectsStartingBalance.php` | Strategy contract | ✓ VERIFIED | File exists |
| `Modules/Import/Public/Dto/StartingBalanceCandidate.php` | DTO | ✓ VERIFIED | File exists |
| `Modules/Import/Internal/Detectors/Camt053StartingBalanceDetector.php` | CAMT detector | ✓ VERIFIED | File exists |
| `Modules/Import/Internal/Detectors/Mt940StartingBalanceDetector.php` | MT940 detector | ✓ VERIFIED | File exists |
| `Modules/Import/Internal/Detectors/IcsPdfStartingBalanceDetector.php` | ICS PDF detector | ✓ VERIFIED | File exists |
| `Modules/Import/Internal/Detectors/PaypalCsvStartingBalanceDetector.php` | PayPal detector | ✓ VERIFIED | File exists |
| `Modules/Import/Public/Services/DetectStartingBalancesQuery.php` | Aggregator | ✓ VERIFIED | File exists |
| `Modules/Import/Public/Enums/BankCsvFormatHint.php` | Enum with Asn/Ing cases | ✓ VERIFIED | Lines 22–23 with adapterFormatKey()/displayName() |
| `Modules/Import/Public/Contracts/RunsImports.php` | Extended with formatHint param | ✓ VERIFIED | BankCsvFormatHint in signature |
| `Modules/Import/Internal/Pipeline/ImportPipeline.php` | BankCsvFormatHint param + Layer B guard | ✓ VERIFIED | Lines 71–82 |
| `Modules/Import/Public/Dto/ConsolidatedPreviewSection.php` | DTO | ✓ VERIFIED | File exists |
| `Modules/Import/Public/Dto/ConsolidatedPreviewBatch.php` | DTO | ✓ VERIFIED | File exists |
| `Modules/Import/Public/Services/BuildConsolidatedPreviewQuery.php` | Read-side query | ✓ VERIFIED | Lines 52+83 |
| `Modules/Import/Public/Actions/ConfirmImport.php` | dispatchChain flag | ✓ VERIFIED | Line 61 |
| `Modules/Import/Public/Contracts/ConfirmsImports.php` | Contract matches | ✓ VERIFIED | Line 28 |
| `Modules/Onboarding/Internal/Http/Livewire/StartingBalanceCard.php` | Livewire child component | ✓ VERIFIED | state machine at line 120 |
| `Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` | Rewrite (no upload-wizard) | ✓ VERIFIED | DB::transaction + dispatchChain + balanceConfirmations; no upload-wizard |
| `Modules/Onboarding/Resources/views/components/consolidated-preview-section.blade.php` | Blade partial | ✓ VERIFIED | File exists, referenced in first-import-step blade |
| `.planning/ROADMAP.md` | D-22 title + goal + 10-plan list | ✓ VERIFIED (partial tracking) | Title/goal/10 plans listed; counter shows 6/10 (tracking artifact) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `app-sidebar.blade.php` | `resources/brand/logo.svg` | `Vite::asset()` | ✓ WIRED | Line 21 exact match |
| `app.blade.php` | `OAuthClientWizardModal` | global `@livewire` mount | ✓ WIRED | Line 136 |
| `app-wizard.blade.php` | `OAuthClientWizardModal` | global `@livewire` mount | ✓ WIRED | Line 71 |
| `ConnectEmailStep.php` | `OAuthClientWizardModal` | `dispatch('oauth-client-wizard:open', ...)` | ✓ WIRED | Line 63 |
| `ConnectBankStep.php` | `wizard_progress.data` | `$data['bank_import_run_id'] = $result->importRunId` | ✓ WIRED | Line 223 |
| `ConnectCardStep.php` | `wizard_progress.data` | `$data['card_import_run_ids']` array write | ✓ WIRED | Lines 180–190 |
| `ConnectBankStep.php` | `ImportPipeline` | `runFromUpload(..., $formatHint)` | ✓ WIRED | `BankCsvFormatHint::from` in submit() |
| `ImportPipeline::preview()` | BankCsvFormatHint Layer B guard | `InvalidArgumentException` on CSV+no-hint | ✓ WIRED | Lines 81–82 |
| `BackfillMigration` | `BackfillStartingBalanceFromStatementSummaries` | container resolution | ✓ WIRED | Pattern confirmed by Plan 05 |
| `ImportServiceProvider` | 4 detectors | `starting-balance.detector` tag | ✓ WIRED | 4 occurrences in provider |
| `ImportServiceProvider` | `DetectStartingBalancesQuery` | singleton binding | ✓ WIRED | Line 186 |
| `ImportServiceProvider` | `BuildConsolidatedPreviewQuery` | singleton binding | ✓ WIRED | Present in provider (confirmed by Plan 07 test passing) |
| `FirstImportStep.php` | `BuildConsolidatedPreviewQuery` | constructor DI + `build()` call | ✓ WIRED | 8 occurrences |
| `FirstImportStep.php` | `DetectStartingBalancesQuery` | constructor DI + `collect()` call | ✓ WIRED | present in class |
| `FirstImportStep::commit()` | `ConfirmImport` | `dispatchChain: false` in DB::transaction | ✓ WIRED | Lines 229+238 |
| `first-import-step.blade.php` | `consolidated-preview-section` | `<x-onboarding::consolidated-preview-section>` | ✓ WIRED | Line 82 |
| `first-import-step.blade.php` | `StartingBalanceCard` | `<livewire:onboarding.starting-balance-card>` | ✓ WIRED | Line 128 |
| `HeaderSniffer.php` | `UnsupportedPaypalCsvShapeException` | BRR check before language-profile check | ✓ WIRED | Lines 175–184 (comment confirms order) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `FirstImportStep.php` | `$preview` (ConsolidatedPreviewBatch) | `BuildConsolidatedPreviewQuery::build()` reading `import_runs` + `PreviewCache` | Yes — reads DB + cache | ✓ FLOWING |
| `FirstImportStep.php` | `$startingBalances` (list of candidates) | `DetectStartingBalancesQuery::collect()` reading `statement_summaries` | Yes — reads DB | ✓ FLOWING |
| `FirstImportStep.php` | `$balanceConfirmations` | `#[On('starting-balance.confirmed')]` listener from child cards | Real user input → aggregated | ✓ FLOWING |
| `StartingBalanceCard.php` | `$detectedMinor` / `$detectedDate` | Parent-injected props from `StartingBalanceCandidate` | Real DB-backed values | ✓ FLOWING |
| `ConnectBankStep.php` | `wizard_progress.data['bank_import_run_id']` | `RunsImports::runFromUpload()` return value | Real import pipeline result | ✓ FLOWING |
| `ConnectCardStep.php` | `wizard_progress.data['card_import_run_ids']` | Per-file `runFromUpload()` loop | Real import pipeline results | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| DB::transaction in FirstImportStep::commit() | `grep -c "->transaction("` | 2 matches | ✓ PASS |
| dispatchChain: false in commit loop | `grep -c "dispatchChain: false"` | 1 match | ✓ PASS |
| upload-wizard removed from first-import-step blade | `grep -c "upload-wizard"` | 0 | ✓ PASS |
| BRR fires before language check | Code read of HeaderSniffer lines 175–184 | Comment + throw order confirmed | ✓ PASS |
| All 9 must-have Plan tests green | `vendor/bin/pest` on all phase test files | 9 test suites / 60+ assertions all PASS | ✓ PASS |

### Probe Execution

Step 7c — No conventional probe scripts found. No probe declarations in any PLAN.md. SKIPPED (no probes).

### Requirements Coverage

All plans declared `requirements: []` — these are UAT close-out plans with no formal REQ-ID. No orphaned requirements in REQUIREMENTS.md were identified that reference Phase 16.1.1. Requirements coverage is N/A for this phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `Modules/Onboarding/Internal/Http/Livewire/Steps/FirstImportStep.php` | 29 | PHPDoc says "This step replaces the earlier upload-wizard wrapper" — history-in-comment violates `feedback_docs_describe_current_state` memory | ⚠ Warning | Low — PHPDoc only; no runtime impact; future reader confusion minor |

No TBD/FIXME/XXX markers found in any production source file modified by this phase.

### Human Verification Required

See frontmatter `human_verification` list. Five items require manual testing in the running .app:

**1. End-to-end wizard commit flow (G9 core-value path)**

**Test:** Sign up fresh, run wizard: WelcomeStep → ConnectBankStep (CAMT.053 file) → ConnectCardStep (2–3 ICS PDFs) → FirstImportStep (review consolidated preview + confirm starting balances) → click "Commit everything (N transactions) →"
**Expected:** `transactions` table populated; `accounts.starting_balance_*` written; wizard advances to DoneStep; no toast error
**Why human:** Three commit-path regression tests (CommitEverythingTest, CommitRollbackTest, StaleFilterTest) not written before harness kill. DB::transaction logic IS implemented but end-to-end coverage is automated-test gap only.

**2. ConnectEmailStep OAuth modal (G6)**

**Test:** Sign up, reach ConnectEmailStep, click "Connect Gmail" / "Connect Microsoft"
**Expected:** Modal opens from layout mount; not from inline step mount; both provider buttons work
**Why human:** Visual confirmation of modal origin in DOM requires browser DevTools inspection.

**3. ConnectBankStep format chips + D-18 copy (G3)**

**Test:** Navigate to ConnectBankStep, interact with 3 format chips, verify CSV gate, verify no "(ASN)" copy outside bank-list
**Expected:** Chips render; CSV gate works; D-18 copy constraint satisfied in rendered HTML
**Why human:** aria-disabled visual rendering and exact copy verification need visual inspection.

**4. ConnectCardStep multi-file drop (G8)**

**Test:** Drop 3 ICS PDFs on ConnectCardStep simultaneously
**Expected:** Per-file chips appear; all 3 files stashed; non-PDF rejected with inline error
**Why human:** Livewire multi-file drop behavior in the built .app bundle requires real interaction.

**5. /dev/artisan page rendering (G2)**

**Test:** Sign in as developer, navigate to /dev/artisan, verify non-blank page + "Run a command" button functional; check dark-mode DOM for no transparent backgrounds
**Expected:** Page renders non-blank; no inline-var background regression in dark mode
**Why human:** Layer B no-repro outcome; visual dark-mode regression check needed in bundled .app.

### Gaps Summary

No blocking code gaps were found. All required artifacts exist, are substantive, and are wired. All automated test suites pass. The phase goal is achieved in the codebase.

**Two items that are NOT blockers:**

1. **Three missing commit-path regression tests** (pre-acknowledged): `FirstImportStepCommitEverythingTest`, `FirstImportStepCommitRollbackTest`, `FirstImportStepStaleFilterTest` do not exist. The underlying `DB::transaction` + `dispatchChain: false` logic IS implemented and wired. Coverage gap only — recommend a small follow-up plan to add these 3 tests.

2. **ROADMAP tracking counter stale** (tracking artifact): `**Plans:** 6/10 plans executed` should read `10/10`. Plans 04, 07, 07b are marked `[ ]` in the checklist but all three have committed code, passing tests, and SUMMARY.md files. The D-22 substantive requirements (title, goal, 10-plan list) are all met. The counter is a documentation artifact that the orchestrator's `roadmap.annotate-dependencies` step normally updates.

**Status rationale:** `human_needed` because five items require in-app visual/interaction verification. All automated checks pass.

---

## Manual UAT Checklist (Post-Build Verification)

After `php artisan native:build` and opening the .app:

**Setup Wizard Flow:**

- [ ] Sign up as a fresh user (no prior data)
- [ ] WelcomeStep renders with brand SVG in top-left sidebar and "beatrax" wordmark
- [ ] ConnectBankStep: three format chips (CAMT.053 / MT940 / CSV) visible above drop zone
- [ ] ConnectBankStep: selecting CSV disables drop zone with amber gating message; picking ASN or ING activates it
- [ ] ConnectBankStep: drop a CAMT.053 file — preview appears, step advances; `wizard_progress.data['bank_import_run_id']` set (check via `php artisan tinker`)
- [ ] ConnectCardStep: drop 2 ICS PDFs — per-file chips appear; step advances; `wizard_progress.data['card_import_run_ids']` is an array of 2 IDs
- [ ] ConnectEmailStep: "Connect Gmail" opens OAuthClientWizardModal (verify in DevTools: `<body>` contains the modal, NOT the `<connect-email-step>` subtree)
- [ ] ConnectEmailStep: "Skip" advances wizard without OAuth
- [ ] FirstImportStep: consolidated preview shows "FROM YOUR BANK STATEMENT" section + "FROM YOUR ICS CARD STATEMENTS" section with sample rows
- [ ] FirstImportStep: starting-balance cards appear for each detected account with detected value pre-filled
- [ ] FirstImportStep: clicking "Edit" on a starting-balance card reveals inline number + date inputs; entering a future date shows amber warning; clicking "Save" dispatches confirmed event
- [ ] FirstImportStep: "Commit everything (N transactions) →" button is present and shows correct deduplicated count
- [ ] After clicking commit: `transactions` table has rows; `accounts.starting_balance_minor` and `starting_balance_date` are set; wizard advances to DoneStep
- [ ] DoneStep renders; dashboard shows transactions and balance

**Regression Checks:**

- [ ] `/dev/artisan` renders non-blank for developer user (not white page)
- [ ] Reconnect on /inboxes page opens the OAuth modal (global mount covers this path too)
- [ ] Dropping a PayPal BRR CSV shows a clear "Activity Download shape required" error, not a confusing "language profile not supported" error
- [ ] ICS PDF preview on ConnectCardStep produces no ERROR-status rows for a valid monthly statement

---

_Verified: 2026-05-26_
_Verifier: Claude (gsd-verifier)_
