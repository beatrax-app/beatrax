---
phase: 16.1.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.js
  - Modules/Core/Resources/views/livewire/app-sidebar.blade.php
  - resources/css/app.css
  - Modules/Core/tests/Feature/BrandSvgRenderTest.php
autonomous: true
requirements: []
closes: [G5]
decisions: [D-19]
tags: [phase-16.1.1, brand, vite, layout]
must_haves:
  truths:
    - "After signup + login, the brand SVG renders in the authenticated sidebar's top-left .side-brand slot in place of the text 'b' glyph."
    - "The SVG asset is published to public/build/ via Vite (cache-busted hash); the blade resolves the URL via Vite::asset(...) so dev + bundle builds both work."
  artifacts:
    - path: "vite.config.js"
      provides: "Vite input array includes resources/brand/logo.svg"
      contains: "resources/brand/logo.svg"
    - path: "Modules/Core/Resources/views/livewire/app-sidebar.blade.php"
      provides: ".side-brand row renders <img> with Vite::asset(...)"
      contains: "Vite::asset('resources/brand/logo.svg')"
    - path: "resources/css/app.css"
      provides: ".side-brand .logo-svg + .drop-zone[aria-disabled] rules appended"
      contains: ".side-brand .logo-svg"
    - path: "Modules/Core/tests/Feature/BrandSvgRenderTest.php"
      provides: "HTTP assertion against authenticated layout proves <img> rendered"
      contains: "logo.svg"
  key_links:
    - from: "Modules/Core/Resources/views/livewire/app-sidebar.blade.php"
      to: "resources/brand/logo.svg"
      via: "Vite::asset() helper resolves manifest entry"
      pattern: "Vite::asset\\(['\"]resources/brand/logo\\.svg['\"]\\)"
    - from: "vite.config.js"
      to: "public/build/manifest.json"
      via: "npm run build emits hashed copy of logo.svg + manifest entry"
      pattern: "resources/brand/logo\\.svg"
---

<objective>
Render the existing committed brand SVG (`resources/brand/logo.svg`) in the authenticated app shell's top-left `.side-brand .logo` slot, replacing the placeholder text `b` glyph. Also pre-stage the small CSS rules that the wider Phase 16.1.1 surfaces will need so they ship in one CSS commit rather than spread across the rewrite plans.

Purpose: Close UAT finding G5 (brand SVG missing from app chrome) per locked decision D-19. The asset is already committed for Phase 19 REL-04; this plan wires it up.

Output: Three small modifications (vite.config.js input, app-sidebar blade swap, app.css rule appends) plus one Pest feature test that proves the rendered HTML carries the Vite-hashed SVG URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-CONTEXT.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-RESEARCH.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-UI-SPEC.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The Vite asset helper signature used here is standard Laravel; the blade slot uses the existing .side-brand layout. -->

Existing .side-brand markup at Modules/Core/Resources/views/livewire/app-sidebar.blade.php lines 19-24:
```blade
<div class="side-brand">
    <span class="logo" aria-hidden="true">b</span>
    <span>beatrax</span>
    <span class="version-chip">v2.0.0-dev</span>
</div>
```

Existing vite.config.js (the entire file is short):
```js
import { defineConfig } from 'vite';
import laravel from 'laravel-vite-plugin';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
    plugins: [
        laravel({
            input: ['resources/css/app.css', 'resources/js/app.js'],
            refresh: true,
        }),
        tailwindcss(),
    ],
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish logo.svg via Vite + swap the .side-brand glyph</name>
  <files>vite.config.js, Modules/Core/Resources/views/livewire/app-sidebar.blade.php</files>
  <read_first>
    - vite.config.js (the entire file; small)
    - Modules/Core/Resources/views/livewire/app-sidebar.blade.php (the entire blade; brand row at lines 19-24)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-RESEARCH.md Hook 10 (asset publishing — Option A is the chosen path)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md Pattern 15 (brand-logo SVG swap)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-UI-SPEC.md lines 47, 444 (locked sizing + slot)
  </read_first>
  <behavior>
    - vite.config.js `input` array now contains exactly three entries in this order: `'resources/css/app.css'`, `'resources/js/app.js'`, `'resources/brand/logo.svg'`. No other vite.config.js changes.
    - `Modules/Core/Resources/views/livewire/app-sidebar.blade.php`: the line `<span class="logo" aria-hidden="true">b</span>` is replaced by `<img src="{{ Vite::asset('resources/brand/logo.svg') }}" alt="beatrax" width="24" height="24" class="logo logo-svg" />`. The neighboring `<span>beatrax</span>` and `<span class="version-chip">v2.0.0-dev</span>` are unchanged. The `.side-brand` container's `class` / `aria-*` attributes are unchanged.
    - After `npm run build`, `public/build/manifest.json` contains a key `resources/brand/logo.svg` mapping to a hashed file path like `build/assets/logo-<hash>.svg`.
    - Rendering the authenticated layout (any auth-gated route) returns HTML whose `.side-brand` div contains exactly one `<img>` whose `src` attribute begins with `/build/assets/logo-` and ends with `.svg`, and whose `alt` is `beatrax`, `width` and `height` are both `24`, and `class` contains both `logo` and `logo-svg`.
  </behavior>
  <action>
    Add the SVG to Vite's input array per RESEARCH.md Hook 10 Option A and swap the .side-brand glyph per PATTERNS.md Pattern 15. Concretely:
    1. In `vite.config.js`, extend the `input` array on the `laravel({...})` plugin call so it reads `['resources/css/app.css', 'resources/js/app.js', 'resources/brand/logo.svg']`. Preserve the surrounding `refresh: true` flag and the `tailwindcss()` plugin entry.
    2. In `Modules/Core/Resources/views/livewire/app-sidebar.blade.php`, locate the `.side-brand` row (currently at lines 19-24 per PATTERNS.md). Replace the single `<span class="logo" aria-hidden="true">b</span>` line with the `<img>` tag specified in `<behavior>` above. Do not touch the wordmark span or the version chip span.
    3. Do not introduce any GSD / phase / UAT / G-id references in PHPDocs, comments, or blade comments (per memory `feedback_codebase_gsd_agnostic`). The change is self-explanatory and needs no new comment.
    4. Run `npm run build` once locally to populate the manifest so the Pest feature test in the next task can resolve the Vite-hashed URL during testing.
  </action>
  <verify>
    <automated>vendor/bin/pint --test Modules/Core/Resources/views/livewire/app-sidebar.blade.php vite.config.js; npm run build; grep -q 'resources/brand/logo.svg' public/build/manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resources/brand/logo.svg" vite.config.js` returns `1`.
    - `grep -c "Vite::asset('resources/brand/logo.svg')" Modules/Core/Resources/views/livewire/app-sidebar.blade.php` returns `1`.
    - `grep -c 'aria-hidden="true">b</span>' Modules/Core/Resources/views/livewire/app-sidebar.blade.php` returns `0` (the placeholder glyph is gone).
    - `grep -E 'logo-svg' Modules/Core/Resources/views/livewire/app-sidebar.blade.php` matches the new `<img>` tag.
    - After `npm run build`, the file `public/build/manifest.json` exists and contains the string `resources/brand/logo.svg`.
    - `vendor/bin/pint --test` returns 0.
  </acceptance_criteria>
  <done>vite.config.js inputs include the SVG; the sidebar brand row renders the SVG via `Vite::asset(...)`; Pint passes; the manifest carries the asset entry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append .logo-svg + .drop-zone[aria-disabled] CSS rules; add Pest feature test for the brand chrome</name>
  <files>resources/css/app.css, Modules/Core/tests/Feature/BrandSvgRenderTest.php</files>
  <read_first>
    - resources/css/app.css (only the end of the file — the existing `@theme` block and any prior `.side-brand` rules)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-UI-SPEC.md lines 549-565 (CSS to append — exact block)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md Pattern 15 (logo-svg CSS) and Pattern E (Pest feature test against a Livewire component)
    - Modules/Onboarding/tests/Feature/EmailStepEmbedsOAuthModalTest.php (Pest feature test shape with Livewire::test + assertSeeHtml — closest analog)
    - Modules/Core/tests/TestCase.php (if it exists — bootstrapping; otherwise Pest.php in the same tests dir provides the base)
  </read_first>
  <behavior>
    - `resources/css/app.css` gains the two new rule blocks from UI-SPEC lines 549-565 (the `.side-brand .logo-svg` block and the `.drop-zone[aria-disabled="true"]` + `.drop-zone[aria-disabled="true"] .drop-zone-glyph` blocks). The `@theme` block and prior rules are unchanged. The rules are appended at the end of the file, after any existing class rules.
    - `Modules/Core/tests/Feature/BrandSvgRenderTest.php` exists, is tagged with `#[Group('phase-16.1.1')]` (or equivalent Pest annotation pattern used elsewhere in the project), and contains one test that:
      1. Creates a user via `User::query()->create(...)` mirroring `EmailStepEmbedsOAuthModalTest.php` setup.
      2. Calls `$this->actingAs($user)` to authenticate.
      3. Renders the app-sidebar Livewire component via `Livewire::test(\Modules\Core\Internal\Http\Livewire\AppSidebar::class)` (or whatever the namespaced Livewire class is — discover via grep before writing).
      4. Asserts the rendered HTML contains a `<img>` tag whose `src` matches the regex `/build/assets/logo-[A-Za-z0-9_-]+\.svg`, whose `alt` is `beatrax`, and whose `class` contains `logo-svg`.
      5. Asserts the rendered HTML does NOT contain the previous `<span class="logo" aria-hidden="true">b</span>` text.
  </behavior>
  <action>
    Append the two CSS blocks from UI-SPEC lines 549-565 verbatim to the end of `resources/css/app.css`. Then create the feature test under `Modules/Core/tests/Feature/BrandSvgRenderTest.php` following PATTERNS.md Pattern E. Concretely:
    1. Append to `resources/css/app.css`:
       - The `.side-brand .logo-svg { ... }` block (24×24, var(--radius-md), display: block).
       - The `.drop-zone[aria-disabled="true"] { cursor: not-allowed; opacity: 0.55; }` block.
       - The `.drop-zone[aria-disabled="true"] .drop-zone-glyph { filter: grayscale(1); }` block.
       Do not modify the `@theme` declarations or any existing rule. No new tokens; both blocks resolve color / size / radius only from existing `--radius-md` and constants.
    2. Discover the FQN of the app-sidebar Livewire component: run `grep -rn "class AppSidebar" Modules/Core --include="*.php"` and use the resulting class. The blade view's `@livewire('core.app-sidebar')` invocation maps to that class.
    3. Create `Modules/Core/tests/Feature/BrandSvgRenderTest.php` as a Pest file (functional `it('...')` style), copying the bootstrapping pattern from `Modules/Onboarding/tests/Feature/EmailStepEmbedsOAuthModalTest.php`. Use `Livewire::test(<AppSidebarFqn>)->assertSeeHtml(...)` to assert the rendered img tag exists, and `->assertDontSeeHtml('aria-hidden="true">b</span>')` to assert the placeholder is gone.
    4. Tag the test for the phase wave selector — if the project's Pest convention uses `uses(Tests\TestCase::class)->group('phase-16.1.1');` or a PHP attribute, mirror whatever the most recent feature test in this codebase uses (look at `Modules/Onboarding/tests/Feature/FirstImportStepWidthTest.php` if present, else use the module's standard).
    5. Never reference GSD / phase / UAT labels inside the test code itself (no `// G5 - tests brand svg`); instead the test name should read like `it('renders the brand svg in the authenticated sidebar')`. The Pest group annotation is metadata, not code commentary.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Core/tests/Feature/BrandSvgRenderTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.side-brand \.logo-svg" resources/css/app.css` returns `1`.
    - `grep -c "drop-zone\[aria-disabled" resources/css/app.css` returns `2` (one for the base rule, one for `.drop-zone-glyph` filter).
    - `vendor/bin/pest Modules/Core/tests/Feature/BrandSvgRenderTest.php` exits 0.
    - The test file contains an `assertSeeHtml` (or `assertSee`) assertion that matches the rendered `<img src=".../logo-...svg" alt="beatrax"` pattern.
    - The test file contains exactly one `it(...)` block (single focused assertion).
    - The test file does not contain any of the strings: `G5`, `UAT`, `Phase 16.1.1`, `.planning`, `D-19`.
  </acceptance_criteria>
  <done>CSS rules appended; Pest feature test exists, named non-GSD-specifically, and exits 0; running `vendor/bin/pest Modules/Core/tests/Feature/BrandSvgRenderTest.php` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server (auth-gated) | The brand SVG is requested by an authenticated user via the Vite-hashed asset URL. The asset is a static SVG with no executable surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.1-01 | Tampering | resources/brand/logo.svg served via Vite hashed URL | accept | The SVG is committed to source and read-only at build time. Vite's content-hash naming defeats opportunistic CDN-style cache poisoning. SVG content is rendered via `<img>` (not inlined as `<svg>` content), so any embedded `<script>` inside the SVG is sandboxed by the browser's image-rendering rules. |
| T-16.1.1-02 | Information disclosure | Vite manifest disclosure | accept | `public/build/manifest.json` is publicly readable by design; it carries asset URLs only, no secrets. No new disclosure introduced by this plan. |

No HIGH-severity threats. Plan is cosmetic.
</threat_model>

<verification>
- `npm run build` populates `public/build/manifest.json` with a `resources/brand/logo.svg` key.
- `vendor/bin/pest Modules/Core/tests/Feature/BrandSvgRenderTest.php` returns 0.
- `vendor/bin/pint --test` returns 0 across all modified files.
- `vendor/bin/phpstan analyse --memory-limit=2G Modules/Core/tests/Feature/BrandSvgRenderTest.php Modules/Core/Resources/views/livewire/app-sidebar.blade.php` returns 0 (Larastan L10 strict; blade files via the project's blade-extension if registered).
- Manual sanity (one-time): boot the app, sign in, observe a ~24px tall brand SVG in the top-left of the sidebar, with the wordmark "beatrax" and version chip to its right.
</verification>

<success_criteria>
- Authenticated app sidebar renders the brand SVG in the top-left `.side-brand .logo` slot.
- No occurrence of the placeholder text `b` glyph remains in `app-sidebar.blade.php`.
- `vite.config.js` declares `resources/brand/logo.svg` as an input; `npm run build` emits the hashed copy and the manifest entry.
- Pest feature test `BrandSvgRenderTest` is green.
- Pint + Larastan L10 strict pass for every file in `files_modified`.
</success_criteria>

<output>
Create `.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-01-SUMMARY.md` when done.
</output>
