---
phase: 16.1.1
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements: []
closes: []
decisions: [D-22]
tags: [phase-16.1.1, roadmap, docs]
must_haves:
  truths:
    - "ROADMAP.md Phase 16.1.1 entry has a title that reflects the full close-out cluster (all nine G-ids: starting balance + upload→persist + multi-PDF + bank-format copy + OAuth modal mount-once + ICS PDF preview + Dev Mode artisan popover + brand logo)."
    - "ROADMAP.md Phase 16.1.1 Goal text is replaced with the cleaned-up domain statement (no `[Urgent work - to be planned]` placeholder)."
    - "ROADMAP.md Phase 16.1.1 Requirements row reflects the G-id close-out posture (no REQ-IDs; this is a UAT close-out)."
    - "ROADMAP.md Phase 16.1.1 plan list enumerates ALL TEN plans (16.1.1-01, 02, 03, 04, 05, 05b, 06, 07, 07b, 08) with brief objectives — reflecting the checker-driven 05+05b and 07+07b splits."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 16.1.1 title + goal + plans list updated per D-22"
      contains: "Phase 16.1.1"
  key_links:
    - from: ".planning/ROADMAP.md (Phase 16.1.1 entry)"
      to: ".planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/"
      via: "Phase slug + plan filenames match the directory"
      pattern: "16\\.1\\.1"
---

<objective>
Update the `.planning/ROADMAP.md` Phase 16.1.1 entry per locked decision D-22. The current entry's title lists only 5 of the 9 close-out findings and the Goal reads `[Urgent work - to be planned]` — both stale. This plan replaces both with the cleaned-up domain statement from CONTEXT.md `<domain>` and lists the TEN plans this phase ships (eight original plans + 05b + 07b created by the planner's checker-driven scope_sanity split).

Purpose: Honor D-22 (the ROADMAP.md update is a locked decision). Single-file docs change.

Output: Two edits to `.planning/ROADMAP.md` — replace the Phase 16.1.1 title, replace the Goal text, and update the Plans list to include all ten plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-CONTEXT.md

<interfaces>
<!-- Current Phase 16.1.1 entry in .planning/ROADMAP.md (lines 257-265 from the loaded file) — replacing this block: -->

```markdown
### Phase 16.1.1: Starting balance setup + bank-format-driven wizard copy + ICS PDF preview fix + Dev Mode artisan popover fix + app-chrome brand logo (UAT close-out from phase 16.1) (INSERTED)

**Goal:** [Urgent work - to be planned]
**Requirements**: TBD
**Depends on:** Phase 16.1
**Plans:** 0 plans

Plans:
- [ ] TBD (run /gsd-plan-phase 16.1.1 to break down)
```

<!-- Replacement block — derived from CONTEXT.md <domain>, D-22 suggestion, plus the planner's 05/07 split: -->

```markdown
### Phase 16.1.1: First-run wizard UAT close-out — starting balance + upload→persist + multi-PDF + bank-format copy + OAuth modal mount-once + ICS PDF preview + Dev Mode artisan popover + brand logo (UAT close-out from Phase 16.1) (INSERTED)

**Goal:** Make the first-run setup wizard actually work end-to-end on a fresh install so a new user can sign up → run the wizard → see their real transactions AND their real starting balance on the dashboard — not just transaction deltas, not "we did nothing" silence, not "upload twice", not "ASN-only" copy. All nine UAT findings (G1–G9) close in this phase.
**Slug:** `16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pdf-preview-fix-dev-mode-artisan-popover-fix-app-chrome-brand-logo`
**Depends on:** Phase 16.1
**Plans:** 10 plans

Plans:
- [ ] 16.1.1-01-PLAN.md — Brand logo + supporting CSS (G5)
- [ ] 16.1.1-02-PLAN.md — OAuth modal global mount in both authenticated layouts (G6)
- [ ] 16.1.1-03-PLAN.md — Dev Mode artisan popover reproduce-and-fix (G2)
- [ ] 16.1.1-04-PLAN.md — ICS PDF preview reproduce-and-fix (G4)
- [ ] 16.1.1-05-PLAN.md — Starting-balance schema + backfill + Account fillable (G1 schema half)
- [ ] 16.1.1-05b-PLAN.md — Tagged starting-balance detectors + aggregator query with D-04 conflict resolution (G1 detection half)
- [ ] 16.1.1-06-PLAN.md — Format-first wizard copy + multi-file ICS upload + ImportRun stash plumbing (G3 + G8)
- [ ] 16.1.1-07-PLAN.md — Consolidated preview query + DTOs + ConfirmImport.dispatchChain flag (G7 query/contract surface)
- [ ] 16.1.1-07b-PLAN.md — StartingBalanceCard + consolidated-preview-section + FirstImportStep rewrite + commit-everything (G1 + G7 + G9 UI/commit surface)
- [ ] 16.1.1-08-PLAN.md — ROADMAP.md title + goal update (D-22)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the Phase 16.1.1 ROADMAP.md entry with the close-out title + cleaned-up goal + ten-plan list</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 255-266 — the current Phase 16.1.1 block; verify exact line numbers haven't drifted)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-CONTEXT.md `<domain>` (lines 6-89 — the cleaned-up goal text)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-CONTEXT.md D-22 (lines 305-313 — the suggested title)
  </read_first>
  <behavior>
    - The Phase 16.1.1 block in `.planning/ROADMAP.md` is replaced verbatim with the markdown block from `<interfaces>` above. The replacement preserves the heading depth (`### Phase 16.1.1: ...`), maintains the project's existing entry shape (Goal / Slug / Depends on / Plans / Plans-list), and inserts the ten plans in dependency order:
      - Wave 1: 01, 02, 03, 05, 06, 08
      - Wave 2: 04, 05b
      - Wave 3: 07
      - Wave 4: 07b
    - Surrounding ROADMAP.md content (Phase 16.1 entry above, Phase 17 entry below, milestone tables, Progress table) is unchanged.
    - The progress table at the bottom of ROADMAP.md (around line 348-362) does NOT change — Phase 16.1.1 isn't on it yet because it hasn't completed any plans; the plan-phase orchestrator's roadmap.annotate-dependencies step will add it as plans complete.
  </behavior>
  <action>
    Do one surgical edit to `.planning/ROADMAP.md`. Concretely:
    1. Read the current Phase 16.1.1 block. Confirm the start anchor is `### Phase 16.1.1: ...` and the end anchor is the empty line above `### Phase 17: ...`.
    2. Replace that block with the new block from the `<interfaces>` section above. Preserve the trailing blank line so Phase 17's heading starts on a fresh line.
    3. Do not touch the Phase 16.1 entry (above), the Phase 17 entry (below), the Progress table (further down), or any other content.
    4. The replacement adds the `**Slug:**` line to match the project's existing entry style (compare with Phase 16's entry which lists `**Slug:** 16-developer-mode-ui`).
    5. Verify the ten plan filenames match the actual files created by Plans 01-08 + 05b + 07b (they should be `16.1.1-01-PLAN.md` through `16.1.1-08-PLAN.md` plus `16.1.1-05b-PLAN.md` and `16.1.1-07b-PLAN.md`).
    6. No code comments needed — the ROADMAP.md is the documentation surface.
  </action>
  <verify>
    <automated>grep -c 'Phase 16\.1\.1: First-run wizard UAT close-out' .planning/ROADMAP.md; grep -c 'Urgent work - to be planned' .planning/ROADMAP.md; grep -c '16\.1\.1-0[1-8]-PLAN\.md\|16\.1\.1-05b-PLAN\.md\|16\.1\.1-07b-PLAN\.md' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 16\\.1\\.1: First-run wizard UAT close-out" .planning/ROADMAP.md` returns `1`.
    - `grep -c "Urgent work - to be planned" .planning/ROADMAP.md` returns `0` (the placeholder is gone).
    - `grep -cE '16\\.1\\.1-(0[1-8]|05b|07b)-PLAN\\.md' .planning/ROADMAP.md` returns `10` (all ten plan filenames listed).
    - `grep -c '\*\*Plans:\*\* 10 plans' .planning/ROADMAP.md` returns `1` (the count matches).
    - The Phase 16.1 entry above (its title, Goal, Slug, Requirements, Plans) is unchanged — `git diff .planning/ROADMAP.md` shows no edits outside the Phase 16.1.1 block.
    - The Phase 17 entry below is unchanged.
    - The Progress table at the bottom is unchanged.
  </acceptance_criteria>
  <done>ROADMAP.md Phase 16.1.1 block reflects the full close-out title + cleaned-up goal + ten-plan list; no other content drifted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a — docs-only change | No runtime surface; ROADMAP.md is a planning artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.1-27 | n/a | n/a | accept | Docs-only edit; no security-relevant surface. The codebase-agnostic-from-GSD memory does NOT apply to `.planning/` files (those are GSD docs by definition). |

No HIGH-severity threats. Documentation update.
</threat_model>

<verification>
- The three grep assertions in the `<automated>` block pass.
- A `git diff .planning/ROADMAP.md` shows the diff is contained to the Phase 16.1.1 block.
- No other Pest / Pint / Larastan runs needed; this plan touches no PHP / Blade / TypeScript / CSS.
</verification>

<success_criteria>
- ROADMAP.md Phase 16.1.1 entry has the close-out title, the cleaned-up goal, and the ten-plan list.
- Surrounding content (Phase 16.1, Phase 17, Progress table) is unchanged.
- The plan list filenames match the actual `.planning/phases/16.1.1-.../*.md` files created by Plans 01-08 + 05b + 07b.
</success_criteria>

<output>
Create `.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-08-SUMMARY.md` when done.
</output>
