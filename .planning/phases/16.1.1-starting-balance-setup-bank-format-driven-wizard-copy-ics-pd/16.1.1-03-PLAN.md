---
phase: 16.1.1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/DevMode/Resources/views/livewire/artisan-runner-page.blade.php
  - Modules/DevMode/Internal/Http/Livewire/ArtisanRunnerPage.php
  - Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php
autonomous: false
requirements: []
closes: [G2]
decisions: [D-20]
tags: [phase-16.1.1, devmode, artisan, livewire, reproduce-first]
must_haves:
  truths:
    - "Navigating to /dev/artisan in the built .app does NOT render a white page; the SAFE-tier artisan-runner surface mounts and renders its content."
    - "If the 'Run a command' button currently opens the ⌘K palette (existing behavior post-batch-8), that behavior remains. If the dormant <flux:modal name='run-command'> is the surface the user calls 'popover', it gets a caller AND its background is fixed to explicit Tailwind utilities (no `style='background: var(...)'` indirection that broke batch-8)."
    - "A Pest feature test asserts /dev/artisan renders HTTP 200 with the page's H1 (or equivalent identifier) and that no inline `style=\"background: var(...)` appears on dark-mode-relevant modal surfaces within the page."
  artifacts:
    - path: "Modules/DevMode/Resources/views/livewire/artisan-runner-page.blade.php"
      provides: "Fix layer A and/or B per reproduction (caller for the dormant modal AND/OR replace `style=\"background: var(...)\"` with explicit Tailwind dark-variant classes)"
    - path: "Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php"
      provides: "Pest feature test for /dev/artisan rendering correctness + no inline-var background regression"
      contains: "/dev/artisan"
  key_links:
    - from: "Modules/DevMode/Routes/web.php"
      to: "Modules/DevMode/Internal/Http/Livewire/ArtisanRunnerPage.php"
      via: "Route::get('/artisan', ArtisanRunnerPage::class)->name('dev.artisan');"
      pattern: "ArtisanRunnerPage"
---

<objective>
Diagnose and fix the "white page" the user reports at `/dev/artisan` in the freshly-built `.app` bundle. RESEARCH.md Hook 7 narrows the failure layers to three candidates:
- **Layer A:** The dormant `<flux:modal name="run-command">` at `artisan-runner-page.blade.php` line 78 has no caller dispatching `flux-modal-show name="run-command"`, so it never opens and may render a blank shell.
- **Layer B:** Same class of bug batch-8 fixed for the palette modal (inline `style="background: var(--color-bg, #fff); ..."` not resolving in the bundled NativePHP runtime). Re-apply the same fix shape: replace inline `style="background: var(...)"` with explicit Tailwind utilities `bg-white dark:bg-[#0b1220]`.
- **Layer C:** The UAT description ("form with arg inputs + Run button + live stdout/stderr stream") might describe a missing feature, not a bug — the planner explicitly asks the user mid-task whether to scope a feature add (out of scope here) or just fix the white-page regression (in scope).

Purpose: Close UAT finding G2 per locked decision D-20. The plan is reproduce-first — Task 1 captures the actual reproduction in the built bundle; Task 2 pins the fix layer; Task 3 writes the regression test.

Output: A reproduction note (informal — captured as a comment in the SUMMARY.md, NOT in source code per `feedback_codebase_gsd_agnostic`), the surgical fix at the identified layer (A and/or B), and a Pest feature test that asserts /dev/artisan renders non-blank HTML and contains no inline-var background regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-CONTEXT.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-RESEARCH.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md
@.planning/debug/16-uat-batch-8-bugs.md
@CLAUDE.md

<interfaces>
<!-- The dormant Flux modal and existing "Run a command" button per PATTERNS.md Pattern 17 + RESEARCH.md Hook 7 -->

Modules/DevMode/Resources/views/livewire/artisan-runner-page.blade.php — relevant sections:
- Lines 6-16: `<flux:button>` "Run a command" with `x-on:click="$dispatch('palette:open')"` → opens the ⌘K palette (already fixed in batch-8 commit 189d901).
- Lines 78-105: `<flux:modal name="run-command" :dismissible="true">` rendering the SAFE-tier command list. No caller currently dispatches `modal-show` against this modal.

Modules/DevMode/Routes/web.php (the route binding for /dev/artisan):
`Route::get('/artisan', ArtisanRunnerPage::class)->name('dev.artisan');`

Batch-8 fix precedent (commit 189d901) — the fix shape to mirror if Layer B applies:
- Replace `style="background: var(--color-bg, #fff); ..."` with `class="bg-white dark:bg-[#0b1220] ..."` on any element inside a Flux modal body.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Reproduce /dev/artisan in the built .app bundle and capture the failure layer</name>
  <what-built>
    Nothing built yet — this is a checkpoint requiring a human reproduction step before any fix is written. The fix layer (A, B, or A+B) is pinned by what the user observes.
  </what-built>
  <how-to-verify>
    1. Build the Mac amd64 bundle: `php artisan native:build` (if a fresh build is needed; otherwise re-open the existing `dist/diederik.app`).
    2. Open `dist/diederik.app` (or wherever `native:build` emitted it; see PROJECT.md / `make build` if there's a convenience target).
    3. Sign up as a fresh user (the first user becomes the developer per Phase 16 DEVUI-01).
    4. Sign in.
    5. Navigate to `/dev/artisan` via the URL bar (or via the sidebar's Dev block → Artisan link).
    6. Capture the following observations into the reproduction note:
       (a) Does the page render at all? (Is the H1 / page chrome visible, or is the body literally blank/white?)
       (b) Click the "Run a command" button at the top of the page.
           - If it opens the ⌘K palette → the user's "white page" complaint may have been about the dormant `<flux:modal name="run-command">` rather than the page itself. Continue to (c).
           - If it does NOT open the palette and the page goes white → Layer C: command-palette regression (re-investigate batch-8 commit `189d901`'s scope).
       (c) Open the macOS dev tools (Cmd+Opt+I in the .app) → Console tab. Look for: (i) any JS errors; (ii) the rendered DOM near `<flux:modal name="run-command">` — is its inner div present? does any element have `style="background: var(--color-bg, #fff)..."` that resolves to transparent?
       (d) Tail `~/Library/Application Support/beatrax/storage/logs/laravel.log` while clicking around; capture any new ERROR or WARNING lines.
    7. Record the captured DOM + log output + console output into the SUMMARY.md as a reproduction note. Categorise the failure into one of:
       - **Layer A (no caller):** The Flux modal exists in DOM but is never shown — fix by adding a caller.
       - **Layer B (style indirection):** Inline `style="background: var(...)"` produces transparent background in the bundled runtime — fix by replacing with explicit Tailwind utilities.
       - **Layer A + B together:** The dormant modal needs a caller AND has the inline-var bug.
       - **Layer C (palette regression):** The ⌘K palette is broken in the latest bundle — STOP here and re-investigate batch-8 commit `189d901`, OR escalate to a separate plan because the scope diverges.
       - **No reproduction:** The white page is fixed by a prior commit; capture which one (likely `189d901` or `3fbf947`). In that case Task 2 only adds the regression test.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - `layer-a` (need caller; will fix by adding a wire-click or x-on-click that dispatches `flux-modal-show name="run-command"` somewhere on the page)
    - `layer-b` (need style→class fix; will replace inline `style="background: var(...)"` per batch-8 precedent)
    - `layer-a+b` (both)
    - `layer-c` (palette regression; STOP — opens a different plan)
    - `no-repro` (page already works; will add regression test only)
    - or describe what you saw in your own words and let Claude classify
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the surgical fix per the pinned failure layer (A, B, or A+B)</name>
  <files>Modules/DevMode/Resources/views/livewire/artisan-runner-page.blade.php, Modules/DevMode/Internal/Http/Livewire/ArtisanRunnerPage.php</files>
  <read_first>
    - Modules/DevMode/Resources/views/livewire/artisan-runner-page.blade.php (FULL file, especially lines 6-16 and 78-105 per PATTERNS.md Pattern 17)
    - Modules/DevMode/Internal/Http/Livewire/ArtisanRunnerPage.php (full file; small)
    - Modules/DevMode/Routes/web.php (verify the `/dev/artisan` route binding)
    - .planning/debug/16-uat-batch-8-bugs.md (the batch-8 fix precedent — read entirely; commit `189d901` is the analog)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-RESEARCH.md Hook 7 (the three layer candidates)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md Pattern 17 + "Anti-Patterns to Avoid" (Anti-Pattern 1 — inline-var background)
  </read_first>
  <behavior>
    Branch on the resume-signal from Task 1:
    - **If `layer-a` or `layer-a+b`:** The page gains a working caller for the dormant `<flux:modal name="run-command">`. Concrete shape: the existing "Run a command" button currently dispatches `palette:open` — if the user wants both surfaces, add a SECOND button "Run a SAFE command directly" that dispatches the Flux modal via `wire:click` calling a new `openRunCommandModal()` method on `ArtisanRunnerPage.php` which uses Flux's modal-show convention (`$this->modal('run-command')->show()` or the documented Flux API; verify against Flux docs / existing Flux modal callers in the project via `grep -rn "->modal(" Modules`).
    - **If `layer-b` or `layer-a+b`:** Every `style="background: var(--color-bg, ...)` and similar `style="background: var(...)"` indirection inside `artisan-runner-page.blade.php` is replaced with explicit Tailwind utilities (`class="bg-white dark:bg-[#0b1220]"` for the modal panel, or whatever matches the surrounding chrome). Mirror the batch-8 fix shape from commit `189d901`.
    - **If `no-repro`:** No source changes; proceed to Task 3 for regression test only.
    - **If `layer-c`:** STOP this plan. The fix scope diverged. Recommend the user run `/gsd-debug 16-uat-palette-regression` as a separate session.
    - In every case: do NOT add comments referring to G2 / UAT / .planning / D-20 — per memory `feedback_codebase_gsd_agnostic`. If you add a comment to explain the dispatcher pattern, keep it plain English ("Opens the SAFE-tier artisan-runner modal from the page body").
  </behavior>
  <action>
    Per the resume-signal classification from Task 1, apply ONLY the matching change. Concretely:
    1. **layer-a:** In `Modules/DevMode/Internal/Http/Livewire/ArtisanRunnerPage.php`, add a public method `openRunCommandModal(): void` whose body either calls Flux's modal-show convention (whatever the project's Flux integration uses — grep the codebase for existing Flux modal openers via `grep -rn "modal-show\|->modal(" Modules/DevMode Modules/Categorization Modules/Community`) or dispatches a Livewire event the Flux modal listens to. In the blade, add `wire:click="openRunCommandModal"` to a button (or — if the user said "the Run a command button should open this modal, not the palette" — change the existing button's handler from `x-on:click="$dispatch('palette:open')"` to `wire:click="openRunCommandModal"`). Discover the actual Flux modal-show pattern in use before writing.
    2. **layer-b:** Open `artisan-runner-page.blade.php`; find every inline `style="background: var(...)"` attribute on elements inside `<flux:modal name="run-command">` (lines 78-105) and on any other dark-mode-relevant chrome on the page (page background, button backgrounds inside the modal). Replace each with the corresponding Tailwind utility pair: `bg-white dark:bg-[#0b1220]` for page/modal background; `bg-slate-50 dark:bg-slate-800` for subtle chrome; `bg-slate-100 dark:bg-slate-700` for hover surfaces. Cross-reference batch-8 commit `189d901` (`.planning/debug/16-uat-batch-8-bugs.md` documents the exact replacement set).
    3. **no-repro:** Skip. Proceed to Task 3.
    4. **layer-a+b:** Apply both #1 and #2.
    5. **layer-c:** Output `## PLAN SCOPE DIVERGED — escalating to /gsd-debug 16-uat-palette-regression` as the SUMMARY.md note and stop without source changes.
    6. Run `vendor/bin/pint --test` and `vendor/bin/phpstan analyse Modules/DevMode` after the edit; address any new violation.
    7. No GSD / phase comments anywhere in the modified source.
  </action>
  <verify>
    <automated>vendor/bin/pint --test Modules/DevMode; vendor/bin/phpstan analyse --memory-limit=2G Modules/DevMode</automated>
  </verify>
  <acceptance_criteria>
    - For **layer-b** / **layer-a+b**: `grep -c 'style="background: var(' Modules/DevMode/Resources/views/livewire/artisan-runner-page.blade.php` returns `0` (no inline-var background regression). The same grep across all of `Modules/DevMode/Resources/views/` returns `0` (full hygiene sweep).
    - For **layer-a** / **layer-a+b**: `grep -c "openRunCommandModal\|run-command.*show\|wire:click=.*run-command" Modules/DevMode/Internal/Http/Livewire/ArtisanRunnerPage.php Modules/DevMode/Resources/views/livewire/artisan-runner-page.blade.php` returns a value ≥ 2 (method definition + caller).
    - `vendor/bin/pint --test Modules/DevMode` returns 0.
    - `vendor/bin/phpstan analyse --memory-limit=2G Modules/DevMode` returns 0 at Larastan L10 strict.
    - No file in `Modules/DevMode/` contains the strings `G2`, `UAT`, `Phase 16.1.1`, `.planning`, `D-20` (grep returns 0).
  </acceptance_criteria>
  <done>The pinned failure layer is fixed; Pint + Larastan green; no GSD-leakage strings in source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pest feature test — /dev/artisan renders HTTP 200 with non-blank body and no inline-var background regression</name>
  <files>Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php</files>
  <read_first>
    - Modules/DevMode/tests/Pest.php and Modules/DevMode/tests/TestCase.php (Pest bootstrap for the module)
    - Modules/DevMode/Routes/web.php (verify route name + path)
    - Modules/DevMode/Internal/Http/Livewire/ArtisanRunnerPage.php (verify what's renderable — the page's H1 or recognizable string)
    - Modules/DevMode/tests/Feature/ (look for existing Pest tests against /dev/* routes — copy their bootstrapping)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md Pattern E
  </read_first>
  <behavior>
    - `Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php` exists, is grouped `phase-16.1.1`, and contains:
      1. `it('renders /dev/artisan with HTTP 200 and a non-blank body for a developer user')` — creates a developer user (sets `is_developer = true`), `actingAs` them, performs `$this->get('/dev/artisan')`, asserts status 200, asserts the response contains a recognizable on-page string (the page H1 text — discover during read-first).
      2. `it('does not regress to the inline-var background pattern on the artisan-runner page')` — calls `$this->get('/dev/artisan')`, asserts the response body does NOT contain the substring `style="background: var(`.
      3. `it('forbids /dev/artisan for a non-developer user')` — creates a non-developer user, gets /dev/artisan, asserts 404 (per Phase 16 DEVUI-01's EnsureDeveloperMode middleware that returns 404 not 403 for non-developers).
  </behavior>
  <action>
    Create the Pest feature test file using existing /dev/* tests as the bootstrap analog. Concretely:
    1. Discover the recognizable on-page string by reading the blade — the H1 is likely "Artisan" or "Run a command" or similar. Use a stable string that survives Task 2's edit.
    2. Discover how to create a developer user — search `grep -rn "is_developer\\s*=\\s*true" Modules/DevMode/tests Modules/Auth/tests` for the canonical fixture pattern.
    3. Write three `it(...)` blocks per `<behavior>`. Use Pest functional style.
    4. Tag with `->group('phase-16.1.1')`.
    5. The second test (`does not regress to the inline-var background pattern...`) uses `assertDontSee('style="background: var(', escape: false)` (Laravel test helper).
    6. No GSD / G2 / UAT / D-20 references in test names or code. The test names describe behavior in plain English.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php</automated>
  </verify>
  <acceptance_criteria>
    - File `Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php` exists.
    - `vendor/bin/pest Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php` exits 0.
    - File contains exactly three `it(` blocks.
    - `grep -c 'assertDontSee.*style=' Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php` returns at least `1`.
    - `grep -c 'assertStatus(200)' Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php` returns at least `1`.
    - `grep -c 'assertStatus(404)' Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php` returns at least `1`.
    - No file in `files_modified` contains `G2`, `UAT`, `Phase 16.1.1`, `.planning`, `D-20`.
  </acceptance_criteria>
  <done>Three-assertion regression test exists, exits 0, and locks in the fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /dev/artisan | Authenticated developer-only route guarded by `EnsureDeveloperMode` middleware (Phase 16 DEVUI-01). |
| /dev/artisan → ArtisanRunnerPage Livewire component | Live SAFE-tier command invocation surface — already gated by the SAFE-tier whitelist (Phase 16 DEVUI-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.1-06 | Elevation of privilege | /dev/artisan accessible to non-developer | mitigate | Phase 16 DEVUI-01's `EnsureDeveloperMode` middleware returns 404 (not 403) for non-developers. Task 3's third assertion locks this in as a regression guard. |
| T-16.1.1-07 | Tampering | New `openRunCommandModal` action (if layer-a applies) executes arbitrary artisan command | mitigate | The action does NOT execute a command — it only opens the Flux modal which displays the SAFE-tier whitelist; the actual `spawn()` invocation goes through the existing Phase 16 SAFE-tier whitelist guard (`Modules/DevMode/Internal/Services/CommandRegistry`). No new command-execution path opened. |
| T-16.1.1-08 | Tampering | Inline-var background indirection causes silent UI corruption (the original G2 bug) | mitigate | Task 2 layer-b fix removes the indirection; Task 3 second assertion is a permanent regression guard against re-introducing the pattern. |

No HIGH-severity threats. The plan is a regression fix; no new command-execution surface introduced.
</threat_model>

<verification>
- `vendor/bin/pest Modules/DevMode/tests/Feature/ArtisanRunnerNotWhitePageTest.php` exits 0.
- `vendor/bin/pint --test Modules/DevMode` exits 0.
- `vendor/bin/phpstan analyse --memory-limit=2G Modules/DevMode` exits 0.
- Manual sanity (one-time): re-build the .app bundle (`php artisan native:build`), reopen, navigate to /dev/artisan, confirm the page renders. If layer-a was applied, click the dispatcher and verify the Flux modal opens with the SAFE-tier list.
- `grep -rn 'style="background: var(' Modules/DevMode/` returns zero matches (full module hygiene sweep).
</verification>

<success_criteria>
- `/dev/artisan` renders with non-blank body for a developer user.
- The dormant `<flux:modal name="run-command">` either gains a caller (layer-a) or is left alone (no-repro), and any inline-var background indirection is gone (layer-b).
- Pest regression test green; locks the fix in place.
- All CI gates green: Pint, Larastan L10 strict, Pest.
- No GSD-leakage strings in any production source file modified.
</success_criteria>

<output>
Create `.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-03-SUMMARY.md` when done. The SUMMARY MUST include the reproduction note captured during Task 1 (what layer was hit, what the DOM/console/log showed) — that note is the only place the diagnostic detail lives, and lives outside source code per the codebase-agnostic-from-GSD memory.
</output>
