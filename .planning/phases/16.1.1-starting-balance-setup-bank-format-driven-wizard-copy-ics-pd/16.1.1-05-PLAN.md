---
phase: 16.1.1
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/Ledger/Database/Migrations/2026_05_27_000001_add_starting_balance_to_accounts_table.php
  - Modules/Ledger/Database/Migrations/2026_05_27_000002_backfill_starting_balance_from_statement_summaries.php
  - Modules/Ledger/Internal/Services/BackfillStartingBalanceFromStatementSummaries.php
  - Modules/Ledger/Models/Account.php
  - Modules/Ledger/tests/Unit/AccountStartingBalanceMigrationTest.php
autonomous: true
requirements: []
closes: [G1]
decisions: [D-01, D-02]
tags: [phase-16.1.1, schema, ledger, starting-balance]
must_haves:
  truths:
    - "Accounts table has nullable `starting_balance_minor` (bigInteger) and `starting_balance_date` (date) columns, idempotently backfilled from the earliest opening_balance per account in statement_summaries."
    - "Re-running the backfill migration is idempotent — accounts whose `starting_balance_*` is already set are skipped, preserving any user override."
    - "Account model `$fillable` includes the two new columns so Plan 07's commit-everything UPDATE path can write them via Eloquent."
  artifacts:
    - path: "Modules/Ledger/Database/Migrations/2026_05_27_000001_add_starting_balance_to_accounts_table.php"
      provides: "Schema migration adding starting_balance_minor + starting_balance_date columns to accounts"
      contains: "starting_balance_minor"
    - path: "Modules/Ledger/Database/Migrations/2026_05_27_000002_backfill_starting_balance_from_statement_summaries.php"
      provides: "Idempotent backfill migration calling BackfillStartingBalanceFromStatementSummaries"
      contains: "BackfillStartingBalanceFromStatementSummaries"
    - path: "Modules/Ledger/Internal/Services/BackfillStartingBalanceFromStatementSummaries.php"
      provides: "Backfill service — writes earliest opening_balance per account only when accounts.starting_balance_minor IS NULL"
    - path: "Modules/Ledger/Models/Account.php"
      provides: "$fillable contains starting_balance_minor + starting_balance_date"
      contains: "starting_balance_minor"
  key_links:
    - from: "Modules/Ledger/Database/Migrations/2026_05_27_000002_backfill_starting_balance_from_statement_summaries.php"
      to: "Modules/Ledger/Internal/Services/BackfillStartingBalanceFromStatementSummaries.php"
      via: "Container resolution at migration boundary (the documented DI exception)"
      pattern: "BackfillStartingBalanceFromStatementSummaries"
---

<objective>
Land the schema foundation for G1 starting-balance setup — the two `accounts.starting_balance_*` columns, the idempotent backfill that seeds them from existing `statement_summaries`, and the Account model fillable extension so Plan 07's `commit()` UPDATE path has a writable column to target.

Purpose: Close UAT finding G1 schema half per locked decisions D-01 + D-02. This plan ships ONLY the schema + backfill + Account fillable. The detector strategies + aggregator query (the OTHER half of G1) ship in Plan 05b, which depends on this plan.

Output: Two migrations + one backfill service + Account model fillable extension + one three-assertion Pest migration test.

(Split context: Plan 05 was originally a single 19-file plan covering schema + 4 detectors + aggregator + 6 tests. Per checker scope_sanity feedback, the plan was split: Plan 05 keeps the schema layer, Plan 05b takes the detector + aggregator layer. The split keeps each plan well under the 15-file blast-radius threshold and lets the executor focus on one concern at a time.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-CONTEXT.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-RESEARCH.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Reference shape for the schema migration (per PATTERNS.md Pattern 1) — analog has the SAME accounts.opening_balance_* columns the Forecasting plan added: -->

Existing analog at Modules/Forecasting/Database/Migrations/2026_05_19_010005_add_forecast_columns_to_accounts.php (lines 29-73):
```php
return new class extends Migration {
    private ?DatabaseManager $resolvedDb = null;

    public function up(): void
    {
        $this->schema()->table('accounts', static function (Blueprint $table): void {
            $table->bigInteger('forecast_min_buffer_minor')->nullable()->after('default_currency');
            $table->bigInteger('opening_balance_minor')->nullable()->after('forecast_min_buffer_minor');
            $table->date('opening_balance_as_of_date')->nullable()->after('opening_balance_minor');
        });
    }
    // ... down() drops, schema() + db() helpers resolve via Container::getInstance() ...
};
```

<!-- IMPORTANT: accounts.opening_balance_minor / opening_balance_as_of_date ALREADY EXIST (Forecasting). The new columns are DIFFERENT and SEMANTICALLY DISTINCT: -->
<!-- - accounts.opening_balance_minor / opening_balance_as_of_date — Forecasting's manual fallback for PayPal-style accounts (locked) -->
<!-- - accounts.starting_balance_minor / starting_balance_date — Phase 16.1.1's auto-detected baseline driven by the wizard -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema migration (D-01) + idempotent backfill migration + backfill service (D-02) + Account model fillable extension + AccountStartingBalanceMigrationTest</name>
  <files>Modules/Ledger/Database/Migrations/2026_05_27_000001_add_starting_balance_to_accounts_table.php, Modules/Ledger/Database/Migrations/2026_05_27_000002_backfill_starting_balance_from_statement_summaries.php, Modules/Ledger/Internal/Services/BackfillStartingBalanceFromStatementSummaries.php, Modules/Ledger/Models/Account.php, Modules/Ledger/tests/Unit/AccountStartingBalanceMigrationTest.php</files>
  <read_first>
    - Modules/Forecasting/Database/Migrations/2026_05_19_010005_add_forecast_columns_to_accounts.php (EXACT analog for the schema migration — PATTERNS.md Pattern 1)
    - Modules/Ledger/Database/Migrations/2026_05_13_010001_rederive_fingerprints_to_v3.php (analog for the idempotent backfill migration — PATTERNS.md Pattern 2)
    - Modules/Ledger/Models/Account.php (FULL file — current `$fillable` is `['user_id', 'name', 'slug', 'kind', 'iban', 'default_currency']` per `grep`)
    - Modules/Ledger/Database/Migrations/2026_05_13_010005_create_statement_summaries_table.php (verify column names: `account_id`, `opening_balance_minor`, `opening_balance_currency`, `opening_balance_date`, `user_id`, `import_run_id`)
    - Modules/Ledger/Models/StatementSummary.php (verify the column casts and relationships)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md Pattern 1 + 2 + Shared Patterns A, B
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-RESEARCH.md "Runtime State Inventory" + "Recommended Module Layout"
    - Modules/Onboarding/tests/Feature/WizardProgressMigrationTest.php (analog for the migration smoke test — PATTERNS.md "New Pest tests" table)
  </read_first>
  <behavior>
    - **Migration 1** (`2026_05_27_000001_add_starting_balance_to_accounts_table.php`): `up()` adds two nullable columns to `accounts` — `starting_balance_minor` (bigInteger, nullable, positioned after `default_currency`) and `starting_balance_date` (date, nullable, positioned after `starting_balance_minor`). `down()` drops both columns. The anonymous-migration shape from `add_forecast_columns_to_accounts.php` is reused verbatim — `schema()` + `db()` helpers + `resolvedDb` cache.
    - **Migration 2** (`2026_05_27_000002_backfill_starting_balance_from_statement_summaries.php`): `up()` resolves `BackfillStartingBalanceFromStatementSummaries::class` from the container and calls `$service->run()`. `down()` is a no-op with a comment "Backfill is data-only; rollback restores via SQLite backup if needed." (NO history-in-comments per `feedback_docs_describe_current_state` — the comment describes current behavior, not history.)
    - **BackfillStartingBalanceFromStatementSummaries** (new Internal service): constructor takes `DatabaseManager $db`. `run(): void` selects `MIN(opening_balance_date) AS earliest_date, account_id, opening_balance_minor` grouped by `account_id` from `statement_summaries` (a single CTE / sub-query with `ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY opening_balance_date ASC)` OR a simpler "two-pass" approach: first pass collects earliest_date per account, second pass picks the matching row's minor value). Then UPDATEs `accounts.starting_balance_minor` + `starting_balance_date` only when `accounts.starting_balance_minor IS NULL` AND `accounts.starting_balance_date IS NULL` (idempotency). Wraps in a single `$db->connection()->transaction(...)`.
    - **Account model** (`Modules/Ledger/Models/Account.php`): `$fillable` array gains two new entries — `'starting_balance_minor'`, `'starting_balance_date'`. The array order matches the migration order. The `$casts` property (if present) adds `'starting_balance_minor' => 'integer'`, `'starting_balance_date' => 'date'`. If `$casts` doesn't exist on the model yet, add a minimal cast definition for the two new columns (do not touch existing column casts).
    - **AccountStartingBalanceMigrationTest** (new Pest unit test): three `it(...)` blocks:
      1. `it('adds starting_balance_minor and starting_balance_date nullable columns to accounts')` — uses `Schema::hasColumn('accounts', 'starting_balance_minor')` + `hasColumn('accounts', 'starting_balance_date')` after migrations run.
      2. `it('backfills starting_balance_minor from the earliest statement_summaries.opening_balance per account')` — sets up two accounts, three statement_summaries (two with the same account, one with the other account, with explicit `opening_balance_date` values), runs the backfill service, asserts each account's `starting_balance_minor` matches the earliest summary's value and `starting_balance_date` matches the earliest date.
      3. `it('is idempotent — re-running the backfill does not overwrite a non-null starting_balance')` — pre-sets `accounts.starting_balance_minor` to a known value (e.g., user override), runs the backfill, asserts the value is unchanged.
  </behavior>
  <action>
    Follow PATTERNS.md Pattern 1 + Pattern 2 verbatim, swapping the column names and the backfill source. Concretely:
    1. Create `Modules/Ledger/Database/Migrations/2026_05_27_000001_add_starting_balance_to_accounts_table.php` by copying the anonymous-migration shape from `add_forecast_columns_to_accounts.php` and replacing the two `$table->...` lines with the new column definitions. Use `->after('default_currency')` for the first column and `->after('starting_balance_minor')` for the second.
    2. Create `Modules/Ledger/Internal/Services/BackfillStartingBalanceFromStatementSummaries.php` with constructor `public function __construct(private readonly DatabaseManager $db)` and a `run(): void` method that implements the idempotent backfill per `<behavior>` above. Use the explicit `where('user_id', ...)` filter pattern from PATTERNS.md Shared Pattern B on every statement_summaries read (no reliance on global scope).
    3. Create `Modules/Ledger/Database/Migrations/2026_05_27_000002_backfill_starting_balance_from_statement_summaries.php` mirroring `rederive_fingerprints_to_v3.php`'s container-resolution shape; `up()` resolves and runs the service; `down()` is a no-op.
    4. Open `Modules/Ledger/Models/Account.php`; extend `$fillable` to `['user_id', 'name', 'slug', 'kind', 'iban', 'default_currency', 'starting_balance_minor', 'starting_balance_date']`. If `$casts` exists, add the two new entries; if it doesn't, add a `$casts` property containing only the two new entries (don't enumerate existing implicit casts).
    5. Create `Modules/Ledger/tests/Unit/AccountStartingBalanceMigrationTest.php` with the three `it(...)` blocks from `<behavior>`. Use Pest's `RefreshDatabase` (or whatever the Ledger module's test bootstrap provides). The test file groups itself as `phase-16.1.1`.
    6. PHPDoc on `BackfillStartingBalanceFromStatementSummaries` describes WHAT the service does in plain English ("Writes the earliest opening balance per account into accounts.starting_balance_*, skipping rows where the user has already set a value.") — current state only, no history.
    7. No `.planning/` / G1 / D-01 / D-02 / UAT references anywhere in source or PHPDocs. The migration filenames are date-only per Laravel convention.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Ledger/tests/Unit/AccountStartingBalanceMigrationTest.php</automated>
  </verify>
  <acceptance_criteria>
    - Both migration files exist with the date prefix `2026_05_27_000001_` and `2026_05_27_000002_` and run cleanly via `php artisan migrate:fresh` (verified by the Pest test using `RefreshDatabase`).
    - `Modules/Ledger/Models/Account.php` `$fillable` contains both `'starting_balance_minor'` and `'starting_balance_date'` — verified by `grep`.
    - `vendor/bin/pest Modules/Ledger/tests/Unit/AccountStartingBalanceMigrationTest.php` exits 0.
    - The test file contains exactly three `it(` blocks.
    - `grep -c "where('user_id'" Modules/Ledger/Internal/Services/BackfillStartingBalanceFromStatementSummaries.php` returns at least `1` (explicit user-id filter on every statement_summaries read).
    - `vendor/bin/phpstan analyse --memory-limit=2G Modules/Ledger` exits 0 at Larastan L10 strict.
    - `vendor/bin/pint --test Modules/Ledger` exits 0.
    - No grep hit for `G1|UAT|Phase 16.1.1|.planning|D-01|D-02` in any file under `Modules/Ledger/`.
  </acceptance_criteria>
  <done>Schema migration + backfill migration + backfill service + Account fillable + three-assertion Pest test all in place, all green. Plan 05b can now ship its detectors + aggregator on top of this schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration boundary → container | `BackfillStartingBalanceFromStatementSummaries` is resolved from the container at migration time (documented DI exception per PATTERNS.md Pattern 2). |
| Backfill service → SQLite | Per-user-scoped reads against `statement_summaries`, UPDATEs against `accounts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.1-12a | Tampering | `BackfillStartingBalanceFromStatementSummaries::run()` overwrites a user override | mitigate | The backfill UPDATE clause includes `WHERE starting_balance_minor IS NULL AND starting_balance_date IS NULL` — re-running the migration is idempotent and never overwrites a user-set value. Locked by the third `it(...)` block in `AccountStartingBalanceMigrationTest`. |
| T-16.1.1-14 | Information disclosure | `accounts.starting_balance_*` columns visible cross-user via raw DB access | accept | `accounts` already carries the `BelongsToUser` global scope (Phase 12). The new columns inherit that scope. No new disclosure surface. |

No HIGH-severity threats. Pure schema + backfill plane. Detector-related threats live in Plan 05b's threat model.
</threat_model>

<verification>
- `vendor/bin/pest --group=phase-16.1.1 Modules/Ledger/tests` returns 0.
- `vendor/bin/pint --test Modules/Ledger` returns 0.
- `vendor/bin/phpstan analyse --memory-limit=2G Modules/Ledger` returns 0.
- `php artisan migrate:fresh --env=testing` runs cleanly (verified by the migration test's `RefreshDatabase` boot).
</verification>

<success_criteria>
- `accounts` table has nullable `starting_balance_minor` + `starting_balance_date` columns.
- Idempotent backfill writes the earliest opening_balance per account from `statement_summaries`.
- Account model `$fillable` accepts both new columns; Plan 07 can call `Account::update([...])` to set them.
- Three-assertion Pest migration test green.
- All CI gates green: Pint, Larastan L10 strict, Pest on the phase-16.1.1 group.
- No GSD-leakage strings in source.
</success_criteria>

<output>
Create `.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-05-SUMMARY.md` when done.
</output>
