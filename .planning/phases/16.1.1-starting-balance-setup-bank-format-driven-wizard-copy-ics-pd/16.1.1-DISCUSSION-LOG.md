# Phase 16.1.1: UAT close-out from Phase 16.1 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in 16.1.1-CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-25
**Phase:** 16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pdf-preview-fix-dev-mode-artisan-popover-fix-app-chrome-brand-logo
**Areas discussed:** Phase scope (all 9 vs subset), G1 starting balance schema + auto-detect + UX, G7+G8+G9 wizard upload→persist + multi-PDF, G6 OAuth modal mount, G3 bank-format copy + gate flow

---

## Area 0 — Gray-area selection

| Option | Description | Selected |
|--------|-------------|----------|
| Phase scope — all 9 or just 5? | ROADMAP title lists G1+G2+G3+G4+G5. UAT.md grew to 9; G9 is core-value-blocking. Does 16.1.1 close out all 9 or split into 16.2? | ✓ |
| Starting balance (G1) — schema + auto-detect | Where balance lives (accounts vs users vs derived); which parsers auto-detect; manual-override UX. | ✓ |
| Wizard upload→persist (G7+G9 cluster) | Auto-commit per step vs consolidated FirstImportStep commit. | ✓ |
| Bank wizard copy (G3) — minimal or restructured? | Replace "ASN" with bank list vs format-first chip selection. | ✓ |

**User's choice:** All four areas selected for discussion.

---

## Area 1 — Phase scope

| Option | Description | Selected |
|--------|-------------|----------|
| All 9 — full UAT close-out | 16.1.1 covers every UAT finding (G1–G9). Title gets updated. G9 (CRITICAL) ships with its cluster; G1 actually works because uploads now persist; user re-tests once. | ✓ |
| Five in title + insert 16.2 | 16.1.1 = G1+G2+G3+G4+G5 only; G6+G7+G8+G9 split into 16.2. Tighter phase but starting balance is moot until G9 lands. | |
| Scope to G1+G9+G7+G8 — wizard cluster only | Focus on architectural coherence; defer cosmetic + independent debug items. Ships nothing user can see in chrome. | |

**User's choice:** All 9 — full UAT close-out (Recommended).
**Notes:** Locks scope before deeper architecture questions; title in ROADMAP.md needs updating per D-22.

---

## Area 2 — Starting balance (G1)

### Schema location

| Option | Description | Selected |
|--------|-------------|----------|
| accounts.starting_balance_minor + .starting_balance_date | Per-account, multi-currency-safe via existing default_currency; future-proof for multi-account / partner scenarios. | ✓ |
| users.starting_balance_minor + .starting_balance_date | Single-account v1 — breaks the moment user has both ASN account + ICS card. | |
| Derive at query time from statement_summaries | No new column; computed every render. Can't override; expensive. | |

**User's choice:** accounts columns (Recommended).

### Auto-detect sources

| Option | Description | Selected |
|--------|-------------|----------|
| CAMT.053 <OpngBal> via genkgo/camt | ASN + ING; already in statement_summaries.opening_balance_minor. | ✓ |
| MT940 :60F: via kingsquare/php-mt940 | ASN + ING + Rabobank + Triodos + SNS + Bunq. | ✓ |
| ICS PDF closing balance | Mijn ICS monthly PDF "Vorig saldo" / "Nieuw saldo" line; needs research confirmation. | ✓ |
| PayPal running balance column | PayPal Activity CSV first row's pre-transaction balance. | ✓ |

**User's choice:** All four detectors fire.
**Notes:** Per-source detector strategies discovered via service-container tagging (mirrors PaymentTypeHinter pattern from 16.1 D-07).

### Manual override UX

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-detect + one-click confirm + 'Edit' link | Card per account with detected value; one click confirms, Edit reveals inline inputs. | ✓ |
| Always-editable two-field form (no auto-detect surfacing) | Empty inputs pre-filled silently; no separate confirm moment. | |
| Auto-detect silently — expose override only in Settings later | Wizard auto-commits; user edits later in Settings → Accounts. | |

**User's choice:** Auto-detect + one-click confirm + Edit link (Recommended).

---

## Area 3 — Wizard upload→persist architecture (G7+G9 cluster)

### Architecture choice

| Option | Description | Selected |
|--------|-------------|----------|
| Option B — stash preview IDs, FirstImportStep commits all | Connector steps preview only + stash ImportRun ids in wizard_progress.data; FirstImportStep loads them, renders unified preview, one "Commit everything" button calls ConfirmImport against all in one DB transaction. | ✓ |
| Option A — auto-commit at each connector step | Each step's submit() does preview + commit synchronously; FirstImportStep becomes read-only recap. Simpler but can't see + correct before commit. | |
| Option B' — same as B but per-source commit buttons | Granular but more clicks; breaks coherent starting-balance derivation. | |

**User's choice:** Option B (Recommended).
**Notes:** Matches UI-SPEC intent + UAT G7 recommendation + lets G1 derive starting balance from the preview before commit.

### G8 multi-PDF wiring

| Option | Description | Selected |
|--------|-------------|----------|
| Plural array<UploadedFile> $statements + multiple file input | Loop preview pipeline once per file; FingerprintStage handles cross-file dedup; per-file validation via Livewire array rule syntax. | ✓ |
| Single file + 'Add another statement' button between submits | Many round-trips for 12 monthly statements; ugly UX. | |

**User's choice:** Plural array (Recommended).

---

## Area 4 — G6 OAuth modal mount

| Option | Description | Selected |
|--------|-------------|----------|
| Mount once in main authenticated layout | Globally-mounted modal; ConnectEmailStep + InboxesPage + future surfaces dispatch into it. Mirrors CR-01 SuggestMappingModal fix. | ✓ |
| Keep per-step embed, fix the event-name mismatch only | Minimal touch but same bug class will recur for the next surface. | |

**User's choice:** Mount once in main authenticated layout (Recommended).

---

## Area 5 — G3 bank wizard copy

### Copy approach

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal — rename 'ASN' to format-driven bank list | Keep current step shape; replace "ASN" mentions with format + bank list. Format-chip atomic component renders three formats. | |
| Restructured — format-first chip selection drives copy | User picks CAMT.053 / MT940 / CSV chip first; each chip reveals format-specific helper copy + bank list + extension hint. | ✓ |
| Hybrid — minimal copy + format-chip is informational only | Passive informational badges with hover tooltip listing banks. Poor touch discoverability. | |

**User's choice:** Restructured.

### Gate flow

| Option | Description | Selected |
|--------|-------------|----------|
| Optional — chip selection re-skins drop zone but doesn't block upload | All chips visible; drop always active; clicking a chip updates helper inline. | |
| Required — user must pick a chip first; drop zone unlocks after | Maximally guided but extra click; fights existing auto-detect. | |
| Required for CSV only — CAMT.053 + MT940 auto-detect, CSV needs bank-format chip | CSV is ambiguous (per-bank column shapes); CAMT + MT940 auto-detect from extension. Targets the actual ambiguity. | ✓ |

**User's choice:** Required for CSV only.
**Notes:** Implies a new BankCsvFormatHint enum + format_hint field on the upload submit; parser dispatch bypasses content sniff for CSV when hint is present (D-17).

---

## Final confirmation

| Option | Description | Selected |
|--------|-------------|----------|
| All good — write CONTEXT.md | Lock decisions + write artifact. | ✓ |
| Revisit G2 (artisan popover) or G4 (PDF preview) | Drill deeper into the two debug-first items. | |
| Revisit G5 brand logo placement | Confirm exact slot in authenticated layout. | |

**User's choice:** Write CONTEXT.md.

---

## Claude's Discretion

Areas left to the researcher / planner judgement (documented in CONTEXT.md `<decisions>` → "Claude's Discretion"):

- G1 confirm UX: new sixth wizard step vs section on FirstImportStep.
- Plan ordering + grouping (suggested parallelization in CONTEXT.md).
- `wizard_progress.data` JSON shape details (baseline: flat `bank_import_run_id` + `card_import_run_ids[]` keys).
- `BuildConsolidatedPreview` query module home (Import.Public.Queries vs Onboarding.Internal.Services).
- Whether to fold batch-7 Issue 1 (canonical-fingerprint CSV↔XML dedup) into this phase or treat as precondition.
- G2 + G4 reproduce-first investigation; specific file targets identified during research.

## Deferred Ideas

Carried into Deferred section of CONTEXT.md and NOT acted on in this phase:

- Multi-account starting-balance dashboard widget (future polish phase).
- Settings → Accounts starting-balance correction surface (future polish phase).
- CSV bank-format auto-detect by column sniffing (future "import polish" phase).
- diederik-bot GitHub App + draft-PR submission (already deferred in 16.1).
- Live community-corpus fetch on app launch (already deferred to Phase 18).
- Receipt-image OCR / Apple Wallet receipt scanning (already deferred).
- Wizard re-run from Settings → First-run tour (future polish — re-run already architecturally supported per 16.1 D-04).
