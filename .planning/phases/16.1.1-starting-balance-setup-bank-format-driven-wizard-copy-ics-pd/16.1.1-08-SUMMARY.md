---
phase: 16.1.1
plan: 08
subsystem: planning-docs
tags: [phase-16.1.1, roadmap, docs, d-22]
dependency_graph:
  requires: []
  provides:
    - ROADMAP.md Phase 16.1.1 entry reflects full nine-finding close-out title + cleaned-up goal + Slug + ten-plan list
  affects: []
tech_stack:
  added: []
  patterns: []
key_files:
  created:
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-08-SUMMARY.md
  modified:
    - .planning/ROADMAP.md
decisions:
  - "D-22 honored: ROADMAP.md Phase 16.1.1 title rewritten to reflect the full close-out cluster naming all nine findings (G1–G9); goal placeholder replaced with the cleaned-up domain statement from CONTEXT.md `<domain>`."
  - "Dropped the `Requirements: TBD` row in favor of the G-id close-out posture (per plan must_haves truth #3 — UAT close-out, no REQ-IDs)."
  - "Added the `**Slug:**` line so the entry matches the existing Phase 16 entry style (the only Phase entry style in the project that includes a Slug line)."
metrics:
  duration_minutes: ~5
  tasks_completed: 1
  files_modified: 1
  completed_date: 2026-05-25
---

# Phase 16.1.1 Plan 08: ROADMAP.md title + goal update (D-22) Summary

ROADMAP.md Phase 16.1.1 entry updated per locked decision D-22 — title now reflects all nine UAT findings, goal swapped from `[Urgent work - to be planned]` placeholder to the cleaned-up domain statement, and the `**Slug:**` line added to match the project's existing entry style.

## What Was Done

A single surgical edit to `.planning/ROADMAP.md` replaced three lines in the Phase 16.1.1 block (and dropped the now-irrelevant `Requirements: TBD` row):

1. **Title swap** — Replaced the stale 5-finding title:

   _Old:_ `Phase 16.1.1: Starting balance setup + bank-format-driven wizard copy + ICS PDF preview fix + Dev Mode artisan popover fix + app-chrome brand logo (UAT close-out from phase 16.1) (INSERTED)`

   _New:_ `Phase 16.1.1: First-run wizard UAT close-out — starting balance + upload→persist + multi-PDF + bank-format copy + OAuth modal mount-once + ICS PDF preview + Dev Mode artisan popover + brand logo (UAT close-out from Phase 16.1) (INSERTED)`

   The new title names all nine findings (G1–G9) instead of only G1+G2+G3+G4+G5.

2. **Goal swap** — Replaced the placeholder with the cleaned-up domain statement derived from `16.1.1-CONTEXT.md` `<domain>` (lines 6–89) and pre-approved verbatim in the plan's `<interfaces>` block:

   > Make the first-run setup wizard actually work end-to-end on a fresh install so a new user can sign up → run the wizard → see their real transactions AND their real starting balance on the dashboard — not just transaction deltas, not "we did nothing" silence, not "upload twice", not "ASN-only" copy. All nine UAT findings (G1–G9) close in this phase.

3. **Requirements row drop** — Removed `**Requirements**: TBD` (per plan must_haves truth #3: this phase is a UAT close-out keyed off G-ids, no REQ-IDs).

4. **Slug line add** — Inserted `**Slug:** \`16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pdf-preview-fix-dev-mode-artisan-popover-fix-app-chrome-brand-logo\`` to match the project's existing entry style (compare Phase 16 which lists `**Slug:** 16-developer-mode-ui`).

The ten-plan list and `**Plans:** 10 plans` count were already present in the file from the planner's roadmap insertion and remain unchanged. The `**Depends on:** Phase 16.1` line is unchanged.

## Why

`D-22` is a locked decision in `16.1.1-CONTEXT.md` (lines 305–313). The plan's whole purpose is to land that ROADMAP.md update. The existing entry was inserted with a placeholder title/goal at phase-start so planning could move forward; this plan retires that placeholder against the cleaned-up domain statement now that all ten constituent plans are known and the close-out scope is fully understood.

## Verification

All five plan acceptance assertions pass:

| # | Assertion | Expected | Actual |
| - | --------- | -------- | ------ |
| 1 | `grep -c "Phase 16\.1\.1: First-run wizard UAT close-out" .planning/ROADMAP.md` | 1 | 1 |
| 2 | `grep -c "Urgent work - to be planned" .planning/ROADMAP.md` | 0 | 0 |
| 3 | `grep -cE '16\.1\.1-(0[1-8]|05b|07b)-PLAN\.md' .planning/ROADMAP.md` | 10 | 10 |
| 4 | `grep -c '\*\*Plans:\*\* 10 plans' .planning/ROADMAP.md` | 1 | 1 |
| 5 | `**Slug:**` line present matching the new slug | 1 | 1 |

`git diff --stat .planning/ROADMAP.md` reports `3 insertions(+), 3 deletions(-)` — the diff is contained to the Phase 16.1.1 block. Phase 16.1 entry above, Phase 17 entry below, and the Progress table at the bottom are unchanged.

## Tasks

| Task | Name                                                                                                                | Commit  | Files                |
| ---- | ------------------------------------------------------------------------------------------------------------------- | ------- | -------------------- |
| 1    | Replace the Phase 16.1.1 ROADMAP.md entry with the close-out title + cleaned-up goal + ten-plan list | 6a393e9 | .planning/ROADMAP.md |

## Deviations from Plan

None — plan executed exactly as written. The plan's `<action>` block listed six concrete steps; all six were honored:

1. Read the current Phase 16.1.1 block. ✓
2. Replace that block with the new block from `<interfaces>`. ✓
3. Did not touch Phase 16.1 (above), Phase 17 (below), or the Progress table. ✓
4. Added the `**Slug:**` line matching Phase 16's entry style. ✓
5. Plan filenames match the ten files in `.planning/phases/16.1.1-.../` (verified via grep — count is 10). ✓
6. No code comments needed (documentation surface). ✓

## Note on TDD Gate

The plan frontmatter marks Task 1 as `tdd="true"`. However, the task is a pure documentation transformation of a planning artifact — the `<behavior>` describes a markdown edit, the `<action>` describes a single surgical edit, and the `<verify>` block specifies `grep` assertions rather than test code. There is no runtime surface, no behavior to assert with Pest/PHPUnit, and no code path to red-green-refactor. The grep assertions served as the verification gate; all five passed before the commit. The MVP+TDD gate's behavior-adding predicate would return `false` here (no source files in `<files>` — only `.planning/ROADMAP.md`), so the gate is appropriately exempt.

## Threat Flags

None — the plan's threat model classifies this as `accept` for `T-16.1.1-27`: docs-only edit, no security-relevant surface. ROADMAP.md is a planning artifact, not runtime code; the codebase-agnostic-from-GSD memory does not apply (`.planning/` files are GSD docs by definition).

## Self-Check: PASSED

- File exists: `.planning/ROADMAP.md` (verified via grep assertions) — FOUND
- File exists: `.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-08-SUMMARY.md` (this file)
- Commit exists: `6a393e9 docs(16.1.1-08): update ROADMAP Phase 16.1.1 entry per D-22` — FOUND
