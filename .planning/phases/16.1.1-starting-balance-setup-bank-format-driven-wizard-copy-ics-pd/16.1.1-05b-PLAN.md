---
phase: 16.1.1
plan: 05b
type: execute
wave: 2
depends_on: ["16.1.1-05"]
files_modified:
  - Modules/Import/Public/Contracts/DetectsStartingBalance.php
  - Modules/Import/Public/Dto/StartingBalanceCandidate.php
  - Modules/Import/Internal/Detectors/Camt053StartingBalanceDetector.php
  - Modules/Import/Internal/Detectors/Mt940StartingBalanceDetector.php
  - Modules/Import/Internal/Detectors/IcsPdfStartingBalanceDetector.php
  - Modules/Import/Internal/Detectors/PaypalCsvStartingBalanceDetector.php
  - Modules/Import/Public/Services/DetectStartingBalancesQuery.php
  - Modules/Import/Providers/ImportServiceProvider.php
  - Modules/Import/tests/Unit/Camt053StartingBalanceDetectorTest.php
  - Modules/Import/tests/Unit/Mt940StartingBalanceDetectorTest.php
  - Modules/Import/tests/Unit/IcsPdfStartingBalanceDetectorTest.php
  - Modules/Import/tests/Unit/PaypalCsvStartingBalanceDetectorTest.php
  - Modules/Import/tests/Unit/DetectStartingBalancesQueryConflictTest.php
  - Modules/Import/tests/Feature/StartingBalanceDetectorRegistryTest.php
autonomous: true
requirements: []
closes: [G1]
decisions: [D-03, D-04]
tags: [phase-16.1.1, detectors, import, starting-balance]
must_haves:
  truths:
    - "Four StartingBalanceDetector implementations (Camt053, Mt940, IcsPdf, PaypalCsv) are bound under the `starting-balance.detector` container tag and discoverable via `$app->tagged('starting-balance.detector')`."
    - "DetectStartingBalancesQuery aggregates per-account candidates across all four sources and applies D-04 conflict resolution (earliest opening_balance_date wins; CAMT.053 preferred over MT940 on tie)."
    - "PaypalCsvStartingBalanceDetector::detect() returns an empty list (never `0` as a balance) so the wizard surfaces a manual-entry card per RESEARCH.md Hook 2."
    - "Plan 07's FirstImportStep can resolve `DetectStartingBalancesQuery` from the container and call `collect($importRunIds, $user)` to produce the StartingBalanceCandidate list rendered as confirm cards."
  artifacts:
    - path: "Modules/Import/Public/Contracts/DetectsStartingBalance.php"
      provides: "Strategy interface — supports(string $sourceFormat): bool + detect(array $importRunIds, User $user): array"
    - path: "Modules/Import/Public/Dto/StartingBalanceCandidate.php"
      provides: "Readonly DTO with accountId, openingBalanceMinor, openingBalanceDate, sourceFormat"
    - path: "Modules/Import/Public/Services/DetectStartingBalancesQuery.php"
      provides: "Aggregator over the four tagged detectors; applies D-04 conflict resolution"
    - path: "Modules/Import/Providers/ImportServiceProvider.php"
      provides: "Container tag binding + DetectStartingBalancesQuery singleton wired to iterable<DetectsStartingBalance>"
      contains: "starting-balance.detector"
  key_links:
    - from: "Modules/Import/Public/Services/DetectStartingBalancesQuery.php"
      to: "$app->tagged('starting-balance.detector')"
      via: "Iterable injection of the four detector implementations"
      pattern: "starting-balance\\.detector"
    - from: "Modules/Import/Providers/ImportServiceProvider.php"
      to: "Modules/Import/Internal/Detectors/*StartingBalanceDetector.php"
      via: "STARTING_BALANCE_DETECTOR_FQNS const + foreach singleton+tag binding loop"
      pattern: "STARTING_BALANCE_DETECTOR_FQNS"
---

<objective>
Build the detector strategies + aggregator query layer on top of Plan 05's schema. This plan ships the `DetectsStartingBalance` contract, the `StartingBalanceCandidate` DTO, the four per-source detector implementations, the `ImportServiceProvider` tag binding, the `DetectStartingBalancesQuery` aggregator (with D-04 conflict resolution), and six new Pest tests (one registry feature test, four per-detector unit tests, one aggregator conflict test).

Purpose: Close the OTHER half of UAT finding G1 per locked decisions D-03 + D-04. Mirrors the `PaymentTypeHinter` tagged-strategy pattern from 16.1 D-07 verbatim. Plan 07 consumes this plan's `DetectStartingBalancesQuery::collect()` to render the starting-balance confirm cards.

(Split context: Originally Plan 05 was a single 19-file plan covering schema + detectors + aggregator + 7 tests. Per checker scope_sanity feedback, the plan was split: Plan 05 ships the schema, Plan 05b ships the detector + aggregator layer. The split serializes the work (05 → 05b) at no parallelization cost — both pairs already ran serially since 05's Account.starting_balance_* schema is the substrate the consumer plans target.)

Output: One contract + one DTO + four detector classes + one aggregator service + one service-provider extension + six Pest tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-CONTEXT.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-RESEARCH.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md
@.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- PaymentTypeHinter contract analog (PATTERNS.md Pattern 3) — entire file: -->
Modules/Import/Public/Contracts/PaymentTypeHinter.php:
```php
interface PaymentTypeHinter
{
    public function hint(CanonicalTransaction $tx, string $sourceFormat): ?PaymentTypeHint;
}
```

<!-- The contract for DetectsStartingBalance is similar but reads from statement_summaries, not from a CanonicalTransaction. -->

<!-- Strategy registration analog at Modules/Import/Providers/ImportServiceProvider.php lines 76-137 (PATTERNS.md Pattern 5) — const list + tag loop + tagged aggregator. Use the same shape with FQN const + foreach tag + DetectStartingBalancesQuery singleton binding. -->

<!-- PayPal Saldo column rationale (Hook 2 / Pitfall 3) — the PayPal detector MUST return [] not [Candidate(0, ...)]. -->

<!-- D-04 conflict resolution semantics: -->
<!-- 1. Earliest openingBalanceDate wins. -->
<!-- 2. If two candidates share the same date, prefer sourceFormat === 'asn-camt053' over 'asn-mt940'. -->
<!-- 3. If still tied (e.g., two CAMT.053 imports of overlapping periods with different values), return BOTH candidates for that account — the UI in Plan 07b renders the conflict variant. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DetectsStartingBalance contract + StartingBalanceCandidate DTO + four detector implementations + ImportServiceProvider tag binding + per-source unit tests + registry feature test</name>
  <files>Modules/Import/Public/Contracts/DetectsStartingBalance.php, Modules/Import/Public/Dto/StartingBalanceCandidate.php, Modules/Import/Internal/Detectors/Camt053StartingBalanceDetector.php, Modules/Import/Internal/Detectors/Mt940StartingBalanceDetector.php, Modules/Import/Internal/Detectors/IcsPdfStartingBalanceDetector.php, Modules/Import/Internal/Detectors/PaypalCsvStartingBalanceDetector.php, Modules/Import/Providers/ImportServiceProvider.php, Modules/Import/tests/Unit/Camt053StartingBalanceDetectorTest.php, Modules/Import/tests/Unit/Mt940StartingBalanceDetectorTest.php, Modules/Import/tests/Unit/IcsPdfStartingBalanceDetectorTest.php, Modules/Import/tests/Unit/PaypalCsvStartingBalanceDetectorTest.php, Modules/Import/tests/Feature/StartingBalanceDetectorRegistryTest.php</files>
  <read_first>
    - Modules/Import/Public/Contracts/PaymentTypeHinter.php (EXACT analog for the contract — PATTERNS.md Pattern 3)
    - Modules/Import/Public/Dto/PaymentTypeHint.php (EXACT analog for the DTO — PATTERNS.md Pattern 7)
    - Modules/Import/Internal/Parsers/Asn/AsnCamt053PaymentTypeHinter.php (positive-case analog — PATTERNS.md Pattern 4)
    - Modules/Import/Internal/Parsers/Paypal/PaypalCsvPaymentTypeHinter.php (decline-only-case analog — PATTERNS.md Pattern 4)
    - Modules/Import/Providers/ImportServiceProvider.php (FULL file, especially the PAYMENT_TYPE_HINTER_FQNS block + foreach tag — PATTERNS.md Pattern 5)
    - Modules/Import/tests/Feature/PaymentTypeHinterRegistryTest.php (EXACT analog for the registry test — PATTERNS.md "Container-tag registry test")
    - Modules/Import/tests/Unit/IcsPdfPaymentTypeHinterTest.php (analog for per-source unit tests)
    - Modules/Ledger/Models/Account.php POST-Plan-05 (the new `starting_balance_minor` + `starting_balance_date` fillable entries — verify they are present)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-RESEARCH.md "Code Examples" (lines 821-948) — verbatim usable code for the contract + IcsPdf detector + PayPal detector
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-RESEARCH.md Hook 2 (PayPal returns empty list)
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md Pattern 3 + 4 + 5 + Shared Pattern A + B
  </read_first>
  <behavior>
    - **DetectsStartingBalance contract** under `Modules/Import/Public/Contracts/`: `interface DetectsStartingBalance { public function supports(string $sourceFormat): bool; public function detect(array $importRunIds, User $user): array; }` returning `list<StartingBalanceCandidate>`. PHPDoc describes "Per-source strategy. Returns an empty list when this source has no usable opening-balance signal." Plain English; no GSD references.
    - **StartingBalanceCandidate DTO** under `Modules/Import/Public/Dto/`: extends `Spatie\LaravelData\Data` (matches `PaymentTypeHint` shape). Public readonly fields: `int $accountId`, `int $openingBalanceMinor`, `string $openingBalanceDate` (ISO date string), `string $sourceFormat`.
    - **Camt053StartingBalanceDetector** under `Modules/Import/Internal/Detectors/`: `supports()` returns `$sourceFormat === 'asn-camt053'`. `detect()` queries `statement_summaries` joined to `import_runs`, filters `import_runs.source_format = 'asn-camt053'`, `import_runs.user_id = $user->id`, `whereIn('import_run_id', $importRunIds)`, `whereNotNull('opening_balance_minor')` + `whereNotNull('opening_balance_date')`, orders `opening_balance_date ASC`, returns earliest row per account_id as a `StartingBalanceCandidate`. Constructor takes `DatabaseManager $db`.
    - **Mt940StartingBalanceDetector**: same shape, `supports()` returns `$sourceFormat === 'asn-mt940'`; query filters `source_format = 'asn-mt940'`.
    - **IcsPdfStartingBalanceDetector**: same shape, `supports()` returns `$sourceFormat === 'ics-pdf'`; query filters `source_format = 'ics-pdf'`. Use the exact code from RESEARCH.md lines 852-913 (drop-in).
    - **PaypalCsvStartingBalanceDetector**: `supports()` returns `$sourceFormat === 'paypal-csv'`. `detect()` returns `[]` unconditionally. PHPDoc explains the rationale in plain English (no Saldo-as-balance — verbatim usable from RESEARCH.md lines 917-948).
    - **ImportServiceProvider** gains a parallel `STARTING_BALANCE_DETECTOR_FQNS` const + `foreach (self::STARTING_BALANCE_DETECTOR_FQNS as $fqn) { if (class_exists($fqn)) { $this->app->singleton($fqn); $this->app->tag([$fqn], 'starting-balance.detector'); } }` block. The `DetectStartingBalancesQuery` singleton binding is added in Task 2 of this plan — for now only the FQN-list + tag-loop lands.
    - **Per-source unit tests**: each test seeds a small set of `statement_summaries` rows for the user + accounts, invokes the detector's `detect()`, asserts the returned list matches the expected `StartingBalanceCandidate`s (count, accountId, openingBalanceMinor, openingBalanceDate, sourceFormat).
    - **StartingBalanceDetectorRegistryTest** (Feature test in `Modules/Import/tests/Feature/`): two `it(...)` blocks — (1) asserts exactly four implementations are tagged under `'starting-balance.detector'`; (2) asserts the documented order (Camt053, Mt940, IcsPdf, PaypalCsv).
  </behavior>
  <action>
    Mirror the PaymentTypeHinter machinery 1:1 with the per-source code from RESEARCH.md. Concretely:
    1. Create `Modules/Import/Public/Contracts/DetectsStartingBalance.php` with the interface shape per `<behavior>`. Strict types declared, plain-English PHPDoc.
    2. Create `Modules/Import/Public/Dto/StartingBalanceCandidate.php` as a readonly Spatie Data DTO with the four public readonly fields per `<behavior>`. Mirror `PaymentTypeHint.php`'s file structure.
    3. Create the four detector classes under `Modules/Import/Internal/Detectors/`. Each is `final`, `implements DetectsStartingBalance`, has a `private const SOURCE_FORMAT = '<source>'`, constructor `DatabaseManager $db` (except PaypalCsv which needs no constructor params), `supports()` does an exact-string check against the const, `detect()` either runs the canonical query (Camt053 / Mt940 / IcsPdf) or returns `[]` (PaypalCsv). Use the verbatim query in RESEARCH.md lines 880-911 for IcsPdf and adapt the `where('source_format', ...)` clause for Camt053 + Mt940.
    4. Open `Modules/Import/Providers/ImportServiceProvider.php`. Add a new private const `STARTING_BALANCE_DETECTOR_FQNS = [Camt053StartingBalanceDetector::class, Mt940StartingBalanceDetector::class, IcsPdfStartingBalanceDetector::class, PaypalCsvStartingBalanceDetector::class]` (use FQN strings if the FQNs aren't `use`-imported, or class-name constants if they are — match the existing `PAYMENT_TYPE_HINTER_FQNS` style). In `register()`, add a parallel `foreach ... class_exists ... singleton ... tag` block. (Task 2 of this plan adds the `DetectStartingBalancesQuery` singleton — for now, this task only ships the tag-loop.)
    5. Create four Pest unit tests under `Modules/Import/tests/Unit/` — one per detector. Each seeds 2-3 `statement_summaries` rows + the related `import_runs` + `accounts` (use the `User` fixture pattern from `AsnCamt053PaymentTypeHinterTest`) and asserts the returned candidate count + field values.
    6. Create `Modules/Import/tests/Feature/StartingBalanceDetectorRegistryTest.php` mirroring `PaymentTypeHinterRegistryTest` line-for-line per PATTERNS.md "Container-tag registry test" (verbatim transplant possible).
    7. Use the explicit `where('user_id', $user->id)` filter pattern from PATTERNS.md Shared Pattern B on every detector query (no reliance on `BelongsToUser` global scope — workers / artisan commands bypass it).
    8. PHPDocs on every new class describe current behavior in plain English. PayPal's PHPDoc explains the Saldo-zero rationale per RESEARCH.md lines 924-932 — VERBATIM from research.
    9. No GSD-leakage strings anywhere.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Import/tests/Unit/Camt053StartingBalanceDetectorTest.php Modules/Import/tests/Unit/Mt940StartingBalanceDetectorTest.php Modules/Import/tests/Unit/IcsPdfStartingBalanceDetectorTest.php Modules/Import/tests/Unit/PaypalCsvStartingBalanceDetectorTest.php Modules/Import/tests/Feature/StartingBalanceDetectorRegistryTest.php</automated>
  </verify>
  <acceptance_criteria>
    - The four detector files exist under `Modules/Import/Internal/Detectors/`.
    - The contract + DTO exist under `Modules/Import/Public/Contracts/` + `Modules/Import/Public/Dto/`.
    - `Modules/Import/Providers/ImportServiceProvider.php` contains `starting-balance.detector` string at least once — verified by `grep`.
    - `vendor/bin/pest Modules/Import/tests/Unit/*StartingBalanceDetectorTest.php Modules/Import/tests/Feature/StartingBalanceDetectorRegistryTest.php` exits 0 with five test files green.
    - The registry test asserts `$this->app->tagged('starting-balance.detector')` yields exactly four implementations in the documented order.
    - The PayPal unit test asserts `detect([...], $user)` returns `[]` (empty list) for any non-empty input — locks Pitfall 3 in place.
    - `vendor/bin/phpstan analyse --memory-limit=2G Modules/Import` exits 0 at Larastan L10 strict.
    - `vendor/bin/pint --test Modules/Import` exits 0.
    - No grep hit for `G1|UAT|Phase 16.1.1|.planning|D-03|D-04` in any file under `Modules/Import/`.
  </acceptance_criteria>
  <done>Contract + DTO + four detectors + service-provider tag + four unit tests + one registry test in place and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DetectStartingBalancesQuery aggregator + D-04 conflict resolution + ImportServiceProvider singleton binding + DetectStartingBalancesQueryConflictTest</name>
  <files>Modules/Import/Public/Services/DetectStartingBalancesQuery.php, Modules/Import/Providers/ImportServiceProvider.php, Modules/Import/tests/Unit/DetectStartingBalancesQueryConflictTest.php</files>
  <read_first>
    - Modules/Import/Internal/Pipeline/Stages/PaymentTypeClassifierStage.php (PATTERNS.md Pattern 6 — aggregator over iterable<Hinter>; same walk-and-rank shape)
    - The four detector files created in Task 1
    - Modules/Import/Providers/ImportServiceProvider.php (the FQN-list + tag-loop added by Task 1)
    - The DetectsStartingBalance contract + StartingBalanceCandidate DTO created in Task 1
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-CONTEXT.md D-04 (the conflict-resolution rule: "earliest opening_balance_date wins; if same date, prefer canonical CAMT.053 over MT940; if still tied, surface both")
    - .planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-PATTERNS.md Pattern 6 + Pattern 8
  </read_first>
  <behavior>
    - **DetectStartingBalancesQuery** in `Modules/Import/Public/Services/`: `final readonly class` with constructor `public function __construct(private DatabaseManager $db, private iterable $detectors)`. PHPDoc on the constructor annotates `iterable<DetectsStartingBalance>`.
    - One public method: `collect(array $importRunIds, User $user): array` returning `list<StartingBalanceCandidate>` — one candidate per detected account, applying D-04 conflict resolution.
    - **D-04 conflict resolution semantics**: For each accountId across the union of detector outputs, pick the winner by:
      1. Earliest `openingBalanceDate` wins.
      2. If two candidates have the same date, prefer `sourceFormat === 'asn-camt053'` over `'asn-mt940'`.
      3. If still tied (e.g., two CAMT.053 imports of overlapping periods), return BOTH candidates for that account — the UI in Plan 07b renders the conflict-resolution card variant.
    - Two more derived helpers (optional, internal): `groupByAccount(list<StartingBalanceCandidate> $all): array<int, list<StartingBalanceCandidate>>` and `pickWinner(list<StartingBalanceCandidate> $forAccount): list<StartingBalanceCandidate>` (returns 1 or 2 candidates).
    - The aggregator walks the four detectors, calls `detect()` on each with the FULL list of importRunIds (each detector's `supports()` + `where(source_format, ...)` filter already restricts internally). The aggregator unions all returned candidates, then applies the conflict resolution.
    - **ImportServiceProvider** gains the singleton binding for `DetectStartingBalancesQuery::class`: resolves the tagged iterable from `$app->tagged('starting-balance.detector')` and passes it + the DatabaseManager into the constructor (mirrors PaymentTypeClassifierStage's singleton binding in the same provider — PATTERNS.md Pattern 5 / 6).
    - **DetectStartingBalancesQueryConflictTest** (Pest Unit test): four `it(...)` blocks:
      1. `it('returns one candidate per account when only one detector fires')` — seeds CAMT-only data for one account; expects 1 candidate.
      2. `it('prefers earliest opening_balance_date when two detectors fire for the same account')` — seeds CAMT (2026-01-01) + MT940 (2026-02-01) for the same account; expects the CAMT candidate (earliest date).
      3. `it('breaks ties on the same date in favour of CAMT.053 over MT940')` — seeds CAMT + MT940 with identical `opening_balance_date`; expects the CAMT candidate.
      4. `it('surfaces both candidates when CAMT and MT940 disagree on the same date with different values')` — same date, different `opening_balance_minor`, both CAMT.053-sourced — expects two candidates returned in the list.
  </behavior>
  <action>
    Implement the aggregator per PATTERNS.md Pattern 6 and add the singleton binding per Pattern 5. Concretely:
    1. Create `Modules/Import/Public/Services/DetectStartingBalancesQuery.php` as a `final readonly class` with the constructor + `collect()` method per `<behavior>`. The class lives in `Public/Services/` (per PATTERNS.md Pattern 8 / RESEARCH.md Hook 4 — `Public/Services/{Name}Query.php` is the project convention; there is no `Public/Queries/` directory).
    2. In `collect()`: iterate `$this->detectors`, call `detect($importRunIds, $user)` on each, flatten into a single `list<StartingBalanceCandidate>`, group by `accountId`, apply the D-04 conflict-resolution ordering to each group, and union the picked winners into the return.
    3. Open `Modules/Import/Providers/ImportServiceProvider.php`. After the tag-loop added in Task 1, add a singleton binding for `DetectStartingBalancesQuery::class`. The closure resolves `iterator_to_array($app->tagged('starting-balance.detector'))` into `$detectors` and the `DatabaseManager` into `$db`, returning `new DetectStartingBalancesQuery($db, $detectors)`. Match the existing PaymentTypeClassifierStage singleton-binding style.
    4. Create `Modules/Import/tests/Unit/DetectStartingBalancesQueryConflictTest.php` with the four `it(...)` blocks per `<behavior>`. Each test seeds `accounts` + `import_runs` + `statement_summaries` rows directly via Eloquent (the Account model + StatementSummary model already exist; use them) and asserts the returned candidate list shape.
    5. PHPDoc on `DetectStartingBalancesQuery::collect()` describes the D-04 conflict-resolution rule in plain English — no D-04 / G1 references.
    6. Tag the test group as `phase-16.1.1`.
    7. Run Pint + Larastan + the test.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Import/tests/Unit/DetectStartingBalancesQueryConflictTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `Modules/Import/Public/Services/DetectStartingBalancesQuery.php` exists with a `final readonly class`, the constructor pair, and a `collect()` method returning `list<StartingBalanceCandidate>`.
    - `Modules/Import/Providers/ImportServiceProvider.php` contains both `'starting-balance.detector'` (the tag string, from Task 1) AND a `singleton(DetectStartingBalancesQuery::class` binding (added in this task) — verified by `grep`.
    - `vendor/bin/pest Modules/Import/tests/Unit/DetectStartingBalancesQueryConflictTest.php` exits 0.
    - The test file contains exactly four `it(` blocks.
    - The fourth test asserts the returned list contains exactly two candidates for the conflict account.
    - `vendor/bin/phpstan analyse --memory-limit=2G Modules/Import` exits 0 at Larastan L10 strict.
    - `vendor/bin/pint --test Modules/Import` exits 0.
    - No grep hit for GSD-leakage strings in `Modules/Import/`.
  </acceptance_criteria>
  <done>Aggregator + binding + four-assertion conflict test all in place and green; Plan 07 can now consume `DetectStartingBalancesQuery::collect()`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `DetectStartingBalancesQuery::collect()` → SQLite | Per-user-scoped reads against `statement_summaries` + `import_runs` + `accounts`. |
| `$app->tagged('starting-balance.detector')` → caller | Trusted container resolution; the FQN list lives in `ImportServiceProvider`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.1.1-12 | Information disclosure | Detector queries leak another user's `statement_summaries` via `whereIn('import_run_id', ...)` if the IDs list isn't user-scoped first | mitigate | Every detector's `detect()` query carries an explicit `->where('statement_summaries.user_id', $user->id)` AND `->where('import_runs.user_id', $user->id)` filter (per PATTERNS.md Shared Pattern B). Pest unit tests verify a stashed import run id from user B is invisible to user A. |
| T-16.1.1-15 | Tampering | The `STARTING_BALANCE_DETECTOR_FQNS` list could be tampered with to swap in a malicious detector | accept | Same trust model as `PAYMENT_TYPE_HINTER_FQNS` shipped in 16.1. The const list lives in `ImportServiceProvider` which is itself part of the trusted source tree; arch tests verify module boundaries. No new attack vector. |

No HIGH-severity threats. Detector/aggregator plane reads only; UPDATEs happen in Plan 07's commit() inside a per-user-scoped transaction.
</threat_model>

<verification>
- `vendor/bin/pest --group=phase-16.1.1 Modules/Import/tests` returns 0.
- `vendor/bin/pint --test Modules/Import` returns 0.
- `vendor/bin/phpstan analyse --memory-limit=2G Modules/Import` returns 0.
- `php artisan tinker` smoke (one-time, manual): `app(\Modules\Import\Public\Services\DetectStartingBalancesQuery::class)` resolves a fully-wired aggregator.
</verification>

<success_criteria>
- Four `DetectsStartingBalance` impls are tagged under `'starting-balance.detector'` and discoverable.
- `DetectStartingBalancesQuery::collect()` aggregates per-source candidates and applies D-04 conflict resolution.
- PayPal detector returns an empty list (no Saldo-as-balance regression).
- Six new tests (four detector unit tests, one registry feature test, one aggregator conflict test) — all green.
- All CI gates green: Pint, Larastan L10 strict, Pest on the phase-16.1.1 group.
- No GSD-leakage strings in source.
</success_criteria>

<output>
Create `.planning/phases/16.1.1-starting-balance-setup-bank-format-driven-wizard-copy-ics-pd/16.1.1-05b-SUMMARY.md` when done.
</output>
