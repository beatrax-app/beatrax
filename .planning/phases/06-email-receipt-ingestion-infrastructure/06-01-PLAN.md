---
phase: 06-email-receipt-ingestion-infrastructure
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Modules/EmailScan/composer.json
  - Modules/EmailScan/Providers/EmailScanServiceProvider.php
  - Modules/EmailScan/Routes/web.php
  - Modules/EmailScan/tests/TestCase.php
  - Modules/EmailScan/tests/Pest.php
  - Modules/EmailScan/tests/fixtures/eml/paypal/sample-receipt.eml
  - Modules/EmailScan/tests/fixtures/eml/ics/sample-statement-notice.eml
  - Modules/EmailScan/tests/fixtures/eml/googleplay/sample-purchase.eml
  - Modules/EmailScan/tests/fixtures/api-responses/gmail/messages-list-page-1.json
  - Modules/EmailScan/tests/fixtures/api-responses/gmail/messages-get-raw.json
  - Modules/EmailScan/tests/fixtures/api-responses/graph/messages-page-1.json
  - Modules/EmailScan/tests/fixtures/api-responses/graph/delta-baseline.json
  - Modules/EmailScan/tests/fixtures/scenario.md
  - Modules/EmailScan/Internal/Clients/FakeGmailApiClient.php
  - Modules/EmailScan/Internal/Clients/FakeGraphApiClient.php
  - composer.json
  - phpunit.xml
  - tests/Pest.php
  - bootstrap/providers.php
  - tests/Contracts/BoundaryArchTest.php
  - tests/Contracts/NoExtImapTest.php
autonomous: true
requirements:
  - PLT-05
  - EML-03
user_setup: []

must_haves:
  truths:
    - "Modules/EmailScan/ module skeleton exists and is registered as a service provider"
    - "Per-module Pest TestCase is bound in tests/Pest.php foreach map"
    - "Composer audit invariant blocks any future ext-imap / webklex regression"
    - "BoundaryArchTest extension enforces no transactions-table writes from Modules/EmailScan"
    - "Synthesised .eml fixtures + Fake API clients exist so future plans can exercise the pipeline without real OAuth"
    - "D-120: known_senders system seed list (paypal.com / @ics.nl / googleplay-noreply@google.com) is the source of the three fixture senders synthesised in this plan"
    - "D-129: Modules/EmailScan/ bounded module skeleton (composer.json + ServiceProvider + Public/Internal split) follows the Phase 5 Modules/Chains/ shape"
    - "D-132: Phase 6/Phase 7 boundary — BoundaryArchTest::noTransactionWritesFromEmailScan asserts no Modules/EmailScan/ file writes the transactions table"
    - "D-140: synthesised .eml fixtures + FakeGmailApiClient / FakeGraphApiClient injection seam ship in Wave 0 so all downstream plans test without real OAuth"
  artifacts:
    - path: "Modules/EmailScan/composer.json"
      provides: "Module manifest with PSR-4 autoload + autoload-dev"
      contains: "diederik/email-scan"
    - path: "Modules/EmailScan/Providers/EmailScanServiceProvider.php"
      provides: "Minimal service provider — registered in bootstrap/providers.php"
      contains: "class EmailScanServiceProvider"
    - path: "Modules/EmailScan/tests/TestCase.php"
      provides: "Module TestCase shell extending root Tests\\TestCase"
    - path: "Modules/EmailScan/Internal/Clients/FakeGmailApiClient.php"
      provides: "Fixture-driven fake Gmail client implementing the GmailApiClient contract"
    - path: "Modules/EmailScan/Internal/Clients/FakeGraphApiClient.php"
      provides: "Fixture-driven fake Graph client implementing the GraphApiClient contract"
    - path: "tests/Contracts/BoundaryArchTest.php"
      provides: "Module containment + facade carve-out + noTransactionWritesFromEmailScan rule"
      contains: "Modules\\\\EmailScan\\\\Internal"
    - path: "tests/Contracts/NoExtImapTest.php"
      provides: "ext-imap + webklex package conflict audit (composer.json conflict block + composer.lock grep)"
      contains: "webklex"
    - path: "composer.json"
      provides: "conflict block locks out webklex/laravel-imap, webklex/php-imap, ddeboer/imap"
      contains: "conflict"
  key_links:
    - from: "bootstrap/providers.php"
      to: "Modules\\EmailScan\\Providers\\EmailScanServiceProvider"
      via: "return [ ... ::class ]"
      pattern: "EmailScanServiceProvider::class"
    - from: "tests/Pest.php"
      to: "Modules\\EmailScan\\Tests\\TestCase"
      via: "foreach map entry"
      pattern: "Modules/EmailScan.*TestCase"
    - from: "composer.json"
      to: "Modules\\EmailScan\\Tests\\"
      via: "autoload-dev psr-4"
      pattern: "Modules\\\\\\\\EmailScan\\\\\\\\Tests"
    - from: "phpunit.xml"
      to: "Modules/EmailScan/tests/{Unit,Feature,Contracts,Integration}"
      via: "testsuite directory entries"
      pattern: "Modules/EmailScan/tests"
---

<objective>
Wave 0 — module bootstrap + synthesised fixtures + boundary invariants. Lays down the empty Modules/EmailScan/ shell, registers it with Laravel, wires it into the Pest + phpunit + composer autoload triad, and adds the PLT-05 belt-and-braces conflict block + audit test. Creates the synthesised .eml fixture trio (PayPal / ICS / Google Play) + Fake API clients (Gmail / Graph) that all downstream plans use to drive the pipeline in tests. No production behaviour ships; the success criterion is a green test suite with the module present + a red-baseline fixture set ready for later plans to drive the ingest pipeline.

Purpose: Phase 5 D-107 + Phase 4 D-58 precedent — synthesised fixtures + boundary tests must land BEFORE any production class, so all downstream tasks have green-CI guard rails and a stable corpus to exercise the .eml-then-DB ordering and OAuth callback paths. Without this plan, the executor would write production code without anywhere to anchor its tests.

Output: Module skeleton + Fake API clients + 3 anonymised .eml fixtures + JSON API-response fixtures + BoundaryArchTest extension + composer.json conflict block + NoExtImapTest extension. Full test suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-email-receipt-ingestion-infrastructure/06-CONTEXT.md
@.planning/phases/06-email-receipt-ingestion-infrastructure/06-RESEARCH.md
@.planning/phases/06-email-receipt-ingestion-infrastructure/06-PATTERNS.md
@.planning/phases/06-email-receipt-ingestion-infrastructure/06-VALIDATION.md

@Modules/Chains/composer.json
@Modules/Chains/tests/Pest.php
@Modules/Chains/tests/TestCase.php
@Modules/Chains/Providers/ChainsServiceProvider.php
@Modules/Chains/Routes/web.php
@tests/Pest.php
@phpunit.xml
@composer.json
@bootstrap/providers.php
@tests/Contracts/BoundaryArchTest.php
@tests/Contracts/NoExtImapTest.php
</context>

<interfaces>
<!-- Key signatures the executor needs for the Fake clients. The real interfaces don't exist yet —
     this plan defines the contracts that real GmailApiClient + GraphApiClient (Plan 03 + 05 + 06)
     will implement. Treat the Fake clients' public methods AS the contract. -->

FakeGmailApiClient public methods (Phase 6 contract):
  - listSenderMessages(int $inboxId, array $senderPatterns, ?string $pageToken): array { messages, nextPageToken, historyId }
  - getRawMessage(int $inboxId, string $providerMessageId): string  // base64url-decoded RFC 822 .eml byte stream
  - listHistory(int $inboxId, string $startHistoryId): array { history, historyId }  // throws CursorExpiredException on 404
  - listDiscoveryCandidates(int $inboxId, array $keywords, array $excludeSenders): array { messages, nextPageToken }

FakeGraphApiClient public methods (Phase 6 contract):
  - listSenderMessagesPaged(int $inboxId, array $senderPatterns, \DateTimeImmutable $windowStart, ?string $nextLink): array { messages, nextLink }
  - getRawMessage(int $inboxId, string $providerMessageId): string
  - deltaPage(int $inboxId, ?string $deltaLink): array { messages, deltaLink, nextLink }  // throws CursorExpiredException on 410
  - listDiscoveryCandidatesPaged(int $inboxId, array $keywords, array $excludeSenders, ?string $nextLink): array

CursorExpiredException — sentinel exception thrown by both Fake clients when fixture file simulates 404 (Gmail) / 410 (Graph).
  Lives at: Modules/EmailScan/Internal/Clients/CursorExpiredException.php

All Fake methods read JSON fixtures from Modules/EmailScan/tests/fixtures/api-responses/{gmail,graph}/*.json keyed by the call shape.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer → composer registry | npm/composer installs add code to vendor/ — slopcheck audit must remain green |
| Modules/EmailScan/ → transactions table | Hard architectural barrier — Phase 6 must never write to transactions (D-132 / Phase 7 boundary) |
| composer.lock → CI | Regression of ext-imap / webklex must hard-fail at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | composer install | mitigate | composer.json "conflict" block + tests/Contracts/NoExtImapTest.php asserts no `ext-imap` / `webklex/*` in composer.lock |
| T-06-01-SC | Tampering | composer install of Phase 6 SDKs (deferred to Plan 03 + 05) | mitigate | RESEARCH.md § Package Legitimacy Audit verified all 6 packages slopcheck-clean on 2026-05-16; later plans installing them get checkpoint coverage if needed |
| T-06-01-02 | Elevation of privilege | Modules/EmailScan/ → transactions table | mitigate | tests/Contracts/BoundaryArchTest.php `noTransactionWritesFromEmailScan` arch rule — fails on any file in Modules/EmailScan calling Transaction::query / Transaction::create / `->table('transactions')->insert|update|delete` |
| T-06-01-03 | Information disclosure | Fake API client fixture data | accept | Synthesised fixtures contain anonymised receipt data only (no real PII); same posture as Phase 5 D-107 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Modules/EmailScan/ module skeleton + composer + Pest + autoload registration</name>
  <files>
    Modules/EmailScan/composer.json,
    Modules/EmailScan/Providers/EmailScanServiceProvider.php,
    Modules/EmailScan/Routes/web.php,
    Modules/EmailScan/tests/TestCase.php,
    Modules/EmailScan/tests/Pest.php,
    composer.json,
    phpunit.xml,
    tests/Pest.php,
    bootstrap/providers.php
  </files>
  <read_first>
    Modules/Chains/composer.json (full file — 13 lines; verbatim shape to copy with diederik/email-scan + Modules\\EmailScan namespace),
    Modules/Chains/Providers/ChainsServiceProvider.php (lines 1-110 — see register/boot structure; Phase 6 minimal provider only loads migrations + routes + views + registers Livewire components later),
    Modules/Chains/Routes/web.php (lines 1-25 — full file; one Route::middleware group),
    Modules/Chains/tests/TestCase.php (full file — 13 lines; abstract class extending Tests\TestCase),
    Modules/Chains/tests/Pest.php (full file; inert pattern — root tests/Pest.php is the load-bearing wire-up),
    composer.json (lines 41-56 — autoload-dev psr-4 map; add Modules\\EmailScan\\Tests\\),
    phpunit.xml (lines 1-46 — testsuite block — add EmailScanUnit / EmailScanFeature / EmailScanContracts / EmailScanIntegration entries mirroring the Chains pattern at lines 28-37),
    tests/Pest.php (lines 1-60 — foreach map; add 'Modules/EmailScan' => Modules\EmailScan\Tests\TestCase::class entry),
    bootstrap/providers.php (full file — 24 lines; insert EmailScanServiceProvider entry alphabetically),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-PATTERNS.md (§"New module scaffold" + §"Modules/EmailScan/composer.json (module manifest)" + §"Modules/EmailScan/Providers/EmailScanServiceProvider.php")
  </read_first>
  <action>
    Create Modules/EmailScan/composer.json — verbatim shape from Modules/Chains/composer.json with name="diederik/email-scan", description="EmailScan module — Gmail + Microsoft Graph OAuth ingestion, raw .eml persistence, resumable scan cursors.", PSR-4 namespace Modules\\EmailScan\\, dev-namespace Modules\\EmailScan\\Tests\\ pointing at tests/.

    Create Modules/EmailScan/Providers/EmailScanServiceProvider.php as `final class EmailScanServiceProvider extends Illuminate\Support\ServiceProvider`. register() is empty in Wave 0 (singletons land in later plans). boot(LivewireManager $livewire) loads migrations from Database/Migrations if dir exists, routes from Routes/web.php if file exists, views from Resources/views with namespace 'email-scan' if dir exists. No Livewire component registration yet — components land in Plan 03+. No View Factory composer yet — lands in Plan 07 alongside the top-nav badge.

    Create Modules/EmailScan/Routes/web.php with single Route::middleware(['web','auth'])->group(...) — empty group body in Wave 0 (routes land in Plan 03). Add the same docblock note from Modules/Chains/Routes/web.php that Route::facade is permitted in Routes/ files per BoundaryArchTest exemption.

    Create Modules/EmailScan/tests/TestCase.php as `abstract class TestCase extends Tests\TestCase {}` in namespace Modules\EmailScan\Tests.

    Create Modules/EmailScan/tests/Pest.php as the inert one-liner (`pest()->extend(TestCase::class)->in('Feature')` style, mirroring Modules/Chains/tests/Pest.php — inert because the root tests/Pest.php foreach is load-bearing).

    Extend root composer.json `autoload-dev.psr-4` map with `"Modules\\\\EmailScan\\\\Tests\\\\": "Modules/EmailScan/tests/"` — alphabetical position after Chains, before Transfers (preserve sort order matching the existing block at lines 47-55).

    Extend phpunit.xml `<testsuites>` block with FOUR new testsuites: `EmailScanUnit`, `EmailScanFeature`, `EmailScanContracts`, `EmailScanIntegration`, each pointing at `./Modules/EmailScan/tests/{Unit,Feature,Contracts,Integration}/` with `suffix="Test.php"` — mirror the ChainsUnit/ChainsFeature/ChainsContracts entries at lines 28-37.

    Extend root tests/Pest.php foreach map with `'Modules/EmailScan' => Modules\EmailScan\Tests\TestCase::class` — alphabetical placement between Core and Import. The foreach already binds Feature + Unit + Integration + Contracts directories per the visible pattern; no extra wire-up needed.

    Extend bootstrap/providers.php with `EmailScanServiceProvider::class` — alphabetical insertion between CoreServiceProvider and ImportServiceProvider; add the matching `use Modules\EmailScan\Providers\EmailScanServiceProvider;` import alphabetically in the use block.

    Run `composer dump-autoload` after the file edits so the new PSR-4 namespace resolves before tests run.
  </action>
  <verify>
    <automated>vendor/bin/pest --filter=EmailScan 2>&1 | head -20</automated>
    Test that `composer dump-autoload -o 2>&1 | tail -5` succeeds with no class-not-found.
    Test that `php -r "require 'vendor/autoload.php'; new ReflectionClass(Modules\EmailScan\Providers\EmailScanServiceProvider::class);"` exits 0.
    Test that `php artisan tinker --execute='echo class_exists(Modules\EmailScan\Tests\TestCase::class) ? 1 : 0;'` echoes `1`.
  </verify>
  <done>
    Modules/EmailScan/ directory exists with composer.json + Providers/ + Routes/ + tests/. EmailScanServiceProvider boots from bootstrap/providers.php without errors. `composer dump-autoload -o` resolves the new namespaces. phpunit.xml + tests/Pest.php both reference the new module. Pest runs with `--filter=EmailScan` and reports "no tests found" rather than autoload / class-not-found errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Synthesise .eml + API-response fixtures + Fake Gmail + Fake Graph clients</name>
  <files>
    Modules/EmailScan/tests/fixtures/eml/paypal/sample-receipt.eml,
    Modules/EmailScan/tests/fixtures/eml/ics/sample-statement-notice.eml,
    Modules/EmailScan/tests/fixtures/eml/googleplay/sample-purchase.eml,
    Modules/EmailScan/tests/fixtures/api-responses/gmail/messages-list-page-1.json,
    Modules/EmailScan/tests/fixtures/api-responses/gmail/messages-list-page-2-empty.json,
    Modules/EmailScan/tests/fixtures/api-responses/gmail/messages-get-raw-paypal.json,
    Modules/EmailScan/tests/fixtures/api-responses/gmail/messages-get-raw-ics.json,
    Modules/EmailScan/tests/fixtures/api-responses/gmail/messages-get-raw-googleplay.json,
    Modules/EmailScan/tests/fixtures/api-responses/gmail/history-list-404.json,
    Modules/EmailScan/tests/fixtures/api-responses/gmail/rate-limit-403.json,
    Modules/EmailScan/tests/fixtures/api-responses/graph/messages-page-1.json,
    Modules/EmailScan/tests/fixtures/api-responses/graph/messages-page-2-empty.json,
    Modules/EmailScan/tests/fixtures/api-responses/graph/delta-baseline.json,
    Modules/EmailScan/tests/fixtures/api-responses/graph/delta-410.json,
    Modules/EmailScan/tests/fixtures/api-responses/graph/throttle-429.json,
    Modules/EmailScan/tests/fixtures/scenario.md,
    Modules/EmailScan/Internal/Clients/CursorExpiredException.php,
    Modules/EmailScan/Internal/Clients/FakeGmailApiClient.php,
    Modules/EmailScan/Internal/Clients/FakeGraphApiClient.php
  </files>
  <read_first>
    Modules/Chains/tests/fixtures/scenario-1/ (whole dir — synthesised cross-source corpus; scenario-1.md narrative + expected-state JSONs are the precedent),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-PATTERNS.md (§"Modules/EmailScan/tests/fixtures/eml/{...}/*.eml" + §"Files With No Codebase Analog"),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-RESEARCH.md (Example 3 zbateson header API — drives sender-email + subject extraction the Fakes must surface in JSON shape; Pattern 4 two-phase Graph scan — drives the Graph fixture file split into "backfill list" + "delta baseline"; Pitfall 3 + 4 cursor-expiry — drives history-list-404 + delta-410 fixtures; Pitfall 8 quota cost — drives rate-limit-403 + throttle-429 fixtures),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-CONTEXT.md (D-140 fixture synthesis requirement)
  </read_first>
  <action>
    Synthesise 3 anonymised .eml files. Each is a RFC 822 message with realistic headers + minimal text/plain body. Headers MUST be Q-encoded UTF-8 in at least one of the three subjects (so the zbateson parser is exercised against the spec-compliant edge case). Headers required: From: (with display name + email), Subject:, Date:, Message-ID:, To: a synthetic local user. Body length ≥ 200 bytes each. NEVER use the real user's email; use synthetic addresses (`receipts@example-paypal.test` does not work — providers' From: matches sender domain, so use `service@paypal.com`, `noreply@ics.nl`, `googleplay-noreply@google.com` per CONTEXT.md D-120 system seed list). Names anonymised. Amounts synthetic. NO real reference IDs.
      - paypal/sample-receipt.eml — From: "PayPal" <service@paypal.com>; Subject Q-encoded e.g. "=?UTF-8?Q?Bedankt_voor_je_betaling?="; text/plain body containing fake merchant name + EUR amount.
      - ics/sample-statement-notice.eml — From: "ICS Cards" <noreply@ics.nl>; subject plain ASCII; body fake.
      - googleplay/sample-purchase.eml — From: "Google Play" <googleplay-noreply@google.com>; subject plain ASCII; body fake.

    Synthesise JSON API-response fixtures.
      - gmail/messages-list-page-1.json — shape mirrors Google API real response: {messages: [{id, threadId}], resultSizeEstimate: int, nextPageToken: "..."}. Three message ids matching the three .eml filenames (without extension, e.g. "paypal-sample-receipt").
      - gmail/messages-list-page-2-empty.json — {messages: [], resultSizeEstimate: 0} (no nextPageToken — last page sentinel).
      - gmail/messages-get-raw-{paypal|ics|googleplay}.json — {id: "...", historyId: "12345", internalDate: "1747... (ms since epoch)", raw: "<base64url-encoded contents of corresponding .eml>"} — base64url means tr +/ -> -_ and strip = padding. Generate the raw field by reading the matching .eml at fixture-write time and encoding.
      - gmail/history-list-404.json — { "error": { "code": 404, "message": "Requested entity was not found.", "errors": [ { "reason": "notFound" } ] } } — the Fake client maps reading this fixture to throw CursorExpiredException.
      - gmail/rate-limit-403.json — { "error": { "code": 403, "errors": [ { "reason": "rateLimitExceeded" } ] } }; Fake exposes via a `simulateRateLimit($inboxId)` flag the test toggles.
      - graph/messages-page-1.json — {"@odata.context": "...", "value": [{id, from: {emailAddress: {address, name}}, subject, receivedDateTime, ...}], "@odata.nextLink": "..."}. Three message ids.
      - graph/messages-page-2-empty.json — {"value": [], no nextLink}.
      - graph/delta-baseline.json — {"@odata.deltaLink": "https://graph.microsoft.com/v1.0/.../$delta?token=baseline-xyz", "value": []}.
      - graph/delta-410.json — { "error": { "code": "syncStateNotFound", "message": "The sync state was not found." } } — Fake maps to CursorExpiredException.
      - graph/throttle-429.json — Fake exposes via a `simulateRateLimit($inboxId, int $retryAfterSeconds=2)` flag.

    Write Modules/EmailScan/tests/fixtures/scenario.md narrative — same shape as Modules/Chains/tests/fixtures/scenario-1/scenario-1.md. Document the three fixture senders, what each .eml represents, what the JSON fixtures encode (page boundaries, cursor-expiry trigger, rate-limit trigger), and the expected post-fetch state of `inbox_messages` (3 rows status='fetched' with sender_email matching the .eml From: address, sender_name = display name, subject = Q-decoded subject, internal_date = parsed from JSON internalDate / receivedDateTime).

    Create Modules/EmailScan/Internal/Clients/CursorExpiredException.php — `final class CursorExpiredException extends RuntimeException` (use \RuntimeException). No constructor params beyond optional message + previous. PHPDoc describes the typed-fallback semantic without GSD codes.

    Create Modules/EmailScan/Internal/Clients/FakeGmailApiClient.php — `final class FakeGmailApiClient` (Wave 0: no abstract interface yet; the Fake's public methods ARE the contract — Plan 03 + 05 will introduce the real GmailApiClient with the same method signatures). Constructor takes `private readonly Illuminate\Filesystem\Filesystem $files`. Public methods:
      - listSenderMessages(int $inboxId, array $senderPatterns, ?string $pageToken): array — reads gmail/messages-list-page-1.json on first call (pageToken null), gmail/messages-list-page-2-empty.json on second call. Decodes JSON via $this->files->get + json_decode strict.
      - getRawMessage(int $inboxId, string $providerMessageId): string — reads gmail/messages-get-raw-$providerMessageId.json, base64url-decodes the `raw` field (apply tr +/ -> -_ inverse, add padding) and returns the resulting RFC 822 byte stream. Throws RuntimeException if fixture missing for the id.
      - listHistory(int $inboxId, string $startHistoryId): array — reads gmail/history-list-404.json shape; if status 404 (read JSON to check), throws CursorExpiredException with provider hint "gmail.historyId.expired".
      - listDiscoveryCandidates(int $inboxId, array $keywords, array $excludeSenders): array — returns gmail/messages-list-page-1.json reduced shape; for Wave 0 this can be a stub returning the same list with sender metadata extracted.
      - Public test seam: `simulateRateLimit(int $inboxId, int $retryAfterSeconds = 2): void` — sets internal flag; subsequent listSenderMessages call throws a typed `RateLimitedException` (also create at Modules/EmailScan/Internal/Clients/RateLimitedException.php with `public readonly int $retryAfterSeconds`).

    Create Modules/EmailScan/Internal/Clients/FakeGraphApiClient.php with parallel shape:
      - listSenderMessagesPaged(int $inboxId, array $senderPatterns, \DateTimeImmutable $windowStart, ?string $nextLink): array — reads graph/messages-page-1.json then graph/messages-page-2-empty.json based on $nextLink null vs not.
      - getRawMessage(int $inboxId, string $providerMessageId): string — reads matching .eml verbatim (Graph's /$value endpoint returns raw RFC 822 directly, no base64).
      - deltaPage(int $inboxId, ?string $deltaLink): array — first call returns graph/delta-baseline.json; if the Fake's `simulateCursorExpired($inboxId)` flag was set, throws CursorExpiredException.
      - listDiscoveryCandidatesPaged(...) — stub similar to Gmail.
      - simulateRateLimit($inboxId, $retryAfterSeconds): void — same shape as Gmail Fake.
      - simulateCursorExpired($inboxId): void — sets internal flag.

    Both Fakes also expose a `getRequestedCalls(): array` test-introspection method returning a chronological list of `['method' => str, 'args' => array]` records so tests can assert call ordering.
  </action>
  <verify>
    <automated>vendor/bin/pest --filter=EmailScan 2>&1 | tail -10</automated>
    Test that `php -r "require 'vendor/autoload.php'; \$f = new Modules\EmailScan\Internal\Clients\FakeGmailApiClient(new Illuminate\Filesystem\Filesystem()); var_dump(method_exists(\$f, 'listSenderMessages'));"` echoes `bool(true)`.
    Test that all three .eml fixture files exist: `ls -la Modules/EmailScan/tests/fixtures/eml/*/*.eml | wc -l` returns 3.
    Test that all JSON fixtures parse: `for f in Modules/EmailScan/tests/fixtures/api-responses/*/*.json; do php -r "json_decode(file_get_contents('$f'), null, 512, JSON_THROW_ON_ERROR);" || echo "FAIL: $f"; done` produces no FAIL lines.
    Test that the synthesised PayPal fixture contains the Q-encoded subject byte sequence: `grep -c "=?UTF-8?Q?" Modules/EmailScan/tests/fixtures/eml/paypal/sample-receipt.eml` returns at least 1.
    Test that gmail/messages-get-raw-paypal.json's `raw` field base64url-decodes to bytes starting with "From:" or "Return-Path:" or other RFC 822 header: `php -r "\$j = json_decode(file_get_contents('Modules/EmailScan/tests/fixtures/api-responses/gmail/messages-get-raw-paypal.json'), true); \$d = base64_decode(strtr(\$j['raw'], '-_', '+/') . str_repeat('=', (4 - strlen(\$j['raw']) % 4) % 4)); echo strpos(\$d, 'From:') !== false ? 'OK' : 'FAIL';"` echoes `OK`.
  </verify>
  <done>
    Three .eml fixtures + JSON API-response fixtures exist under Modules/EmailScan/tests/fixtures/. FakeGmailApiClient + FakeGraphApiClient + CursorExpiredException + RateLimitedException classes exist and are autoloadable. PayPal .eml contains a Q-encoded subject. JSON files parse cleanly. Gmail raw fixture base64url-decodes back to the original .eml byte stream. scenario.md describes the corpus' expected post-fetch state.
  </done>
</task>

<task type="auto">
  <name>Task 3: BoundaryArchTest extension + NoExtImapTest extension + composer.json conflict block</name>
  <files>
    composer.json,
    tests/Contracts/BoundaryArchTest.php,
    tests/Contracts/NoExtImapTest.php
  </files>
  <read_first>
    tests/Contracts/BoundaryArchTest.php (full file — every existing arch rule + the noResolverWritesTransactions grep at the lines that match the pattern; Phase 6 mirrors that rule shape verbatim),
    tests/Contracts/NoExtImapTest.php (full file — the existing PLT-05 ext-imap regex; Phase 6 extends with composer.lock webklex / ddeboer grep),
    composer.json (full file — locate where to insert the "conflict" block; goes between "require-dev" and "autoload" alphabetically — or follow whatever shape Composer accepts),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-PATTERNS.md (§"tests/Contracts/BoundaryArchTest.php (extend)" + §"tests/Contracts/PltExtImapAuditTest.php (extend)"),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-RESEARCH.md (§"Standard Stack" lines 199-209 — the composer.json `"conflict"` block recommendation, verbatim shape)
  </read_first>
  <action>
    Extend root composer.json with a top-level `"conflict"` block (object) listing `"webklex/laravel-imap": "*"`, `"webklex/php-imap": "*"`, `"ddeboer/imap": "*"`. Place the block after `"require-dev"` and before `"autoload"` per standard composer ordering. Composer will hard-fail `composer install` / `composer update` if any future change tries to add one of these packages. Run `composer validate --no-check-publish` to confirm composer.json remains valid; if validate complains about ordering, normalise via `composer normalize` is NOT installed — manual edit is fine.

    Extend tests/Contracts/BoundaryArchTest.php with the following four additions, each modelled on existing rules in the same file:

      (a) **Internal containment rule** — add a new `arch(...)->expect('Modules\\EmailScan\\Internal')->toOnlyBeUsedIn('Modules\\EmailScan');` row alongside the existing `Modules\\Chains\\Internal` row.

      (b) **Facade carve-out extension** — append the three new Phase 6 job FQNs to the `->ignoring([...])` list of the existing "no Laravel facade usage in module code" arch rule:
        - `Modules\\EmailScan\\Internal\\Jobs\\BackfillInboxJob`
        - `Modules\\EmailScan\\Internal\\Jobs\\IncrementalScanJob`
        - `Modules\\EmailScan\\Internal\\Jobs\\DiscoveryScanJob`
        (The classes don't exist yet — that's fine; the arch test reads namespace strings, not loaded classes. If the test framework eagerly resolves namespaces, gate the carve-outs with `if (class_exists(...))` per Phase 5 precedent OR move them into Plan 05 / Plan 07 / Plan 08 when each Job class lands. Default: include the strings now — arch test does not require class_exists.)

      (c) **noTransactionWritesFromEmailScan grep rule** — mirror the existing `noResolverWritesTransactions` test (which scopes the grep to `Modules/Chains/Internal/Resolvers/`). Phase 6 mirror scopes to `Modules/EmailScan/` (entire module subtree). Same regex pair: `Transaction::query|Transaction::where` and `->table\(['"]transactions['"]\)[^;]*->(update|insert|delete)\s*\(`. Strip block + line comments before matching (existing pattern: `preg_replace('#/\\*.*?\\*/|//[^\\n]*#s', '', $contents)`). Skip `tests/` subtree. Fail with informative message listing offending file paths. Test name: `noTransactionWritesFromEmailScan`.

      (d) **noOtherInboxScanStateMutator grep rule** — mirror the existing `noOtherCardStatementStateMutator` rule shape (single allowed mutator class). Phase 6's single allowed mutator is `Modules\\EmailScan\\Internal\\InboxScanStateMachine` (lands in Plan 07). Grep pattern: `->table\(['"]inbox_scan_state['"]\)[^;]*->update\s*\(` (any insert is allowed — only the status mutation must funnel through the state machine; the grep can be the same broad shape as the analog and refined later when the column structure stabilises). Allowed paths: `Modules/EmailScan/Internal/InboxScanStateMachine.php` AND all migrations under `Modules/EmailScan/Database/Migrations/` (migrations seed initial rows). Skip `tests/`. Test name: `noOtherInboxScanStateMutator`. **Verify the scope catches both the simple stub (Plan 05) AND the lockForUpdate variant (Plan 07)** — the grep must remain green after Plan 07 replaces the stub.

      (e) **noOAuthTokensInEmailScanSchema grep rule** — PLT-03 baseline invariant: no OAuth token / client_secret / access_token column ever lands in any Phase 6 DB migration. Iterate every file under `Modules/EmailScan/Database/Migrations/`; for each migration, read contents, strip block + line comments (same `preg_replace('#/\\*.*?\\*/|//[^\\n]*#s', ...)` pattern as the other rules), then grep for `/refresh_token|client_secret|access_token/i`. Any hit is a fail with a clear message naming the offending column + file. Test name: `noOAuthTokensInEmailScanSchema`. This rule complements OAuthSecretsRepository's chmod-600 JSON contract (Plan 02) — secrets live ONLY in `storage/app/secrets/email-oauth.json`, NEVER in the database. The rule is trivially satisfied on the current tree (no migrations exist yet) and will continue to pass after Plan 02 lands the schema, provided no migration accidentally adds a token column.

    Extend tests/Contracts/NoExtImapTest.php — add a new test or extend the existing `it(...)` block to also grep `composer.lock` for the strings `"webklex/laravel-imap"`, `"webklex/php-imap"`, `"ddeboer/imap"`. Each occurrence is a fail with a clear message naming the offending package. Combine with the existing `ext-imap` grep so a single test asserts the consolidated invariant. Skip the test gracefully when composer.lock does not exist (CI environments running fresh — same skip predicate pattern as Phase 5 D-101 HorizonBootsTest).

    DO NOT add D-numbers / REQ-IDs / `.planning/` references anywhere in the new tests or the composer.json — describe rationale in plain technical language per CLAUDE.md (Phase 5 D-101 lesson).
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/BoundaryArchTest.php tests/Contracts/NoExtImapTest.php 2>&1 | tail -20</automated>
    Test that `composer validate --no-check-publish 2>&1 | tail -5` exits 0 and reports no errors.
    Test that the conflict block parses: `php -r "\$j = json_decode(file_get_contents('composer.json'), true); echo isset(\$j['conflict']['webklex/laravel-imap']) ? 'OK' : 'FAIL';"` echoes `OK`.
    Failure-mode probe: temporarily create `Modules/EmailScan/Internal/Hack.php` with the line `Transaction::create([])`; re-run the boundary test. The test MUST fail with a message naming the sentinel file. Then delete the sentinel and re-run. Same failure-mode probe for the InboxScanStateMachine mutator rule with a temporary `->table('inbox_scan_state')->update([])` call in a non-allowed file. Do not commit the sentinel files.
  </verify>
  <done>
    composer.json has top-level `conflict` block listing the three IMAP packages. tests/Contracts/BoundaryArchTest.php contains four new assertions (containment, facade carve-out extension, noTransactionWritesFromEmailScan, noOtherInboxScanStateMutator). tests/Contracts/NoExtImapTest.php asserts both ext-imap absence AND webklex/ddeboer absence in composer.lock. All four added assertions pass on the current tree. Failure-mode probe with a sentinel write was verified during execution (sentinel deleted before commit).
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

```
composer validate --no-check-publish
composer dump-autoload -o
vendor/bin/pest --filter=EmailScan
vendor/bin/pest tests/Contracts/BoundaryArchTest.php tests/Contracts/NoExtImapTest.php
vendor/bin/phpstan analyse --memory-limit=1G 2>&1 | tail -5
```

All commands must exit 0.
</verification>

<success_criteria>
- [ ] Modules/EmailScan/ exists with composer.json + ServiceProvider + Routes/ + tests/
- [ ] EmailScanServiceProvider boots via bootstrap/providers.php (Laravel does not error on app boot)
- [ ] Root composer.json includes `Modules\\EmailScan\\Tests\\` autoload-dev row AND a top-level `conflict` block locking out webklex + ddeboer
- [ ] phpunit.xml exposes EmailScanUnit/Feature/Contracts/Integration testsuites
- [ ] tests/Pest.php foreach map includes Modules/EmailScan row
- [ ] FakeGmailApiClient + FakeGraphApiClient + CursorExpiredException + RateLimitedException exist and are autoloadable
- [ ] 3 anonymised .eml fixtures + 10+ JSON API-response fixtures + scenario.md exist
- [ ] Q-encoded subject is present in at least one .eml fixture (zbateson exercise)
- [ ] tests/Contracts/BoundaryArchTest.php asserts noTransactionWritesFromEmailScan + Internal containment + facade carve-outs + noOtherInboxScanStateMutator + noOAuthTokensInEmailScanSchema
- [ ] tests/Contracts/NoExtImapTest.php asserts composer.lock contains no webklex/ddeboer occurrences
- [ ] Failure-mode probe on noTransactionWritesFromEmailScan caught a deliberate violation (sentinel deleted)
- [ ] `composer validate --no-check-publish` exits 0
- [ ] Full Pest suite + Larastan stay green
</success_criteria>

<output>
Write summary to `.planning/phases/06-email-receipt-ingestion-infrastructure/06-01-SUMMARY.md` covering:
- Module scaffold files created (verbatim path list)
- Whether the synthesised Q-encoded subject string survived round-trip through `zbateson/mail-mime-parser` if you exercise it in a smoke test (not required; mention if attempted)
- Whether the failure-mode probe on noTransactionWritesFromEmailScan caught the sentinel (yes/no + how the sentinel looked)
- Whether composer validate had to be re-run after the conflict block insertion (some shells re-order keys)
- Any deviation from PATTERNS.md analog file structure (e.g., if you needed to register Livewire components in boot() earlier than planned)
</output>
