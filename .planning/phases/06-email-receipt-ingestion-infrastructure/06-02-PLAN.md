---
phase: 06-email-receipt-ingestion-infrastructure
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - Modules/EmailScan/Database/Migrations/2026_05_16_020001_create_inboxes_table.php
  - Modules/EmailScan/Database/Migrations/2026_05_16_020002_create_inbox_scan_state_table.php
  - Modules/EmailScan/Database/Migrations/2026_05_16_020003_create_inbox_messages_table.php
  - Modules/EmailScan/Database/Migrations/2026_05_16_020004_create_known_senders_table.php
  - Modules/EmailScan/Database/Migrations/2026_05_16_020005_create_discovered_senders_table.php
  - Modules/EmailScan/Models/Inbox.php
  - Modules/EmailScan/Models/InboxScanState.php
  - Modules/EmailScan/Models/InboxMessage.php
  - Modules/EmailScan/Models/KnownSender.php
  - Modules/EmailScan/Models/DiscoveredSender.php
  - Modules/EmailScan/Public/Dto/InboxHealthDto.php
  - Modules/EmailScan/Public/Dto/KnownSenderDto.php
  - Modules/EmailScan/Public/Dto/InboxMessageDto.php
  - Modules/EmailScan/Public/Dto/InboxCredentials.php
  - Modules/EmailScan/Public/Dto/ScanCursor.php
  - Modules/EmailScan/Public/Dto/EmailScanHealthTile.php
  - Modules/EmailScan/Public/Services/OAuthSecretsRepository.php
  - Modules/EmailScan/Public/Services/InboxMessageQuery.php
  - Modules/EmailScan/tests/Unit/Services/InboxMessageQueryTest.php
  - Modules/EmailScan/tests/Integration/MigrationsTest.php
  - Modules/EmailScan/tests/Integration/ReFetchIdempotentTest.php
  - Modules/EmailScan/tests/Unit/Dto/ScanCursorTest.php
  - Modules/EmailScan/tests/Unit/Services/OAuthSecretsRepositoryTest.php
  - Modules/EmailScan/tests/Unit/Services/OAuthSecretsAtomicRotationTest.php
  - Modules/EmailScan/tests/Unit/Services/OAuthSecretsDirModeTest.php
  - tests/Contracts/UserIdColumnArchTest.php
autonomous: true
requirements:
  - EML-03
  - EML-06
  - PLT-03
user_setup: []

must_haves:
  truths:
    - "Five user-scoped tables exist (inboxes, inbox_scan_state, inbox_messages, known_senders, discovered_senders) with FND-03 nullable user_id + cascade-on-delete"
    - "known_senders is seeded with 3 system rows (paypal.com, @ics.nl, googleplay-noreply@google.com) carrying user_id=NULL + source=system"
    - "inbox_messages.UNIQUE (inbox_id, provider_message_id) makes re-fetch idempotent"
    - "ScanCursor value object normalises Gmail historyId + Graph deltaLink behind one type"
    - "OAuthSecretsRepository writes storage/app/secrets/email-oauth.json with chmod 0600 via atomic tmp+fsync+rename"
    - "Parent dir storage/app/secrets/ is created with chmod 0700 on first write"
    - "Failed mid-write leaves the prior file content intact (atomic rotation crash-safe)"
    - "OAuth client_id + client_secret + refresh_token never land in any DB column (PLT-03 invariant)"
    - "D-112: Single chmod-600 JSON at storage/app/secrets/email-oauth.json holds provider client credentials + per-inbox refresh tokens; OAuthSecretsRepository is the only Public service that touches the file"
    - "D-117: inbox_messages table is the Phase 6/Phase 7 handoff contract; status enum 'fetched' is the entry value Phase 7 reads; UNIQUE (inbox_id, provider_message_id) makes re-fetch idempotent"
    - "D-120: known_senders + discovered_senders tables both user-scoped per FND-03; known_senders seeded with the three system rows; discovered_senders state enum (candidate / added / dismissed) drives the promotion loop"
    - "D-129: Public surface ships InboxQuery + KnownSenderQuery + InboxMessageQuery + OAuthSecretsRepository + all DTOs (InboxHealthDto, KnownSenderDto, InboxMessageDto, InboxCredentials, ScanCursor) so Phase 7 can consume the module on day one"
  artifacts:
    - path: "Modules/EmailScan/Database/Migrations/2026_05_16_020001_create_inboxes_table.php"
      provides: "inboxes table (id, user_id, provider, email, backfill_window_months, backfill_progress JSON, created_at, updated_at) + BEFORE INSERT/UPDATE trigger pair on provider column"
    - path: "Modules/EmailScan/Database/Migrations/2026_05_16_020002_create_inbox_scan_state_table.php"
      provides: "inbox_scan_state table (id, user_id, inbox_id, folder, last_history_id, last_delta_link, last_scan_at, status, error_message, retry_attempts, ...) + UNIQUE (inbox_id, folder) + status enum trigger pair"
    - path: "Modules/EmailScan/Database/Migrations/2026_05_16_020003_create_inbox_messages_table.php"
      provides: "inbox_messages table (id, user_id, inbox_id, provider_message_id, internal_date, sender_email, sender_name nullable, subject nullable, status, fetched_at, ...) + UNIQUE (inbox_id, provider_message_id) + status enum trigger pair"
    - path: "Modules/EmailScan/Database/Migrations/2026_05_16_020004_create_known_senders_table.php"
      provides: "known_senders table + system seeds for PayPal + ICS + Google Play (user_id=NULL, source='system')"
    - path: "Modules/EmailScan/Database/Migrations/2026_05_16_020005_create_discovered_senders_table.php"
      provides: "discovered_senders table + UNIQUE (user_id, inbox_id, sender_email) + state enum trigger pair"
    - path: "Modules/EmailScan/Public/Services/OAuthSecretsRepository.php"
      provides: "Atomic chmod-600 JSON repository (load/save/rotate) — only DI surface touching storage/app/secrets/email-oauth.json"
      contains: "writeAtomic"
    - path: "Modules/EmailScan/Public/Dto/ScanCursor.php"
      provides: "Final readonly value object normalising Gmail historyId + Graph deltaLink"
      contains: "public static function gmail"
    - path: "Modules/EmailScan/Public/Services/InboxMessageQuery.php"
      provides: "Public read-side surface — forStatus(string): iterable<InboxMessageDto>; chunked iteration via cursor()/lazy() so Phase 7 can stream status='fetched' rows without loading the whole set"
      contains: "forStatus"
  key_links:
    - from: "Modules/EmailScan/Database/Migrations/2026_05_16_020003_create_inbox_messages_table.php"
      to: "inbox_messages.UNIQUE(inbox_id, provider_message_id)"
      via: "Blueprint $table->unique(['inbox_id','provider_message_id'])"
      pattern: "unique.*inbox_id.*provider_message_id"
    - from: "Modules/EmailScan/Public/Services/OAuthSecretsRepository.php"
      to: "storage/app/secrets/email-oauth.json"
      via: "writeAtomic: tmp + flock + fwrite + fflush + fsync + rename + chmod 0600"
      pattern: "rename.*\\.tmp"
    - from: "Modules/EmailScan/Database/Migrations/2026_05_16_020004_create_known_senders_table.php"
      to: "known_senders system seed rows"
      via: "INSERT after create"
      pattern: "paypal\\.com"
---

<objective>
Wave 1 schema + secrets foundation. Five user-scoped migrations (inboxes / inbox_scan_state / inbox_messages / known_senders / discovered_senders) with full FND-03 + enum-trigger discipline; matching Eloquent models; full Public DTO set including the ScanCursor value object that normalises Gmail historyId + Graph deltaLink; and the first piece of production code in the module — OAuthSecretsRepository, the only DI surface that touches the chmod-600 JSON. PLT-03 invariant is structurally enforced once this plan lands.

Purpose: Downstream plans (03 Gmail OAuth, 04 Microsoft OAuth, 05 Backfill, 07 Incremental) all depend on these tables + the OAuthSecretsRepository read/write API existing. The schema is the contract; the repository is the secrets-write contract. Both must exist before any OAuth dance can persist a refresh token.

Output: Five migrations + five models + six DTOs + OAuthSecretsRepository + a full unit-test suite (atomic-rotation crash safety, dir mode, file mode) + integration tests (migrations apply cleanly, UNIQUE constraint enforces idempotency, ScanCursor round-trips).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-email-receipt-ingestion-infrastructure/06-CONTEXT.md
@.planning/phases/06-email-receipt-ingestion-infrastructure/06-RESEARCH.md
@.planning/phases/06-email-receipt-ingestion-infrastructure/06-PATTERNS.md
@.planning/phases/06-email-receipt-ingestion-infrastructure/06-01-SUMMARY.md

@Modules/Chains/Database/Migrations/2026_05_16_010001_create_chain_links_table.php
@Modules/Chains/Database/Migrations/2026_05_16_010002_create_card_statements_table.php
@Modules/Chains/Database/Migrations/2026_05_16_010004_backpopulate_card_statements_from_statement_summaries.php
@Modules/Chains/Models/CardStatement.php
@Modules/Chains/Models/ChainLink.php
@Modules/Chains/Public/Dto/ChainLinkRow.php
@Modules/Chains/Public/Dto/CardStatementForecastTile.php
@Modules/Chains/Internal/CardStatementStateMachine.php
@tests/Contracts/UserIdColumnArchTest.php
</context>

<interfaces>
<!-- Contracts downstream plans consume. Treat this section as the canonical surface. -->

ScanCursor (Public/Dto/ScanCursor.php) — final readonly class:
  - public string $provider                  // 'gmail' | 'microsoft'
  - public ?string $historyId                // Gmail only; null otherwise
  - public ?string $deltaLink                // Graph only; null otherwise
  - public static function gmail(string $historyId): self
  - public static function microsoft(string $deltaLink): self
  - public static function emptyFor(string $provider): self          // both nullable fields null
  - public function isEmpty(): bool

Cursor-write contract (per D-123 + BL-3 + BL-7): All writes to inbox_scan_state.last_history_id / last_delta_link MUST funnel through `Modules\EmailScan\Internal\InboxScanStateMachine::recordCursor(int $inboxId, ScanCursor $cursor): void`. The state machine class lives in Plan 05 (stub) + Plan 07 (strict). NO raw `->table('inbox_scan_state')->update(['last_history_id' => ...])` or `->update(['last_delta_link' => ...])` writes are permitted anywhere in Modules/EmailScan/ — the BoundaryArchTest `noOtherInboxScanStateMutator` grep rule from Plan 01 catches violators (the grep covers ANY `->update(...)` against `inbox_scan_state`, scoped to InboxScanStateMachine + migrations only).

recordCursor contract (defined in Plan 05 stub, hardened in Plan 07):
  - Validates $cursor->provider matches the row's inbox provider (gmail row ↔ ScanCursor::gmail, microsoft row ↔ ScanCursor::microsoft); throws InvalidArgumentException otherwise.
  - Writes last_history_id when $cursor->provider === 'gmail' AND $cursor->historyId !== null.
  - Writes last_delta_link when $cursor->provider === 'microsoft' AND $cursor->deltaLink !== null.
  - Skips silently when ScanCursor::emptyFor(...) is passed (no-op — caller wanted to clear, but Phase 6 never clears).
  - Wraps the update in a transaction + PRAGMA busy_timeout = 5000 (same shape as applyStatus).

InboxCredentials (Public/Dto/InboxCredentials.php) — final readonly class extends spatie\LaravelData\Data:
  - public readonly int $inboxId
  - public readonly string $provider
  - public readonly string $refreshToken
  - public readonly string $scope
  - public readonly ?\DateTimeImmutable $expiresAt
  - public readonly ?string $accessToken      // freshly-refreshed cache, may be null on first load

InboxHealthDto (Public/Dto/InboxHealthDto.php) — extends spatie\LaravelData\Data:
  - public readonly int $inboxId
  - public readonly int $userId
  - public readonly string $provider          // 'gmail' | 'microsoft'
  - public readonly string $email
  - public readonly int $backfillWindowMonths
  - public readonly ?\DateTimeImmutable $lastScanAt
  - public readonly string $status            // mirrors inbox_scan_state.status enum
  - public readonly int $retryAttempts
  - public readonly ?string $errorMessage
  - public readonly ?int $backfillFetchedCount        // null when no active backfill
  - public readonly ?int $backfillTotalEstimated      // null when no active backfill

KnownSenderDto (Public/Dto/KnownSenderDto.php) — extends spatie\LaravelData\Data:
  - public readonly int $id
  - public readonly ?int $userId               // null for system seeds
  - public readonly string $emailPattern
  - public readonly string $label
  - public readonly string $source             // 'system' | 'user'

InboxMessageDto (Public/Dto/InboxMessageDto.php) — extends spatie\LaravelData\Data:
  - public readonly int $id
  - public readonly int $userId
  - public readonly int $inboxId
  - public readonly string $providerMessageId
  - public readonly \DateTimeImmutable $internalDate
  - public readonly string $senderEmail
  - public readonly ?string $senderName
  - public readonly ?string $subject
  - public readonly string $status             // 'fetched' (Phase 6 default)
  - public readonly \DateTimeImmutable $fetchedAt

EmailScanHealthTile (Public/Dto/EmailScanHealthTile.php) — extends spatie\LaravelData\Data:
  - public readonly array $lines               // array<InboxHealthLine> where InboxHealthLine is a small readonly DTO with provider + emailLocalPart + lastScanAt + status; planner can inline as anonymous class shape or define separately
  - public readonly string $overallStatus      // 'healthy' | 'stale' | 'reauth'
  - public readonly int $overflowCount         // number beyond first 3 lines (0 when ≤3)

InboxMessageQuery public methods (Public/Services/InboxMessageQuery.php) — final readonly class:
  - public function __construct(private readonly Illuminate\Database\DatabaseManager $db) {}
  - forStatus(string $status): iterable<InboxMessageDto>
       // Streams rows from inbox_messages WHERE status = ? via chunked cursor / lazy iteration
       // (use `->orderBy('id')->lazy()` or `->cursor()` per the established Phase 5 Public-query precedent;
       // Phase 7 consumes status='fetched' rows; iterable contract avoids materialising 50k rows into memory).
       // Throws InvalidArgumentException if $status not in {'fetched','parsed','skipped','unmatched'}.
       // Each yielded row is mapped to InboxMessageDto (lazy mapping via generator).

OAuthSecretsRepository public methods:
  - hasProviderClient(string $provider): bool                         // does providers.{provider} exist with client_id + client_secret?
  - saveProviderClient(string $provider, string $clientId, string $clientSecret, string $redirectUri): void
  - loadProviderClient(string $provider): ?{client_id, client_secret, redirect_uri}    // typed array, NOT a DTO (no transactional read needs an immutable wrap)
  - loadInbox(int $inboxId): ?InboxCredentials
  - saveInboxRefreshToken(int $inboxId, string $provider, string $email, string $refreshToken, string $scope, ?\DateTimeImmutable $expiresAt): void
  - rotateRefreshToken(int $inboxId, string $newRefreshToken, ?string $newAccessToken, ?\DateTimeImmutable $expiresAt): void   // single-use rotation (Microsoft Graph rotates on every refresh)
  - removeInbox(int $inboxId): void                                   // explicit "purge from JSON" path; Phase 6 does not ship a UI for this but the contract exists so a future plan / artisan command can call it

All writes go through writeAtomic($absolutePath, $jsonBytes) — private method — tmp + fwrite + fflush + fsync + chmod 0600 + rename.
All reads use json_decode strict (JSON_THROW_ON_ERROR). Empty file or missing file returns the empty `{providers: {}, inboxes: []}` shape.

PHP constants:
  - PATH_RELATIVE = 'app/secrets/email-oauth.json'
  - DIR_MODE = 0700
  - FILE_MODE = 0600
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (storage/app/secrets/) → process | OAuth client_secret + refresh_token live on disk; only OAuthSecretsRepository may touch the file |
| migration runtime → DB | Five new tables + system-row INSERTs; trigger-enforced enum integrity |
| Eloquent model → application code | Models expose no setters for state columns that have downstream invariants (lifecycle columns must funnel through state machines added in later plans) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information disclosure | refresh_token in storage/app/secrets/email-oauth.json | mitigate | chmod 0600 on file + chmod 0700 on parent dir; OAuthSecretsRepository is the ONLY DI touchpoint per BoundaryArchTest extension (added in Plan 07 alongside the InboxScanStateMachine grep — Plan 02 lays the file but doesn't yet have other code that could leak it) |
| T-06-02-02 | Tampering | atomic-rotation half-write leaves orphaned tmp file | mitigate | fwrite → fflush → fsync → chmod 0600 → rename (POSIX-atomic); test asserts crash-during-write leaves the prior file intact |
| T-06-02-03 | Information disclosure | refresh_token leaking into Laravel exception logs | mitigate | Repository catches its own exceptions and re-throws a typed `SecretsWriteFailed` exception that NEVER includes the token payload — only the file path and error code |
| T-06-02-04 | Tampering | enum column accepting unexpected values via raw INSERT | mitigate | BEFORE INSERT/UPDATE trigger pair on every enum column (inbox_scan_state.status, inbox_messages.status, inboxes.provider, known_senders.source, discovered_senders.state) — same pattern as Phase 5 chain_links.kind |
| T-06-02-05 | Repudiation | cross-user row read via inbox_id-only query | mitigate | FND-03 user_id column on every table + UserIdColumnArchTest extension covers the new tables; runtime queries use where('user_id', $user->id) per CLAUDE.md DI-only convention |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Five migrations + system seed + Eloquent models + UserIdColumnArchTest extension</name>
  <files>
    Modules/EmailScan/Database/Migrations/2026_05_16_020001_create_inboxes_table.php,
    Modules/EmailScan/Database/Migrations/2026_05_16_020002_create_inbox_scan_state_table.php,
    Modules/EmailScan/Database/Migrations/2026_05_16_020003_create_inbox_messages_table.php,
    Modules/EmailScan/Database/Migrations/2026_05_16_020004_create_known_senders_table.php,
    Modules/EmailScan/Database/Migrations/2026_05_16_020005_create_discovered_senders_table.php,
    Modules/EmailScan/Models/Inbox.php,
    Modules/EmailScan/Models/InboxScanState.php,
    Modules/EmailScan/Models/InboxMessage.php,
    Modules/EmailScan/Models/KnownSender.php,
    Modules/EmailScan/Models/DiscoveredSender.php,
    tests/Contracts/UserIdColumnArchTest.php
  </files>
  <read_first>
    Modules/Chains/Database/Migrations/2026_05_16_010001_create_chain_links_table.php (entire file — the canonical Phase 6 migration shape: Container DI + memoised DatabaseManager + Blueprint columns + BEFORE INSERT/UPDATE trigger pair),
    Modules/Chains/Database/Migrations/2026_05_16_010002_create_card_statements_table.php (entire file — paired model with enum state column; second trigger pair shape),
    Modules/Chains/Database/Migrations/2026_05_16_010004_backpopulate_card_statements_from_statement_summaries.php (entire file — INSERT-after-create shape for the known_senders system seed),
    Modules/Chains/Models/CardStatement.php (entire file — Eloquent model shape with $fillable, $casts, BelongsToUser if applicable),
    Modules/Chains/Models/ChainLink.php (entire file — second Eloquent precedent),
    tests/Contracts/UserIdColumnArchTest.php (entire file — the existing schema invariant arch test; Phase 6 tables must register here),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-PATTERNS.md (§"Migrations + models" + §"Modules/EmailScan/Database/Migrations/*_create_inbox_messages_table.php" + §"Modules/EmailScan/Database/Migrations/*_create_known_senders_table.php" — column lists + enum value sets are spelled out there),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-CONTEXT.md (D-117, D-120, D-123, D-128 — column definitions of every table)
  </read_first>
  <action>
    Create five migrations under Modules/EmailScan/Database/Migrations/. Each is a `return new class extends Migration { ... }` anonymous class mirroring the Phase 5 chain_links shape with the memoised DatabaseManager pattern (Container::getInstance()->make(...) so the migration runs cleanly during artisan migrate AND inside Pest's RefreshDatabase).

    **Migration 1 — inboxes** (2026_05_16_020001_create_inboxes_table.php):
      Columns: id (bigIncrements), user_id (foreignId nullable constrained 'users' cascadeOnDelete — FND-03), provider (string 16 — enum 'gmail' | 'microsoft'), email (string 320 — RFC 5321 max), backfill_window_months (unsignedTinyInteger default 3), backfill_progress (json nullable — carries {fetched_count, total_estimated, last_message_date}), timestamps.
      Indexes: ['user_id', 'provider'] + ['user_id', 'created_at'].
      Trigger pair on provider: BEFORE INSERT + BEFORE UPDATE OF provider. Allowed values: 'gmail', 'microsoft'. Use the exact pattern from chain_links.kind trigger (CREATE TRIGGER ... WHEN NEW.provider NOT IN (...) BEGIN SELECT RAISE(ABORT, 'Invalid inboxes.provider value'); END).

    **Migration 2 — inbox_scan_state** (2026_05_16_020002):
      Columns: id, user_id (FND-03), inbox_id (foreignId constrained 'inboxes' cascadeOnDelete), folder (string 64 default 'INBOX'), last_history_id (string 64 nullable), last_delta_link (text nullable — Graph deltaLink can exceed 255 chars), last_scan_at (timestamp nullable), status (string 32 default 'idle'), error_message (text nullable), retry_attempts (unsignedInteger default 0), timestamps.
      UNIQUE: ['inbox_id', 'folder'].
      Index: ['user_id', 'status'].
      Trigger pair on status. Allowed values: 'idle', 'backfilling', 'scanning', 'rate_limited', 'needs_reauth', 'error'.

    **Migration 3 — inbox_messages** (2026_05_16_020003):
      Columns: id, user_id, inbox_id (foreignId constrained cascadeOnDelete), provider_message_id (string 128 — Gmail ids are 16-hex, Graph ids are ~100 chars), internal_date (timestamp — message's RFC 822 Date header or provider-stamped time), sender_email (string 320), sender_name (string 320 nullable), subject (string 998 nullable — RFC 5322 max line), status (string 16 default 'fetched'), fetched_at (timestamp), timestamps.
      UNIQUE: ['inbox_id', 'provider_message_id'] — the D-117 idempotency contract.
      Index: ['user_id', 'status'] (Phase 7 fetched-scan hot path), ['inbox_id', 'internal_date'] (date-range queries).
      Trigger pair on status. Allowed values: 'fetched', 'parsed', 'skipped', 'unmatched'. Phase 6 only ever writes 'fetched'; the other values are present so Phase 7 doesn't need a follow-up schema change.

    **Migration 4 — known_senders** (2026_05_16_020004):
      Columns: id, user_id (FND-03 — NULLABLE; system rows carry NULL), email_pattern (string 320), label (string 100), source (string 16 default 'user'), added_at (timestamp), timestamps.
      Index: ['user_id'] (per-user reads) + ['source'] (system-row discovery).
      Trigger pair on source. Allowed values: 'system', 'user'.
      **System seed** (run AFTER create + trigger creation, inside up()):
        - {user_id: NULL, email_pattern: 'paypal.com', label: 'PayPal', source: 'system'}
        - {user_id: NULL, email_pattern: '@ics.nl', label: 'ICS Cards', source: 'system'}
        - {user_id: NULL, email_pattern: 'googleplay-noreply@google.com', label: 'Google Play', source: 'system'}
      Set added_at + created_at + updated_at to now() for all three rows.

    **Migration 5 — discovered_senders** (2026_05_16_020005):
      Columns: id, user_id, inbox_id (foreignId constrained cascadeOnDelete), sender_email (string 320), sender_name (string 320 nullable), occurrence_count (unsignedInteger default 1), last_seen_at (timestamp), sample_message_id (foreignId nullable constrained 'inbox_messages' nullOnDelete — soft FK so deleting a sample message doesn't drop the discovered row), state (string 16 default 'candidate'), timestamps.
      UNIQUE: ['user_id', 'inbox_id', 'sender_email'].
      Index: ['user_id', 'state'], ['user_id', 'occurrence_count'].
      Trigger pair on state. Allowed values: 'candidate', 'added', 'dismissed'.

    Create five Eloquent models under Modules/EmailScan/Models/ matching the analog shape of Modules/Chains/Models/CardStatement.php + ChainLink.php. Each model:
      - extends Illuminate\Database\Eloquent\Model
      - protected $guarded = ['id'] (or explicit $fillable per project convention — match what Phase 5's models use, currently $guarded = ['id'])
      - Casts: timestamps; enums NOT cast (kept as strings — Phase 5 precedent); JSON columns cast as 'array' (Inbox.backfill_progress)
      - Use the `Modules\Core\Public\Concerns\BelongsToUser` trait verbatim (confirmed pattern: `Modules/Chains/Models/ChainLink.php` line 10 and `Modules/Chains/Models/CardStatement.php` line 10 both `use Modules\Core\Public\Concerns\BelongsToUser`). Add the corresponding `use Modules\Core\Public\Concerns\BelongsToUser;` import and `use BelongsToUser;` trait declaration to every Phase 6 Eloquent model (Inbox, InboxScanState, InboxMessage, KnownSender, DiscoveredSender).
      - DO NOT add D-numbers / REQ-IDs in PHPDocs; describe each model's role in plain technical language

    Extend tests/Contracts/UserIdColumnArchTest.php to include the five new Phase 6 tables in the list of tables that MUST have a `user_id` column. The test already iterates a list of tables; append the five names alphabetically.

    DO NOT add D-numbers / REQ-IDs in migration files or model PHPDocs — describe column purpose in plain technical language. Migrations exist for fixing the schema, not for documenting Phase 6 decisions.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/UserIdColumnArchTest.php 2>&1 | tail -10</automated>
    Test that all five migrations apply cleanly: `php artisan migrate:fresh --env=testing 2>&1 | grep -E "(emailscan|inbox|known_sender|discovered)" | wc -l` returns at least 5 (one per migration). Each migration line should contain "DONE" / "Migrated".
    Test that all five tables exist in the sqlite_testing schema: `php artisan tinker --env=testing --execute='foreach(["inboxes","inbox_scan_state","inbox_messages","known_senders","discovered_senders"] as \$t){echo \$t.": ".(\Illuminate\Support\Facades\Schema::hasTable(\$t)?"YES":"NO")."\n";}'` — each line must say YES.
    Test the known_senders system seed: `php artisan tinker --env=testing --execute='\$rows = DB::connection("sqlite_testing")->table("known_senders")->where("source","system")->pluck("email_pattern")->toArray(); echo implode(",", \$rows);'` — must contain "paypal.com", "@ics.nl", "googleplay-noreply@google.com".
    Test trigger enforcement: `php artisan tinker --env=testing --execute='try { DB::connection("sqlite_testing")->table("inbox_scan_state")->insert(["user_id"=>1,"inbox_id"=>1,"folder"=>"INBOX","status"=>"BOGUS","created_at"=>now(),"updated_at"=>now()]); echo "FAIL_NO_ERROR"; } catch(\Throwable \$e) { echo str_contains(\$e->getMessage(), "Invalid") ? "OK" : "FAIL: ".\$e->getMessage(); }'` — must echo OK.
  </verify>
  <done>
    Five migrations exist with correct columns + FND-03 user_id + UNIQUE constraints + enum trigger pairs. Five Eloquent models exist. known_senders contains 3 system rows after migration. Trigger pairs reject invalid enum values. UserIdColumnArchTest covers the five new tables and passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Public DTOs (ScanCursor, InboxCredentials, InboxHealthDto, KnownSenderDto, InboxMessageDto, EmailScanHealthTile) + ScanCursor unit test + idempotency contract test</name>
  <files>
    Modules/EmailScan/Public/Dto/InboxHealthDto.php,
    Modules/EmailScan/Public/Dto/KnownSenderDto.php,
    Modules/EmailScan/Public/Dto/InboxMessageDto.php,
    Modules/EmailScan/Public/Dto/InboxCredentials.php,
    Modules/EmailScan/Public/Dto/ScanCursor.php,
    Modules/EmailScan/Public/Dto/EmailScanHealthTile.php,
    Modules/EmailScan/tests/Integration/MigrationsTest.php,
    Modules/EmailScan/tests/Integration/ReFetchIdempotentTest.php,
    Modules/EmailScan/tests/Unit/Dto/ScanCursorTest.php
  </files>
  <read_first>
    Modules/Chains/Public/Dto/ChainLinkRow.php (full file — Spatie Data DTO shape: final class … extends Data, readonly constructor props, no methods),
    Modules/Chains/Public/Dto/CardStatementForecastTile.php (full file — tile-DTO with "null = hide tile" semantics; sibling Phase 5 precedent for EmailScanHealthTile),
    Modules/Chains/Public/Dto/StatementSettlement.php if it exists; otherwise skip (used as analog for InboxCredentials role-match per PATTERNS.md),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-PATTERNS.md (§"Public surface (DTOs + services + actions)" + §"Modules/EmailScan/Public/Dto/InboxHealthDto.php"),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-RESEARCH.md (Pattern 2 — ScanCursor canonical shape at lines 472-500; copy gmail() / microsoft() / emptyFor() factories verbatim)
  </read_first>
  <action>
    Create the six DTOs under Modules/EmailScan/Public/Dto/. Each is `final class … extends Spatie\LaravelData\Data` (or `final readonly class … extends Data` per PHP 8.5 + Spatie support — match the existing Chains DTOs' shape; if Chains uses `final class … extends Data` with readonly per-property, mirror that exactly):

      - ScanCursor — see <interfaces> block. Three named constructors (gmail, microsoft, emptyFor), one isEmpty(): bool method. Validate provider in private constructor: throws InvalidArgumentException if not 'gmail' or 'microsoft'. Throws InvalidArgumentException from gmail() if $historyId is empty. Throws InvalidArgumentException from microsoft() if $deltaLink does not start with 'https://graph.microsoft.com/'.
      - InboxCredentials — see <interfaces>. Pure data carrier; no methods.
      - InboxHealthDto — see <interfaces>. Pure data carrier.
      - KnownSenderDto — see <interfaces>. Pure data carrier.
      - InboxMessageDto — see <interfaces>. Pure data carrier.
      - EmailScanHealthTile — see <interfaces>. The `$lines` array is `array<InboxHealthLine>` where InboxHealthLine is a small readonly class also under Public/Dto/InboxHealthLine.php with fields (string $provider, string $emailLocalPart, ?\DateTimeImmutable $lastScanAt, string $status). Add a static `null-return-means-hide` semantic via a comment on EmailScanHealthTile's PHPDoc — the corresponding query method (added in Plan 07) returns ?EmailScanHealthTile.

    Create Modules/EmailScan/tests/Unit/Dto/ScanCursorTest.php with `it` blocks covering:
      - gmail() factory returns provider='gmail', historyId set, deltaLink null
      - microsoft() factory returns provider='microsoft', deltaLink set, historyId null
      - emptyFor('gmail') returns isEmpty() === true
      - emptyFor('microsoft') returns isEmpty() === true
      - Constructing via the public static factories with invalid provider throws InvalidArgumentException
      - gmail('') throws InvalidArgumentException (empty historyId rejected)
      - microsoft('https://wrong.example.com/delta') throws InvalidArgumentException (non-Graph URL rejected)
      - Round-trip serialise via Spatie Data: toArray() and ::from() reconstruct equal value objects for both provider variants

    Create Modules/EmailScan/tests/Integration/MigrationsTest.php (uses the module TestCase + RefreshDatabase from root tests/Pest.php). Assert via Schema::hasTable + Schema::hasColumn that all five tables + their critical columns exist; assert via DB query that known_senders has 3 system rows; assert via Schema::getIndexListing or sqlite_master query that UNIQUE indexes exist on (inbox_id, provider_message_id), (inbox_id, folder), (user_id, inbox_id, sender_email).

    Create Modules/EmailScan/tests/Integration/ReFetchIdempotentTest.php — exercises the inbox_messages UNIQUE (inbox_id, provider_message_id) contract:
      1. Seed a user + inbox row.
      2. Insert an inbox_messages row with provider_message_id='abc' status='fetched' via DB::connection()->table('inbox_messages')->insertOrIgnore([...]).
      3. Insert the SAME row again with insertOrIgnore — assert no exception, assert count remains 1.
      4. Insert the SAME row with raw insert (NOT insertOrIgnore) — assert exception fires (SQLite UNIQUE constraint violation).
    This test proves the D-117 idempotency contract that BackfillInboxJob (Plan 05) will rely on.

    DO NOT use Eloquent::query()->exists() (strict-rules ban — Phase 4/5 pattern); use DB::connection()->table(...)->count() > 0 or scoped raw query builder for any assertion.

    **InboxMessageQuery (per BL-2 — D-129 Public surface)** — create Modules/EmailScan/Public/Services/InboxMessageQuery.php — `final readonly class InboxMessageQuery`. Constructor: `public function __construct(private DatabaseManager $db) {}`.
      Method forStatus(string $status): iterable<InboxMessageDto> —
        Validate $status in the allowed set ['fetched','parsed','skipped','unmatched'] — throw InvalidArgumentException otherwise.
        Stream rows via `$this->db->connection()->table('inbox_messages')->where('status', $status)->orderBy('id')->lazy()` (or `->cursor()` if `lazy()` is unavailable in the installed Laravel version — both yield row generators without loading the full set into memory).
        For each row, yield an InboxMessageDto via `InboxMessageDto::from((array) $row)` (Spatie Data factory).

    Register InboxMessageQuery as a singleton in Modules/EmailScan/Providers/EmailScanServiceProvider.php::register() alongside the other Public surface bindings.

    Create Modules/EmailScan/tests/Unit/Services/InboxMessageQueryTest.php:
      - Seed user + inbox + 5 inbox_messages rows: 3 with status='fetched', 1 with 'parsed', 1 with 'skipped'.
      - Resolve InboxMessageQuery via app(...); iterate forStatus('fetched') into an array.
      - Assert exactly 3 rows yielded, each an InboxMessageDto, each carrying status='fetched'.
      - Assert forStatus('parsed') yields 1 row.
      - Assert forStatus('BOGUS') throws InvalidArgumentException.
      - Assert the iterable contract is lazy: with 1000 seeded rows, `iterator_to_array(...->forStatus('fetched'))` materialises 1000 DTOs but the generator can be partially consumed without preloading the full set (acceptance signal: peak memory remains reasonable; testable via `memory_get_peak_usage` boundary check OR via interrupt-and-resume semantics through Generator::send/return).
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/EmailScan/tests/Unit/Dto/ScanCursorTest.php Modules/EmailScan/tests/Integration/MigrationsTest.php Modules/EmailScan/tests/Integration/ReFetchIdempotentTest.php Modules/EmailScan/tests/Unit/Services/InboxMessageQueryTest.php 2>&1 | tail -20</automated>
    Test that all six DTOs are autoloadable: `php -r "require 'vendor/autoload.php'; foreach(['ScanCursor','InboxCredentials','InboxHealthDto','InboxHealthLine','KnownSenderDto','InboxMessageDto','EmailScanHealthTile'] as \$c){\$f='Modules\\\\EmailScan\\\\Public\\\\Dto\\\\'.\$c; echo \$c.': '.(class_exists(\$f)?'OK':'MISSING')."\n";}"` — every line ends in OK.
    Test ScanCursor round-trip via composer's Spatie Data: `php artisan tinker --env=testing --execute='\$c = Modules\EmailScan\Public\Dto\ScanCursor::gmail("12345"); \$a = \$c->toArray(); \$c2 = Modules\EmailScan\Public\Dto\ScanCursor::from(\$a); echo \$c->historyId === \$c2->historyId ? "OK" : "FAIL";'` echoes OK.
  </verify>
  <done>
    Six DTOs (+ one supporting InboxHealthLine) live under Modules/EmailScan/Public/Dto/. ScanCursor exposes the three named constructors and isEmpty(). ScanCursorTest covers happy paths + validation rejections + Spatie round-trip. MigrationsTest asserts schema shape from a clean RefreshDatabase. ReFetchIdempotentTest proves D-117 idempotency via UNIQUE constraint.
  </done>
</task>

<task type="auto">
  <name>Task 3: OAuthSecretsRepository (atomic chmod-600 JSON I/O) + register singleton in EmailScanServiceProvider + 3 unit tests</name>
  <files>
    Modules/EmailScan/Public/Services/OAuthSecretsRepository.php,
    Modules/EmailScan/Providers/EmailScanServiceProvider.php,
    Modules/EmailScan/tests/Unit/Services/OAuthSecretsRepositoryTest.php,
    Modules/EmailScan/tests/Unit/Services/OAuthSecretsAtomicRotationTest.php,
    Modules/EmailScan/tests/Unit/Services/OAuthSecretsDirModeTest.php
  </files>
  <read_first>
    Modules/Chains/Internal/CardStatementStateMachine.php (full file — final-class DI shape + private const config + DatabaseManager injection pattern — apply same shape for OAuthSecretsRepository with Filesystem instead of DatabaseManager),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-RESEARCH.md (Example 2 lines 836-877 — canonical atomic-rotation shape; copy verbatim with constructor DI + private const PATH + DIR_MODE + FILE_MODE + writeAtomic),
    .planning/phases/06-email-receipt-ingestion-infrastructure/06-PATTERNS.md (§"Modules/EmailScan/Public/Services/OAuthSecretsRepository.php" — no codebase analog; RESEARCH.md is the template),
    Modules/EmailScan/Providers/EmailScanServiceProvider.php (from Plan 01 — Wave 0 empty register; this task adds the singleton binding)
  </read_first>
  <action>
    Create Modules/EmailScan/Public/Services/OAuthSecretsRepository.php — `final class OAuthSecretsRepository` (final, not readonly — has mutable file state).

    Constructor: `public function __construct(private readonly Illuminate\Filesystem\Filesystem $files) {}`.

    Constants: `private const PATH_RELATIVE = 'app/secrets/email-oauth.json'`, `private const DIR_MODE = 0700`, `private const FILE_MODE = 0600`.

    Public methods (signatures per <interfaces> block above):
      - hasProviderClient(string $provider): bool — reads file, checks isset on providers.{provider}.client_id AND providers.{provider}.client_secret.
      - saveProviderClient(string $provider, string $clientId, string $clientSecret, string $redirectUri): void — reads current state, sets providers.{provider} to {client_id, client_secret, redirect_uri}, writes atomically. Validates $provider in ['gmail','microsoft'] (throws InvalidArgumentException otherwise).
      - loadProviderClient(string $provider): ?array — returns the typed dict or null.
      - loadInbox(int $inboxId): ?InboxCredentials — finds inboxes[i] where inboxes[i].id === $inboxId; returns InboxCredentials DTO or null.
      - saveInboxRefreshToken(int $inboxId, string $provider, string $email, string $refreshToken, string $scope, ?\DateTimeImmutable $expiresAt): void — inserts or replaces by inbox_id.
      - rotateRefreshToken(int $inboxId, string $newRefreshToken, ?string $newAccessToken, ?\DateTimeImmutable $expiresAt): void — finds existing inbox entry, updates refresh_token + access_token + expires_at, writes atomically. Throws RuntimeException if inbox not found.
      - removeInbox(int $inboxId): void — drops the matching entry from inboxes[]; writes atomically.

    Private methods:
      - readAll(): array — reads file via $this->files->get(storage_path(self::PATH_RELATIVE)); on file-missing returns default skeleton ['providers' => [], 'inboxes' => []]; on json_decode failure throws RuntimeException with a message that NEVER contains file contents (only path + json_last_error_msg()).
      - writeAtomic(array $data): void — implements the RESEARCH.md Example 2 sequence verbatim:
          1. ensure parent dir exists with `mkdir($absoluteDir, self::DIR_MODE, recursive: true)` if missing
          2. compute absolute path via storage_path(self::PATH_RELATIVE)
          3. $tmp = $absolutePath . '.tmp'
          4. open $tmp with fopen($tmp, 'wb')
          5. flock(LOCK_EX)
          6. fwrite the JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES encoded bytes (json_encode with JSON_THROW_ON_ERROR; catch and rethrow as a SecretsWriteFailed)
          7. fflush
          8. fsync IF function_exists('fsync') (gracefully skip on platforms without the function)
          9. flock(LOCK_UN)
          10. fclose
          11. chmod($tmp, self::FILE_MODE) before rename
          12. rename($tmp, $absolutePath) — atomic on POSIX
      - On any failure during writeAtomic, attempt @unlink($tmp) defensively, then throw a typed `SecretsWriteFailed extends RuntimeException` (also create at Modules/EmailScan/Public/Services/SecretsWriteFailed.php).

    NEVER log the refresh_token or access_token at any layer. NEVER include the token payload in exception messages (RESEARCH.md anti-pattern).

    Register the singleton in Modules/EmailScan/Providers/EmailScanServiceProvider.php — add to register(): `$this->app->singleton(OAuthSecretsRepository::class);`. The repository's only constructor dep is Filesystem, which Laravel resolves out of the box. Keep the provider's other registrations slot for future plans (don't pre-register classes that don't yet exist).

    Create three unit tests:

    **OAuthSecretsRepositoryTest** — exercises happy-path read/write:
      - Seed: ensure storage_path('app/secrets/email-oauth.json') is absent before each test (use beforeEach with @unlink).
      - hasProviderClient('gmail') on virgin state returns false.
      - saveProviderClient + hasProviderClient + loadProviderClient round-trip.
      - saveInboxRefreshToken + loadInbox round-trip; InboxCredentials DTO carries the same refresh_token + scope + expires_at.
      - rotateRefreshToken updates the value; subsequent loadInbox returns the new token.
      - removeInbox removes the entry; subsequent loadInbox returns null.
      - File on disk is `chmod 0600`: `stat -f '%Lp' storage/app/secrets/email-oauth.json` returns 600 (via PHP fileperms() & 0777 === self::FILE_MODE).
      - Parent dir is `chmod 0700`: same check on dirname.

    **OAuthSecretsAtomicRotationTest** — crash-safety:
      - Seed a known prior file content via saveProviderClient.
      - Force writeAtomic to fail mid-rotation by injecting a substitute Filesystem (or by overwriting the .tmp file to be a directory mid-test) that throws on the rename or chmod step.
      - Assert the PRIOR file content is intact (read back, parse, confirm the original providers.gmail.client_id is still there).
      - Assert no `.tmp` file remains (@unlink cleanup ran).
      - **Implementation strategy**: easier path is to subclass OAuthSecretsRepository with a protected hook method `protected function performRename(string $tmp, string $final): bool` and override in the test to return false, simulating a failed rename. The test asserts the prior content is intact AND a typed SecretsWriteFailed exception fires.

    **OAuthSecretsDirModeTest** — dir permissions on first save:
      - Remove storage_path('app/secrets') entirely.
      - Call saveProviderClient — assert the dir exists afterwards with mode 0700 (fileperms() & 0777 === 0700).
      - Call saveProviderClient again (dir already exists) — assert no exception, no mode change.
      - Skip cleanly on non-POSIX hosts (always skip on Windows; use PHP_OS_FAMILY check) — same skip predicate pattern as Phase 5 D-101 HorizonBootsTest.

    All tests run under the EmailScan TestCase. Use $this->app->make(OAuthSecretsRepository::class) to resolve the service via DI (not `new`).
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/EmailScan/tests/Unit/Services/OAuthSecretsRepositoryTest.php Modules/EmailScan/tests/Unit/Services/OAuthSecretsAtomicRotationTest.php Modules/EmailScan/tests/Unit/Services/OAuthSecretsDirModeTest.php 2>&1 | tail -15</automated>
    Test that the service binds: `php artisan tinker --env=testing --execute='echo get_class(app(Modules\EmailScan\Public\Services\OAuthSecretsRepository::class));'` echoes the FQN.
    Test that file mode is correct after a write: `php artisan tinker --env=testing --execute='\$r = app(Modules\EmailScan\Public\Services\OAuthSecretsRepository::class); \$r->saveProviderClient("gmail", "id", "secret", "http://127.0.0.1/cb"); echo decoct(fileperms(storage_path("app/secrets/email-oauth.json")) & 0777);'` echoes `600`.
    Test that the file contains no logged tokens: `grep -r "refresh_token\|client_secret" storage/logs/ 2>/dev/null | wc -l` returns 0 (test environment has empty logs).
  </verify>
  <done>
    OAuthSecretsRepository exists as a singleton-bound Public service. Atomic rotation is implemented per RESEARCH.md Example 2. File is chmod 0600, parent dir is chmod 0700 — verified via fileperms() in tests. Three unit tests pass: happy-path read/write round-trip, mid-write failure leaves prior file intact, dir mode is 0700 on first creation. No tokens / secrets appear in logs (defensive grep). SecretsWriteFailed typed exception lives next to the repository.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

```
php artisan migrate:fresh --env=testing 2>&1 | grep emailscan_or_inbox_or_known_or_discovered
vendor/bin/pest Modules/EmailScan 2>&1 | tail -10
vendor/bin/pest tests/Contracts 2>&1 | tail -10
vendor/bin/phpstan analyse --memory-limit=1G 2>&1 | tail -5
composer format:check 2>&1 | tail -5
```

All commands exit 0. Specifically:
- Five Phase 6 migrations apply on a fresh testing DB.
- All Plan 02 tests (Migrations, ReFetchIdempotent, ScanCursor, OAuthSecretsRepository, OAuthSecretsAtomicRotation, OAuthSecretsDirMode) pass.
- BoundaryArchTest stays green (no facade leaks; no transaction writes from EmailScan; user_id columns present on the five new tables).
- Larastan stays clean (no strict-rules regressions).
</verification>

<success_criteria>
- [ ] Five migrations create the five user-scoped tables with FND-03 nullable user_id + cascade-on-delete + enum-trigger pairs
- [ ] known_senders contains exactly 3 system seed rows after migration (PayPal, ICS, Google Play)
- [ ] inbox_messages.UNIQUE (inbox_id, provider_message_id) blocks a raw duplicate insert and is a no-op on insertOrIgnore
- [ ] Six DTOs (ScanCursor, InboxCredentials, InboxHealthDto, KnownSenderDto, InboxMessageDto, EmailScanHealthTile) live under Modules/EmailScan/Public/Dto/ + supporting InboxHealthLine
- [ ] InboxMessageQuery Public service exists at Modules/EmailScan/Public/Services/InboxMessageQuery.php; forStatus(string): iterable<InboxMessageDto> streams via lazy/cursor; registered singleton; InboxMessageQueryTest passes (D-129 Public surface)
- [ ] ScanCursor exposes gmail() / microsoft() / emptyFor() factories with validation rejections for invalid provider / empty historyId / non-Graph deltaLink
- [ ] OAuthSecretsRepository writes storage/app/secrets/email-oauth.json with chmod 0600 + parent dir chmod 0700
- [ ] Atomic-rotation test proves a failed mid-write leaves the prior file intact (no half-written JSON)
- [ ] SecretsWriteFailed typed exception is thrown on write failure; no token payload ever logged
- [ ] OAuthSecretsRepository is registered as a singleton in EmailScanServiceProvider::register()
- [ ] tests/Contracts/UserIdColumnArchTest.php covers the five new tables
- [ ] PHPDocs + migration comments describe rationale in plain technical language; no D-numbers / REQ-IDs / .planning paths anywhere in the runtime code
- [ ] Full Pest suite green; Larastan level 10 strict green; Pint format green
</success_criteria>

<output>
Write summary to `.planning/phases/06-email-receipt-ingestion-infrastructure/06-02-SUMMARY.md` covering:
- Verbatim final column lists for each of the five tables (in case the planner under-specified a column type)
- Whether $guarded = ['id'] or explicit $fillable was the Phase 5 model precedent the executor chose to mirror
- Confirmation that the chmod-600 file-mode test actually validated against fileperms() — what value was observed
- Whether `function_exists('fsync')` returned true on the host platform (some CI hosts disable it; the writeAtomic gracefully skips)
- Whether SecretsWriteFailed was thrown without leaking any token payload (grep over the exception's getMessage())
- Any deviation from PATTERNS.md analog file structure (e.g., if the existing BelongsToUser trait shape differs from what was expected)
</output>
