---
phase: 15-desktop-shell-nativephp-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - bootstrap/providers.php
  - config/nativephp.php
  - Modules/Desktop/Providers/DesktopServiceProvider.php
  - Modules/Desktop/Internal/NativeAppServiceProvider.php
  - Modules/Desktop/Public/Events/FileOpenedFromOs.php
  - Modules/Desktop/Routes/web.php
  - Modules/Desktop/composer.json
  - tests/Contracts/BoundaryArchTest.php
  - phpstan.neon
  - Modules/Desktop/tests/Pest.php
  - Modules/Desktop/tests/Feature/FileOpenedFromOsTest.php
  - Modules/Desktop/tests/Unit/NativeAppServiceProviderTest.php
  - .gitignore
autonomous: false
requirements: [PKG-04, PKG-05]

must_haves:
  truths:
    - "composer require nativephp/desktop ^2.2 resolves and installs"
    - "php artisan native:build produces a launchable macOS .dmg"
    - "Native\\Desktop\\* imports are forbidden outside Modules/Desktop by an arch test"
    - "Modules/Desktop/ module is registered and boots without error"
  artifacts:
    - path: "Modules/Desktop/Providers/DesktopServiceProvider.php"
      provides: "Desktop module registration entry point"
      contains: "class DesktopServiceProvider"
    - path: "Modules/Desktop/Public/Events/FileOpenedFromOs.php"
      provides: "Public file-open intent event (DTO)"
      contains: "final readonly class FileOpenedFromOs"
    - path: "tests/Contracts/BoundaryArchTest.php"
      provides: "noNativePhpImportsOutsideDesktopModule invariant"
      contains: "noNativePhpImportsOutsideDesktopModule"
  key_links:
    - from: "bootstrap/providers.php"
      to: "Modules\\Desktop\\Providers\\DesktopServiceProvider"
      via: "provider registration array"
      pattern: "DesktopServiceProvider"
    - from: "config/nativephp.php"
      to: "Modules\\Desktop\\Internal\\NativeAppServiceProvider"
      via: "provider config key"
      pattern: "NativeAppServiceProvider"
---

<objective>
Install `nativephp/desktop ^2.2`, create the new `Modules/Desktop/` bounded module, lock the `Native\Desktop\*` containment boundary with an arch-test invariant, scaffold all Wave 0 test files this phase depends on, and prove `php artisan native:build` produces a launchable macOS `.dmg`.

Purpose: This is the foundational vertical slice — after it, a developer can build and launch diederik as a native window. Every later Phase 15 plan builds inside the `Modules/Desktop/` module and against the test scaffolding created here.
Output: Installed NativePHP, registered `Modules/Desktop/` module, arch invariant, Wave 0 test stubs, a built `.dmg`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-CONTEXT.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-RESEARCH.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing patterns the executor must replicate. Do not explore further. -->

bootstrap/providers.php — uses `array_values(array_filter([...]))` with a
`class_exists()`-guarded conditional Horizon entry; all other module providers
are unconditional entries. Append `\Modules\Desktop\Providers\DesktopServiceProvider::class`
unconditionally (nativephp/desktop is a hard `require`, always present).

composer.json autoload-dev — each module has a `"Modules\\<Name>\\Tests\\": "Modules/<Name>/tests/"`
PSR-4 entry. Add `"Modules\\Desktop\\Tests\\": "Modules/Desktop/tests/"`.

tests/Contracts/BoundaryArchTest.php — analog `noHorizonImportsInShippedBuildCode`
(line ~1151): a grep-based `it(...)` walking `['app','Modules','bootstrap','routes']`,
stripping comments via `preg_replace('#/\*.*?\*/|//[^\n]*#s', ...)`, skipping
`/tests/` and the carve-out directory, asserting `$hits === []`.

tests/Contracts/BoundaryArchTest.php — analog facade carve-out (line ~52):
`arch('no Laravel facade usage in module code')->expect('Illuminate\\Support\\Facades')
->not->toBeUsedIn('Modules')->ignoring(['Modules\\Core\\Public\\Support\\LockStore'])`.

phpstan.neon — `ignoreErrors:` block (line ~23) with per-file `path:` entries
justified by a comment block; the `LockStore` carve-out is the precedent.

Public event analog — `Modules/Forecasting/Public/Events/ForecastShortfallDetected.php`:
`final readonly class` with constructor-promoted public scalar payload, no methods.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm nativephp/desktop package legitimacy before install</name>
  <read_first>
    - .planning/phases/15-desktop-shell-nativephp-integration/15-RESEARCH.md (§ "Package Legitimacy Audit")
  </read_first>
  <what-built>
    Nothing built yet — this is a pre-install verification gate. RESEARCH.md
    could not run slopcheck in the sandbox, so per the graceful-degradation rule
    `nativephp/desktop` is treated as `[ASSUMED]` and must be human-verified
    before `composer require` runs.
  </what-built>
  <how-to-verify>
    1. Open https://packagist.org/packages/nativephp/desktop — confirm it exists,
       is published by the NativePHP organisation, version `2.2.0` (or later `^2.2`)
       is present, and download count is high (official Laravel-ecosystem package).
    2. Open https://github.com/NativePHP/desktop — confirm the source repo is the
       official NativePHP project (maintained by Marcel Pociot / BeyondCode).
    3. Confirm the transitive packages `nativephp/electron` and `nativephp/php-bin`
       are also NativePHP-organisation packages on Packagist.
  </how-to-verify>
  <acceptance_criteria>
    - Human has confirmed `nativephp/desktop` is the official NativePHP package on Packagist + GitHub.
    - Human has typed "approved" (or described a concern that blocks the install).
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with composer require, or describe the concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install NativePHP, scaffold Modules/Desktop/ module, register providers</name>
  <files>composer.json, bootstrap/providers.php, config/nativephp.php, Modules/Desktop/Providers/DesktopServiceProvider.php, Modules/Desktop/Internal/NativeAppServiceProvider.php, Modules/Desktop/Public/Events/FileOpenedFromOs.php, Modules/Desktop/Routes/web.php, Modules/Desktop/composer.json, .gitignore</files>
  <read_first>
    - composer.json (root — autoload + autoload-dev blocks)
    - bootstrap/providers.php (current provider list)
    - Modules/Receipts/Providers/ReceiptsServiceProvider.php (DesktopServiceProvider analog — class shape, boot() resource loading, register() singleton binding)
    - Modules/Receipts/composer.json (module composer.json analog)
    - Modules/Forecasting/Public/Events/ForecastShortfallDetected.php (FileOpenedFromOs event analog)
    - Modules/Import/Routes/web.php (Routes/web.php analog)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-PATTERNS.md (§ DesktopServiceProvider, § NativeAppServiceProvider, § FileOpenedFromOs, § bootstrap/providers.php)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-RESEARCH.md (§ "Recommended Project Structure", § "Installation")
  </read_first>
  <action>
    Run `composer require nativephp/desktop` (resolves `^2.2`), then `php artisan native:install`
    to publish `config/nativephp.php` and the default `NativeAppServiceProvider`.

    Create the `Modules/Desktop/` module mirroring the `Modules/Receipts/` shape:
    - `Modules/Desktop/composer.json` — PSR-4 namespace `Modules\Desktop\` (copy the Receipts module composer.json structure).
    - `Modules/Desktop/Providers/DesktopServiceProvider.php` — `final class DesktopServiceProvider extends ServiceProvider`. `boot()` loads migrations (`is_dir` guard), routes (`is_file` guard), and views under the `desktop` namespace, exactly as `ReceiptsServiceProvider`. `register()` is empty for now (singleton bindings come in later plans).
    - `Modules/Desktop/Internal/NativeAppServiceProvider.php` — the NativePHP-booted provider. `boot()` is empty for now (native chrome is plan 02); the class exists so `config/nativephp.php` can point at it. Add a class-level docblock stating it is the sole sanctioned home for `Native\Desktop\Facades\*` calls.
    - `Modules/Desktop/Public/Events/FileOpenedFromOs.php` — `final readonly class FileOpenedFromOs` with constructor-promoted public payload: `string $path`, `string $extension` (values `'eml'` or `'csv'`). Dumb DTO, no methods. Docblock notes the path is validated at the emission boundary, not here.
    - `Modules/Desktop/Routes/web.php` — empty route file with a `Route::middleware(['web','auth'])->group(...)` skeleton (staging routes land in plan 04). Mirror `Modules/Import/Routes/web.php`.

    In root `composer.json`: add `"Modules\\Desktop\\Tests\\": "Modules/Desktop/tests/"` to `autoload-dev.psr-4`.
    Delete the default published `App\Providers\NativeAppServiceProvider` if `native:install` created one.

    In `config/nativephp.php`: set the `provider` key to `Modules\Desktop\Internal\NativeAppServiceProvider::class`.
    Set `cleanup_env_keys` to strip dev-only env (`DIEDERIK_DEV_MODE`, any `REDIS_*`) from the shipped bundle per RESEARCH.md Runtime State Inventory.

    In `bootstrap/providers.php`: append `\Modules\Desktop\Providers\DesktopServiceProvider::class` as an unconditional entry (no `class_exists()` guard).

    In `.gitignore`: add the NativePHP build output dir (`vendor/nativephp/electron/dist` per RESEARCH.md A6 — verify the actual path after install) and any NativePHP build temp dirs.

    Run `composer dump-autoload`.
  </action>
  <verify>
    <automated>php artisan about | grep -i nativephp && composer dump-autoload && php artisan config:clear</automated>
  </verify>
  <acceptance_criteria>
    - `composer show nativephp/desktop` reports a `2.2.x` version.
    - `config/nativephp.php` `provider` key string equals `Modules\Desktop\Internal\NativeAppServiceProvider::class`.
    - `bootstrap/providers.php` contains `DesktopServiceProvider::class` as an unconditional array entry.
    - `php artisan about` runs without error (Desktop provider boots cleanly).
    - `Modules/Desktop/Public/Events/FileOpenedFromOs.php` declares `final readonly class FileOpenedFromOs` with public `string $path` and `string $extension` constructor properties.
    - Root `composer.json` `autoload-dev.psr-4` contains the `Modules\Desktop\Tests\` entry.
  </acceptance_criteria>
  <done>NativePHP installed; Modules/Desktop/ module exists, is registered in bootstrap/providers.php, and config/nativephp.php points the provider key at the module's NativeAppServiceProvider.</done>
</task>

<task type="auto">
  <name>Task 3: Add noNativePhpImportsOutsideDesktopModule arch invariant + Wave 0 test scaffolding</name>
  <files>tests/Contracts/BoundaryArchTest.php, phpstan.neon, Modules/Desktop/tests/Pest.php, Modules/Desktop/tests/Feature/FileOpenedFromOsTest.php, Modules/Desktop/tests/Unit/NativeAppServiceProviderTest.php</files>
  <read_first>
    - tests/Contracts/BoundaryArchTest.php (analog: `noHorizonImportsInShippedBuildCode` ~line 1151; facade carve-out `arch('no Laravel facade usage...')` ~line 52; the `Internal`-containment `arch(...)->toOnlyBeUsedIn(...)` rules ~lines 8-50)
    - phpstan.neon (the `ignoreErrors:` block ~line 23 with the LockStore carve-out)
    - Modules/Receipts/tests/ (Pest.php and a Feature test for the test-bootstrap shape)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-PATTERNS.md (§ BoundaryArchTest new invariant — the exact regex idiom)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-RESEARCH.md (§ "Assumptions Log" A2 — namespace correction)
  </read_first>
  <action>
    Verify the actual NativePHP v2 facade/import namespace from the installed package
    (it is `Native\Desktop\*`, NOT `Native\Laravel\*` — CONTEXT.md's `Native\Laravel\*`
    wording is v1-era; RESEARCH.md Assumptions Log A2). The arch invariant MUST target
    the real installed namespace or it passes vacuously.

    In `tests/Contracts/BoundaryArchTest.php` add three rules:
    1. `it('does not allow Native\\Desktop imports outside Modules/Desktop (noNativePhpImportsOutsideDesktopModule)', ...)` — grep-based, walking `['app','Modules','bootstrap','routes']`, stripping comments, skipping `/tests/` and `/Modules/Desktop/`, asserting `$hits === []`. Mirror `noHorizonImportsInShippedBuildCode` exactly. Use the verified namespace in the regex.
    2. An `Internal`-containment rule: `arch('Modules\\Desktop\\Internal is only used inside Modules\\Desktop')->expect('Modules\\Desktop\\Internal')->toOnlyBeUsedIn('Modules\\Desktop')`.
    3. Extend the existing facade carve-out: add `Modules\\Desktop\\Internal\\NativeAppServiceProvider` (and the two native builders `Modules\\Desktop\\Internal\\Native\\AppMenuBuilder`, `Modules\\Desktop\\Internal\\Native\\TrayMenuBuilder` — created in plan 02) to the `->ignoring([...])` list of `arch('no Laravel facade usage in module code')`, with a comment justifying the DI-only exception (NativePHP facades are unavoidable in native-chrome code).

    In `phpstan.neon`: add per-file `ignoreErrors` entries for `Modules/Desktop/Internal/NativeAppServiceProvider.php` and the two `Native/*Builder.php` files, mirroring the `LockStore` carve-out comment-justified block. Scope the ignore to facade-call / undefined-static-method messages only.

    Create Wave 0 test scaffolding (these are the test files later plans fill in — create them now as failing/skipped stubs so the Nyquist verification map has real targets):
    - `Modules/Desktop/tests/Pest.php` — Pest bootstrap for the module test dir (mirror `Modules/Receipts/tests/Pest.php`).
    - `Modules/Desktop/tests/Feature/FileOpenedFromOsTest.php` — `it('routes .csv FileOpenedFromOs to the import staging flow')->todo()` and `it('routes .eml FileOpenedFromOs to the receipts staging flow')->todo()` and `it('pending file intent survives the login round-trip')->todo()` and `it('single-instance file open focuses the existing window')->todo()`. Mark `->todo()` so they are not red — plan 04 implements them.
    - `Modules/Desktop/tests/Unit/NativeAppServiceProviderTest.php` — `it('configures the application window')->todo()`, `it('configures the app menu')->todo()`, `it('configures the system tray')->todo()`. Plan 02 implements them.

    Run `composer dump-autoload` so the new test namespace is discoverable.
  </action>
  <verify>
    <automated>composer dump-autoload && php artisan test --filter="noNativePhpImportsOutsideDesktopModule" && vendor/bin/phpstan analyse --no-progress 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter="noNativePhpImportsOutsideDesktopModule"` passes (the invariant exists and is green — no offending imports yet).
    - The arch invariant regex targets `Native\Desktop` (verified against the installed package namespace), not `Native\Laravel`.
    - `vendor/bin/phpstan analyse` exits 0 with the new Desktop carve-out entries in place.
    - `php artisan test --filter="FileOpenedFromOs"` reports the four tests as todo/incomplete (not failing).
    - `Modules/Desktop/tests/Unit/NativeAppServiceProviderTest.php` exists with three `->todo()` cases.
  </acceptance_criteria>
  <done>noNativePhpImportsOutsideDesktopModule + Internal-containment + facade carve-out are in BoundaryArchTest and green; phpstan carve-out in place; Wave 0 test stubs exist for plans 02 and 04.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: native:build produces a launchable macOS .dmg</name>
  <what-built>
    NativePHP is installed and the Desktop module is registered. `php artisan native:build`
    wraps electron-builder to produce a `.dmg`. This build + OS-install step cannot run
    headlessly in CI on the dev box, so it is verified manually (PKG-04 SC1).
  </what-built>
  <how-to-verify>
    1. Confirm Node.js 22+ is available: `node --version`.
    2. Run `php artisan native:build` (macOS target). It produces a `.dmg` under the
       NativePHP build output dir (`vendor/nativephp/electron/dist` or as reported).
    3. Open the `.dmg`, drag diederik into `/Applications`.
    4. Launch `/Applications/diederik.app`. Confirm a native window opens.
       (The window will show the app — login or dashboard depending on DB state.
       Native chrome polish is plan 02; this step only proves the build + launch path.)
  </how-to-verify>
  <acceptance_criteria>
    - `php artisan native:build` completes without error and emits a `.dmg`.
    - The `.dmg` installs diederik into `/Applications/diederik.app`.
    - Launching the app opens a native window that renders the diederik web UI.
  </acceptance_criteria>
  <resume-signal>Type "approved" if the .dmg builds and launches, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Packagist → build | New third-party dependency (`nativephp/desktop` + transitives) enters the dependency tree |
| Shipped bundle → disk | `config/nativephp.php` `cleanup_env_keys` controls what env reaches the packaged app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `composer require nativephp/desktop` (supply chain) | mitigate | Task 1 blocking-human checkpoint verifies the package against Packagist + the official NativePHP GitHub org before install; slopcheck was unavailable so human verification is mandatory and non-auto-approvable |
| T-15-02 | Information Disclosure | Secrets bundled into the shipped app | mitigate | `config/nativephp.php` `cleanup_env_keys` strips `DIEDERIK_DEV_MODE` + `REDIS_*` from the production bundle (Task 2); `.env.bundled` template carrying no real secrets is Phase 17 (CI-06) |
| T-15-SC | Tampering | npm/composer installs in this plan | mitigate | Task 1 blocking-human checkpoint for the `[ASSUMED]` NativePHP packages; no `[SLOP]`/`[SUS]` packages present per RESEARCH.md audit |
</threat_model>

<verification>
- `composer show nativephp/desktop` reports `^2.2`.
- `php artisan test --filter="noNativePhpImportsOutsideDesktopModule"` green.
- `vendor/bin/phpstan analyse` exits 0.
- `php artisan native:build` produces a `.dmg` that installs and launches (manual checkpoint).
</verification>

<success_criteria>
- NativePHP `^2.2` installed and verified.
- `Modules/Desktop/` module registered; `config/nativephp.php` provider key points at it.
- `noNativePhpImportsOutsideDesktopModule` invariant exists, targets `Native\Desktop`, and is green.
- Wave 0 test stubs exist for downstream plans.
- A `.dmg` builds and launches a native window.
</success_criteria>

<output>
Create `.planning/phases/15-desktop-shell-nativephp-integration/15-01-SUMMARY.md` when done.
</output>
