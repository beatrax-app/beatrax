---
phase: 15-desktop-shell-nativephp-integration
plan: 07
type: execute
wave: 3
depends_on: [15-06]
files_modified:
  - Modules/Categorization/Resources/views
  - Modules/Chains/Resources/views
  - Modules/EmailScan/Resources/views
  - Modules/Receipts/Resources/views
  - Modules/Recurring/Resources/views
  - Modules/DriftAlerts/Resources/views
  - Modules/Desktop/Resources/views
  - tests/Contracts/BoundaryArchTest.php
autonomous: true
requirements: [PKG-05]

must_haves:
  truths:
    - "Every remaining module's Blade views render correct dark variants"
    - "The Desktop module's own screens (setup/welcome/staging/close prompt) are dark-themed"
    - "No Blade view anywhere keeps a bg-white or text-slate-900 without a dark: companion"
  artifacts:
    - path: "tests/Contracts/BoundaryArchTest.php"
      provides: "dark-companion arch guard with an empty allow-list (full coverage)"
      contains: "dark"
  key_links:
    - from: "Modules/Categorization/Resources/views"
      to: "UI-SPEC dark token table"
      via: "dark: companion utilities"
      pattern: "dark:"
---

<objective>
Complete the dark-theme retrofit (D-15): add `dark:` variants to every remaining module's Blade views — Categorization, Chains, EmailScan, Receipts, Recurring, DriftAlerts — and the Desktop module's own new screens, then close the dark-companion arch guard's allow-list so full coverage is enforced.

Purpose: Finishes what plan 06 started. After this slice, diederik has zero unthemed views — the dark theme is complete and regression-locked. This plan touches only Blade views, no native code, so it can run alongside the shell plans (it depends only on plan 06's infrastructure + token mapping).
Output: ~27 themed views + a full-coverage dark-companion arch guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-CONTEXT.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-RESEARCH.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-UI-SPEC.md
@CLAUDE.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-06-SUMMARY.md

<interfaces>
<!-- UI-SPEC dark token table (same source of truth plan 06 used): -->
<!-- bg white -> dark:bg-slate-950 ; bg slate-50 -> dark:bg-slate-900 -->
<!-- text-slate-900 -> dark:text-slate-100 ; caption text-slate-500 -> dark:text-slate-400 -->
<!-- emerald-600 -> dark:emerald-500 ; rose-600 -> dark:rose-500 -->
<!-- drift "↗/↘" pills rose-50/500/900 -> dark:rose-950/800/200 -->
<!-- amber-50/300/900 warning alert -> dark:amber-950/800/200 -->

Plan 06 delivered: app.css dark variant, themed layout, themed Core/Auth/Ledger/
Import/Forecasting views, and a dark-companion arch guard (Blade scan flagging
bg-white/text-slate-900 without a dark: companion) — currently scoped via an
allow-list that this plan empties.

Desktop module views (created by plans 02/04/05): setup.blade.php, welcome.blade.php,
staging.blade.php, and the close-window prompt's flux:modal markup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark variants for Categorization, Chains, and Receipts views</name>
  <files>Modules/Categorization/Resources/views, Modules/Chains/Resources/views, Modules/Receipts/Resources/views</files>
  <read_first>
    - resources/views/layouts/app.blade.php (the themed layout — token pattern)
    - Modules/Categorization/Resources/views (all 8 Blade views)
    - Modules/Chains/Resources/views (all 3 Blade views)
    - Modules/Receipts/Resources/views (all 2 Blade views)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-UI-SPEC.md (Color token table; slate-500-forbidden-on-dark rule; contrast requirement)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-06-SUMMARY.md (the exact token mapping plan 06 applied — match it)
  </read_first>
  <action>
    Add `dark:` companion utilities to every Blade view in `Modules/Categorization`,
    `Modules/Chains`, and `Modules/Receipts`, strictly per the UI-SPEC Color token
    table and matching the mapping plan 06 already applied: `bg-white`→`dark:bg-slate-950`,
    `bg-slate-50`→`dark:bg-slate-900`, `text-slate-900`→`dark:text-slate-100`, caption
    `text-slate-500`→`dark:text-slate-400` (slate-500 body text FORBIDDEN on dark),
    `emerald-600`→`dark:emerald-500`, `rose-600`→`dark:rose-500`. Category-bar fills
    use the accent-ink dark value (`slate-100`). Introduce no new hues; every dark
    pair clears WCAG AA.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - No Blade view in `Modules/Categorization`, `Modules/Chains`, `Modules/Receipts` has a `bg-white` or `text-slate-900` utility without a `dark:` companion.
    - No `text-slate-500` body text on a dark surface.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The Categorization, Chains, and Receipts views render correct, contrast-passing dark variants.</done>
</task>

<task type="auto">
  <name>Task 2: Dark variants for EmailScan, Recurring, DriftAlerts, and Desktop views</name>
  <files>Modules/EmailScan/Resources/views, Modules/Recurring/Resources/views, Modules/DriftAlerts/Resources/views, Modules/Desktop/Resources/views</files>
  <read_first>
    - resources/views/layouts/app.blade.php (themed layout — token pattern)
    - Modules/EmailScan/Resources/views (all 4 Blade views)
    - Modules/Recurring/Resources/views (all 4 Blade views)
    - Modules/DriftAlerts/Resources/views (all 4 Blade views)
    - Modules/Desktop/Resources/views (setup/welcome/staging + close-window prompt — created by plans 02/04/05)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-UI-SPEC.md (Color token table; drift "↗/↘" pill dark variants; contrast requirement)
  </read_first>
  <action>
    Add `dark:` companion utilities to every Blade view in `Modules/EmailScan`,
    `Modules/Recurring`, `Modules/DriftAlerts`, and `Modules/Desktop`, per the
    UI-SPEC Color token table — same mapping as Task 1. DriftAlerts: the drift
    "↗/↘" rose pills get their dark variants (`dark:rose-950/800/200`). The Desktop
    module's own screens (`setup.blade.php`, `welcome.blade.php`, `staging.blade.php`,
    the `flux:modal` close-window prompt) must be dark-themed too — `flux:modal`
    inherits the theme, but any raw Tailwind utilities in those views need `dark:`
    companions. Introduce no new hues; every dark pair clears WCAG AA.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - No Blade view in `Modules/EmailScan`, `Modules/Recurring`, `Modules/DriftAlerts`, `Modules/Desktop` has a `bg-white` or `text-slate-900` utility without a `dark:` companion.
    - DriftAlerts "↗/↘" pills use the dark rose variants.
    - The Desktop setup/welcome/staging/close-prompt screens render correct dark variants.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The EmailScan, Recurring, DriftAlerts, and Desktop views render correct dark variants — every module is now dark-themed.</done>
</task>

<task type="auto">
  <name>Task 3: Close the dark-companion arch guard to full coverage</name>
  <files>tests/Contracts/BoundaryArchTest.php</files>
  <read_first>
    - tests/Contracts/BoundaryArchTest.php (the dark-companion guard added in plan 06 — its allow-list)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-06-SUMMARY.md (which views plan 06 themed)
  </read_first>
  <action>
    Empty the allow-list on the dark-companion arch guard introduced in plan 06 so it
    scans EVERY Blade view across `Modules/*/Resources/views` and `resources/views`
    and fails if any element carries `bg-white` or `text-slate-900` without a `dark:`
    companion. With plans 06 + 07 complete, every view is themed — the guard should
    pass with a fully empty allow-list. This regression-locks the dark theme: any
    future view added without a dark companion fails CI. If the guard surfaces a
    missed view, fix that view rather than re-adding it to the allow-list.
  </action>
  <verify>
    <automated>php artisan test --filter="dark" 2>&1 | tail -5 && php artisan test --parallel 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The dark-companion arch guard has an empty allow-list and passes.
    - `php artisan test --parallel` (full suite) is green.
    - The guard scans both `Modules/*/Resources/views` and `resources/views`.
  </acceptance_criteria>
  <done>The dark-companion arch guard enforces full-coverage dark theming with no allow-list exceptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only adds CSS utility classes to existing Blade views |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-20 | Tampering | Blade view markup edits across 13 modules | mitigate | Changes are CSS-utility-only (`dark:` companions); no logic, no data flow, no new bindings; the full Pest suite (Task 3) confirms no view regressed |
| T-15-21 | Information Disclosure | Dark theme could expose previously-hidden elements | accept | Dark variants only re-color existing elements; no element is shown/hidden by theme — visibility is unchanged from the established light UI |
</threat_model>

<verification>
- `php artisan test --filter="dark"` green with an empty allow-list.
- `php artisan test --parallel` (full suite) green.
- `npm run build` succeeds.
- Manual (carried to phase HUMAN-UAT): walk every module's screens in dark mode — confirm readability and no unthemed white surfaces.
</verification>

<success_criteria>
- Every remaining module's views (Categorization, Chains, EmailScan, Receipts, Recurring, DriftAlerts) render dark variants.
- The Desktop module's own screens are dark-themed.
- The dark-companion arch guard enforces full coverage with no exceptions.
</success_criteria>

<output>
Create `.planning/phases/15-desktop-shell-nativephp-integration/15-07-SUMMARY.md` when done.
</output>
