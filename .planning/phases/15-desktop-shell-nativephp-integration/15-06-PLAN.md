---
phase: 15-desktop-shell-nativephp-integration
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - resources/css/app.css
  - resources/views/layouts/app.blade.php
  - Modules/Core/Database/Migrations/2026_05_22_000001_add_theme_to_users.php
  - Modules/Core/Internal/Http/Livewire/SettingsPage.php
  - Modules/Core/Resources/views/livewire/settings-page.blade.php
  - Modules/Desktop/Public/Contracts/OsThemeSignal.php
  - Modules/Core/Resources/views
  - Modules/Auth/Resources/views
  - Modules/Ledger/Resources/views
  - Modules/Import/Resources/views
  - Modules/Forecasting/Resources/views
  - tests/Contracts/BoundaryArchTest.php
  - Modules/Core/tests/Feature/ThemePreferenceTest.php
autonomous: true
requirements: [PKG-05]

must_haves:
  truths:
    - "Dark mode follows the OS by default; Settings has a Light/Dark/System control"
    - "The app layout toggles a dark class on <html> from the user's theme preference with no flash"
    - "The shell, auth, dashboard, import, and forecasting views render correct dark variants"
  artifacts:
    - path: "resources/css/app.css"
      provides: "Tailwind v4 class-strategy dark variant"
      contains: "custom-variant dark"
    - path: "Modules/Core/Database/Migrations/2026_05_22_000001_add_theme_to_users.php"
      provides: "users.theme column (light/dark/system)"
      contains: "theme"
    - path: "Modules/Core/Internal/Http/Livewire/SettingsPage.php"
      provides: "Light/Dark/System theme control (D-16)"
      contains: "theme"
  key_links:
    - from: "resources/views/layouts/app.blade.php"
      to: "users.theme preference"
      via: "server-side dark class on <html> + pre-paint inline script"
      pattern: "dark"
    - from: "Modules/Core/Internal/Http/Livewire/SettingsPage.php"
      to: "users.theme column"
      via: "instant-apply update"
      pattern: "theme"
---

<objective>
Stand up the dark-theme infrastructure and retrofit the highest-traffic surfaces: the Tailwind v4 dark variant, the `users.theme` preference, the Settings Light/Dark/System control (D-16), the layout dark-class wiring with no flash (D-15), and `dark:` variants for the app shell, Auth, Ledger, Import, and Forecasting views.

Purpose: diederik has zero dark styling today. This slice makes dark mode real and correct for the screens the partner sees most — login, dashboard, imports, forecast — and establishes the token mapping plan 07 follows for the remaining modules. It touches no NativePHP code, so it runs in parallel with the shell plans.
Output: Dark-mode infrastructure + Settings control + ~35 themed views.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-CONTEXT.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-RESEARCH.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-PATTERNS.md
@.planning/phases/15-desktop-shell-nativephp-integration/15-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- UI-SPEC dark token table (the source of truth for every dark: companion): -->
<!-- bg white -> dark:bg-slate-950 ; bg slate-50 -> dark:bg-slate-900 -->
<!-- text-slate-900 -> dark:text-slate-100 ; caption text-slate-500 -> dark:text-slate-400 -->
<!-- (slate-500 body text is FORBIDDEN on dark — step to slate-400) -->
<!-- emerald-600 -> dark:emerald-500 ; rose-600 -> dark:rose-500 -->
<!-- amber-50/300/900 alert -> dark:amber-950/800/200 ; rose-50/500/900 -> dark:rose-950/800/200 -->

resources/css/app.css — current: `@import "tailwindcss";` + Inter :root block +
tabular-nums rule. Zero dark config. Add `@custom-variant dark (&:where(.dark, .dark *));`.

resources/views/layouts/app.blade.php — the single app layout. Current tags hard-code
light: `<html class="bg-white text-slate-900">`, `<body class="antialiased bg-white text-slate-900">`.

SettingsPage — Modules/Core/Internal/Http/Livewire/SettingsPage.php: instant-apply
`toggleAutoImport()` pattern (raw query-builder update on `users`), `#[Validate]`
attribute on public props, `mount(CurrentUser)` reads the user row. NO constructor DI.

theme migration analog — Modules/Core/Database/Migrations/*_add_auto_import_drop_folder_to_users.php:
anonymous-class migration, lazy DatabaseManager, column added with `->default(...)->after(...)`.

OsThemeProbe — created in plan 02 (Modules/Desktop/Internal/Native/OsThemeProbe.php),
wraps `Native\Desktop\Facades\System::theme()`. This plan adds the Public contract
`OsThemeSignal` it implements so the layout can read the OS theme for `system`
resolution without importing Desktop internals. NOTE plan 06 has no depends_on plan 02
in frontmatter — they can run in parallel; the layout's `system` resolution falls back
to the client-side `prefers-color-scheme` script if OsThemeProbe is not yet bound.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dark-mode infrastructure — theme column, Settings control, layout wiring</name>
  <files>resources/css/app.css, resources/views/layouts/app.blade.php, Modules/Core/Database/Migrations/2026_05_22_000001_add_theme_to_users.php, Modules/Core/Internal/Http/Livewire/SettingsPage.php, Modules/Core/Resources/views/livewire/settings-page.blade.php, Modules/Desktop/Public/Contracts/OsThemeSignal.php, tests/Contracts/BoundaryArchTest.php, Modules/Core/tests/Feature/ThemePreferenceTest.php</files>
  <read_first>
    - resources/css/app.css (current full file)
    - resources/views/layouts/app.blade.php (current html/body tags)
    - Modules/Core/Internal/Http/Livewire/SettingsPage.php (instant-apply toggle pattern, #[Validate], mount())
    - Modules/Core/Resources/views/livewire/settings-page.blade.php (settings section markup; flux:radio.group usage)
    - Modules/Core/Database/Migrations/*_add_auto_import_drop_folder_to_users.php (migration analog)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-PATTERNS.md (§ theme migration; § SettingsPage theme control; § Layout dark-class wiring)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-UI-SPEC.md (Dark mode strategy; Component Inventory — Appearance section)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-CONTEXT.md (D-15, D-16)
  </read_first>
  <behavior>
    - Test: the `users` table has a `theme` column defaulting to `system`.
    - Test: the Settings Appearance control persists `light`/`dark`/`system` to `users.theme` with instant-apply (no submit button).
    - Test: `SettingsPage` rejects a `theme` value outside `light,dark,system` via the `#[Validate]` rule.
  </behavior>
  <action>
    Add the `users.theme` column: a migration in `Modules/Core/Database/Migrations/`
    (Core owns the `users` table — PATTERNS.md is explicit it goes here, NOT in
    `Modules/Desktop/`). Anonymous-class migration mirroring the
    `add_auto_import_drop_folder_to_users` analog: `$table->string('theme', 16)
    ->default('system')->after('auto_import_drop_folder')`.

    `resources/css/app.css`: add `@custom-variant dark (&:where(.dark, .dark *));`
    after the `@import "tailwindcss";` (Tailwind v4 class-strategy, UI-SPEC).

    `resources/views/layouts/app.blade.php`: toggle a `dark` class on `<html>`
    server-side from the authenticated user's `theme` column. For `theme = light` /
    `dark`, set the class directly. For `theme = system`, add an inline `<head>`
    script that reads `prefers-color-scheme` and sets the class BEFORE first paint
    (UI-SPEC — prevents the flash). Give the `<html>`/`<body>` tags their `dark:`
    companions per the token table (`bg-white`→`dark:bg-slate-950`,
    `text-slate-900`→`dark:text-slate-100`).

    `SettingsPage` + its Blade view: add a new `## Appearance` section with a
    Light / Dark / System segmented control (`flux:radio.group`), instant-apply
    matching the existing `toggleAutoImport` pattern (raw query-builder `update` on
    `users`, no submit button). Public prop `#[Validate('required|in:light,dark,system')]
    public string $theme = 'system'`; `mount()` reads `users.theme`.

    Create the Public contract `Modules/Desktop/Public/Contracts/OsThemeSignal.php` —
    a one-method interface (`currentOsTheme(): string`) that `OsThemeProbe` (plan 02)
    implements. This is the cross-module seam the layout uses for `system` resolution
    when running in the bundle, without importing Desktop internals. If the binding
    is absent (plan 02 not yet merged / running in Herd), the layout falls back to the
    client-side `prefers-color-scheme` script — so this plan does not hard-depend on
    plan 02.

    Add a recommended arch/grep guard to `tests/Contracts/BoundaryArchTest.php`
    (RESEARCH.md Wave 0 Gaps, optional-but-recommended): a test scanning Blade views
    that flags a `bg-white` or `text-slate-900` utility with no `dark:` companion on
    the same element. Scope it to the views this plan + plan 07 will have themed (or
    make it an allow-list that plan 07 empties). Keep it green for this plan's scope.

    Write `ThemePreferenceTest` covering the three behavior cases.
  </action>
  <verify>
    <automated>php artisan migrate --no-interaction && php artisan test --filter="ThemePreference" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter="ThemePreference"` passes.
    - The `users` table has a `theme` column (`php artisan db:show` / a schema assertion confirms it) defaulting to `system`.
    - `resources/css/app.css` contains `@custom-variant dark`.
    - `resources/views/layouts/app.blade.php` sets a `dark` class on `<html>` from `users.theme` and has a pre-paint `prefers-color-scheme` script for `system`.
    - The Settings Appearance control is instant-apply (no submit button) and rejects out-of-enum values.
    - `npm run build` (Tailwind/Vite) succeeds.
  </acceptance_criteria>
  <done>Dark mode is wired end-to-end: theme column, Tailwind dark variant, layout dark-class with no flash, and an instant-apply Light/Dark/System Settings control.</done>
</task>

<task type="auto">
  <name>Task 2: Dark variants for the app shell, Auth, and Ledger views</name>
  <files>Modules/Core/Resources/views, Modules/Auth/Resources/views, Modules/Ledger/Resources/views</files>
  <read_first>
    - resources/views/layouts/app.blade.php (the themed layout from Task 1 — the token pattern to follow)
    - Modules/Core/Resources/views (all 7 Blade views — top-nav, dashboard, settings, system-alerts-banner)
    - Modules/Auth/Resources/views (all 8 Blade views — login, signup, recovery codes, profile selector)
    - Modules/Ledger/Resources/views (all 3 Blade views)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-UI-SPEC.md (Color token table — every light→dark mapping; the slate-500-forbidden-on-dark rule; the amber/rose alert dark variants)
  </read_first>
  <action>
    Add `dark:` companion utilities to every Blade view in `Modules/Core`,
    `Modules/Auth`, and `Modules/Ledger`, strictly per the UI-SPEC Color token table.
    Every `bg-white` gains `dark:bg-slate-950`; every `bg-slate-50` gains
    `dark:bg-slate-900`; every `text-slate-900` gains `dark:text-slate-100`; caption
    `text-slate-500` becomes `dark:text-slate-400` (slate-500 body text is FORBIDDEN
    on dark). `emerald-600` → `dark:emerald-500`; `rose-600` → `dark:rose-500`. The
    `SystemAlertsBanner` amber/rose alert chrome gets the dark variants
    (`amber-950/800/200`, `rose-950/800/200`). The in-app brand mark in the top-nav
    and on the login/signup screens (D-18) references `resources/brand/logo.svg`.
    Introduce NO new hues — dark is a tonal inversion of the existing slate/emerald/rose
    families. Every dark text/background pair must clear WCAG AA per the UI-SPEC
    contrast requirement.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && php artisan test --filter="ThemePreference"</automated>
  </verify>
  <acceptance_criteria>
    - No Blade view in `Modules/Core`, `Modules/Auth`, `Modules/Ledger` has a `bg-white` or `text-slate-900` utility without a `dark:` companion on the same element.
    - No `text-slate-500` is used as body text on a dark surface (caption text uses `dark:text-slate-400`).
    - The recommended dark-companion arch guard from Task 1 stays green for these modules' views.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The app shell, Auth, and Ledger views render correct, contrast-passing dark variants.</done>
</task>

<task type="auto">
  <name>Task 3: Dark variants for Import and Forecasting views</name>
  <files>Modules/Import/Resources/views, Modules/Forecasting/Resources/views</files>
  <read_first>
    - resources/views/layouts/app.blade.php (themed layout from Task 1)
    - Modules/Import/Resources/views (all 6 Blade views — wizard, preview, confirm)
    - Modules/Forecasting/Resources/views (all 11 Blade views — forecast, what-if scenarios, charts)
    - .planning/phases/15-desktop-shell-nativephp-integration/15-UI-SPEC.md (Color token table; contrast rule; KPI/money tabular-nums)
  </read_first>
  <action>
    Add `dark:` companion utilities to every Blade view in `Modules/Import` and
    `Modules/Forecasting`, per the UI-SPEC Color token table — same mapping rules as
    Task 2. Forecasting carries charts and KPI/money cells: KPI numbers render at
    Display size with `tabular-nums`; ensure positive/negative net amounts use the
    dark emerald/rose values (`dark:emerald-500` / `dark:rose-500`) and that chart
    surfaces (cards, axes labels) get `dark:bg-slate-900` / `dark:text-slate-100`
    companions. Introduce no new hues; every pair clears WCAG AA.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && php artisan test --filter="ThemePreference"</automated>
  </verify>
  <acceptance_criteria>
    - No Blade view in `Modules/Import`, `Modules/Forecasting` has a `bg-white` or `text-slate-900` utility without a `dark:` companion.
    - No `text-slate-500` body text on a dark surface.
    - Positive/negative money cells use `dark:emerald-500` / `dark:rose-500`.
    - The recommended dark-companion arch guard stays green for these modules' views.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The Import and Forecasting views render correct, contrast-passing dark variants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User theme preference → rendered HTML | The `users.theme` value drives a class on `<html>` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-18 | Tampering | `users.theme` value injected into the layout class attribute | mitigate | `SettingsPage` validates `theme` against the `in:light,dark,system` allow-list (`#[Validate]`); the layout maps the value to a fixed class, never echoes it raw into markup — no stored-XSS surface |
| T-15-19 | Information Disclosure | The pre-paint inline `prefers-color-scheme` script | accept | The inline script is a fixed, app-authored snippet reading a browser media query — no user data, no dynamic interpolation; standard no-flash-of-light-theme pattern |
</threat_model>

<verification>
- `php artisan test --filter="ThemePreference"` green.
- `npm run build` succeeds.
- The dark-companion arch guard is green for the modules in scope.
- Manual (carried to phase HUMAN-UAT): toggle Settings Light/Dark/System and the OS theme — confirm no flash and correct dark rendering on the shell, login, dashboard, imports, forecast.
</verification>

<success_criteria>
- Dark mode follows the OS by default; Settings has an instant-apply Light/Dark/System control (D-16).
- The layout toggles a `dark` class on `<html>` with no flash (D-15).
- The shell, Auth, Ledger, Import, and Forecasting views render correct dark variants.
</success_criteria>

<output>
Create `.planning/phases/15-desktop-shell-nativephp-integration/15-06-SUMMARY.md` when done.
</output>
