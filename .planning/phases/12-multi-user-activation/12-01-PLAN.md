---
phase: 12-multi-user-activation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Modules/Auth/module.json
  - Modules/Auth/Providers/AuthServiceProvider.php
  - Modules/Auth/Internal/Fortify/.gitkeep
  - Modules/Auth/Public/Actions/.gitkeep
  - Modules/Auth/Internal/Http/Livewire/.gitkeep
  - Modules/Auth/Internal/Http/Middleware/.gitkeep
  - Modules/Auth/Internal/Recovery/.gitkeep
  - Modules/Auth/Internal/Console/.gitkeep
  - Modules/Auth/Internal/Listeners/.gitkeep
  - Modules/Auth/Models/.gitkeep
  - Modules/Auth/Database/Migrations/.gitkeep
  - Modules/Auth/Resources/views/livewire/.gitkeep
  - Modules/Auth/Resources/views/partials/.gitkeep
  - Modules/Auth/Routes/web.php
  - Modules/Auth/Routes/console.php
  - Modules/Auth/tests/Feature/.gitkeep
  - Modules/Auth/tests/Unit/.gitkeep
  - tests/Contracts/BoundaryArchTest.php
  - tests/Contracts/ModulePrioritiesArchTest.php
autonomous: true
requirements: [MULTI-01]
tags: [auth, module-skeleton, arch-test, di]

must_haves:
  truths:
    - "Modules/Auth/ exists and is loaded by nwidart/laravel-modules with priority 1 (Core remains priority 0)."
    - "Auth::user() / Auth::id() / auth() / request()->user() / request()->session() / session() (helper) / Auth::loginUsingId() are forbidden across every Modules/* file EXCEPT the D-24 file-by-file allow-list."
    - "The allow-list is a precise array of relative file paths inside tests/Contracts/BoundaryArchTest.php — never a glob."
    - "BoundaryArchTest's noAuthFacadeOrHelper invariant is green when no offending symbols exist outside the allow-list."
    - "Codebase remains GSD-agnostic — no .planning/ / PLAN.md / RESEARCH.md / D-NN references in source, comments, or PHPDocs."
  artifacts:
    - path: "Modules/Auth/module.json"
      provides: "module manifest registering Auth at priority 1"
      contains: "\"priority\": 1"
    - path: "Modules/Auth/Providers/AuthServiceProvider.php"
      provides: "service provider boot/register skeleton with loadMigrationsFrom + loadRoutesFrom + loadViewsFrom calls"
      contains: "namespace Modules\\Auth\\Providers"
    - path: "Modules/Auth/Routes/web.php"
      provides: "empty Route::middleware(['web']) group placeholder (real routes wired in 12-03/12-04/12-05)"
    - path: "tests/Contracts/BoundaryArchTest.php"
      provides: "extended with noAuthFacadeOrHelper invariant + D-24 allow-list array"
      contains: "noAuthFacadeOrHelper"
  key_links:
    - from: "Modules/Auth/Providers/AuthServiceProvider.php"
      to: "nwidart/laravel-modules"
      via: "module.json discovery + autoload-psr4 registration in composer.json"
      pattern: "Modules\\\\Auth\\\\"
    - from: "tests/Contracts/BoundaryArchTest.php"
      to: "Modules/* (entire module tree)"
      via: "RecursiveIteratorIterator scan with allow-list filter"
      pattern: "noAuthFacadeOrHelper"
---

<objective>
Stand up the `Modules/Auth/` module skeleton (manifest + service provider + directory tree + empty route files) and extend `tests/Contracts/BoundaryArchTest.php` with the `noAuthFacadeOrHelper` invariant per D-24.

Purpose: Every subsequent plan (12-02 through 12-07) writes into this module. The arch-test invariant must be green from the moment the skeleton lands so any later commit that touches `Auth::user()` / `auth()` / `request()->user()` / `request()->session()` / `session()` (helper) / `Auth::loginUsingId()` outside the precise file-by-file allow-list breaks CI immediately.

Output: A loaded `Modules/Auth/` module (visible in `php artisan module:list`), a working `noAuthFacadeOrHelper` arch test pinned to the empty-allow-list state, and a documented `Modules/Auth/` namespace in `composer.json` autoload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-multi-user-activation/12-CONTEXT.md
@.planning/phases/12-multi-user-activation/12-PATTERNS.md
@.planning/phases/12-multi-user-activation/12-VALIDATION.md

<interfaces>
<!-- Existing arch-test patterns the new invariant clones; see PATTERNS.md "tests/Contracts/BoundaryArchTest.php (APPENDED)" -->

From tests/Contracts/BoundaryArchTest.php (existing analogs):
- `noFacadeCallsFromCoreConsoleCommands` (lines 926-965) — RecursiveIteratorIterator + preg_replace comment-stripping + preg_match banned-pattern; allow-list is implicit (scoped to `Modules/Core/Internal/Console`).
- `noLaravelGlobalHelpersInCoreConsoleCommands` (lines 967-1028) — same loop with explicit banned-functions array + `(?<![>:])\\b(...)\\s*\\(` lookbehind regex to distinguish helper calls from method calls.
- Comment stripping: `preg_replace('#/\\*.*?\\*/|//[^\\n]*#s', '', $contents)` — preserve PHPDoc `@see Auth::user()` references without false positives.

D-24 ALLOW-LIST (precise relative paths; Plan 12-01 lands the FULL list because subsequent plans add files into these exact paths):
- Modules/Auth/Public/Actions/LoginAction.php
- Modules/Auth/Public/Actions/SignupAction.php
- Modules/Auth/Public/Actions/LogoutAction.php
- Modules/Auth/Public/Actions/ResetPasswordAction.php
- Modules/Auth/Public/Actions/RegenerateRecoveryCodesAction.php
- Modules/Auth/Public/Actions/ImpersonateUserAction.php
- Modules/Auth/Public/Actions/EndImpersonationAction.php
- Modules/Auth/Public/Actions/AddUserAction.php
- Modules/Auth/Internal/Fortify/FortifyServiceProvider.php
- Modules/Auth/Internal/Fortify/Authenticator.php
- Modules/Auth/Internal/Http/Middleware/ImpersonationBannerMiddleware.php

From `ModulePrioritiesArchTest.php` (line ~52 — existing rule): "Core has strictly the lowest priority"; Auth must be `>= 1`. Per PATTERNS.md §Pattern Assignment "Modules/Auth/module.json" — set `priority: 1`.

From `Modules/Categorization/module.json`:
```json
{
    "name": "Categorization",
    "alias": "categorization",
    "description": "...",
    "keywords": [...],
    "priority": 4,
    "providers": ["Modules\\Categorization\\Providers\\CategorizationServiceProvider"],
    "files": []
}
```

From `Modules/Categorization/Providers/CategorizationServiceProvider.php` (boot pattern, lines 67-83):
- `loadMigrationsFrom(__DIR__.'/../Database/Migrations')`
- `loadRoutesFrom(__DIR__.'/../Routes/web.php')`
- `loadRoutesFrom(__DIR__.'/../Routes/console.php')`
- `loadViewsFrom(__DIR__.'/../Resources/views', 'auth')`

</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Modules/Auth/ skeleton (module.json, service provider, dirs, empty route files)</name>
  <files>
    Modules/Auth/module.json,
    Modules/Auth/Providers/AuthServiceProvider.php,
    Modules/Auth/Routes/web.php,
    Modules/Auth/Routes/console.php,
    Modules/Auth/Public/Actions/.gitkeep,
    Modules/Auth/Internal/Fortify/.gitkeep,
    Modules/Auth/Internal/Http/Livewire/.gitkeep,
    Modules/Auth/Internal/Http/Middleware/.gitkeep,
    Modules/Auth/Internal/Recovery/.gitkeep,
    Modules/Auth/Internal/Console/.gitkeep,
    Modules/Auth/Internal/Listeners/.gitkeep,
    Modules/Auth/Models/.gitkeep,
    Modules/Auth/Database/Migrations/.gitkeep,
    Modules/Auth/Resources/views/livewire/.gitkeep,
    Modules/Auth/Resources/views/partials/.gitkeep,
    Modules/Auth/tests/Feature/.gitkeep,
    Modules/Auth/tests/Unit/.gitkeep,
    composer.json,
    modules_statuses.json
  </files>
  <read_first>
    Modules/Categorization/module.json,
    Modules/Categorization/Providers/CategorizationServiceProvider.php,
    Modules/Core/Providers/CoreServiceProvider.php,
    composer.json,
    modules_statuses.json,
    .planning/phases/12-multi-user-activation/12-PATTERNS.md
  </read_first>
  <action>
Create the `Modules/Auth/` module skeleton mirroring the Categorization module shape.

`Modules/Auth/module.json` per PATTERNS.md §"Modules/Auth/module.json": name `Auth`, alias `auth`, description "Authentication, recovery codes, password reset, and impersonation — DI-only auth surface.", keywords `["auth", "recovery-codes", "impersonation"]`, `priority: 1` (loads immediately after Core which is priority 0), providers `["Modules\\Auth\\Providers\\AuthServiceProvider"]`, `files: []`.

`Modules/Auth/Providers/AuthServiceProvider.php` per PATTERNS.md §"AuthServiceProvider.php" analog `Modules/Categorization/Providers/CategorizationServiceProvider.php` lines 48-83. Namespace `Modules\Auth\Providers`. Final class extending `Illuminate\Support\ServiceProvider`. In `register(): void` leave a `// Bindings added by later Phase 12 plans (12-03 onward)` placeholder comment. In `boot(Illuminate\Contracts\Events\Dispatcher $events, Livewire\LivewireManager $livewire): void` call `loadMigrationsFrom(__DIR__.'/../Database/Migrations')`, `loadRoutesFrom(__DIR__.'/../Routes/web.php')`, `loadRoutesFrom(__DIR__.'/../Routes/console.php')`, `loadViewsFrom(__DIR__.'/../Resources/views', 'auth')`. No Livewire components / actions / listeners registered yet — later plans append to this provider.

`Modules/Auth/Routes/web.php`: Single empty `Route::middleware(['web'])->group(static function (): void { /* Routes added by later Phase 12 plans (12-03 onward) */ });`. Use `Illuminate\Support\Facades\Route` import (route-file carve-out is conventional per PATTERNS.md note).

`Modules/Auth/Routes/console.php`: Empty file `<?php declare(strict_types=1);` plus a single-line comment placeholder for the three CLI commands wired in 12-05.

Create `.gitkeep` files for empty directories so the tree exists in git.

`composer.json`: Append `"Modules\\Auth\\": "Modules/Auth/"` to `autoload.psr-4` (preserve alphabetical order, currently Categorization → Chains → Core → ...). Append `"Modules\\Auth\\Tests\\": "Modules/Auth/tests/"` to `autoload-dev.psr-4` matching the pattern used for other modules.

`modules_statuses.json`: Add `"Auth": true` entry (alphabetical) so nwidart/laravel-modules activates it.

Run `composer dump-autoload` after the file changes. Run `php artisan module:list` to verify `Auth | true | local | 1` appears.

Class-level docblock on `AuthServiceProvider`: describes the current state ("Service provider for the Auth module; loads migrations, routes, and views. Concrete bindings (actions, services, Livewire components) are added by the relevant Phase 12 plan."). NO `.planning/` / `Phase 12` / `D-NN` references per CLAUDE.md C-09 + user-memory `feedback_codebase_gsd_agnostic.md` rules.
  </action>
  <verify>
    <automated>php artisan module:list 2>&1 | grep -E '^\| Auth\s+\| true\s+\| local\s+\| 1' &amp;&amp; composer dump-autoload &amp;&amp; php -r 'require "vendor/autoload.php"; var_dump(class_exists("Modules\\Auth\\Providers\\AuthServiceProvider"));' | grep 'bool(true)'</automated>
  </verify>
  <acceptance_criteria>
    - File: `Modules/Auth/module.json` exists; `grep '"priority": 1' Modules/Auth/module.json` matches.
    - File: `Modules/Auth/Providers/AuthServiceProvider.php` exists; class is `final` and extends `Illuminate\Support\ServiceProvider`.
    - File: `Modules/Auth/Routes/web.php` exists; `grep "Route::middleware(\\['web'\\])->group" Modules/Auth/Routes/web.php` matches.
    - Source: `composer.json` `autoload.psr-4` block contains `"Modules\\\\Auth\\\\": "Modules/Auth/"`.
    - Source: `modules_statuses.json` contains `"Auth": true`.
    - CLI: `php artisan module:list` lists Auth with priority 1 and enabled state.
    - Source: NO occurrence of `.planning`, `PLAN.md`, `RESEARCH.md`, or `D-\d\d` in any new file (verified by `grep -rn '\.planning\|PLAN\.md\|RESEARCH\.md\|D-[0-9][0-9]' Modules/Auth/ | wc -l` returning `0`).
    - Auth's priority is strictly greater than Core's priority (verified by running existing `pest tests/Contracts/ModulePrioritiesArchTest.php` — must stay green).
  </acceptance_criteria>
  <done>Module loads, priority is 1, autoload works, no GSD leakage in new files, existing `ModulePrioritiesArchTest` stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend BoundaryArchTest with noAuthFacadeOrHelper invariant + D-24 allow-list</name>
  <files>tests/Contracts/BoundaryArchTest.php</files>
  <read_first>
    tests/Contracts/BoundaryArchTest.php,
    .planning/phases/12-multi-user-activation/12-PATTERNS.md,
    .planning/phases/12-multi-user-activation/12-CONTEXT.md
  </read_first>
  <action>
Append a new Pest test block `noAuthFacadeOrHelper` to `tests/Contracts/BoundaryArchTest.php`, cribbing verbatim from the existing `noLaravelGlobalHelpersInCoreConsoleCommands` (lines 967-1028) shape.

Scope: `base_path('Modules')` recursive scan (all modules, not just Auth).

Banned symbols (combine in a single regex with `|` alternation; copy the comment-stripping pre-pass from the analog):
- `Illuminate\\\\Support\\\\Facades\\\\Auth\\b` (the import path of the facade)
- `Auth::user\s*\(`
- `Auth::id\s*\(`
- `Auth::loginUsingId\s*\(`
- `(?<![>:])\\bauth\s*\(` (the helper, distinguishes from method calls)
- `request\s*\(\s*\)\s*->\s*user\s*\(`
- `request\s*\(\s*\)\s*->\s*session\s*\(`
- `(?<![>:])\\bsession\s*\(` (the helper, NOT `$this->session->get(` — the lookbehind handles this)

Skip rules (path filters applied before the regex):
- Skip files matching `'/tests/'` in path (test helpers may legitimately call `actingAs($user)` which Pest provides — these are NOT module test files; they live under `Modules/*/tests/`).
- Skip files matching `'/Database/Migrations/'` (migrations are anonymous classes outside the rule per PATTERNS.md analog note).
- Skip files inside the D-24 allow-list array (exact relative-path match against `$path` after `realpath` normalization to `Modules/Auth/...`).

D-24 allow-list (declare as a `private static $allowList = [ ... ]` const at the top of the test file or as a local array inside the test closure):
```
Modules/Auth/Public/Actions/LoginAction.php
Modules/Auth/Public/Actions/SignupAction.php
Modules/Auth/Public/Actions/LogoutAction.php
Modules/Auth/Public/Actions/ResetPasswordAction.php
Modules/Auth/Public/Actions/RegenerateRecoveryCodesAction.php
Modules/Auth/Public/Actions/ImpersonateUserAction.php
Modules/Auth/Public/Actions/EndImpersonationAction.php
Modules/Auth/Public/Actions/AddUserAction.php
Modules/Auth/Internal/Fortify/FortifyServiceProvider.php
Modules/Auth/Internal/Fortify/Authenticator.php
Modules/Auth/Internal/Http/Middleware/ImpersonationBannerMiddleware.php
```

NONE of these files exist yet — that's expected. The allow-list is forward-declared; Plans 12-03 through 12-07 create the files. Plan 12-01's job is to land the empty allow-list with the precise paths so subsequent commits flow through without needing arch-test edits.

Comment block above the test (visible to future readers): brief description of what is forbidden and that the allow-list is per-file precise — never a glob — and adding to it requires code-review justification. Do not reference `.planning/` or `D-24` codenames per CLAUDE.md C-09; the rule may say "see Auth module module-level docblock for rationale" instead.

Assertion shape: `expect($hits)->toBe([], "Modules/* may not call Auth facade / auth helper / request()->user / request()->session / session() helper / Auth::loginUsingId outside the allow-list. Offenders:\n  " . implode("\n  ", $hits));`.

After appending, run `pest tests/Contracts/BoundaryArchTest.php --filter='noAuthFacadeOrHelper'` — must pass (the codebase today contains exactly one `FortifyServiceProvider.php` under `Modules/Core/Internal/Providers/` that uses `Auth::loginUsingId()` per PATTERNS.md `Modules/Core/Internal/Providers/FortifyServiceProvider.php` lines 37-53 — but that file is **not** on the allow-list. Two options:
  (a) Plan 12-01 also temporarily adds `Modules/Core/Internal/Providers/FortifyServiceProvider.php` to the allow-list (Plan 12-03 MOVES this file to `Modules/Auth/Internal/Fortify/FortifyServiceProvider.php` and removes the temporary entry).
  (b) Plan 12-01 first inspects whether the legacy file actually calls these symbols today; if it does, take option (a).
Pick option (a) per the cleaner ordering. Document the temporary entry inline with a comment: `// TEMP: remove when Plan 12-03 moves this file to Modules/Auth/Internal/Fortify/`.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noAuthFacadeOrHelper'</automated>
  </verify>
  <acceptance_criteria>
    - File: `tests/Contracts/BoundaryArchTest.php` contains the string `noAuthFacadeOrHelper`.
    - Test: `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noAuthFacadeOrHelper'` exits 0.
    - Source: the allow-list inside the test contains exactly the 11 paths listed in D-24 (verified by `grep -c "Modules/Auth/" tests/Contracts/BoundaryArchTest.php` returning `>= 11`).
    - Behavior: introducing `Auth::user()` in any `Modules/Core/Public/*.php` file makes the test fail (verified manually OR by a follow-up commit; not a CI gate). Spot-check with a temp file that touches `Auth::user()` in `Modules/Core/Public/Test.php`, run the test, expect failure, delete the test file.
    - Source: NO occurrence of `.planning`, `PLAN.md`, `RESEARCH.md`, or `D-\d\d` codenames in the appended test code.
    - Existing arch tests still pass: `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` exits 0.
  </acceptance_criteria>
  <done>noAuthFacadeOrHelper invariant green, allow-list pre-populated with 11 Auth-module paths plus the temporary Core/Internal/Providers/FortifyServiceProvider.php entry, no GSD codename leakage.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module loader → application | nwidart/laravel-modules autoload registers Modules\Auth\* — must load with the correct priority and not before Core |
| arch-test → CI | `noAuthFacadeOrHelper` is the gate that enforces D-24 at every commit |

## STRIDE Threat Register (ASVS V14 Configuration · V4 Access Control)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | Modules/Auth/module.json | mitigate | priority 1 enforced by ModulePrioritiesArchTest (Core stays priority 0 < Auth) |
| T-12-01-02 | Elevation of Privilege | future Modules/*/*.php | mitigate | noAuthFacadeOrHelper invariant rejects `Auth::loginUsingId(` smuggled outside the D-24 allow-list (ASVS V4 Access Control) |
| T-12-01-03 | Information Disclosure | tests/Contracts/BoundaryArchTest.php | accept | allow-list size grows over time; per-file precise list + code-review gate keeps it auditable |
| T-12-01-04 | Tampering | composer.lock / modules_statuses.json | accept | Phase 12 only adds the Auth namespace; no third-party deps added — package legitimacy audit not required |

No `high` severity threats. Plan blocks on `noAuthFacadeOrHelper` green only.

</threat_model>

<verification>
- Module loaded: `php artisan module:list` shows Auth/true/local/1.
- Autoload resolves the provider class.
- `vendor/bin/pest tests/Contracts/BoundaryArchTest.php` passes (all invariants, including the new one).
- `vendor/bin/pest tests/Contracts/ModulePrioritiesArchTest.php` passes.
- No GSD codename leakage in any new file (`grep -rn '\.planning\|PLAN\.md\|RESEARCH\.md\|D-[0-9][0-9]' Modules/Auth/`).
</verification>

<success_criteria>
- Modules/Auth/ skeleton exists; service provider boots; module listed by nwidart/laravel-modules with priority 1.
- noAuthFacadeOrHelper invariant green with the 11-path allow-list plus the temporary Core entry.
- All existing arch tests still green.
- Larastan L10 strict + Pint clean on the new files.
</success_criteria>

<output>
Create `.planning/phases/12-multi-user-activation/12-01-SUMMARY.md` when done. Record: files created, allow-list contents, the temporary Core FortifyServiceProvider entry (for Plan 12-03 to remove), composer/modules-status edits, and the arch-test result.
</output>
