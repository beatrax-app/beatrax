---
phase: 12-multi-user-activation
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - Modules/Auth/Internal/Fortify/FortifyServiceProvider.php
  - Modules/Auth/Public/Actions/LoginAction.php
  - Modules/Auth/Public/Actions/LogoutAction.php
  - Modules/Auth/Internal/Http/Livewire/LoginPage.php
  - Modules/Auth/Resources/views/livewire/login-page.blade.php
  - Modules/Auth/Routes/web.php
  - Modules/Auth/Providers/AuthServiceProvider.php
  - Modules/Core/Providers/CoreServiceProvider.php
  - Modules/Core/Internal/Providers/FortifyServiceProvider.php
  - tests/Contracts/BoundaryArchTest.php
  - config/fortify.php
  - config/auth.php
  - config/session.php
  - Modules/Auth/tests/Feature/LoginPageTest.php
autonomous: true
requirements: [MULTI-02]
tags: [auth, fortify, login, livewire, volt, session]

must_haves:
  truths:
    - "Fortify is wired with `username` as the login identifier (not `email`)."
    - "Fortify features array does NOT contain `Features::resetPasswords()`, `Features::registration()`, `Features::emailVerification()`, or `Features::twoFactorAuthentication()`."
    - "The `EnsureLoginIsNotThrottled` middleware is removed from the Fortify authentication pipeline (D-12)."
    - "Manually-seeded user with `username` + bcrypt-hashed `password` can POST to `/login` and receive a 302 redirect + a session cookie."
    - "Wrong credentials show the generic copy `Username or password is incorrect.` per UI-SPEC."
    - "`/logout` POST signs the user out and redirects to `/login`."
    - "SESSION_DRIVER is `database` (D-26) and existing `sessions` table is used."
    - "Remember-me checkbox sets the `remember_web_*` cookie with the standard Laravel lifetime (D-27)."
    - "The `BoundaryArchTest::noAuthFacadeOrHelper` allow-list reflects the moved FortifyServiceProvider (Core entry removed; Auth entry now real)."
    - "Codebase remains GSD-agnostic — no .planning/ / PLAN.md / RESEARCH.md / D-NN references."
  artifacts:
    - path: "Modules/Auth/Internal/Fortify/FortifyServiceProvider.php"
      provides: "Fortify configuration: username login, no rate limit, no email reset, no registration"
      contains: "Fortify::authenticateUsing"
    - path: "Modules/Auth/Public/Actions/LoginAction.php"
      provides: "constructor-DI action that returns success/failure outcome for credential check"
      contains: "final class LoginAction"
    - path: "Modules/Auth/Internal/Http/Livewire/LoginPage.php"
      provides: "Livewire 4 component rendering /login form per UI-SPEC"
      contains: "final class LoginPage extends Component"
    - path: "Modules/Auth/Resources/views/livewire/login-page.blade.php"
      provides: "blade template per UI-SPEC sign-in flow"
      contains: "Sign in"
  key_links:
    - from: "Modules/Auth/Internal/Http/Livewire/LoginPage.php"
      to: "Modules/Auth/Public/Actions/LoginAction.php"
      via: "Livewire action-method DI"
      pattern: "LoginAction \\$login"
    - from: "Modules/Auth/Internal/Fortify/FortifyServiceProvider.php"
      to: "Modules/Core/Models/User.php"
      via: "Fortify::authenticateUsing closure looking up by username"
      pattern: "User::query\\(\\)->where\\('username'"
---

<objective>
Move the existing `FortifyServiceProvider` from `Modules/Core/Internal/Providers/` to `Modules/Auth/Internal/Fortify/`, rewrite the authenticator closure for `username` (D-02), drop the rate-limiter (D-12), disable every email-related Fortify feature, and ship the `/login` + `/logout` Volt-class-shape Livewire pages per UI-SPEC.

Purpose: This plan turns the auth subsystem ON. After this lands, a manually-seeded user with a username+password can sign in and reach the dashboard. Subsequent plans (12-04 signup, 12-05 reset/recovery, 12-07 impersonation) all build atop this baseline.

Output: Working sign-in / sign-out flow; the Fortify provider relocated to the Auth module; the existing legacy Core FortifyServiceProvider deleted; the BoundaryArchTest allow-list updated to reflect the move; comprehensive feature tests proving valid+invalid credential paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-multi-user-activation/12-CONTEXT.md
@.planning/phases/12-multi-user-activation/12-PATTERNS.md
@.planning/phases/12-multi-user-activation/12-UI-SPEC.md
@.planning/phases/12-multi-user-activation/12-VALIDATION.md

<interfaces>
<!-- Existing Fortify provider to be MOVED + rewritten; PATTERNS.md §FortifyServiceProvider -->

Source file at `Modules/Core/Internal/Providers/FortifyServiceProvider.php` (lines 33-53 quoted in PATTERNS.md):
```php
public function boot(Hasher $hasher, RateLimiter $rateLimiter): void
{
    Fortify::loginView('core::auth.login');
    Fortify::authenticateUsing(static function (Request $request) use ($hasher): ?User {
        $email = $request->input('email');
        // ... bcrypt check against where('email', $email)
    });
    // ... rate limiter block at lines 56-63 (drop entire block per D-12)
}
```

Rewrite shape (RESEARCH.md §Pattern 2 + PATTERNS.md §FortifyServiceProvider):
- `Fortify::loginView('auth::livewire.login-page')` — points to the new Auth-namespaced Livewire view, OR (preferred) bypass `Fortify::loginView` entirely and let `Modules/Auth/Routes/web.php` register `Route::get('/login', LoginPage::class)->name('login')` since the project standard is class-based Livewire components (PATTERNS.md Pattern Map LoginPage analog: RulesPage).
- `Fortify::authenticateUsing(static function (Request $request) use ($hasher): ?User { $username = $request->input('username'); $password = $request->input('password'); if (! is_string($username) || ! is_string($password)) return null; $normalized = strtolower(trim($username)); /** @var User|null $user */ $user = User::query()->where('username', $normalized)->first(); if ($user instanceof User && $hasher->check($password, $user->password)) return $user; return null; });` — generic null on failure (no user enumeration; Fortify renders the generic error string per V7 ASVS).
- `Fortify::authenticateThrough(static fn (Request $request) => [\Laravel\Fortify\Actions\AttemptToAuthenticate::class, \Laravel\Fortify\Actions\PrepareAuthenticatedSession::class])` — explicitly drops `EnsureLoginIsNotThrottled` per D-12 + Pitfall 6 (RESEARCH.md lines 1357-1374).
- Constructor `public function boot(Hasher $hasher): void` — drop the `RateLimiter` parameter entirely. Hasher is the only collaborator now.
- Class-level docblock: describes WHAT this provider does today (username-based Fortify pipeline, no rate limiter, no email features). NO mention of the old email pipeline.

LoginPage (PATTERNS.md §LoginPage.php analog `Modules/Categorization/Internal/Http/Livewire/RulesPage.php`):
- `final class LoginPage extends Component`
- NO constructor (PATTERNS.md critical note line 36: "Constructor-free Livewire component").
- Public properties for `$username`, `$password`, `$rememberMe` (default `true` per UI-SPEC), `$flashMessage`.
- Action method `submit(LoginAction $login, AuthManager $auth, UrlGenerator $urls): void` — DI on the method per PATTERNS.md.
- `render(ViewFactory $views): View` using `$view->extends('layouts.app', ['title' => 'Sign in · diederik'])` with `@phpstan-ignore-next-line method.notFound` comment (PATTERNS.md §"Phase 12 difference from research §Pattern 1" — match the codebase's older $view->extends pattern, NOT the research draft's `#[Layout]`).

LoginAction (PATTERNS.md §LoginAction.php analog `CreateCategorizationRule.php`):
- `final class LoginAction`
- Constructor: `public function __construct(private readonly Hasher $hasher, private readonly AuthManager $auth)`.
- `__invoke(string $usernameInput, string $password, bool $rememberMe): bool` — returns true on success (caller redirects), false on failure (caller renders generic error). LoginAction is on the D-24 allow-list so it MAY call `$this->auth->guard()->login($user, $rememberMe)` — this is the only file that legitimately mutates the guard at sign-in.
- Inside `__invoke`: normalize `strtolower(trim($usernameInput))`; lookup `User::query()->where('username', $normalized)->first()`; `$this->hasher->check($password, $user->password)`; on success `$this->auth->guard()->login($user, $rememberMe);` + return true.
- NO `system_alerts` emission on failure for sign-in (UI-SPEC line 148 — generic copy, never per-field; never differentiated). Failure path returns false and the Livewire component sets `$this->flashMessage` with the generic copy.

LogoutAction (PATTERNS.md §LogoutAction):
- `final class LogoutAction` with `public function __construct(private readonly AuthManager $auth, private readonly SessionContract $session)`.
- `__invoke(): void` calls `$this->auth->guard()->logout()`, `$this->session->invalidate()`, `$this->session->regenerateToken()` per Laravel session hygiene.
- On the D-24 allow-list.

UI-SPEC §Sign-in flow (lines 197-201) — verbatim copy: H1 `Sign in`, subhead `Sign in to continue.`, remember-me label `Stay signed in on this device`, helper link `Lost your password? Use a recovery code.` → `/reset-password`. Error line `Username or password is incorrect.` `text-sm text-rose-600`.

UI-SPEC §Typography/Color sections — emerald primary button, slate input chrome, `text-sm text-rose-600` for inline errors.

Configuration (RESEARCH.md §Pattern 2 lines 583-603):
- `config/fortify.php`: features array MUST be `[]` for Phase 12 — NO `Features::registration()` (custom `/signup` route in Plan 12-04 enforces the `User::count() === 0` gate per D-03), NO `Features::resetPasswords()` (recovery-codes flow in 12-05), NO `Features::emailVerification()` (no email), NO `Features::twoFactorAuthentication()` (deferred per CONTEXT.md). Set `'username' => 'username'` if Fortify exposes a config knob for the auth column name; otherwise the closure already takes care of it.
- `config/auth.php`: ensure `providers.users.model = Modules\Core\Models\User::class` (already the case). The model's auth identifier is implicit via `getAuthIdentifierName()` defaulting to `id`; no change.
- `config/session.php`: `'driver' => env('SESSION_DRIVER', 'database')` per D-26. Verify the existing `sessions` table is still referenced; `'table' => 'sessions'`. Lifetime stays the project default (or set per UI-SPEC's "5-year remember-me" — that's controlled separately via the remember-me cookie, not session lifetime).

BoundaryArchTest update:
- Remove the temporary entry `Modules/Core/Internal/Providers/FortifyServiceProvider.php` from the allow-list (the file is being deleted).
- Verify the precise entries `Modules/Auth/Internal/Fortify/FortifyServiceProvider.php` and `Modules/Auth/Public/Actions/LoginAction.php`, `Modules/Auth/Public/Actions/LogoutAction.php` are already present (they were forward-declared in Plan 12-01).

CoreServiceProvider update:
- Remove the line `$this->app->register(FortifyServiceProvider::class);` per PATTERNS.md note. Delete the corresponding `use Modules\Core\Internal\Providers\FortifyServiceProvider;` import.

AuthServiceProvider update:
- In `register()` add `$this->app->register(\Modules\Auth\Internal\Fortify\FortifyServiceProvider::class);`.
- Register `LoginAction`, `LogoutAction` as singletons via `$this->app->singleton(...)` per PATTERNS.md §AuthServiceProvider.
- In `boot()` register Livewire component: `$livewire->component('auth.login-page', LoginPage::class);`.

Route registration (PATTERNS.md §Routes/web.php):
- Guest group (no `auth` middleware): `Route::middleware(['web', 'guest'])->group(function () { Route::get('/login', LoginPage::class)->name('login'); });` — using class-based component routing.
- Auth group: `Route::middleware(['web', 'auth'])->group(function () { Route::post('/logout', /* LogoutAction invocation */)->name('logout'); });`. The POST /logout route handler is a closure that resolves `LogoutAction` via DI and invokes it; closure-style handlers are allowed per `Modules/Core/Routes/web.php` precedent.

</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move FortifyServiceProvider to Modules/Auth/Internal/Fortify/; rewrite for username; drop rate limiter; configure no-email features</name>
  <files>
    Modules/Auth/Internal/Fortify/FortifyServiceProvider.php,
    Modules/Core/Internal/Providers/FortifyServiceProvider.php,
    Modules/Core/Providers/CoreServiceProvider.php,
    Modules/Auth/Providers/AuthServiceProvider.php,
    tests/Contracts/BoundaryArchTest.php,
    config/fortify.php,
    config/session.php
  </files>
  <behavior>
    - Test: Booting the application registers Fortify::authenticateUsing with a closure that takes a Request and returns a User|null based on `username` (not `email`).
    - Test: `config('fortify.features')` returns an empty array `[]` or an array NOT containing `Features::resetPasswords()`, `Features::registration()`, `Features::emailVerification()`, `Features::twoFactorAuthentication()`.
    - Test: `config('session.driver') === 'database'` in the testing environment.
    - Test: `pest tests/Contracts/BoundaryArchTest.php --filter='noAuthFacadeOrHelper'` exits 0 after the file moves and allow-list updates.
    - Test: Existing v1.0 test that referenced `Modules\Core\Internal\Providers\FortifyServiceProvider` (if any) — none expected per PATTERNS.md; confirm via `grep -rn 'Modules\\\\Core\\\\Internal\\\\Providers\\\\FortifyServiceProvider'` returning 0 lines after the move.
  </behavior>
  <read_first>
    Modules/Core/Internal/Providers/FortifyServiceProvider.php,
    Modules/Core/Providers/CoreServiceProvider.php,
    Modules/Auth/Providers/AuthServiceProvider.php,
    tests/Contracts/BoundaryArchTest.php,
    config/fortify.php,
    config/session.php,
    .planning/phases/12-multi-user-activation/12-PATTERNS.md
  </read_first>
  <action>
Move `Modules/Core/Internal/Providers/FortifyServiceProvider.php` to `Modules/Auth/Internal/Fortify/FortifyServiceProvider.php` and rewrite per PATTERNS.md §FortifyServiceProvider + RESEARCH.md §Pattern 2.

New file `Modules/Auth/Internal/Fortify/FortifyServiceProvider.php`:
- Namespace `Modules\Auth\Internal\Fortify`.
- Imports: `Illuminate\Contracts\Hashing\Hasher`, `Illuminate\Http\Request`, `Illuminate\Support\ServiceProvider`, `Laravel\Fortify\Fortify`, `Laravel\Fortify\Actions\AttemptToAuthenticate`, `Laravel\Fortify\Actions\PrepareAuthenticatedSession`, `Modules\Core\Models\User`.
- `final class FortifyServiceProvider extends ServiceProvider`.
- `public function register(): void {}` — no DI bindings needed (Fortify auto-binds).
- `public function boot(Hasher $hasher): void {` — no RateLimiter parameter (D-12).
  - `Fortify::authenticateUsing(static function (Request $request) use ($hasher): ?User { $username = $request->input('username'); $password = $request->input('password'); if (! is_string($username) || ! is_string($password)) return null; $normalized = strtolower(trim($username)); /** @var User|null $user */ $user = User::query()->where('username', $normalized)->first(); if ($user instanceof User && $hasher->check($password, $user->password)) return $user; return null; });`
  - `Fortify::authenticateThrough(static fn (Request $request) => [AttemptToAuthenticate::class, PrepareAuthenticatedSession::class]);` — explicit list, no rate-limit middleware.
- Class-level docblock: present-state description ("Configures Fortify to authenticate the active user against `users.username` via the injected `Hasher` contract. The pipeline omits the throttle middleware by design; per CLAUDE.md the local-only deployment relies on bcrypt's natural cost factor instead.") — NO mention of "email" or "previous implementation".

Delete `Modules/Core/Internal/Providers/FortifyServiceProvider.php` (`git rm`).

Update `Modules/Core/Providers/CoreServiceProvider.php`:
- Remove import `use Modules\Core\Internal\Providers\FortifyServiceProvider;` (line ~22).
- Remove the `$this->app->register(FortifyServiceProvider::class);` line (line ~50).
- Confirm no other Core code paths reference the deleted file.

Update `Modules/Auth/Providers/AuthServiceProvider.php`:
- Add `use Modules\Auth\Internal\Fortify\FortifyServiceProvider;`.
- In `register()` add `$this->app->register(FortifyServiceProvider::class);`.

Update `config/fortify.php`:
- `'features' => []` — explicit empty array. Add an inline comment in the config file: `/* Features intentionally empty: signup uses a custom route gate; password reset uses recovery codes; no email verification or 2FA in v2.0. */`. (Comment language must NOT reference Phase 12 / D-NN.)
- `'username' => 'username'` if Fortify's config exposes that key (check current Fortify config shape).

Update `config/session.php`:
- `'driver' => env('SESSION_DRIVER', 'database')` — confirm this is the current default; if currently `file` or other, change to `database`.

Update `tests/Contracts/BoundaryArchTest.php`:
- Remove the temporary `Modules/Core/Internal/Providers/FortifyServiceProvider.php` entry from the D-24 allow-list (added in Plan 12-01).
- Verify `Modules/Auth/Internal/Fortify/FortifyServiceProvider.php` is in the allow-list (it should be — pre-declared in 12-01).
- Run `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noAuthFacadeOrHelper'` to confirm green.

No GSD codename leakage. Class-level docblocks rewritten wholesale; no diffs that say "moved from Core" or "Phase 12".
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noAuthFacadeOrHelper' &amp;&amp; php -r 'require "vendor/autoload.php"; require "bootstrap/app.php"; var_dump(class_exists("Modules\\Auth\\Internal\\Fortify\\FortifyServiceProvider")); var_dump(! class_exists("Modules\\Core\\Internal\\Providers\\FortifyServiceProvider"));' | grep -c 'bool(true)' | grep -E '^2$'</automated>
  </verify>
  <acceptance_criteria>
    - File: `Modules/Auth/Internal/Fortify/FortifyServiceProvider.php` exists.
    - File: `Modules/Core/Internal/Providers/FortifyServiceProvider.php` does NOT exist (deleted).
    - Source: new file contains `Fortify::authenticateUsing` and references `'username'` (not `'email'`).
    - Source: new file does NOT contain `RateLimiter`, `EnsureLoginIsNotThrottled`, or `throttle` references.
    - Source: `config/fortify.php` `features` array is empty `[]` or excludes the four banned features.
    - Source: `config/session.php` driver is `database`.
    - Test: `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noAuthFacadeOrHelper'` exits 0.
    - Source: `grep -rn 'Modules\\\\Core\\\\Internal\\\\Providers\\\\FortifyServiceProvider' Modules/ config/ 2>/dev/null | wc -l` returns 0.
    - Source: NO `.planning`, `PLAN.md`, `RESEARCH.md`, `D-\d\d`, "Phase 12", "previously", "used to be", "moved from" in the new file's docblock.
  </acceptance_criteria>
  <done>Fortify provider lives in Auth module; rate limiter removed; email features off; arch test green; no facade smuggling; no GSD codename leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LoginAction + LogoutAction + LoginPage (Livewire) + Blade template + /login + /logout routes + LoginPage feature test</name>
  <files>
    Modules/Auth/Public/Actions/LoginAction.php,
    Modules/Auth/Public/Actions/LogoutAction.php,
    Modules/Auth/Internal/Http/Livewire/LoginPage.php,
    Modules/Auth/Resources/views/livewire/login-page.blade.php,
    Modules/Auth/Routes/web.php,
    Modules/Auth/Providers/AuthServiceProvider.php,
    Modules/Auth/tests/Feature/LoginPageTest.php
  </files>
  <behavior>
    - Test: GET `/login` returns 200 + renders the H1 `Sign in` + subhead `Sign in to continue.` + the helper link `Lost your password? Use a recovery code.` (UI-SPEC verbatim).
    - Test: GET `/login` is publicly accessible (no `auth` middleware redirect).
    - Test: Livewire `submit` action with valid `username` + `password` for a seeded user → `assertRedirect('/')` (or wherever the dashboard lives) + the auth guard `Auth::check()` returns true (verified by accessing an authenticated route).
    - Test: Livewire `submit` with wrong password → no redirect; `$flashMessage` contains `Username or password is incorrect.`.
    - Test: Livewire `submit` with username that does NOT exist → SAME flash message (no enumeration).
    - Test: Livewire `submit` with `$rememberMe = true` sets the `remember_web_*` cookie on the response.
    - Test: POST `/logout` from an authenticated session → 302 to `/login` + `Auth::check()` returns false.
    - Test: Username is normalised to lowercase before lookup — seeded user `username = 'alice'`, submit with `'Alice'` → success (case-insensitive login).
  </behavior>
  <read_first>
    Modules/Categorization/Internal/Http/Livewire/RulesPage.php,
    Modules/Categorization/Public/Actions/CreateCategorizationRule.php,
    Modules/Categorization/tests/Feature/AssignCategoryTest.php,
    Modules/Core/Resources/views/auth/login-form.blade.php,
    .planning/phases/12-multi-user-activation/12-PATTERNS.md,
    .planning/phases/12-multi-user-activation/12-UI-SPEC.md
  </read_first>
  <action>
Create `Modules/Auth/Public/Actions/LoginAction.php` per PATTERNS.md §LoginAction analog `CreateCategorizationRule.php`:
- `final class LoginAction`
- Constructor: `public function __construct(private readonly Hasher $hasher, private readonly AuthManager $auth) {}`.
- `public function __invoke(string $usernameInput, string $password, bool $rememberMe): bool` — normalize `strtolower(trim($usernameInput))`, lookup `User::query()->where('username', $normalized)->first()`, on null OR hash mismatch return false, on match `$this->auth->guard()->login($user, $rememberMe);` and return true.
- Class docblock: "Single permissible write path for credential-based sign-in. Normalises the supplied username and verifies the bcrypt password hash via the injected `Hasher`. On success, logs the user in via the active guard; on failure, returns false without distinguishing missing user from wrong password."
- File is on the D-24 allow-list (it legitimately calls `$this->auth->guard()->login()`).

Create `Modules/Auth/Public/Actions/LogoutAction.php`:
- `final class LogoutAction`
- Constructor: `public function __construct(private readonly AuthManager $auth, private readonly \Illuminate\Contracts\Session\Session $session) {}`.
- `public function __invoke(): void` calls `$this->auth->guard()->logout()`, `$this->session->invalidate()`, `$this->session->regenerateToken()` — standard Laravel session hygiene to prevent session fixation.
- Class docblock: "Single permissible sign-out path. Invalidates and regenerates the session token to prevent fixation."
- File is on the D-24 allow-list.

Create `Modules/Auth/Internal/Http/Livewire/LoginPage.php` per PATTERNS.md §LoginPage analog `RulesPage.php`:
- `namespace Modules\Auth\Internal\Http\Livewire`.
- `final class LoginPage extends Component`.
- Public properties: `public string $username = ''; public string $password = ''; public bool $rememberMe = true; public string $flashMessage = '';`.
- NO constructor (PATTERNS.md critical: Livewire components are constructor-free).
- Action method `public function submit(LoginAction $login, UrlGenerator $urls): void` — call `$login($this->username, $this->password, $this->rememberMe)`; on true call `$this->redirect($urls->route('dashboard'), navigate: false)`; on false `$this->flashMessage = 'Username or password is incorrect.'`; reset `$this->password = ''` (never echo plaintext password back into the wire snapshot).
- `public function render(ViewFactory $views): View` per PATTERNS.md $view->extends('layouts.app') shape with `@phpstan-ignore-next-line method.notFound` comment + `['title' => 'Sign in · diederik']`.
- Class docblock per the RulesPage template style: present-state description, mention the constructor-free Livewire convention.

Create `Modules/Auth/Resources/views/livewire/login-page.blade.php` per UI-SPEC §Sign-in flow (lines 197-201) + UI-SPEC §Copy contract row "Primary CTA — `/login`" (verbatim text). Use the calm-Linear chrome from PATTERNS.md §"View-template chrome" + `Modules/Core/Resources/views/auth/login-form.blade.php` as the analog:
- Outer wrapper: `min-h-screen flex items-center justify-center bg-white`.
- Inner container: `max-w-md mx-auto space-y-6 px-6`.
- Header: `<header class="space-y-1"><h1 class="text-2xl font-semibold text-slate-900 tracking-tight">Sign in</h1><p class="text-sm text-slate-600">Sign in to continue.</p></header>`.
- Form: `<form wire:submit="submit" class="space-y-4">` with `<label for="username">Username</label>` + text input with `autocomplete="username"`; `<label for="password">Password</label>` + password input with `autocomplete="current-password"`; remember-me checkbox checked-by-default with label `Stay signed in on this device`; submit button `class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-medium rounded-md py-2 text-sm ..."` text `Sign in`; below-form helper text-link `<a href="{{ route('password.reset') }}" class="text-sm text-slate-600 hover:text-slate-900 underline underline-offset-2">Lost your password? Use a recovery code.</a>`.
- Error: `@if ($flashMessage) <p class="text-sm text-rose-600">{{ $flashMessage }}</p> @endif`.
- Accessibility per UI-SPEC §Accessibility — every input has paired `<label for="...">`; visible focus state classes.

Update `Modules/Auth/Routes/web.php`:
- Guest group: `Route::middleware(['web', 'guest'])->group(function () { Route::get('/login', LoginPage::class)->name('login'); });`.
- Auth group: `Route::middleware(['web', 'auth'])->group(function () { Route::post('/logout', function (LogoutAction $logout, UrlGenerator $urls) { $logout(); return redirect()->route('login'); })->name('logout'); });`. NOTE: `redirect()` is a global helper — but route files have an implicit carve-out per PATTERNS.md note (the rule scans `Modules/*` for `Illuminate\Support\Facades\*` imports and helper calls; route-file usage of `redirect()` is accepted by precedent in existing route files). VERIFY: if the arch test catches this, replace with `Route::post('/logout', [LogoutAction::class, 'invokeFromRoute'])` and add a method on LogoutAction that returns a RedirectResponse from an injected `UrlGenerator`. Pick whichever passes the arch test cleanly.

Update `Modules/Auth/Providers/AuthServiceProvider.php`:
- `register()`: `$this->app->singleton(LoginAction::class);` `$this->app->singleton(LogoutAction::class);`.
- `boot()`: `$livewire->component('auth.login-page', LoginPage::class);`.

Create `Modules/Auth/tests/Feature/LoginPageTest.php` per PATTERNS.md §"tests/Feature analog" `Modules/Categorization/tests/Feature/AssignCategoryTest.php`:
- `beforeEach` creates a User via the factory with `username = 'alice'` and a known bcrypt password (use the Hasher contract).
- `it('renders the sign-in page with correct copy')` — `$this->get('/login')->assertSeeText('Sign in')->assertSeeText('Sign in to continue.')->assertSeeText('Lost your password? Use a recovery code.')`.
- `it('signs in with valid credentials')` — `Livewire::test(LoginPage::class)->set('username', 'alice')->set('password', 'correct-password')->call('submit')->assertRedirect(route('dashboard'));` (or '/').
- `it('shows generic error on wrong password')` — wrong password → assertSet('flashMessage', 'Username or password is incorrect.').
- `it('shows generic error for nonexistent user')` — same generic copy; no user enumeration.
- `it('username login is case-insensitive')` — seeded `'alice'`, submit `'Alice'` → redirect.
- `it('remember-me sets the remember cookie')` — submit with `$rememberMe = true`, assert the response cookies include one whose name starts with `remember_web_`.
- `it('logout signs the user out')` — `actingAs($user)->post('/logout')->assertRedirect('/login');` then `$this->assertGuest();`.

Apply Pint + Larastan L10 strict to every new file. Verify `composer analyse` + `composer format:check` are clean.
  </action>
  <verify>
    <automated>vendor/bin/pest Modules/Auth/tests/Feature/LoginPageTest.php</automated>
  </verify>
  <acceptance_criteria>
    - File: `Modules/Auth/Public/Actions/LoginAction.php` exists with `final class LoginAction`.
    - File: `Modules/Auth/Public/Actions/LogoutAction.php` exists with `final class LogoutAction`.
    - File: `Modules/Auth/Internal/Http/Livewire/LoginPage.php` exists with `final class LoginPage extends Component`.
    - File: `Modules/Auth/Resources/views/livewire/login-page.blade.php` exists; contains literal text `Sign in`, `Sign in to continue.`, `Stay signed in on this device`, `Lost your password? Use a recovery code.`, `Username or password is incorrect.` (the last conditional on flash).
    - Behavior: GET `/login` returns 200 (verified by `curl -s -o /dev/null -w '%{http_code}' http://diederik.test/login` returning `200`).
    - Behavior: Livewire valid-credential submit redirects + sets session cookie.
    - Behavior: All 7 LoginPageTest cases pass.
    - Behavior: Username `Alice` and `alice` resolve to the same user (case-insensitive).
    - Source: `LoginAction.php` constructor takes `Hasher` + `AuthManager` (constructor DI, no facade).
    - Source: NO occurrence of `Auth::user()`, `auth()`, `request()->user(`, `request()->session(`, `Hash::check(` (helper) in any new file (the contract `Hasher::check` is OK).
    - Source: NO `.planning`, `PLAN.md`, `RESEARCH.md`, `D-\d\d` references in any new file.
    - Test: `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noAuthFacadeOrHelper'` exits 0.
    - Static analysis: `composer analyse` exits 0; `composer format:check` exits 0.
  </acceptance_criteria>
  <done>/login + /logout work end-to-end with username-based credentials; generic error copy; case-insensitive; remember-me cookie; all tests pass; Larastan + Pint green; no facade smuggling.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /login POST | untrusted input enters the auth pipeline |
| Fortify closure → User::query | username string lands in a parameterised query (Eloquent) |
| LoginAction → AuthManager | the only Modules/Auth file that mutates the active guard at sign-in time |
| browser → session cookie | session id + remember-me cookie issued by Laravel |

## STRIDE Threat Register (ASVS V2 Authentication · V3 Session Management · V7 Error Handling · V14 Configuration)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Information Disclosure | login error response | mitigate | UI-SPEC verbatim copy `Username or password is incorrect.` — never differentiates missing user vs wrong password (ASVS V7.1 Generic Error) |
| T-12-03-02 | Spoofing | username case folding | mitigate | LoginAction.php normalises `strtolower(trim(...))` before lookup; Fortify closure does the same |
| T-12-03-03 | Spoofing | session fixation across sign-in | mitigate | Fortify's `PrepareAuthenticatedSession` middleware regenerates the session id on login (built-in); LogoutAction calls `$this->session->invalidate()` + `regenerateToken()` (ASVS V3.2 Session Binding) |
| T-12-03-04 | Tampering | rate limiter removed (D-12) | accept | Local-only deployment; bcrypt cost factor is the defense; explicit `accept` decision logged in CONTEXT.md D-12 and surfaced here for Phase 19 audit |
| T-12-03-05 | Elevation of Privilege | Auth::loginUsingId() smuggling | mitigate | `noAuthFacadeOrHelper` arch test rejects the literal symbol outside the D-24 allow-list (ASVS V4.1.5) |
| T-12-03-06 | Information Disclosure | password echoed back via Livewire wire snapshot | mitigate | LoginPage `submit()` resets `$this->password = ''` after the action call; password is never re-rendered |
| T-12-03-07 | Information Disclosure | remember-me token theft via XSS | mitigate | Laravel's remember cookie is `httpOnly` + `secure` (default config); no JS surface introduced in this plan |
| T-12-03-08 | Tampering | wrong Fortify features enabled | mitigate | `config/fortify.php` `features = []`; arch-test follow-up in Plan 12-04 could enforce; for Plan 12-03 a unit test on `config('fortify.features')` is sufficient |

No `high` severity threats unmitigated.

</threat_model>

<verification>
- `vendor/bin/pest Modules/Auth/tests/Feature/LoginPageTest.php` passes.
- `vendor/bin/pest tests/Contracts/BoundaryArchTest.php --filter='noAuthFacadeOrHelper'` passes.
- `composer analyse` + `composer format:check` clean.
- `php artisan migrate:fresh && php artisan db:seed --class=...` (or manual factory) + `curl -X POST /login` with valid creds returns 302.
- No GSD codename leakage (`grep -rn '\.planning\|PLAN\.md\|RESEARCH\.md\|D-[0-9][0-9]' Modules/Auth/Internal/Fortify Modules/Auth/Public/Actions/LoginAction.php Modules/Auth/Public/Actions/LogoutAction.php Modules/Auth/Internal/Http/Livewire/LoginPage.php Modules/Auth/Resources/views/livewire/login-page.blade.php`).
</verification>

<success_criteria>
- A manually-seeded user with `username` + bcrypt password can `/login` and reach the dashboard.
- `/logout` POST works.
- Fortify config + session driver finalised.
- Old Core FortifyServiceProvider deleted.
- BoundaryArchTest::noAuthFacadeOrHelper green with the allow-list reflecting the new Auth-module file paths.
- Larastan L10 strict + Pint + Pest all green.
</success_criteria>

<output>
Create `.planning/phases/12-multi-user-activation/12-03-SUMMARY.md` when done. Record: Fortify provider relocation, config changes, route table additions, the LoginPageTest results (all 7 cases), confirmation that the temporary Core allow-list entry was removed.
</output>
