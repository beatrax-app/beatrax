---
phase: 17-ci-cd-pipeline-code-signing
plan: 10
type: execute
wave: 5
depends_on:
  - 17-05a
  - 17-05b
  - 17-06a
  - 17-06b
  - 17-06c
  - 17-09c
files_modified:
  - .docs/features/auth/ (4 files)
  - .docs/features/categorization/ (4 files)
  - .docs/features/chains/ (4 files)
  - .docs/features/community/ (4 files)
  - .docs/features/core/ (4 files)
  - .docs/features/counterparties/ (4 files)
  - .docs/features/desktop/ (4 files)
  - .docs/features/dev-mode/ (4 files)
  - .docs/features/drift-alerts/ (4 files)
  - .docs/features/email-scan/ (4 files)
  - .docs/features/forecasting/ (4 files)
  - .docs/features/import/ (4 files)
  - .docs/features/ingestion/ (4 files)
  - .docs/features/ledger/ (4 files)
  - .docs/features/onboarding/ (4 files)
  - .docs/features/receipts/ (4 files)
  - .docs/features/recurring/ (4 files)
  - .docs/features/00-index.md (updated)
autonomous: false
requirements:
  - gap-docs-features-full-coverage
requirements_addressed:
  - gap-docs-features-full-coverage
must_haves:
  truths:
    - "Every Module in Modules/ has a .docs/features/<module-slug>/ subdirectory containing architecture.md + code.md + specs.md + how-to-test.md (4 files each)"
    - "All 17 modules are covered: auth, categorization, chains, community, core, counterparties, desktop, dev-mode, drift-alerts, email-scan, forecasting, import, ingestion, ledger, onboarding, receipts, recurring"
    - "Each features doc passes the noGsdLeakage arch invariant"
    - "features/00-index.md table lists all 17 modules with one-line synopses + links to their subdirs"
  artifacts:
    - path: ".docs/features/<each-module>/{architecture,code,specs,how-to-test}.md"
      provides: "Per-module documentation following the _template/ shape from Plan 17-09"
  key_links:
    - from: ".docs/features/00-index.md"
      to: "Each module's subdirectory"
      via: "Navigation table"
---

<objective>
Fill the per-module feature documentation tree with all 17 modules × 4 files = 68 documents, following the template established in Plan 17-09c.

Purpose: D-34 + F-26 (full per-module coverage per A-04). This is bulky content work — straight-up technical writing for each module's architecture + code layout + behavioral specs + how-to-test. The "everything that touches your money" amendment expanded the Counterparties module which is included here.

Split rationale: This is split from Plan 17-09 because 68 files of substantive prose is its own work unit; bundling it into the .docs/ tree skeleton plan would have created an 80%+ context plan. The template is established in 17-09; this plan fills it consistently across all modules.

Output: Every module in Modules/ has navigable, accurate, present-tense documentation in .docs/features/. The features/00-index.md is the canonical entry point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.docs/features/_template/architecture.md
@.docs/features/_template/code.md
@.docs/features/_template/specs.md
@.docs/features/_template/how-to-test.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: First 6 modules — auth, categorization, chains, community, core, counterparties</name>
  <files>.docs/features/auth/{architecture,code,specs,how-to-test}.md, .docs/features/categorization/{...}.md, .docs/features/chains/{...}.md, .docs/features/community/{...}.md, .docs/features/core/{...}.md, .docs/features/counterparties/{...}.md</files>
  <read_first>
    - .docs/features/_template/{architecture,code,specs,how-to-test}.md (template shape from Plan 17-09c)
    - For each module, read its key source files. Suggested approach per module:
      - Read the module's ServiceProvider (`Modules/<Name>/Providers/<Name>ServiceProvider.php`) — discovers bindings + boot
      - List the Public/ directory (`ls Modules/<Name>/Public/`) — public contracts + services + events
      - List the Internal/ directory — internal classes
      - Read the module's migrations (`ls Modules/<Name>/Database/Migrations/`) — schema
      - List tests (`ls Modules/<Name>/tests/`) — test inventory
      - Read 1-2 representative Feature tests to understand the behavioral surface
  </read_first>
  <action>For each of the 6 modules in this task, write 4 files following the template shape from Plan 17-09c. Style: prose paragraphs (not bullet-only), present-tense, present-state ("the module does X" — not "the module was changed to do X"). No GSD vocabulary. Length per file: half-page to one page (300-700 words).

    **auth/** — Modules/Auth/. Architecture: Fortify integration + recovery codes + owner-resets-partner + cross-user 404; module boundary forbids Auth:: facade outside CurrentUserService. Code: directory layout (Public/Contracts/CurrentUser, Internal/Http/Livewire login/signup/recovery, Models/User extension). Specs: signup ceremony, recovery code generation/redemption, owner-resets-partner. How-to-test: pest paths + the cross-user 404 arch test.
    **categorization/** — Modules/Categorization/. Architecture: rule-based classifier + per-user merchant memory + the default seed set; AppliesAutoCategory contract; ImportPipeline ApplyAutoCategoryStage. Code: directory; categorization_rules table; rule evaluator. Specs: rule application order, ≥40% gate, seed set scope (no personal identifiers). How-to-test: pest paths + live-distribution-sampled gate fixture.
    **chains/** — Modules/Chains/. Architecture: PayPal funding chains + ICS bulk-iDEAL settlement chains; ChainsLinkBuilder; the post-commit chain-job dispatch from Confirm­Import. Code: chain_links + chain_resolution_runs tables; ResolverServices. Specs: pair_transaction_id contract, kind='ics_bulk_settle' chain link, idempotency. How-to-test: pest fixture path with NO manually-injected unrealistic rows (per Phase 16.1.2.1 lesson).
    **community/** — Modules/Community/. Architecture: bundled YAML corpus + crowd-sourced merchant identification + MysteryMerchantsPage + SuggestMappingModal + OpenExternalUrlAction (github.com allow-list). Code: community_merchant_mappings + users.community_settings; CorpusLoader + GitHubCompareUrlBuilder. Specs: corpus seed on UserInstalled, share-once-per-user, http vs https allow-list. How-to-test: pest paths + the unknown-category warning.
    **core/** — Modules/Core/. Architecture: shared services (UserDataPathService, ElectronUpdateChannel, SecretsColumnRegistry, HealthController, HelpDataLocations); the cross-cutting events (UserInstalled, NotificationDeepLink, FileOpenedFromOs); the SystemAlertsBanner orchestration. Code: directory inventory; the Public/ surface that every other module consumes. Specs: BelongsToUser scope (Concerns/), CurrentUser contract, EnsureDatabaseReady middleware, /health endpoint shape. How-to-test: HealthEndpointTest, AppKeyRegenerationTest, BoundaryArchTest infrastructure.
    **counterparties/** — Modules/Counterparties/ (newly landed in Plans 17-05a / 17-05b / 17-06a / 17-06b). Architecture: 5-type taxonomy + 7-step resolution chain + ImportPipeline ResolveCounterpartyStage + CounterpartyGarbageCollectorJob; cross-module consumption via CounterpartyResolver Public contract. Code: counterparties table + transactions.counterparty_id; resolver service; 3 Livewire pages + per-type partials. Specs: privacy default for personal type (IBAN never in slug/URL/title); self_account routes to /accounts; slug collision suffixing. How-to-test: CounterpartyResolverTest + PrivacyDefaultsTest + SlugCollisionTest + 3 page Feature tests.

    All files commit clean with no TODO markers. Use Markdown links to other docs in the tree where relevant (e.g., counterparties/architecture.md links to chains/architecture.md when describing funding-chain consumption).</action>
  <verify>
    <automated>for module in auth categorization chains community core counterparties; do for file in architecture code specs how-to-test; do test -f .docs/features/$module/$file.md || (echo "MISSING: .docs/features/$module/$file.md" && exit 1); done; done && vendor/bin/pest tests/Contracts/GsdLeakageTest.php --stop-on-failure</automated>
  </verify>
  <done>24 files (6 modules × 4) materialize; each contains substantive prose covering the four template sections; noGsdLeakage stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Remaining 11 modules — desktop, dev-mode, drift-alerts, email-scan, forecasting, import, ingestion, ledger, onboarding, receipts, recurring + features/00-index.md update</name>
  <files>.docs/features/desktop/{...}.md, .docs/features/dev-mode/{...}.md, .docs/features/drift-alerts/{...}.md, .docs/features/email-scan/{...}.md, .docs/features/forecasting/{...}.md, .docs/features/import/{...}.md, .docs/features/ingestion/{...}.md, .docs/features/ledger/{...}.md, .docs/features/onboarding/{...}.md, .docs/features/receipts/{...}.md, .docs/features/recurring/{...}.md, .docs/features/00-index.md</files>
  <read_first>
    - Same approach as Task 1 — read each module's ServiceProvider + Public/ + Internal/ + Database/Migrations/ + tests/ to inform the prose
    - .docs/features/00-index.md (current — created in Plan 17-09b with placeholder rows, refined in 17-09c; this task fills with actual links)
  </read_first>
  <action>Write 4 files per module for the 11 remaining modules following the same shape as Task 1. Brief per-module notes:

    **desktop/** — NativePHP integration; Modules/Desktop/ quarantines every Native\Laravel\* import; arch invariant noNativePhpImportsOutsideDesktopModule; FirstLaunchBootstrap + window/menu/tray/notifications/dark-mode + file association handlers + EnsureAppKey hook from Plan 17-03.
    **dev-mode/** — Phase 16 deliverable; EnsureDeveloperMode middleware + is_developer flag; SAFE/DESTRUCTIVE artisan runner; log tailer with Monolog redaction; queue inspector replacing Horizon dashboard in shipped builds; ⌘K command palette; export-everything action linked from /help/data-locations.
    **drift-alerts/** — Phase 9 deliverable; DetectDriftAlertsJob + unique-until-processing; the drift-detection algorithm; per-recurring-pattern drift threshold.
    **email-scan/** — Phase 6+7 deliverable; Gmail API + Microsoft Graph adapters; OAuth secrets per-user via Modules/Auth oauth_secrets table; template matchers; ReconsentRequiredException flow.
    **forecasting/** — Phase 10 deliverable; cash-flow forecast + what-if scenarios; the FixedPaymentForecaster + RecurringForecastMerger.
    **import/** — the ingestion pipeline; ImportPipeline orchestration + stages; CanonicalTransaction DTO; PaymentTypeClassifierStage + ApplyAutoCategoryStage + ResolveCounterpartyStage + post-commit boundary.
    **ingestion/** — the source-format parsers (CAMT.053 via genkgo/camt + MT940 via kingsquare/php-mt940 + CSV via league/csv + PDF via your-PDF-parser); ASN + ICS + PayPal + Google Play adapters; the synthetic-IBAN accounts for PayPal/ICS.
    **ledger/** — Phase 1 deliverable; Account + Transaction models; BelongsToUser; the canonical "this month at a glance" view.
    **onboarding/** — Phase 16.1 deliverable; /setup wizard; ConnectBankStep/ConnectCardStep/ConnectEmailStep/ConnectPaypalStep/FirstImportStep; wizard_progress table; PreviewSeedHelper.
    **receipts/** — Phase 6 deliverable; .eml file association handling; receipt-template matchers.
    **recurring/** — Phase 8 deliverable; recurring-pattern detection (always-suggest-never-auto-apply); recurring_series + recurring_series_history tables.

    After all 11 module subdirs land, UPDATE `.docs/features/00-index.md` to be the canonical entry point: rewrite the table to list all 17 modules (the 6 from Task 1 + the 11 from Task 2) with: column 1 = module name (linked to `<slug>/architecture.md`), column 2 = one-line synopsis (e.g., "Per-user authentication + recovery codes (no SMTP)"), column 3 = "see: [architecture](<slug>/architecture.md) · [code](<slug>/code.md) · [specs](<slug>/specs.md) · [tests](<slug>/how-to-test.md)".</action>
  <verify>
    <automated>for module in desktop dev-mode drift-alerts email-scan forecasting import ingestion ledger onboarding receipts recurring; do for file in architecture code specs how-to-test; do test -f .docs/features/$module/$file.md || (echo "MISSING: .docs/features/$module/$file.md" && exit 1); done; done && grep -c "^|" .docs/features/00-index.md | xargs test 17 -le && vendor/bin/pest tests/Contracts/GsdLeakageTest.php --stop-on-failure</automated>
  </verify>
  <done>44 files (11 modules × 4) materialize; features/00-index.md lists all 17 modules with links; noGsdLeakage stays green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete per-module documentation coverage — 17 modules × 4 files = 68 documents (Tasks 1-2)</what-built>
  <how-to-verify>
    1. `find .docs/features -name "*.md" | wc -l` returns 69 (68 module files + 1 features/00-index.md + 4 _template files = actually 73 total in the features/ subtree; verify the per-module 68 specifically: `find .docs/features -mindepth 2 -name "*.md" -not -path "*/_template/*" | wc -l` should return 68).
    2. Open features/00-index.md → confirm all 17 modules listed; click 3-4 module links at random + walk through their architecture.md / code.md / specs.md / how-to-test.md → confirm prose is substantive + accurate.
    3. Cross-check accuracy on 2-3 modules: open a module's architecture.md, then open the actual Modules/<name>/Providers/<Name>ServiceProvider.php and confirm the doc matches the actual bindings/boot. Catch any inaccuracies and fix.
    4. Run `vendor/bin/pest tests/Contracts/GsdLeakageTest.php` — green.
    5. Render features/ on GitHub via gh browse — confirm the tree navigates cleanly.
    6. Reply with `approved` + a sample of 2-3 module screenshots showing rendered docs, OR describe inaccuracies to fix.
  </how-to-verify>
  <resume-signal>Type `approved` with sample rendered docs, OR list inaccuracies that need correction.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → public users | Inaccurate docs mislead future contributors; misleading specs cause bugs in PRs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10-01 | Repudiation | docs claiming a behavior the code doesn't implement | mitigate | Task 1+2 actions require reading the actual source before writing each section; checkpoint cross-checks 2-3 modules manually |
| T-17-10-02 | Information disclosure | docs revealing security-sensitive implementation detail | accept | Standard architectural prose; nothing revealed here that's not visible from the source itself |
</threat_model>

<verification>
After both tasks + checkpoint:

1. 68 files exist (17 modules × 4) under .docs/features/<slug>/
2. features/00-index.md lists all 17 modules with working links
3. noGsdLeakage green
4. Manual cross-check of 2-3 modules confirms doc accuracy
</verification>

<success_criteria>
- All 4 must_haves true
- 68 module docs + 1 updated index
- Spot-check accuracy verified during checkpoint
- noGsdLeakage stays green throughout
</success_criteria>

<output>
Create `.planning/phases/17-ci-cd-pipeline-code-signing/17-10-SUMMARY.md` capturing: the file count per module, any modules where the structure deviated from the template, and any inaccuracies discovered + fixed during the checkpoint.
</output>
