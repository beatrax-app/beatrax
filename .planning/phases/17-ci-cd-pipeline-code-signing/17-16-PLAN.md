---
phase: 17-ci-cd-pipeline-code-signing
plan: 16
type: execute
wave: 9
depends_on:
  - 17-15
files_modified:
  - (git tag v1.0.0 — no file edits in this plan)
autonomous: false
requirements:
  - gap-v100-graduation
requirements_addressed:
  - gap-v100-graduation
must_haves:
  truths:
    - "User explicitly invokes the v1.0.0 graduation — no automation triggers it (per D-18)"
    - "Tag v1.0.0 is pushed to the public origin (stable channel → DRAFT release)"
    - "User manually promotes the DRAFT release to published via gh release edit or the GitHub UI"
    - "v1.0.0 release notes summarize the v0.x → v1.0.0 graduation arc + link to the latest v0.x release for upgrade-from-v0.x guidance"
    - "config/nativephp.php default version stays at '0.0.0-dev' (the tag-as-source-of-truth pattern means default never changes — release.yml strips the leading v from the tag and sets NATIVEPHP_APP_VERSION at build time)"
    - "Auto-update path is verified: a v0.x install detects + verifies + offers the v1.0.0 update via the existing Ed25519-signed manifest infrastructure"
  artifacts:
    - path: "(GitHub release v1.0.0 — not a file)"
      provides: "The graduation moment; first v1.0.0 binaries available + signed"
  key_links:
    - from: "v1.0.0 release"
      to: "Auto-update manifest (latest.yml + latest.yml.sig)"
      via: "Ed25519-signed manifest pushed by the publish job"
---

<objective>
The explicit v1.0.0 graduation. User pulls the trigger by name; no automation reaches this tag.

Purpose: D-18 ("v1.0.0 is the explicit graduation tag — the user pulls the trigger by name; no automation graduates from v0.x to v1.0"). This is the ABSOLUTE LAST plan of Phase 17 and gates on every other plan being green + the user's explicit "ship it" call. Per the project memory `project_v0x_versioning_until_explicit_graduation`, ONLY this plan touches v1.0.0; until the user invokes this plan by name, every prior tag stays in the v0.x.x range.

Output: A v1.0.0 stable release published on GitHub Releases, signed via the existing Ed25519 manifest infrastructure, available to every v0.x install via the auto-update banner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ci-cd-pipeline-code-signing/17-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Pre-graduation readiness review</task></what-built>
  <how-to-verify>
    DO NOT proceed without the user's explicit go-ahead. This is the moment the user has been "saving" the v1.0.0 namespace for since the start of v0.x.

    Walk this checklist together; only proceed when EVERY item is confirmed:

    1. **First public v0.x release has been live for ≥ 7 days** (recommended; user can override). The cooling period gives early-adopter feedback time to surface install + smoke regressions that didn't show up in CI.
    2. **No open critical bugs in the public Issues tab.** Walk the issue list; any P0/P1 issues should be closed or have a documented mitigation.
    3. **No regressions discovered post-release** that haven't been fixed + re-released.
    4. **All prior Plan 17 plans confirmed green:** 01 (versioning) ✓, 02 (CI matrix) ✓, 03 (release.yml + smoke + /health + first-launch sentinel) ✓, 04 (Ed25519 + auto-update) ✓, 05+06 (Counterparties) ✓, 07 (community docs) ✓, 08 (arch invariants) ✓, 09 (/help/data-locations + .docs/ tree) ✓, 10 (per-module docs) ✓, 11 (REVIEW-DEEP.md) ✓, 12 (repo settings) ✓, 13 (graduation pass) ✓, 14 (.planning/ purge + skill rename) ✓, 15 (first release + visibility flip + v1.1) ✓.
    5. **Composer test green on both PHP axes** in the latest CI run on main.
    6. **README + install-bypass copy unchanged since first release** OR updated based on user feedback from the cooling period.
    7. **Auto-update path proven** — at least one upgrade between two v0.x.y releases successfully fired the banner + verified + installed (proves the Ed25519 + SHA-512 path works end-to-end with real users, not just synthetic tests).
    8. **User confirms by name:** the user types `Ship v1.0.0` (or equivalent unambiguous phrase) so there is zero ambiguity that this graduation is intentional.

    If any item fails: fix the underlying issue + re-tag a v0.x.y release first; revisit this graduation later. Do NOT shortcut the graduation just because all the plan deliverables are technically done.

    Reply with `approved — ship v1.0.0` + a one-line confirmation of each checklist item.
  </how-to-verify>
  <resume-signal>Type `approved — ship v1.0.0` + checklist confirmation, OR list blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Push v1.0.0 tag → release.yml publishes DRAFT → manual promote to published</name>
  <files>(no file edits; pure git + gh operations)</files>
  <read_first>
    - .docs/cicd/release-cadence.md (Plan 17-01 — confirms v*.*.* → stable channel → DRAFT release)
    - .docs/runbooks/release-cut.md (Plan 17-09b — the operational procedure)
  </read_first>
  <action>Step A — Tag + push:
    `git tag v1.0.0`
    `git push --tags`

    Step B — Watch the release.yml run:
    `gh run watch`
    All five jobs (gate × 2 PHP axes, build-macos, build-windows, build-linux, publish-with-Ed25519-signing) must succeed.

    Step C — Confirm the DRAFT release published:
    `gh release view v1.0.0 --json url,isDraft,isPrerelease,assets --jq '.'`
    Expected: `isDraft: true, isPrerelease: false` (per D-02 asymmetric publish — stable tags create a DRAFT for human eyeballs).

    Step D — Compose the v1.0.0 release notes. Open the release in the GitHub UI (`gh release edit v1.0.0 --notes-file -`) and paste/edit the body:
    - First paragraph: "beatrax v1.0.0 is the first stable release. The v0.x series has been the public-preview series since {date of v0.1.0}; this graduation closes that out."
    - Section "What changed since the last v0.x" — bullet list of notable items shipped in the final v0.x.y before graduation
    - Section "Upgrading from v0.x" — instructions for v0.x users (the in-app auto-update banner will detect v1.0.0 within 4 hours; users can also manually re-download from this release)
    - Section "What's next" — link to the v1.1 milestone
    - Section "Thanks" — acknowledge any beta-cohort-equivalent contributors (CONTEXT.md D-51 acknowledged that there was NO formal beta cycle; just thank early adopters who filed issues during the v0.x weeks)

    Step E — Manually promote DRAFT → published:
    `gh release edit v1.0.0 --draft=false`
    OR navigate to the release in the GitHub UI → click "Edit release" → uncheck "Set as draft" → Publish release.
    This is the moment v1.0.0 is officially available.

    Step F — Mark as latest:
    `gh release edit v1.0.0 --latest`
    (Confirms v1.0.0 is the "Latest" pinned release on the repo home + the auto-update channel uses it.)

    Step G — Verify auto-update banner fires for v0.x installs: on a running v0.x install, wait up to 4 hours OR force a check via Dev Console (if implemented). Confirm the banner appears with the v1.0.0 manifest verified via Ed25519.

    Step H — Post a v1.0.0 announcement in GitHub Discussions Announcements category: short post linking to the v1.0.0 release + acknowledging this is the graduation milestone the project has been building toward. Tone: matter-of-fact (not breathless).

    Step I — Delete the pre-purge backup from Plan 17-14 (created in the pre-checkpoint of that plan). After v1.0.0 ships successfully, the backup is no longer needed: `rm -rf ~/beatrax-pre-purge-backup-{date}`. (The user can keep it longer if they prefer; this step is recommended-not-required.)

    No file edits in this plan. The release notes live in GitHub Releases, not in repo files. The auto-update manifest is regenerated by release.yml's publish job per D-20.</action>
  <verify>
    <automated>gh release view v1.0.0 --json isDraft,isPrerelease,isLatest,assets --jq '{draft: .isDraft, prerelease: .isPrerelease, latest: .isLatest, assetCount: (.assets | length)}' | grep -q '"draft": false' && gh release view v1.0.0 --json isLatest --jq .isLatest | grep -q "true"</automated>
  </verify>
  <done>v1.0.0 release is live (not draft); marked as latest; release notes cover the four sections; auto-update banner confirmed firing for v0.x installs; Discussions announcement posted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>v1.0.0 graduation complete</what-built>
  <how-to-verify>
    1. Open `https://github.com/nightworksio/beatrax/releases/latest` in an incognito tab — confirm v1.0.0 loads + assets downloadable.
    2. On a real v0.x install, confirm the auto-update banner offers v1.0.0 (or force a manual install: download + install the v1.0.0 artifact; the running app should accept it via the Ed25519 verification path).
    3. Confirm `/health` on the installed v1.0.0 returns `{"status":"ok","app_version":"1.0.0",...}` — exact `1.0.0` (no `-rc`, no `-dev`).
    4. Open the Discussions announcement → confirm it's visible + the link to the release resolves.
    5. Walk through the README install instructions one more time on a different platform than what you used in Plan 17-15's first-release checkpoint — confirm the bypass UX works for v1.0.0.
    6. Update beatrax STATE.md or equivalent local tracking to note the v1.0.0 graduation date — this is a notable milestone the maintainer should remember.
    7. Final reply: `approved — v1.0.0 shipped on {date}` + the release URL + the auto-update confirmation.

    After this checkpoint approves: Phase 17 is COMPLETE. The maintainer can return to v1.1 planning in the (now-gitignored) .planning/ workspace.
  </how-to-verify>
  <resume-signal>Type `approved — v1.0.0 shipped on {date}` with the release URL.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-graduation public users → v1.0.0 release | The first stable release sets expectations for the project's quality bar; a regression here erodes trust the v0.x series built up |
| auto-update path → v1.0.0 binary | Ed25519 verification is the sole integrity signal; a tampered v1.0.0 manifest would refuse to install (intentionally) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-16-01 | Tampering | someone tagging v1.0.0 before readiness | mitigate | Branch protection on main + signed commits + checkpoint requires explicit "Ship v1.0.0" phrase + 7-day cooling period |
| T-17-16-02 | Repudiation | v1.0.0 graduation being premature | mitigate | Pre-graduation checklist (8 items) covers readiness; user explicitly invokes |
| T-17-16-03 | Information disclosure | release notes leaking GSD vocabulary or internal process detail | mitigate | Step D explicitly drafts notes for a public audience; no D-XX references; manual review before publishing |
</threat_model>

<verification>
After all tasks + both checkpoints:

1. v1.0.0 tag pushed
2. release.yml succeeded end-to-end
3. DRAFT manually promoted to published
4. Marked as latest
5. Auto-update banner confirmed firing
6. Discussions announcement posted
7. Pre-purge backup deleted (optional)
</verification>

<success_criteria>
- All 6 must_haves true
- v1.0.0 is the latest release on github.com/nightworksio/beatrax
- Auto-update from v0.x successfully detects + verifies + offers v1.0.0
- Phase 17 closeout complete; project enters v1.1 planning cycle
</success_criteria>

<output>
Create `.planning/phases/17-ci-cd-pipeline-code-signing/17-16-SUMMARY.md` capturing: the v1.0.0 release URL, the Discussions announcement URL, the auto-update verification outcome, and the date of graduation. This is the LAST plan SUMMARY for Phase 17.
</output>
