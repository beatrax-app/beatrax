---
phase: 17-ci-cd-pipeline-code-signing
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - NOTICE.md
  - SECURITY.md
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - README.md
  - composer.json
  - resources/brand/logo-512.png
  - resources/brand/logo.icns
  - resources/brand/logo.ico
  - resources/brand/favicon.ico
  - .docs/legal/license-rationale.md
autonomous: false
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
requirements_addressed:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
must_haves:
  truths:
    - "LICENSE contains the Hippocratic License 3.0 verbatim text from firstdonoharm.dev"
    - "NOTICE.md explains the source-available / not-OSI-approved trade-off"
    - "composer.json declares license: Hippocratic-3.0 (with license-validation: false if validator rejects, else use proprietary + NOTICE.md attribution)"
    - "SECURITY.md documents vulnerability-reporting policy + scope + safe-harbor"
    - "CONTRIBUTING.md documents DI rule + arch tests + branch + PR conventions (paraphrased from CLAUDE.md, no GSD vocabulary)"
    - "CODE_OF_CONDUCT.md is Contributor Covenant 2.1 verbatim from contributor-covenant.org"
    - "README hero uses resources/brand/logo.svg + What/Who/Install/Screenshots sections"
    - "README install section includes verbatim macOS Gatekeeper + Windows SmartScreen + Linux bypass walkthroughs from 17-UI-SPEC.md (A-05)"
    - "resources/brand/ contains logo.svg (existing) + logo-512.png + logo.icns + logo.ico + favicon.ico (all generated from the SVG)"
    - ".docs/legal/license-rationale.md explains Hippocratic-3.0 + no-paid-signing rationale (linked from README + install-bypass footnote)"
  artifacts:
    - path: "LICENSE"
      provides: "Hippocratic License 3.0 verbatim"
    - path: "NOTICE.md"
      provides: "Source-available explainer"
    - path: "SECURITY.md"
      provides: "Vulnerability reporting policy"
    - path: "CONTRIBUTING.md"
      provides: "DI rule + arch tests + PR conventions"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant 2.1 verbatim"
    - path: "README.md"
      provides: "Public-audience hero + What/Who/Install/Screenshots + install-bypass walkthroughs"
    - path: "resources/brand/logo-512.png"
      provides: "512x512 PNG export of logo.svg for installer + favicon use"
    - path: "resources/brand/logo.icns"
      provides: "macOS .icns bundle"
    - path: "resources/brand/logo.ico"
      provides: "Windows .ico bundle"
    - path: "resources/brand/favicon.ico"
      provides: "Browser favicon"
    - path: ".docs/legal/license-rationale.md"
      provides: "Why Hippocratic-3.0 + why no paid signing certs (#no-paid-signing anchor for install-bypass footnote)"
  key_links:
    - from: "README install section macOS/Windows footnotes"
      to: ".docs/legal/license-rationale.md#no-paid-signing"
      via: "Anchor link"
      pattern: "license-rationale.md"
    - from: "composer.json license field"
      to: "LICENSE file"
      via: "SPDX identifier"
      pattern: "Hippocratic-3.0"
---

<objective>
Land the public-release community docs (LICENSE + NOTICE + SECURITY + CONTRIBUTING + CODE_OF_CONDUCT), rewrite the README for a public audience with the install-bypass walkthroughs (A-05), and generate the brand-asset exports the installer + favicon need.

Purpose: REL-01..04 + A-05. These are the texts and assets a curious GitHub visitor sees first. The install-bypass copy is load-bearing — without paid signing certs (A-01), every first-launch hits Gatekeeper/SmartScreen; the README walkthroughs are the only safety net between a frustrated visitor and a closed tab.

Output: Repo root contains all five community files + the rewritten README; resources/brand/ ships the four required exports; .docs/legal/license-rationale.md is the canonical "why" page linked from the install bypass footnotes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ci-cd-pipeline-code-signing/17-CONTEXT.md
@.planning/phases/17-ci-cd-pipeline-code-signing/17-RESEARCH.md
@.planning/phases/17-ci-cd-pipeline-code-signing/17-UI-SPEC.md
@CLAUDE.md
@README.md
@composer.json
@resources/brand/logo.svg
@scripts/regenerate_app_icon.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: LICENSE + NOTICE.md + composer.json SPDX + .docs/legal/license-rationale.md</name>
  <files>LICENSE, NOTICE.md, composer.json, .docs/legal/license-rationale.md</files>
  <read_first>
    - composer.json (current — locate the `license` field; verify whether it's already set or absent)
    - .planning/phases/17-ci-cd-pipeline-code-signing/17-RESEARCH.md § "REL-01" (Hippocratic-3.0 SPDX caveat — the SPDX list does NOT yet have Hippocratic-3.0; Hippocratic-2.1 IS registered; workaround options documented)
    - Fetch the Hippocratic License 3.0 text from `https://firstdonoharm.dev/version/3/0/` (or whichever canonical URL hosts the 3.0 text)
  </read_first>
  <action>Step A — LICENSE: download the Hippocratic License 3.0 verbatim text and write it to `/LICENSE` (no modifications, no preamble, no trailing notes). The file should be the license body and nothing else. If the text is unavailable from firstdonoharm.dev at execution time, use the verbatim text from `https://github.com/EthicalSource/hippocratic-license/blob/master/LICENSE.txt` (the canonical EthicalSource repo). Cross-check the file SHA against the upstream version once written.

    Step B — NOTICE.md: write a short explainer at repo root covering: (1) beatrax is licensed under Hippocratic License 3.0 — a source-available license, NOT OSI-approved. (2) Source-available means you can read + run + modify the code; the ethical-use clauses in the Hippocratic License 3.0 differentiate it from OSI-approved licenses (which require no use restrictions). (3) We chose Hippocratic-3.0 because we want users to know the code is real + auditable, but we also want the license to express that we don't want this code used to harm people. (4) Cross-link to .docs/legal/license-rationale.md for the longer-form reasoning. Prose, not bullet-only. Tone matches CLAUDE.md project tone (matter-of-fact, no marketing). No GSD vocabulary.

    Step C — composer.json: add or update the `license` field. Try `"license": "Hippocratic-3.0"` first; run `composer validate --no-check-publish` to confirm the validator accepts it. If composer complains the SPDX identifier is invalid (per RESEARCH.md caveat — Hippocratic-3.0 was not yet in the SPDX list as of phase planning), fall back to one of the two paths in RESEARCH.md REL-01:
    - Path A: Keep `"license": "Hippocratic-3.0"` + add `"license-validation": false` to composer's config block to bypass validation
    - Path B: Use `"license": "proprietary"` + ensure NOTICE.md prominently states the Hippocratic-3.0 attribution
    Planner picks Path A by default (it preserves the SPDX identifier intent); Path B is the fallback if Path A still fails. Document the path chosen in NOTICE.md.

    Step D — .docs/legal/license-rationale.md: create with two sections — `## Why Hippocratic License 3.0?` (the longer-form reasoning: source-available > closed-source for trust + auditability; ethical-use clauses reflect maintainer values without requiring users to adopt them; not OSI-approved means it can't be redistributed under permissive terms but can still be read + run + modified by individuals) AND `## Why no paid signing certificates? {#no-paid-signing}` (the explicit anchor for the README install-bypass footnote — explain: Apple Developer ID is $99/yr + per-developer registration; Azure Trusted Signing is $10/mo + business verification; both gate the project on subscriptions whose lapse would break shipping; the trade-off is users see a one-time Gatekeeper/SmartScreen dialog on first launch; reassure that ad-hoc signing (macOS) + Ed25519 manifest signing on auto-update (every platform) still provide cryptographic integrity guarantees on the update path). Both sections in prose. No GSD vocabulary.</action>
  <verify>
    <automated>test -f LICENSE && grep -q "Hippocratic" LICENSE && test -f NOTICE.md && grep -q "Hippocratic License 3.0" NOTICE.md && grep -q "license" composer.json && composer validate --no-check-publish 2>&1 | grep -qE "(valid|publish)" && test -f .docs/legal/license-rationale.md && grep -q "no-paid-signing" .docs/legal/license-rationale.md && ! grep -Ei "\.planning/|PLAN\.md|RESEARCH\.md|\bD-[0-9]{2,3}\b|gsd[-_]" LICENSE NOTICE.md .docs/legal/license-rationale.md</automated>
  </verify>
  <done>LICENSE is verbatim Hippocratic 3.0; NOTICE.md explains source-available + cross-links rationale; composer.json passes `composer validate --no-check-publish` (or Path B is documented as the active fallback); rationale doc exists with the `#no-paid-signing` anchor; all files pass leakage grep.</done>
</task>

<task type="auto">
  <name>Task 2: SECURITY.md + CONTRIBUTING.md + CODE_OF_CONDUCT.md</name>
  <files>SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md</files>
  <read_first>
    - CLAUDE.md (source-material for CONTRIBUTING.md per PATTERNS.md Area 8 — DI rule, arch tests, modular boundary, Pint/Pest/Larastan L10 gate)
    - Fetch Contributor Covenant 2.1 verbatim from `https://www.contributor-covenant.org/version/2/1/code_of_conduct/` (canonical source per CONTEXT.md canonical_refs)
    - GitHub's recommended SECURITY.md shape (typical: reporting channel + scope + safe-harbor + disclosure timeline)
  </read_first>
  <action>Step A — SECURITY.md: write at repo root. Sections (heading depth `##`):
    - `## Reporting a vulnerability` — instruct users to use GitHub's private vulnerability reporting (Security tab → Report a vulnerability) — NOT public issues. Reference the upcoming repo Settings → Security → Private vulnerability reporting toggle (enabled in Plan 17-12 walkthrough).
    - `## Scope` — what's in scope (the beatrax codebase + its bundled dependencies) + what's out of scope (third-party services like Gmail/Graph OAuth, the user's machine's OS-level security, social-engineering of the maintainer).
    - `## Safe harbor` — short statement that good-faith security research is welcomed; researchers acting in good faith will not face legal action; coordinated disclosure timeline is 90 days from acknowledgment to public disclosure.
    - `## Response timeline` — acknowledge within 7 days; triage within 14 days; fix or status update within 60 days.
    Tone: friendly + clear, not legal-template. No GSD vocabulary.

    Step B — CONTRIBUTING.md: write at repo root. Sections (heading depth `##`):
    - `## Welcome` — short paragraph; note source-available license + ethical-use clauses; contributors agree to the Code of Conduct.
    - `## Setup` — link to .docs/local_development/setup.md (created in Plan 17-08); brief mention of Laravel Herd + composer install + npm ci + php artisan migrate.
    - `## Code conventions` — paraphrase from CLAUDE.md WITHOUT GSD vocabulary:
      - "Constructor dependency injection only — no facade calls (`Auth::user()`, `Cache::get(...)`) or global helpers (`auth()`, `config(...)`, `request(...)`) in module code. Eloquent models are direct-OK; facades and helpers are restricted to Laravel's `routes/web.php` (the static Route DSL) and ServiceProvider registration."
      - "Modular boundary — code lives in `Modules/<Name>/Public/` (cross-module entry points) or `Modules/<Name>/Internal/` (module-private). Cross-module access only via Public service classes or events. Arch invariants in `tests/Contracts/BoundaryArchTest.php` enforce both rules."
      - "Quality gate — every PR must pass Larastan level 10 strict + Laravel Pint formatting + Pest (unit + feature + arch). Run `composer test` locally before opening a PR."
    - `## Branch + commit conventions` — short imperative one-liners; e.g., feature branches prefixed `feat/`, fix branches `fix/`, chore branches `chore/`; commit messages follow conventional commits (feat/fix/chore/docs/refactor/test/perf); signed commits required on `main` (Plan 17-12 enforces in repo settings).
    - `## Pull request flow` — open PR against `main`; CI must pass (both PHP 8.4 + 8.5 axes); CODEOWNERS auto-requests review; squash + rebase only (no merge commits — linear history); auto-delete head branches after merge.
    - `## What's in scope for v1.x contributions` — short paragraph: bug fixes, perf improvements, additional ingestion adapters, additional categorization rules, additional email-receipt parsers, additional language translations (when i18n lands in v1.1+). Out of scope at v1.0: features in PROJECT.md "Out of Scope" list (telemetry, partner-sharing modes, cloud sync, mobile clients).
    No GSD vocabulary anywhere; describe the project as a Laravel + Livewire desktop app, never as a "GSD project."

    Step C — CODE_OF_CONDUCT.md: download Contributor Covenant 2.1 verbatim from contributor-covenant.org and write to repo root. The only edit allowed is filling in the `[INSERT CONTACT METHOD]` placeholder with the same vulnerability-reporting channel used in SECURITY.md (GitHub private reporting).</action>
  <verify>
    <automated>test -f SECURITY.md && test -f CONTRIBUTING.md && test -f CODE_OF_CONDUCT.md && grep -q "Contributor Covenant" CODE_OF_CONDUCT.md && grep -q "Constructor dependency injection" CONTRIBUTING.md && grep -q "Pest" CONTRIBUTING.md && grep -q "Private vulnerability reporting" SECURITY.md && ! grep -Ei "\.planning/|PLAN\.md|RESEARCH\.md|\bD-[0-9]{2,3}\b|gsd[-_]" SECURITY.md CONTRIBUTING.md CODE_OF_CONDUCT.md</automated>
  </verify>
  <done>All three files exist; CODE_OF_CONDUCT.md is verbatim Contributor Covenant 2.1; CONTRIBUTING.md paraphrases the DI/arch/modular rules from CLAUDE.md without GSD vocabulary; SECURITY.md references private vulnerability reporting; all pass leakage grep.</done>
</task>

<task type="auto">
  <name>Task 3: Brand-asset exports (PNG-512 + .icns + .ico + favicon) from logo.svg</name>
  <files>resources/brand/logo-512.png, resources/brand/logo.icns, resources/brand/logo.ico, resources/brand/favicon.ico</files>
  <read_first>
    - resources/brand/logo.svg (current — confirm it exists and is the canonical 2026-05-23 Phase 15 D-20 asset)
    - scripts/regenerate_app_icon.php (analog — existing helper script per PATTERNS.md Area 8; understand the conversion mechanism it uses — likely shells out to ImageMagick `convert` or `rsvg-convert` + `iconutil`)
  </read_first>
  <action>Generate the four exports from `resources/brand/logo.svg` per D-26 (these files are committed to the repo because installer builds + browser need them deterministically):
    - `logo-512.png` — 512x512 PNG, transparent background. Use `rsvg-convert -w 512 -h 512 resources/brand/logo.svg -o resources/brand/logo-512.png` OR ImageMagick `magick convert -background none -resize 512x512 resources/brand/logo.svg resources/brand/logo-512.png`. Pick whichever is already available locally.
    - `logo.icns` (macOS) — build via `iconutil -c icns iconset/` after generating the standard iconset (16, 32, 64, 128, 256, 512, 1024 in @1x + @2x variants from logo.svg). The existing `scripts/regenerate_app_icon.php` likely handles this — invoke it if it exists; otherwise script the conversion inline.
    - `logo.ico` (Windows) — multi-resolution `.ico` (16, 32, 48, 64, 128, 256) via ImageMagick: `magick convert resources/brand/logo.svg -define icon:auto-resize=256,128,64,48,32,16 resources/brand/logo.ico`.
    - `favicon.ico` — same as logo.ico but smaller resolutions only (16, 32, 48): `magick convert resources/brand/logo.svg -define icon:auto-resize=48,32,16 resources/brand/favicon.ico`.

    Verify each export visually opens (no corruption). Commit all four to git. Update any existing `<link rel="icon">` references in `resources/views/layouts/app.blade.php` to point at `/resources/brand/favicon.ico` (or wherever Laravel's public-asset pipeline maps it — likely served via a route from resources/ or symlinked into public/).</action>
  <verify>
    <automated>test -f resources/brand/logo-512.png && test -f resources/brand/logo.icns && test -f resources/brand/logo.ico && test -f resources/brand/favicon.ico && file resources/brand/logo-512.png | grep -q "PNG image data" && file resources/brand/logo.icns | grep -qi "icon" && file resources/brand/logo.ico | grep -qi "ms windows icon"</automated>
  </verify>
  <done>All four exports exist; each passes the `file(1)` magic-byte check; existing favicon reference in layouts/app.blade.php (if any) points at the new file.</done>
</task>

<task type="auto">
  <name>Task 4: README rewrite — public-audience hero + What/Who/Install (with bypass walkthroughs) + Screenshots</name>
  <files>README.md</files>
  <read_first>
    - README.md (current — discover existing sections + tone)
    - .planning/phases/17-ci-cd-pipeline-code-signing/17-UI-SPEC.md § "README install-bypass copy" (verbatim macOS + Windows + Linux + Verification sections — load-bearing per A-05)
    - .planning/PROJECT.md (Core Value statement for the hero paragraph)
    - CLAUDE.md Project section (the one-paragraph project description)
    - Sketch findings skill `Skill("sketch-findings-diederik")` is auto-loaded per CLAUDE.md for Phase 17 surfaces. UI-SPEC.md § "README install-bypass copy" is the authoritative source for the install copy because it includes the platform-specific Gatekeeper/SmartScreen walkthroughs that are explicitly load-bearing per A-05; the sketch findings inform overall README tone/aesthetic alignment with prior dashboard work but do NOT override the verbatim copy contract.
  </read_first>
  <action>Rewrite README.md from scratch. Sections in order:

    1. **Hero (top)** — center the SVG logo via `<p align="center"><img src="resources/brand/logo.svg" width="200" alt="beatrax"></p>`. Below: a one-line tagline matching the topic list from D-50 (e.g., `> A local-first personal finance dashboard for the unified picture of your cross-account money.`). Below tagline: 4-5 badges (license, PHP version, Laravel version, latest release) using shields.io URLs.

    2. **What is beatrax?** — 2-3 paragraphs adapted from CLAUDE.md Project section + PROJECT.md Core Value. Focus on "unified picture across ASN/ICS/PayPal/Google Play; resolves cross-account funding chains; local-only privacy". Use plain prose. NEVER call it "open source" (it's source-available per D-23 + REL-01); use "source-available" everywhere.

    3. **Who is this for?** — short paragraph describing the target audience: a single user (or 2-user household) running personal finances across multiple Dutch bank/card/PayPal sources who wants to see their monthly position in one place instead of cobbling together statements; technically literate enough to install a desktop app and grant OAuth to Gmail/Microsoft Graph.

    4. **Install** — three sub-sections: `### Installing on macOS`, `### Installing on Windows`, `### Installing on Linux`. Use the VERBATIM copy from UI-SPEC § README install-bypass copy for each (including the screenshot placeholders + the Terminal one-liner + the brand-aware footnote linking to `.docs/legal/license-rationale.md#no-paid-signing`). Add a fourth sub-section `### Verifying the download` with the verification copy verbatim from UI-SPEC. Replace screenshot placeholders with actual screenshot paths (commit screenshots under `.docs/assets/install/` if not already committed; capture them on a real install during the human-verify checkpoint).

    5. **Screenshots** — short section showing 4-5 screenshots of the running app: dashboard, chains review, forecast, dev console, multi-user profile selector. Reference paths under `.docs/assets/screenshots/`. If screenshots aren't yet captured, leave placeholder `<!-- TODO: capture during release walkthrough -->` markers; the human-verify checkpoint will fill them in.

    6. **Project status** — short paragraph: "beatrax is in active development. Current version: {latest tag}. See [CHANGELOG.md](CHANGELOG.md) for release history." (CHANGELOG.md may not exist yet; if so, omit the link.)

    7. **Contributing** — one-line: "See [CONTRIBUTING.md](CONTRIBUTING.md)."

    8. **License + ethics** — `beatrax is licensed under the [Hippocratic License 3.0](LICENSE). It's source-available, not OSI-approved — see [NOTICE.md](NOTICE.md) for the rationale.`

    9. **Security** — one-line: "Report vulnerabilities via [Security Policy](SECURITY.md)."

    NO GSD vocabulary anywhere in the README. NO references to phases, plans, sprints, milestones beyond "v1.0.0", or development-process internals. Tone matches calm Linear/Notion aesthetic per CLAUDE.md.</action>
  <verify>
    <automated>test -f README.md && grep -q "logo.svg" README.md && grep -q "Installing on macOS" README.md && grep -q "Installing on Windows" README.md && grep -q "Installing on Linux" README.md && grep -q "Verifying the download" README.md && grep -q "xattr -d com.apple.quarantine" README.md && grep -q "Hippocratic License 3.0" README.md && grep -q "source-available" README.md && ! grep -qi "open source" README.md && ! grep -Ei "\.planning/|PLAN\.md|RESEARCH\.md|\bD-[0-9]{2,3}\b|gsd[-_]" README.md</automated>
  </verify>
  <done>README contains all 9 sections; install-bypass copy is verbatim from UI-SPEC; "source-available" used (not "open source"); brand-aware footnotes link to .docs/legal/license-rationale.md; passes leakage grep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All public-facing community docs + brand exports + rewritten README with install bypass (Tasks 1-4)</what-built>
  <how-to-verify>
    1. Open each file in a Markdown previewer and confirm it renders cleanly (no broken syntax, no truncated sections):
       - LICENSE, NOTICE.md, SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, README.md, .docs/legal/license-rationale.md
    2. Verify the four brand exports visually (open in Preview / Image Viewer):
       - `resources/brand/logo-512.png` — sharp 512x512 PNG
       - `resources/brand/logo.icns` — opens in macOS Preview showing multiple resolutions
       - `resources/brand/logo.ico` — opens in any Windows viewer
       - `resources/brand/favicon.ico` — opens; size is small
    3. Capture install screenshots on a real platform — at least macOS Gatekeeper dialog + Windows SmartScreen dialog — commit them under `.docs/assets/install/` and update the README placeholders to actual paths. Capture 4-5 app screenshots (dashboard, chains, forecast, dev console, multi-user) and commit them under `.docs/assets/screenshots/`; update README.
    4. Run `composer validate` — confirm `license` field validates (or Path B fallback documented in NOTICE.md is active).
    5. Open the rendered README on GitHub (push the changes to a draft branch first, view via `gh browse`): hero renders, badges resolve, install bypass walkthroughs are clear, no GSD vocabulary visible.
    6. Reply with `approved` + the branch URL + screenshots of the rendered README.
  </how-to-verify>
  <resume-signal>Type `approved` with branch URL + rendered README screenshots, OR describe what needs fixing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public visitor → README install instructions | Visitor follows install steps; bad copy could lead to insecure bypass instructions |
| LICENSE → downstream forkers | License text governs all derivative use; tampered text could mislead users about their rights |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07-01 | Tampering | LICENSE deviating from canonical Hippocratic 3.0 | mitigate | Task 1 downloads verbatim from canonical source (firstdonoharm.dev or EthicalSource repo); SHA cross-check after write |
| T-17-07-02 | Spoofing | README claiming "open source" instead of "source-available" | mitigate | Verify step grep-asserts "open source" does not appear (false-positive risk for the phrase "open source" inside neighboring prose — accept this; manually re-check during checkpoint) |
| T-17-07-03 | Information disclosure | install-bypass walkthroughs teaching users to disable security wholesale | accept | The bypass is targeted (per-app xattr clear, SmartScreen "Run anyway" for a specific .exe); the README does NOT instruct users to disable Gatekeeper or SmartScreen globally |
| T-17-07-04 | Repudiation | License rationale being out of sync with actual license file | mitigate | NOTICE.md cross-links rationale; both update together |
</threat_model>

<verification>
After all tasks + checkpoint:

1. All 5 community files exist at repo root; CODE_OF_CONDUCT.md is verbatim Contributor Covenant 2.1
2. composer.json validates (or fallback path is documented)
3. Brand exports all materialize + open cleanly
4. README renders correctly on GitHub with no truncation
5. Install-bypass walkthroughs match UI-SPEC verbatim
6. No GSD vocabulary leakage in any file
</verification>

<success_criteria>
- All 10 must_haves true and verifiable
- License rationale + install bypass cross-link is functional (anchor `#no-paid-signing` exists + reachable)
- Brand assets ship at the canonical paths in resources/brand/
- README is publishable as-is (no TODO placeholders left after the human-verify checkpoint)
</success_criteria>

<output>
Create `.planning/phases/17-ci-cd-pipeline-code-signing/17-07-SUMMARY.md` capturing: the actual license-validation outcome (Path A or B), the four brand-export file sizes, the captured screenshot paths committed to the repo, and the final README headings list.
</output>
