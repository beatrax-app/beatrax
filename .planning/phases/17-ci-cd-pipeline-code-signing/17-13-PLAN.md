---
phase: 17-ci-cd-pipeline-code-signing
plan: 13
type: execute
wave: 6
depends_on:
  - 17-07
  - 17-08
  - 17-09a
  - 17-09b
  - 17-09c
  - 17-10
  - 17-11
  - 17-12
files_modified:
  - (graduation pass — moves from .planning/ to .docs/ as appropriate)
  - .gitignore (modified in Plan 17-14, NOT here)
autonomous: false
requirements:
  - gap-planning-graduation-pass
requirements_addressed:
  - gap-planning-graduation-pass
must_haves:
  truths:
    - "Every worth-keeping artifact in .planning/ has been graduated to .docs/ (per D-38)"
    - "Architectural patterns documented in CLAUDE.md tech-stack section land in .docs/architecture/ (if not already there from Plan 17-09c)"
    - "Key project decisions from PROJECT.md Key Decisions table land as ADRs (covered in Plan 17-09c — verify completeness here)"
    - "Module conventions discovered during the v0.x phases land in the appropriate .docs/features/<module>/ subdirectory (Plan 17-10 covered these — verify completeness here)"
    - "Anything in .planning/ NOT graduated is explicitly deferred (lives only in the local working tree until purged in Plan 17-14)"
    - "A graduation-pass log enumerates what was promoted (or noted as not worth keeping)"
  artifacts:
    - path: "(various — graduation pass enriches existing .docs/ files OR creates new ones if Plans 17-09/17-10 missed something)"
      provides: "Final canonical reference set; nothing load-bearing remains only in .planning/"
  key_links:
    - from: ".docs/ tree"
      to: ".planning/ (which becomes gitignored next plan)"
      via: "Graduated content; .planning/ becomes a write-only local workspace going forward"
---

<objective>
Walk every directory under `.planning/` and graduate worth-keeping artifacts to `.docs/` per D-38. This is the LAST chance to surface anything from the planning corpus that should outlive the .planning/ history purge in Plan 17-14.

Purpose: D-38 explicitly defines this as a pre-purge graduation pass. Plans 17-09 + 17-10 covered the bulk of the structural work (ADRs + architecture topics + features per-module docs). This plan is the safety net: walk the actual .planning/ tree top-down and confirm nothing load-bearing was missed.

Output: A graduation log (in `.planning/phases/17-ci-cd-pipeline-code-signing/17-13-SUMMARY.md`) listing what was promoted + where it went; any remaining .planning/ content is explicitly noted as either (a) intentionally kept local for future GSD work or (b) noise / one-shot debug notes / outdated discussions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Walk .planning/ + identify graduation candidates</name>
  <files>(read-only walk; output is a markdown checklist)</files>
  <read_first>
    - `find .planning -maxdepth 2 -type d` to enumerate top-level structure
    - `ls .planning/phases/ | sort` to enumerate phase directories
    - For each top-level item under .planning/, do a quick scan to classify:
      - PROJECT.md, ROADMAP.md, REQUIREMENTS.md, STATE.md, RETROSPECTIVE.md (if present)
      - phases/ subdirectories (each phase has CONTEXT.md, RESEARCH.md, PLAN files, SUMMARY files, and possibly UI-SPEC, VALIDATION, PATTERNS, REVIEW, UAT, VERIFICATION files)
      - milestones/ (the archived v1.0 ROADMAP)
      - codebase/, graphs/, debug/, todos/ if present
  </read_first>
  <action>Walk every top-level + first-level directory under `.planning/` and produce a graduation triage checklist. For each artifact, classify:

    **GRADUATE** (worth keeping in .docs/):
    - PROJECT.md → already informed Plan 17-07's NOTICE/CONTRIBUTING + Plan 17-09c's ADRs. Verify nothing new — if anything load-bearing remains (e.g., the Core Value statement, project constraints), confirm it's in .docs/ somewhere (likely README + license-rationale.md).
    - Architectural patterns from phase RESEARCH files that informed long-lived decisions → check whether they're covered in .docs/architecture/<topic>.md (Plan 17-09c created 5 topics); if any major pattern is missing, add a new topic file here.
    - Notable phase SUMMARY artifacts that document load-bearing decisions (e.g., D-XX decisions that the executor materialized) → check whether they're covered in an ADR; if not, promote.
    - Operational learnings from phase RETROSPECTIVE.md (if exists) → check whether they're covered in .docs/runbooks/; if not, promote.

    **DO NOT GRADUATE** (intentionally stays in local-only .planning/):
    - PLAN.md files themselves (these are workflow artifacts, not reference documentation)
    - CONTEXT.md per-phase files (these are scaffolding for plans, not user-facing reference)
    - DISCUSSION-LOG.md per-phase files
    - Debug notes, todo files, exploratory research
    - The v1.1 working area (Plan 17-15 creates the v1.1 milestone setup; v1.1 planning happens entirely in the now-gitignored .planning/)

    **NOTE / FLAG** (something the walk surfaces that's neither obvious-graduate nor obvious-discard):
    - Anything ambiguous → flag for the checkpoint discussion

    Write the triage checklist into the eventual `17-13-SUMMARY.md` body (defer until SUMMARY is written).

    For each GRADUATE item, do the actual graduation:
    - Add new ADR if a major decision is missing from .docs/adr/ (continue numbering past 0010 — 0011, 0012, ...)
    - Add new .docs/architecture/<topic>.md if an architectural pattern is missing
    - Add new .docs/runbooks/<runbook>.md if an operational learning is missing
    - Enrich existing .docs/features/<module>/specs.md or how-to-test.md if a module-specific learning is missing

    Re-run noGsdLeakage after every promotion to confirm no GSD vocabulary slipped in.</action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/GsdLeakageTest.php --stop-on-failure && test -f .docs/00-index.md && test -d .docs/adr && test -d .docs/architecture && test -d .docs/runbooks</automated>
  </verify>
  <done>Graduation pass complete; any new .docs/ files materialize cleanly; noGsdLeakage stays green; the triage checklist informs the SUMMARY file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Graduation pass — final .docs/ promotion before .planning/ history purge</what-built>
  <how-to-verify>
    1. Walk through the triage checklist with Claude. For each FLAGGED item, decide together: graduate, discard, or note as "v1.1 working material — stays local".
    2. After graduations land, confirm:
       - .docs/adr/ count >= 10 (Plan 17-09c baseline; may be higher after graduation)
       - .docs/architecture/ topics cover everything load-bearing
       - .docs/runbooks/ covers every operational procedure a future maintainer needs
       - REVIEW-DEEP.md from Plan 17-11 hasn't been forgotten (it's NOT in .docs/ but at repo root — confirm it's committed)
    3. Confirm CLAUDE.md (the developer profile) is NOT under .planning/ and stays committed at repo root (it's not graduating; it's a separate concern that the project relies on for local Claude Code workflow + is acceptable in the public repo).
    4. Acknowledge that anything REMAINING in .planning/ after this checkpoint is intentionally local-only and will disappear from the public commit history after Plan 17-14's filter-repo run.
    5. Reply with `approved` + a one-line list of files promoted during this plan, OR list items that need re-triage.

    Critical: do NOT proceed to Plan 17-14 (history purge) until this checkpoint is approved. Anything not graduated here is gone-from-history after the purge.
  </how-to-verify>
  <resume-signal>Type `approved` with the promoted file list, OR list items needing re-triage before Plan 17-14 runs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/ → .docs/ graduation decisions | Anything not graduated disappears from public history in Plan 17-14; over-graduating leaks process detail; under-graduating loses load-bearing context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13-01 | Repudiation | a load-bearing decision graduating with no clear context for why | mitigate | ADRs follow Status/Context/Decision/Consequences template; graduations preserve the "why" |
| T-17-13-02 | Information disclosure | graduating something that should stay private (e.g., a user's IBAN used in a debug fixture) | mitigate | Triage step explicitly checks for personal/secret content before promoting |
</threat_model>

<verification>
After Task 1 + checkpoint:

1. Triage checklist captured in SUMMARY
2. Promoted artifacts pass noGsdLeakage
3. composer test green (nothing structurally broke)
</verification>

<success_criteria>
- All 6 must_haves true
- Nothing load-bearing remains only in .planning/ after this plan completes
- Triage checklist documents decisions for future audit
</success_criteria>

<output>
Create `.planning/phases/17-ci-cd-pipeline-code-signing/17-13-SUMMARY.md` capturing: the full triage checklist (top-level item by top-level item), the list of newly-created .docs/ files, and any explicit "intentionally stays local" notes that justify why something was not graduated.
</output>
