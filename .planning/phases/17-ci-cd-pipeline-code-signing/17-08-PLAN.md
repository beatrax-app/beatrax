---
phase: 17-ci-cd-pipeline-code-signing
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Contracts/GsdLeakageTest.php
  - Modules/Core/Public/Services/SecretsColumnRegistry.php
  - tests/Contracts/SecretsInLivewireSnapshotTest.php
  - (cleanup edits across Modules/, routes/, config/, app/ as the arch tests identify)
autonomous: true
requirements:
  - REL-05
  - REL-07
requirements_addressed:
  - REL-05
  - REL-07
must_haves:
  truths:
    - "tests/Contracts/GsdLeakageTest.php exists and asserts zero matches across runtime PHP + Blade for .planning/, PLAN.md, RESEARCH.md, CONTEXT.md, REVIEW.md, \\bD-\\d{2,3}\\b, gsd[-_]"
    - "Every match identified by the arch test is purged from the codebase (or carve-out explicitly justified in the test allow-list)"
    - "Modules/Core/Public/Services/SecretsColumnRegistry.php enumerates every secrets-tagged column"
    - "tests/Contracts/SecretsInLivewireSnapshotTest.php walks every Livewire Component and fails if any public property / $listeners / $queryString references a registry entry"
    - "`composer test` runs both new arch invariants and they are green"
  artifacts:
    - path: "tests/Contracts/GsdLeakageTest.php"
      provides: "noGsdLeakage arch invariant — fails on GSD vocabulary in runtime code"
    - path: "Modules/Core/Public/Services/SecretsColumnRegistry.php"
      provides: "Single source-of-truth enumerating secrets-tagged columns"
    - path: "tests/Contracts/SecretsInLivewireSnapshotTest.php"
      provides: "noSecretsInLivewireSnapshot arch invariant"
  key_links:
    - from: "tests/Contracts/SecretsInLivewireSnapshotTest.php"
      to: "Modules/Core/Public/Services/SecretsColumnRegistry"
      via: "Static method SecretsColumnRegistry::columns() returns the registry list the test asserts against"
      pattern: "SecretsColumnRegistry::columns"
---

<objective>
Land two arch invariants that lock the codebase against public-release regressions: noGsdLeakage (REL-05) + noSecretsInLivewireSnapshot (REL-07).

Purpose: Both invariants prevent classes of subtle leaks that would otherwise survive a public-release: GSD planning vocabulary surfacing in user-visible error messages / route names / view-data keys, and secrets-bearing column values being serialized into Livewire's wire snapshot (where a user with browser devtools can read them). These are belt-and-braces tests that, once green, lock the codebase against future regressions without requiring code reviewer vigilance.

Output: Two passing arch invariants + a clean codebase (every match found during the initial green-the-test pass is either purged from runtime code or explicitly carve-outed in the test's allow-list with documented justification) + a SecretsColumnRegistry that enumerates the small initial set of secret-bearing columns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ci-cd-pipeline-code-signing/17-CONTEXT.md
@.planning/phases/17-ci-cd-pipeline-code-signing/17-PATTERNS.md
@tests/Contracts/BoundaryArchTest.php
@Modules/Core/Public/Services/UserDataPathService.php
@Modules/Core/Internal/Http/Livewire/SystemAlertsBanner.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: noGsdLeakage arch invariant + purge sweep</name>
  <files>tests/Contracts/GsdLeakageTest.php, (cleanup edits across runtime code as test identifies)</files>
  <read_first>
    - tests/Contracts/BoundaryArchTest.php lines 262-303 (noPaypalApiRoute analog) + lines 1147-1212 (noStoragePathHardCodedOutsideUserDataPathService analog) — the recursive-walk + grep + comment-strip + allow-list pattern
    - .planning/phases/17-ci-cd-pipeline-code-signing/17-PATTERNS.md § "tests/Contracts/GsdLeakageTest.php" (full verbatim test scaffold to copy)
  </read_first>
  <behavior>
    - Test 1 (no false positives in green state): after the cleanup sweep, the arch test passes with `expect($hits)->toBe([])`
    - Test 2 (catches new leakage): adding a sentinel comment `// PLAN.md says...` to any file under routes/, Modules/, config/, app/ causes the test to fail (verify manually during execution by adding then removing)
    - Test 3 (test files are excluded): files under `Modules/*/tests/`, `tests/Contracts/`, `tests/Feature/`, `tests/Unit/` are excluded from the walk (test fixtures + the arch-test file itself reference the GSD corpus by name and would otherwise self-flag)
  </behavior>
  <action>Step A — Write the arch invariant: create `tests/Contracts/GsdLeakageTest.php` using the verbatim scaffold from PATTERNS.md (Pest `it(...)` test; recursive directory walk over `routes/`, `Modules/`, `config/`, `app/`; comment-strip via `preg_replace('#/\*.*?\*/|//[^\n]*|\{\{--.*?--\}\}#s', ...)`; case-insensitive grep against pattern `/\.planning\/|PLAN\.md|RESEARCH\.md|CONTEXT\.md|REVIEW\.md|\bD-\d{2,3}\b|gsd[-_]/i`; allow-list excludes any path containing `/tests/`). Add ONE additional carve-out for the literal substring `Phase ` followed by a digit if it appears in PHPDocs (some prior-phase comments use "Phase 15-05" style references — planner picks whether to keep these or sweep them; recommend sweep). The test reports each offender with its full path so manual cleanup is straightforward.

    Step B — Run the test ONCE in failing state to enumerate every offender. Triage each hit:
    - **Runtime-code GSD reference** → DELETE (rewrite the comment/log/string to describe present-tense behavior without naming the artifact)
    - **PHPDoc citing a prior phase** → REWRITE to describe what the code IS without history (per the project's docs-describe-current-state rule)
    - **Comment that's already historical context not load-bearing** → DELETE entirely
    - **Legitimate match the test should not flag** → ADD to a documented allow-list (an array at the top of the test) WITH inline rationale

    Step C — Re-run the test; it should now be green with `expect($hits)->toBe([])`. Iterate Step B/C until clean.

    Step D — Document the cleanup outcome in the test's PHPDoc: list (a) categories of offenders found, (b) how many of each were purged, (c) any allow-list entries with their justifications. This serves as the change-log for the test itself.

    Throughout: do NOT delete or modify .planning/ content (that's local-only per D-37, gitignored after Plan 17-13). Do NOT touch test fixtures that reference the GSD corpus on purpose. Do NOT touch CLAUDE.md (which is part of the developer profile, not runtime).</action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/GsdLeakageTest.php --stop-on-failure</automated>
  </verify>
  <done>noGsdLeakage arch invariant is green; the test's PHPDoc documents the sweep outcome; manual sanity check confirms that injecting a fake GSD reference into a non-test file causes the test to fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SecretsColumnRegistry + noSecretsInLivewireSnapshot arch invariant</name>
  <files>Modules/Core/Public/Services/SecretsColumnRegistry.php, tests/Contracts/SecretsInLivewireSnapshotTest.php</files>
  <read_first>
    - Modules/Core/Public/Services/UserDataPathService.php (analog — static accessor pattern with instance shim; PATTERNS.md Area 7)
    - tests/Contracts/BoundaryArchTest.php lines 1147-1212 (the recursive-walk + comment-strip + allow-list infrastructure to piggyback on)
    - .planning/phases/17-ci-cd-pipeline-code-signing/17-PATTERNS.md § "SecretsColumnRegistry.php" + § "tests/Contracts/SecretsInLivewireSnapshotTest.php"
    - `grep -rln "oauth_secrets" Modules/` to discover where the oauth_secrets table is used + which columns hold secret material
  </read_first>
  <behavior>
    - Test 1 (registry returns the documented columns): SecretsColumnRegistry::columns() returns at least ['oauth_secrets.access_token', 'oauth_secrets.refresh_token', 'oauth_secrets.client_secret']
    - Test 2 (clean baseline passes): given the current codebase has NO Livewire component declaring any registry entry as a public property / $listeners / $queryString, the arch test passes
    - Test 3 (catches public property leak): adding `public string $oauthAccessToken` to a Livewire Component (synthetic test fixture) causes the test to fail
    - Test 4 (catches $listeners leak): adding `protected $listeners = ['oauth_secrets.access_token' => 'handle']` causes the test to fail
    - Test 5 (catches $queryString leak): adding `protected $queryString = ['oauth_secrets.refresh_token']` causes the test to fail
  </behavior>
  <action>Step A — Registry: create `Modules/Core/Public/Services/SecretsColumnRegistry.php` per the UserDataPathService analog. `final class SecretsColumnRegistry` with static `public static function columns(): array` returning the list documented in `<interfaces>`. Add instance shim `public function all(): array { return self::columns(); }` so DI consumers can inject the service. Initial column set:
      `oauth_secrets.access_token`, `oauth_secrets.refresh_token`, `oauth_secrets.client_secret`, `users.password`, `users.remember_token`, `user_recovery_codes.code_hash` (if that's the column name — discover via `grep -rln "recovery" Modules/Auth/Database/Migrations/`).
    PHPDoc on `columns()` describes WHY this registry exists in present-tense + WHO consumes it (the arch invariant). No GSD vocabulary.

    Step B — Arch invariant: create `tests/Contracts/SecretsInLivewireSnapshotTest.php`. Reflection-driven walk per PATTERNS.md Area 7 — this is a NEW pattern in the codebase. Test logic:
    - Discover every `Livewire\Component` subclass under `Modules/*/Internal/Http/Livewire/` via class-loader scan (use `composer.lock` or `composer.autoload` registered namespaces; or recurse the filesystem and instantiate-check)
    - For each class, use `ReflectionClass` to enumerate:
      - `getProperties(ReflectionProperty::IS_PUBLIC)` — public property names
      - `getProperty('$listeners')` if present — value (a static array literal)
      - `getProperty('$queryString')` if present — value
    - For each property name / listener key / queryString entry, check it against the registry. The check matches if the property name contains any registry entry (e.g., property `$oauthAccessToken` matches registry `oauth_secrets.access_token`) OR if the string value of a listener/queryString entry exactly equals a registry entry.
    - Accumulate violations in a `$hits = []` array; assert `expect($hits)->toBe([])` at the end.
    - Add 1-2 synthetic fixture Livewire components OUTSIDE the production `Modules/` tree (e.g., under `tests/Contracts/Fixtures/`) that DO violate, and use a separate test method `it('catches violations in fixtures')` to assert the inverse — proves the test mechanism works.

    Step C — Run both tests; resolve any unexpected violations. The current production codebase should pass cleanly per Phase 16 audits.

    Throughout: DI-only on SecretsColumnRegistry (no static-only — provide both surfaces). Test uses pure Pest `it(...)` (not the `arch()` plugin shape per PATTERNS.md — reflection-based, not namespace-based).</action>
  <verify>
    <automated>vendor/bin/pest tests/Contracts/SecretsInLivewireSnapshotTest.php --stop-on-failure</automated>
  </verify>
  <done>SecretsColumnRegistry enumerates the documented columns; noSecretsInLivewireSnapshot arch invariant is green against the production codebase; fixture-based synthetic violation test proves the mechanism catches real leaks; Larastan + Pint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime code → public users | GSD vocabulary in error messages / view-data keys exposes internal development process to public users |
| Livewire wire snapshot → browser devtools | Public properties + listeners + queryString are serialized into the wire payload visible in browser devtools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08-01 | Information disclosure | OAuth tokens / API secrets exfiltrated via Livewire snapshot | mitigate | noSecretsInLivewireSnapshot arch invariant (Task 2) blocks new violations at CI gate; existing audit confirms zero violations at baseline |
| T-17-08-02 | Information disclosure | GSD planning artifacts visible to public users via error messages / route names | mitigate | noGsdLeakage arch invariant (Task 1) blocks new vocabulary at CI gate |
| T-17-08-03 | Tampering | future contributor disabling the arch tests | mitigate | Tests live under tests/Contracts/ which is owned via CODEOWNERS (Plan 17-03 Task 3); any disable would require owner review |
</threat_model>

<verification>
After both tasks:

1. `vendor/bin/pest tests/Contracts/GsdLeakageTest.php tests/Contracts/SecretsInLivewireSnapshotTest.php` both green
2. `composer test` (full quality gate) green
3. Manually inject a GSD reference into a non-test file → noGsdLeakage fails as expected
4. Manually add a registry-referencing public property to a fixture Livewire → noSecretsInLivewireSnapshot fails as expected
</verification>

<success_criteria>
- All 5 must_haves true
- Both arch invariants green at baseline
- Manual injection of a violation triggers test failure (verified by hand)
- SecretsColumnRegistry covers OAuth + password + recovery-code-hash columns
</success_criteria>

<output>
Create `.planning/phases/17-ci-cd-pipeline-code-signing/17-08-SUMMARY.md` capturing: the cleanup sweep outcome (categories + counts of offenders purged), the final registry column list, the fixture file paths used for SecretsInLivewireSnapshot self-test, and any allow-list entries added to GsdLeakageTest with justifications.
</output>
