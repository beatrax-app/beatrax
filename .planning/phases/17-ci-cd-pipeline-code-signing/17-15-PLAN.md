---
phase: 17-ci-cd-pipeline-code-signing
plan: 15
type: execute
wave: 6
depends_on:
  - 17-01
  - 17-02
  - 17-03
  - 17-04
  - 17-05
  - 17-06
  - 17-07
  - 17-08
  - 17-09
  - 17-10
  - 17-11
  - 17-12
  - 17-13
  - 17-14
files_modified:
  - .docs/roadmap-v1.1.md
  - (GitHub Milestone + Issues created via gh CLI — not file edits)
  - (repo visibility flipped via gh CLI — not a file edit)
autonomous: false
requirements:
  - gap-v11-milestone
  - gap-first-release
  - gap-visibility-flip
requirements_addressed:
  - gap-v11-milestone
  - gap-first-release
  - gap-visibility-flip
must_haves:
  truths:
    - "A GitHub Milestone named 'v1.1' exists with at least 5 seed issues covering the deferred items from CONTEXT.md (counterparty-profile polish, OS Keychain integration, SMTP password reset, WebAuthn / passkeys, partner-sharing modes, Sentry decision, Pulse evaluation, post-release issues triage)"
    - ".docs/roadmap-v1.1.md exists with a 1-paragraph milestone summary + link to the GitHub Milestone view"
    - "The first release-pipeline run on a v0.x.x stable tag (or v0.x.x-rc.N preview tag — user picks) completes end-to-end"
    - "The release artifacts (signed by Ed25519 manifest) install + smoke-pass on macOS + Windows + Linux"
    - "Repo visibility flips from private → public AFTER the first release succeeds (A-07 + D-50 baseline)"
    - "A GitHub Discussions welcome post lands in Announcements introducing beatrax to first-time visitors"
  artifacts:
    - path: ".docs/roadmap-v1.1.md"
      provides: "v1.1 milestone summary + link"
    - path: "(GitHub Milestone + Issues — not files)"
      provides: "Public visible roadmap for next milestone"
  key_links:
    - from: ".docs/roadmap-v1.1.md"
      to: "GitHub Milestone view URL"
      via: "Hyperlink"
---

<objective>
Ship the first public release, set up the v1.1 milestone for deferred work, flip repo visibility to public, and welcome the world via Discussions.

Purpose: D-39 (v1.1 milestone) + the first non-graduation release (CONTEXT.md "Proposed execution order" item 20) + repo visibility flip (A-07 + D-50). This plan brings together every prior plan's deliverable: the release pipeline (Plan 17-04), the Ed25519 verification (Plan 17-05), the Counterparties feature (Plans 17-06/17-07), all the docs (Plans 17-09 through 17-13), the repo settings (Plan 17-15), the clean history (Plan 17-15).

CRITICAL: this is NOT the v1.0.0 graduation tag — D-18 reserves v1.0.0 for explicit user-triggered graduation in Plan 17-16. This plan tags v0.1.0 (or v0.1.0-rc.1 per user preference) — the FIRST public release in the v0.x series.

Output: A public GitHub repo at github.com/nightworksio/beatrax with the first v0.x release published + a v1.1 milestone visible + a welcome Discussions post.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ci-cd-pipeline-code-signing/17-CONTEXT.md
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create v1.1 milestone + seed issues + .docs/roadmap-v1.1.md</name>
  <files>.docs/roadmap-v1.1.md</files>
  <read_first>
    - .planning/phases/17-ci-cd-pipeline-code-signing/17-CONTEXT.md § "v1.1 Milestone Seeds" (provisional list of items)
    - .planning/REQUIREMENTS.md § "v2.1+ Requirements" (the deferred-with-trigger list — AUTH-21..23, TELE-01..03, SHARE-01)
  </read_first>
  <action>Step A — Create the GitHub Milestone via gh CLI:
    `gh api repos/nightworksio/beatrax/milestones -f title="v1.1" -f description="Post-v1.0.0 follow-up: deferred features, polish, and follow-ups from the first public release." -f state=open --jq '.number'`
    Capture the returned milestone number for use in Step B.

    Step B — Create seed issues per the provisional list from CONTEXT.md § "v1.1 Milestone Seeds". For each issue, use `gh issue create --title "..." --body "..." --milestone "v1.1"`:
    - "Counterparty profile polish — CSV export per profile" — body: brief description; reference UI-SPEC § Out of scope where this was originally deferred
    - "Counterparty profile polish — monthly digest email" — same shape
    - "Counterparty profile polish — comparison view" — same shape
    - "Counterparty merge UI for end users (currently Dev Mode action only)" — body
    - "Counterparty delete action (currently garbage-collected only)" — body
    - "OS Keychain integration for OAuth secrets (AUTH-21)" — body referencing CLAUDE.md trade-off
    - "SMTP password reset via Gmail OAuth (AUTH-22)" — body
    - "WebAuthn / passkeys (AUTH-23)" — body
    - "Sentry crash reporting decision + UX (TELE-01)" — body explicitly noting this needs an EXPLICIT decision before any code lands
    - "Anonymous telemetry decision (TELE-02 — likely 'no' but document the call)" — body
    - "Laravel Pulse evaluation (TELE-03 — requires Redis cache reconfig)" — body
    - "Per-user-data partner-sharing modes (SHARE-01)" — body
    - "Triage queue: post-release issues from the first public-release wave" — placeholder for collecting day-1 GitHub Issues

    Step C — Write `.docs/roadmap-v1.1.md`. One short paragraph: "The v1.1 milestone tracks deferred work from the v1.0 closeout + any issues that surface in the first weeks of public release. Active items are visible at the [v1.1 milestone view](https://github.com/nightworksio/beatrax/milestone/{N})." Replace {N} with the actual milestone number. Add a 2-line note: "Detailed planning happens locally (the planning workspace is gitignored per .docs/00-index.md). Items that mature into long-lived references promote into this `.docs/` tree."

    No GSD vocabulary in either the issue bodies or roadmap-v1.1.md.</action>
  <verify>
    <automated>gh api repos/nightworksio/beatrax/milestones --jq '.[] | select(.title == "v1.1") | .number' | grep -E "^[0-9]+$" && test -f .docs/roadmap-v1.1.md && grep -q "v1.1 milestone view" .docs/roadmap-v1.1.md && gh issue list --milestone "v1.1" --json title --jq '. | length' | xargs test 5 -le</automated>
  </verify>
  <done>Milestone exists; at least 5 seed issues attached; roadmap-v1.1.md cross-links the milestone view.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>First release pipeline run on a non-graduation tag (v0.1.0 or v0.1.0-rc.1)</what-built>
  <how-to-verify>
    Decide with user: tag `v0.1.0` (stable channel → DRAFT release) OR `v0.1.0-rc.1` (preview channel → immediate publish)? Recommend `v0.1.0-rc.1` first (immediate publish + can iterate freely before promoting to stable).

    1. Pick the tag name with the user. Confirm `v0.1.0-rc.1` (recommended) or alternative.
    2. Push the tag:
       `git tag v0.1.0-rc.1 && git push --tags`
    3. Watch the release run end-to-end:
       `gh run watch`
    4. Confirm all five jobs succeed (gate × 2 PHP axes, build-macos, build-windows, build-linux, publish with Ed25519 manifest signing).
    5. Verify the release published:
       `gh release view v0.1.0-rc.1 --json url,assets --jq '{url, assets: [.assets[].name]}'`
    6. Download + install on at least one platform (recommend macOS via your developer machine):
       - Download the .dmg from the release
       - Install + walk through the Gatekeeper bypass per README install copy (`xattr -d com.apple.quarantine /Applications/beatrax.app` or right-click → Open)
       - Confirm the app launches + the dashboard renders
       - Sign up a fresh user; confirm first-launch APP_KEY regeneration sentinel materializes
       - Visit /counterparties (empty state); /help/data-locations (correct paths)
    7. Verify Ed25519 manifest verification on the running install: trigger an update check (the bundled v0.1.0-rc.1 is currently the latest, so no banner; tag a synthetic v0.1.1-rc.1 if testing the upgrade path is desired — defer until v1.1 if not).
    8. Reply with `approved` + release URL + screenshots of the running v0.1.0-rc.1 install, OR describe what failed end-to-end.

    Failure modes to watch: Ed25519 signing step fails because secret isn't set (re-run Plan 17-04 pre-code checkpoint) · smoke test on a platform fails (debug the platform-specific issue) · publish job fails because gh-release action permissions are wrong (verify `contents: write` permission is set on the publish job).
  </how-to-verify>
  <resume-signal>Type `approved` with release URL + install screenshots, OR describe failures.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Repo visibility flip private → public (A-07 + D-50) + welcome Discussions post</what-built>
  <how-to-verify>
    With the first release successfully shipped AND smoke-tested, flip visibility. This is the moment beatrax becomes public.

    1. **Final pre-flip checklist:**
       - First release published + smoke-tested per previous checkpoint ✓
       - .planning/ purged from history per Plan 17-14 ✓ (re-verify: `git ls-tree -r HEAD | grep -c .planning/` returns 0)
       - LICENSE + NOTICE.md + SECURITY.md + CONTRIBUTING.md + CODE_OF_CONDUCT.md + README.md all present ✓
       - composer test green
       - Branch protection + secret scanning + Dependabot + CodeQL all enabled per Plan 17-12 ✓
       - .docs/ tree complete ✓
       - REVIEW-DEEP.md committed ✓
       - Backup from Plan 17-14 still on disk (do not delete yet — keep for one more week)
    2. **Flip visibility:**
       `gh repo edit nightworksio/beatrax --visibility public --accept-visibility-change-consequences`
       OR navigate to Settings → General → Danger Zone → Change visibility → Make public → type the repo name to confirm.
    3. **Verify public:** open an incognito browser tab → navigate to `https://github.com/nightworksio/beatrax` → confirm the repo loads + README renders + release is visible.
    4. **Welcome Discussions post:** in the Announcements category, create a new discussion:
       Title: `Welcome to beatrax v0.1.0-rc.1`
       Body: brief paragraph: what beatrax is (paste the README hero + What is beatrax? paragraph), where to start (link to the release page + install instructions), how to give feedback (link to Issues + Discussions), the project's license posture (source-available, Hippocratic 3.0), and a note that this is the first public release — patience requested for the first few weeks while issues surface.
    5. **Update README badges if needed:** if the README uses a private-repo badge URL, replace with the public-repo URL (download counts, release version, etc., should now resolve cleanly).
    6. **Final verification:** clone the public repo in a fresh location to confirm everything works for an outsider:
       `git clone https://github.com/nightworksio/beatrax.git /tmp/fresh-clone && cd /tmp/fresh-clone && composer install --no-dev && php artisan migrate && php artisan native:build` (or whichever subset of the install instructions is easiest to verify)
    7. Reply with `approved + public URL + Discussions post URL`, OR describe what went wrong.
  </how-to-verify>
  <resume-signal>Type `approved` with the public URL + Discussions post URL.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| private repo → public repo | Once flipped, history is permanently public; nothing can be retroactively redacted without invalidating prior clones |
| first public release → first users | Day-1 install issues land in the public Issues tab; bad release-pipeline state means a bad first impression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-15-01 | Information disclosure | secret or personal data in pre-purge history surviving the flip | mitigate | Plan 17-14 purged .planning/; this plan's pre-flip checklist re-verifies; backup retained for one more week as recovery insurance |
| T-17-15-02 | Tampering | malicious actor opening drive-by PRs the moment the repo goes public | mitigate | Branch protection requires CI pass + signed commits + 1 review; fork PRs cannot push tags so release pipeline is unreachable from forks |
| T-17-15-03 | Denial of service | first-day GitHub Issues flood from install-bypass confusion | accept | Per D-51 user accepted this risk; mitigations are README install-bypass copy (Plan 17-08), Dev Mode for diagnostics (Phase 16), auto-update for fast remediation |
| T-17-15-04 | Repudiation | the welcome post being missing or misleading | mitigate | The post is part of the checkpoint flow; user reviews before posting |
</threat_model>

<verification>
After all tasks + both checkpoints:

1. v1.1 milestone exists with at least 5 seed issues
2. .docs/roadmap-v1.1.md cross-links the milestone
3. First v0.x release published + smoke-tested
4. Repo visibility is public
5. Welcome Discussions post is live
6. Fresh-clone test (step 6 of visibility-flip checkpoint) succeeded
</verification>

<success_criteria>
- All 6 must_haves true
- Public repo loads + README renders
- First release visible + downloadable
- v1.1 milestone open + issues attached
- Welcome post in Announcements
</success_criteria>

<output>
Create `.planning/phases/17-ci-cd-pipeline-code-signing/17-15-SUMMARY.md` capturing: the chosen first-release tag, release URL, public repo URL, Discussions welcome post URL, v1.1 milestone URL, and the seed issue count.
</output>
