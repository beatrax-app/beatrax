---
phase: 17-ci-cd-pipeline-code-signing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CI-01
requirements_addressed:
  - CI-01
must_haves:
  truths:
    - "Both PHP 8.4 AND PHP 8.5 axes run on every PR + every push to main"
    - "A failure on either axis fails CI (matrix retains fail-fast: false so both axes always report)"
    - "All third-party GitHub Actions are SHA-pinned (not tag-pinned) per the Q1 2026 supply-chain hardening rule"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "PR gate matrix on PHP 8.4 + 8.5"
      contains: "['8.4', '8.5']"
  key_links:
    - from: ".github/workflows/release.yml (Plan 17-04)"
      to: ".github/workflows/ci.yml"
      via: "release.yml's gate job mirrors this matrix shape per D-12"
      pattern: "php: \\['8.4', '8.5'\\]"
---

<objective>
Widen the existing PR-gate matrix from single-axis PHP 8.4 to dual-axis PHP 8.4 + 8.5 and harden every third-party action to a full 40-char SHA pin.

Purpose: CI-01 + the supply-chain hardening rule. Shipped bundle runs PHP 8.4 (the version `nativephp/php-bin` bundles); developer's local box runs 8.5; both must be honest about Larastan L10 strict + Pint + Pest passing. The current `ci.yml` skeleton was deliberately structured by Phase 15 P05 so this widen is a single-line matrix edit. The SHA-pinning is the load-bearing supply-chain mitigation per the Q1 2026 attacks on `tj-actions/changed-files` + Trivy + Nx (23,000+ repos compromised through mutable tag references).

Output: Updated `.github/workflows/ci.yml` with both axes + SHA-pinned actions; green CI on both axes for at least one PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ci-cd-pipeline-code-signing/17-CONTEXT.md
@.planning/phases/17-ci-cd-pipeline-code-signing/17-RESEARCH.md
@.planning/phases/17-ci-cd-pipeline-code-signing/17-PATTERNS.md
@.github/workflows/ci.yml

<interfaces>
<!-- ci.yml current shape (Phase 15 P05 baseline). Executor edits in-place. -->
<!-- Key extracted invariants the matrix widen must preserve: -->
- `on:` trigger: `pull_request` + `push: branches: [main]` — UNCHANGED
- `permissions: contents: read` — UNCHANGED
- Job name template: `quality (PHP ${{ matrix.php }})` — UNCHANGED (label auto-expands)
- `strategy.fail-fast: false` — UNCHANGED (we want both axes to always report)
- Matrix axis: currently `php: ['8.4']` — CHANGES to `php: ['8.4', '8.5']`
- `env.TZ: Europe/Amsterdam` — UNCHANGED
- Composer cache key: `composer-${{ runner.os }}-${{ matrix.php }}-${{ hashFiles('**/composer.lock') }}` — already parameterized on `${{ matrix.php }}`, no change needed
- Extensions list: `mbstring, sqlite3, pdo_sqlite, bcmath, gd, intl, xml, dom, fileinfo, tokenizer, json` — UNCHANGED (ext-imap deliberately omitted per project hard-conflict)
- Gates run: `vendor/bin/pint --test`, `vendor/bin/phpstan analyse --memory-limit=1G --no-progress`, `php artisan test --parallel`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen matrix to PHP 8.4 + 8.5 and SHA-pin every third-party action</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (entire current file — only ~83 lines; understand the existing comment + step layout before editing in-place)
    - .planning/phases/17-ci-cd-pipeline-code-signing/17-RESEARCH.md § "Pattern 1: SHA-pinned GitHub Action with renovate-friendly comment"
  </read_first>
  <action>Modify `.github/workflows/ci.yml` with two changes: (a) replace `php: ['8.4']` with `php: ['8.4', '8.5']` on the matrix axis (single-line change); (b) SHA-pin every third-party action with the human-readable tag retained as an inline comment per the project's supply-chain hardening rule. Specifically:
    - `uses: actions/checkout@v4` → `uses: actions/checkout@<full-40-char-SHA-for-v4>  # v4`
    - `uses: shivammathur/setup-php@v2` → `uses: shivammathur/setup-php@7c071dfe9dc99bdf297fa79cb49ea005b9fcadbc  # v2.37.1` (RESEARCH.md verified this SHA)
    - `uses: actions/cache@v4` → `uses: actions/cache@<full-40-char-SHA-for-v4>  # v4`
    Resolve the unknown SHAs at runtime via `gh api repos/actions/checkout/git/refs/tags/v4 --jq '.object.sha'` (this returns either a tag-object SHA or directly the commit SHA — if it's a tag object, follow with `gh api repos/actions/checkout/git/tags/<sha> --jq '.object.sha'`; same for `actions/cache`). Update the existing Phase 15-era comment block at the top of the file: the phrase "Phase 17 will extend the `php` axis below to `['8.4', '8.5']`" is now historical and MUST be removed (rewrite the head comment to describe the CURRENT state per the project's docs-describe-current-state rule — say what the file IS, not what it became). Do NOT introduce any GSD vocabulary in the rewritten comments. Do NOT add `release.yml` references in this file; Plan 17-04 owns that.</action>
  <verify>
    <automated>grep -q "php: \['8.4', '8.5'\]" .github/workflows/ci.yml && grep -E "uses: (actions/checkout|actions/cache|shivammathur/setup-php)@[a-f0-9]{40}" .github/workflows/ci.yml | wc -l | grep -q "^[[:space:]]*3$" && ! grep -Ei "\.planning/|PLAN\.md|RESEARCH\.md|\bD-[0-9]{2,3}\b|gsd[-_]|Phase 17" .github/workflows/ci.yml</automated>
  </verify>
  <done>Matrix shows both `'8.4'` and `'8.5'`; exactly 3 third-party action references are SHA-pinned (each 40 hex chars); head comment describes CURRENT state and contains zero GSD/phase vocabulary; `yamllint .github/workflows/ci.yml` reports clean (or no yamllint installed → skip).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ci.yml with PHP 8.4 + 8.5 matrix + SHA-pinned actions (Task 1)</what-built>
  <how-to-verify>
    Push the workflow change on a throwaway branch, open a PR, and confirm BOTH matrix axes succeed.

    1. From the working tree (after Task 1 commits), run:
       `git checkout -b ci/widen-matrix-php-8-5`
       `git push -u origin ci/widen-matrix-php-8-5`
       (Repo is `nightworksio/beatrax` per A-07, currently private — origin already set.)
    2. Open a PR against `main`:
       `gh pr create --title "ci: widen quality matrix to PHP 8.4 + 8.5" --body "Closes the CI-01 dual-axis requirement. SHA-pins added per supply-chain hardening rule."`
    3. Watch the CI run:
       `gh pr checks --watch`
    4. Confirm BOTH the `quality (PHP 8.4)` and `quality (PHP 8.5)` jobs reach status `pass`.
    5. If either axis fails for a code reason (a Larastan / Pint / Pest hit on 8.5 that the 8.4 axis previously hid), fix the underlying issue in a follow-up commit on the same branch — do NOT loosen the matrix.
    6. Reply with `approved` + the PR URL once both axes are green. The PR itself can be merged (or not) at user's discretion; the verification is the green run.
  </how-to-verify>
  <resume-signal>Type `approved` with the PR URL once both PHP 8.4 + PHP 8.5 jobs reach pass status, OR describe the failure if either axis fails for an unfixable reason.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → installed action code | Third-party actions execute in CI runner context with access to checked-out source + repo secrets |
| PR contributor → workflow file | Workflow changes from a forked PR run with restricted context (no secrets) per default `pull_request` (not `pull_request_target`) trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Tampering | third-party action references | mitigate | All three third-party actions SHA-pinned to immutable 40-char commits; Dependabot's `github-actions` ecosystem (already configured in `.github/dependabot.yml`) auto-bumps via reviewed PRs |
| T-17-02-02 | Elevation of privilege | fork-PR secret exfil | accept | ci.yml uses `pull_request` (NOT `pull_request_target`) — fork PRs run with restricted GITHUB_TOKEN and no access to repo secrets. Release.yml in Plan 17-04 keeps the same posture (push: tags only) |
| T-17-02-03 | Denial of service | matrix-wide failure on one axis | mitigate | `fail-fast: false` retained so both axes always report — a 8.5-only regression is surfaced explicitly rather than masked by 8.4 finishing first |
</threat_model>

<verification>
After Task 1 + checkpoint:

1. `grep -c "uses:.*@[a-f0-9]\{40\}" .github/workflows/ci.yml` reports 3
2. `gh run list --workflow ci.yml --limit 1 --json conclusion --jq '.[0].conclusion'` reports `success` against the verification PR
3. Both job names `quality (PHP 8.4)` and `quality (PHP 8.5)` visible in `gh run view --log <run-id>`
</verification>

<success_criteria>
- ci.yml matrix is `['8.4', '8.5']`
- All three third-party actions SHA-pinned to 40-char SHAs with human-readable tag comments
- One PR run completed with both axes green
- Head comment rewritten to describe current state (no phase references, no GSD vocabulary)
</success_criteria>

<output>
Create `.planning/phases/17-ci-cd-pipeline-code-signing/17-02-SUMMARY.md` capturing the PR URL, the actual SHAs pinned, and the run-IDs of the green dual-axis runs.
</output>
