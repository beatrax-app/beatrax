---
phase: 17-ci-cd-pipeline-code-signing
plan: 14
type: execute
wave: 6
depends_on:
  - 17-13
files_modified:
  - .gitignore
  - (LOCAL git history rewrite — touches every commit; force-push to private origin)
autonomous: false
requirements:
  - gap-planning-history-purge
requirements_addressed:
  - gap-planning-history-purge
must_haves:
  truths:
    - "git filter-repo --path .planning --invert-paths has been run on a FRESH clone of nightworksio/beatrax"
    - "After the purge: zero .planning/ blobs exist in any commit in the rewritten history"
    - ".gitignore is updated to add `.planning/` so future commits never include it"
    - "The rewritten history is force-pushed to the private origin nightworksio/beatrax (still safe — repo is private; visibility flip is Plan 17-15)"
    - "The working tree retains .planning/ for ongoing local GSD work (the directory exists on the developer's machine; just no longer tracked)"
    - "All existing branches that contained .planning/ commits have been updated or deleted"
    - "Skill rename diederik → beatrax also lands here (D-40..D-42) since both touch identifier hygiene + the .planning/ purge requires no .planning/ references in skills that get committed alongside"
  artifacts:
    - path: ".gitignore"
      provides: "Adds .planning/ ignore line so future commits skip it"
    - path: "(rewritten git history — not a file)"
      provides: "Public commit history free of .planning/ blobs"
  key_links:
    - from: "Local working tree"
      to: "Private origin nightworksio/beatrax"
      via: "Force-push of rewritten history (last force-push; future pushes are normal)"
---

<objective>
Run the DESTRUCTIVE .planning/ git-history purge per D-35..D-37 + A-07. Add .planning/ to .gitignore so future work stays local. Rename the sketch-findings skill diederik → beatrax (D-40..D-42).

Purpose: D-35..D-37 (purge before first public push) + A-07 (repo is private; purge runs against private origin; visibility flip happens later in Plan 17-15). The skill rename (D-40..D-42) lands in this plan because it shares the "identifier hygiene before public release" concern and the skill SKILL.md frontmatter + folder name align with the broader diederik → beatrax brand rename.

This is the LAST git-history modification before the first public push. After this: only normal commits.

Output: A clean public history (rewritten via git filter-repo) force-pushed to private origin; .planning/ added to .gitignore so the working tree keeps the directory but git stops tracking it; the sketch-findings skill renamed in both project-level (./.claude/skills/) and user-level (~/.claude/skills/) paths; CLAUDE.md updated to reference the new skill name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ci-cd-pipeline-code-signing/17-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>PRE-PURGE confirmation step — DESTRUCTIVE operation requires explicit user confirmation</what-built>
  <how-to-verify>
    DESTRUCTIVE. This plan rewrites every commit in the repo's history. Once force-pushed to origin, the old history is gone (except in any other clones on contributor machines). Confirm before proceeding:

    1. Confirm Plan 17-13 was approved (graduation pass complete; nothing load-bearing remains only in .planning/).
    2. Confirm the local working tree is clean: `git status` → no uncommitted changes (or any uncommitted changes are intentional working state that will survive the purge — `git filter-repo` operates on the committed history, not the working tree).
    3. Confirm origin is the PRIVATE nightworksio/beatrax repo (per A-07): `git remote -v` → shows `git@github.com:nightworksio/beatrax.git` (or https equivalent). The repo MUST still be private during this purge.
    4. Confirm `git-filter-repo` is installed locally: `which git-filter-repo` → returns a path. If not installed: `brew install git-filter-repo` (macOS) or `pip install --user git-filter-repo` (anywhere).
    5. Confirm ALL collaborators have been told a force-push is coming (for nightworksio/beatrax with a single owner, this is trivially satisfied).
    6. Acknowledge that any open PRs in the private repo will be invalidated after the force-push (close/reopen any active PRs).
    7. Take a safety backup: clone the current state of the repo to `~/beatrax-pre-purge-backup-{date}`:
       `git clone --mirror git@github.com:nightworksio/beatrax.git ~/beatrax-pre-purge-backup-$(date +%Y%m%d-%H%M%S)`
       Keep this backup on disk until at least Plan 17-15 (first public release) confirms the new history works end-to-end.
    8. Reply with `approved + pre-purge backup at <path>` once steps 1-7 are confirmed.

    DO NOT proceed without explicit confirmation. This is irreversible without the backup.
  </how-to-verify>
  <resume-signal>Type `approved` with the pre-purge backup path, OR explain what's blocking.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Run git filter-repo on a fresh clone + update .gitignore + force-push</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current — confirm .planning/ is NOT already in the file; the purge step adds it)
    - .planning/phases/17-ci-cd-pipeline-code-signing/17-CONTEXT.md § "D-35" (`git filter-repo --path .planning --invert-paths`)
  </read_first>
  <action>Step A — Fresh clone (filter-repo requires a fresh clone for safety; do NOT run filter-repo against the working repo):
    `git clone git@github.com:nightworksio/beatrax.git /tmp/beatrax-purge-clone`
    `cd /tmp/beatrax-purge-clone`

    Step B — Run the filter:
    `git filter-repo --path .planning --invert-paths`
    This rewrites every commit; the .planning/ tree is removed from every blob and every tree object. filter-repo will refuse to run if it detects local modifications or weird state — if so, re-clone and retry.

    Step C — Verify the purge worked:
    `git log --all -- .planning | head -5` should produce ZERO output (no commit references .planning/ anymore).
    `git ls-tree -r HEAD | grep .planning` should produce ZERO output.

    Step D — Add `.planning/` to .gitignore:
    Add a new line `.planning/` near the top of .gitignore (above the existing project-specific ignores; group with other "local workspace" ignores). Add a brief inline comment: `# Local-only planning workspace (kept on disk; not tracked since v0.x closeout)`. Commit: `git add .gitignore && git commit -m "chore: gitignore local planning workspace"`.

    Step E — Force-push to private origin:
    `git push --force --all origin`
    `git push --force --tags origin`
    (Force-push is permitted here because origin is still private per A-07; branch protection on main does NOT apply because Plan 17-12 enabled it on the new history's main — but to be safe, temporarily disable the branch-protection ruleset, force-push, then re-enable. Capture this in the runbook for future audits.)

    Step F — Replace the working repo with the rewritten state:
    Back in the original working tree (not /tmp/beatrax-purge-clone), pull the rewritten history:
    `cd ~/Development/diederik` (or wherever the working repo is)
    `git fetch --all`
    `git reset --hard origin/main`
    The `.planning/` directory remains on disk (working tree only, untracked); confirm with `ls .planning/` → still present.
    Run `git status` → should report clean working tree with `.planning/` shown as untracked (and ignored by the new .gitignore so it doesn't appear in `git status` at all).

    Step G — Validate: run `vendor/bin/pest tests/Contracts/GsdLeakageTest.php` (should still be green — runtime code doesn't reference .planning/ at all). Run `composer test` (should still be green — nothing structural changed).

    NO MODIFICATIONS to .planning/ content during this plan — it stays on disk for future local GSD work.</action>
  <verify>
    <automated>git log --all -- .planning 2>&1 | wc -l | grep -q "^[[:space:]]*0$" && git ls-tree -r HEAD 2>&1 | grep -c "\.planning/" | grep -q "^0$" && grep -q "^\.planning/" .gitignore && composer test 2>&1 | tail -3</automated>
  </verify>
  <done>git log + git ls-tree both confirm zero .planning/ references in tracked history; .gitignore contains .planning/; working tree retains .planning/ as untracked-and-ignored; composer test green.</done>
</task>

<task type="auto">
  <name>Task 2: Skill rename diederik → beatrax + CLAUDE.md update</name>
  <files>./.claude/skills/sketch-findings-beatrax/* (rename target), ~/.claude/skills/sketch-findings-beatrax/* (user-level), CLAUDE.md</files>
  <read_first>
    - `ls ./.claude/skills/` to see current state (likely partly-renamed already per git status showing staged deletions on sketch-findings-diederik/)
    - `ls ~/.claude/skills/` to see user-level state
    - CLAUDE.md (locate the "Project Skills" section that references the old name)
    - .planning/phases/17-ci-cd-pipeline-code-signing/17-CONTEXT.md § "Skill Rename (item #3)" (D-40..D-42)
  </read_first>
  <action>Step A — Project-level rename: confirm current state via `ls ./.claude/skills/`. If `sketch-findings-beatrax/` already exists (from earlier in-progress rename) + `sketch-findings-diederik/` still has staged deletions → complete the rename by committing the staged deletions + ensuring the beatrax directory has all the file content (skill markdown, references, sources, themes). If only `sketch-findings-diederik/` exists → rename via `git mv .claude/skills/sketch-findings-diederik .claude/skills/sketch-findings-beatrax`. If neither exists at project level (only user-level) → skip Step A.

    Step B — User-level rename (per D-40 step 1): `mv ~/.claude/skills/sketch-findings-diederik ~/.claude/skills/sketch-findings-beatrax` (the user-level path is outside the repo so this is a one-shot filesystem move not under git).

    Step C — SKILL.md frontmatter update (per D-42): in both renamed directories (project + user level), open the SKILL.md file and update the `name:` frontmatter field from `sketch-findings-diederik` to `sketch-findings-beatrax`. Also update any internal description text that references the old name.

    Step D — CLAUDE.md update (per D-41): find the "## Project Skills" section in CLAUDE.md. Replace every reference to `sketch-findings-diederik` with `sketch-findings-beatrax`. Update the auto-load conditions if needed (the current entry mentions Phase 16+ UI surfaces, Counterparty surfaces — these stay; just the skill name string changes).

    Step E — Verify the skill loads correctly: `claude code` (or whichever entry point loads skills) reports `sketch-findings-beatrax` as available; no errors about missing diederik skill.

    Commit: `git add .claude/skills CLAUDE.md && git commit -m "chore: rename sketch-findings skill diederik → beatrax"`. Push normally (no force; the .planning/ purge is the only force-push needed in this plan).</action>
  <verify>
    <automated>test -d .claude/skills/sketch-findings-beatrax && ! test -d .claude/skills/sketch-findings-diederik && grep -q "sketch-findings-beatrax" CLAUDE.md && ! grep -q "sketch-findings-diederik" CLAUDE.md</automated>
  </verify>
  <done>Project-level skill directory renamed; user-level skill directory renamed; SKILL.md name: frontmatter updated in both; CLAUDE.md references updated; skill loads cleanly on next Claude session.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>.planning/ purged from git history; .planning/ added to .gitignore; sketch-findings skill renamed (Tasks 1-2)</what-built>
  <how-to-verify>
    1. **Purge verification:**
       - `git log --all -- .planning` returns empty (or only the .gitignore commit if you grep for "gitignore" too)
       - `git ls-tree -r HEAD` shows zero .planning/ entries
       - `git log --oneline | head -10` shows the rewritten history starts where you expect (first commit hashes likely changed)
       - `du -sh .git/` is smaller than before the purge
    2. **Working tree retained:**
       - `ls .planning/` shows the directory still has all the phase folders + planning content (working tree only)
       - `git status` shows clean working tree (no untracked .planning/ files reported because .gitignore excludes them)
    3. **Origin force-push verified:**
       - `gh repo view nightworksio/beatrax --json defaultBranchRef --jq .defaultBranchRef.name` returns `main`
       - `gh api repos/nightworksio/beatrax/commits/main --jq .sha` returns the new rewritten-HEAD SHA
    4. **Skill rename verified:**
       - `ls .claude/skills/` shows `sketch-findings-beatrax/`, no `sketch-findings-diederik/`
       - `ls ~/.claude/skills/` same
       - CLAUDE.md "Project Skills" section references new name
       - Restart Claude Code session; confirm the skill loads under the new name
    5. **No regression:**
       - `composer test` green
       - `vendor/bin/pest tests/Contracts/GsdLeakageTest.php` green
    6. **Backup retained:** confirm `~/beatrax-pre-purge-backup-{date}` from the pre-checkpoint still exists (do NOT delete until Plan 17-15's first release succeeds)
    7. Reply with `approved` + before/after `du -sh .git/` numbers + backup path confirmed retained, OR describe what went wrong.

    If something goes wrong, the recovery path is: `cd ~/beatrax-pre-purge-backup-{date}` → push --mirror to a temporary location → re-clone from that location to restore the pre-purge state.
  </how-to-verify>
  <resume-signal>Type `approved` with .git/ size before/after + backup confirmation, OR describe what needs recovery.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local clone → origin | Force-push permanently overwrites origin's history; recovery requires the local backup (pre-checkpoint step 7) |
| .planning/ tracked → .planning/ ignored | After the purge + gitignore add, any developer who didn't pull the rewritten history will conflict on next push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-14-01 | Tampering | force-push wiping commits other contributors had not yet pulled | accept | Single-owner repo at this point; pre-checkpoint requires confirmation that collaborators have been notified |
| T-17-14-02 | Information disclosure | .planning/ content leaking to a third-party clone that exists somewhere | accept | Repo has never been pushed to a public origin (per CONTEXT.md F.); only the maintainer's local clone + the private origin contain .planning/ in history; backup keeps the pre-purge state if recovery is needed |
| T-17-14-03 | Repudiation | Skill rename desynchronizing user-level and project-level skills | mitigate | Task 2 covers both; verification checkpoint includes a Claude Code restart to confirm load |
| T-17-14-04 | Denial of service | force-push interacting badly with branch protection on main | mitigate | Plan 17-12 documented the temporary disable + re-enable pattern for force-push; .docs/runbooks/repo-security-setup.md captures it |
</threat_model>

<verification>
After both tasks + both checkpoints:

1. .planning/ purged from tracked history
2. .gitignore contains .planning/
3. Force-push to private origin succeeded
4. Working tree retains .planning/ (untracked)
5. Skill renamed in both paths
6. CLAUDE.md updated
7. composer test green
8. Backup retained on disk
</verification>

<success_criteria>
- All 7 must_haves true
- Public history is clean of .planning/
- Skill consistently named sketch-findings-beatrax everywhere
- Backup available until first release succeeds
</success_criteria>

<output>
Create `.planning/phases/17-ci-cd-pipeline-code-signing/17-14-SUMMARY.md` capturing: the actual filter-repo command output, before/after .git/ size, the backup path, and the skill-rename verification.
</output>
