<?php

declare(strict_types=1);

/*
 * Per-module Pest.php is documented inert — Pest only auto-loads the
 * project-root tests/Pest.php. The Migration TestCase + RefreshDatabase
 * wire-up lives in that root file's per-module map; this file exists to
 * match the convention shared by every other module.
 */
