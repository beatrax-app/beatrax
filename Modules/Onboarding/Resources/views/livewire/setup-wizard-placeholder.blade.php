{{--
    Placeholder body for the SetupWizard parent component before the
    backbone build replaces it with the real wizard chrome + step
    switch. Kept inside a single Livewire root <div> so the component
    boots without a "multiple roots" exception and the placeholder
    copy is recognisable in logs / browser tabs while the full
    surface lands.
--}}
<div class="wiz-page">
    <p>Setup wizard placeholder.</p>
</div>
