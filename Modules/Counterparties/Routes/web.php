<?php

declare(strict_types=1);

/*
 * Counterparties module routes — `/counterparties` index, `/counterparties/triage`,
 * `/counterparties/{slug}` profile. The UI surfaces and their Livewire
 * components land in the follow-up plan; this file ships empty so the
 * module's service provider can load it unconditionally without an
 * `is_file()` guard.
 */
